After this contact transformation, put

$$
y _ { 3 n - 2 } ^ { \prime } = y _ { 3 n - 1 } ^ { \prime } = y _ { 3 n } ^ { \prime } = x _ { 3 n - 2 } ^ { \prime } = x _ { 3 n - 1 } ^ { \prime } = x _ { 3 n } ^ { \prime } = 0 ,
$$

and drop the primes. Then,

$$
\frac { d x _ { i } } { d t } = \frac { \partial H } { \partial y _ { i } } , \frac { d y _ { i } } { d t } = - \frac { \partial H } { \partial x _ { i } } , ( i = 1 , 2 , . . . , 3 n - 3 ) ,
$$

$$
H = \sum _ { \kappa = 1 } ^ { n - 1 } \left( \frac { y _ { 3 \kappa - 2 } ^ { 2 } } { 2 m _ { 3 \kappa - 2 } ^ { \prime } } + \frac { y _ { 3 \kappa - 1 } ^ { 2 } } { 2 m _ { 3 \kappa - 1 } ^ { ' } } + \frac { y _ { 3 \kappa } ^ { 2 } } { 2 m _ { 3 \kappa } ^ { ' } } \right) - \sum _ { \mu , \nu = 1 } ^ { n } \frac { m _ { 3 \mu } m _ { 3 \nu } } { r _ { 3 \mu , 3 \nu } } ,
$$

where $\boldsymbol { r } _ { 3 { \mu } , 3 { \nu } }$ is expressed in terms of new coordinates, using (59), (67), and (68),and the primes are dropped.Jacobi and Bertrand obtained the transformation for the case $n = 3$ . It is extended to the general case $\boldsymbol { n }$ in the present discussion.

# 5.7TRANSFORMATION Y

Put

$$
\begin{array} { l } { { m _ { 3 n - 2 } ^ { \prime } = \displaystyle \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu - 2 } , \qquad m _ { i } ^ { \prime } = m _ { i } ~ ( i = 1 , 2 , . . . , 3 n - 3 ) , } } \\ { { } } \\ { { y _ { 3 n - 2 } ^ { \prime } = \displaystyle \sum _ { \mu = 1 } ^ { n - 1 } y _ { 3 \mu - 2 } = m _ { 3 n - 2 } ^ { \prime } \displaystyle \frac { d x _ { 3 n - 2 } ^ { \prime } } { d t } , } } \\ { { } } \\ { { { } \displaystyle 2 { x _ { 3 n - 2 } ^ { \prime } = \displaystyle \sum _ { n = 1 } ^ { n - 1 } m _ { 3 \mu - 2 } x _ { 3 \mu - 2 } } , } } \end{array}
$$

$$
\begin{array} { c } { { x _ { 3 \mu - 2 } ^ { \prime } = x _ { 3 \mu - 2 } - x _ { 3 n - 2 } \qquad ( \mu = 1 , 2 , . . . , n - 1 ) , } } \\ { { { } } } \\ { { y _ { i } ^ { \prime } = m _ { i } ^ { \prime } \displaystyle \frac { d x _ { i } ^ { \prime } } { d t } \qquad ( i = 1 , 2 , . . . , 3 n ) . } } \end{array}
$$

The momenta $y _ { 3 n - 1 } ^ { \prime }$ and $y _ { 3 n } ^ { \prime }$ are transformed similarly to $y _ { 3 n - 2 } ^ { \prime } ; x _ { 3 n - 1 } ^ { \prime }$ and $\pmb { x } _ { 3 n } ^ { \prime }$ similarly to $x _ { 3 n - 2 } ^ { \prime } ; x _ { 3 \mu - 1 } ^ { \prime }$ and $\pmb { x _ { 3 \mu } ^ { \prime } }$ similarly to $x _ { 3 \mu - 2 } ^ { \prime }$ .The sum of the masses of the system is $m _ { 3 n - 2 } ^ { \prime } = m _ { 3 n - 1 } ^ { \prime } = m _ { 3 n } ^ { \prime }$ ； the coordinates of the center of mass are $x _ { 3 n - 2 } ^ { \prime } , x _ { 3 n - 1 } ^ { \prime } , x _ { 3 n } ^ { \prime }$ .The coordinates of the $\mu \mathrm { t h }$ body relative to the nth body are $x _ { 3 \mu - 2 } ^ { \prime } , \ x _ { 3 \mu - 1 } ^ { \prime } , \ x _ { 3 \mu } ^ { \prime }$ .The momentum components of the $\mu \mathrm { t h }$ body relative to the nth body are $y _ { 3 \mu - 2 } ^ { \prime } , y _ { 3 \mu - 1 } ^ { \prime } , y _ { 3 \mu } ^ { \prime } .$ This transformation $\gamma$ does not satisfy conditions (48) or (49).Hence, the transformed equations are not canonical. Accordingly we cannot eliminate the nodes by this transformation.

We fix the center of mass, transfer the coordinate origin to the center of mass, and put $x _ { 3 n - 2 } ^ { \prime } = x _ { 3 n - 1 } ^ { \prime } = x _ { 3 n } ^ { \prime } = 0$ ， $y _ { 3 n - 2 } ^ { \prime } = y _ { 3 n - 1 } ^ { \prime } = y _ { 3 n } ^ { \prime } = 0$ The transformed equations, after the primes are dropped, are

$$
\underline { { \underline { { \ { \ - 2 } } } } } = \frac { \partial H _ { \mu } } { \partial y _ { 3 \mu - 2 } } , \qquad \frac { d x _ { 3 \mu - 1 } } { d t } = \frac { \partial H _ { \mu } } { \partial y _ { 3 \mu - 1 } } , \qquad \frac { d x _ { 3 \mu } } { d t } = \frac { \partial H _ { \mu } } { \partial y _ { 3 \mu } } ,
$$

$$
\frac { d y _ { 3 \mu - 2 } } { d t } = \triangledown - \frac { \partial H _ { \mu } } { \partial x _ { 3 \mu - 2 } } , \frac { d y _ { 3 \mu - 1 } } { d t } = \triangledown - \frac { \partial H _ { \mu } } { \partial x _ { 3 \mu - 1 } } , \frac { d y _ { 3 \mu } } { d t } = \triangledown - \frac { \partial H _ { \mu } } { \partial x _ { 3 \mu } } ,
$$

$$
\begin{array} { c }  { H _ { \mu } = { \displaystyle { \frac { 1 } { 2 } } \left( \frac { y _ { 3 \mu - 2 } ^ { 2 } } { m _ { 3 \mu - 2 } } + \frac { y _ { 3 \mu - 1 } ^ { 2 } } { m _ { 3 \mu - 1 } } + \frac { y _ { 3 \mu } ^ { 2 } } { m _ { 3 \mu } } \right) \nonumber - \displaystyle { \frac { m _ { 3 \mu } m _ { 3 n } } { r _ { 3 \mu , 3 n } } - \sum _ { i = 1 } ^ { n - 1 } \frac { m _ { 3 \mu } m _ { 3 \nu } } { r _ { 3 \mu , 3 \nu } } } } } \\ { { + \displaystyle { \sum _ { \nu = 1 \atop ( \mu \neq \nu ) } ^ { n - 1 } \frac { m _ { 3 \mu } m _ { 3 \nu } } { r _ { 3 \mu , 3 \nu } ^ { 3 } } \left( x _ { 3 \mu - 2 } x _ { 3 \nu - 2 } + x _ { 3 \mu - 1 } x _ { 3 \nu - 1 } + x _ { 3 \mu } x _ { 3 \nu } \right) } } } \\ { { \nonumber } } \\ { { ( \mu = 1 , 2 , . . . , n - 1 ) . } } \end{array}
$$

These are the usual equations of motion in the perturbation theory. The Hamiltonian $H$ differs only by the last term, that is,

$$
H _ { \mu } = T - U + \sum _ { \stackrel { \nu = 1 } { ( \mu \neq \nu ) } } ^ { n - 1 } \frac { m _ { 3 \mu } m _ { 3 \nu } } { r _ { 3 \mu , 3 \nu } ^ { 3 } } ( x _ { 3 \mu - 2 } x _ { 3 \nu - 2 } + x _ { 3 \mu - 1 } x _ { 3 \nu - 1 } + x _ { 3 \mu } x _ { 3 \nu } ) ,
$$

where

$$
T = \frac { 1 } { 2 } \sum _ { i = 1 \atop i = 1 } ^ { 3 n - 3 } \frac { 1 } { m _ { i } } y _ { i } ^ { 2 } , \qquad U = et { } { ' } \sum _ { \stackrel { \mu , v } { ( \mu \neq v ) } } \frac { m _ { 3 \mu } m _ { 3 v } } { r _ { 3 \mu , 3 v } } .
$$

The perturbation of the vth body due to presence of the $\mu \mathrm { t h }$ body is

$$
\frac { m _ { 3 v } } { r _ { 3 \mu , 3 v } } - \frac { m _ { 3 v } } { r _ { 3 \mu , 3 v } ^ { 3 } } ( x _ { 3 \mu - 2 } x _ { 3 v - 2 } + x _ { 3 \mu - 1 } x _ { 3 v - 1 } + x _ { 3 \mu } x _ { 3 v } ) ,
$$

and is called the disturbing function or the perturbative function. The first term is called the direct part and the second the indirect part.Such a situation occurs in the case when the mass of the nth body is very large, compared with the masses of the remaining $n - 1$ bodies. The motion of the $\mu \mathrm { t h }$ body around the nth body is disturbed by the perturbative action of the remaining $n - 2$ bodies.The perturbation is included in the perturbative function and is multiplied by the disturbing mass $m _ { 3 v }$ ·

# 5.8REDUCTION FROM $6 n - 6 \mathtt { T o } 6 n - 1 0$

Using the angular momentum integrals and the elimination of the nodes, we reduce the order of the differential equations (47) from $6 n - 6$ to $6 n - 1 0$ (Whittaker,1937; Bennett, 1905).

To (56) we apply the contact transformation

$$
x _ { i } = \frac { \partial W } { \partial y _ { i } } , \qquad y _ { i } ^ { \prime } = \frac { \partial W } { \partial x _ { i } ^ { \prime } } \qquad ( i = 1 , 2 , . . . , 3 n - 3 ) ,
$$

$$
\begin{array} { r l } { \displaystyle } & { \quad \times \sum _ { j = 1 } ^ { N } \log _ { j } \log _ { k = 1 } \exp _ { \frac { j } { k } \to 0 } \exp _ { \frac { j } { k } \to 0 } - \frac { 1 } { N } \exp _ { \frac { j } { k } \to 0 } \exp _ { \frac { j } { k } \to 0 } \exp _ { \frac { j } { k } \to 0 } } \\ & { \qquad + ( \sum _ { j = 1 } ^ { N } \log _ { j \to \infty } \sin \big ( \sin ( \theta _ { j } \cdot \cos \theta _ { j } ) \big )  } \\ & { \qquad \quad + ( \sum _ { j = 1 } ^ { N } \sin ( \theta _ { j } \cdot \cos \theta _ { j } ) \cos \big ( \sin \theta _ { j } \cdot \cos \theta _ { j } \big )   } \\ & { \qquad  \quad -  \nabla _ { \frac { j } { k } \to 0 } \sin ( \theta _ { j } \cdot \cos \theta _ { j } ) )   } \\ & { \qquad  \quad - ( \sin ( \theta _ { j } \cdot \cos \theta _ { j } ) \cos \theta _ { j } \cdot \sin \theta _ { j } )   } \\ & { \qquad  \quad + \frac { 1 } { N } \sin ( \theta _ { j } \cdot \cos \theta _ { j } ) \sin \theta _ { j }   } \\ & { \quad +   \frac { 1 } { N } \sin ( \theta _ { j } \cdot \cos \theta _ { j } ) \cos \theta _ { j }   \sin \theta _ { j }  } \\ & { \quad   + \frac { 1 } { N } \sin ( \theta _ { j } \cdot \cos \theta _ { j } ) \cos \theta _ { j }   \sin \theta _ { j }  } \\ & { \quad   + \frac { 1 } { N } \sin ( \theta _ { j } \cdot \cos \theta _ { j } ) \sin \theta _ { j }   + \frac { 1 } { N } \cos ( \theta _ { j } \cdot \sin \theta _ { j } ) \sin \theta _ { j }  \Biggr . } \\ & { \quad   + \frac { 1 } { N } \sin ( \theta _ { j } \cdot \cos \theta _ { j } )   } \\ & { \quad   + \frac { 1 } { N } \sin ( \theta _ { j } \cdot \cos \theta _ { j } ) \sin ( \theta _ { j } \cdot \cos \theta _ { j } )   } \\ &  \quad   + \frac { 1 } { N } \sin (  \end{array}
$$

This is the coordinate transformation from OXYZ to $O X ^ { \prime } Y ^ { \prime } Z ^ { \prime }$ with the nth body as the origin, the $X ^ { \prime } Y ^ { \prime }$ plane containing the $( n \mathrm { ~ - ~ } 1 ) t \mathbf { h }$ and the $( n \mathrm { ~ - ~ } 2 ) \operatorname { t h }$ bodies； the direction cosines are $l _ { 1 }$ ， $m _ { 1 } , n _ { 1 }$ ， etc., as shown in Table 1. $O X ^ { \prime }$ is in the direction of the intersection of the $X Y$ plane and the $X ^ { \prime } Y ^ { \prime }$ plane. $\pmb { x } _ { 3 \nu - 2 } ^ { \prime }$ $x _ { 3 \nu - 1 } ^ { \prime }$ ， $\pmb { x } _ { 3 \nu } ^ { \prime }$ $( \nu = 1 , 2 , . . . , n - 3 )$ ， are the coordinates of the vth body; $x _ { 3 n - 8 } ^ { \prime } , \ x _ { 3 n - 7 } ^ { \prime } , \ 0$ are the coordinates of the $( n \mathrm { ~ - ~ } 2 ) \operatorname { t h }$ body;and ${ x _ { 3 n - 6 } ^ { \prime } } , { x _ { 3 n - 5 } ^ { \prime } } , 0$ are those of the $( n \mathrm { ~ - ~ } 1 ) { \operatorname { t h } }$ body;

TABLE 1   

<table><tr><td>0x</td><td>OY</td><td>0z</td></tr><tr><td>0X&#x27; l1 = cos x3n-4,</td><td>m1 = sin xsn -4)</td><td>𝑛1= 0,</td></tr><tr><td>OY&#x27; l2 = -sin xsn-4 Cos xsn-3, m2 = COs xsn-4 COs xsn-3) OZ&#x27; l = sin xn-4sin x3n-3,  mg = -COs xsn-4sin xsn-3, n3 = Cos x3n-3</td><td></td><td>n2 = sin x3n-3</td></tr></table>

all referred to the $O X ^ { \prime } Y ^ { \prime } Z ^ { \prime }$ axes. $x _ { 3 n - 4 } ^ { \prime }$ is the angle between $O X$ and $O X ^ { \prime }$ ,and $9 0 ^ { \circ } - x _ { 3 n - 3 } ^ { \prime }$ is the angle between $O Y ^ { \prime }$ and 02 $\therefore y _ { 3 \nu - 2 } ^ { \prime } , y _ { 3 \nu - 1 } ^ { \prime } , y _ { 3 \nu } ^ { \prime }$ $( \nu = 1 , 2 , . . . , n - 3 )$ , are the components of the momentum of the vth body; $y _ { 3 n - 8 } ^ { \prime } , y _ { 3 n - 7 } ^ { \prime }$ ,0 are those of the $( n \mathrm { ~ - ~ } 2 ) t h$ body; and $y _ { 3 n - 6 } ^ { \prime } , y _ { 3 n - 5 } ^ { \prime } , \mathcal { ( }$ are those of the $( n \mathrm { ~ - ~ } 1 ) { \operatorname { t h } }$ body in the directions $O X ^ { \prime } , O Y ^ { \prime }$ and $o z ^ { \prime }$ ， respectively. Since

$$
\begin{array} { l } { \displaystyle { y _ { 3 n - 4 } ^ { \prime } = \sum _ { \nu = 1 } ^ { n - 3 } \left( - y _ { 3 \nu - 2 } x _ { 3 \nu - 1 } + y _ { 3 \nu - 1 } x _ { 3 \nu - 2 } \right) } } \\ { \displaystyle { \qquad + \ ( - y _ { 3 n - 8 } x _ { 3 n - 7 } + y _ { 3 n - 7 } x _ { 3 n - 8 } ) } } \\ { \displaystyle { \qquad + \ ( - y _ { 3 n - 6 } x _ { 3 n - 5 } + y _ { 3 n - 5 } x _ { 3 n - 6 } ) , } } \end{array}
$$

therefore, $- y _ { 3 n - 4 } ^ { \prime }$ is the total angular momentum around $O Z$ Since

$$
\begin{array} { c } { { y _ { 3 n - 3 } ^ { \prime } = \displaystyle \sum _ { \nu = 1 } ^ { n - 3 } [ x _ { 3 \nu - 1 } ^ { \prime } ( l _ { 3 } y _ { 3 \nu - 2 } + m _ { 3 } y _ { 3 \nu - 1 } + n _ { 3 } y _ { 3 \nu } )  } } \\ { { \mathrm { } } } \\ { { \displaystyle \qquad - x _ { 3 \nu } ^ { \prime } ( m _ { 3 } y _ { 3 \nu - 2 } + m _ { 2 } y _ { 3 \nu - 1 } + m _ { 1 } y _ { 3 \nu } ) ] } } \\ { { \displaystyle \qquad + x _ { 3 n - 7 } ^ { \prime } ( l _ { 3 } y _ { 3 n - 8 } + m _ { 3 } y _ { 3 n - 7 } + n _ { 3 } y _ { 3 n - 6 } ) } } \\ { { \displaystyle \qquad + x _ { 3 n - 5 } ^ { \prime } ( l _ { 3 } y _ { 3 n - 5 } + m _ { 3 } y _ { 3 n - 4 } + n _ { 3 } y _ { 3 n - 3 } ) , } } \end{array}
$$

therefore $y _ { 3 n - 3 } ^ { \prime }$ is the total angular momentum around $O X ^ { \prime }$

Denote the momentum in the direction $o z ^ { \prime }$ of the $( n \mathrm { ~ - ~ } 2 ) \operatorname { t h }$ and the $( n \mathrm { ~ - ~ } 1 )  \mathbf { t h }$ bodies by $\Gamma$ and $\Gamma ^ { \prime }$ , respectively, and drop the primes of the new $_ x$ and $y$ variables.Then

$$
y _ { 3 n - 3 } = \sum _ { \nu = 1 } ^ { n - 3 } { \left| \begin{array} { l l } { x _ { 3 \nu - 1 } } & { x _ { 3 \nu } } \\ { y _ { 3 \nu - 1 } } & { y _ { 3 \nu } } \end{array} \right| } + { \left| \begin{array} { l l } { x _ { 3 n - 7 } } & { 0 } \\ { y _ { 3 n - 7 } } & { \Gamma } \end{array} \right| } + { \left| \begin{array} { l l } { x _ { 3 n - 5 } } & { 0 } \\ { y _ { 3 n - 5 } } & { \Gamma ^ { \prime } } \end{array} \right| } ,
$$

$$
- y _ { 3 n - 4 } = n _ { 3 } \Bigl \langle \sum _ { \nu = 1 } ^ { n - 3 } \left| { x _ { 3 \nu - 2 } \atop y _ { 3 \nu - 2 } } \right. \quad { x _ { 3 \nu - 1 } } \ + \ \left| { x _ { 3 n - 8 } \atop y _ { 3 n - 8 } } \ { x _ { 3 n - 7 } } \right| + \ \left| { x _ { 3 n - 6 } \atop y _ { 3 n - 6 } } \ { x _ { 3 n - 5 } } \right|
$$

$$
+  n _ { 2 } \Biggl \{ \sum _ { \nu = 1 } ^ { n - 3 } | \begin{array} { c c } { { x _ { 3 \nu } } } & { { x _ { 3 \nu - 2 } } } \\ { { y _ { 3 \nu } } } & { { y _ { 3 \nu - 2 } } } \end{array} | + | \begin{array} { c c } { { 0 } } & { { x _ { 3 n - 8 } } } \\ { { \Gamma } } & { { y _ { 3 n - 8 } } } \end{array} | + | \begin{array} { c c } { { 0 } } & { { x _ { 3 n - 6 } } } \\ { { \Gamma ^ { \prime } } } & { { y _ { 3 n - 6 } } } \end{array} | \Biggr \} .
$$

Thus $\Gamma$ and $\Gamma ^ { \prime }$ are given by the two linear equations:

$$
\Gamma x _ { 3 n - 7 } + \Gamma ^ { \prime } x _ { 3 n - 5 } = y _ { 3 n - 3 } - \sum _ { \nu = 1 } ^ { n - 3 } \bigg | \begin{array} { l l } { { x _ { 3 \nu - 1 } } } & { { x _ { 3 \nu } } } \\ { { y _ { 3 \nu - 1 } } } & { { y _ { 3 \nu } } } \end{array} \bigg | ,
$$

$$
\Gamma x _ { 3 n - 8 } + \Gamma ^ { \prime } x _ { 3 n - 6 } = y _ { 3 n - 4 } \ \mathrm { c o s e c } \ x _ { 3 n - 3 } + \sum _ { \nu = 1 } ^ { n - 3 } \left| { x _ { 3 \nu } } \quad { x _ { 3 \nu - 2 } } \right|
$$

$$
\begin{array} { r l } { { } + \cot x _ { 3 n - 3 } \bigg \{ \displaystyle \sum _ { \nu = 1 } ^ { n - 3 } \bigg | \displaystyle x _ { 3 \nu - 2 } } & { { } x _ { 3 \nu - 1 } \bigg | + \bigg | \displaystyle x _ { 3 n - 8 } } & { { } x _ { 3 n - 7 } \bigg | } \\ { { } } & { { } \qquad \quad + \mathrm { ~ c o t ~ } x _ { 3 n - 3 } \bigg \{ \displaystyle \sum _ { \nu = 1 } ^ { n - 3 } \bigg | \displaystyle x _ { 3 \nu - 2 } } & { { } y _ { 3 \nu - 1 } \bigg | + \bigg | \displaystyle y _ { 3 n - 8 } } & { { } y _ { 3 n - 7 } \bigg | } \\ { { } } & { { } \qquad \quad + \mathrm { ~ } \bigg | \displaystyle x _ { 3 n - 6 } } & { { } x _ { 3 n - 5 } \bigg | \bigg \} . }  \end{array}
$$

These equations do not depend on $x _ { 3 n - 4 }$ . Thus, the Hamiltonian in the new variables is

$$
H = \sum _ { i = 1 } ^ { 3 n - 9 } \frac { y _ { i } ^ { 2 } } { 2 m _ { i } } + \frac { 1 } { 2 m _ { 3 n - 6 } } ( y _ { 3 n - 8 } ^ { 2 } + y _ { 3 n - 7 } ^ { 2 } + \Gamma ^ { 2 } )
$$

$$
\begin{array} { r l } { \displaystyle } & { + \frac { 1 } { 2 m _ { 3 n - 3 } } ( g _ { 9 n - 6 } ^ { n } + y _ { 9 n - 5 } ^ { 2 } + \Gamma ^ { n } ) } \\ & { + \frac { 1 } { 2 m _ { 3 n } } \displaystyle \left[ \left( \sum _ { \gamma = 1 } ^ { n - 3 } y _ { 3 n - 6 } + y _ { 9 n - 6 } + y _ { 9 n - 6 } \right) ^ { 2 } \right. } \\ & { \qquad + \left. \left( \sum _ { \nu = 1 } ^ { n - 3 } y _ { 9 n - 1 } + y _ { 3 n - 7 } + y _ { 3 n - 5 } \right) ^ { 2 } \right. } \\ & { \qquad + \left. \left( \sum _ { \gamma = 1 } ^ { n - 3 } y _ { 3 n } + \Gamma + \Gamma \right) ^ { 2 } \right] - \sum _ { \nu = 1 } ^ { n } \frac { n } { \gamma _ { 3 n } m _ { 3 n } } , } \end{array}
$$

where $r _ { 3 \mu , 3 \nu }$ is expressed in terms of $x _ { 1 } , x _ { 2 } , . . . , x _ { 3 n - 5 } ,$ As this expression for $\boldsymbol { H }$ does not depend on $x _ { 3 n - 4 }$ in the canonical equations

$$
\frac { d x _ { i } } { d t } = \frac { \partial H } { \partial y _ { i } } , \frac { d y _ { i } } { d t } = - \frac { \partial H } { \partial x _ { i } } ( i = 1 , 2 , . . . , 3 n - 3 ) ,
$$

the canonical equations have the integral

$$
y _ { 3 n - 4 } = K = \tt c o n s t a n t .
$$

$x _ { 3 n - 4 }$ appears only in $d x _ { 3 n - 4 } / d t = \partial H / \partial y _ { 3 n - 4 }$ .Hence, there is no need to consider $x _ { 3 n - 4 }$ in our discussion.We obtain $x _ { 3 n - 4 }$ by mere quadrature in the last stage. Thus we come to the differential equations of order $6 n - 8$ ：

$$
\frac { d x _ { i } } { d t } = \frac { \partial H } { \partial y _ { i } } , \frac { d y _ { i } } { d t } = - \frac { \partial H } { \partial x _ { i } } \qquad ( i = 1 , 2 , \ldots , 3 n - 5 , 3 n - 3 ) ,
$$

where we have put $\kappa$ instead of $y _ { 3 n - 4 }$

Now we choose the $o z$ axis parallel to the resultant angular momentum of the $n$ -body system,so that the $X Y$ plane is now the invariable plane.There are two integrals for the angular momentum; that is, the angular momentum around $O X ^ { \prime }$ is zero and the angular momentum around $O Z ^ { \prime }$ is equal to $- \kappa$ cos $x _ { 3 n - 3 }$ ; that is,

$$
\begin{array} { c } { { y _ { 3 n - 3 } = 0 , } } \\ { { \ } } \\ { { \displaystyle - K \cos x _ { 3 n - 3 } = \sum _ { \nu = 1 } ^ { n - 3 } \left| \begin{array} { l l l } { { \displaystyle x _ { 3 \nu - 2 } } } & { { \displaystyle x _ { 3 \nu - 1 } } } \\ { { \displaystyle y _ { 3 \nu - 2 } } } & { { \displaystyle y _ { 3 \nu - 1 } } } \end{array} \right| + \left| \begin{array} { l l l } { { \displaystyle x _ { 3 n - 8 } } } & { { \displaystyle \hat { x } _ { 3 n - 7 } } } \\ { { \displaystyle y _ { 3 n - 8 } } } & { { \displaystyle y _ { 3 n - 7 } } } \end{array} \right| } } \\ { { \ \displaystyle ~ + \left| \begin{array} { l l l } { { \displaystyle x _ { 3 n - 6 } } } & { { \displaystyle x _ { 3 n - 5 } } } \\ { { \displaystyle y _ { 3 n - 6 } } } & { { \displaystyle y _ { 3 n - 5 } } } \end{array} \right| . } } \end{array}
$$

Let $H ^ { \prime }$ be the result of substituting these values of $x _ { 3 n - 3 }$ and ${ y _ { 3 n - 3 } }$ in $\pmb { H }$ and let one of these variables be $\pmb { S } ,$ Then,

$$
\frac { \partial H ^ { \prime } } { \partial s } = \frac { \partial H } { \partial s } + \frac { \partial H } { \partial x _ { 3 n - 3 } } \frac { \partial x _ { 3 n - 3 } } { \partial s } .
$$

But, since $y _ { 3 n - 3 } = 0$ and $\partial H / \partial x _ { 3 n - 3 } + d y _ { 3 n - 3 } / d t = 0 _ { \mathrm { ; } }$ ，therefore

$$
\frac { \partial H ^ { \prime } } { \partial s } = \frac { \partial H } { \partial s } \cdot
$$

The reduced differential equations are now

$$
\frac { d x _ { i } } { d t } = \frac { \partial H ^ { \prime } } { \partial y _ { i } } , \frac { d y _ { i } } { d t } = - \frac { \partial H ^ { \prime } } { \partial x _ { i } } ~ ( i = 1 , 2 , . . . , 3 n - 5 ) ,
$$

$$
\begin{array} { r l } & { \quad \frac { \partial ^ { 2 } \eta _ { 1 } } { \partial t } = } \\ & { = \frac { 1 } { 2 } \frac { 1 } { \partial q _ { 2 } } [ ( \displaystyle \sum _ { i = 1 } ^ { 3 } \frac { \partial } { \partial { x } _ { i } } + \frac { \partial } { \partial { x } _ { i } } \ y _ { i + 1 } \ y _ { i + 2 } ) ^ { 2 }  } \\ & { \qquad + \displaystyle \sum _ { i = 1 } ^ { 3 } \frac { \partial } { \partial { x } _ { i } } \ y _ { i - 1 } + \frac { \partial } { \partial { x } _ { i } } \ y _ { i - 3 } \ y _ { i } + \displaystyle \sum _ { i = 1 } ^ { 3 } ( \displaystyle \sum _ { i = 1 } ^ { 3 } \frac { \partial } { \partial { x } _ { i } } - \Gamma + \Gamma ) ^ { 2 } ] } \\ & { = \frac { 1 } { \sqrt { 3 } } \displaystyle \sum _ { i = 1 } ^ { 3 } \frac { \partial } { \partial q _ { 2 } \partial q _ { 3 } } [ ( \partial _ { i } \eta _ { 1 } - 2 - x _ { 1 } , \ldots , 2 ) ^ { 2 }  } \\ & { \qquad +  ( \partial _ { i \Omega _ { \Delta \omega - 1 } } - x _ { 2 } , \ldots , \ldots , ) ^ { 2 }  +   ( \partial _ { i \Omega _ { \Delta \omega - 1 } } - x _ { 3 } , \ldots , \ldots , ) ^ { 3 } ] ^ { 3 - 1 } } \\ & { \qquad -  \partial _ { \Omega \omega - 1 } \displaystyle \sum _ { i = 1 } ^ { 3 } \rho _ { 0 } [ ( x _ { i } - x _ { i } , \ldots , x _ { i } , \ldots , x _ { i } , \ldots , x _ { i } , 0 ) ^ { 2 } + ( \partial _ { i } \eta _ { 1 } - \delta _ { i } , \ldots , x _ { i } , 0 ) ^ { 2 } + ( \partial _ { i } \eta _ { 1 } - \delta _ { i } , \ldots , x _ { i } , \ldots , ) ^ { 2 } ] } \\ &  \qquad \quad - \Pi _ { \omega - 1 } \displaystyle \sum _ { i = 1 } ^ { 3 } \frac { \partial } { \partial x _ { i } } [ ( \partial _ { i } \eta _ { i } - 2 - x _ { i } , \ldots , x _ { i } , 0 ) ^ { 2 } + ( \partial _ { i } \eta _ { 1 } - 2 - x _  \end{array}
$$

By (79), the expressions for $\Gamma$ and $\Gamma ^ { \prime }$ in the new variables become

$$
\begin{array} { r l } { \Gamma x _ { 9 n - 7 } + \Gamma ^ { \prime } x _ { 9 n - 5 } = } & { - \displaystyle \sum _ { i = 1 } ^ { n - 3 } \left| { x _ { 9 n - 1 } \atop | y _ { 9 n - 1 } } \right. \ } \\ { \mathrm { T x _ { 9 n - 9 } ~ + ~ \Gamma ^ { \prime } x _ { 9 n - 6 } ~ = ~  { K \operatorname { c o s c } _ { < 0 n - 3 } + \displaystyle \sum _ { i = 1 } ^ { n - 3 } \left| { x _ { 3 n } \atop y _ { 9 n - 9 } } \right| \ll _ { 9 n - 2 } \left| \right|} , } } \\ { \displaystyle } & { \displaystyle = \sum _ { i = 1 } ^ { n - 3 } \left| { x _ { 9 n } \atop y _ { 9 n } } \right. \ \ \cdot \ { x _ { 9 n - 2 } }  } \\ &  \displaystyle \phantom { + \ \sum _ { i = 1 } ^ { n - 3 } \left| { x _ { 9 n - 1 } \atop y _ { 9 n - 1 } } \right| \ } + \left. { K ^ { 2 } - \left[ \displaystyle \sum _ { i = 1 } ^ { n - 3 } \left| { x _ { 9 n - 2 } \atop y _ { 9 n - 2 } } \right. \ \right. \right. } \\ & { \displaystyle \phantom { + \ \sum _ { i = 1 } ^ { n - 3 } \left| { x _ { 9 n - 6 } \atop y _ { 9 n - 6 } } \right.} + \left| { x _ { 9 n - 6 } \atop y _ { 9 n - 6 } } \right. \ \left. \right. \left. \right| + \left| { x _ { 3 n - 8 } \atop y _ { 9 n - 5 } } \right. \ \  } \\ & { \displaystyle \phantom { + \sum _ { i = 1 } ^ { n - 3 } \left| { x _ { 9 n - 6 } \atop y _ { 9 n - 6 } } \right.} + \left| \right. _ { 9 n - 6 }  \ \left. \ { x _ { 9 n - 5 } } \right| \Bigg \} } \end{array}
$$

Thus the order of the equations of motion has been reduced to $6 n - 1 0$

The next step is to reduce the order to $6 n - 1 2$ , by using the energy integral and the elimination of time.This is the final stage of the reduction — using all the known first integrals - as has been shown in Section 4.10.

# ELLIPTIC MOTIONS

# 5.9MOTION IN CONIC SECTIONS

Consider an $_ { n }$ -body problem where one of the $_ { n }$ bodies has a mass much greater than the mass of any one of the other bodies. We discuss two cases.First, we investigate the motion of one of the bodies of smaller mass around the body of larger mass; the effects of the remaining bodies can then be regarded as small perturbations superposed on the motion in the two-body problem. Second,we will inquire into the case where the distance between two of the $\pmb { n }$ bodies is very small compared with their distances from the other bodies;the motion of these two can then be treated as a two-body problem,and the effect of the other bodies is considered a small superposed perturbation.The first case corresponds to the motion of the planets in the solar system; the second case to the motion ofa satellite (including the moon and artificial earth satellites) around the planet to which the satellite belongs.We start with the rigorous solution of the two-body problem with two finite masses. The solution of the two-body problem is a motion in a conic section, that is,a Keplerian motion along a plane quadratic curve. It must be admitted that Keplerian motion is often not close enough even to serve as a first approximation to the actual complicated motions observed by modern techniques; so that we usually consider a more elaborate motion, as the first approximation. Nevertheless, here we shall begin by considering Keplerian motion, as it has long been the traditional first approximation in elementary theory.

Suppose that a mass $m _ { 1 }$ is at the coordinate origin. The kinetic energy and the force function for the motion of a mass $m _ { 2 }$ around the mass $m _ { 1 }$ expressed in polar coordinates are

$$
T = { \frac { m } { 2 } } \left[ \left( { \frac { d r } { d t } } \right) ^ { 2 } + r ^ { 2 } \Bigl ( { \frac { d \phi } { d t } } \Bigr ) ^ { 2 } + r ^ { 2 } \cos ^ { 2 } \phi \Bigl ( { \frac { d \theta } { d t } } \Bigr ) ^ { 2 } \right] ,
$$

$$
U = \frac { \beta } { r } , \qquad \beta = k ^ { 2 } m , \qquad m = m _ { 1 } + m _ { 2 } .
$$

The corresponding momenta are

$$
\frac { \partial T } { \partial \dot { r } } = m \frac { d r } { d t } , \qquad \dot { p } _ { \phi } = \frac { \partial T } { \partial \dot { \phi } } = m r ^ { 2 } \Big ( \frac { d \phi } { d t } \Big ) , \qquad \dot { p } _ { \theta } = \frac { \partial T } { \partial \dot { \theta } } = m r ^ { 2 } \cos ^ { 2 } \phi \frac { d \theta } { d t } .
$$

Hence, if we write $m \beta$ and $m h _ { 1 }$ respectively for $\beta$ and $h _ { 1 }$ ,the Hamilton-Jacobi partial differential equation becomes

$$
\left( { \frac { \partial W } { \partial r } } \right) ^ { 2 } + { \frac { 1 } { r ^ { 2 } } } \left( { \frac { \partial W } { \partial \phi } } \right) ^ { 2 } + { \frac { 1 } { r ^ { 2 } \cos ^ { 2 } \phi } } \left( { \frac { \partial W } { \partial \theta } } \right) ^ { 2 } = { \frac { 2 \beta } { r } } + 2 h _ { 1 } .
$$

By separation of variables (Section 2.5) we obtain

$$
r ^ { 2 } \Big [ \Big ( \frac { \partial W } { \partial r } \Big ) ^ { 2 } - \frac { 2 \beta } { r } - 2 h _ { 1 } \Big ] = - \Big ( \frac { \partial W } { \partial \phi } \Big ) ^ { 2 } - \frac { 1 } { \cos ^ { 2 } \phi } \Big ( \frac { \partial W } { \partial \theta } \Big ) ^ { 2 } .
$$

As the left-hand member depends only on $r$ while the right-hand member does not depend on $r _ { : }$ ，both members must equal a constant in order to be equal. Let the constant be $- h _ { 2 } ^ { 2 }$ ; then,

$$
\cos ^ { 2 } \phi \Big [ \Big ( \frac { \partial W } { \partial \phi } \Big ) ^ { 2 } - h _ { 2 } ^ { 2 } \Big ] = - \Big ( \frac { \partial W } { \partial \theta } \Big ) ^ { 2 } ,
$$

$$
\left( \frac { \partial W } { \partial r } \right) ^ { 2 } = \frac { 2 \beta } { r } + 2 h _ { 1 } - \frac { h _ { 2 } ^ { 2 } } { r ^ { 2 } } .
$$

Similarly,by equating both sides of the equation in $\phi$ and $\theta$ to a constant $- h _ { 3 } ^ { 2 }$ ，we obtain

$$
\left( \frac { \partial W } { \partial \phi } \right) ^ { 2 } = h _ { 2 } ^ { 2 } - \frac { h _ { 3 } ^ { 2 } } { \cos ^ { 2 } \phi } , \qquad \left( \frac { \partial W } { \partial \theta } \right) ^ { 2 } = h _ { 3 } ^ { 2 } .
$$

Hence, by the method treated in Section 1.12,we obtain

$$
\begin{array} { l } { d t = \displaystyle \frac { d r } { \sqrt { \displaystyle \frac { 2 \beta } { r } + 2 h _ { 1 } - \frac { h _ { 2 } ^ { 2 } } { r ^ { 2 } } } } } \\ { 0 = h _ { 2 } \displaystyle \frac { d \phi } { \sqrt { h _ { 2 } ^ { 2 } - \frac { h _ { 3 } ^ { 2 } } { \cos ^ { 2 } \phi } } } - h _ { 2 } \displaystyle \frac { d r } { r ^ { 2 } \sqrt { \displaystyle \frac { 2 \beta } { r } + 2 h _ { 1 } - \frac { h _ { 2 } ^ { 2 } } { r ^ { 2 } } } } } \\ { 0 = d \theta - h _ { 3 } \displaystyle \frac { d \phi } { \cos ^ { 2 } \phi \displaystyle \sqrt { h _ { 2 } ^ { 2 } - \frac { h _ { 3 } ^ { 2 } } { \cos ^ { 2 } \phi } } } , } \end{array}
$$

and

$$
W = \int _ { r _ { 0 } } ^ { r } { \sqrt { { \frac { 2 \beta } { r } } + 2 h _ { 1 } - { \frac { h _ { 2 } ^ { 2 } } { r ^ { 2 } } } } } d r + \int _ { 0 } ^ { \theta } h _ { 3 } d \theta + \int _ { 0 } ^ { \phi } { \sqrt { h _ { 2 } ^ { 2 } - { \frac { h _ { 3 } ^ { 2 } } { \cos ^ { 2 } \phi } } } } d \phi .
$$

Put

$$
\begin{array} { l } { { \displaystyle \frac { 1 } { r } = \rho , \qquad U ( \rho ) = 2 h _ { 1 } + 2 \beta \rho - h _ { 2 } ^ { 2 } \rho ^ { 2 } , } } \\ { { \displaystyle h _ { 3 } = h _ { 2 } \cos I , \qquad \sin \phi = \sin I \sin \psi ; } } \end{array}
$$

then, with the integration constants $\gamma _ { 1 } , \gamma _ { 2 } , \gamma _ { 3 }$ ，equations (84) take the form

$$
\begin{array} { c } { { \gamma _ { 1 } + t = - \displaystyle \int \frac { d \rho } { \rho ^ { 2 } \sqrt { U ( \rho ) } } , } } \\ { { { } } } \\ { { \gamma _ { 2 } = \psi + h _ { 2 } \displaystyle \int \frac { d \rho } { \sqrt { U ( \rho ) } } , } } \\ { { { } } } \\ { { \gamma _ { 3 } = \theta - \tan ^ { - 1 } { \left( { \tan \psi \cos { I } } \right) } . } } \end{array}
$$

From the first and the third equations of (84a),we obtain the law of conservation of angular momentum, or the Keplerian second law

$$
r ^ { 2 } \frac { d \psi } { d t } = h _ { 2 } ,
$$

where $h _ { 2 }$ is twice the areal velocity. The third equation of (84a) gives

$$
\tan \psi \cos I = \tan ( \theta - \gamma _ { 3 } ) .
$$

If we take the ecliptic as our reference plane, and the vernal equinox $\boldsymbol { \Upsilon }$ of an epoch as the standard direction- that is, if we take a certain fixed epoch as reference - then $\theta$ is the longitude, $\phi$ is the latitude,and (85) and (86) give their relations in a right-angled spherical triangle.The motion occurs always on the plane having inclination $I$ to the ecliptic and intersecting it at a point of longitude $\gamma _ { 3 }$ . The variation of $r$ and $\psi$ represents the motion. $\psi$ is called the argument of latitude (Figure l).Hence, the problem is reduced to the discussion of

$$
\begin{array} { l } { \displaystyle \gamma _ { 1 } + t = - \int \frac { d \rho } { \rho ^ { 2 } \sqrt { U ( \rho ) } } , } \\ { \displaystyle \gamma _ { 2 } - \psi = h _ { 2 } \int \frac { d \rho } { \sqrt { U ( \rho ) } } . } \end{array}
$$

![](images/9a79ea1b4bdd3dba1ff25877a45ffb273a31ab45d2459d58800b654dc3393a60.jpg)  
FIGURE I

# Consider

$$
U ( \rho ) = 2 h _ { 1 } + 2 \beta \rho - h _ { 2 } ^ { 2 } \rho ^ { 2 } .
$$

If $U ( \rho ) = 0$ has no real root, then $U ( \rho ) > 0$ for a real motion. If $U ( \rho ) = 0$ has real roots $\rho _ { 1 } , \rho _ { 2 } \left( \rho _ { 1 } \right) \geq \rho _ { 2 } )$ ,then $U ( \rho ) = h _ { 2 } ^ { 2 } ( \rho _ { 1 } - \rho ) ( \rho - \rho _ { 2 } )$ and $\rho$ varies between $\pmb { \rho _ { 1 } }$ and $\rho _ { 2 }$ . According as $h _ { 1 } > 0 , h _ { 1 } < 0 , h _ { 1 } = 0$ we have $\rho _ { 1 } \rho _ { 2 } < 0$ ， $\rho _ { 1 } \rho _ { 2 } > 0$ ， $\rho _ { 1 } \rho _ { 2 } = 0$

If $\rho _ { 1 } \rho _ { 2 } < 0$ , then one root is positive and the other negative and the motion extends to infinity, as $\rho = 1 / r \geq 0$ But, by (87),it is an asymptotic motion where $\pmb { \rho }$ approaches zero as $t$ approaches infinity. We have also $t = O ( \rho ^ { - 1 } )$ and $r = O ( t )$ . This is a hyperbolic motion.

If $\rho _ { 1 } \rho _ { 2 } > 0$ , then the two roots are real. The motion occurs in

$$
r _ { 1 } \ \triangleq r \ \leqq \ r _ { 2 } ,
$$

where $r _ { 1 } = 1 / \rho _ { 1 } , r _ { 2 } = 1 / \rho _ { 2 } . r _ { 1 }$ is the perihelion distance and $r _ { 2 }$ is the aphelion distance; this is an elliptic motion ora circular motion.

If $\rho _ { 1 } \rho _ { 2 } = 0$ ， then one root is zero. Let $\rho _ { 2 } = 0$ ； then，by the first equation of (87), this is an asymptotic motion where $\rho$ approaches zero. We have $t = O ( \rho ^ { - 3 / 2 } )$ or $t = O ( r ^ { 2 / 3 } )$ . This is a parabolic motion.

Consider in particular a straight-line motion. Suppose that $h _ { 2 } = 0$ The motion occurs on the straight line $\psi = \gamma _ { 2 }$ .From the first equation of (87) we obtain

$$
\left( { \frac { d r } { d t } } \right) ^ { 2 } = { \frac { 2 \beta } { r } } + 2 h _ { 1 } .
$$

If $h _ { 1 } > 0$ ，then $\pmb { r }$ must monotonically increase or decrease,as the righthand member does not vanish for $h _ { 1 } > 0$ ； that is, the two bodies either recede to infinity or collide.Let $h _ { 1 } = - f < 0 .$ Let variable $\pmb { u }$ be defined by

$$
\left( { \frac { d u } { d t } } \right) ^ { 2 } = { \frac { \epsilon } { r } } ;
$$

then

$$
\left( \frac { d r } { d u } \right) ^ { 2 } = \frac { 2 f } { \epsilon } \left( \frac { \beta } { f } - r \right) ,
$$

where $\epsilon$ is a positive constant to be determined later. Let $\boldsymbol { u }$ increase monotonically with $t .$ Put $\epsilon = f ^ { 2 } / ( 2 \beta )$ and integrate (90); then

$$
r = \frac { \beta } { f } ( 1 - u ^ { 2 } ) .
$$

Substitution of (91) in (89) gives

$$
u \sqrt { 1 - u ^ { 2 } } + \sin ^ { - 1 } u = \frac { \sqrt { 2 } f ^ { 3 / 2 } } { \beta } ( t - t _ { 0 } ) .
$$

Equations (91） and (92) are the solution for the straight-line case. $u < 0$ for $t \ < \ t _ { 0 }$ . The variable $\boldsymbol { u }$ increases from zero at first,until $r$ attains its maximum, $\beta / f .$ After this, $r$ decreases and then vanishes；that is,the two bodies collide in a finite time $t = t _ { 0 } + \beta \pi / ( 2 \sqrt { 2 } f ^ { 3 / 2 } )$

Let the instant of the collision be $t = 0$ and let $r = \sqrt [ 3 ] { \beta } w$ Then the equation of motion becomes

$$
\frac { d ^ { 2 } w } { d t ^ { 2 } } = - \frac { 1 } { w ^ { 2 } } \cdotp
$$

Substitute $w = \gamma t ^ { m }$ in this equation; then $m = \textstyle { \frac { 2 } { 3 } }$ . Hence,

$$
w = \sqrt [ 3 ] { \frac { 9 } { 2 } } t ^ { 2 / 3 } = \tau
$$

is a particular solution.In order to obtain the general solution, put

$$
w = \tau + \alpha _ { 2 } \tau ^ { 2 } + \alpha _ { 3 } \tau ^ { 3 } + \cdot \cdot \cdot
$$

in the equation of motion and determine $\alpha _ { 2 } , \alpha _ { 3 } , \ldots .$ ，This gives the general solution

$$
r = \sqrt [ 3 ] { \bar { \beta } } ( \tau + \alpha _ { 2 } \tau ^ { 2 } - { \textstyle \frac { 3 } { 7 } } \alpha _ { 2 } ^ { 2 } \tau ^ { 2 } + { \textstyle \frac { 2 } { 6 } } { \textstyle \frac { 3 } { 3 } } \alpha _ { 2 } ^ { 3 } \tau ^ { 4 } + \cdot \cdot \cdot ) .
$$

${ \pmb { \alpha } } _ { 2 }$ is one of the integration constants, such that $h _ { 1 } = 5 \beta ^ { 2 / 3 } \alpha _ { 2 }$ .The other integration constant is the time of collision.This treatment is due to Burrau (1894).

An important fact known in this particular case of collision is that the mutual distance $r$ is expressed in the form of an integral power series of $t ^ { 2 / 3 }$ ， $t$ being counted backward from the instant of collision.According to Sundman (l9l3),a similar situation occurs in the collision of two bodies in the three-body problem，as will be discussed in Chapter 22.The differential equation remains unaltered when we change the sign of $t .$ Hence,if $t$ denotes the time interval elapsed since the instant of collision, the solution can likewise be represented by the same integral power series of $t ^ { 2 / 3 }$ .Mathematically, the solution can be analytically continued after the collision. Sundman shows that this is true for the three-body problem. It is also true for the isosceles-triangular solution treated in Section 3.9.

# 5.10ELLIPTIC MOTIONS

An elliptic motion takes place for $\rho _ { 1 } \rho _ { 2 } > 0$ and accordingly for $h _ { 1 } = - f < 0 .$ By Section 2.4,we have

$$
\frac { d r } { d u } = \sqrt { ( r _ { 2 } - r ) ( r - r _ { 1 } ) } ,
$$

$$
\frac { d u } { d t } = \frac { h _ { 2 } } { r } \sqrt { \rho _ { 1 } \rho _ { 2 } } ,
$$

or, by integrating (95),

$$
\frac { 1 } { \rho } = r = \frac { r _ { 1 } + r _ { 2 } } { 2 } - \frac { r _ { 2 } - r _ { 1 } } { 2 } \cos u .
$$

On the other hand,from (84b) we obtain

$$
r ^ { 2 } { \frac { d { \psi } } { d t } } = h _ { 2 } = { \frac { 1 } { \rho ^ { 2 } } } { \frac { d \psi } { d t } } \cdot
$$

Solve (98) together with $d \rho / d t = - \rho ^ { 2 } h _ { 2 } \sqrt { \left( \rho _ { 1 } - \rho \right) \left( \rho - \rho _ { 2 } \right) }$ and denote the integration constant by $\pi / 2 + \varpi - \Omega$ ; then

$$
\frac { \pi } { 2 } - \ 4 + \varpi - \ \Omega = \sin ^ { - 1 } \left[ \left( { \rho } - \frac { { \rho } _ { 1 } + { \rho } _ { 2 } } { 2 } \right) \middle / \left( \frac { { \rho } _ { 1 } - { \rho } _ { 2 } } { 2 } \right) \right] .
$$

Put

$$
\cdot \left( \rho _ { 1 } + \rho _ { 2 } \right) = \frac 1 2 \left( \frac 1 { r _ { 1 } } + \frac 1 { r _ { 2 } } \right) = \frac 1 \rho , \qquad r _ { 1 } = a ( 1 - e ) , \qquad r _ { 2 } = a ( 1 + e
$$

then, $a { \sqrt { 1 - e ^ { 2 } } } = p$ and the integral is

$$
r = \frac { \phi } { 1 + e \cos { ( \psi - \varpi + \Omega ) } } .
$$

The constant $\Omega$ is $\gamma _ { 3 }$ in the former notation. $\boldsymbol { \varpi }$ is the value of $\psi + \Omega$ for $\boldsymbol { r } = \boldsymbol { r } _ { 1 }$ . Note that the counting of the angles is over two different planes, that is, $\Omega$ is counted on the ecliptic and $\psi$ on the orbital plane. $\eqcirc$ is called the longitude of the perihelion, $\Omega$ the longitude of the ascending node. In the case of asteroids and comets, $\omega$ is often used for $\varpi - \Omega$ Equation (99) is the equation for an ellipse and expresses the first law of Kepler; $e$ is the eccentricity, $^ a$ the semimajor axis,and $\boldsymbol { u }$ (equation 97） the eccentric anomaly.

Solving (97), we obtain

$$
r = a ( 1 - e \cos u ) .
$$

Write (96) as $r d u = \sqrt { \overline { { \rho _ { 1 } \rho _ { 2 } } } } h _ { 2 } d t$ . Substitute this in (100); then,

$$
u \mathrm { ~ - ~ } e \sin u = \frac { \sqrt { \rho _ { 1 } \rho _ { 2 } } h _ { 2 } } { a } ( t - t _ { 0 } ) .
$$

From $h _ { 2 } ^ { 2 } = a ( 1 - e ^ { 2 } ) \beta , h _ { 1 } = - \beta / 2 a$ ，we obtain $\sqrt { \rho _ { 1 } \rho _ { 2 } } h _ { 2 } / a = \sqrt { \overline { { \beta } } } / a ^ { 3 / 2 }$ Also $d t = { \sqrt { a / \mu } } r d u$ ,by (96). The period with respect to $\pmb { u }$ is

$$
T = \int _ { 0 } ^ { 2 \pi } a ( 1 - e \cos u ) d u = \frac { 2 \pi a ^ { 3 / 2 } } { \sqrt { \beta } } \cdot
$$

Hence, if we put $n = 2 \pi / T = \sqrt { \bar { \beta } } / a ^ { 3 / 2 }$ we have $n ^ { 2 } a ^ { 3 } = \beta$ .This is the third law of Kepler. $_ { n }$ is called the mean motion. Thus

$$
u - e \sin u = n ( t - t _ { 0 } ) = M .
$$

This is Kepler's equation. $M$ is the mean anomaly and $t _ { 0 }$ the epoch of the perihelion passage. From (98) we have $r ^ { 2 } d \psi / d t = n a ^ { 2 } \nu ^ { \prime } \overline { { 1 - e ^ { 2 } } }$ .Integrate this equation by writing $\psi = v + \varpi - \Omega$ and substituting (100） for $r$ ； then

$$
\tan { \frac { \upsilon } { 2 } } = \sqrt { \frac { 1 + \ell } { 1 - \ell } } \tan { \frac { \upsilon } { 2 } } \cdotp
$$

$v$ is called the true anomaly. $M , u ,$ and $v$ all equal zero at the perihelion and $\pi$ at the aphelion,and have a common period $T$

$$
r \cos v = a ( \cos u - e ) , \qquad r \sin v = a \sqrt { 1 - e ^ { 2 } } \sin u .
$$

$\mathcal { L } = \boldsymbol { \psi } + \boldsymbol { \Omega } = \boldsymbol { v } + \boldsymbol { \varpi }$ is called the longitude in the orbit. It is customary to write

$$
M = n t + \epsilon - \varpi , \qquad \lambda = n t + \epsilon = M + \varpi .
$$

$\lambda$ is the mean longitude or the mean longitude in the orbit, $\epsilon$ the mean longitude at the epoch,that is,for $t = 0$ .These angles are counted over two different planes, just like the angle $\boldsymbol { \varpi }$ ·

For the ecliptic we have rectangular coordinates $x _ { 1 } , \ x _ { 2 } , \ x _ { 3 }$ with the vernal equinox as the $\scriptstyle x _ { 1 }$ direction,and

$$
\begin{array} { r l } & { x _ { 1 } = r ( \cos \psi \cos \Omega - \sin \psi \sin \Omega \cos I ) , } \\ & { x _ { 2 } = r ( \cos \psi \sin \Omega + \sin \psi \cos \Omega \cos I ) , } \\ & { x _ { 3 } = r \sin \psi \sin I . } \end{array}
$$

Substitute in (105) these expressions (105a) for $\pmb { r }$ cos $\psi$ and $r \sin \psi$ ：

$$
\begin{array} { r } { r \cos \psi = r \cos v \cos ( \varpi - \Omega ) - r \sin v \sin ( \varpi - \Omega ) , } \\ { r \sin \psi = r \cos v \sin ( \varpi - \Omega ) + r \sin v \cos ( \varpi - \Omega ) ; } \end{array}
$$

but first substitute in (l05a) the expressions for $\boldsymbol { r }$ cos $v$ and $r$ sin $v$ given in (103).We thus obtain

$x _ { 1 } = a ( \cos u - e ) [ \cos ( \varpi - \Omega ) \cos \Omega - \sin ( \varpi - \Omega ) \sin \Omega \cos I ]$ $- \left. a \sqrt { 1 - e ^ { 2 } } \sin u [ \sin \left( \varpi - \Omega \right) \cos \Omega + \cos \left( \varpi - \Omega \right) \sin \Omega \cos \Omega ] \right\} \cos \Omega ,$ sI], $\begin{array} { r } { x _ { 2 } = a ( \cos u - e ) [ \cos ( \varpi - \Omega ) \sin \Omega + \sin ( \varpi - \Omega ) \cos \Omega \cos } \\ { + a \sqrt { 1 - e ^ { 2 } } \sin u [ - \sin ( \varpi - \Omega ) \sin \Omega + \cos ( \varpi - \Omega ) \cos \Omega } \end{array}$ $x _ { 3 } = a ( \cos u - e ) \sin ( \varpi - \Omega ) \sin I + a \sqrt { 1 - e ^ { 2 } } \sin u \cos ( \varpi - \Omega ) \sin$ in I; or, if we let

$$
\begin{array} { l } { { X = r \cos { ( v - M ) } } } \\ { { \ = a ( \cos { u } - e ) \cos { M } + a \sqrt { 1 - e ^ { 2 } } \sin { u } \sin { M } , } } \\ { { \ } } \\ { { Y = r \sin { ( v - M ) } } } \\ { { \ = - a ( \cos { u } - e ) \sin { M } + a \sqrt { 1 - e ^ { 2 } } \sin { u } \cos { M } . } } \end{array}
$$

we obtain

$$
a ( \cos u - e ) = X \cos M - Y \sin M ,
$$

$$
a { \sqrt { 1 - e ^ { 2 } } } \sin u = X \sin M + Y \cos M ;
$$

accordingly,

$x _ { 1 } = X [ \cos { ( M + \varpi - \Omega ) } \cos { \Omega } - \sin { ( M + \varpi - \Omega ) } \sin { \Omega } \cos { \Omega }$ 1 $- \ { \cal Y } [ \sin \left( M + \varpi - \Omega \right) \cos \Omega + \cos \left( M + \varpi - \Omega \right) \sin ^ { 2 } \left( M + \varpi - \Omega \right) \cos \Omega ] .$ n Ω cos I],   
x2= $\mathbf { \Omega } = X [ \cos { ( M + \varpi - \Omega ) } \sin { \Omega } + \sin { ( M + \varpi - \Omega ) } \cos { \Omega } \cos { I } ]$ 1 $+ \ Y [ - \sin { ( M + \varpi - \Omega ) } \sin { \Omega } + \cos { ( M + \varpi - \Omega ) } \cos { \Omega } \cos { \Omega }$ I],   
${ x _ { 3 } } = X \sin \left( { M + \varpi - \Omega } \right) \sin I + Y \cos \left( { M + \varpi - \Omega } \right) \sin I ;$ (107)   
or

$$
\begin{array} { c } { { x _ { 1 } = X \Big [ \cos ^ { 2 } { \frac { I } { 2 } } \cos \lambda + \sin ^ { 2 } { \frac { I } { 2 } } \cos { ( \lambda - 2 \Omega ) } \Big ] } } \\ { { { } } } \\ { { { } ~ - Y \Big [ \cos ^ { 2 } { \frac { I } { 2 } } \sin \lambda + \sin ^ { 2 } { \frac { I } { 2 } } \sin { ( \lambda - 2 \Omega ) } \Big ] , } } \\ { { { } } } \\ { { { } ~ x _ { 2 } = X \Big [ \cos ^ { 2 } { \frac { I } { 2 } } \sin \lambda - \sin ^ { 2 } { \frac { I } { 2 } } \sin { ( \lambda - 2 \Omega ) } \Big ] } } \\ { { { } ~ + Y \Big [ \cos ^ { 2 } { \frac { I } { 2 } } \cos \lambda - \sin ^ { 2 } { \frac { I } { 2 } } \cos { ( \lambda - 2 \Omega ) } \Big ] , } } \end{array}
$$

$$
x _ { 3 } = X \sin I \sin ( \lambda - \Omega ) + Y \sin I \cos ( \lambda - \Omega ) .
$$

The constants appearing in (84) are related to the Keplerian constants $a , e , I , \Omega , \varpi , t _ { 0 }$ by

$$
h _ { 1 } = \mathrm { ~ - ~ } \frac { \beta } { 2 a } , \qquad h _ { 2 } = \sqrt { \beta a ( 1 - \mathrm { ~ } e ^ { 2 } ) } , \qquad h _ { 3 } = \sqrt { \beta a ( 1 - \mathrm { ~ } e ^ { 2 } ) } \cos { I } ,
$$

$$
\gamma _ { 1 } = - t _ { 0 } , \qquad \gamma _ { 2 } = \varpi - \Omega , \qquad \gamma _ { 3 } = \Omega .
$$

The constants $h _ { 1 } , h _ { 2 } , h _ { 3 } , \gamma _ { 1 } , \gamma _ { 2 } , \gamma _ { 3 }$ are determined by the method of Hamilton-Jacobi and are called the canonical constants. $h$ and $\gamma$ are canonically conjugate.

# 5.11CAUCHY's FIRST THEOREM

Suppose that $s$ can be expanded in a Laurent series for all values of $_ z$ in the ring domain,excluding O and $\infty$ , of a complex variable $z$ and for all values of $e$ in a domain enclosing $\epsilon = 0$ ：

$$
S = \sum _ { j \mathrm { ~ = ~ - ~ } \infty } ^ { + \infty } P _ { j } z ^ { j } .
$$

Then, by Cauchy's theorem on residues in the theory ofanalytic functions, we have

$$
P _ { j } = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int ^ { ( 0 + ) } z ^ { - j - 1 } { \cal S } d z \qquad ( j \not \equiv 0 )
$$

for a closed integration contour surrounding the point $o$ in the positive sense.Take the variable $\pmb { \mathscr { s } }$ such that

$$
z = s \exp \left[ - \frac { e } { 2 } \left( s - s ^ { - 1 } \right) \right] .
$$

The right-hand member is convergent for all values in the domain enclosing $\epsilon = 0$ ; indeed, it can be developed in a uniformly convergent Laurent series in the ring domain of $\pmb { \mathscr { s } }$ , excluding $s = 0$ and $s = \infty$ .Then,

$$
d z = \left[ 1 - \frac { e } { 2 } ( s + s ^ { - 1 } ) \right] \exp \left[ - \frac { e } { 2 } ( s - s ^ { - 1 } ) \right] d s .
$$

The derivative $d z / d s$ does not become infinite except for $s = 0$ or $\infty$ Hence,

$$
P _ { j } = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int ^ { ( 0 + ) } { s ^ { - j - 1 } U d s } ,
$$

where

$$
U = S \bigg [ 1 - { \frac { e } { 2 } } ( s + s ^ { - 1 } ) \bigg ] \exp \bigg [ { \frac { j e } { 2 } } ( s - s ^ { - 1 } ) \bigg ] .
$$

The integration contour for $\pmb { \mathscr { s } }$ is obtained by the transformation (112) of

the corresponding integration contour for $z ,$ But, as there is no singularity, the contour for $s$ can be deformed to the unit circle around $o$ .Thus,

$$
U = \sum _ { j = - \infty } ^ { \infty } P _ { j } s ^ { j } .
$$

Since

$$
P _ { j } = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int ^ { ( 0 + ) } \frac { 1 } { j } z ^ { - j } \frac { d S } { d z } d z ,
$$

which, by (112),

$$
= \frac { 1 } { 2 \pi \sqrt { - 1 } j } \int ^ { ( 0 + ) } s ^ { - j } \exp \left[ \frac { j e } { 2 } ( s - s ^ { - 1 } ) \right] \frac { d S } { d s } d s ,
$$

therefore,

$$
P _ { j } = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int ^ { ( 0 + ) } s ^ { - j } V d s ,
$$

where

$$
V = \frac { 1 } { j } \frac { d S } { d s } \exp \left[ \frac { j e } { 2 } \left( s - s ^ { - 1 } \right) \right] .
$$

The result (114) and (115) is called Cauchy's first theorem.In other words, take the unit circles around $O$ as the integration contour both for $_ z$ and for $s _ { : }$ and put

$$
z = \exp ( \sqrt { - 1 } \ M ) , \qquad s = \exp ( \sqrt { - 1 } \ u ) ;
$$

then a coefficient of the Fourier series expansion of $s$ in the mean anomaly $M$ equals the corresponding coefficient of the Fourier series expansion of the function $U$ defined by (1l4a） in the eccentric anomaly $\boldsymbol { u }$ and equals the corresponding coefficient of the Fourier series expansion of $V s$ defined by (115a) in $\boldsymbol { u }$

As an example, take

$$
S = { \frac { a } { r } } = ( 1 - \epsilon \cos u ) ^ { - 1 } = \left[ 1 - { \frac { e } { 2 } } ( s + s ^ { - 1 } ) \right] ^ { - 1 } .
$$

Because of (114a), we have

$$
U = \exp \left[ { \frac { j e } { 2 } } \left( s - s ^ { - 1 } \right) \right]
$$

and

$$
P _ { j } = P _ { - j } = J _ { j } ( j e )
$$

with the Bessel function. Therefore

$$
\frac { a } { r } = 1 + 2 \sum _ { j = 1 } ^ { \infty } J _ { j } ( j e ) \cos j M .
$$

Note that the singularities

$$
s _ { 1 } = \frac { e } { 1 - \sqrt { 1 - e ^ { 2 } } } \equiv \frac { 1 } { \omega } > 1
$$

and

$$
s _ { 2 } = \frac { 1 - \sqrt { 1 - e ^ { 2 } } } { e } \equiv \omega < 1 ,
$$

obtained from

$$
1 - \frac { e } { 2 } ( s + s ^ { - 1 } ) = 0 ,
$$

occur in the transition from the complex $s$ plane to the complex $z$ plane by the condition $d s / d z = \infty$ .The corresponding points in the $_ z$ plane are $z _ { 1 } = ( 1 / \omega ) \exp { ( - \sqrt { 1 - e ^ { 2 } } ) }$ and zz = ω exp $( \sqrt { 1 - e ^ { 2 } } )$ ， $z _ { 1 } > 1 > z _ { 2 } ,$ where $d z / d s = 0$ The expansions of $z \mathrm { ~ - ~ } z _ { 1 }$ and $z \mathrm { ~ - ~ } z _ { 2 }$ in the neighborhood of these points begin with the terms $( s - s _ { 1 } ) ^ { 2 }$ and $( s \mathrm { ~ - ~ } s _ { 2 } ) ^ { 2 }$ ， respectively. On the other hand, the expansions of $s \mathrm { ~ - ~ } s _ { 1 }$ and $s \mathrm { ~ - ~ } s _ { 2 }$ are in integral power series of $\sqrt { z - z _ { 1 } }$ and $\sqrt { z - z _ { 2 } }$ , respectively.

Let $j , \ q$ be positive integers or zero and $\pmb { \mathscr { p } }$ be a positive or negative integer or zero,and consider

$$
\Big ( x + \frac { 1 } { x } \Big ) ^ { j } \Big ( x - \frac { 1 } { x } \Big ) ^ { q } = \sum _ { p = - \infty } ^ { \infty } N _ { - p , j , q } x ^ { p } .
$$

$N _ { - p , j , q }$ is called a Cauchy number, and we have

$$
\begin{array} { l } { { \displaystyle N _ { - p , j , q } = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int ^ { ( 0 + ) } x ^ { - p - 1 } \biggl ( x + \frac { 1 } { x } \biggr ) ^ { j } \biggl ( x - \frac { 1 } { x } \biggr ) ^ { q } d x } } \\ { { \displaystyle \qquad = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int ^ { ( 0 + ) } ( - 1 ) ^ { q } x ^ { - p - j - q - 1 } ( 1 + x ^ { 2 } ) ^ { j } ( 1 - x ^ { 2 } ) ^ { q } d x } } \\ { { \displaystyle \qquad = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int ^ { ( 0 + ) } x ^ { - p + j + q - 1 } \biggl ( 1 + \frac { 1 } { x ^ { 2 } } \biggr ) ^ { j } \biggl ( 1 - \frac { 1 } { x ^ { 2 } } \biggr ) ^ { q } d x . } } \end{array}
$$

Insert cross cuts between $\infty$ and $^ { - 1 }$ , between O and $+ 1$ ， between $+ i$ and $+ i \infty$ ，and between $- i$ and $- i \infty$ , and let the integration contour surround the cross cut between O and $+ 1$ . It is easily seen that $N _ { - p , j , q } = 1$ for $- \phi + j + q = 0 , N _ { - p , j , q } = ( - 1 ) ^ { q }$ for $- \phi - j - q = 0 .$ $N _ { - p , j , q } = 0$ for $- \phi + j + q < 0$ or $- p - j - q > 0$ and $N _ { - p , j , q } = 0$ for an odd $- \phi + j + q > 0$ and for an odd $- p - j - q < 0$

By formal substitution $x = 1 / x ^ { \prime }$ in (117) we obtain

$$
N _ { - p , j , - q } = ( - 1 ) ^ { q } N _ { p , j , q } .
$$

From

$$
\begin{array} { l } { x ^ { - p - ( j + 1 ) - q - 1 } ( 1 + x ^ { 2 } ) ^ { j + 1 } \qquad } \\ { \qquad \equiv x ^ { - p + 1 - j - q - 1 } ( 1 + x ^ { 2 } ) ^ { j } + x ^ { - p - 1 - j - q - 1 } ( 1 + x ^ { 2 } ) ^ { j } , } \end{array}
$$

we obtain

$$
\begin{array} { r } { N _ { - p , j + 1 , q } = N _ { - p + 1 , j , q } + N _ { - p - 1 , j , q } , } \\ { N _ { - p , j , q + 1 } = N _ { - p + 1 , j , q } - N _ { - p - 1 , j , q } . } \end{array}
$$

In particular, we can compute $N _ { p , 1 , q }$ when we know $N _ { p , \circ , q }$ ，because $N _ { - p , 1 , q } = N _ { - p + 1 , 0 , q } + N _ { - p - 1 , 0 , q }$ and we can compute $N _ { - p , j , q }$ successively by the above recurrence formula. The numerical values were given by Bourget (1863).

By formal substitution $x ^ { 2 } = y$ in (117), we obtain

$$
N _ { - p , 0 , q } = \frac { ( - 1 ) ^ { q } } { 2 \pi \sqrt { - 1 } } \int ^ { ( 0 + ) } y ^ { - ( p + q ) / 2 - 1 } ( 1 - y ) ^ { q } d y .
$$

Since $q > 0 , y = 1$ is an ordinary point, but $y = 0$ is a singular point. Consider an integration contour that surrounds $y = 0$ in the positive sense,extends from $y = 0$ to $y = + 1$ along the real axis, surrounds $y = 1$ in the positive sense and returns to $y = 0$ along the real axis. If the integral taken from $y = 0$ to $y = 1$ be l,then the integral taken from $y = 1$ to $y = 0$ is equal to

$$
\exp \left[ - 2 \pi \sqrt { - 1 } \left( - \frac { \not p + \not q } { 2 } - 1 \right) \right] ,
$$

because the contour surrounds $y = 0$ once in the positive sense (see Figure 2, Section 5.12). Hence, using Gausss beta function $B ( \xi , \eta )$ we obtain

$$
N _ { - p , 0 , q } = ( - 1 ) ^ { q } \frac { 1 - \exp \left\{ - 2 \pi \sqrt { - 1 } \left[ ( p + q ) / 2 \right] \right\} } { 2 \pi \sqrt { - 1 } }
$$

$$
\begin{array} { r l } & { \quad \times \int _ { 0 } ^ { 1 } y ^ { - ( p + q ) / 2 - 1 } ( 1 - y ) ^ { q } d y } \\ & { = ( - 1 ) ^ { q } ( - 1 ) ^ { - ( p + q ) / 2 } \frac { \sin { \left[ \pi \left( \rho + q \right) / 2 \right] } } { \pi } B \Bigl ( - \frac { \dot { p } + q } { 2 } , q + 1 \Bigr ) } \\ & { = ( - 1 ) ^ { ( q - p ) / 2 } \frac { 1 } { \Gamma \left[ - \left( \dot { p } + q \right) / 2 \right] \Gamma \left[ 1 + \left( \dot { p } + q \right) / 2 \right] } } \\ & { \quad \times \frac { \Gamma \left[ - \left( \dot { p } + q \right) / 2 \right] \Gamma \left( q + 1 \right) } { \Gamma \left[ - \left( \dot { p } - q \right) / 2 + 1 \right] } } \\ & { = ( - 1 ) ^ { ( q - p ) / 2 } \frac { q ! } { \left[ \left( q + \dot { p } \right) / 2 \right] ! \left[ \left( q - \dot { p } \right) / 2 \right] ! } . } \end{array}
$$

Similarly, we can prove that

$$
N _ { - p , j , 0 } = { \frac { j ! } { [ ( j + p ) / 2 ] ! [ ( j - p ) / 2 ] ! } } \cdot
$$

Substitute $x = \exp \left( { \sqrt { - 1 } \ \theta } \right)$ in

$$
N _ { 0 , j , q } = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int ^ { ( 0 + ) } x ^ { - 1 } \biggl ( x + \frac { 1 } { x } \biggr ) ^ { j } \biggl ( x - \frac { 1 } { x } \biggr ) ^ { q } d x ;
$$

then

$$
N _ { 0 , j , q } = \frac { 1 } { 2 \pi } \int _ { 0 } ^ { 2 \pi } \cos ^ { j } \theta \sin ^ { q } \theta d \theta \cdot z ^ { j + q } ( - 1 ) ^ { q / 2 } .
$$

Divide the integration path into the path from O to $\pi$ and the path from $\pi$ to $2 \pi$ , and write $\pi + \theta ^ { \prime }$ instead of $\theta$ in the second integral. Then $j + q$ must be even.By dividing the integration path from O to $\pi$ into the paths from O to $\pi / 2$ and $\pi / 2$ to $\pmb { \pi }$ ，and writing $\pi / 2 + \theta ^ { \prime }$ instead of $\theta$ in the second integral, we see that $q$ must also be even. Hence,

$$
{ \begin{array} { r l } & { N _ { 0 , j , q } = { \frac { ( - 1 ) ^ { q / 2 } 2 ^ { j + q } } { \pi } } 2 { \displaystyle \int _ { 0 } ^ { \pi / 2 } } \cos ^ { j } \theta \sin ^ { q } \theta d \theta } \\ & { \qquad = { \frac { ( - 1 ) ^ { q / 2 } 2 ^ { j + q } } { \pi } } { \frac { \Gamma [ ( j + 1 ) / 2 ] \Gamma [ ( q + 1 ) / 2 ] } { \Gamma [ ( j + q ) / 2 + 1 ] } } } \\ & { \qquad = ( - 1 ) ^ { q / 2 } { \frac { 1 \cdot 3 \cdot 5 \cdot \cdot ( j - 1 ) \cdot 1 \cdot 3 \cdot 5 \cdot \cdot ( q - 1 ) } { [ ( j + q ) / 2 ] ! } } \cdot } \end{array} }
$$

This is a new formula due to Hagihara (1935).

For an application of Cauchy's numbers, consider

$$
\frac { a ^ { 2 } } { r ^ { 2 } } = \frac { 1 } { 2 \sqrt { 1 - e ^ { 2 } } } + \sum _ { j } c _ { j } \cos j M .
$$

We see that

$$
c _ { j } = 2 \sum _ { k } \sum _ { q } \frac { j ^ { q } } { q ! } \left( \frac { e } { 2 } \right) ^ { k + q } N _ { - j , k , q } .
$$

Further,by applying this expansion to

$$
\frac { d v } { d t } = \frac { a ^ { 2 } } { r ^ { 2 } } \sqrt { 1 - e ^ { 2 } } ,
$$

we obtain

$$
v - M = \sqrt { 1 - e ^ { 2 } } \sum _ { j } c _ { j } { \frac { \sin j M } { j } } \cdotp
$$

This expression is called the equation of center. Thus the expansions in the Keplerian motion can be obtained simply by the use of Cauchy's first theorem and Cauchy's numbers.

Note that a function may have different mean values taken with respect to the mean anomaly $M _ { : }$ ，the eccentric anomaly $\pmb { u } _ { : }$ ，or the true anomaly $v$ ， respectively，although the three anomalies have equal periods.Kozai (l959,l962) has given the formula (see also Tisserand, 1889) :

$$
\begin{array} { c } { { \displaystyle \frac { 1 } { 2 \pi } \int _ { 0 } ^ { 2 \pi } \cos { j \upsilon } d M = ( - e ) ^ { j } [ 1 + \sqrt { 1 - e ^ { 2 } } ] ^ { - j } [ 1 + j \sqrt { 1 - e ^ { 2 } } ] } } \\ { { { } } } \\ { { = ( - \omega ) ^ { j } [ 1 + j \sqrt { 1 - e ^ { 2 } } ] , } } \end{array}
$$

where

$$
\omega = { \frac { 1 - { \sqrt { 1 - e ^ { 2 } } } } { e } } = { \frac { e } { 1 + { \sqrt { 1 - e ^ { 2 } } } } } \cdotp
$$

Kozai has also given the formula

$$
\gamma = \alpha + 2 \sum _ { j = 1 } ^ { \infty } a _ { j } \sin j \alpha ,
$$

with

$$
j a _ { j } = \frac { 1 } { 2 \pi } \int _ { 0 } ^ { 2 \pi } \cos j \alpha ~ d \gamma \qquad ( j \neq 0 )
$$

where $\propto$ and $\gamma$ are any two of the three kinds of anomaly.

If $\alpha = u$ and $\gamma = M ,$ ，then, $M = u - e \sin u ;$

$$
\begin{array} { l l } { { \displaystyle { \frac { 1 } { 2 \pi } } \int _ { 0 } ^ { 2 \pi } \cos j u d M = - \frac { e } { 2 } ~ } } & { { ~ ( j = 1 ) , } } \\ { { { } } } & { { { } } } \\ { { { } = 0 ~ } } & { { ~ ( j > 1 ) . } } \end{array}
$$

If $\alpha = M$ and $\gamma = v$ ,then the coefficient of sin jM in the equation of center can be expanded in a power series of $^ { e }$ (Tisserand,1889).

If $\alpha = M$ and $\gamma = u$ ,then,

$$
u = M + 2 \sum _ { j = 1 } ^ { \infty } \frac { 1 } { j } J ( j e ) \sin j M ,
$$

and the mean value is

$$
\frac { 1 } { 2 \pi } \int _ { 0 } ^ { 2 \pi } \cos j M d v = J _ { j } ( j e ) .
$$

If $\alpha = u$ and $\gamma = v$ ,then

$$
v = u + 2 \sum _ { j = 1 } ^ { \infty } \frac { 1 } { j } \omega ^ { j } \sin j u ,
$$

and

$$
{ \frac { 1 } { 2 \pi } } \int _ { 0 } ^ { 2 \pi } \cos j u d v = \omega ^ { j } .
$$

If $\alpha = v$ and $\gamma = u$ ,then

$$
u = v + 2 \sum _ { j = 1 } ^ { \infty } { \frac { 1 } { j } } \left( - \omega \right) ^ { j } \sin { j v } ,
$$

and

$$
{ \frac { 1 } { 2 \pi } } \int _ { 0 } ^ { 2 \pi } \cos j v \ d u = ( - \omega ) ^ { j } .
$$

Numerical tables of the coefficients in the expansions for elliptic motions have been published by Cayley (l86l) and recently by Jarnagin (1965).

# 5.12CAUCHY's SECOND THEOREM

Put

$$
t = \exp { ( \sqrt { - 1 } \ v ) } , \qquad s = \exp { ( \sqrt { - 1 } \ u ) } , \qquad z = \exp { ( \sqrt { - 1 } \ M ) }
$$

and

$$
\omega = \frac { e } { 1 + \sqrt { 1 - e ^ { 2 } } } = \frac { 1 - \sqrt { 1 - e ^ { 2 } } } { e } , \qquad \eta = \frac { 1 + \sqrt { 1 - e ^ { 2 } } } { 2 } = \frac { 1 } { 1 + e ^ { 2 } } ,
$$

$$
\omega \eta = \frac { e } { 2 } ;
$$

then

$$
s = \frac { t + \omega } { 1 + \omega t } , \qquad t = \frac { s - \omega } { 1 - \omega s } \cdot
$$

This is a linear fractional transformation in the theory of modular functions. The point $s = 0$ is carried to $t = \infty$ ,and $s = \infty$ to $t = - 1 / \infty _ { \mathrm { ; } }$ by this transformation. The invariant points $t = \pm 1$ are defined by

$$
t = { \frac { t + \omega } { 1 + \omega t } } \cdot
$$

We have

$$
\frac { d t } { d s } = \frac { t } { s } \frac { 1 - \omega ^ { 2 } } { ( 1 - \omega s ) ( 1 - \omega s ^ { - 1 } ) } = \frac { ( 1 + \omega t ) ^ { 2 } } { 1 - \omega ^ { 2 } } = \frac { 1 - \omega s + \omega ( s - \omega ) } { ( 1 - \omega s ) ^ { 2 } } ,
$$

$$
z = t { \biggl ( } 1 + { \frac { \omega } { t } } { \biggr ) } ( 1 + \omega t ) ^ { - 1 } \exp { \biggl [ } { - } { \frac { 1 - \omega ^ { 2 } } { 1 + \omega ^ { 2 } } } { \biggl ( } { \frac { \omega t } { 1 + \omega t } } - { \frac { \omega t ^ { - 1 } } { 1 + \omega t ^ { - 1 } } } { \biggr ) } { \biggr ] } ,
$$

$$
\frac { r } { a } = \frac { 1 } { 1 + \omega ^ { 2 } } ( 1 - \omega s ) ( 1 - \omega s ^ { - 1 } ) = \frac { ( 1 - \omega ^ { 2 } ) ^ { 2 } } { 1 + \omega ^ { 2 } } \frac { 1 } { ( 1 + \omega t ) ( 1 + \omega t ^ { - 1 } ) }
$$

$$
\frac { d s } { d t } = \frac { 1 + \omega ^ { 2 } s } { 1 - \omega ^ { 2 } } \frac { r } { t } \frac { r } { a } ,
$$

$$
{ \frac { d z } { d t } } = { \frac { 1 + \omega ^ { 2 } } { 1 - \omega ^ { 2 } } } { \frac { z } { t } } \left( { \frac { r } { a } } \right) ^ { 2 } .
$$

The variable $_ z$ ,regarded as a function of $\mathfrak { s }$ ,has singularities at $s = 0$ and $\infty$ , and,as a function of $t _ { : }$ ,has a simple pole at $t = \infty$ and an essential singularity at $t = \omega ^ { - 1 }$ . Its reciprocal $1 / z _ { ; }$ ，regarded as a function of $s ,$ has a simple pole at $t = 0$ and an essential singularity at $t = - \omega$ . The radius vector $r _ { \mathrm { \Omega } }$ ,regarded as a function of $s$ ,has no singularity, but, as a function of $t _ { : }$ ,has simple poles at $t = - 1 / \omega$ and $- \boldsymbol { \omega }$ . The reciprocal $1 / r ,$ regarded as a function of $\pmb { \mathscr { s } }$ ,has simple poles at $s = \omega$ and $s = 1 / \omega$ ,but, as a function of $t$ ，has no singularity.Hence, in the transition from the $_ z$ plane to the $t$ plane there are essential singularities at $t = - \omega$ and $t = - 1 / \omega$ ，so that we construct in the $t$ plane two cross cuts extending from $- \infty$ to $- 1 / \omega$ and from $- \boldsymbol { \omega }$ to O.The second cross cut is contained in the unit circle with $O$ as the center.Hence,as in Cauchy's first theorem,the coefficient $P _ { j }$ in the expansion

$$
S = \sum _ { j = - \infty } ^ { \infty } P _ { j } z ^ { j }
$$

equals the coefficient of $t ^ { j }$ in the expansion of

$$
\begin{array} { c } { { \displaystyle \frac { 1 } { \sqrt { 1 - e ^ { 2 } } } \left( \frac { r } { a } \right) ^ { 2 } \left( \frac { t } { z } \right) ^ { j } S = \frac { ( 1 - \omega ^ { 2 } ) ^ { 3 } } { 1 + \omega ^ { 2 } } ( 1 + \omega t ) ^ { j - 2 } ( 1 + \omega t ^ { - 1 } ) ^ { - j - 2 } } } \\ { { \mathrm { } } } \\ { { \mathrm { } \times \exp \left[ - j \frac { 1 - \omega ^ { 2 } } { 1 + \omega ^ { 2 } } \left( \frac { \omega t } { 1 + \omega t } - \frac { \omega t ^ { - 1 } } { 1 + \omega t ^ { - 1 } } \right) \right] S } } \end{array}
$$

It also equals the coefficient of $t ^ { j }$ in the expansion of

$$
\frac { t } { j } \ : ( 1 + \omega t ) ^ { j } ( 1 + \omega t ^ { - 1 } ) ^ { - j } \exp \left[ j \frac { 1 - \omega ^ { 2 } } { 1 + \omega ^ { 2 } } \left( \frac { \omega t } { 1 + \omega t } - \frac { \omega t ^ { - 1 } } { 1 + \omega t ^ { - 1 } } \right) \right] \frac { d S } { d t } .
$$

This is called Cauchy's second theorem.

In general, an expression of the form

$$
f = { \biggl ( } { \frac { r } { a } } { \biggr ) } ^ { \rho } s ^ { \beta } t ^ { \gamma } = { \biggl ( } { \frac { r } { a } } { \biggr ) } ^ { \rho } [ \cos { ( \beta u + \gamma v ) } + { \sqrt { - 1 } } \sin { ( \beta u + \gamma v ) } ]
$$

can be written

$$
f = \sum _ { n } a _ { n } z ^ { n } = \sum _ { n } b _ { n } s ^ { n } = \sum _ { n } c _ { n } t ^ { n } ,
$$

where $\alpha , \beta , \gamma$ are arbitrary， with the proviso that $\beta + \gamma$ must be an integer so that $f$ will admit the period $2 \pi$ with respect to one of the anomalies $M , u , v .$ 、It can be seen (Andoyer,1923) that

$$
\begin{array} { l } { { a _ { n } = \displaystyle \sum _ { j } b _ { j } J _ { n - 1 } ( n e ) \mathrm { , } } } \\ { { \displaystyle b _ { n } = \eta ^ { o } K _ { n - \beta - \gamma } ^ { \rho - \gamma , \rho + \gamma } = ( - 1 ) ^ { n - \beta - \gamma } \frac { \left( \eta ^ { \prime } \right) ^ { - \rho - 1 } } { \theta } K _ { n - \beta - \gamma } ^ { - \rho - \beta + n - 1 , - \rho + \beta - n - 1 } \mathrm { , } } } \\ { { \displaystyle c _ { n } = ( - 1 ) ^ { n - \beta - \gamma } ( \eta ^ { \prime } ) ^ { - o } K _ { n - \beta - \gamma } ^ { - \rho - \beta - \gamma - \rho + \beta } = \theta \eta ^ { \rho - 1 } K _ { n - \beta - \gamma } ^ { \rho - \gamma + n - 1 , \rho + \gamma - n - 1 } \mathrm { , } } } \end{array}
$$

with

$$
\begin{array} { l } { { C _ { p } ^ { m } = { \displaystyle { \frac { p ( \rlap / p - 1 ) ( \rlap / p - 2 ) \cdot \cdot \cdot \cdot ( \rlap / p - m + 1 ) } { 1 \cdot 2 \cdot \cdot \cdot \cdot m } } } , \qquad ( m > 0 ) , \qquad C _ { p } ^ { 0 } = 1 , } } \\ { { \ } } \\ { { C _ { p } ^ { m } = ( - 1 ) ^ { n } \displaystyle { \sum _ { m ^ { \prime } , m ^ { \prime } } ^ { } } \omega ^ { m ^ { \prime } + m ^ { \prime } C _ { p } ^ { m ^ { \prime } } C _ { q } ^ { m ^ { \prime } } } } } \\ { { \ } } \\ { { \ = ( - 1 ) ^ { n } ( 1 - \omega ^ { 2 } ) ^ { p + q + 1 } K _ { n } ^ { n - p - 1 , - n - q - 1 } , } } \end{array}
$$

where

$$
\begin{array} { l } { { \epsilon = \sin \phi , \theta = \cos \phi = \sqrt { 1 - e ^ { 2 } } , \theta = \cos ^ { 2 } \phi , \theta = \frac { \theta } { \sin ^ { 2 } \phi } , } } \\ { { \omega = \tan \left( \phi / 2 \right) = e / ( 1 + \sqrt { 1 - e ^ { 2 } } ) , \theta = \frac { \theta } { \sin ^ { 2 } \phi } , \theta = \frac { 1 + \sqrt { 1 - e ^ { 2 } } } { 2 ( 1 - e ^ { 2 } ) } , \theta = \frac { \theta } { \cos ^ { 2 } \phi } , \theta = \frac { \theta } { \sin ^ { 2 } \phi } , \theta = \frac { \theta } { \theta } , \theta = \frac { \theta } { \theta } , \theta = \frac { \pi } { \theta } , \theta = \frac { \pi } { \theta } , \theta = \frac { \theta } { \theta } , \theta = \frac { \pi } { \theta } , \theta = \frac { \theta } { \theta } , \theta = \frac { \pi } { \theta } , } } \\ { { \eta = \cos ^ { 2 } \left( \phi / 2 \right) = \frac { 1 + \sqrt { 1 - e ^ { 2 } } } { 2 ( 1 - e ^ { 2 } ) } , \theta = \frac { \cos ^ { 2 } \left( \phi / 2 \right) } { \cos ^ { 2 } \phi } = \frac { 1 + \sqrt { 1 - e ^ { 2 } } } { 2 ( 1 - e ^ { 2 } ) } , } } \end{array}
$$

the last summation is to be extended to all nonnegative integers $m ^ { \prime }$ ， $m ^ { \prime \prime }$ such that $m ^ { \prime } - m ^ { \prime \prime } = n$ If we put

$$
\left[ 1 - \frac { e } { 2 } \left( s + s ^ { - 1 } \right) \right] ^ { p } = \sum _ { n } H _ { n } ^ { p } s ^ { n } ,
$$

we obtain

$$
\begin{array} { l } { { b _ { n } = H _ { n } ^ { p } , \qquad c _ { n } = ( - 1 ) ^ { n } \theta ^ { 2 p } H _ { n } ^ { - p } , \qquad H _ { n } ^ { p } = \eta K _ { n } ^ { p } , } } \\ { { \nonumber } } \\ { { H _ { n } ^ { p } = H _ { - n } ^ { p } = ( - 1 ) ^ { n } \displaystyle \sum _ { m } \left( \frac { e } { 2 } \right) ^ { n + 2 m } C _ { p } ^ { n + 2 m } C _ { n + 2 m } ^ { m } . } } \end{array}
$$

Put $( \lambda , k ) = \Gamma ( \lambda + k ) / \Gamma ( \lambda )$ .The series

$$
F ( \alpha , \beta , \gamma , x ) = \sum _ { j = 0 } ^ { \infty } \frac { ( \alpha , j ) ( \beta , j ) } { ( \gamma , j ) ( 1 , j ) } x ^ { j }
$$

is absolutely and uniformly convergent not only within the circle $| x | < 1$ ， but also on the circumference of the circle $| { \boldsymbol x } | = 1$ if the real part

$$
\Re ( \alpha + \beta - \gamma )
$$

is less than zero,and defines a function of $_ x$ provided that we exclude negative integral values of $\gamma$ If $\pmb { \alpha }$ or $\beta$ is a negative integer, then there are only a finite number of terms in the series (l21).The series $F ( \alpha , \beta , \gamma , x )$ is called Gauss's hypergeometric series.A general hypergeometric series is of the form $x ^ { \kappa } ( 1 - x ) ^ { \lambda } F ( \alpha , \beta , \gamma , x )$ , with real numbers $\kappa$ and $\lambda$ ,and satisfies Gauss's differential equation

$$
x ( 1 - x ) \frac { d ^ { 2 } y } { d x ^ { 2 } } + \left[ \gamma - ( \alpha + \beta + 1 ) x \right] \frac { d y } { d x } - \alpha \beta \gamma y = 0 .
$$

The solution of this differential equation is represented by linear combinations of the four functions listed below:

$$
\begin{array} { r l } & { y _ { 1 } = F ( \alpha , \beta , \gamma , x ) = F ( \beta , \alpha , \gamma , x ) , } \\ & { y _ { 2 } = x ^ { 1 - \gamma } F ( \alpha - \gamma + 1 , \beta - \gamma + 1 , 2 - \gamma , x ) , } \\ & { y _ { 3 } = ( 1 - x ) ^ { \gamma - \alpha - \beta } F ( \gamma - \beta , \gamma - \alpha , \gamma , x ) , } \\ & { y _ { 4 } = x ^ { 1 - \gamma } ( 1 - x ) ^ { \gamma - \alpha - \beta } F ( 1 - \beta , 1 - \alpha , 2 - \gamma , x ) . } \end{array}
$$

There are 24 such contiguous functions including these four. Kummer divided them into groups of four functions according to their domain and saw that in any domain one of the corresponding functions is expressed

![](images/7a8ca78e4fb806e81d18e42c735f18ef5c792fc092693d07fa5d73f8ebf177ec.jpg)  
FIGURE 2

linearly by the rest of the four functions. For example,in the domain $| x | < 1$ we have $y _ { 1 } = y _ { 3 } , y _ { 2 } = y _ { 4 }$

Gauss made an integral representation of the solution of the differential equation (122) in the form (Schlesinger,1922; Klein,1933; Poole,1936; Goursat,1936;Bailey,1936)

$$
f ( x ) = { \frac { 1 } { 2 \pi { \sqrt { - 1 } } } } \int _ { c } w ^ { { \beta } - 1 } ( 1 - w ) ^ { \gamma - { \beta } - 1 } ( 1 - x w ) ^ { - { \alpha } } d w ,
$$

where the integration contour $C$ is the one taken twice around $x = 1$ in the positive and negative senses；that is, the integration contour is such that

$$
\Phi ( w ) = w ^ { \beta } ( 1 - w ) ^ { \gamma - \beta } ( 1 - x w ) ^ { - \alpha - 1 }
$$

recovers its initial value when we follow along the contour $C$ (Figure 2). Put $\phi ( w ) = w ^ { \beta - 1 } ( 1 - w ) ^ { \gamma - \beta - 1 } ( 1 - x w ) ^ { - \alpha }$ ,and assume that $\Re ( \beta ) > 0$ ， $\Re ( \gamma - \beta ) > 0$ ; then,

$$
f ( x ) = \frac { 1 } { 2 \pi \sqrt { - 1 } } \left\{ \int _ { 0 } ^ { 1 } + e ^ { 2 \pi \sqrt { - 1 } ( \gamma - \beta ) } \int _ { 1 } ^ { 0 } + e ^ { 2 \pi \sqrt { - 1 } \gamma } \int _ { 0 } ^ { 1 } \ d \right.
$$

$$
+ ~ e ^ { 2 \pi \sqrt { - 1 } \beta } \int _ { 1 } ^ { 0 } \biggr \} \phi ( w ) ~ d w .
$$

We carry out the integration by noticing that $\Gamma ( z ) \Gamma ( 1 - z ) = \pi / \sin \pi z$ ； then we obtain

$$
\int _ { 0 } ^ { 1 } w ^ { \beta - 1 } ( 1 - w ) ^ { \gamma - \beta - 1 } ( 1 - x w ) ^ { - \alpha } d w = B ( \beta , \gamma - \beta ) F ( \alpha , \beta , \gamma , x ) ,
$$

where the integration contour for $\begin{array} { r } { B ( \phi , q ) = \int _ { 0 } ^ { 1 } w ^ { p - 1 } ( 1 - w ) ^ { q - 1 } } \end{array}$ dw is $C _ { i }$ just defined,and $B ( \boldsymbol { p } , \boldsymbol { q } ) = \Gamma ( \boldsymbol { p } ) \Gamma ( \boldsymbol { q } ) / \Gamma ( \boldsymbol { p } + \boldsymbol { q } )$ (Nielsen,1906；Whittaker and Watson,l937). The expression (l25） is an analytic function of $\beta$ 、Hence, we can drop the restriction $\Re ( \beta ) > 0$ ,by the principle of analytic continuation.

The function $f ( { \pmb x } )$ defined by (l24) remains unaltered if we make one of the transformations

$$
\begin{array} { l } { w = 1 - w _ { 1 } , } \\ { w = \displaystyle \frac { w _ { 1 } } { 1 - x + w _ { 1 } x } , } \\ { w = \displaystyle \frac { 1 - w _ { 1 } } { 1 - w _ { 1 } x } . } \end{array}
$$

Thus, we obtain Euler's transformation of hypergeometric functions

$$
\begin{array} { l } { F ( \alpha , \beta , \gamma , x ) = ( 1 - x ) ^ { - \alpha } F \Big ( \alpha , \gamma - \beta , \gamma , \displaystyle \frac { x } { x - 1 } \Big ) } \\ { \qquad = ( 1 - x ) ^ { - \beta } F \Big ( \gamma - \alpha , \beta , \gamma , \displaystyle \frac { x } { x - 1 } \Big ) } \\ { \qquad = ( 1 - x ) ^ { \gamma - \alpha - \beta } F ( \gamma - \alpha , \gamma - \beta , \gamma , x ) . } \end{array}
$$

After this intermediary of hypergeometric functions we apply Cauchy's second theorem. The coefficient $X _ { j } ^ { n m }$ of the expansion of

$$
\left( { \frac { r } { a } } \right) ^ { n } \exp \left( { \sqrt { - 1 } } \ m v \right) = \sum _ { j = - \infty } ^ { \infty } X _ { j } ^ { n m } z ^ { j }
$$

is called Hansen’s coeffcent. It is given by

$$
X _ { j } ^ { n m } = { \frac { 1 } { 2 \pi { \sqrt { - 1 } } } } \int _ { c } ^ { ( 0 + ) } { \Big ( } { \frac { r } { a } } { \Big ) } ^ { n } t ^ { m } z ^ { - j - 1 } d z .
$$

Writing $S = ( r / a ) ^ { n } t ^ { m }$ in Cauchy's second theorem, we obtain

$$
X _ { j } ^ { n m } = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int _ { c } ^ { ( 0 + ) } \frac { s ^ { m } } { ( 1 + \omega ^ { 2 } ) ^ { n + 1 } } \left( 1 - \omega s \right) ^ { n - m + 1 } \biggr ( 1 - \frac { \omega } { s } \biggr ) ^ { n + m + 1 }
$$

$$
\times \exp \left[ \frac { j \omega } { 1 + \omega ^ { 2 } } \left( s - \frac { 1 } { s } \right) \right] s ^ { - j - 1 } d s ,
$$

$$
X _ { j } ^ { n m } = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int _ { c } ^ { ( 0 + ) } \frac { ( 1 - \omega ^ { 2 } ) ^ { 2 n + 3 } } { ( 1 + \omega ^ { 2 } ) ^ { 2 n + 1 } } t ^ { m } ( 1 + \omega t ) ^ { - n + j - 2 } \Big ( 1 + \frac { \omega } { t } \Big ) ^ { - n + j } 
$$

$$
\times \exp \left[ j \frac { 1 - \omega ^ { 2 } } { 1 + \omega ^ { 2 } } \left( \frac { \omega t } { 1 + \omega t } - \frac { \omega t ^ { - 1 } } { 1 + \omega t ^ { - 1 } } \right) \right] t ^ { - j - 1 } d t ,
$$

where the integration contour is the same as was presented in the proof of Cauchy's second theorem.

Put $j = 0$ ， $s = \omega \sigma$ in (127); then,

$$
\begin{array} { c } { { X _ { 0 } ^ { n m } = \displaystyle \frac { 1 } { 2 \pi \sqrt { - 1 } } \int _ { c } ^ { ( 0 + ) } \frac { ( - 1 ) ^ { n + m + 1 } } { ( 1 + \omega ^ { 2 } ) ^ { n + 1 } } } } \\ { { \times \omega ^ { m } \sigma ^ { - n - 2 } ( 1 - \sigma ) ^ { m + n + 1 } ( 1 - \omega ^ { 2 } \sigma ) ^ { n - m + 1 } d \sigma } } \end{array}
$$

We deform the integration contour so that the contour makes a single circuit around $\sigma = 0$ and a single circuit around $\sigma = 1$ ，both in the positive sense.We have $\sigma = 1 / \omega ^ { 2 } > 1$ . If we suppose that $n + 2 > 0$ and $n + m + 1 > 0$ ，then $\sigma = 1$ is not a pole; $\sigma = 0$ is the only pole, and a function is multiplied by exp $[ - 2 \pi \sqrt { - 1 } ~ ( n + 2 ) ]$ after a single circuit around $\sigma = 0$ Hence,

$$
\begin{array} { c } { { \begin{array} { c } { { \mathrm {  ~ \psi ~ } _ { 0 } ^ { r n m } = \displaystyle \frac { ( - 1 ) ^ { n + m + 1 } } { 2 \pi \sqrt { - 1 } } \frac { \omega ^ { m } } { ( 1 + \omega ^ { 2 } ) ^ { n + 1 } } \left\{ \exp \left[ - 2 \pi \sqrt { - 1 } \left( n + \frac { \omega } { 2 } \right) \right] \right. } } \end{array} } } \\ { { \begin{array} { r l } { { } } & { { } } \\ { { } } & { { \times \displaystyle \int _ { 0 } ^ { 1 } \sigma ^ { - n - 2 } ( 1 - \sigma ) ^ { n + m + 1 } ( 1 - \omega ^ { 2 } \sigma ) ^ { n - m + 1 } d \sigma } } \end{array} } } \\ { { \begin{array} { l } { { } } & { { } } \\ { { = \displaystyle \frac { ( - 1 ) ^ { m } \omega ^ { m } } { ( 1 + \omega ^ { 2 } ) ^ { n + 1 } } \frac { \Gamma ( m + n + 2 ) } { \Gamma ( n + 2 ) \Gamma ( m + 1 ) } } } \end{array} } } \\ { { \begin{array} { r l } { { } } & { { } } \\ { { \times { \cal F } ( m - n - 1 , - n - 1 , m + 1 , \omega ^ { 2 } ) . } } \end{array} } } \end{array}
$$

There are only a finite number of terms in the expansion of the hypergeometric function if $n + m + 1 > 0$ 、If we suppose that $n + 2 < 0$ and $n + m + 1 > 0$ ，then neither $\sigma = 0$ nor $\sigma = 1$ is a pole and $X _ { 0 } ^ { n m } = 0$ If $n + 2 < 0$ and $n + m + 1 < 0$ ,then $\sigma = 0$ is an ordinary point and $\sigma = 1$ is a singularity. Hence,

$$
\begin{array} { c } { { X _ { 0 } ^ { n m } = \displaystyle \frac { ( - 1 ) ^ { m } \omega ^ { m } ( 1 - \omega ^ { 2 } ) ^ { 2 n + 3 } } { ( 1 + \omega ^ { 2 } ) ^ { n + 1 } } \displaystyle \frac { \Gamma ( - n - 1 ) } { \Gamma ( m + 1 ) \Gamma ( - m - n - 1 ) } } } \\ { { \times F ( n + 2 , m + n + 2 , m + 1 , \omega ^ { 2 } ) . } } \end{array}
$$

Substitute in (l27) the expansion in Bessel functions

$$
\exp \left[ { \frac { j \omega } { 1 + \omega ^ { 2 } } } \left( s - { \frac { 1 } { s } } \right) \right] = \sum _ { p = - \infty } ^ { \infty } J _ { p } { \left( { \frac { 2 j \omega } { 1 + \omega ^ { 2 } } } \right) } s ^ { p }
$$

and put $s = \omega \sigma$ ; then,

$$
\begin{array} { l } { { \displaystyle X _ { j } ^ { n m } = \sum _ { p = - \infty } ^ { \infty } J _ { p } \Big ( \frac { 2 j \omega } { 1 + \omega ^ { 2 } } \Big ) X _ { j p } ^ { n m } } , } \\ { { \displaystyle X _ { j p } ^ { n m } = \frac { ( - 1 ) ^ { n + m + 1 } } { ( 1 + \omega ^ { 2 } ) ^ { n + 1 } } \frac { \omega ^ { m + p - j } } { 2 \pi \sqrt { - 1 } } } } \\ { { \displaystyle ~ \times \int _ { c } ^ { ( 0 + ) } \sigma ^ { p - n - j - 2 } ( 1 - \sigma ) ^ { n + m + 1 } ( 1 - \omega ^ { 2 } \sigma ) ^ { n - m + 1 } d \sigma } . } \end{array}
$$

$\mathrm { I f } ~ \hbar - \mathscr { n } - j - 2 < 0$ and $n + m + 1 > 0$ ,then,

$$
= ( - 1 ) ^ { p + m - j } { \frac { \omega ^ { m + p - j } } { ( 1 + \omega ^ { 2 } ) ^ { n + 1 } } } { \frac { \Gamma ( n + m + 2 ) } { \Gamma ( - p + n + j + 2 ) \Gamma ( p + m - j + 1 ) } }
$$

$$
\times F ( m - n - 1 , \boldsymbol { \phi } - n - j - 1 , \boldsymbol { \phi } + m - j + 1 , \omega ^ { 2 } ) ;
$$

and $F$ consists of a finite number of terms. If $\not { p } - \mathscr { n } - \dot { \mathscr { j } } - 2 > 0$ and $n + m + 1 > 0$ ,then $X _ { j p } ^ { n m } = 0$ .If

$$
\dot { p } - n - \dot { j } - 2 > 0 \quad \mathrm { a n d } \quad n + m + 1 < 0 ,
$$

then

$$
\begin{array} { l } { { X _ { j p } ^ { n m } = \displaystyle \frac { \omega ^ { m + p - j } } { ( 1 + \omega ^ { 2 } ) ^ { n - 1 } } \displaystyle \frac { \Gamma ( \phi - n - j - 1 ) } { \Gamma ( p + m - j + 1 ) \Gamma ( - n - m - 1 ) } \nonumber } } \\ { { \times F ( m - n - 1 , \phi - n - j - 1 , \phi + m - j + 1 , \omega ^ { 2 } ) , } } \end{array}
$$

or, if we use (123),

$$
\begin{array} { l } { { X _ { j p } ^ { n m } = \displaystyle \frac { \omega ^ { m + p - j } ( 1 - \omega ^ { 2 } ) ^ { 2 n + 3 } } { ( 1 + \omega ^ { 2 } ) ^ { n + 1 } } \displaystyle \frac { \Gamma ( \phi - n - j - 1 ) } { \Gamma ( p + m - j + 1 ) \Gamma ( - n - m - 1 ) } } } \\ { { \times \displaystyle F ( \phi + n - j + 2 , - m - n - 2 , \phi + m - j + 1 , \omega ^ { 2 } ) , } } \end{array}
$$

which consists of a finite number of terms.

We can deduce similar formulas from (l28) (Hagihara, 1935).

# 5.13EXPANSION IN SEVERAL VARIABLES

Let $C$ and $C ^ { \prime }$ be two simple integration contours around $z = x$ and $z ^ { \prime } = x ^ { \prime }$ ， respectively, in the complex plane of two complex variables $_ z$ and $z ^ { \prime }$ ; and let $F ( z , z ^ { \prime } )$ be a single-valued continuous analytic function inside $C$ and $C ^ { \prime }$ . Then by the residue theorem of Cauchy.in the theory of analytic functions (Goursat， 1913；Osgood,1914,1924；Behnke and Thullen,1934; Bochner and Martin,1948; Forsyth,1914; Picard,1926; Picard and Simart, 1897),we have

$$
\begin{array} { l } { { \displaystyle F ( x , x ^ { \prime } ) = \frac { 1 } { ( 2 \pi \sqrt { - 1 } ) ^ { 2 } } \int _ { c } d z \int _ { c ^ { \prime } } \frac { F ( z , z ^ { \prime } ) ~ d z ^ { \prime } } { ( z - x ) ( z ^ { \prime } - x ^ { \prime } ) } , } } \\ { { \displaystyle \frac { \partial ^ { m + n } F ( x , x ^ { \prime } ) } { \partial x ^ { m } \partial x ^ { \prime n } } = \frac { m ! n ! } { ( 2 \pi \sqrt { - 1 } ) ^ { 2 } } \int _ { c } d z \int _ { c ^ { \prime } } \frac { F ( z , z ^ { \prime } ) ~ d z ^ { \prime } } { ( z - x ) ^ { m + 1 } ( z ^ { \prime } - x ^ { \prime } ) ^ { n + 1 } } . } } \end{array}
$$

If a single-valued continuous analytic function $S ( z , z ^ { \prime } )$ can be expanded in the double Laurent series

$$
S ( z , z ^ { \prime } ) = \sum _ { j = - \infty } ^ { \infty } \sum _ { j ^ { \prime } = - \infty } ^ { \infty } P _ { j j ^ { \prime } } z ^ { j } z ^ { \prime j ^ { \prime } }
$$

such that $0 < r < | z | < R < \infty$ ， $0 < r ^ { \prime } < | z ^ { \prime } | < R ^ { \prime } < \infty$ ，then, according to this theorem, the coefficient $P _ { j j ^ { \prime } }$ can be written

$$
P _ { j j ^ { \prime } } = \frac { 1 } { ( 2 \pi \sqrt { - 1 } ) ^ { 2 } } { \int _ { c } ^ { ( 0 + ) } \int _ { c ^ { \prime } } ^ { ( 0 + ) } S ( z , z ^ { \prime } ) z ^ { - j - 1 } z ^ { \prime - j ^ { \prime } - 1 } d z d z ^ { \prime } } .
$$

If we transform by

$$
z = s \exp \left[ - \frac { e } { 2 } \left( s - s ^ { - 1 } \right) \right] , \qquad z ^ { \prime } = s ^ { \prime } \exp \left[ - \frac { e ^ { \prime } } { 2 } \left( s ^ { \prime } - s ^ { \prime - 1 } \right) \right] ,
$$

then

$$
P _ { j j ^ { \prime } } = \frac { 1 } { ( 2 \pi \sqrt { - 1 } ) ^ { 2 } } \int _ { c } ^ { ( 0 + ) } \int _ { c ^ { \prime } } ^ { ( 0 + ) } U s ^ { - j - 1 } s ^ { \prime - j ^ { \prime } - 1 } d s d s ^ { \prime } ,
$$

where

$$
\begin{array} { c } { { U = S \Bigg \{ 1 - \displaystyle \frac { e } { 2 } \left( s + s ^ { - 1 } \right) \Bigg \} \Bigg \{ 1 - \displaystyle \frac { e ^ { \prime } } { 2 } \left( s ^ { \prime } + s ^ { \prime - 1 } \right) \Bigg \} } } \\ { { \times \exp \Bigg \{ \displaystyle \frac { j e } { 2 } \left( s - s ^ { - 1 } \right) + \displaystyle \frac { j ^ { \prime } e ^ { \prime } } { 2 } \left( s ^ { \prime } - s ^ { \prime - 1 } \right) \Bigg \} . } } \end{array}
$$

This is an extension of Cauchy's first theorem to two variables (Hill, 1905).

Put $| z | = 1 , | z ^ { \prime } | = 1$ in these integration contours and write

$$
\begin{array} { l c r } { { z = \exp { ( \sqrt { - 1 } \ : M ) } , \ : \ : } } & { { z ^ { \prime } = \exp { ( \sqrt { - 1 } \ : M ^ { \prime } ) } , } } \\ { { \ : } } & { { \ : } } \\ { { s = \exp { ( \sqrt { - 1 } \ : u ) } , \ : \ : } } & { { s ^ { \prime } = \exp { ( \sqrt { - 1 } \ : u ^ { \prime } ) } ; } } \end{array}
$$

then,

$$
\begin{array} { l } { { P _ { j j ^ { \prime } } = \displaystyle \frac { 1 } { ( 2 \pi \sqrt { - 1 } ) ^ { 2 } } \int _ { 0 } ^ { 2 \pi } \int _ { 0 } ^ { 2 \pi } S \exp [ - \sqrt { - 1 } ( j M + j ^ { \prime } M ^ { \prime } ) ] d M d M ^ { \prime } } } \\ { { \ } } \\ { { \displaystyle = \frac { 1 } { 4 \pi ^ { 2 } } \int _ { 0 } ^ { 2 \pi } \int _ { 0 } ^ { 2 \pi } U \exp [ - \sqrt { - 1 } ( j u + j ^ { \prime } u ^ { \prime } ) ] d u d u ^ { \prime } , \qquad \displaystyle ( \mathscr { m } ^ { \prime } - \mathscr { G } ^ { \prime } - \mathscr { G } ^ { \prime } ) d u d w ^ { \prime } , \quad \mathrm { ~ \ } } } \end{array}
$$

$$
U = S ( 1 - \epsilon \cos u ) ( 1 - \epsilon ^ { \prime } \cos u ^ { \prime } ) \exp { [ \sqrt { - 1 } ( j \epsilon \sin u + j ^ { \prime } \epsilon ^ { \prime } \sin u ^ { \prime } ) }
$$

Hence, the coefficient of the double Fourier expansion of $s$ in the mean anomalies equals the corresponding coefficient in the double Fourier expansion of $U$ in the eccentric anomalies (Hagihara, 1936).

The relation between $s$ and $\boldsymbol { u }$ can be generalized in the form

$$
M = u - \alpha \phi ( u ) ,
$$

replacing the Kepler equation. Put

$$
\begin{array} { l } { { \displaystyle { \cal F } ( u , u ^ { \prime } ) = \sum _ { p = - \infty } ^ { \infty } \sum _ { p ^ { \prime } = - \infty } ^ { \infty } B _ { p p ^ { \prime } } \exp \left[ \sqrt { - 1 } \left( \rlap / p u + \rlap / p ^ { \prime } u ^ { \prime } \right) \right] } } \\ { { = \sum _ { q = - \infty } ^ { \infty } \sum _ { q ^ { \prime } = - \infty } ^ { \infty } A _ { q q ^ { \prime } } \exp \left[ \sqrt { - 1 } \left( q M + q ^ { \prime } M ^ { \prime } \right) \right] ; } } \end{array}
$$

then,

$$
\begin{array} { l } { { d _ { \mathrm { \tiny { \# z ^ { \prime } } } } = \displaystyle \frac { 1 } { 4 \pi ^ { 2 } } \int _ { 0 } ^ { 2 \pi } \int _ { 0 } ^ { 2 \pi } F ( u , u ^ { \prime } ) \exp \left[ - \sqrt { - 1 } \left( q M + q ^ { \prime } M ^ { \prime } \right) \right] d M d M ^ { \prime } } } \\ { { \ } } \\ { { \displaystyle = \frac { 1 } { 4 \pi ^ { 2 } } \int _ { 0 } ^ { 2 \pi } \int _ { 0 } ^ { 2 \pi } F ( u , u ^ { \prime } ) \frac { \partial ^ { 2 } \Psi } { \partial M \partial M ^ { \prime } } d M d M ^ { \prime } } } \\ { { \displaystyle = \frac { 1 } { 4 \pi ^ { 2 } q ^ { \prime } } \int _ { 0 } ^ { 2 \pi } \int _ { 0 } ^ { 2 \pi } \frac { \partial ^ { 2 } F } { \partial u \partial u ^ { \prime } } \Psi [ u - e \sin u , u ^ { \prime } - e ^ { \prime } \sin u ^ { \prime } ] d u d u ^ { \prime } } } \\ { { \displaystyle = \frac { 1 } { 4 \pi ^ { 2 } q \bar { q } ^ { \prime } } \sum _ { p = - \infty } ^ { \infty } \sum _ { \sigma } ^ { \infty } \sum _ { p ^ { \prime } = - \infty } ^ { p } p b \bar { p } ^ { \prime } B _ { p p ^ { \prime } } } } \end{array}
$$

where

$$
\Psi = - \frac { 1 } { q q ^ { \prime } } \exp { [ - \sqrt { - 1 } ( q M + q ^ { \prime } M ^ { \prime } ) ] } .
$$

Hence,

$$
A _ { q q ^ { \prime } } = \sum _ { p = - \infty } ^ { \infty } \sum _ { p ^ { \prime } = - \infty } ^ { \infty } \frac { p / p ^ { \prime } } { q q ^ { \prime } } B _ { p p ^ { \prime } } J _ { p - q } ( q \ell ) J _ { p ^ { \prime } - q ^ { \prime } } ( q ^ { \prime } e ^ { \prime } ) .
$$

In particular, if we denote the mutual distance of the two planets by $\Delta$ ，then $s ^ { 2 } s ^ { \prime } { } ^ { 2 } \Delta ^ { 2 } = R ( s , \ s ^ { \prime } )$ is a sextic integral polynomial in $\mathfrak { s }$ and $s ^ { \prime }$

(Hagihara，1936；Poincaré,1892，1907；Brown and Shook，1933;Andoyer 1923). Write

$$
\begin{array} { r } { F = \displaystyle \frac { 1 } { \Delta } = \sum _ { p = - \infty } ^ { \infty } \sum _ { p ^ { \prime } = - \infty } ^ { \infty } B _ { p p ^ { \prime } } \exp { [ \sqrt { - 1 } ( \rlap / { p } u + \rlap / { p ^ { \prime } } u ^ { \prime } ) ] } } \\ { = \displaystyle \sum _ { q = - \infty } ^ { \infty } \sum _ { q ^ { \prime } = - \infty } ^ { \infty } A _ { q q ^ { \prime } } \exp { [ \sqrt { - 1 } ( q M + q ^ { \prime } M ^ { \prime } ) ] } ; } \end{array}
$$

then

$$
B _ { p p ^ { \prime } } = - \frac { 1 } { 4 \pi ^ { 2 } } \int _ { C } \int _ { C ^ { \prime } } \frac { d s d s ^ { \prime } } { s ^ { p } s ^ { \prime } ^ { p ^ { \prime } } \sqrt { R ( s , s ^ { \prime } ) } } ,
$$

and

$$
\begin{array} { l } { { \displaystyle { \cal A } _ { q q ^ { \prime } } = \frac { 1 } { 4 \pi ^ { 2 } q q ^ { \prime } } \int _ { 0 } ^ { 2 \pi } \int _ { 0 } ^ { 2 \pi } \frac { \partial ^ { 2 } ( 1 / \Delta ) } { \partial u \partial u ^ { \prime } } \exp \left[ - \sqrt { - 1 } ( q M + q ^ { \prime } M ^ { \prime } ) \right] d u d u ^ { \prime } } } \\ { { \mathrm { } ~ = ~ - \displaystyle { \frac { 1 } { 4 \pi ^ { 2 } q q ^ { \prime } } } \int _ { c } \int _ { c ^ { \prime } } \frac { \partial ^ { 2 } ( 1 / \Delta ) } { \partial s \partial s ^ { \prime } } \exp \left[ \sqrt { - 1 } q e \sin u + \sqrt { - 1 } q ^ { \prime } e ^ { \prime } \sin u \right. } } \\ { { \mathrm { } ~ \times \displaystyle { \frac { d s } { s ^ { q } } } \frac { d s ^ { \prime } } { s ^ { \prime \prime } } } } \end{array}
$$

This expression has been used by Poincaré (Poincaré, l898;Féraud, 1902).

In extending the theorems from two variables to more than two variables, put

$$
\exp \left[ \sum _ { j = 1 } ^ { k } \frac { z _ { j } } { 2 } \left( t ^ { j } - t ^ { - j } \right) \right] = \sum _ { n = - \infty } ^ { \infty } J _ { n } ( z _ { 1 } , . . . , z _ { k } ) t ^ { n } ;
$$

then the Laurent series converges in a ring domain excluding $t = 0$ and $\infty$ Write $t ^ { - 1 }$ in the place of $t$ and add or subtract the corresponding expressions,and put $t = \exp { ( - \sqrt { - 1 } \phi ) }$ ; then

$$
\begin{array} { l } { { { \displaystyle \cos \left( \sum _ { j = 1 } ^ { k } z _ { j } \sin j \phi \right) = \sum _ { n = 1 } ^ { \infty } \left[ J _ { n } ( z _ { 1 } , . . . , z _ { k } ) + J _ { - n } ( z _ { 1 } , . . . , z _ { k } ) \right] \cos n \phi } , } } \\ { { { \displaystyle \sin \left( \sum _ { j = 1 } ^ { k } z _ { j } \sin j \phi \right) = \sum _ { n = 1 } ^ { \infty } \left[ J _ { n } ( z _ { 1 } , . . . , z _ { k } ) - J _ { - n } ( z _ { 1 } , . . . , z _ { k } ) \right] \sin n \phi } } } \end{array}
$$

Differentiate the first equation of (l39) with respect to $\phi$ and put $\phi = 0$ ； then,

$$
\sum _ { j = 1 } ^ { k } j z _ { j } = \sum _ { n = 1 } ^ { \infty } n [ J _ { n } ( z _ { 1 } , . . . , z _ { k } ) - J _ { - n } ( z _ { 1 } , . . . , z _ { k } ) ] .
$$

Put $\phi = 0$ before carrying out the differentiation; then

$$
1 = \sum _ { n = \infty } ^ { \infty } J _ { n } ( z _ { 1 } , . . . , z _ { k } ) { \mathrm { . } }
$$

Multiply (l38) by the expression obtained by replacing $t$ by $t ^ { - 1 }$ in (138); then

$$
1 = C _ { 0 } + \sum _ { n = 1 } ^ { \infty } C _ { n } t ^ { n } + \sum _ { n = 1 } ^ { \infty } C _ { - n } t ^ { - n } .
$$

This relation is independent of $t .$ Hence, $C _ { 0 } = 1 , \ C _ { n } = 0 , \ C _ { - n } = 0$ $( n = 1 , 2 , \ldots )$ . The relation $C _ { 0 } = 1$ is explicitly written

$$
1 = \sum _ { n = - \infty } ^ { \infty } J _ { n } ^ { 2 } ( z _ { 1 } , . . . , z _ { k } ) .
$$

Hence

$$
| J _ { n } ( z _ { 1 } , . . . , z _ { k } ) | < 1 .
$$

Multiply both sides of (138) by exp $( - \sqrt { - 1 } ~ 2 \pi \phi ) ~ d \phi / 2 \pi _ { ; }$ , integrate with respect to $\phi$ , and take the real part; then

$$
J _ { n } ( z _ { 1 } , \ldots , z _ { k } ) = { \frac { 1 } { \pi } } \int _ { 0 } ^ { \pi } \cos \left( n \phi - \sum _ { j = 1 } ^ { k } z _ { j } \sin j \phi \right) d \phi .
$$

When there is only one $_ z$ , this reduces to the usual integral representation of Bessel functions.Put

$$
y _ { k - 1 } = m \phi - \sum _ { j = 1 } ^ { k - 1 } z _ { j } \sin j \phi ;
$$

then (140） becomes

$$
\begin{array} { r } { J _ { m } ( z _ { 1 } , . . . , z _ { k } ) = \displaystyle \frac { 1 } { \pi } \int _ { 0 } ^ { \pi } \cos y _ { k - 1 } \cos \left( z _ { k } \sin k \phi \right) d \phi } \\ { + \displaystyle \frac { 1 } { \pi } \int _ { 0 } ^ { \pi } \sin y _ { k - 1 } \sin \left( z _ { k } \sin k \phi \right) d \phi . } \end{array}
$$

By the usual expansion formulas in Bessel functions,

$$
{ \frac { 1 } { \pi } } \int _ { 0 } ^ { \pi } \cos y _ { k - 1 } d \phi = J _ { m } ( z _ { 1 } , . . . , z _ { k - 1 } ) ,
$$

$$
{ \frac { 1 } { \pi } } \int _ { 0 } ^ { \pi } 2 \cos y _ { k - 1 } \cos 2 j k \phi d \phi
$$

$$
\begin{array} { c } { { \displaystyle = \displaystyle \frac { 1 } { \pi } \int _ { 0 } ^ { \pi } \cos \left[ y _ { k - 1 } - 2 j k \phi \right] + \displaystyle \frac { 1 } { \pi } \int _ { 0 } ^ { \pi } \cos \left( y _ { k - 1 } + 2 j k \phi \right) d \phi } } \\ { { \mathrm { } } } \\ { { \mathrm { } = J _ { m - 2 j k } ( z _ { 1 } , . . . , z _ { k - 1 } ) + J _ { m + 2 j k } ( z _ { 1 } , . . . , z _ { k - 1 } ) , } } \\ { { \displaystyle \frac { 1 } { \pi } \int _ { 0 } ^ { \pi } 2 \sin y _ { k - 1 } \sin \left( 2 j - 1 \right) k \phi d \phi } } \end{array}
$$

$$
\begin{array} { l } { { \displaystyle = \frac { 1 } { \pi } \int _ { 0 } ^ { \pi } \cos \left[ y _ { k - 1 } - ( 2 j - 1 ) k \phi \right] d \phi } } \\ { { \mathrm { } } } \\ { { \displaystyle \quad - \frac { 1 } { \pi } \int _ { 0 } ^ { \pi } \cos \left[ y _ { k - 1 } + ( 2 j - 1 ) k \phi \right] d \phi } } \\ { { \mathrm { } } } \\ { { \displaystyle = J _ { m - ( 2 j - 1 ) k } \big ( z _ { 1 } , . . . , z _ { k - 1 } \big ) - J _ { m + ( 2 j - 1 ) k } \big ( z _ { 1 } , . . . , z _ { k - 1 } \big ) . } } \end{array}
$$

From these expressions we obtain

$$
\begin{array} { l } { { z _ { k } ) = { \cal J } _ { 0 } ( z _ { k } ) { \cal J } _ { m } ( z _ { 1 } , \dots , z _ { k - 1 } ) } } \\ { { \displaystyle ~ + \sum _ { j = 1 } ^ { \infty } { \cal J } _ { 2 j } ( z _ { k } ) [ { \cal J } _ { m - 2 j k } ( z _ { 1 } , \dots , z _ { k - 1 } ) + { \cal J } _ { m + 2 j k } ( z _ { 1 } , \dots , z _ { k - 1 } ) ] } } \\ { { \displaystyle ~ + \sum _ { j = 1 } ^ { \infty } { \cal J } _ { 2 j - 1 } ( z _ { k } ) [ { \cal J } _ { m - ( 2 j - 1 ) k } ( z _ { 1 } , \dots , z _ { k - 1 } ) } } \\ { { \displaystyle ~ - { \cal J } _ { m + ( 2 j - 1 ) k } ( z _ { 1 } , \dots , z _ { k - 1 } ) ] . } } \end{array}
$$

But, since

$$
J _ { 2 j } ( z _ { k } ) = J _ { - 2 j } ( z _ { k } ) , J _ { 2 j - 1 } ( z _ { k } ) = - J _ { - ( 2 j - 1 ) } ( z _ { k } ) ,
$$

therefore

$$
J _ { m } ( z _ { 1 } , . . . , z _ { k } ) = \sum _ { q _ { k } = - \infty } ^ { \infty } J _ { m - k q _ { k } } ( z _ { 1 } , . . . , z _ { k - 1 } ) J _ { q _ { k } } ( z _ { k } ) .
$$

By repeated use of this formula we finally obtain

$$
\begin{array} { r l r } {  { z _ { 1 } , z _ { 2 } , \dots , z _ { k } ) } } \\ & { = \sum _ { q _ { 2 } = - \infty } ^ { \infty } \sum _ { q _ { 3 } = - \infty } ^ { \infty } \quad } & { \sum _ { q _ { k } = - \infty } ^ { \infty } \ J _ { m - 2 q _ { 2 } - 3 q _ { 3 } - \cdots - k q _ { k } } ( z _ { 1 } ) J _ { q _ { 2 } } ( z _ { 2 } ) \cdot \cdot \cdot J _ { q _ { k } } ( z _ { k } ) . } \end{array}
$$

This formula has been deduced by Jekhowsky (Appell,1915,1924,1925; Peres,1915; Akimoff, 1915,1916,1917,1924,1927; Jekhowsky,1916, 1916a,1917,1917a,1918,1920,1921,1927; Humbert,1920).

We now expand $y = \cos { \beta v }$ with respect to $_ x$ ， such that

$$
x = v - \sum _ { j = 1 } ^ { k } \alpha _ { j } \sin j v ,
$$

with $| \alpha _ { 1 } | + | \alpha _ { 2 } | + \cdot \cdot \cdot + | \alpha _ { k } | < 1$ Since $y$ remains unaltered as u increases by $2 \pi / \hbar$ ，the function $y$ is periodic.By the Weierstrassian approximation theorem of Section 2.6,it can be written

$$
y = { \scriptstyle { \frac { 1 } { 2 } } } A _ { 0 } + \sum _ { j = 1 } ^ { \infty } A _ { j } \cos j x ,
$$

where

$$
\begin{array} { l } { { A _ { j } \displaystyle = \frac { 2 } { \pi } \int _ { 0 } ^ { \pi } \cos { \phi } v \cos { j x } d x = \frac { 2 \phi } { \pi j } \int _ { 0 } ^ { \pi } \sin { \beta v } \sin { j \left( v - \sum _ { i = 1 } ^ { k } \alpha _ { i } \sin i v \right) } d v } } \\ { { \displaystyle \phantom { \frac { 2 p } { \pi j } = \frac { 2 } { \pi j } \int _ { 0 } ^ { \pi } \cos { \beta v } \cos { j x } d x } = \frac { \beta } { j } \left[ J _ { j - p } ( j \alpha _ { 1 } , j \alpha _ { 2 } , . . . , j \alpha _ { k } ) - J _ { j + p } ( j \alpha _ { 1 } , . . . , j \alpha _ { k } ) \right] . } } \end{array}
$$

Hence,

$$
( \pm \sqrt { - 1 } p v ) = - \frac { \hbar \alpha _ { p } } { 2 } \pm \hbar \sum _ { j = - \infty } ^ { \infty } J _ { j \mp p } ( j \alpha _ { 1 } , . . . , j \alpha _ { k } ) \frac { 1 } { j } \exp { ( \sqrt { - 1 } j x ) } ,
$$

where in the summation $j = 0$ is excluded. Put $z = \exp { ( \sqrt { - 1 } \upsilon ) }$ ， $s = \exp { ( \sqrt { - 1 } \boldsymbol { x } ) }$ ; then Cauchy's theorem is generalized; that is, the coefficient $P _ { j }$ in the expansion

$$
S = \sum _ { j = - \infty } ^ { \infty } P _ { j } z ^ { j }
$$

equals the coefficient of $s ^ { j }$ in the expansion of

$$
U = S \left[ 1 - \frac { 1 } { 2 } \sum _ { l = 1 } ^ { k } l \alpha _ { l } ( s ^ { l } + s ^ { - l } ) \right] \exp { \left[ \frac { j } { 2 } \sum _ { l = 1 } ^ { k } \alpha _ { l } ( s ^ { l } - s ^ { - l } ) \right] } ,
$$

or equals the coefficient of $s ^ { j }$ in the expansion of

$$
V = \frac { 1 } { j } \frac { d S } { d s } s \exp \bigg [ \frac { j } { 2 } \sum _ { l = 1 } ^ { k } \alpha _ { l } \big ( s ^ { l } - s ^ { - l } \big ) \bigg ] ,
$$

where $_ z$ and $\pmb { \mathscr { s } }$ are related by

$$
z = s \exp \left[ - { \textstyle \frac { 1 } { 2 } } \sum _ { j = 1 } ^ { k } \alpha _ { j } \big ( s ^ { j } - s ^ { - j } \big ) \right] .
$$

In the case where the number of variables in the arguments of the Bessel functions is infinite, it is sufficient to have

$$
| z _ { 1 } | + 2 | z _ { 2 } | + 3 | z _ { 3 } | + \cdot \cdot \cdot + k | z _ { k } | + \cdot \cdot \cdot < 1
$$

in order that the series be convergent. The formulas obtained in the foregoing study are also true for $k \to \infty$ . Such expansions were used by Hagihara (l93l） for the expansions of quasi-periodic motions in the relativistic trajectories of Sections 2.15-2.20.

# 5.14RADIUS OF CONVERGENCE

In Kepler's equation (101),

$$
u - e \sin u = M ,
$$

the singularity occurs when

$$
\frac { d M } { d u } = 1 - e \cos u = 0 \qquad ( 0 < \ell < 1 ) ,
$$

in representing the series for $\boldsymbol { u }$ as power series in $M$ or in $t \mathrm { ~ - ~ } t _ { 0 }$

On the complex plane for $u = z + \sqrt { - 1 } w$ we erect a rectangle of very large height between $z = m \pi$ and $z = ( m + 1 ) \pi$ perpendicular to the real axis.Briot and Bouquet (l875) proved that there are two roots in the rectangle for an even positive or an odd negative integer $_ m$ and no root for an odd positive or an even negative integer $\pmb { m }$ . For a very large $_ m$ the two roots whose real parts are between $2 m \pi$ and $( 2 m + 1 ) \pi$ tend asymptotically（Goursat,1913） to

$$
2 m \pi + \frac { \pi } { 2 } \pm \sqrt { - 1 } \left[ \log \left( \frac { 2 } { e } \right) + \log \left( 2 m \pi + \frac { \pi } { 2 } \right) \right] .
$$

Moulton (1903) computed the singular points in the complex $\pmb { u }$ -plane to be

$$
z = 2 m \pi , w = \pm \cosh ^ { - 1 } \left( { \frac { 1 } { e } } \right) = \pm \log { \frac { 1 + \sqrt { 1 - e ^ { 2 } } } { e } }
$$

with an integer m. Using an arbitrary value $e _ { 0 }$ of $\pmb { \ell }$ $( 0 \leq e _ { 0 } \leq 1 _ { . } $ as center, draw a circle through the nearest singular point. The radius of this circle is the convergence radius of the expansion at $\ell = e _ { 0 }$

In Lagrange's theorem of Section 2.3 put

$$
t = e , \phi ( z ) = \sin u , z = u , a = M ;
$$

then a function $f$ of $\pmb { u }$ with Kepler's equation $\boldsymbol { u } = \boldsymbol { M } + e \sin \boldsymbol { u }$ can be expanded in an integral power series of $\pmb { e }$ in the form

$$
\begin{array} { l } { { \displaystyle f ( u ) = f ( M ) + \frac { e } { 1 } f ^ { \prime } ( M ) \sin M + \cdot \cdot \cdot } } \\ { { \displaystyle ~ + \frac { e ^ { n + 1 } } { ( n + 1 ) ! } \frac { \partial ^ { n } } { \partial M ^ { n } } [ f ^ { \prime } ( M ) \sin ^ { n + 1 } M ] + \cdot \cdot \cdot , } } \end{array}
$$

where

$$
| e \sin u | < | u - M | .
$$

Suppose that $\pmb { \ell }$ and $M$ are given, what is the maximum value of $\pmb { e }$ satisfying this inequality? Let

$$
u = M + \epsilon { \mathrm { s i n } } u { \mathrm { \equiv } } M + \rho \cos \chi + { \sqrt { - 1 } } \rho { \mathrm { s i n } } \chi , = 1 \omega
$$

where ${ \boldsymbol { \rho } } =$ constant. From this,

$$
\begin{array} { r l } & { \sin u = \sin ( M + \rho \cos \chi ) \cosh ( \rho \sin \chi ) } \\ & { \qquad + \sqrt { - 1 } \cos ( M + \rho \cos \chi ) \sinh ( \rho \sin \chi ) . } \end{array}
$$

Hence,

$$
| \sin u | ^ { 2 } = \cos h ^ { 2 } ( \rho \sin \chi ) - \cos ^ { 2 } ( M + \rho \cos \chi ) .
$$

The worst situation occurs when $| \sin u |$ is a maximum, that is,when

$$
\sin \chi = \pm 1 , \qquad \cos \left( M + \rho \cos \chi \right) = 0 ,
$$

or $\chi = \pm \pi / 2 ,$ $M = \pm \pi / 2$ ，or when $| \sin u | = \cosh \rho .$ Hence， the required condition is

$$
\displaystyle e < \frac { \rho } { \cosh \rho } .
$$

The maximum of $\rho / { \mathsf { c o s h } } \ \rho$ occurs when

$$
\frac { d } { d \rho } \left( \frac { \rho } { \cosh \rho } \right) = 0 ,
$$

or when cosh $\rho = \rho$ sinh $\pmb { \rho }$ or

$$
\exp \left( 2 \rho \right) = \frac { \rho + 1 } { \rho - 1 } ,
$$

that is, $\rho = 1 . 9 9 6 7 \cdots$ Thus, if

$$
\ell = \tan \alpha = \frac { \rho } { \cosh \rho } = \frac { 1 } { \sinh \rho } ,
$$

then the maximum occurs when $\alpha = 3 3 ^ { \circ } 3 2 ^ { \prime } 3 . 0$ $e = { 0 . 6 6 2 7 4 3 4 \cdot \cdot \cdot } .$ For any value of $\boldsymbol { e }$ larger than this critical value,any expansion for an elliptic motion as an integral power series of $e$ is not convergent. This finding is due to Laplace.

Now compute the radius of convergence of an integral power series of $e$ in the complex $e$ plane,by putting $e = g + { \sqrt { - 1 } } h$ according to Charlier (1927).The singularities in the $M$ plane are obtained by the two equations

$$
\frac { d M } { d u } = 1 - e \cos { u } = 0 ,
$$

$$
u - M - e \sin u = 0 .
$$

Since

$$
\begin{array} { l } { { \displaystyle \exp \left( \sqrt { - 1 } \ : u \right) = \frac 1 \epsilon \ : \mp \ : \sqrt { \frac { 1 } { \epsilon ^ { 2 } } - 1 } = \exp \left[ \sqrt { - 1 } \ : e \sin u + \sqrt { - 1 } \ : M \right] } } \\ { { \mathrm { } } } \\ { { \mathrm { } = \exp \left[ \mp \sqrt { 1 - e ^ { 2 } } + \sqrt { - 1 } \ : M \right] , } } \end{array}
$$

the singularities are defined by

$$
\gamma = 1 \mp \sqrt { 1 - e ^ { 2 } } = e \exp { [ \mp \sqrt { 1 - e ^ { 2 } } + \sqrt { - 1 } M ] } ,
$$

or by

$$
\left( { \frac { 2 } { \gamma } } - 1 \right) \exp { ( 2 \gamma ) } = \exp { ( 2 + 2 { \sqrt { - 1 } } M ) } .
$$

Write $\gamma = \epsilon \exp \left( { \sqrt { - 1 } } \theta \right)$ and take the real and imaginary parts; then the singularities are at the intersections of the two curves

$$
\frac { 4 } { \epsilon ^ { 2 } } + 1 - \frac { 4 } { \epsilon } \cos \theta = \exp \big ( 4 - 4 \epsilon \cos \theta \big ) ,
$$

$$
{ \frac { 2 } { \epsilon } } \sin \left( 2 M + \theta - 2 \epsilon \sin \theta \right) - \sin \left( 2 M - 2 \epsilon \sin \theta \right) = 0 .
$$

These equations are satisfied by

$$
\epsilon \cos \theta = 1 , \qquad \theta - \tan \theta = { \frac { n \pi } { 2 } } - M \qquad ( n = 0 , \pm 1 , \pm 2 , . . . ) ,
$$

which define a set of parallel straight lines. Other solutions are obtained by the equations

$$
\mu ^ { 2 } = \frac { 4 ( 1 - \lambda ) } { \exp \left[ 4 ( 1 - \lambda ) \right] - 1 } - \lambda ^ { 2 } ,
$$

$$
( \lambda - 1 ) ^ { 2 } = 1 + 2 \mu \cot { ( 2 M - 2 \mu ) } - \mu ^ { 2 } ,
$$

![](images/fa5936cf267e8a9653d4c639318c3217cd5c9a69195ebe9f43ec430fb08c1be1.jpg)  
FIGURE 3

where $\lambda = \epsilon \cos \theta _ { ; }$ $\mu = \epsilon \sin \theta$ ，The solution $\lambda = 1$ corresponds to the set of straight lines just obtained.We can make the curve defined by the second equation of (l45) pass through any point of the curve defined by the first equation. If we denote such values of $\lambda , \mu$ by $\lambda _ { 0 } , \mu _ { 0 }$ ,respectively, then the second equation is satisfied by

$$
\tan { ( 2 M - 2 \mu _ { 0 } ) } = \frac { 2 \mu _ { 0 } } { ( \lambda _ { 0 } - 1 ) ^ { 2 } - 1 + \mu _ { 0 } } \cdot
$$

Take the real and the imaginary parts of this equation by puting

$$
e = g + { \sqrt { - 1 } } h = \kappa \exp \left( { \sqrt { - 1 } } \tau \right) ;
$$

then,

$$
\begin{array} { l } { { \kappa ^ { 2 } \cos 2 \tau = - ( 1 - \lambda ) ^ { 2 } + \mu ^ { 2 } + 1 , } } \\ { { \nonumber } } \\ { { \kappa ^ { 2 } \sin 2 \tau = 2 \mu ( 1 - \lambda ) . } } \end{array}
$$

The solution $\lambda = 1$ corresponds to the part of the $g$ axis in the $\pmb { \ell }$ plane such that $\left| g \right| \geq 1$ . The points for $M = \pi / 2$ corresponds to point $B$ in Figure 3. Put $e = { \sqrt { - 1 } } h$ ; then,

$$
1 + { \sqrt { 1 + h ^ { 2 } } } = h \exp { ( { \sqrt { 1 + h ^ { 2 } } } ) }
$$

and the solution is $h = 0 . 6 6 2 7 \cdots$ ,which is equal to the maximum value of $\pmb { \ell }$ obtained in the foregoing discussion by means of Lagrange's theorem. All points on the singular curve of Figure 3 are singular points of the integral (l44) which is expanded in the form

$$
u = a _ { 0 } + a _ { 1 } ( e - e _ { 0 } ) + a _ { 2 } ( e - e _ { 0 } ) ^ { 2 } + \cdot \cdot \cdot .
$$

The curve is the envelope of a one-parameter family of convergence circles with variable'radius centered at the point $\scriptstyle { e _ { 0 } }$

Elenevskaya (1953,1957,1962) has writen the equations (144), by separating the real and imaginary parts

$$
u = x + { \sqrt { - 1 } } y , \qquad e = \xi + { \sqrt { - 1 } } \eta ,
$$

in the form

$$
\begin{array} { c } { { M - x = - \xi \sin x \cosh y + \eta \cos x \sinh y , } } \\ { { \ } } \\ { { y = \xi \cos x \sinh y + \eta \sin x \cosh y , } } \\ { { \ } } \\ { { 1 = \xi \cos x \cosh y + \eta \sin x \sinh y , } } \\ { { \ } } \\ { { 0 = \xi \sin x \sinh y - \eta \cos x \cosh y . } } \end{array}
$$

The intersections of these two curves in the $( \xi , \eta )$ -plane define the singular points of the integral of the first equation of (l44). The coordinates of these singular points are obtained from the last three equations of (146):

$$
\xi = \frac { y \cos x } { \sinh y } , \qquad \eta = \frac { y \sin x } { \cosh y } ,
$$

or

$$
\sin ^ { 2 } x = { \frac { y \cosh ^ { 2 } y - \sinh y \cosh y } { y } } \cdotp
$$

$_ { x }$ is a multi-valued function of $y$ ,but $\xi$ and $\eta$ are determined uniquely:

$$
\begin{array} { l } { { \displaystyle \xi ^ { 2 } = \frac { y } { \sinh y } \big ( \cosh y - y \sinh y \big ) \mathrm { , } } } \\ { { \displaystyle } } \\ { { \eta ^ { 2 } = \frac { y } { \cosh y } \big ( y \cosh y - \sinh y \big ) \mathrm { . } } } \end{array}
$$

It can be shown that there are no singular points other than those lying on this curve and that any point on this curve is a singular point of the integral.

Elenevskaya (l962） extended the discussion to hyperbolic motions. She derived

$$
\begin{array} { r l r } & { } & { M + x = \xi \sinh x \cos y - \eta \cosh x \sin y , } \\ & { } & { y = \xi \cosh x \sin y + \eta \sinh x \cos y , } \\ & { } & { 1 = \xi \cosh x \cos y - \eta \sinh x \sin y , } \\ & { } & { 0 = \xi \sinh x \sin y + \eta \cosh x \cos y . } \end{array}
$$

![](images/3c988bc206773fe54f9d14330240ea76a31a55f5168000385fc7b2609bc23bd2.jpg)  
FIGURE 4

From these we obtain

$$
\xi = \frac { y \cosh x } { \sin y } , \qquad \eta = \frac { y \sinh x } { \cos y } ,
$$

$$
\cosh ^ { 2 } x = { \frac { y \sin ^ { 2 } y + \sin y \cos y } { y } } \cdotp
$$

With $_ x$ eliminated, the equation of the singular curve for a hyperbolic motion is

$$
\xi ^ { 2 } = \frac { y } { \sin y } ( y \sin y + \cos y ) , \qquad \eta ^ { 2 } = \frac { y } { \cos y } ( \sin y - y \cos y ) .
$$

Figure 4 shows the singular curve.

If $\pmb { e }$ is close to unity, then it is possible to accelerate the convergence by restricting the range of variation of the angular variable. Such an expansion is made in powers of $( 1 - e )$ . We have obtained the singular curve for an elliptic motion by considering (l46) as a system of equations describing two curves and selecting from these the one independent of $M .$ The convergence circle of the expansion of the eccentric anomaly $\pmb { u }$ is centered at the point $\boldsymbol { e } _ { 0 }$ ; this point is inside the singular curve.The point of contact between the convergence circle and the singular curve is the point at which the singular curve is intersected by another curve independent of $M$ and determined by (l46). For a given value of $M _ { i }$ the power series in $( e \mathrm { ~ - ~ } e _ { 0 } )$ will cease to be convergent when $e$ is outside the circle in contact with the singular curve at this point.

When $\boldsymbol e _ { 0 } = 1$ , the expansion of $\boldsymbol { u }$ in powers of $1 \textrm { -- } e$ becomes divergent for any value of $e$ except l, at least for some values of the mean anomaly $M .$ In order to determine the values of $M$ for which the divergence of the expansion does not take place, draw a circle of an arbitrary radius about a point $\boldsymbol { e } _ { 0 } = 1$ . This circle intersects the singular curve at two points.For convenience, consider only the part of the curve lying in the first quadrant.Each point of intersection of the circle with the singular curve is a point at which the singular curve is intersected by one of the family of curves depending on $M$ and determined by (l46).We call this latter curve the anomaly curve. This point gives the value of $M$ for which the expansion of $\pmb { u }$ in powers of $1 \ : - \ : e$ is no longer convergent. Denote this value of $M$ by $M _ { c r }$

Consider the family of curves governed by (l46) in order to determine the functional relation $M _ { c r } = \phi ( e )$ . From (146) we obtain

$$
\xi = \frac { ( x - M ) \cosh y } { \sin x } , \qquad \eta = \frac { ( x - M ) \sinh y } { \cos x } ,
$$

so that the relation between $_ x$ and $y$ is

$$
\sinh ^ { 2 } y = \frac { \sin x \cos x - ( x - M ) \cos ^ { 2 } x } { x - M } .
$$

This shows that $y$ is a multi-valued function of $_ x$ .We choose one region in which $e = \psi ( u )$ is single-valued, that is,the strip of width

$$
- \infty < y < + \infty , \qquad - \pi \leq x \leq \pi .
$$

From (147a) and (148) we obtain the family of anomaly curves

$$
\begin{array} { l } { { \displaystyle \xi ^ { 2 } = \frac { x - M } { \sin x } [ \cos x + ( x - M ) \sin x ] } , } \\ { { \displaystyle } } \\ { { \displaystyle \eta ^ { 2 } = \frac { x - M } { \cos x } [ \sin x - ( x - M ) \cos x ] } . } \end{array}
$$

![](images/df5959abf591486ce60c66c5c1b27d7ee987d8ef0387020b620dba768035a091.jpg)  
FIGURE 5

The curve of this family for $M = 0$ is identical with the singular curve of a hyperbolic motion.It shows that when $M = 0$ the expansion of $\pmb { u }$ in powers of $( 1 ~ - ~ e )$ is divergent for any value of $\boldsymbol { e }$ .Figure 5 shows the family of anomaly curves. For $M = \pi / 2$ the anomaly curve degenerates into'the straight line $\xi = 0$ ,since $_ x$ in the case can only take the value $\pi / 2$ .Hence, for $M > \pi / 2$ , the expansion of $\boldsymbol { \mathscr { u } }$ will be convergent for any value of $\pmb { e }$

Draw a circle of radius $R$ about the point $e _ { 0 } = 1$ ; at some point it touches one of the anomaly curves. Moreover, the curves of the family for which $M \leq M _ { c r }$ intersect the circle,but the curves for which $M > M _ { c r }$ remain outside the circle.The intersection of the circle with the $\xi$ -axis gives the real part of $e _ { \scriptscriptstyle \mathrm { : } }$ ,for which the expansion of $\boldsymbol { u }$ in powers of $1 \textrm { -- } e$ is convergent for all $M > M _ { c r }$ and divergent for all $M \leq M _ { c r }$ ，since for a given value of $\pmb { \ell }$ these points of contact are the singular points of the equation $u - M - e \sin u = 0 .$

Similarly, equations (147b) for a hyperbolic motion give

$$
\begin{array} { l } { { \displaystyle \xi ^ { 2 } = \frac { M + x } { \sinh x } [ \cosh x - ( M + x ) \sinh x ] , } } \\ { { \displaystyle \eta ^ { 2 } = \frac { M + x } { \cosh x } [ - \sinh x + ( M + x ) \cosh x ] . } } \end{array}
$$

![](images/d550f34d4cb32b22ba493d2b9f28d46583daad77e4cac060d989ffa807b57a80.jpg)  
FIGURE 6

These curves are shown in Figure 6. For $M = 0$ the anomaly curves degenerate into the singular curve for an eliptic motion.

We study the general case of the convergence of the expansions of $\pmb { u }$ in powers of $( 1 ~ - ~ e )$ , by considering the expansion $u \mathrm { ~ - ~ } M \mathrm { ~ - ~ } e$ sin u as an integral of the differential equation

$$
{ \frac { d u } { d e } } = { \frac { \sin u } { 1 - e \cos u } } ,
$$

in which $u = x + \textstyle { \sqrt { - 1 } } y , e = \xi + \textstyle { \sqrt { - 1 } } \eta , M = a + \textstyle { \sqrt { - 1 } } b$ This gives as particular cases the singular and the anomaly curves for elliptic $( b = 0 )$ ）and hyperbolic $( a = 0 )$ ） motions. The singular curves are represented by

$$
\begin{array} { l } { { \displaystyle \xi ^ { 2 } = \frac { \alpha } { \sinh \alpha } ( \cosh \alpha - \alpha \sinh \alpha ) \mathrm { , } } } \\ { { \mathrm { } } } \\ { { \displaystyle \eta ^ { 2 } = \frac { \alpha } { \cosh \alpha } ( \alpha \cosh \alpha - \sinh \alpha ) \mathrm { ; } } } \end{array}
$$

and the anomaly curves by

$$
\begin{array} { l } { { \displaystyle { \dot { \xi } } ^ { 2 } = \frac { \beta - c } { \sin \beta } [ \cos \beta - ( \beta - c ) \sin \beta ] , } } \\ { { \displaystyle } } \\ { { \eta ^ { 2 } = \frac { \beta - c } { \cos \beta } [ ( \beta - c ) \cos \beta - \sin \beta ] . } } \end{array}
$$

![](images/ef4ebd176fcc9ebd2580b22101444d81eddcd0f7cd202080742c1b12204b7ed5.jpg)  
FIGURE 7

$\pmb { \alpha }$ and $\beta$ are parameters, real for an ellptic motion and purely imaginary for a hyperbolic motion; $c = a$ for an elliptic and $c = b$ for a hyperbolic motion. Thus, the radius of convergence of the expansions of $\boldsymbol { u }$ in powers of $( 1 ~ - ~ e )$ for all $M$ above a certain critical value is $( 1 ~ - ~ e _ { c r } )$ for an elliptic and $( e _ { c r } \mathrm { ~ - ~ } 1 )$ for a hyperbolic motion.The relation between $e$ and the critical value of $M$ is given by (l50) and (150a). $M _ { c r }$ as a function of $e$ is shown in Figure 7.

We have seen that, when the eccentric anomaly $\boldsymbol { u }$ is expanded in a power series of $( 1 ~ - ~ e )$ ，a certain region in the neighborhood of the point $( e _ { 0 } \mathrm { ~ - ~ } 1 )$ is cut out from the domain of the convergence of the expansion of $\boldsymbol { u }$ .In order to study the motion in this region,Elenevskaya has used Fourier series in the case of an elliptic motion and Laurent series in the case of a hyperbolic motion.

The coordinates of a point moving according to Kepler's laws can be represented in the form

$$
{ \frac { f ( e , u ) } { ( 1 - e \cos u ) ^ { 2 } } } ,
$$

where $f ( e , u )$ is an integral function of $\pmb { u }$ .If we expand the trigonometric functions in series and then substitute $z = \exp { u }$ ，we can transform this expansion for the coordinates to

$$
{ \frac { ( - 1 ) ^ { \alpha } 2 ^ { \alpha } z ^ { \alpha } f ( e , u ) } { e ^ { \alpha } ( z - \beta ) ^ { \alpha } ( z - 1 / \beta ) ^ { \alpha } } } , \qquad \beta = \tan { \frac { \phi } { 2 } } , \qquad \phi = \sin ^ { - 1 } e .
$$

This function has no singularity apart from the poles of order $\pmb { \alpha }$ Hence, it can be expanded in a Laurent series within the ring domain between two circles $z = \beta$ and $z = 1 / \beta$ . The coefficients of such expansions are given by

$$
P _ { n } = \frac { ( - 1 ) ^ { \alpha } 2 ^ { \alpha } } { e ^ { \alpha } 2 \pi \sqrt { - 1 } } \int _ { c } \frac { f ( e , u ) z ^ { n + \alpha - 1 } } { ( z - \beta ) ^ { \alpha } ( z - 1 / \beta ) ^ { \alpha } } d z ,
$$

where $c$ is the circle of unit radius. Since

$$
\beta = \tan \frac { \phi } { 2 } = \frac { \sin \phi _ { 1 } + \sqrt { - 1 } \sinh \phi _ { 2 } } { \cosh \phi _ { 2 } + \cos \phi _ { 1 } } ,
$$

therefore

$$
{ \frac { 1 } { \beta } } = { \frac { \sin \phi _ { 1 } - { \sqrt { - 1 } } \sinh \phi _ { 2 } } { \cosh \phi _ { 2 } - \cos \phi _ { 1 } } } ,
$$

where

$$
\begin{array} { c } { { e = \xi + \sqrt { - 1 } \eta = \sin \phi = \sin ( \phi _ { 1 } + \sqrt { - 1 } \phi _ { 2 } ) } } \\ { { { } } } \\ { { = \sin \phi _ { 1 } \cosh \phi _ { 2 } + \sqrt { - 1 } \cos \phi _ { 1 } \sinh \phi _ { 2 } . } } \end{array}
$$

The singular points are determined by

$$
\begin{array} { l } { { z _ { 1 } = \displaystyle \beta = \exp \left( \sqrt { - 1 } \displaystyle _ { x _ { 1 } } \right) \exp \left( - y _ { 1 } \right) = \displaystyle \frac { \sin \phi _ { 1 } - \sqrt { - 1 } \sinh \phi _ { 2 } } { \cosh \phi _ { 2 } + \cos \phi _ { 1 } } , } } \\ { { \displaystyle z _ { 2 } = \displaystyle \frac { 1 } { \beta } = \exp \left( \sqrt { - 1 } \displaystyle _ { x _ { 2 } } \right) \exp \left( - y _ { 2 } \right) = \displaystyle \frac { \sin \phi _ { 1 } + \sqrt { - 1 } \sinh \phi _ { 2 } } { \cosh \phi _ { 2 } - \cos \phi _ { 1 } } . } } \end{array}
$$

We separate the real and imaginary parts and take the ratios of corre-sponding parts. Then we construct the sum of the squares.We obtain the coordinates of the singular points

$$
\begin{array} { l l } { \tan x _ { 1 } = { \displaystyle \frac { \sinh \phi _ { 2 } } { \sin \phi _ { 1 } } } , } & { \exp \left( - y _ { 1 } \right) = { \displaystyle \frac { \sqrt { \sin ^ { 2 } \phi _ { 1 } + \sinh ^ { 2 } \phi _ { 2 } } } { \cosh \phi _ { 2 } + \cos \phi _ { 1 } } } } \\ { \tan x _ { 2 } = - { \displaystyle \frac { \sinh \phi _ { 2 } } { \sin \phi _ { 1 } } } , } & { \exp \left( - y _ { 2 } \right) = { \displaystyle \frac { \sqrt { \sin ^ { 2 } \phi _ { 1 } + \sinh ^ { 2 } \phi _ { 2 } } } { \cosh \phi _ { 2 } - \cos \phi _ { 1 } } } . } \end{array}
$$

Put

$$
z = \exp { ( - y ) } \exp { ( \sqrt { - 1 } \ : x ) } ,
$$

and transfer the coordinate origin to the point $z - 1$ .Expand(151）in powers of $( 1 - z )$ ； such expansions are convergent everywhere within the circle passing through the nearest singular point.

# 5.15ANDOYER'S EXPANSION

Let $f ( z )$ and $\phi ( z )$ be two holomorphic functions in the domain of a point $^ { a }$ and $C$ be a circle with center $^ { a }$ and radius $\pmb { r }$ such that along the circumference of the circle

$$
| \alpha f ( z ) | + | \beta \phi ( z ) | < r
$$

with small constants $\pmb { \alpha }$ and $\beta$ . The equation

$$
F ( z ) = z - a - \alpha f ( z ) - \beta \phi ( z ) = 0
$$

admits one and only one root $z = \zeta$ in the interior of the circle $C .$ Any holomorphic function $\Pi ( \zeta )$ of the root can be expanded in powers of $\pmb { \alpha }$ and $\beta$ by

$$
\frac { \Pi ( \zeta ) } { F ^ { \prime } ( \zeta ) } = \frac { 1 } { 2 \pi \sqrt { - 1 } } \int _ { c } \frac { \Pi ( z ) ~ d z } { z - a - \alpha f ( z ) - \beta \phi ( z ) } ,
$$

which, by Lagrange's theorem (Section 2.3), gives

$$
{ \frac { \Pi ( \zeta ) } { - \operatorname { e f } ^ { \prime } ( \zeta ) - \beta \phi ^ { \prime } ( \zeta ) } } = \Pi ( a ) + \sum _ { m , n } { \frac { \alpha ^ { m } \beta ^ { n } } { m ! n ! } } { \frac { d ^ { m + n } } { d a ^ { m + n } } } \{ \Pi ( a ) [ f ( a ) ] ^ { m } [ \phi ( a ) ] ^ { n } .
$$

where the combination $m = n = 0$ is excluded in the summation. Put

$$
\Pi ( z ) = \Phi ( z ) [ 1 - \alpha f ^ { \prime } ( z ) - \beta \phi ^ { \prime } ( z ) ] ;
$$

then,

$$
\Phi ( \zeta ) = \Phi ( a ) + \sum _ { m , n } ^ { \prime } \frac { \alpha ^ { m } \beta ^ { n } } { m ! n ! } \frac { d ^ { n + m - 1 } } { d a ^ { m + n - 1 } } \{ \Phi ^ { \prime } ( a ) [ f ( a ) ] ^ { m } [ \phi ( a ) ] ^ { n } \} ,
$$

where the combination $m = n = 0$ is excluded in the summation.This gives Goursat's generalization of the Lagrange theorem from a single variable to two variables (Goursat, 1913).

Consider with Goursat (l915） two functions $z _ { 1 } ( x , y )$ and $z _ { 2 } ( x , y )$ ， vanishing for $x = y = 0$ Let $w = S ( x , y )$ be the integral series of $_ { x }$ and $y$ representing the root ${ \boldsymbol w }$ of the equation

$$
x \exp { ( w ) } + y \exp { ( - w ) } = w ,
$$

such that $S ( x , y )$ is convergent for $| x | < R , | y | < R ^ { \prime }$ and divergent for $| x | > R , | y | > R ^ { \prime }$ . If there are positive numbers $R , R ^ { \prime }$ ， $\rho$ such that

$$
R \exp \left( \rho \right) + R ^ { \prime } \exp \left( - \rho \right) \leq \rho , \qquad R \exp \left( - \rho \right) + R ^ { \prime } \exp \left( \rho \right) \leq \rho ,
$$

![](images/c89bcd8e2617ce26401afe8a9135f0ab3fd0f96a3d153fe8e787aa82c87d2a86.jpg)  
FIGURE 8

then (l52) admits only one root whose modulus is smaller than $\pmb { \rho }$ for $| x | < R$ ， $| y | < R ^ { \prime }$

To prove this theorem of Goursat (l9l5), describe a circle with radius $\pmb { \rho }$ and with the center as origin on the complex plane of w.We have $| x \exp { ( w ) } + y \exp { ( - w ) } | < \rho$ 、Along the circumference of the circle,

$$
\begin{array} { r } { \left| x \exp { \left( w \right) } + y \exp { \left( - w \right) } \right| < R \exp { \left( \rho \cos { \phi } \right) } + R ^ { \prime } \exp { \left( - \rho \cos { \phi } \right) } , } \end{array}
$$

where $\phi$ is the argument of $\boldsymbol { \omega }$ .The maximum of the right-hand member is either $R \exp \left( \rho \right) + R ^ { \prime } \exp \left( - \rho \right)$ or $R$ $R \exp \left( - \rho \right) + R ^ { \prime } \exp \left( \rho \right)$ according as $R \gtrless R ^ { \prime }$ . If (153) are satisfied, then there is one and only one root ${ w _ { 1 } } ( x , y )$ whose modulus is smaller than $\rho$ .This root is a holomorphic function of $_ x$ and $y$ inside the circles $R$ and $R ^ { \prime }$ ， drawn with $x = 0$ and $y = 0$ as centers,respectively. It can be expanded by the generalized Lagrange theorem just proved.

Let $\Pi ( w )$ be a holomorphic function inside the circle $\pmb { \rho }$ By the residue theorem (Section 2.3),we have

$$
\begin{array} { c } { { { \displaystyle \frac { \Pi ( w _ { 1 } ) } { x \exp { ( w _ { 1 } ) } + y \exp { ( - w _ { 1 } ) } } } } } \\ { { { } } } \\ { { { } } } \\ { { { } } } \end{array} \qquad = \displaystyle \frac { 1 } { 2 \pi \sqrt { - 1 } } \int \displaystyle \frac { \Pi ( w ) \ d w } { w - x \exp { ( w ) } - y \exp { ( - w ) } } ; \qquad 
$$

the integral being taken along the circumference of the circle $\pmb { \rho }$

In particular, if

$$
\Pi ( w ) = w [ 1 - x \exp { ( w ) } + y \exp { ( - w ) } ] ,
$$

then

$$
w _ { 1 } ( x , y ) = \sum _ { m , n } { \frac { ( m - n ) ^ { m + n - 1 } } { m ! n ! } } x ^ { m } y ^ { n } ,
$$

where the combination $m = n = 0$ is excluded from the summation. The series (l54) is absolutely convergent if there exists a positive number $\pmb { \rho }$ such that

$$
x | \exp { ( \rho ) } + | y | \exp { ( - \rho ) } < \rho , \qquad | x | \exp { ( - \rho ) } + | y | \exp { ( \rho ) } < \rho
$$

Geometricall, consider the family of straight lines represented by

$$
X \exp \left( \rho \right) + Y \exp \left( - \rho \right) = \rho
$$

with the parameter $\rho , 0 \leq \rho \leq \infty$ .The envelope of these straight lines is the curve $\mathbf { \Gamma } \mathbf { \Gamma } \mathbf { \Gamma } \mathbf { \Gamma } \mathbf { \Gamma } \mathbf { \Gamma } \mathbf { \Gamma } \mathbf { \Gamma } \mathbf { \Gamma }$ represented by

$$
X = \frac { \rho + 1 } { 2 } \exp { ( - \rho ) } , \qquad Y = \frac { \rho - 1 } { 2 } \exp { ( \rho ) } .
$$

The point $( X , Y )$ starts at a point $\left( { \frac { 1 } { 2 } } , \ - { \frac { 1 } { 2 } } \right)$ and describes the infinite branch $\$ 4 Q$ which is asymptotic to the $Y .$ -axis (Figure 8). The abscissa of the intersection $A$ of the curve with the $X$ axis is $1 / 2 . 7 1 8 2 8 \cdots$ The intersection of this curve with the bisectrix $o G$ is the point $B$ for which the value of $\rho$ is given by $( \rho - 1 )$ exp $( 2 \rho ) = \rho + 1$ .The envelope of the straight lines (l55） is symmetric with respect to the bisectrix $_ { O G }$ of the angle $X O Y$ .The series (l54） is absolutely convergent if the point with coordinates $( | x | , | y | )$ is in the region between the broken segment $A B A ^ { \prime }$ and the origin because, in the region OBA,

$$
\begin{array} { l } { { X \exp \left( \rho \right) + Y \exp \left( - \rho \right) - \rho < 0 , } } \\ { { \mathrm { } } } \\ { { X \exp \left( - \rho \right) + Y \exp \left( \rho \right) - \rho < 0 , } } \end{array}
$$

Andoyer (1923） writes

$$
s _ { 1 } = \frac { e } { 2 } s , \qquad s _ { 2 } = \frac { e } { 2 } s ^ { - 1 }
$$

and the function $s$ in Section 5.11 in the form

$$
S = \sum _ { j _ { 1 } , j _ { 2 } } P _ { j _ { 1 } j _ { 2 } } z _ { 1 } ^ { j } \imath z _ { 2 } ^ { j } \imath = \sum _ { j _ { 1 } , j _ { 2 } } Q _ { j _ { 1 } j _ { 2 } } s _ { 1 } ^ { j } \imath s _ { 2 } ^ { j _ { 2 } } .
$$

Put

$$
z _ { 1 } = { \frac { e } { 2 } } z , \qquad z _ { 2 } = { \frac { e } { 2 } } z ;
$$

then, comparing with (l10), that is, $\begin{array} { r } { S = \sum _ { j } P _ { j } z ^ { j } } \end{array}$ ， we obtain

$$
P _ { j } = \sum _ { j _ { 1 } - j _ { 2 } = j } P _ { j _ { 1 } j _ { 2 } } \Big ( \frac { e } { 2 } \Big ) ^ { j _ { 1 } + j _ { 2 } } , \qquad Q _ { j } = \sum _ { j _ { 1 } - j _ { 2 } = j } Q _ { j _ { 1 } j _ { 2 } } \Big ( \frac { e } { 2 } \Big ) ^ { j _ { 1 } + j _ { 2 } } .
$$

By Cauchy's first theorem the coefficient $P _ { j _ { 1 } j _ { 2 } }$ of the expansion of $s$ equals the coefficient $U _ { j _ { 1 } j _ { 2 } }$ of the expansion of

$$
U ( s _ { 1 } , s _ { 2 } ) = S ( 1 - s _ { 1 } - s _ { 2 } ) \exp \left[ ( j _ { 1 } - j _ { 2 } ) ( s _ { 1 } - s _ { 2 } ) \right]
$$

in powers of $s _ { 1 } , s _ { 2 }$ . We have

$$
P _ { j } = \sum _ { j _ { 1 } - j _ { 2 } = j } U _ { j _ { 1 } j _ { 2 } } { \left( { \frac { e } { 2 } } \right) } ^ { j _ { 1 } + j _ { 2 } } ,
$$

or

$$
\begin{array} { c } { { P _ { j } = \displaystyle \frac 1 { 2 \pi \sqrt { - 1 } } \sum _ { ( j _ { 1 } - j _ { 2 } = j ) } \left( \frac e 2 \right) ^ { j _ { 1 } + j _ { 2 } } { \displaystyle \int ^ { ( 0 + ) } } \int ^ { ( 0 + ) } { \cal S } \cdot ( 1 - s _ { 1 } - s _ { 2 } ) } } \\ { { \times \exp { [ ( j _ { 1 } - j _ { 2 } ) ( s _ { 1 } - s _ { 2 } ) ] } \cdot s _ { 1 } ^ { - j _ { 1 } - 1 } s _ { 2 } ^ { - j _ { 2 } - 1 } d s _ { 1 } d s _ { 2 } . } } \end{array}
$$

Suppose that

$$
f = \sum _ { p _ { 1 } , p _ { 2 } } a _ { p _ { 1 } p _ { 2 } } z _ { 1 } ^ { p _ { 1 } } z _ { 2 } ^ { p _ { 2 } } = \sum _ { q _ { 1 } , q _ { 2 } } b _ { q _ { 1 } q _ { 2 } } s _ { 1 } ^ { q _ { 1 } } s _ { 2 } ^ { q _ { 2 } } ;
$$

from this,Andoyer (1923) derived

$$
\begin{array} { l } { { a _ { n } = \displaystyle \sum _ { ( p _ { 1 } - p _ { 2 } = n ) } a _ { p _ { 1 } p _ { 2 } } \biggl ( \frac { e } { 2 } \biggr ) ^ { p _ { 1 } + p _ { 2 } } , } } \\ { { \ } } \\ { { a _ { p _ { 1 } p _ { 2 } } = \displaystyle \sum _ { ( p _ { 1 } q _ { 2 } ( - 1 ) ^ { p _ { 2 } - q _ { 2 } } } \frac { ( q _ { 1 } - q _ { 2 } ) ( \not p _ { 1 } - \not p _ { 2 } ) ^ { p _ { 1 } + p _ { 2 } - q _ { 1 } - q _ { 2 } - 1 } } { ( \not p _ { 1 } - q _ { 1 } ) ! ( \not p _ { 2 } - q _ { 2 } ) ! } , } } \end{array}
$$

where $\mathbf { \nabla } \mathbf { \hat { \rho } } ^ { ! } = 1$ for $\pmb { \mathscr { p } } = 0$ and $\pmb { \mathscr { p } } ! = \infty$ for a negative integer $\boldsymbol { \mathscr { p } }$ For ${ \pmb { \mathscr { p } } } _ { 1 } = { \pmb { \mathscr { p } } } _ { 2 } = { \pmb { \mathscr { p } } } _ { 1 }$

$$
a _ { p , p } = b _ { p , p } - b _ { p , p - 1 } - b _ { p - 1 , p } .
$$

Andoyer further computed the expansions of

$$
X ^ { \rho , \sigma } = \Big ( \frac { r } { a } \Big ) ^ { \rho } \Big ( \frac { t } { z } \Big ) ^ { \sigma } , \qquad Y ^ { \rho , \sigma } = \Big ( \frac { r } { a } \Big ) ^ { \rho } \Big ( \frac { t } { s } \Big ) ^ { \sigma } .
$$

Suppose that a holomorphic function is expanded in double power series of

$$
z _ { 1 } = \frac { e } { 2 } \exp { ( \sqrt { - 1 } M ) } , \qquad z _ { 2 } = \frac { e } { 2 } \exp { ( - \sqrt { - 1 } M ) }
$$

in the form

$$
\sum _ { j _ { 1 } , j _ { 2 } } P _ { j _ { 1 } , j _ { 2 } } z _ { 1 } ^ { j } { \imath } z _ { 2 } ^ { i } { \imath } ^ { \phantom { \dagger } } .
$$

The convergence of this series is judged by the root of

$$
{ \boldsymbol { F } } = { \boldsymbol { w } } - z _ { 1 } \exp \left( { \boldsymbol { w } } \right) + z _ { 2 } \exp \left( - { \boldsymbol { w } } \right) = 0 , \qquad { \boldsymbol { w } } = { \sqrt { - 1 } } \left( { \boldsymbol { u } } - { \boldsymbol { M } } \right)
$$

for which

$$
\frac { \partial F } { \partial w } = 1 - z _ { 1 } \exp { ( w ) } - z _ { 2 } \exp { ( - w ) } \neq 0 .
$$

$F ( z _ { 1 } , z _ { 2 } )$ admits a root $w = 0$ for $z _ { 1 } = z _ { 2 } = 0$ Other roots can be expanded in integral powers of $z _ { 1 }$ and $z _ { 2 }$ ,and are real. The expansion is absolutely convergent for $| z _ { 1 } | < \rho / 2 , | z _ { 2 } | < \rho / 2$ with a sufficiently small number $\rho$ ,if

$$
{ \frac { \partial F } { \partial w } } = 1 - z _ { 1 } \exp \left( w \right) - z _ { 2 } \exp \left( - w \right) \neq 0
$$

for all such values of $z _ { 1 }$ and $z _ { 2 }$

The critical case occurs if $F = 0$ and $\partial F / \partial w = 0$ are satisfied simultaneously. We have

$$
\begin{array} { r } { z _ { 1 } = \frac { 1 } { 2 } ( 1 + w ) \exp { ( - w ) } , \qquad z _ { 2 } = \frac { 1 } { 2 } ( 1 - w ) \exp { ( w ) } . } \end{array}
$$

Put $w = a + { \sqrt { - 1 } } b$ ; then,

$$
\begin{array} { l } { { | z _ { 1 } | ^ { 2 } = { \frac { 1 } { 4 } } [ ( 1 ~ + ~ a ) ^ { 2 } ~ + ~ b ^ { 2 } ] \exp { ( - 2 a ) } , } } \\ { { | z _ { 2 } | ^ { 2 } = { \frac { 1 } { 4 } } [ ( 1 ~ - ~ a ) ^ { 2 } ~ + ~ b ^ { 2 } ] \exp { ( 2 a ) } . } } \end{array}
$$

Then the foregoing condition for $z _ { 1 }$ and $z _ { 2 }$ is that

$$
\left| ( 1 + a ) ^ { 2 } + b ^ { 2 } \right| \exp { ( - 2 a ) } < \rho ^ { 2 } , \qquad \left| ( 1 - a ) ^ { 2 } + b ^ { 2 } \right| \exp { ( 2 a ) } < \rho ^ { 2 }
$$

should admit no common root.

Consider in the complex plane of w the equation

$$
| ( 1 + a ) ^ { 2 } + b ^ { 2 } | \exp { ( - 2 a ) } = \rho ^ { 2 } ,
$$

or

$$
b ^ { 2 } = \rho ^ { 2 } \exp { ( 2 a ) } - ( 1 + a ) ^ { 2 } .
$$

![](images/18910fda88fa9aae77eb063cc78d89d0abc27e5fa342b76c8934cdcbb9dc1e8d.jpg)  
FIGURE 9

The equation $\rho ^ { 2 } \exp { ( 2 a ) } - ( 1 + a ) ^ { 2 } = 0 \ :$ viewed as an equation in $^ { a }$ ， admits three real roots since $0 < \rho < 1$ .One root is smaller than $^ { - 1 }$ ， the second is between $- 1$ and O,and the third is positive.The points satisfying $| ( 1 + a ) ^ { 2 } + b ^ { 2 } |$ exp(-2a)< p² are in the shaded area of Figure 9 on the complex plane of ${ \pmb w }$ .The area for which

$$
| ( 1 + a ) ^ { 2 } + b ^ { 2 } | \exp { ( 2 a ) } < \rho ^ { 2 }
$$

is symmetric to this area with respect to the $b$ -axis.Hence the condition for convergence is $O A _ { 1 } < O A _ { 3 }$ This is satisfied for small values of $\rho , \mathrm { \mathbf { u p } }$ to the value of $\rho$ such that

$$
\rho ^ { 2 } = ( 1 + a ) ^ { 2 } \exp { ( - 2 a ) } = ( 1 - a ) ^ { 2 } \exp { ( 2 a ) } .
$$

This value of $\pmb { \rho }$ is larger than unity for any positive value of $^ { a }$ ,because a = coth $^ { a }$ ， $\rho = \sqrt { a ^ { 2 } - 1 }$ This is equal to the value $\pmb { \rho }$ in (143).

# VARIATION OF CONSTANTS

In formula (70), Section 5.6, put

$$
H ^ { \prime } = \sum _ { \kappa = 1 } ^ { n - 1 } \left( \frac { y _ { 3 \kappa - 2 } ^ { \prime 2 } } { 2 m _ { 3 \kappa - 2 } ^ { \prime } } + \frac { y _ { 3 \kappa - 1 } ^ { \prime 2 } } { 2 m _ { 3 \kappa - 1 } ^ { \prime } } + \frac { y _ { 3 \kappa } ^ { \prime 2 } } { 2 m _ { 3 \kappa } ^ { \prime } } \right) - \sum _ { \kappa = 1 } ^ { n - 1 } \frac { m _ { 3 } m _ { 3 \kappa } } { \left( x _ { 3 \kappa - 2 } ^ { \prime 2 } + x _ { 3 \kappa - 1 } ^ { \prime 2 } + x _ { 3 } ^ { \prime } \right) }
$$

$$
H ^ { \prime \prime } = \sum _ { \kappa = 1 } ^ { n - 1 } \frac { m _ { 3 } m _ { 3 \kappa } } { ( x _ { 3 \kappa - 2 } ^ { \prime 2 } + x _ { 3 \kappa - 1 } ^ { \prime 2 } + x _ { 3 \kappa } ^ { \prime 2 } ) ^ { 1 / 2 } } - \sum _ { \tau , \nu = 1 } ^ { n } \frac { m _ { 3 \tau } m _ { 3 \nu } } { r _ { 3 \tau , 3 \nu } } ,
$$

$$
H = H ^ { \prime } + H ^ { \prime \prime } ,
$$

and suppose that any mass $m _ { i }$ other than $m _ { 1 } = m _ { 2 } = m _ { 3 }$ is of magnitude $\mu \ll 1$ . From (63), Section 5.6,

$$
\begin{array} { c } { { \displaystyle x _ { 3 \kappa - 2 } ^ { \prime } = x _ { 3 ( \kappa + 1 ) - 2 } - x _ { 1 } + \frac { m _ { 4 } x _ { 4 } + \cdot \cdot \cdot + m _ { 3 \kappa - 2 } x _ { 3 \kappa - 2 } } { m _ { 1 } + m _ { 4 } + \cdot \cdot \cdot \cdot + m _ { 3 \kappa - 2 } } } } \\ { { - \displaystyle \sum _ { \tau = 2 } ^ { \kappa } \left( m _ { 3 \tau - 2 } x _ { 3 \tau - 2 } \right) \Biggl / \left( \displaystyle \sum _ { \tau = 1 } ^ { \kappa } m _ { 3 \tau - 2 } \right) ; } } \end{array}
$$

therefore the difference between $( x _ { 3 \kappa - 2 } ^ { \prime 2 } + x _ { 3 \kappa - 1 } ^ { \prime 2 } + x _ { 3 \kappa } ^ { \prime 2 } ) ^ { 1 / 2 }$ and $\pmb { r _ { 3 , 3 \kappa - 3 } }$ is of magnitude $\pmb { \mu }$ .Hence, the difference between the first sum on the right-hand side of $H ^ { \prime \prime }$ and the second sum extending to $\tau = 1$ ， $\nu = 2 , 3 , . . . , n$ is of magnitude $\mu$ .A term with $\tau \neq 1$ ， $\nu \neq 1$ is of magnitude $\mu ^ { 2 }$ Hence, $H ^ { \prime \prime }$ has a factor $\mu$ .Accordingly, $H ^ { \prime \prime }$ is of magnitude $\mu$ compared with $H ^ { \prime }$ , and can be regarded as the perturbation. At first we consider $H ^ { \prime }$ only and determine the intermediary orbit.

We have

$$
\begin{array} { l } { { \displaystyle { H ^ { \prime } = \sum _ { \kappa = 1 } ^ { n - 1 } H _ { \kappa } ^ { \prime } } , } } \\ { { \displaystyle { H _ { \kappa } ^ { \prime } = \sum _ { 2 m _ { 3 \kappa } ^ { \prime } } ^ { 1 } ( y _ { 3 \kappa - 2 } ^ { \prime 2 } + y _ { 3 \kappa - 1 } ^ { \prime 2 } + y _ { 3 \kappa } ^ { \prime 2 } ) - \frac { m _ { 3 } m _ { 3 \kappa } } { ( x _ { 3 \kappa - 2 } ^ { \prime 2 } + x _ { 3 \kappa - 1 } ^ { \prime 2 } + x _ { 3 \kappa } ^ { \prime 2 } ) ^ { 1 / 2 } } } . } } \end{array}
$$

Hence, the Hamiltonian function is separable,

$$
\frac { d x _ { i } } { d t } = \frac { \partial H _ { \kappa } ^ { \prime } } { \partial y _ { i } } , \frac { d y _ { i } } { d t } = \triangledown - \frac { \partial H _ { \kappa } ^ { \prime } } { \partial x _ { i } } \quad  &  \binom { i = 3 \kappa - 2 , 3 \kappa - 1 , 3 \kappa ; } { \kappa = 1 , 2 , . . . , n - 1 } .
$$

Referring to the notation of Sections 5.9 and 5.10, write $\beta _ { \kappa } = m _ { 3 \kappa } ^ { \prime } m _ { 3 } m _ { 3 \kappa }$ in place of $\beta$ and $m _ { 3 \kappa } ^ { \prime } h _ { 1 }$ in place of $h _ { 1 }$ ； this gives us a set of canonical variables derived from (109), Section 5.10,

$$
h _ { 3 \kappa - 2 } = - \frac { \beta _ { \kappa } } { 2 m _ { 3 \kappa } ^ { \prime } a } , \qquad h _ { 3 \kappa - 1 } = \sqrt { \beta _ { \kappa } a _ { \kappa } ( 1 - e _ { \kappa } ^ { 2 } ) } ,
$$

$$
h _ { 3 \kappa } = \sqrt { \beta _ { \kappa } a _ { \kappa } ( 1 - \ell _ { \kappa } ^ { 2 } ) } \cos I _ { \kappa } ,
$$

$$
\gamma _ { 3 \kappa - 2 } = - t _ { \kappa } , \qquad \gamma _ { 3 \kappa - 1 } = \varpi _ { \kappa } - \Omega _ { \kappa } , \qquad \gamma _ { 3 \kappa } = \Omega _ { \kappa } .
$$

Hence，by the principle of variation of constants stated in Sections 1.12-1.17, the differential equations for the variation of elements are

$$
\frac { d \gamma _ { i } } { d t } = \frac { \partial H ^ { \prime \prime } } { \partial h _ { i } } , \quad \frac { d h _ { i } } { d t } = - \frac { \partial H ^ { \prime \prime } } { \partial \gamma _ { i } } \quad ( i = 1 , 2 , . . . , 3 n - 3 ) .
$$

Now transform the variables in (160). Omit the suffixes $\pmb { \kappa }$ .From the relations $n = \sqrt { \beta m ^ { \prime } } / a ^ { 3 / 2 }$ ， $h _ { 1 } = - \beta / 2 a$ of Section 5.10 we obtain $n = \sqrt { m ^ { \prime } } ( - 2 h _ { 1 } ) ^ { 3 / 2 } / \beta .$ . The Hamiltonian $H ^ { \prime \prime }$ is a function of $x _ { 1 } ^ { \prime } , \ldots , x _ { 3 n - 3 } ^ { \prime } .$ The latter variables are expressed in terms of elliptic motions,and hence are periodic functions of $M$ in the expansions described in Sections 5.9-5.15, in particular (l05) of Section 5.10.

In the present notation they are multiply periodic functions of arguments $n _ { \kappa } ( 1 \ + \ \gamma _ { 3 \kappa } )$ . But,as $n _ { \kappa }$ is a function of $h _ { 3 \kappa - 2 }$ ，the time $t$ appears explicitly in front of the periodic function in the right-hand member of (l6l).In order to avoid this inconvenience we make the following transformation:

First, put $l = n ( t + \gamma )$ ; then

$$
\frac { d h } { d t } = - n \frac { \partial H ^ { \prime \prime } } { \partial t } \cdot
$$

Furthermore,

$$
\frac { d l } { d t } = n \bigg ( 1 + \frac { d \gamma } { d t } \bigg ) + ( t + \gamma ) \frac { \partial n } { \partial h } \frac { d h } { d t } \cdot
$$

Let $( \partial H ^ { \prime \prime } / \partial h )$ be the result of differentiating $H ^ { \prime \prime }$ with respect to $h$ by supposing that $h$ is not contained in $\pmb { n }$ .Then

$$
\frac { \partial H ^ { \prime \prime } } { \partial h } = \left( \frac { \partial H ^ { \prime \prime } } { \partial h } \right) + \frac { \partial H ^ { \prime \prime } } { \partial l } \left( t + \gamma \right) \frac { \partial n } { \partial h } .
$$

If we put $d \gamma / d t = \partial H ^ { \prime \prime } / \partial h$ in the above equation for $d l / d t .$ ，then we obtain

$$
\frac { d l } { d t } = n + n \bigg ( \frac { \partial H ^ { \prime \prime } } { \partial h } \bigg ) + n \frac { \partial H ^ { \prime \prime } } { \partial l } \left( t + \gamma \right) \frac { \partial n } { \partial h } + \left( t + \gamma \right) \frac { \partial n } { \partial h } \frac { d h } { d t } .
$$

Hence

$$
\frac { d l } { d t } = n + n \bigg ( \frac { \partial H ^ { \prime \prime } } { \partial h } \bigg ) .
$$

Put $L = { \sqrt { \beta a } }$ ; then, $h = ~ - \beta ^ { 2 } / ( 2 m ^ { \prime } L ^ { 2 } ) , n = \beta ^ { 2 } / ( m ^ { \prime } L ^ { 3 } )$ and

$$
- n { \frac { \partial H ^ { \prime \prime } } { \partial l } } = { \frac { d h } { d t } } = { \frac { \beta ^ { 2 } } { L ^ { 3 } } } { \frac { d L } { d t } } = n { \frac { d L } { d t } } .
$$

Hence, $d L / d t = - \partial H ^ { \prime \prime } / \partial l .$ Furthermore, since

$$
\left( { \frac { \partial H ^ { \prime \prime } } { \partial L } } \right) = \left( { \frac { \partial H ^ { \prime \prime } } { \partial L } } \right) { \frac { \partial h } { \partial L } } = n \left( { \frac { \partial H ^ { \prime \prime } } { \partial h } } \right) ,
$$

we have

$$
\frac { d l } { d t } = n + \Big ( \frac { \partial H ^ { \prime \prime } } { \partial L } \Big ) ,
$$

where $\left( { { \partial { H ^ { \prime \prime } } } / { \partial { L } } } \right)$ denotes the result of differentiating under the supposition that $\scriptstyle { \pmb { L } }$ is not contained in $\pmb { n }$ .Put

$$
- \frac { \beta ^ { 2 } } { 2 m ^ { \prime } L ^ { 2 } } + H " = F ;
$$

then

$$
\frac { d L } { d t } = \frac { \partial F } { \partial l } , \frac { d l } { d t } = - \frac { \partial F } { \partial L } ,
$$

because

$$
- \frac { \partial F } { \partial L } = n + \frac { \partial H ^ { \prime \prime } } { \partial L } .
$$

Now write

$$
F = \sum _ { \kappa = 1 } ^ { n - 1 } \frac { \beta _ { \kappa } ^ { 2 } } { 3 m _ { 3 \kappa } ^ { \prime } L _ { 3 } ^ { 2 } } - H ^ { \prime \prime }
$$

and

$$
\begin{array} { r l } & { L = \sqrt { \beta a } , \qquad G = \sqrt { \beta a ( 1 - e ^ { 2 } ) } , \qquad H = \sqrt { \beta a ( 1 - e ^ { 2 } ) } \cos { I } , } \\ & { \ l = n ( t + \gamma ) , \qquad g = \varpi - \Omega , \qquad h = \Omega , } \end{array}
$$

for each $\pmb { \kappa }$ ; then we have the canonical equations

$$
\frac { d L } { d t } = \frac { \partial F } { \partial l } , \frac { d G } { d t } = \frac { \partial F } { \partial g } , \frac { d H } { d t } = \frac { \partial F } { \partial h } ,
$$

$$
{ \frac { d l } { d t } } = \ - { \frac { \partial F } { \partial L } } , { \frac { d g } { d t } } = \ - { \frac { \partial F } { \partial G } } , { \frac { d h } { d t } } = \ - { \frac { \partial F } { \partial H } } .
$$

In particular, when $\mathscr { n } = 3$ ，

$$
m _ { 6 } ^ { \prime } = \frac { m _ { 3 } m _ { 6 } } { m _ { 3 } + m _ { 6 } } , \qquad m _ { 9 } ^ { \prime } = \frac { m _ { 9 } ( m _ { 3 } + m _ { 6 } ) } { m _ { 3 } + m _ { 6 } + m _ { 9 } } ;
$$

accordingly,

$$
\beta _ { 1 } = \frac { k ^ { 2 } m _ { 3 } ^ { 2 } m _ { 6 } ^ { 2 } } { m _ { 3 } + m _ { 6 } } , \qquad \beta _ { 2 } = \frac { k ^ { 2 } m _ { 9 } ^ { 2 } m _ { 3 } ( m _ { 3 } + m _ { 6 } ) } { m _ { 3 } + m _ { 6 } + m _ { 9 } } ,
$$

$$
F = \frac { \beta _ { 1 } ^ { 2 } } { 2 m _ { 6 } ^ { \prime } L _ { 1 } ^ { 2 } } + \frac { \beta _ { 2 } ^ { 2 } } { 2 m _ { 9 } ^ { \prime } L _ { 2 } ^ { 2 } } + \frac { m _ { 6 } m _ { 9 } } { r _ { 6 , 9 } } + \frac { m _ { 3 } m _ { 9 } } { r _ { 3 , 9 } } - \frac { m _ { 3 } m _ { 9 } } { \sqrt { x _ { 4 } ^ { \prime 2 } + x _ { 5 } ^ { \prime 2 } + x _ { 6 } ^ { \prime 2 } } } .
$$

The difference of the last two terms of $F$ is of magnitude $\mu ^ { 2 }$ ; the third term is of magnitude $\mu ^ { 2 }$ . The first two terms are of magnitude $\pmb { \mu }$ ; we say that they are of order 2 and l,respectively, with regard to the disturbing masses. $L , G , H$ are of order l with regard to $\mu$ .These canonical variables $L , G , H , l , g , h$ are called Delaunay's elements (Delaunay,l860,l867).The variables $a , e , I , t _ { 0 } , \varpi , \Omega$ are called the Keplerian elements.Write

$$
\begin{array} { r l } & { \boldsymbol { F } = F _ { 0 } + \mu \boldsymbol { F } _ { 1 } , } \\ & { \boldsymbol { F } _ { 0 } = \frac { \beta _ { 1 } ^ { 2 } } { 2 m _ { \ell } ^ { 2 } L _ { 1 } ^ { 2 } } + \frac { \beta _ { 2 } ^ { 2 } } { 2 m _ { 9 } ^ { 2 } L _ { 2 } ^ { 2 } } , } \\ & { \mu F _ { 1 } = \frac { m _ { 6 } m _ { 9 } } { r _ { 6 , 9 } } + \frac { m _ { 3 } m _ { 9 } } { r _ { 3 , 9 } } - \frac { m _ { 3 } m _ { 9 } } { \sqrt { x _ { 4 } ^ { \prime 2 } + \ x _ { 5 } ^ { \prime 2 } + \ x _ { 6 } ^ { \prime 2 } } } . } \end{array}
$$

The term $\mu F _ { 1 }$ indicates the disturbing function and is multiplied by the factor $\mu$ of the order of the disturbing masses. The term $F _ { 0 }$ appears only in the right-hand member of dl/dt in (164).Let the solution of (164) for $\mu = 0$ be denoted by $\cdot \ : L ^ { \left( 0 \right) } , \ : G ^ { \left( 0 \right) } , \ : H ^ { \left( 0 \right) } , \ : l ^ { \left( 0 \right) } , \ : g ^ { \left( 0 \right) } , \ : h ^ { \left( 0 \right) }$ , and write

$$
n _ { i } = - \frac { 2 \beta _ { i } ^ { 2 } } { 3 m _ { 3 i } ^ { \prime } L _ { i } ^ { 3 } } = \frac { \partial F _ { 0 } } { \partial L _ { i } } .
$$

Then we obtain $l _ { i } ^ { ( 0 ) } = n _ { i } ( t + \gamma _ { i } )$ ， with the integration constant $\gamma _ { i }$ ; the other elements are now all constants.Denote the initial values for $t = 0$ of these elements by ${ \cal L } ^ { ( 0 ) } , \ { \cal G } ^ { ( 0 ) } , \ { \cal H } ^ { ( 0 ) } , \ { \cal l } ^ { ( 0 ) } , \ g ^ { ( 0 ) } , \ { \cal h } ^ { ( 0 ) } ;$ ，and consider the quantities of magnitude $\pmb { \mu }$ .Then

$$
L _ { i } = L _ { i } ^ { ( 0 ) } + \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial l _ { i } } d t , \qquad G _ { t } = G _ { i } ^ { ( 0 ) } + \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial g _ { i } } d t ,
$$

$$
H _ { i } = H _ { i } ^ { ( 0 ) } + \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial h _ { i } } d t ,
$$

$$
l _ { i } = l _ { i } ^ { ( 0 ) } - \int _ { 0 } ^ { t } \frac { \partial F _ { 0 } } { \partial L _ { i } } d t - \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial L _ { i } } d t , \qquad g _ { i } = g _ { i } ^ { ( 0 ) } - \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial G _ { i } } d t ,
$$

$$
h _ { i } = h _ { i } ^ { ( 0 ) } - \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial H _ { i } } d t .
$$

The terms with the factor $\mu$ all represent the perturbation. Let $F _ { 0 } ^ { ( 0 ) }$ be the result of substituting the values for $\mu = 0 \mathrm { i n } F _ { 0 }$ ; let $\Phi$ be the aggregate of terms of the third and higher orders with regard to $\delta L _ { i }$ ; let $C _ { i j }$ be a function of $L _ { i } ^ { ( 0 ) }$ and ${ \cal L } _ { j } ^ { ( 0 ) }$ with $C _ { i j } = C _ { j i }$ . Then,

$$
F _ { 0 } = F _ { 0 } ^ { ( 0 ) } + \sum _ { i = 1 } ^ { n } n _ { i } \ : \delta L _ { i } + \frac { 1 } { 2 } \sum _ { i , j } C _ { i j } \ : \delta L _ { i } \ : \delta L _ { j } + \Phi .
$$

Indicate the effect of the perturbation on the values for $t = 0$ by putting δ in front,and refer to the relation

$$
{ \frac { \partial F _ { 0 } } { \partial L _ { i } } } = n _ { i } + \sum _ { j = 0 } ^ { n } C _ { i j } \ \delta L _ { j } + { \frac { \partial \Phi } { \partial L _ { i } } } = n _ { i } + \mu \sum _ { j = 1 } ^ { n } C _ { i j } \int { \frac { \partial F _ { 1 } } { \partial l _ { j } } } \ d t + { \frac { \partial \Phi } { \partial L _ { i } } } ;
$$

then

$$
\begin{array} { l } { { \displaystyle \displaystyle \partial { \cal L } _ { i } = \mu \int _ { 0 } ^ { t } \frac { \partial { \cal F } _ { 1 } } { \partial { \cal Z } _ { i } } d t , \qquad \displaystyle \partial { \cal G } _ { \mathrm { i } } = \mu \int _ { 0 } ^ { t } \frac { \partial { \cal F } _ { 1 } } { \partial g _ { \mathrm { i } } } d t , \qquad \displaystyle \partial { \cal H } _ { \mathrm { i } } = \mu \int _ { 0 } ^ { t } \frac { \partial { \cal Z } _ { \mathrm { i } } } { \partial { \cal Z } _ { \mathrm { i } } } } } \\ { { \displaystyle \displaystyle \partial l _ { i } = - \mu \sum _ { j = 1 } ^ { n } C _ { i j } \int _ { 0 } ^ { t } d t \int _ { 0 } ^ { t } \frac { \partial { \cal F } _ { 1 } } { \partial { \cal { \cal L } } _ { j } } d t - \displaystyle \int _ { 0 } ^ { t } \frac { \partial \Phi } { \partial { \cal { L } } _ { i } } d t - \mu \int _ { 0 } ^ { t } \frac { \partial { \cal F } _ { 1 } } { \partial { \cal { \cal L } } _ { i } } d t , } } \\ { { \displaystyle \hat { \delta } q _ { i } = - \mu \int _ { 0 } ^ { t } \frac { \partial { \cal F } _ { 1 } } { \partial { \cal G } _ { i } } d t , \qquad \delta h _ { i } = - \mu \int _ { 0 } ^ { t } \frac { \partial { \cal F } _ { 1 } } { \partial { \cal H } _ { i } } d t . } } \end{array}
$$

This result has been obtained by Poincaré (1905).

# 5.17POINCARE's ELEMENTS

We carry out the following transformation to Delaunay's elements:

$$
\begin{array} { l } { \Lambda = L , \qquad \Gamma = L - G , \qquad Z = G - H , } \\ { \lambda = l + g + h , \qquad \gamma = - g - h , \qquad z = - h . } \end{array}
$$

This is a contact transformaticn,as we see from any of the methods described in Sections 1.7-1.11,or from the relation

$$
\Lambda \lambda + \Gamma \gamma + Z z = L l + G g + H h .
$$

It is written in the Keplerian elements

$$
\begin{array} { l } { { \Lambda = \sqrt { \beta a } , \qquad \Gamma = \sqrt { \beta a } ( 1 - \sqrt { 1 - e ^ { 2 } } ) , \nonumber } } \\ { { \qquad \ } } \\ { { Z = \sqrt { \beta a ( 1 - e ^ { 2 } ) } ( 1 - \cos I ) , \nonumber } } \\ { { \qquad \lambda = M + \varpi , \qquad \gamma = - \varpi , \qquad z = - \Omega . } } \end{array}
$$

$\lambda$ is the mean longitude in the orbit, $\Gamma$ is of magnitude $e ^ { 2 }$ and $z$ is of magnitude $I ^ { 2 }$

Put

$$
\begin{array} { r l } { \xi = \sqrt { 2 \Gamma } \cos \gamma , \quad } & { { } \beta = \sqrt { 2 Z } \cos z , } \\ { \eta = \sqrt { 2 \Gamma } \sin \gamma , \quad } & { { } q = \sqrt { 2 Z } \sin z . } \end{array}
$$

This is also a contact transformation because

$$
\xi d \eta = \cos \gamma \sin \gamma d \Gamma + 2 \Gamma \cos ^ { 2 } \gamma d \gamma = { \textstyle { \frac { 1 } { 2 } } } d ( \Gamma \sin 2 \gamma ) + \Gamma d \gamma .
$$

$\xi , \eta$ are called the eccentric variables, and $\phi , q$ the oblique variables.

$$
\begin{array} { r l } & { \Lambda = \sqrt { \beta a } , \qquad \xi = \sqrt { 2 \Lambda ( 1 - \sqrt { 1 - e ^ { 2 } } ) } \cos \varpi , } \\ & { \beta = \sqrt { 2 \Lambda \sqrt { 1 - e ^ { 2 } } ( 1 - \cos I ) } \cos \Omega , } \\ & { \lambda = M + \varpi , \qquad \eta = - \sqrt { 2 \Lambda ( 1 - \sqrt { 1 - e ^ { 2 } } ) } \sin \varpi , } \\ & { q = - \sqrt { 2 \Lambda \sqrt { 1 - e ^ { 2 } } ( 1 - \cos I ) } \sin \Omega . } \end{array}
$$

From these equations,

$$
\left( \frac { \xi } { \sqrt { \Lambda } } \right) ^ { 2 } + \left( \frac { \eta } { \sqrt { \Lambda } } \right) ^ { 2 } = 2 ( 1 - \sqrt { 1 - e ^ { 2 } } ) = e ^ { 2 } + { \textstyle { \frac { 1 } { 4 } } } e ^ { 4 } + \cdot \cdot \cdot .
$$

Therefore,

$$
e ^ { 2 } = \left( \frac { \xi } { \sqrt { \Lambda } } \right) ^ { 2 } + \left( \frac { \eta } { \sqrt { \Lambda } } \right) ^ { 2 } + \frac { 1 } { 4 } \left[ \left( \frac { \xi } { \sqrt { \Lambda } } \right) ^ { \overset { . } { 2 } } + \left( \frac { \eta } { \sqrt { \Lambda } } \right) ^ { 2 } \right] ^ { 2 } + \cdot \cdot \cdot .
$$

Since ${ \sqrt { 2 ( 1 - { \sqrt { 1 - e ^ { 2 } } } ) } } = e ( 1 + { \textstyle { \frac { 1 } { 8 } } } e ^ { 2 } + \cdot \cdot \cdot )$ and

$$
\frac { \xi } { \sqrt { \Lambda } } = \epsilon \cos \varpi \bigg ( 1 + \sum _ { j = 1 } ^ { \infty } \alpha _ { j } \epsilon ^ { 2 j } \bigg ) , \frac { \eta } { \sqrt { \Lambda } } = - \epsilon \sin \varpi \bigg ( 1 + \sum _ { j = 1 } ^ { \infty } \alpha _ { j } \epsilon ^ { 2 j } \bigg )
$$

with numerical coefficients $\alpha _ { j }$ , therefore,

$$
\begin{array} { l } { { \displaystyle e \cos \varpi = \frac { \xi } { \sqrt \Lambda } \biggl \{ 1 - \frac { 1 } { 8 } \left[ \biggl ( \frac { \xi } { \sqrt \Lambda } \biggr ) ^ { 2 } + \biggl ( \frac { \eta } { \sqrt \Lambda } \biggr ) ^ { 2 } \right] + \cdot \cdot \cdot \biggr \} , } } \\ { { \displaystyle e \sin \varpi = - \frac { \eta } { \sqrt \Lambda } \biggl \{ 1 - \frac { 1 } { 8 } \left[ \biggl ( \frac { \xi } { \sqrt \Lambda } \biggr ) ^ { 2 } + \biggl ( \frac { \eta } { \sqrt \Lambda } \biggr ) ^ { 2 } \right] + \cdot \cdot \cdot \biggr \} . } } \end{array}
$$

Hence $e \cos \varpi$ and $\textit { e s i n } { \varpi }$ can be expanded in integral power series of $\xi / \sqrt { \Lambda }$ and $\eta / \sqrt { \Lambda }$ Conversely, $\xi / \sqrt { \Lambda }$ and $\eta / \sqrt { \Lambda }$ can be expanded in integral power series of $\ell \cos \varpi$ and $\ e \sin \ \varpi$ Thus, $e \cos { \varpi }$ ， $\pmb { \ell }$ sin $\boldsymbol { \varpi }$ ， sin $( I / 2 )$ cos $\Omega$ ，sin $( I / 2 )$ sin $\Omega$ can be expanded in integral power series of $\xi / \sqrt { \Lambda } , \eta / \sqrt { \Lambda } , \phi / \sqrt { \Lambda } , q / \sqrt { \Lambda }$ The converse is also true.

Poincaré（l905；see also Charlier，1927；Picart,1906） treated the three-body problem in this manner.The coefficients of the expansion in the Keplerian elements are available in tabular form,as will be seen in a later chapter,but the coefficients of the expansion in Delaunay's or

Poincaré's elements are scarcely ever published (Noren and Walberg, 1910).So the expansion in either of these latter elements is not of much use,except in theoretical discussion. Delaunay himself has avoided making complete reference to his canonical variables in his lunar theory (Delaunay, 1860, 1867).

By the expansions for an elliptic motion in Sections 5.9-5.15 the expressions $u \mathrm { ~ - ~ } M ,$ cos $( u \mathrm { ~ - ~ } M )$ ，sin $( u \mathrm { ~ - ~ } M )$ can be expanded in integral power series of e cos $M _ { ; }$ esin $M .$ Similarly, $e ^ { 2 }$ cos $( u + M )$ ， $e ^ { 2 }$ sin $( u + M )$ can be expanded. Hence, the expressions $X , ~ Y$ in (106) of Section 5.10

$$
\begin{array} { l } { { \displaystyle \frac { X } { a } = \cos \left( u - M \right) \frac { 1 + \sqrt { 1 - e ^ { 2 } } } { 2 } } } \\ { ~ } \\ { { \displaystyle ~ + \varepsilon ^ { 2 } \cos \left( u + M \right) \frac { 1 - \sqrt { 1 - e ^ { 2 } } } { 2 e ^ { 2 } } - \varepsilon \cos M } , }  \\ { { \displaystyle \frac { Y } { a } = \sin \left( u - M \right) \frac { 1 + \sqrt { 1 - e ^ { 2 } } } { 2 } } } \\ { { \displaystyle ~ - \varepsilon ^ { 2 } \sin \left( u + M \right) \frac { 1 - \sqrt { 1 - e ^ { 2 } } } { 2 e ^ { 2 } } + \varepsilon \sin M } , }  \end{array}
$$

can be expanded in integral power series of e cos $M$ $\pmb { e }$ sin $M$ because

$$
e ^ { 2 } = e ^ { 2 } \cos ^ { 2 } M + e ^ { 2 } \sin ^ { 2 } M .
$$

As

$$
e \cos M = e \cos \varpi \cos \lambda - e \sin \varpi \sin \lambda ,
$$

$$
e \sin M = e \sin \varpi \cos \lambda + e \cos \varpi \sin \lambda ,
$$

the expressions e cos $M$ ， $^ { e }$ sin $M$ are Fourier series in $\lambda$ with the Fourier coefficients expressed as integral power series of $\xi / \sqrt { \Lambda } , \eta / \sqrt { \Lambda }$ . We have similar Fourier expansions for $X , Y .$

The coefficients of $X , Y$ in (108) of Section 5.10 are of the form

$$
\pm \cos ^ { 2 } \frac { I } { 2 } \cos \lambda \pm \sin ^ { 2 } \frac { I \cos } { 2 } \sin \lambda + 2 h ) \qquad \mathrm { o r } \qquad \sin I _ { \sin } ^ { \cos } ( \lambda + h ) .
$$

Their coefficients are expanded in integral power series of

$$
\sin { \frac { I } { 2 } } \cos \Omega , \qquad \sin { \frac { I } { 2 } } \sin \Omega ,
$$

and accordingly they are expanded in integral power series of $\xi / { \sqrt { \Lambda } } _ { \mathrm { { ; } } }$ $\eta / { \sqrt { \Lambda } } , \rho / { \sqrt { \Lambda } } , q / { \sqrt { \Lambda } }$ Hence, $x _ { 1 } , x _ { 2 } , x _ { 3 }$ are expanded in integral power series of these latter quantities and are of the form $\begin{array} { r } { \sum A N \cos { ( j \lambda + b ) } . } \end{array}$ ， where: (i), $N$ is an integral power series of $\xi , \eta , \phi , q$ ； (ii), $A$ and $b$ depend only on $\Lambda$ ; and (ii), $j$ is a positive or negative integer. Thus using (170) we can write the expansion in the form

$$
\sum A ^ { \prime } \Gamma ^ { s _ { 1 } } Z ^ { s _ { 2 } } \cos { ( j \lambda + j _ { 1 } \gamma + j _ { 2 } z + b ^ { \prime } ) } ,
$$

where $A ^ { \prime } , \ b ^ { \prime }$ depend only on $\Lambda$ ,and $j , j _ { 1 } , j _ { 2 }$ are positive or negative integers. The integers $2 s _ { 1 } , 2 s _ { 2 }$ are positive or zero, such that

$$
2 s _ { i } \geq | j _ { i } | \qquad ( i = 1 , 2 ) .
$$

A similar expansion can be obtained with respect to the Keplerian elements.

The perturbative term $\mu F _ { 1 }$ of (167) can also be expanded in a similar form，because ${ x _ { 3 \kappa } } _ { - 2 }$ ， ${ x _ { 3 \kappa } } _ { - 1 }$ ， ${ x } _ { 3 \kappa }$ are expanded in integral powers of $\xi _ { \kappa } , \eta _ { \kappa } , \ p _ { \kappa } , \ q _ { \nu }$ Actually, $\mu F _ { 1 }$ ceases to be regular with respect to $x _ { 3 \kappa - 2 }$ ${ x } _ { 3 \kappa - 1 }$ ， ${ x } _ { 3 \kappa }$ only when any one of the mutual distances of the $\pmb { n }$ bodies becomes zero or when $( x _ { 3 \kappa - 2 } ^ { \prime 2 } + x _ { 3 \kappa - 1 } ^ { \prime 2 } + x _ { 3 \kappa } ^ { \prime 2 } ) ^ { 1 / 2 }$ becomes zero. For the moment we ignore such cases and leave their discussion to a later chapter. In such cases the word“perturbation’’ loses its meaning；we cannot consider the actual motion as a small deviation from the intermediary orbit (in the present case an eliptic motion)，so that a completely different mode of treatment should be adopted. For convenience we limit ourselves to the case of three bodies； then the expansions are represented in the series form

$$
\begin{array} { r l } & { \quad F = F _ { 0 } + \mu F _ { 1 } , \quad \quad \quad \quad ( \quad ) } \\ & { \quad \mu F _ { 1 } = \sum A _ { s _ { 1 } s _ { 2 } s _ { 3 } s _ { 4 } } \Gamma ^ { s _ { 1 } } Z ^ { s _ { 2 } } \Gamma ^ { \prime s _ { 3 } } Z ^ { \prime s _ { 4 } } \cos { [ j \lambda + j ^ { \prime } \lambda ^ { \prime } + j _ { 1 } \gamma + j _ { 2 } \gamma ^ { \prime } + j _ { 3 } z + j _ { 4 } z + j _ { 4 } z ^ { \prime } ] } . } \end{array}
$$

where

$\pmb { A }$ is a function of $\Lambda , \Lambda ^ { \prime }$ ， $\Gamma ^ { \prime }$ is of magnitude $e ^ { 2 }$ ，

and

$z$ is of magnitude $\scriptstyle { I ^ { 2 } }$

The representative term written here is of degree $2 ( s _ { 1 } + s _ { 2 } + s _ { 3 } + s _ { 4 } )$ with respect to $\boldsymbol { e }$ and $\boldsymbol { \mathbf { \mathit { I } } } .$ This is called the degree of the term. Note that $F _ { 0 }$ is a function of $\Lambda$ and $\Lambda ^ { \prime }$ only and is not multiplied by any quantity of the order of the disturbing masses.

The quantities $\Lambda$ and $\Lambda ^ { \prime }$ are constant multiples of $\sqrt { a }$ and $\sqrt { a ^ { \prime } }$ ， respectively; $F _ { 0 }$ is the sum of constant multiples of $L ^ { - 2 }$ and $L ^ { \prime } ^ { - 2 }$ As $\mu F _ { 1 }$ is of degree $^ { - 1 }$ with regard to the length, so is the function $F$ itself.

Hence,the coefficient $A$ of (173） is homogeneous of degree -1 with regard to $a , a ^ { \prime }$ , and homogeneous of degree $- 2$ with regard to $\Lambda , \Lambda ^ { \prime }$ .Let

$$
\mathcal D = j \lambda + j ^ { \prime } \lambda ^ { \prime } + j _ { 1 } \gamma + j _ { 2 } \gamma ^ { \prime } + j _ { 3 } z + j _ { 4 } z ^ { \prime } ,
$$

that is, the quantity under the cosine in (l73).We call $\mathcal { D }$ the argument. From (l04）of Section 5.10,we have $\lambda = n t + \epsilon , \ \lambda ^ { \prime } = n ^ { \prime } t + \epsilon ^ { \prime }$ If $j = j ^ { \prime } = 0$ ，then $t$ does not appear explicitly in the argument,and the corresponding term does not contain $t$ explicitly. Such a constant term is called a secular term. If $j n + j ^ { \prime } n ^ { \prime } = 0$ ， $j \neq 0$ ， $j ^ { \prime } \neq 0$ ，then a similar situation occurs; this is when $n { : } n ^ { \prime }$ is rational, and the corresponding term is called a critical term.

The function $F _ { \mathrm { : } }$ ,being a function of the mutual distances of the three bodies,does not depend on the sense of the longitude. Consequently, $F$ remains unaltered when we change the signs of $\lambda , \lambda ^ { \prime } , \gamma , \gamma ^ { \prime } , z , z ^ { \prime }$ simultaneously.Hence $b ^ { \prime }$ ,representing the phase of the argument, should be zero and each term should be of cosine form.The differences

$$
2 s _ { i } - | j _ { i } | \qquad ( i = 1 , 2 , 3 , 4 ) ,
$$

are even positive integers including O, where $s _ { i }$ is a positive integral multiple of $\textstyle { \frac { 1 } { 2 } }$ Since ${ \pmb F }$ also remains unaltered by the reversal of the sense in which the inclination is taken, the sums $2 s _ { 3 } + 2 s _ { 4 }$ and $j _ { 3 } + j _ { 4 }$ should be even integers. Since $F$ remains unaltered by the rotation through δ in longitude of the coordinate axes around the origin，we obtain $j + j ^ { \prime } + j _ { 1 } + j _ { 2 } + j _ { 3 } + j _ { 4 } = 0$ 、Finally, the degree of a term of $F _ { 1 }$ is equal to or higher than the sum of the coefficients of $\lambda , \lambda ^ { \prime }$ in the argument; that is,

$$
\begin{array} { r l } & { 2 s _ { 1 } + 2 s _ { 2 } + 2 s _ { 3 } + 2 s _ { 4 } \geq | j _ { 1 } | + | j _ { 2 } | + | j _ { 3 } | + | j _ { 4 } | } \\ & { \qquad \geq | j _ { 1 } + j _ { 2 } + j _ { 3 } + j _ { 4 } | \geq | j + j ^ { \prime } | . } \end{array}
$$

Brown and Shook (l933) have called a series of this nature a $\mathbfcal { D } ^ { \ast }$ Alembert series. If we use Poincaré's elements, then the argument is $j \lambda + j ^ { \prime } \lambda ^ { \prime }$ and the coefficient has the factor $\xi ^ { s } { \eta } ^ { s } { _ 1 } { p } ^ { s } { \sf { s } } q ^ { s } { _ 3 }$ for one of the planets,and the factor $\xi ^ { \prime s } { _ { 2 } } \eta ^ { \prime s } { _ { 2 } } \phi ^ { \prime s } { _ { 4 } } { q ^ { \prime s } } _ { 4 } ^ { \prime }$ for the other planet instead of $\Gamma$ ， $z$ .The degree of the term is $s _ { 1 } + s _ { 1 } ^ { \prime } + s _ { 2 } + s _ { 2 } ^ { \prime } + s _ { 3 } + s _ { 3 } ^ { \prime } + s _ { 4 } + s _ { 4 } ^ { \prime }$ .Thus the differential equations are

$$
\frac { d \Gamma } { d t } = \mu \frac { \partial F _ { 1 } } { \partial \gamma } , ~ \frac { d Z } { d t } = \mu \frac { \partial F _ { 1 } } { \partial z } ,
$$

$$
\frac { d \lambda } { d t } = - \frac { \partial F _ { 0 } } { \partial \Lambda } - \mu \frac { \partial F _ { 1 } } { \partial \Lambda } , \qquad \frac { d \gamma } { d t } = - \mu \frac { \partial F _ { 1 } } { \partial \Gamma } , \qquad \frac { d z } { d t } = - \mu \frac { \partial F _ { 1 } } { \partial z } .
$$

A similar argument can be applied to other bodies.If the Hamiltonian function has the form (l56)and (157),then $F$ — given in (162)—is common to all bodies,and the differential equations are in canonical form.The element $\lambda$ ,as can be seen from (l74), varies rapidly compared with the other quantities, which vary slowly in the order of the disturbing mass $\mu$ . Note that $F _ { 0 }$ contains only $\Lambda$ and $\Lambda ^ { \prime }$ ; also, $\mu F _ { 1 }$ is a trigonometric series with the sum of integral multiples of $\lambda , \lambda ^ { \prime } , \gamma , \gamma ^ { \prime }$ ,Z, $z ^ { \prime }$ as argument, and each coefficient is an integral power series of $\Gamma , Z , \Gamma ^ { \prime } , Z ^ { \prime } { } _ { ; }$ ,multiplied by a function of $\Lambda$ ， $\Lambda ^ { \prime }$ .This is the fundamental form of the equations in the perturbation theory of celestial mechanics that will be the main topic of the following discussions.

Corresponding to (164a),we obtain from (l74) the expressions for the perturbation for time $t > 0$ ：

$$
\delta \Lambda = \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial \lambda } d t , \qquad \delta \Gamma = \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial \gamma } d t , \qquad \delta Z = \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial z } d t ,
$$

$$
\begin{array} { l } { { \displaystyle \delta \lambda = - \mu \sum _ { j = 1 } ^ { n } C _ { i j } \int _ { 0 } ^ { t } d t \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial \Lambda } d t - \int _ { 0 } ^ { t } \frac { \partial \Phi } { \partial \Lambda } d t - \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial \Lambda } d t , } } \\ { { \displaystyle \delta \gamma = - \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial \Gamma } d t , \qquad \delta z = - \mu \int _ { 0 } ^ { t } \frac { \partial F _ { 1 } } { \partial Z } d t . } } \end{array}
$$

These expressions are important for the discussion of the form of integrals in perturbation theory (Poincaré, 1905).

Each term in the solution of (174) or in (174a) in the form of trigonometric series corresponds to a term in the trigonometric series expansion of $\mu F _ { 1 }$ with the same argument. According to the method of integration, the degree or order of a term may differ from the degree or order of the corresponding term in the expansion of the disturbing function. Each term in the trignometric series for the expansion of the perturbation is called an inequality. Even though the expansion of the disturbing function may be convergent, it does not necessarily follow that the resulting solution for the perturbation is convergent. Indeed,the trigonometric series obtained in this manner is not,as a rule,uniformly convergent. Here we meet the greatest difficulty in perturbation theory. The theory at present resolves itself into the question; what is the best method of expansion for practical use, in which we can best approximate the rigorous solution by computing as few terms as possble from the series representation thus obtained? The series representation to be studied for such a purpose may be different for different planetary systems,for different satellite systems,or for artificial celestial vehicles. The same situation occurs when we use the Keplerian elements. As a matter of fact, the ordinary perturbation theory uses the Keplerian elements,as will be seen in Volume II.

# 5.18 KEPLERIAN ELEMENTS

In the Keplerian elements the expansion of the disturbing function is, as is easily verified,

$$
\begin{array} { r } { \sum \mathcal { C } \cos \mathcal { D } , \qquad } \\ { \mathcal { C } = \mathcal { A } e ^ { q _ { 1 } } e ^ { \prime q _ { 2 } } ( \tan I ) ^ { q _ { 3 } } ( \tan I ^ { \prime } ) ^ { q _ { 4 } } , \qquad } \\ { \mathcal { D } = j \lambda + j ^ { \prime } \lambda ^ { \prime } - j _ { 1 } \varpi - j _ { 2 } \varpi ^ { \prime } - j _ { 3 } \Omega - j _ { 4 } \Omega ^ { \prime } , } \end{array}
$$

where $\mathcal { A }$ has the same character as $\Lambda$ in (173),so that: (i),it is homo-geneous of degree $^ { - 1 }$ with respect to the two semimajor axes $^ { a }$ and $a ^ { \prime }$ ； (ii), $q _ { i } - | j _ { i } |$ $( i = 1 , 2 , 3 , 4 )$ are even positive integers including O, and; (ii), $q _ { 3 , } + q _ { 4 } \mathrm { a n d } j _ { 3 } + j _ { 4 }$ are both even, ir $\scriptstyle { 1 j + j ^ { \prime } + j _ { 1 } + j _ { 2 } + j _ { 3 } + j _ { 4 } = 0 }$ These are the necessary characteristics of a $D '$ Alembert series.

The Delaunay elements also have this character,as is evident from (168).The expressions $1 - G ^ { 2 } / L ^ { 2 } , 1 / 2 - H / ( 2 G )$ in terms of the Delaunay elements are, respectively, $e ^ { 2 } , 4 \sin ^ { 2 } { ( I / 2 ) }$ and take the place of $\Gamma$ ， $z$ in terms of the Poincaré elements,which are, respectively, $e ^ { 2 }$ ， $\tan ^ { 2 } I$ in terms of the Keplerian elements.Hence,the expansion of the disturbing function is of the form

$$
\begin{array} { l } { { { \displaystyle \sum _ { } A ( L , L ^ { \prime } ) \Big ( 1 - \frac { G ^ { 2 } } { L ^ { 2 } } \Big ) ^ { s _ { 1 } } \Big ( 1 - \frac { G ^ { \prime 2 } } { L ^ { \prime 2 } } \Big ) ^ { s _ { 2 } } \Big ( \frac { 1 } { 2 } - \frac { H } { 2 G } \Big ) ^ { s _ { 3 } } \Big ( \frac { 1 } { 2 } - \frac { H ^ { \prime } } { 2 G ^ { \prime } } \Big ) ^ { s _ { 4 } } } } } \\ { { \times \cos { [ j l + j ^ { \prime } l ^ { \prime } + ( j - j _ { 1 } ) g + ( j ^ { \prime } - j _ { 2 } ) g ^ { \prime } } } } \\ { { + ( j - j _ { 1 } - j _ { 3 } ) h + ( j ^ { \prime } - j _ { 2 } - j _ { 4 } ) h ^ { \prime } ] } . } \end{array}
$$

In his lunar theory Delaunay used the noncanonical elements

$$
\begin{array} { r l } { a } & { { } \mathrm { f o r } \quad L ^ { 2 } / \beta , } \\ { \ e } & { { } \mathrm { f o r } \quad \Big ( 1 - \frac { G ^ { 2 } } { L ^ { 2 } } \Big ) ^ { 1 / 2 } , } \end{array}
$$

The second set of the Delaunay canonical equations (164) is replaced by

$$
\begin{array} { l } { \displaystyle \frac { \partial F } { \partial L } = \frac { 2 } { n a } \frac { \partial F } { \partial a } + \frac { 1 - e ^ { 2 } } { n a ^ { 2 } e } \frac { \partial F } { \partial e } , } \\ { \displaystyle \frac { \partial F } { \partial G } = - \frac { \sqrt { 1 - e ^ { 2 } } } { n a e } \frac { \partial F } { \partial e } + \frac { 1 - 2 \gamma ^ { 2 } } { 4 n a ^ { 2 } \gamma \sqrt { 1 - e ^ { 2 } } } \frac { \partial F } { \partial \gamma } , } \\ { \displaystyle \frac { \partial F } { \partial H } = - \frac { 1 } { 4 n a ^ { 2 } \gamma \sqrt { 1 - e ^ { 2 } } } \frac { \partial F } { \partial \gamma } \qquad ( n ^ { 2 } a ^ { 3 } = \beta ) . } \end{array}
$$

Denote any one of the Keplerian elements by $E _ { i }$ ; then by the formulas of Section 1.14, with the Poisson brackets $( E _ { i } , E _ { j } )$ , we obtain the equations

$$
\frac { d E _ { i } } { d t } = - \sum _ { j } \left( E _ { i } , E _ { j } \right) \frac { \partial F } { \partial E _ { j } }
$$

for the variation of the Keplerian elements. Since these formulas are not canonical, we must refer as the standard to the elliptic motion of the body in question relative to the nth body-see (72),Section 5.7, on Poincaré's transformation $\gamma$ ，Then the following fundamental formulas (the most important in practical dynamical astronomy） are obtained for the variation of the Keplerian elements,similarly to (l63) and (164):

$$
\frac { d a } { d t } = 2 \sqrt { \frac { a } { \beta } } \frac { \partial R } { \partial \epsilon } ,
$$

$$
\begin{array} { r l } { \frac { d } { d t } } & { = - \frac { \omega _ { 0 } + \beta \delta } { \sqrt { \beta } } \frac { d } { d t } \frac { \nabla \cdot \mathbf { n } + \lambda \cdot \mathbf { d } \cdot \boldsymbol { d } \cdot \mathbf { d } \cdot \mathbf { d } \mathbf { \delta } } { \sqrt { \beta } } \frac { d } { \omega t } , } \\ { \frac { d } { d t } } & { = - \frac { 1 } { \omega _ { 0 } + \beta \cdot \mathbf { n } + \lambda \cdot \nabla \cdot \mathbf { d } \cdot \mathbf { d } \cdot \mathbf { d } \cdot \mathbf { \delta } } \frac { d \delta } { \omega t } - \frac { \lambda \cdot \mathbf { d } \cdot \mathbf { d } \cdot \mathbf { \delta } } { \sqrt { \omega t } } \frac { d } { ( 2 \pi \omega + \frac { \beta \delta } { \omega t } ) } \frac { d } { ( 2 \pi \omega + \frac { \beta \delta } { \omega t } ) } , } \\ { \frac { d } { d t } } & { = \frac { \omega _ { 0 } + \beta \cdot \mathbf { n } } { \omega _ { 0 } + \omega _ { \mathrm { m } } + \beta \cdot \mathbf { n } } \frac { d } { d t } , } \\ { \frac { d } { d t } } & { = \frac { \omega _ { 0 } + \beta \cdot \delta \cdot \mathbf { n } } { \sqrt { \beta } } \frac { d } { d t } + \frac { \omega _ { 0 } + \beta \cdot \mathbf { n } } { \sqrt { \beta } } \frac { d } { d t } , } \\ { \frac { d } { d t } } & { = - 2 \int _ { 0 } ^ { \infty } \frac { d } { d t } \frac { \partial \cdot \mathbf { n } } { \sqrt { \beta } } \frac { d } { d t } \frac { \partial \cdot \mathbf { n } + \lambda \cdot \mathbf { d } \cdot \mathbf { d } \cdot \mathbf { \delta } } { \sqrt { \omega t } } + \frac { \delta \cdot \mathbf { n } } { \sqrt { \beta } \cdot \mathbf { n } + \lambda \cdot \nabla \cdot \mathbf { d } \cdot \mathbf { \delta } } \frac { d } { \omega t } } \\ & { = \mathrm { i n } \omega _ { \mathrm { b } } + \frac { \omega _ { 0 } - \beta \cdot \mathbf { \delta } } { \sqrt { \omega t } } - \frac { \lambda \cdot \mathbf { d } \cdot \mathbf { \delta } } { \sqrt { \omega t } } \frac { d } { \omega t } \frac { d } { \omega t } \frac { d } { \omega t } \frac { d } { \omega t } } \\ &  = \kappa ^ { 2 } \sum _  m _ { 0 } < \frac  m _  0  \end{array}
$$

It is s n (173a), the mea nts of the trigonometric functions,and the semimajor axis $^ { a }$ is supposed to be present only in their coeffcients.Then $R$ is differentiated partially with respect to the Keplerian elements and $\mathbf { \boldsymbol { \epsilon } } ^ { \prime }$ substituted for $\epsilon$ in $\lambda$ ，such that $\epsilon + n t = \epsilon ^ { \prime } + \int \bar { n }$ dt. Here put

$$
\rho \equiv \int n d t = \sqrt { \beta } \int a ^ { - 3 / 2 } d t ;
$$

then

$$
\rho = - 3 \int _ { 0 } ^ { t } d t \int _ { 0 } ^ { t } \frac { 1 } { a ^ { 2 } } \frac { \partial R } { \partial \epsilon } d t .
$$

Ordinarily $R$ is called the disturbing function.The first term of $R$ is called its direct part and the second term its indirect part.It is customary in the treatment of planetary motion to take the sun as the nth body,and in the treatment of satellite motion to take the planet as the nth body.

The variation of elements in (175) is expressed by the partial derivatives of the disturbing function $R$ with regard to the Keplerian elements. It is sometimes useful to express this variation by the components of the disturbing force.Let $s$ be the component of the disturbing force in the direction of the instantaneous radius vector; let $_ T$ be the component, in the instantaneous orbital plane, perpendicular to the radius vector in the sense of the motion; and let $W$ be the component perpendicular to the instantaneous orbital plane,making a right-handed system with radius vector and the instantaneous velocity vector. The partial derivatives of $R$ in these directions - that is,the components of grad $R$ — are the components of the disturbing force in those directions.As these vectors are expressed by the Keplerian elements,those partial derivatives can easily be transformed to the partial derivatives with respect to the Keplerian elements.Such formulas for the variation of the elements in terms of $S , \ T , \ W _ { \colon }$ ，can be directly derived even when there does not exist the potential $- R$ for the disturbing force.The result (Tisserand, 1889；Charlier，1927；Plummer，1918；Brown，1896；Brown and Shook,1933；Andoyer，1923；Moulton，1923；Dziobek,1888；Smart, 1953;Danjon, 1953; Brouwer and Clemence,l96l） is:

$$
\frac { d a } { d t } = 2 \sqrt { \frac { a ^ { 3 } } { \beta } } \{ S \tan \phi \cos v + T \sec \phi ( 1 + e \cos v ) \} ,
$$

$$
\frac { d \epsilon } { d t } = \sqrt { \frac { a } { \beta } } \cos \phi \{ S \sin v + T ( \cos v + \cos u ) \} ,
$$

$$
\frac { d I } { d t } = r W \cos { ( \upsilon + \varpi - \Omega ) } / ( \sqrt { \beta a } \cos { \phi } ) ,
$$

$$
\frac { d \Omega } { d t } = r W \sin { \left( v + \varpi - \Omega \right) } / ( \sqrt { \beta a } \cos { \phi } \sin { I } ) ,
$$

$$
\begin{array} { l } { \displaystyle \frac { d \varpi } { d t } = \biggl \{ - a S \cos ^ { 2 } \phi \cos v + r T \sin v ( 2 + e \cos v ) } \\ { \displaystyle \qquad + r W \sin \phi \tan \frac { I } { 2 } \sin \left( v + \varpi - \Omega \right) \biggr \} / ( \sqrt { \beta a } \sin \phi \cos \phi ) , } \\ { \displaystyle \frac { d \epsilon } { d t } = - \frac { 2 r S } { \sqrt { \beta a } } + 2 \sin ^ { 2 } \frac { \phi } { 2 } \frac { d \varpi } { d t } + 2 \cos \phi \sin ^ { 2 } \frac { I } { 2 } \frac { d \Omega } { d t } , } \\ { \displaystyle \frac { d n } { d t } = - 3 \{ S \sin \phi \sin v + T ( 1 + e \cos v ) \} / ( a \cos \phi ) . \qquad ( 1 7 6 \propto \frac { I } { 2 } ) , } \end{array}
$$

ont.)

These formulas are especially important in the numerical computation of the variation of elements.When the positions of the disturbing planets are known at each instant, the numerical values of S, T, $W$ may be computed from these positions and the differential equations of motion numerically integrated.Such a method of computing the perturbation by numerical processes is generally called the theory of special perturbation (Bauschinger, 1928; Stracke,1929).

# 5.19ANGULAR MOMENTUM INTEGRALS

The angular momentum of the $\pmb { n }$ -body system is invariant by Poincare's transformation $\beta$ ，as has been seen in Section 5.6.For these fictitious planets, the angular momentum,by (l60) of Section 5.16 and (84b) of Section 5.9, is equal to $h _ { 2 } = \sqrt { \beta a ( 1 ~ - ~ e ^ { 2 } ) }$ .The orbital plane is determined by the inclination $\boldsymbol { \mathit { I } }$ and the longitude of the node $\Omega$ with reference to the ecliptic.Hence, the components of the angular momentum (which is perpendicular to the instantaneous orbital plane) in the directions of the three coordinate axes are $\sqrt { \beta a ( 1 - e ^ { 2 } ) }$ sin $I \sin \Omega , - \sqrt { \beta a ( 1 - e ^ { 2 } ) }$ sin $I \cos \Omega$ ,and $\sqrt { \beta a ( 1 - e ^ { 2 } ) }$ cos $\boldsymbol { \mathit { I } } .$ The six orbital elements at any given instant are computed from the six data for the coordinates and the momenta at that instant. If the actual motion is rigorously Keplerian, then the six orbital elements thus computed are all constant. If it is not Keplerian, then the values of these orbital elements vary from time to time,and those variations are given by the equations (l64), (174),or (175).Such orbital elements are called the osculating elements. The actual orbit touches the Keplerian orbit at the given instant. Also, the magnitude and the direction of the momenta are the same for the two orbits; so are the expressions for the coordinates and the momenta in terms of the orbital elements and t.The Keplerian elements for the actual orbit vary with time but those for the Keplerian orbit remain constant.Hence, if these expressions for the coordinates are partially differentiated with respect to $\pmb { t }$ (which appears explicitly) they give the velocity components. If they are multiplied by the mass, they give the momentum components.On this basis, the formulas for the variation of elements of Sections 5.17-5.19 are derived; the right-hand members of these formulas are supposed to be given in terms of the elements and t.

Thus the angular momentum just given is the same for the actual orbit and the osculating Keplerian orbit；hence, the components of the resultant angular momentum of the whole system are constant:

$$
\begin{array} { r l r } {  { \sum _ { j = 1 } ^ { n - 1 } \sqrt { \beta _ { j } a _ { j } ( 1 - e _ { j } ^ { 2 } ) } \sin I _ { j } \sin \Omega _ { j } = c _ { 1 } , } } \\ & { } & { - \sum _ { j = 1 } ^ { n - 1 } \sqrt { \beta _ { j } a _ { j } ( 1 - e _ { j } ^ { 2 } ) } \sin I _ { j } \cos \Omega _ { j } = c _ { 2 } , } \end{array}
$$

$$
\sum _ { j = 1 } ^ { n - 1 } \sqrt { \beta _ { j } a _ { j } ( 1 - e _ { j } ^ { 2 } ) } \cos I _ { j } = c _ { 3 } .
$$

In particular, for the three-body problem,with the osculating elements $a , e , I , \Omega , a ^ { \prime } , e ^ { \prime } , I ^ { \prime } ,$ $\Omega ^ { \prime }$ of the two fictitious bodies, we have

$$
\begin{array} { r } { \sqrt { \beta a ( 1 - e ^ { 2 } ) } \sin I \sin \Omega + \sqrt { \beta ^ { \prime } a ^ { \prime } ( 1 - e ^ { \prime 2 } ) } \sin I ^ { \prime } \sin \Omega ^ { \prime } = c _ { 1 } , } \end{array}
$$

$$
- \sqrt { \beta a ( 1 - \ell ^ { 2 } ) } \sin I \cos \Omega - \sqrt { \beta ^ { \prime } a ^ { \prime } ( 1 - \ell ^ { \prime 2 } ) } \sin I ^ { \prime } \cos \Omega ^ { \prime } = c _ { 2 } ,
$$

$$
{ \sqrt { \beta a ( 1 ~ - ~ e ^ { 2 } ) } } \cos I + { \sqrt { \beta ^ { \prime } a ^ { \prime } ( 1 ~ - ~ e ^ { \prime 2 } ) } } \cos I ^ { \prime } = c _ { 3 }
$$

Take the invariable plane of the three bodies as the standard plane and denote the values of $I , \ \Omega$ in this case by ${ { I } _ { 0 } } , \ { { \Omega } _ { 0 } }$ Put $c ^ { 2 } = c _ { 1 } ^ { 2 } + c _ { 2 } ^ { 2 } + c _ { 3 } ^ { 2 }$ and substitute $I _ { 0 } , \Omega _ { 0 } , I _ { 0 } ^ { \prime } , \Omega _ { 0 } ^ { \prime } , 0 , 0 .$ and $\pmb { c }$ for $I , \Omega , I ^ { \prime } , \Omega ^ { \prime } , c _ { 1 } , c _ { 2 } ,$ and $c _ { 3 }$ ， respectively, in (177).Divide the first and second equations side by side, to obtain tan $\Omega _ { 0 } = \tan \Omega _ { 0 } ^ { \prime }$ .Reference to the third equation shows that $\Omega _ { 0 } = \Omega _ { 0 } ^ { \prime } + \pi$ .This last relation expresses the elimination of the nodes (Jacobi).

In terms of the Delaunay elements, (l77) is writen

$$
\begin{array} { r l r } & { } & { \sqrt { G ^ { 2 } - H ^ { 2 } } \sin h + \sqrt { G ^ { \prime 2 } - H ^ { \prime 2 } } \sin h ^ { \prime } = c _ { 1 } , } \\ & { } & { - \sqrt { G ^ { 2 } - H ^ { 2 } } \cos h - \sqrt { G ^ { \prime 2 } - H ^ { \prime 2 } } \cos h ^ { \prime } = c _ { 2 } , } \\ & { } & { H + H ^ { \prime } = c _ { 3 } . } \end{array}
$$

If we take the invariable plane as the standard, then

$$
G ^ { 2 } - H ^ { 2 } = G ^ { \prime 2 } - H ^ { \prime 2 } , \qquad h = h ^ { \prime } + \pi , \qquad H + H ^ { \prime } = c .
$$

By these relations we can reduce the order of the differential equations (164).

According to the third equation of (178a),

$$
\frac { d H } { d t } + \frac { d H ^ { \prime } } { d t } = 0 .
$$

Hence, from (164),

$$
\frac { \partial F } { \partial h } + \frac { \partial F } { \partial h ^ { \prime } } = 0 .
$$

Write the expression (173b) for the expansion of ${ \pmb F }$ in the form

$$
F = \sum A \stackrel { \cos } { \sin } ( i l + i ^ { \prime } l ^ { \prime } + j g + j ^ { \prime } g ^ { \prime } + k h + k ^ { \prime } h ^ { \prime } ) ,
$$

and combine with (179).Then we see that $\pmb { k } + \pmb { k } ^ { \prime } = 0$ ，and that $h , h ^ { \prime }$ appear in $F$ only as $h - h ^ { \prime }$ . We have just seen that $h - h ^ { \prime } = \pi$ when we take the invariable plane as the standard.In this case $\pmb { F }$ depends on neither $h$ nor $h ^ { \prime }$ .Hence by solving (l78a) we obtain

$$
\begin{array} { l } { { G = \Theta , \displaystyle \qquad G ^ { \prime } = \Theta ^ { \prime } , } } \\ { { \displaystyle { \cal H } = \frac { c } { 2 } + \frac { 1 } { 2 c } ( \Theta ^ { 2 } - \Theta ^ { \prime 2 } ) , \qquad { \cal H } ^ { \prime } = \frac { c } { 2 } - \frac { 1 } { 2 c } ( \Theta ^ { 2 } - \Theta ^ { \prime 2 } ) . } } \end{array}
$$

Substituting (164) in $d h / d t = d h ^ { \prime } / d t$ obtained from (178a), we obtain

$$
\frac { \partial F } { \partial H } = \frac { \partial F } { \partial H ^ { \prime } } .
$$

From (181), we obtain

$$
\frac { \partial F } { \partial \Theta } = \frac { \partial F } { \partial G } , \frac { \partial F } { \partial \Theta ^ { \prime } } = \frac { \partial F } { \partial G ^ { \prime } } .
$$

Hence, the differential equations (164) of the three-body problem are reduced to

$$
\begin{array} { l l } { { { \frac { d { \cal L } } { d t } } = { \frac { \partial F } { \partial l } } , { ~ { \cfrac { d \Theta } { d t } } = { \frac { \partial F } { \partial g } } , } } } \\  { { \frac { d \overline { { { l } } } } { d t } } = - { \frac { \partial F } { \partial L } } , { ~ { \cfrac { d g } { d t } } = - { \frac { \partial F } { \partial \Theta } } , } } \\ { { { \frac { d { \cal L } ^ { \prime } } { d t } } = { \frac { \partial F } { \partial l ^ { \prime } } } , { ~ { \cfrac { d \Theta ^ { \prime } } { d t } } = { \frac { \partial F } { \partial g ^ { \prime } } } , } } } \\ { { { \frac { d { l ^ { \prime } } } { d t } } = - { \frac { \partial F } { \partial L ^ { \prime } } } , { ~ { \cfrac { d g ^ { \prime } } { d t } } = - { \frac { \partial F } { \partial \Theta ^ { \prime } } } , } } } \end{array}
$$

which is of the eighth order and has four degrees of freedom.We obtain the common direction of the ascending nodes from dh $/ d t = - \partial F / \partial H$ by substituting in (181） after solving (182).

In the plane three-body problem, $\begin{array} { r } { \pmb { I } = \pmb { 0 } , } \end{array}$ Put

$$
G = H = \Pi , ~ G ^ { \prime } = H ^ { \prime } = \Pi ^ { \prime } ;
$$

then the integral of the angular momentum reduces to one equation

$$
\Pi + \Pi ^ { \prime } = c ,
$$

and $\partial F / \partial g + \partial F / \partial g ^ { \prime } = 0$ Hence, $\pmb { F }$ depends on $g , \ g ^ { \prime }$ only through $g - g ^ { \prime }$ . Write

$$
K = \Pi , \qquad \kappa = g - g ^ { \prime } ;
$$

then

$$
\begin{array} { l } { \displaystyle \frac { d K } { d t } = \frac { d \Pi } { d t } = \frac { \partial F } { \partial g } = \frac { \partial F } { \partial \kappa } , } \\ { \displaystyle \frac { d \kappa } { d t } = - \frac { \partial F } { \partial \Pi } + \frac { \partial F } { \partial \Pi ^ { \prime } } = - \frac { \partial F } { \partial K } , } \end{array}
$$

and (164) is now of the form

$$
\begin{array} { r l r l } & { \frac { d L } { d t } = \frac { \partial F } { \partial l } , } & & { \frac { d L ^ { \prime } } { d t } = \frac { \partial F } { \partial l ^ { \prime } } , } & & { \frac { d K } { d t } = \frac { \partial F } { \partial \kappa } , } \\ & { \frac { d l } { d t } = - \frac { \partial F } { \partial L } , } & & { \frac { d l ^ { \prime } } { d t } = - \frac { \partial F } { \partial L ^ { \prime } } , } & & { \frac { d \kappa } { d t } = - \frac { \partial F } { \partial K } , } \end{array}
$$

which is of the sixth order and has three degrees of freedom. The longitude of the perihelion is determined from $d g / d t = - \partial F / \partial \Pi$ by (184) after we solve (184a).

Since $\Theta = L \sqrt { 1 - e ^ { 2 } }$ in (182)， we have $L ^ { 2 } > \Theta ^ { 2 }$ Similarly, $L ^ { \prime 2 } > \Theta ^ { \prime 2 }$ .But we know that

$$
\Theta \cos I + \Theta ^ { \prime } \cos I ^ { \prime } = c , \qquad \Theta \sin I + \Theta ^ { \prime } \sin I ^ { \prime } = 0 .
$$

Hence,

$$
| \Theta | + | \Theta ^ { \prime } | \geq | c | , \qquad | \Theta | - | \Theta ^ { \prime } | \leq | c | .
$$

If $\Theta$ is approximately equal to $L$ ,then $F$ is not regular with regard to 0.Since (173b) shows that $s _ { 1 } , s _ { 2 }$ are integral multiples of $1 / 2$ , the function $\pmb { F }$ can be developed in an integral power series not of $L - \Theta$ but of $\sqrt { L - \Theta }$ . In such a case we have only to use the variables $\xi , \eta$ given by (170) of Section 5.17. The function $F$ is regular with regard to $\xi , \eta$

Furthermore, if $| \Theta | + | \Theta ^ { \prime } | = | c |$ or $| \Theta | - | \Theta ^ { \prime } | = | c |$ holds approximately，that is,if $\pmb { I }$ and ${ \bf \nabla } { \bf { \mathit { r } } } ^ { \prime }$ are small (for example, if the relation $\Theta + \Theta ^ { \prime } = c$ holds approximately)，then ${ \pmb F }$ can be developed in an integral power series of $\phi , q , \xi , \eta , \phi ^ { \prime } , q ^ { \prime } , \xi ^ { \prime } , \eta ^ { \prime }$ of Section 5.17, that is,of $\sqrt { L - G }$ $\sqrt { L ^ { \prime } - G ^ { \prime } }$ ， $\sqrt { G - H } , \sqrt { G ^ { \prime } - H ^ { \prime } }$ .If ${ \boldsymbol { J } } = { \boldsymbol { I } } ^ { \prime } = 0$ ，then $G = H , G ^ { \prime } = H ^ { \prime }$ , and the last two of the four square roots become zero. The function $\pmb { F }$ is regular with regard to $G , G ^ { \prime }$ $\hat { z } ^ { \prime } , \sqrt { G - H } , \sqrt { G ^ { \prime } - H ^ { \prime } }$ But,as we have discussed in Section 5.17, $F$ remains unaltered when we change the signs of $\pmb { I }$ and ${ \mathbf { } } ^ { \prime \prime }$ , that is, when we increase $\Omega$ and $\Omega ^ { \prime }$ by $1 8 0 ^ { \circ }$ Hence, ${ \pmb F }$ remains unaltered when we change the signs of $\pmb { \mathscr { p } } \ q , \pmb { \mathscr { p } } ^ { \prime }$ ， and $q ^ { \prime }$ simultaneously, and,consequently, the signs of $\sqrt { G - H }$ and $\sqrt { G ^ { \prime } - H ^ { \prime } }$ simultaneously. Thus, $\pmb { F }$ is regular with regard to $G , \ G ^ { \prime }$ ， $\sqrt { ( G - H ) ( G ^ { \prime } - H ^ { \prime } ) }$ ,when $\pmb { I }$ and $\scriptstyle { I ^ { \prime } }$ are small. But, by (181),

$$
\begin{array} { c } { { \sqrt { ( G - H ) ( G ^ { \prime } - H ^ { \prime } ) } = \displaystyle \frac { 1 } { 2 c } \sqrt { [ ( \Theta - c ) ^ { 2 } - ( \Theta ^ { \prime } { } ^ { 2 } ] [ ( \Theta ^ { \prime } - c ) ^ { 2 } - ( \Theta ^ { 2 } ] } } } \\ { { { } } } \\ { { { } = \displaystyle \frac { 1 } { 2 c } ( \Theta + \Theta ^ { \prime } - c ) \sqrt { ( \Theta - c - \Theta ^ { \prime } ) ( \Theta ^ { \prime } - c - \Theta ^ { \prime } ) } . } } \end{array}
$$

Hence, $G , G ^ { \prime }$ ， $\sqrt { ( G - H ) ( G ^ { \prime } - H ^ { \prime } ) }$ are regular with regard to $\Theta , \Theta ^ { \prime }$ ， $\Theta + \Theta ^ { \prime } = c .$ Accordingly, $\pmb { F }$ is regular with regard to $\Theta , \Theta ^ { \prime } , \Theta + \Theta ^ { \prime } = c .$ If the relation $\Theta - \ \Theta ^ { \prime } = c$ holds approximately, then it can be similarly shown that $\pmb { F }$ is regular with regard to @, $\Theta ^ { \prime }$ ， $\Theta - \Theta ^ { \prime } = c$

In the plane three-body problem, $F$ has,by (173b),the form

$$
F = \sum A ( L ^ { 2 } - G ^ { 2 } ) ^ { s _ { 1 } } [ L ^ { \prime 2 } - ( c - G ^ { \prime } ) ^ { 2 } ] ^ { s _ { 2 } } \cos { ( i l + i ^ { \prime } l ^ { \prime } + i g + i ^ { \prime } g ^ { \prime } ) }
$$

If $\tau \approx G$ and $L ^ { \prime } \approx G ^ { \prime }$ ,that is,if $\pmb { \ell }$ or $e ^ { \prime }$ is small, then $\pmb { F }$ is developed in integral powers of $\sqrt { L ^ { 2 } - G ^ { 2 } }$ ， $\sqrt { L ^ { \prime 2 } - G ^ { \prime 2 } }$ and looks as though it were not single-valued. This situation can be avoided by transforming to the variables $\xi , \eta , \xi ^ { \prime } , \eta ^ { \prime }$ by the transformation of Section 5.17.

In the variables (l69) of Section 5.17 the integrals of the angular momentum are

$$
\begin{array} { r l } & { - \sqrt { ( \Lambda - \Gamma ) ^ { 2 } - ( \Lambda - \Gamma - Z ) ^ { 2 } } \sin z } \\ & { \phantom { - } - \sqrt { ( \Lambda ^ { \prime } - \Gamma ^ { \prime } ) ^ { 2 } - ( \Lambda ^ { \prime } - \Gamma ^ { \prime } - Z ^ { \prime } ) ^ { 2 } } \sin z ^ { \prime } = c _ { 1 } , } \\ & { - \sqrt { ( \Lambda - \Gamma ) ^ { 2 } - ( \Lambda - \Gamma - Z ) ^ { 2 } } \cos z } \\ & { \phantom { - } - \sqrt { ( \Lambda ^ { \prime } - \Gamma ^ { \prime } ) ^ { 2 } - ( \Lambda ^ { \prime } - \Gamma ^ { \prime } - Z ^ { \prime } ) ^ { 2 } } \cos z ^ { \prime } = c _ { 2 } , } \\ & { \phantom { - } \Lambda - \Gamma - Z + \Lambda ^ { \prime } - \Gamma ^ { \prime } - Z ^ { \prime } = c _ { 3 } . } \end{array}
$$