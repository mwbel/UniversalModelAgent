# 项目运行指南

## 环境要求

- Node.js 18+
- Python 3.11
- npm

## 1. 一次性安装依赖

在项目根目录执行：

```bash
npm run install:all
cp backend/env.example backend/.env
cp frontend/.env.example frontend/.env.local
```

如需使用 AI 问答，请在 `backend/.env` 中填写自己的 `DEEPSEEK_API_KEY`。

## 2. 同时启动前后端

```bash
npm run dev
```

- 前端：`http://localhost:3000`
- 后端：`http://127.0.0.1:5001`
- 后端健康检查：`http://127.0.0.1:5001/api/health`

本地默认使用 5001，以避开 macOS AirPlay 常占用的 5000 端口。

开发环境默认把前端 `/api` 请求转发到本地 Flask。线上部署可把 `BACKEND_ORIGIN` 设置为线上后端域名，不要在浏览器代码中硬编码后端地址。

## 3. 构建与检查

```bash
npm run check
npm run build
```

`check` 包括前端 TypeScript/ESLint 检查和后端单元测试。

## 主要页面

- 首页：`http://localhost:3000`
- 日历：`http://localhost:3000/cal`
- 天文图：`http://localhost:3000/earth`
- AI 问答：`http://localhost:3000/chat`
- 知识文档：`http://localhost:3000/docs`

## 提交边界

需要提交源码以及 `frontend/public` 中的知识库、主题 HTML 和图片资源；不要提交以下本地产物：

- `node_modules/`
- `backend/venv/`
- `.next/`
- `__pycache__/`
- `.env`、`.env.local`
- `*.log`、`*.tsbuildinfo`
