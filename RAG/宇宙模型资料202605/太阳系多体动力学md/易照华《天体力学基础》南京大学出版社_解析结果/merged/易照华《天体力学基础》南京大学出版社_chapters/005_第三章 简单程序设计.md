# 第三章 简单程序设计

在第二章里，我们学习了字处理软件。在字处理软件的支持下，可以很方便地用计算机进行文字的编辑工作。目前，各式各样的软件在各种计算机上得到了广泛的应用。在不同软件的支持下，人们可以用计算机高效地处理不同领域的问题。软件的核心是计算机工作者用各种程序设计语言编制的一些程序。这些程序控制着计算机，使之有效地完成人们交给它们的任务。为了较深人地了解与应用计算机，学习一些程序设计的基础知识是很有必要的。

要进行程序设计，需要使用程序设计语言。常用的程序设计语言种类很多。从这一章开始，我们选用一种简单易学的BASIC语言，以BASIC程序为例，介绍一下程序设计的基本方法。

# 第一节BASIC语言简介

# 一、什么是BASIC语言

BASIC是英语 Beginner's All-purpose Symbolic Instruction Code 的缩写，意思是“初学者通用符号指令代码”。同时，basic又是一个英语单词，含“基本的、基础的”之义。BASIC 语言是编程序应用计算机解决实际问题时常用的一种语言。用BASIC语言编程序时，可以使用某些简单的英文单词、专用的符号和数字来表达意思。在计算机里，特别是微型机里，一般都配有厂家提供的BASIC语言的解释程序。BASIC解释程序把这些单词、符号和数字表达的意思“翻译”成计算机能执行的指令，指挥计算机工作。BASIC语言有它自已的表示法与规则，我们将在介绍程序设计方法时，陆续介绍BASIC语言的有关表示法和规则。

学校里常用的中华学习机和Apple机，在出厂时就配备了BASIC解释程序。开机时屏幕上显示出的]叫做BASIC提示符，它表示“BASIC解释程序已经开始工作，可以使用BASIC语言了”。

使用BASIC语言有两种方式，一种叫命令方式，也叫立即执行方式；另一种叫程序方式，也叫延迟执行方式。

# 二、命令方式

从键盘上输人BASIC语言中的某些命令，计算机会立即执行命令所规定的操作，例如，计算、显示信息等等。

# 1．让计算机计算

利用 BASIC命令，可以让计算机计算代数式的值。

例如，要让计算机计算 $3 4 0 8 2 + 9 1 7 5$ 的值，可以在屏幕上显示］提示符时，键入PRINT和算术式 $3 4 0 8 2 + 9 1 7 5$ 。按回车键后，计算机会计算这个式子的值并显示出结果来。整个过程的屏幕显示如下：

]PRINT 34082+9175← 43257

]

（为便于说明操作过程，用 $\hookleftarrow$ 表示按回车键，实际操作时，屏幕上并不显示 $\nrightarrow$ ；每行中]后面的字符是从键盘上输入的；开头没有］的字符是计算机输出的。下同。)

BASIC 语言有许多命令，这里使用的是PRINT命令。PRINT的英文原意是印刷、打印。在]提示符后面输入PRINT和一些表达式，计算机就会计算表达式的值并在屏幕上输出结果。这里的 $3 4 0 8 2 + 9 1 7 5$ 是比较简单的BASIC表达式。实际应用时，表达式可能会更复杂，而且BASIC表达式与数学中的式子也略有不同。由于键盘的限制等技术原因，BASIC语言规定，在表达式中:

（1）用 $^ *$ 表示乘号，而且 $^ { * }$ 不能省略。用/表示除号和分数线。加减运算的符号与数学中的相同，即用 $^ +$ 表示加，用－表示减。

（2）用‘表示幂运算，例如，125的3次幂要用 $1 2 5 ~ { \textdegree } ~ 3$ 来表示。

(3）只允许使用圆括号。需要使用多重括号时，圆括号可以嵌套。

（4）开平方、三角函数、对数等运算，用标准函数表示。常用的标准函数及所执行的运算可列表如下：

<table><tr><td rowspan=1 colspan=1>标准函数</td><td rowspan=1 colspan=1>运                      算</td></tr><tr><td rowspan=1 colspan=1>ABS (X)</td><td rowspan=1 colspan=1>求X的绝对值</td></tr><tr><td rowspan=1 colspan=1>SQR (X)</td><td rowspan=1 colspan=1>求X的算术平方根</td></tr><tr><td rowspan=1 colspan=1>INT(X)</td><td rowspan=1 colspan=1>取不大于X的最大整数</td></tr><tr><td rowspan=1 colspan=1>RND(X)</td><td rowspan=1 colspan=1>产生（0,1)之间的随机小数</td></tr><tr><td rowspan=1 colspan=1>SIN(X)</td><td rowspan=1 colspan=1>求X的正弦函数值（X的单位是弧度）</td></tr><tr><td rowspan=1 colspan=1>COS(X)</td><td rowspan=1 colspan=1>求X的余弦函数值（X的单位是弧度）</td></tr><tr><td rowspan=1 colspan=1>TAN(X)</td><td rowspan=1 colspan=1>求X的正切函数值（X的单位是弧度）</td></tr><tr><td rowspan=1 colspan=1>EXP (X)</td><td rowspan=1 colspan=1>求e的幂(e=2.71828183)</td></tr><tr><td rowspan=1 colspan=1>LOG(X)</td><td rowspan=1 colspan=1>求X的自然对数lnx,即以e为底x的对数值</td></tr></table>

（5）计算表达式的值时，运算的优先顺序是，先由内层向外层计算括号里的式子，其次计算标准函数的值，再其次进行乘方运算，然后进行乘、除运算，最后进行加、减运算。

(6）表达式的所有字符必须在一行中输入。  
请看下面几个例子。

例3.1用计算机计算 $- 3 4 \times 2 8 \div 1 7 - 5 ^ { 3 } + \sqrt { 9 }$ 的值。

操作：在]提示符后面键入PRINT和BASIC表达式，屏幕显示如下。

]PRINT $- 3 4 * 2 8 / 1 7 - 5 \land 3 + \mathrm { S O R } ( 9 ) \square$ -178

]

所以， $- 3 4 \times 2 8 \div 1 7 - 5 ^ { 3 } + \sqrt { 9 } = - 1 7 8 .$ 例3.2用计算机计算 $2 \pi - 2 . 8 \sqrt { 2 \pi }$ 的值。

分析：解决这个问题的关键是正确用BASIC表达式表示题目中的代数式， $\pi$ 值可取3.1415926。

操作及显示如下：

]PRINT $2 { \star 3 } . 1 4 1 5 9 2 6 - 2 . 8 { \star } \mathrm { S O R } ( 2 { \star 3 } . 1 4 1 5 9 2 6 ) { \star } ^ { \upsilon }$ -.73537391

]

例3.3求 $\left| { \begin{array} { l } { { \displaystyle 1 } } \end{array} } - { \frac { 1 } { 1 - { \frac { 1 } { 3 } } } } \right|$ 的值。

分析：把繁分式写成BASIC表达式时，应注意加括号，以保证计算时与原式的运算顺序一致。

操作及显示如下：

]PRINT ABS(1/3-1/(1-1/3))← 1.16666667

]

从上面几个例子中，可以看到BASIC表达式与数学表达式的区别，同时还可以看到计算机显示数时的一些特点。如，显示正数时，数的前面没有 $^ +$ 号；显示负数时，前面有一个－号;对于纯小数，显示时省略了小数点前面的0。此外，使用BASIC语言时，计算机对于数还有一些特殊的处理。请看下面的例子。

例3.4 分别用手工和计算机计算1234的3次幂。

解：用手工计算得到

$$
1 2 3 4 ^ { 3 } = 1 8 7 9 0 8 0 9 0 4 ;
$$

用计算机计算，操作过程屏幕显示如下：

]PRINT $1 2 3 4 \cdot 3  \nu$ 1.8790809E+09 ]

比较两次计算的结果，可以看到计算机的输出与手工计算的结果有所不同。计算机输出时，采用的是科学记数法 (也叫指数记数法）。

在中华学习机或Apple机上，一般用科学记数法表示绝对值不小于99999999.2的数和绝对值小于0.01的数，否则用日常的记数方式来表示。用科学记数法表示数的形式是

$$
\pm \mathrm { \bf ~ N E } \pm \mathrm { \bf ~ n n }
$$

其中， $\pm \mathrm { ~ \bf ~ N ~ }$ 叫做尾数，可以是非零整数或整数部分是一位非零数字的小数，小数点后面最多可有8位数字。表示正数时，正号省略掉；E是英文exponent 的第一个字母的大写形式，表示指数。后面的 $\pm$ nn 叫阶码，阶码有两个整数位，表示尾数所乘的10的幂指数。绝对值小于10的阶码，输出时十位上补0。阶码为正时， $^ +$ 号不省略。

因此，例3.4中计算机输出的结果 $1 . 8 7 9 0 8 0 9 \mathrm { E } + 0 9$ 表示 $1 . 8 7 9 0 8 0 9 \times 1 0 ^ { 9 }$ ，即1879080900。又如，让计算机执行PRINT-55/334455661时，屏幕上会显示出 $- 1 . 6 4 4 4 6 3 1 2 \mathrm { E } - 0 7 _ { : }$ ，表示$- 1 . 6 4 4 4 6 3 1 2 \times 1 0 ^ { - 7 }$ ,即－0.0000000164446312。

计算机能处理的数有一定的范围。中华学习机和Apple机能处理的数的绝对值不能大于$1 . 7 0 1 4 1 1 8 3 \times 1 0 ^ { 3 8 }$ ，否则，计算机不能正确计算。例如，让计算机计算18的38次幂时，屏幕上不会输出计算结果，主机扬声器发出“嘟”的一声后，会显示出错信息：

]PRINT $1 7 . 1 \sim 3 8  \nu$ ？OVERFLOW ERROR ]

屏幕上显示的OVERFLOWERROR表示运算过程中发生了“溢出错误”，说明运算结果的绝对值太大了。习惯上把这种现象叫做上溢。如果一个数的绝对值小于 $2 . 9 3 8 7 5 8 8 \times 1 0 ^ { - 3 9 }$ ,计算机也不能正确计算。例如，让计算机计算2.93与10的－39次幂的积时，屏幕上会给出错误结果：

]PRINT 2.93\*1E-39   
0   
]

这种现象叫做下溢。实际上，发生下溢时，计算机把绝对值小于 $2 . 9 3 8 7 5 8 8 \times 1 0 ^ { - 3 9 }$ 的数近似地当作0来处理。对于下溢，计算机不会显示出错信息，扬声器也不会发出声响。

# 2.让计算机输出字符

用 PRINT命令，可以让计算机把用双引号括起来的一串字符照原样输出。

例如，在PRINT后面键入”HELLO!IAMACOMPUTER.",按回车键后，屏幕上下一行会显示出双引号中的那串字符，但不会显示双引号本身：

]PRINT \*HELLO! | AM A COMPUTER:'

HELLO! IAM A COMPUTER.

]

在BASIC语言里，把两个双引号括起来的字符叫做字符串。PRINT命令中如果有双引号括起来的字符串，计算机就会照原样输出字符串的内容。字符串可以由汉字、字母、数字、空格以及其他符号组成。字符串中可以有多个字符，字符的个数叫做字符串的长度。长度为0的字符串叫做空字符串或空串。例如，计算机执行 PRINT""后，会输出一个空行。空串不同于只由空格组成的字符串。

PRINT命令可以有多个输出项。输出项之间可以用分号或逗号隔开。例如，在PRINT命令中顺序键入” $\mathbf { A } = "$ ； $4 ~ * ~ 5$ ， $- 9 9$ 时，屏幕显示如下：

]PRINT $\mathbf { \tilde { \rho } } \mathsf { A } = \mathbf { \tilde { \rho } }$ ;4\*5.-99   
$\mathsf { A } = 2 0$ 199

]

可以看到，屏幕上先显示的是 $\mathbf { A } =$ ,紧接着的是20,空几个格以后显示的是－99。

在这里，PRINT命令有三个输出项" $\mathbf { A } { = } { \boldsymbol { \ " } }$ $4 { * } 5$ 和数－99。其中 $\mathbf { A } =$ 是双引号括起来的字符串，计算机执行时，照原样输出。 $4 { * } 5$ 是表达式，计算机执行时先计算它的值然后输出20这个计算结果。 $- 9 9$ 是一个数，是表达式的一个特例。对于PRINT命令中的这种由一个数构成的表达式，计算机直接输出该数。因此，这里输出的是－99。

${ \bf \ " } \mathbf { A } = \bf \ " $ 与 $4 { * } 5$ 之间是用分号隔开的，而 $4 { * } 5$ 与－99之间是用逗号隔开的。PRINT 命令中用来把输出项隔开的分号和逗号控制着输出的格式。计算机输出用分号隔开的输出项时，会在屏幕上把它们一个紧接着一个地显示出来。我们把这种输出格式叫做紧凑格式；输出用逗号隔开的输出项时，每个输出项会在屏幕上的一些标准位置上开始显示，我们把这种输出格式叫做标准格式或分区格式。

在中华学习机和Apple机的显示器上，每行可以显示40个字符，其中第1、第17、第 33个字符的位置是标准位置。一行中用逗号隔开的输出项多于3个时，从第4项开始会自动转到下一行继续输出。输出的字符显示在一行中第40个字符的位置时，紧接在后面的字符也会自动转到下一行继续输出.

# 三、程序方式

用计算机解决的问题往往很复杂，需要先编好程序，告诉计算机如何一步一步地进行操作，然后再让计算机执行。请看一个例子。

例3.5让计算机接受从键盘上输入的任意两个数，然后分别计算并输出这两个数的和与积。

操作：先输入程序，操作及屏幕显示如下。

]10 INPUT X,Y←   
]20 PRINT“和 $\mathbf { \Phi } = \mathbf { \tilde { \Phi } } ; \mathbf { X } + \mathbf { Y } \longleftrightarrow$   
]30 PRINT“积 $\mathbf { \Phi } = \mathbf { \tilde { \Phi } } ; \mathsf { X } ^ { \ast } \mathsf { Y } \ \gets \pmb { y }$   
]40 END   
]

操作完毕，屏幕上显示出]。这时，计算机不执行什么操作，而是等待进一步的指示。从键盘上输入RUN并按回车键后，屏幕上会显示出一个？号。这时，输入两个数，例如987和 343后(输入时用逗号把两个数隔开)，计算机就会进行一系列的操作，并输出运算结果。过程如下。

]RUN亻  
？ 987,343和 $= 1 3 3 0$   
积 $= 3 3 8 5 4 1$

]

RUN是让计算机运行程序的命令。计算机接到RUN命令后，就开始执行已经输入的程序。

这种先输入一个BASIC程序，然后键人RUN并按回车键让计算机自动运算的方式，叫做BASIC语言的程序方式。

# 第二节BASIC程序

# 一、BASIC程序的组成

在例3.5的操作中，我们输入的是一个简单的BASIC程序。一般说来，BASIC程序是由若干语句行组成的。语句行由行号、语句、行结束符（回车）组成。语句的作用是告诉计算机进行哪些操作。每个语句由语句定义符和语句体构成。这个简单程序可以列表分析如下：

<table><tr><td>行号</td><td>语句定义符</td><td>语句体</td><td>行结束符</td><td>功 能</td></tr><tr><td>10</td><td>INPUT</td><td>XY</td><td>←</td><td>接收从键盘上输入的两个数</td></tr><tr><td>20</td><td>PRINT</td><td>&quot;和=&quot;;X+Y</td><td>←</td><td>输出两个数的和</td></tr><tr><td>30</td><td>PRINT</td><td>&quot;积 =&quot;; X*Y</td><td>←</td><td>输出两个数的积</td></tr><tr><td>40</td><td>END</td><td></td><td>←</td><td>结束</td></tr></table>

每个语句行开头的数字是行号，也叫标号。BASIC语言规定，行号可以是0到63999之间的无符号整数。它确定了语句在程序中的位置。相邻的两个语句行的行号可以不连续，以便需要时插入新的语句行。计算机按行号由小到大的顺序执行各语句行所规定的操作。

语句中的 INPUT、PRINT、END等英文单词叫做语句定义符。BASIC语言的语句定义符是该语句的名称，它规定了语句的功能。不同的语句定义符使计算机进行不同的操作。

语句定义符后面，一般是语句体。语句体为计算机指出操作对象、操作内容等。有些语句定义符（如例3.5中的END)后面没有语句体。这样的语句定义符可以独自确定要计算机执行的具体操作。

# 二、变量

例 3.5的这个程序中，X和Y是变量名。

BASIC 程序中常常用到变量。可以把变量看作计算机存储器里存储数据的一些存储单元。向存储单元中存人一个数据后，代表这个存储单元的变量就获得了该数据的值。变量的值是可变的，存人一个新数据后，变量中的旧值就被新值所取代，因此称之为变量。例如，存人一个数3以后，变量的值是3,再存入另一个数－99.99,变量的值就变成－99.99。

不同的变量有不同的变量名，就像旅馆里不同的房间有不同的房间号一样。根据房间号，旅馆服务员可以很快地找到不同房间的客人。根据变量名，计算机可以找到不同的存储单元，然后把数据放到里面或者从里面读出先前存放的数据。

变量名一般要用一、两个英文字母或者一个英文字母后面跟着一个数字组成。

为便于认读，也可以用两个以上字符作变量名，但计算机只识别前两个字符。例如，在计算机看来，MENU和MEMBER这两个变量名指的是同一个变量。

变量名不能是BASIC语言专用的英文单词，如INPUT、PRINT等，而且，变量名中也不能含有这些英文单词，例如，BEND(含有 END）CPRINTS(含有PRINT)都不能作变量名。变量名中不能使用 $+ - * / \ \backslash$ 等字符。

# 三、几个基本语句

# 1. INPUT语句

在例3.5的程序里，第10语句行中的 语句是键盘输入语句也叫 INPUT语句。INPUT语句的格式是

INPUT $<$ 变量名表

在英语中，INPUT的意思是“输入”。INPUT语句的功能是接收从键盘上输入的数据。计算机执行INPUT语句时，先显示一个？号，等待从键盘上输入的数据。输人数据后，计算机把输入的数据存储在变量名表指定的变量里，以备进一步处理之用。

INPUT语句中可以有一个或多个变量名，它们构成变量名表。如果有多个变量名，要用逗号把它们隔开。例如，

INPUT X,Y,Z

如果分隔多个变量名的不是逗号，例如用了分号，输人数据以后，扬声器会发出声响，并显示出？ SYNTAX ERROR IN xX

]

指出程序的语句有错误，并指出有错误语句的行号。

在 BASIC程序里，用INPUT语句除了可以为变量提供数值以外，还可以提供字符串。请看下面的程序。

例3.6

10 PRINT "HOW OLD ARE YOU? "

20 INPUT "I AM "； A

30 PRINT "WHAT IS YOUR NAME? "

40 INPUT "MY NAME IS"； N\$

50 PRINT \*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*

60 PRINT "HELLO "； N\$ ； "！ "

70 PRINT "YOU ARE "; A; " YEARS OLD"

80 END

在 20、40这两个语句行的INPUT语句中，变量名前面都有一个用双引号括起来的字符串，并用分号与后面的变量名隔开。INPUT语句中的这种字符串叫做提示字符串。计算机执行带有提示字符串的INPUT语句时，先输出提示字符串的内容，然后等待从键盘上输人的信息。这时，计算机不再在屏幕上显示问号 (除非提示字符串中含有问号）。

带有提示字符串的INPUT语句的格式是

# INPUT”提示信息"； $<$ 变量名表

第 40语句行中的N\$是以\$结尾的变量名，这类变量叫做字符串变量。字符串变量是用来

存放字符串的。

把这个程序输入计算机，键入RUN并按回车键以后，屏幕上首先显示出

HOW OLD ARE YOU? - AM

输入一个数，例如17,并按回车键后，屏幕上又显示出

WHAT IS YOUR NAME? MY NAME IS

这时输入一串字符，例如WANG HEPING,按回车键后屏幕上就会输出

HELLO WANG HEPING!

YOUR ARE 17 YEARS OLD.

计算机执行语句40时，我们从键盘上输人了一个字符串WANGHEPING。计算机接受了这个字符串以后，把它存放在语句40中指出的变量N\$里。计算机执行语句60时，又把存放在N\$ 里的字符串输出到了屏幕上。

计算机执行语句20时，应为变量A输人一个数值型数据。如果错误地输入了字符串，屏幕上会显示出

？REENTER？

要求重新输入数值型数据。计算机执行语句40时，应为字符变量N\$输人一个字符串。如果输入了数，计算机会把它当作字符串来对待。

# 2.PRINT语句

例3.5的程序中，20、30这两行语句是PRINT语句。PRINT语句的格式是

PRINT $<$ 输出项表

其中，输出项可以是数、字符串、变量或其他表达式。有多个输出项时，要用分号或逗号隔开。例如,

PRINT $" { \mathbf A } = "$ ； A, $\mathbf { \ " B } = \mathbf { \ " }$ ; B

例3.5中的这两个PRINT语句输出项的表达式中，出现了变量名。计算机执行时，先从变量名指定的变量中读出两个数据的值，然后用这些具体的值代入表达式中的变量进行计算并输出结果。

在PRINT语句的末尾，可以使用逗号或分号。PRINT语句末尾的逗号或分号不影响本语句的输出格式，但对程序中这个语句后面再次出现的第一个PRINT语句起作用。例如，计算机执

100 PRINT 39，40 ;   
110 PRINT 41

这两个语句后，屏幕上会显示出39 4041

又如，计算机执行

200 PRINT 1，17,  
210 PRINT 33

这两个语句后，屏幕上的输出情况如下：

1 17 33

这说明，计算机执行相邻的两个PRINT语句时，如果前面的PRINT语句末尾有分号，后一个语句输出的第一项会紧接着前一个语句所输出的最后一项；如果前面的PRINT语句末尾有逗号，后一个语句输出的第一项会位于前一个语句输出的最后一项的下一个标准位置。

# 3.END语句

例3.5的程序中，语句40是END语句。END语句没有语句体。计算机遇到END语句时，就停止执行这个程序。

# 4.赋值语句

在BASIC程序中，可以在程序运行时，用INPUT语句把数据存储在变量里。这样做的优点是，编程序时可以不知道程序要处理的是什么数据，程序运行时，再根据需要输入具体的数据，比较灵活。缺点是机器等待输入数据时，占用了一些时间。INPUT语句没有计算功能，它只能把数值或字符串提供给变量。

在许多情况下，常用赋值语句完成为变量提供数据的操作。

例3.7计算半径为15的圆的面积的程序。

10 LET $\mathrm { P I } { = } 3 . 1 4 1 5 9$   
20 LET $\scriptstyle \mathbf { A } = \mathbf { P I } \ * \ 1 5 \ \hat { \mathbf { \Omega } } 2$   
30 PRINT A  
40 END  
键入RUN并按回车键以后，屏幕上会显示出

706.85775

在这个程序里，语句10、20叫做赋值语句。赋值语句的格式是其中， $^ { * } = { } ^ { n }$ 叫做赋值号。赋值语句的功能是把赋值号右边表达式的值存放到赋值号左边变量名指定的变量中去。如果表达式需要计算才能确定它的值，计算机会先进行计算。例如，计算机执行

时，先计算出 $3 5 + 4 4$ 的值 79,然后再把79存放到变量Y中去。

变量被赋予一个值以后，这个值就会被保存在该变量里，直到用LET或 INPUT语句给它一个新值，才可能发生变化。在BASIC程序里，用LET或 INPUT语句为变量提供具体值之前，数值型变量中的值是0；字符串型变量为空串（其他型号的机器可能有例外)。

英文单词LET有“让……怎么样”的意思。赋值号可以读做“变成”。因此，从字面上看，语句10的意思是：“让变量PI的值变成3.14159”。计算机执行语句10这个赋值语句时，把赋值号左边的变量PI原来的值0变成了赋值号右边的值3.14159;语句20的意思是“让变量A的值变成赋值号右边表达式 $\mathbf { P I } * 1 5 \hat { \mathbf { \Omega } } 2$ 的值”。计算机执行语句20时，先计算出表达式的值706.85775，然后把这个值存放在变量A中。

比较一下这个例子与例3.5,可以发现，用PRINT语句和LET语句都可以计算表达式的值。但是，它们之间是有很大区别的。用PRINT语句计算时，计算完了马上输出，不保存计算结果；用LET语句计算时，计算完以后不输出结果，而是把结果保存在一个变量里，供以后处理时使用。

赋值语句的语句定义符LET可以省略，例如，LET $\mathbf { Y } = 3 5 + 4 4$ 可以写成 $\mathbf { Y } = 3 5 + 4 4$ 。

# 四、BASIC程序的输入、修改与运行

输入BASIC程序时，要一行一行地输入每个语句行的所有字符 (包括空格符)，每输人完一行，按一次回车键。按回车键的作用是告诉计算机这一行语句已经输入完毕。计算机接到回车键的信号后，就把回车键前面输入的内容存入内存储器。等到所有的语句都输入完以后，计算机内存储器里就存好了一个完整的BASIC程序。

为了查看一下输入到内存储器中的程序，可以用LIST命令在屏幕上列出程序的清单。例如，输入了例3.5的那个程序后，键入LIST和回车键，屏幕显示如下：

]LIST 10 INPUT X,Y 20 PRINT“和 $= \because x + Y$ 30 PRINT“积 $= { } ^ { \prime \prime } { \cdot } \mathsf { X } ^ { \ast } \mathsf { Y }$ 40 END

]

当程序较长时，可以在LIST命令中加上一些参数来显示指定的某些行语句，使每次显示的程序清单能在一个屏幕的范围之内。LIST命令的格式及功能如下表所示：

<table><tr><td>命令格式</td><td>功 能</td></tr><tr><td>LIST LIST n1 LIST n - n2 LIST n - LIST -n2</td><td>显示整个程序清单 显示行号为n，的一个语句行 显示从行号n到行号n的程序清单 显示从行号n，开始的程序清单 显示从第一个语句行到语句行n的程序清单</td></tr><tr><td colspan="2">（其中的n、n是非负整数。）</td></tr></table>

如果输入程序时，按回车键前发现有错误，可以按标有 $\triangleleft$ 或 $\twoheadleftarrow$ 的光标移动键把屏幕上闪烁着的矩形光标移到错误字符的位置上，然后再输入正确的字符。如果发现已输人的语句行有错，可以在提示符]后面重新输入那个语句行，按回车键后，计算机会用重新输人的语句行取代行号相同的原来那个语句行。

如果要在程序中插入新语句，可以在提示符]后面输入要插入的语句，用适当的新行号决定新语句在程序中的位置，而不必考虑]的位置。例如，要让例3.5中的那个程序增加计算并输出两个数的差与商的功能，可以进行下列操作：

]35 PRINT“商 $\boldsymbol { \mathbf { \mu } } = : \mathsf { X } / \mathsf { Y } \gets \boldsymbol { \mathbf { \mu } } \cdot \mathsf { X }$ ]25PRINT“差 $\quad = \mathbf { \ " } ; \mathbf { \times - \nabla } \mathcal { S } \sim \mathbf { - } \mathbf { \nabla }$ 1

计算机会把这两个新语句存人内存储器。用LIST命令列程序清单时，可以看到它们已经被安排到了正确的位置上：

]LIST 10 INPUT X,Y 20PRINT和 $ = \because x + Y$ 25PRINT差 $\mathbf { \partial } = \mathbf { \tilde { \gamma } } ; \mathsf { X } - \mathsf { Y }$ 30 PRINT“积 $\boldsymbol { \mathbf { \check { \Pi } } } = \boldsymbol { \mathbf { \check { \Psi } } } ; \mathsf { X } ^ { \ast } \mathsf { Y }$ 35 PRINT“商 $= \mathrm { \tilde { \Omega } } ; \times / \mathrm { \Delta Y }$ 40 END

]

如果要删除某个语句行，可以在提示符]后面键人要删除的那个语句行的行号，然后按回车键，该语句行就会被删除。

程序输入以后，如果不关机就会一直保存在计算机内存储器里。为了避免混乱，输人新程序时，一般先用NEW命令把内存储器中的旧程序清除掉。

为了在程序中标明整个程序或某段程序的功能，还可以在程序中适当的位置上安排一些注释语句。注释语句的语句定义符是REM(英文remark 的缩写，意为“注意")，因此也叫REM语句。语句体可以是说明性的文字。例如，可以在例3.5中的程序里增加25和35语句行后再加上

# 5REM本程序可用于计算任意两个数的和差积商

这个语句①，来标明这个程序的功能。计算机遇到REM语句时，并不执行任何操作。REM语句的作用只是为程序做些注释，以便人们查看程序清单时能对程序的功能、结构一目了然。

在输入、修改程序等操作中，为了使屏幕显示整洁，可以用HOME命令清除屏幕上的所有字符。键入HOME命令后，屏幕上显示的程序或其他字符都会消失，]提示符会重新显示在屏幕的左上角。这时，已经输入的程序并没有消失，仍然存放在内存储器里。

程序经输人、修改，确认无误后，就可以键人RUN命令让计算机运行了。计算机接到RUN命令后，就会按照行号从小到大的顺序逐条执行存放在内存储器里的程序中各语句规定的操作。例如，要用扩展了功能后的例3.5中的程序计算89与37的和、差、积、商，操作及显示如下：

]RUN  
${ \ ? } 8 9 , 3 7  { \nu }$   
和 $= 1 2 6$   
差 $= 5 2$   
积 $= 3 2 9 3$   
商 $= 2 . 4 0 5 4 0 5 4 1$

]

存放在内存储器里的程序可以多次使用。例如，再输人RUN命令和1992与1993这两个数，计算机会再次输出所要的结果：

]RUN   
？1992,1993亻   
和 $= 3 9 8 5$   
差 $= - 1$   
积 $= 3 9 7 0 0 5 6$   
商 $=$ .9999498244   
]

如果程序中有不符合BASIC语言规则的地方，例如，输错了语句定义符的字母，计算机执行时，扬声器会发出声响，并显示出

？ SYNTAX ERROR IN xXX

指出程序的语句有错误，并指明出错语句的行号。如果输入的数据有问题，也可能出现错误。例如，运行这个程序时输入了12993和0这两个数后，计算机执行到语句35时，扬声器会发出声

响，屏幕上会显示出？ DIVSION BY ZERO ERROR IN 35]  
指出在执行语句35时，出现了用0做除数的错误。

# 第三节顺序结构程序举例

# 一、顺序结构的程序

至此，我们已经接触了一些BASIC程序。这些程序运行时，计算机按行号从小到大的顺序逐行执行各个语句所规定的操作。我们说这样的程序是顺序结构的程序，简称顺序程序。下面再看几个顺序程序的例子。

例3.8某代销点出售《无线电》、《计算机》和《看世界》三种杂志，它们的定价分别是1.20元、1.55元和2.00元。利用下面的这个程序可以在输入各种杂志的订购数量后，立即输出一个货单。

100 REM 输入数据  
110 PRINT"请输入订购数量"  
120 PRINT "\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*"  
130 INPUT“无线电："；N1  
140 INPUT"计算机:"；N2  
150 INPUT“看世界:"；N3  
160 REM 计算金额与总价  
170 LET $\mathbf { P } 1 = 1 . 2 : { \mathrm { L E T ~ P } } 2 { = } 1 . 5 5 : { \mathrm { L E T ~ P } } 3 { = } 2$   
180 LET $\mathbf { T } 1 = \mathbf { P } 1 * \mathbf { N } 1 : \mathbf { L } \mathbf { E } \mathbf { T } \ \mathbf { T } 2 { = } \mathbf { P } 2 * \mathbf { N } 2 : \mathbf { L } \mathbf { E } \mathbf { T } \ \mathbf { T } 3 { = } \mathbf { P } 3 * \mathbf { N } 3$   
190 LET $\mathrm { T L } { = } \mathrm { T } 1 { + } \mathrm { T } 2 { + } \mathrm { T } 3$   
200 REM 输出货单  
210 HOME  
220 PRINT“品名数量",”单价","金额"  
230 PRINT "\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*"  
240 PRINT“无线电 "; N1, P1, T1  
250 PRINT"计算机"; N2,P2,T2  
260 PRINT"看世界";N3,P3,T3  
270 PRINT \*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\*\* "  
280 PRINT"总计人民币:"；TL；"元"  
290 END

在这个程序里，100、160、200等三个REM语句注明三个程序段的功能。第二个程序段的170、180两个语句行中都出现了三个语句。BASIC语言允许在一行中输人用冒号隔开的多个语句，计算机按它们的先后顺序执行。

程序运行时，先由第一个程序段的三个INPUT语句把键盘上输入的三种杂志的订数分别存入 N1,N2和 N3这三个变量。再由第二个程序段计算杂志所需金额。最后由第三个程序段输出货单。例如，从键盘上输入三种杂志的订货数10、20和30后，屏幕上会输出如下货单：

<table><tr><td colspan="2">品名数量 单价 金额</td></tr><tr><td colspan="2">水***水*水*水*水*水*水 水****</td></tr><tr><td>无线电 10</td><td>1.2 12</td></tr><tr><td>计算机 20</td><td>1.55 31</td></tr><tr><td>看世界</td><td>60</td></tr><tr><td colspan="2">30 2</td></tr></table>

总计人民币：103元

# 二、赋值语句的应用

用LET语句可以为一个变量重复赋值。每次赋予变量一个新值以后，变量中的旧值就被新值所取代。请看一个例子。

例3.9某粮库3月4日存粮 ${ 5 0 0 0 0 0 } \mathrm { { k g } } ,$ ，3月5日调进粮食 $3 0 0 0 0 \mathrm { k g }$ ，3月6日调出全部存粮的二分之一，3月7日调出 $1 5 0 0 0 \mathbf { k g }$ 。编一个程序，让计算机输出每天的库存数。

分析：粮库的库存数是逐日变化的，可以设置变量来存放每天的库存数。为了节省存储空间，可以只设置一个变量。处理时，每次把当天的库存数统计好存人变量里，然后输出适当的文字和变量的当前值，来说明当天的库存数。

程序：

10REM 粮库的库存数  
20 LET $\mathbf { X } { = } 5 0 0 0 0$   
30 PRINT $^ { \prime \prime } 3$ 月4日库存数:"；X;"KG"  
40 LET $\mathbf { X } { = } \mathbf { X } { + } 3 0 0 0 0$   
50 PRINT $" 3$ 月5日库存数:"；X;"KG"  
60 LET ${ \bf X } = { \bf X } / 2$   
70 PRINT $" 3$ 月6日库存数:"；X;"KG"  
80 LET $\mathbf { X } { = } \mathbf { X } { - } 1 5 0 0 0$   
90 PRINT $^ { \prime \prime } 3$ 月7日库存数:"；X;"KG"  
100 END  
运行这个程序，屏幕上会输出  
3月4日库存数：50000KG  
3月5日库存数：80000KG  
3月6日库存数：40000KG  
3月7日库存数：25000KG

在数学里像 $\scriptstyle \mathbf { X } = \mathbf { X } + 3 0 0 0 0$ 、 $\mathbf { X } { = } \mathbf { X } { - } 1 5 0 0 0$ 这样的式子是不成立的，这是因为数学里的 ${ \bf \omega } ^ { \prime \prime } = { \bf \omega } ^ { \prime \prime }$ 是等号，表示的是相等关系。BASIC语言的赋值语句里， $^ { \ast } = { } ^ { \ast }$ 是赋值号，表示的是计算机把表达式的值赋给变量的一种操作。这样，从字面上看，语句20的意思是“让X的值变成 $5 0 0 0 0 ^ { \prime }$ ；语句40的意思是“让X的值变成X原来的值加上 $3 0 0 0 0 ^ { \prime }$ ；语句60的意思是“让X的值变成X原来值的二分之一"；语句80的意思是“让X的值变成X原来的值减去 $1 5 0 0 0 ^ { \prime }$ 。实际上，计算机执行这四个语句时就是按上述意思去操作的，即执行语句20时，让变量X的值变成 50000;执行语句40时，先取来变量X的值50000,与表达式中的数 30000相加后把结果再存人变量X,使变量X的值变成了80000;执行语句60时，先取来变量X的值 80000,除以2后把商再存人变量X使变量X的值又变成了40000;执行语句80时，先取来变量X的值40000,减去15000后把差存人变量X,使程序结束时变量X的值变成了25000。

# 三、字符串相加

利用LET语句也可以给字符串变量赋值。请看下面的程序。

例3.10

10 LET $\mathbf { A } = 3 : { \mathrm { L E T ~ } } \mathbf { B } = 4$   
20 LET $\mathbf { A } \mathbb { S } = { } " \mathrm { T H E }$ " : LET $\mathbf { B } \mathbb { S } = \mathbf { \ " B } \mathbf { O } \mathbf { X } ^ { \prime \prime }$   
30 LET $\mathbf { C } = \mathbf { A }$ : LET ${ \bf { D } } { = } { \bf { A } } { + } { \bf { B } }$   
40 LET $\mathbf { C } \mathbf { \mathbb { S } } { = } \mathbf { B } \mathbf { \mathbb { S } }$ : LET ${ \bf D } \mathbb { S } = { \bf A } \mathbb { S } + { \bf B } \mathbb { S }$   
50 PRINT C,D,A   
60 PRINT C\$,D\$,B\$   
70 END

运行这个程序，屏幕上会显示出

<table><tr><td>3 7</td><td>3</td></tr><tr><td>BOX THE BOX</td><td>BOX</td></tr></table>

计算机执行40语句行中的LET ${ \bf D } { \mathbb S } = { \bf A } { \mathbb S } + { \bf B } { \mathbb S }$ 时，先把A\$和B\$这两个字符串变量中存放的字符“THE”和“BOX”连接起来得到一个新字符串“THE BOX”，然后让D\$的内容变成“THE BOX”。这种把字符串连接起来的操作，叫字符串相加。

# 四、程序设计的几个步骤

应用计算机解决实际问题时，一般需要经过 $\textcircled{1}$ 提出问题、 $\textcircled{2}$ 确立数学模型、 $\textcircled{3}$ 确定算法、$\textcircled{4}$ 画出框图、 $\textcircled{5}$ 编写程序、 $\textcircled{6}$ 上机调试等几个步骤。下面我们举一个简单的例子初步说明一下这几个步骤。

# 1．提出问题

例如，有甲、乙两个杯子分别装着果汁和咖啡，要求把两个杯子中的饮料交换过来。试模拟这个交换过程。

# 2．确立数学模型

计算机不能代替我们做实物的移动和交换，但可以模拟这个过程。如果用变量A和变量B分别模拟甲、乙两个杯子，用代码1表示果汁，用代码2表示咖啡，就可以把交换饮料的物理问题转换成交换两个变量值的数学问题。后者（交换两个变量的值）可以看作前者（交换两个杯子中的饮料）的数学模型。

# 3.确定算法

为解决一个问题而采取的方法和步骤，通常叫做算法。算法不仅指数值计算的方法和步骤，而且也包括非数值运算的方法和步骤。例如，棋谱可以看作下棋的算法，菜谱可以看作做菜的算法等等。

对于这个例子，可以把交换甲、乙两个杯子中的饮料的算法表示如下：

$\textcircled{1}$ 将甲杯中的果汁倒入空杯丙中;  
$\textcircled{2}$ 将乙杯中的咖啡倒入甲杯;  
$\textcircled{3}$ 将丙杯中的果汁倒入乙杯。

实现这个算法的关键是使用第三个杯子 (丙杯)。同样地，可以确定交换变量A和变量B的值的算法：

$\textcircled{1}$ $\mathbf { T }  \mathbf { A } ($ 将变量A的值赋给变量T)$\textcircled{2}$ $\mathbf { A }  \mathbf { B } ($ 将变量 B的值赋给变量 A)$\textcircled{3}$ $\mathbf { B } \gets \mathrm { \Delta T } ($ (将变量T的值赋给变量B)

请结合交换饮料的过程想一想，下面的算法能不能交换变量A和变量B的值，为什么？

$$
\begin{array} { r } { \textcircled { 1 } \quad \mathbf { B } \gets \mathbf { A } } \\ { \textcircled { 2 } \quad \mathbf { A } \gets \mathbf { B } } \end{array}
$$

# 4.画出框图

画框图就是用图形来描述算法，也常常有一个由粗到细的过程。框图又叫流程图。用框图可

以把解决问题的思路、方法、步骤等，清晰、直观地表示出来，为下一步的编写程序打下良好的基础。尤其是遇到较复杂的程序时，画框图的过程不能省略。

根据算法，可以画出解决这个问题的框图：

![](../chunks/易照华《天体力学基础》南京大学出版社_part_0001_pages_0001-0070/auto/images/74fb8660f36e1613ec4d9b945bc78fdb1167944db86396d7872ecf9d5f9d0411.jpg)  
图3.1(a)  
图3.1 (b)

习惯上把左右为圆弧的框叫做起止框，用来表示处理过程的开始与结束；把平行四边形框叫做输入输出框，用来表示处理过程中的输入输出操作；把矩形框叫做处理框，用来表示计算等操作。图3.1(a)是一个粗框图，经过细化得到图3.1(b)。

# 5.编写程序

根据框图，可以写出下面的程序。

10 REM 交换两个变量的值   
20 LET $\mathbf { A } { = } 1 : \operatorname { L E T } \ \mathbf { B } { = } 2$   
30 PRINT $\mathbf { \ " { A } } = \mathbf { \ " { } }$ ；A， $\mathbf { \ " B } = \mathbf { \ " }$ ；B   
40 LET $\mathbf { T } = \mathbf { A }$   
50 LET $\mathbf { A } = \mathbf { B }$   
60 LET $\mathbf { B } = \mathbf { T }$   
70 PRINT $" \mathbf { A } = "$ ;A， $\mathbf { \ " B } = \mathbf { \ " }$ ;B   
80 END

请指出，图3.1(b)中的各框分别与程序中的哪些语句行对应。

# 6.上机调试

程序编好后，要输入计算机。在编写与输入的过程中，往往会发生一些错误，在计算机上运行时也可能出错。这就需要检查、修改以排除程序中的错误。这个过程叫做调试。

把上面的程序输入计算机并且运行它，看看屏幕上是否输出

$\mathsf { A } = \mathsf { 1 }$ $\mathsf { B } = 2$ $\mathsf { A } = 2$ $\mathsf { B } = \mathsf { 1 }$ ]

如果输出结果与此相符，说明程序完成了题目的要求，交换了变量A与B的值。如果没得到这个结果，说明程序有错或录人有误，这时就需要检查和修正错误。

下面再看一个例子。

例3.11 设计一个BASIC程序，用计算机统计并输出每个学生语文、数学、英语、物理和计算机等各科考试成绩的总分和平均分。

分析：要解决这个问题，需要先输人一个学生的姓名、学号、各科成绩等信息，然后由计算机计算总分，再把总分除以5得到平均分，最后输出有关信息。反复运行程序，就能统计出所有学生的总分和平均分。

框图：由于问题比较简单，可以一步画出较细的框图 (见图3.2)。

程序：用适当的语句完成框图中每一框规定的操作，可编出下面的程序。

100 REM 计算并输出各科考试成绩的总分和平均分

110 REM输人有关信息

120 INPUT"请输入学号与姓名:"；N，N\$

130 PRINT“请输入各科考试成绩："

140 INPUT“语文:"；CH  
150 INPUT“数学:"；MA  
160 INPUT"英语:"；EN  
170 INPUT“物理:"；PH  
180INPUT"计算机:"；CO  
190 REM 计算总分和平均分  
$2 0 0 \mathrm { \ T L } { = } \mathrm { C H } { + } \mathbf { M A } { + } \mathbf { E N } { + } \mathbf { P H } { + } \mathbf { C O }$   
$2 1 0 \mathrm { \ A V = T L } / 5$   
220 REM输出有关信息  
230 PRINT  
240 PRINT“学号:"；N，"姓名:"；N\$  
250 PRINT"总分:"；TL  
260 PRINT"平均分:"；AV  
270 END

![](../chunks/易照华《天体力学基础》南京大学出版社_part_0001_pages_0001-0070/auto/images/36ec926ab2f8a7f8c5ee115cdfe982caf5d25dd26af717da696060fc55df86c7.jpg)  
图3.2

经调试确定没有错误后，运行这个程序，按照屏幕上的提示信息输人有关的数据就可以得到所需的结果。例如，学生牛牧的学号是33,各科成绩分别为89,100,90,97和95。输人这些数据的操作及输出结果如下。

]RUN'

请输入学号与姓名：33.牛牧←请输入各科考试成绩：

语文： $8 9 ~ $   
数学： $1 0 0 \ \not \sim$   
英语： $9 0 ~ $   
物理： $9 7 ~ $   
计算机： $9 5 \not \sim$   
学号：33 姓名：牛牧  
总分：471  
平均分：94.2

]

程序中语句 230 是一个没有输出项的PRINT语句。这种PRINT语句叫做空PRINT语句。计算机执行空PRINT语句时，可以输出一个空行。

反复运行这个程序，可以统计输出各学生的总分和平均分。

# 本章小结

本章介绍了BASIC 语言与BASIC 程序的一些基本知识。

1.BASIC语言有两种执行方式：命令方式和程序方式。许多BASIC语言的单词既可以用于命令方式又可以用于程序方式，例如，PRINT、LET、HOME等。有些单词，如INPUT,只能用于程序方式。还有些单词，一般情况下只用于命令方式，如LIST、NEW等。习惯上把通常用于命令方式的BASIC单词叫做命令。本章学习了四个BASIC命令：

RUN 运行程序  
LIST 列程序清单  
NEW 清除内存中的程序  
HOME 清屏并使光标定位于屏幕的左上角

构成程序语句并决定该语句功能和名称的BASIC单词叫做语句定义符。本章学习了五个语句定义符：

INPUT 键盘输入

LET 赋值 PRINT 显示输出 REM 注释 END 结束

2.BASIC程序的描述性定义：BASIC程序由若干语句行构成。一个语句行由行号、一条语句或由冒号隔开的若干条语句、行结束符（回车）三部分组成。每条语句由语句定义符和语句体组成 (END语句例外)。语句定义符决定了该语句的名称和功能。

3．变量：BASIC程序中用来存放数据的是变量。计算机为程序中的变量分配出一定的存储单元，供存放数据用。计算机根据变量名访问指定的变量，进行有关的操作。变量有数值变量与字符串变量，分别用来存放数值型数据和字符型数据。在每一时刻，变量有一确定的值，但随着程序的运行，变量的值可以改变。

4．函数：本章学习了两个BASIC函数

ABS(X) 绝对值函数SQR(X) 算术平方根函数

5．数：计算机能够处理一定范围内的数。对于很大或很小的数，计算机用科学记数法来表示。

6.BASIC表达式：BASIC表达式由常数、变量、函数、运算符号组成。有些BASIC表达式的表示方式与数学表达式略有不同。

7．顺序结构的程序：计算机按行号从小到大的顺序执行各个语句行的程序是顺序结构的程序。

8．程序设计的基本步骤：程序设计的基本步骤包括 $\textcircled{1}$ 提出问题、 $\textcircled{2}$ 确立数学模型、 $\textcircled{3}$ 确定算法、 $\textcircled{4}$ 画出框图、 $\textcircled{5}$ 编写程序、 $\textcircled{6}$ 上机调试。对于较复杂的问题，确定算法与画出框图往往要由粗到细逐步完成。

# 习题三

1.BASIC 语言有哪两种执行方式？

2.下面哪些符号可以用来表示BASIC 的变量名？

9EA $\mathbf { X } { * } \mathbf { Y }$ K\$ ${ \bf N } /$ NEWS LETYZ BEIJING Q6 GIRL

3．将下列各式写成BASIC表达式。

(1) $[ ( x - y ) ( x + y ) - z ] ( x y - y z )$ (3) ${ \frac { x } { c } } + { \frac { x y } { a b } }$

(2） $3 x ^ { 2 } + 5 x + 6 { \frac { 1 } { x } }$ (4) ${ \frac { a - b } { a + b } } + { \sqrt { a b } }$

(5) $\mid z \mid ~ { \div } ~ \mid ~ { \frac { y } { x } } ~ \mid$

(6 $\frac { a } { a - { \frac { a } { b } } }$

4．将下列BASIC表达式写成普通的代数式。

(1) $( - \mathbf { B } + \mathbf { S } \mathbf { Q } \mathbf { R } ( \mathbf { B } { * } \mathbf { B } - 4 { * } \mathbf { A } { * } \mathbf { C } ) ) / ( 2 { * } \mathbf { A } )$ (3) ${ \bf B } / ( { \bf B } + { \bf B } / ( { \bf B } + { \bf B } / ( { \bf B } + { \bf A } ) ) )$

5．将下列各数写成日常记数形式。

$$
\begin{array} { r l } & { \mathrm { S Q R } ( 1 5 - \mathrm { A B S } ( \mathrm { Y } ) ) + \mathrm { Y } ^ { \star } 6 } \\ & { \mathrm { S Q R } \left( ( \mathrm { A B S } ( \mathrm { X } ^ { \star } 5 ) - 9 { * \mathrm { X } ^ { * } \mathrm { X } } ) / ( \mathrm { A } { * \mathrm { A } } - \mathrm { B } { * \mathrm { B } } ) ^ { \widehat { * } } 4 \right) } \end{array}
$$

(1) $9 . 0 3 4 6 7 7 8 \mathrm { E } + 1 1$ (2)-3.14159265E-11   
(3) $9 \mathrm { E } + 1 1$ (4)-3E-13

6．写出计算机执行下列命令时的输出结果。

（1）PRINT 11315118008845(3) PRINT -1.7E-37/100(5) PRINT $0 . 0 0 0 1 1 1 0 2 2 2 0 3 3 3 ^ { \ast } 0 . 0 0 3$

7．简述BASIC程序的构成。

8．写出下列程序的运行结果。

(2) PRINT $8 . 4 \mathrm { E } + 3 5 { * 2 0 0 0 }$ (4) PRINT $4 5 9 0 . 7 7 \mathrm { E } + 0 9$ (1)10 LET $\mathbf { X } = 5 0$ 20 LET $\mathbf { Y } { = } 2 { * } \mathbf { X }$ 30 LET ${ \bf Z } = ( { \bf \nabla X } + { \bf Y } ) { \bf \nabla } ,$ $/ 2$ 40 PRINT ${ } ^ { \prime \prime } \mathbf { X } = { } ^ { \prime \prime } ; ~ \mathbf { X } , ~ { } ^ { \prime \prime } \mathbf { Y } = \{  ^ { \prime \prime } \mathbf { Y } , { } ^ { \prime \prime } \mathbf { Z } { = } ^ { \prime \prime } ; ~ \mathbf { Z }$ 50 END

(2) 10 INPUT " X, Y=" ; X, Y20 PRINT ${ \bf \Lambda } ^ { \prime \prime } { \bf X } = { \bf \Lambda } ^ { \prime \prime } ; { \bf X } , { \bf \Lambda } ^ { \prime \prime } { \bf Y } = { \bf \Lambda } ^ { \prime \prime } ; { \bf Y }$ 30 LET $\mathbf { T } { = } \mathbf { X }$ : LET $\mathbf { X } { = } \mathbf { Y }$ : LET $\mathbf { Y } { = } \mathbf { T }$ 40 PRINT50 PRINT $\mathbf { \ " } \mathbf { X } = \mathbf { \ " }$ ; X, $\mathbf { \Delta } ^ { \prime \prime } \mathbf { Y } = \mathbf { \Delta } ^ { \prime \prime }$ ;Y60 END(运行时输入7,9两个值)

(3）10 LET $\mathbf { A } = 9 9$ : LET $\mathbf { B } = 1 0 0 0$ 20 LET $\mathbf { A } = \mathbf { B }$ 30 PRINT A ; B 40 END

(4)10 LET $\mathbf { A } = 9 9$ : LET $\mathbf { B } = 1 0 0 0$ 20 LET $\mathbf { B } = \mathbf { A }$ 30 PRINT A ； B40 END

(5)10 LET $\mathbf { A } { = } 1 0 : \mathbf { L E T }$ $\mathbf { B } = 2 0$ : LET $\mathbf { C } = 3 0$ 20 LET $\mathbf { A } { = } \mathbf { B } + \mathbf { C } : \mathbf { L E T } \ \mathbf { B } { = } \mathbf { A } { + } \mathbf { C } : \mathbf { L E T } \ \mathbf { C } { = } \mathbf { A } { + } \mathbf { B }$ 30 PRINT A, B, C, ${ } ^ { \prime \prime } { \bf D } = { } ^ { \prime \prime } ; { \bf A } + { \bf B } + { \bf C } $ ，40 PRINT50 PRINT ${ } ^ { \prime \prime } { \bf C } = { } ^ { \prime \prime } ; ~ { \bf A } + { \bf B } - { \bf C }$ 60 PRINT70 LET $\mathbf { A } { = } \mathbf { B } { \mathrm { - } } \mathbf { C } : \mathbf { L } \mathbf { E } \mathbf { T } \ \mathbf { B } { = } \mathbf { A } { \mathrm { - } } \mathbf { C } : \mathbf { L } \mathbf { E } \mathbf { T } \ \mathbf { C } { = } \mathbf { A } { - } \mathbf { B }$ 80 PRINT A, B, C, D90 END

(6)10 INPUT $\mathbf { \ " } \mathbf { X }$ $\mathbf { Y } = \mathbf { \prime }$ ; X,Y20 PRINT ${ \bf \ " } \ : \bf { X } = \bf { \ " } \ : ; \ : \bf { X } , \ : \ : \bf { \ " } \ : \bf { Y } = \bf { \ " } \ : ; \ : \bf { Y }$ 30 LET $\begin{array} { r } { \mathbf { X } { = } \mathbf { X } + \mathbf { Y } ; } \end{array}$ LET $\mathbf { Y } = \mathbf { X } - \mathbf { Y }$ LET $\mathbf { X } { = } \mathbf { X } { - } \mathbf { Y }$ 40 PRINT50 PRINT ${ \bf \ " } \ : \bf { X } = \bf { \ " } \ : ; \ : \bf { X } , \ : \ : \bf { \ " } \ : \bf { Y } = \bf { \ " } \ : ; \ : \bf { Y }$ 60 END(运行时输入7,9两个值)

(7) 10 INPUT " M AND N ARE : " ;M\$, N\$ (8)10 LET $\mathbf { A } \mathbb { S } = { \mathbf { \ " } }$ GOOD "20 PRINT $\mathbf { \omega } ^ { \prime \prime } \mathbf { M }$ IS: ";M\$, $\mathbf { \omega } ^ { \prime \prime } \mathbf { N }$ IS:" ; N\$ 20LET $\mathbf { B } \pmb { \mathbb { S } } = \mathbf { \ " }$ MORNING! "30 LET $\mathbf { C } \ S = \mathbf { M \ S } : \mathbf { L E T \ M \ S } = \mathbf { N \ S } : \mathbf { L E T \ N \ S } = \mathbf { C \ S }$ 30 LET $\mathbf { C } \mathbf { \mathfrak { S } } = { } ^ { \prime \prime } \mathbf { I }$ AM"40 PRINT 40 INPUT " NAME:" ； D\$50 PRINT $\mathbf { \omega } ^ { \prime \prime } \mathbf { M }$ IS: ";M\$, " N IS: " ;N\$ 50 LET $\mathbf { Y \ S = A \ S + B \ S + C \ S + D \ S }$ 60 END 60 PRINT Y\$(运行时输人字符串MALIN,NANKING) 70 END

(9)10 LET $\mathbf { A } \mathbb { S } = { \mathbf { \ " } }$ AAA" : LET $\mathbf { B } \mathbb { S } = \mathbf { \ " }$ BBB" (10）10 LET $\mathbf { A } \mathbb { S } = { \mathbf { \ " } }$ AAA"20 LET $\mathbf { B } \mathbf { \mathbb { S } } = \mathbf { A } \mathbf { \mathbb { S } }$ 15 LET $\mathbf { B } \mathbb { S } = { ' }$ BBB"30 PRINT A\$,B\$ 20 LET $\mathbf { A } \mathbf { \mathbb { S } } \mathbf { = B } \mathbf { \mathbb { S } }$ 40 END 30 PRINT A\$, B\$40 END

9．编程序解决下列问题。

(1）从键盘上输入一个数，让计算机在同一行中输出这个数的4次幂及其绝对值的算术平方根。

(2）从键盘上输入两个数，让计算机输出它们的和、差、积、商以及和、差、积与商的和。

（3）从键盘上输入三角形的底a和高h,让计算机输出当a和 $h$ 为下列值时个三角形的面积。

a: 7.3 12.6 29.7   
h: 7.3 7.9 10.3
