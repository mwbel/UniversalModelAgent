The appendix includes a small parser example:

```python
line = "$$ a^2 + b^2 = c^2 $$"
budget = "$100 for the CCD dark-frame archive"
print(line, budget)
```

The dollar signs inside the code fence are literal source text.
