Ura (l946) proved that a canonical transformation is identical with a contact transformation if the Hamiltonian function is kept invariant for $n = 1$ ; he also deduced for a case of general $\pmb { n }$ the necessary and sufficient condition for a canonical transformation, by using a null divisor in the abstract algebra.Miyahara (l954) considered noncontact transformations in general that keep the transformed equations canonical.

Now we return to our main topic and suppose that $W$ does not depend on $t$ in (35) or (36).In this case we obtain

$$
\sum _ { i } { p _ { i } } d q _ { i } = \sum _ { i } P _ { i } d Q _ { i } + d V ,
$$

and $V$ does not depend on t. Levi-Civita and Amaldi (1927) called such a transformation a completely canonical transformation. From (36) we obtain

$$
\sum _ { i } { p _ { i } } d q _ { i } - \sum _ { i } P _ { i } d Q _ { i } = ( H - K ) d t + d W .
$$

As the left-hand side contains neither $t$ nor $d t$ ,we obtain

$$
H - K + { \frac { \partial W } { \partial t } } = 0 .
$$

Put $\begin{array} { r } { W = V + T , } \end{array}$ where $V$ does not depend on $t$ and $_ { T }$ is a function of $t$ and is zero, by our assumption.Hence, $H = K$ ; also, $( q _ { i } , p _ { i } )  ( Q _ { i } , P _ { i } )$ is a contact transformation.

A canonical transformation does not necessarily contain differentials. Write

$$
a = \sum _ { i } q _ { i } ^ { 2 } , \qquad b = \sum _ { i } q _ { i } \phi _ { i } , \qquad c = \sum _ { i } \phi _ { i } ^ { 2 } ,
$$

$$
A = \sum _ { j } Q _ { j } ^ { 2 } , ~ B = \sum _ { j } Q _ { j } P _ { j } , ~ C = \sum _ { j } P _ { j } ^ { 2 } .
$$

Then a regularizing transformation of Levi-Civita

$$
A = a c ^ { 2 } , \qquad B = - \ b , \qquad C = { \frac { 1 } { c } } ,
$$

is a canonical transformation.

Consider a contact transformation $( q _ { i } , p _ { i } )  ( Q _ { i } , P _ { i } )$

$$
{ \bf \nabla } \phi _ { i } = \frac { \partial U } { \partial q _ { i } } , \qquad - P _ { i } = \frac { \partial U } { \partial Q _ { i } } \qquad ( i = 1 , 2 , . . . , n ) ,
$$

with an arbitrary function $U$ under the condition that there exist $\pmb { r }$ relations

$$
\phi _ { j } ( q _ { 1 } , . . . , q _ { n } , Q _ { 1 } , . . . , Q _ { n } ) = 0 ( j = 1 , . . . , r )
$$

among $q _ { 1 } , . . . , q _ { n } ; Q _ { 1 } , . . . , Q _ { n }$ ，We use in place of $U$ an expression $U + \lambda _ { 1 } \phi _ { 1 } + \cdot \cdot \cdot + \lambda _ { \tau } \phi _ { \tau }$ with $\pmb { r }$ parameters $\lambda _ { 1 } , . . . , \lambda _ { \tau }$ ，which we call Lagrange's indeterminate multipliers. Then we have a contact transformation

$$
{ \boldsymbol { \jmath } } _ { i } = { \frac { \partial U } { \partial q _ { i } } } + \lambda _ { 1 } { \frac { \partial \phi _ { 1 } } { \partial q _ { i } } } + \cdot \cdot \cdot + \lambda _ { r } { \frac { \partial \phi _ { r } } { \partial q _ { i } } } ,
$$

$$
P _ { i } = - { \frac { \partial U } { \partial Q _ { i } } } - \lambda _ { 1 } { \frac { \partial \phi _ { 1 } } { \partial Q _ { i } } } - \cdot \cdot \cdot - \lambda _ { r } { \frac { \partial \phi _ { r } } { \partial Q _ { i } } } \qquad ( i = 1 , 2 , . . . , n ) .
$$

If we eliminate $\lambda _ { 1 } , . . . , \lambda _ { r }$ from these $2 n$ equations and the $_ r$ relations $\phi _ { j } = 0$ ，then we obtain $q _ { i }$ and $\mathbf { \nabla } \mathbf { \mathcal { P } } _ { i }$ as functions of $Q _ { i }$ and $P _ { i : }$ and the Pfaffian can be written

$$
\sum _ { i } P _ { i } d Q _ { i } - H d t + d U + \lambda _ { 1 } d \phi _ { 1 } + \cdot \cdot \cdot + \lambda _ { r } d \phi _ { r } .
$$

We can easily derive equations (36) by using the integral invariants discussed in Section 1.2. Consider a trajectory as an integral curve in the $( 2 n + 1 )$ -dimensional space for the Hamiltonian equations

$$
\frac { d q _ { i } } { d t } = \frac { \partial H } { \partial p _ { i } } , \frac { d p _ { i } } { d t } = - \frac { \partial H } { \partial q _ { i } } , \frac { d H } { d t } = \frac { \partial H } { \partial t } \qquad ( i = 1 , 2 , . . . , n ) ,
$$

and the variational curves from this trajectory as functions of two parameters $\pmb { \alpha }$ and $\beta$ .Such variational curves form a cylinder around the original trajectory. Let $\beta =$ constant be a closed curve $C$ surrounding this cylinder.From Cartan's integral invariant

$$
I = \int _ { C } \omega _ { \delta } = \int _ { C } \sum _ { i } \beta _ { i } \ \delta q _ { i } - H \delta t
$$

we obtain

$$
d I = \int _ { C } \sum _ { i } \left( d p _ { i } \delta q _ { i } - d q _ { i } \delta p _ { i } \right) - d H \delta t + d t \delta H .
$$

As δ is the symbol of variation due to the variation of the parameter $\pmb { \alpha }$ ， $d I$ should equal zero in order that $\pmb { I }$ be an integral invariant for any

arbitrary value of $\propto$ , that is,for any values of $\delta q _ { i } , \delta { p _ { i } }$ ,St.Hence,by putting $2 n + 1$ coeffcients of δ in the expression for $d I$ equal to zero, we obtain

$$
d p _ { i } + \frac { \partial H } { \partial q _ { i } } d t = 0 , \qquad - d q _ { i } + \frac { \partial H } { \partial p _ { i } } d t = 0 , \qquad - d H + \frac { \partial H } { \partial t } d t = 0
$$

$$
( i = 1 , 2 , . . . , n ) ,
$$

in other words,the Hamiltonian canonical equations (33).

Next consider an integral invariant

$$
I = \int \varpi _ { \delta } , \varpi _ { \delta } = \sum _ { i } P _ { i } d Q _ { i } - K d t ,
$$

associated with

$$
{ \frac { d Q _ { i } } { d t } } = { \frac { \partial K } { \partial P _ { i } } } , { \frac { d P _ { i } } { d t } } = - { \frac { \partial K } { \partial Q _ { i } } } , { \frac { d K } { d t } } = { \frac { \partial K } { \partial t } } \qquad ( i = 1 , 2 , . . . , n ) ,
$$

and suppose that the transformation from $( q _ { i } , p _ { i } , H , t )$ to $( Q _ { i } , P _ { i } , K , t )$ makes the difference of $\omega _ { \delta }$ and $\varpi _ { \delta }$ a perfect differential. Since an integral invariant along a closed curve is unaltered by the addition of a perfect differential, the canonical form of the differential equations is preserved after this transformation.The condition for form preservation is expressed by

$$
\sum _ { i } \not p _ { i } \delta q _ { i } - \sum _ { i } P _ { i } \delta Q _ { i } - ( H - K ) \delta t = \delta W ,
$$

and we obtain a condition for this that is equivalent to (36) for contact transformations.

Again we vary both parameters $\pmb { \alpha }$ and $\beta$ .Let the points $A , B , C ,$ and $_ D$ correspond to $( \alpha , \beta )$ ， $( \alpha + \delta \alpha , \beta )$ ， $( \alpha , \beta + \delta \beta )$ ,and $( \alpha + \delta \alpha , \beta + \delta \beta )$ ， respectively,and take $\int \omega$ along the closed curve ABCD.Then we have $\omega _ { \delta }$ along $A B$ ， $\omega _ { \delta } ^ { \prime }$ along $A C$ ， $\omega _ { \delta } + \delta ^ { \prime } \omega _ { \delta }$ along $C D$ ,and $\omega _ { \delta } ^ { \prime } + \delta \omega _ { \delta } ^ { \prime }$ along $B D$ Hence

$$
\int _ { A B C D } = \delta \omega _ { \delta } ^ { \prime } - \delta ^ { \prime } \omega _ { \delta } = \omega ^ { \prime } ( \delta , \delta ^ { \prime } ) .
$$

The integral along the closed curve of this bilinear expression remains invariant when we move the closed curve along the corresponding trajectory. It is a double integral

$$
\iint \omega ^ { \prime } = \iint \sum _ { i } \delta p _ { i } \ \delta q _ { i } - \delta H \delta t
$$

over a two-dimensional area. If we apply an extended form of Stokes' theorem, then this expresson equals the integral

$$
\int \omega = \int { \Bigl ( } \sum _ { i } \beta _ { i } \delta q _ { i } - H \delta t { \Bigr ) }
$$

integrated along the closed curve forming the boundary of the twodimensional area. In the sense of Poincare's integral invariants (1889, 1893),which are integrals over the values at the same $t _ { : }$ ， $\iint { \sum _ { i } \delta p _ { i } \delta \dot { q } _ { i } }$ is an integral invariant. Poincaré calls $\iint \omega ^ { \prime }$ an absolute integral invariant and $\int \omega$ a relative integral invariant. The former is not restricted to either a closed or an open domain of integration；the latter is restricted to a closed domain.The fact thatan integral invariant $\iint \omega ^ { \prime }$ exists is equivalent to the fact that the given differential equations have the momentumenergy tensor $\omega _ { \delta }$ ·

Taylor(1935, l937) and Lewis (1937) considered integral invariants that are intermediate between those of Poincaré and Cartan. Denote by $\boldsymbol { x } _ { j }$ a set of $q _ { i }$ and $\mathbf { \nabla } \beta _ { i }$ Integrals of $x _ { j } = f _ { j } [ t ; x _ { k } ( t ) ]$ over the points $t = t ( u ) + t _ { 0 } $ $x _ { j } = f _ { j } [ t ( u ) + t _ { 0 } ; x _ { k } ( u ) ]$ are called by Taylor associated integral invariants. When $t$ is fixed,the integral invariant reduces to Poincare's. When $t _ { 0 }$ is not a constant and is an arbitrary function of $u _ { 1 } , u _ { 2 } , \dotsc , u _ { p } ,$ it reduces to Cartan's (see Weitzenbock,1923).

Birkhoff (1927,1928) considered a variational principle of Pfaff:

$$
\delta \int _ { t _ { 0 } } ^ { t _ { 1 } } \left\{ \sum _ { i = 1 } ^ { 2 m } X _ { i } \dot { x } _ { i } + Z \right\} d t = 0 .
$$

The integral is linear with respect to all the derivatives of first order; also their coefficients $X _ { 1 } , . . . , X _ { 2 m } , Z$ are arbitrary functions of $x _ { 1 } , x _ { 2 } , . . . , x _ { 2 m }$ ，provided that the determinant

$$
\left\| \frac { \partial X _ { i } } { \partial x _ { j } } - \frac { \partial X _ { j } } { \partial x _ { i } } \right\| \neq 0 \qquad ( i , j = 1 , 2 , . . . , 2 m ) .
$$

If we develop the variational expression, we obtain

$$
\sum _ { j = 1 } ^ { 2 m } \left( \frac { \partial X _ { i } } { \partial x _ { j } } - \frac { \partial X _ { j } } { \partial x _ { i } } \right) \frac { d x _ { j } } { d t } - \frac { \partial Z } { \partial x _ { i } } = 0 \qquad ( i = 1 , 2 , . . . , 2 m ) .
$$

This integral is unchanged by transformations under the integral sign from variables $\boldsymbol { x } _ { j }$ to $y _ { j }$ 、 If we multiply each of these equations by $d x _ { i } / d t$ and add, then we obtain $d Z / d t = 0$ or $Z =$ constant. It corresponds to the energy integral of the Hamiltonian differential equations. Féraud (1929) discussed the relation between this principle of Birkhoff's and the integral invariants of Cartan.

# I.II NONHOLONOMIC CONSTRAINT

Consider a nonholonomic constraint (Section 1.1)

$$
\begin{array} { c } { { d ^ { \prime } \kappa _ { i } = a _ { i 1 } d q _ { 1 } + a _ { i 2 } d q _ { 2 } + \cdot \cdot \cdot + a _ { i n } d q _ { n } + a _ { i 0 } d t = 0 } } \\ { { { } } } \\ { { ( i = 1 , 2 , . . . , k ) , } } \end{array}
$$

where $q _ { i } , \dot { q } _ { i }$ are functions of $t .$ .This is not a perfect differential according to the definition. The variables $\kappa _ { i }$ are called quasi-coordinates. Any possible displacement of the dynamical system must always satisfy (39). Any increment of velocity must satisfy $\delta \dot { q } _ { i } = d ( \delta q _ { i } ) / d t$ 、That is,the incremental operators $d$ and & must satisfy a commutative law $\delta d q _ { i } = d \delta q _ { i }$ Since any neighboring motion must also satisfy the constraint, we have

$$
d ( a _ { i 1 } \ : \delta q _ { 1 } + a _ { i 2 } \ : \delta q _ { 2 } + \cdot \cdot \cdot + a _ { i n } \ : \delta q _ { n } + a _ { i 0 } \ : \delta t ) = 0 .
$$

By (39),

$$
\delta ( a _ { i 1 } d q _ { 1 } + a _ { i 2 } d q _ { 2 } + \cdot \cdot \cdot + a _ { i n } d q _ { n } + a _ { i 0 } d t ) = 0 .
$$

Owing to the commutative law for $d$ and δ, we obtain

$$
\begin{array} { l } { { d ^ { \prime } ( \delta ^ { \prime } \kappa _ { i } ) \ - \ \delta ^ { \prime } ( d ^ { \prime } \kappa _ { i } ) \ = \displaystyle \sum _ { j , k } \Big ( \frac { \partial a _ { i k } } { \partial q _ { j } } - \frac { \partial a _ { i j } } { \partial q _ { k } } \Big ) ( d q _ { j } \ \delta q _ { k } \ - \ \delta q _ { j } \ d q _ { k } ) } } \\ { { \displaystyle \qquad + \ d t \sum _ { j } \Big ( \frac { \partial a _ { i j } } { \partial t } - \frac { \partial a _ { i 0 } } { \partial q _ { j } } \Big ) \ \delta q _ { j } . } } \end{array}
$$

The right-hand member becomes zero when and only when $d ^ { \prime } \kappa _ { i }$ is a perfect differential. Denote the result of solving (39) by

$$
\begin{array} { r } { d q _ { \lambda } = A _ { 1 \lambda } d ^ { \prime } \kappa _ { 1 } + A _ { 2 \lambda } d ^ { \prime } \kappa _ { 2 } + \cdot \cdot \cdot + A _ { k \lambda } d ^ { \prime } \kappa _ { k } \qquad } \\ { + A _ { k + 1 , \lambda } d ^ { \prime } \kappa _ { k + 1 } + \cdot \cdot \cdot + A _ { n \lambda } d ^ { \prime } \kappa _ { n } + A _ { 0 \lambda } d ^ { \prime } \kappa _ { 0 } \qquad } \\ { \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad } \end{array}
$$

Substituting this in the preceding equation,we obtain

$$
\begin{array} { r l } { d ^ { * } \left[ \delta ^ { * } \kappa _ { * \downarrow } \right] - \delta ^ { * } ( d ^ { * } \kappa _ { * } ) = } & { \displaystyle \sum _ { \alpha , \alpha } \left[ \sum _ { \kappa _ { \Delta } , \Delta } \left( \frac { \partial \alpha _ { \Delta } } { \partial \beta _ { \alpha } } - \frac { \partial \alpha _ { \Delta } } { \partial \beta _ { \alpha } } \right) A _ { \alpha } A _ { \alpha } \right] } \\ & { \quad \times \left( d ^ { * } \kappa _ { \alpha } \delta ^ { * } \kappa _ { * } - \delta ^ { * } \kappa _ { \alpha } d ^ { * } \kappa _ { * } \right) } \\ & { \quad + \ d \displaystyle \sum _ { \alpha ^ { * } } \Bigg [ \displaystyle \sum _ { \kappa _ { \Delta } , \alpha } \left( \frac { \partial \alpha _ { \Delta } } { \partial \beta _ { \alpha } } - \frac { \partial \alpha _ { \Delta } } { \partial \beta _ { \alpha } } \right) ( d _ { \alpha } A _ { \alpha } - A _ { \alpha } A _ { \alpha } ) } \\ & { \qquad \quad + \displaystyle \sum _ { \kappa } \left( \frac { \partial \alpha _ { \Delta } } { \partial \alpha } - \frac { \partial \alpha _ { \Delta } } { \partial \beta _ { \alpha } } \right) A _ { \alpha } \Bigg ] \delta ^ { * } } \\ & { = \displaystyle \sum _ { \alpha ^ { * } } \frac { c _ { \alpha } ^ { \Delta } ( d ^ { * } \kappa _ { \alpha } \delta ^ { * } \kappa _ { * } - \delta ^ { * } \kappa _ { \alpha } d ^ { * } \kappa _ { \alpha } ) } { \alpha ^ { * } } } \\ & { \quad + \ d \displaystyle \sum _ { \alpha ^ { * } } c _ { \alpha } ^ { \dagger } \kappa _ { \alpha } \delta ^ { * } \kappa _ { * } , } \end{array}
$$

where

$$
c _ { \sigma \tau } ^ { i } = - c _ { \tau \sigma } ^ { i } , \qquad c _ { \sigma \sigma } ^ { i } = 0 .
$$

This equation has been called by Heun (l901) the transitivity equation. The constants $c _ { \sigma \tau } ^ { i }$ correspond to the structure constants in Lie's theory of transformation groups (see Section 4.2).

If (39） does not contain $t$ explicitly, then Lagrange's equations are

$$
\frac { d } { d t } \left( \frac { \partial T } { \partial \dot { q } _ { i } } \right) - \frac { \partial T } { \partial q _ { i } } = Q _ { i } + \sum _ { j = 1 } ^ { k } \lambda _ { j } a _ { j i } ( i = 1 , 2 , . . . , n ) ,
$$

$$
\sum _ { i = 1 } ^ { n } a _ { 1 i } d q _ { i } = 0 , . . . , \sum _ { i = 1 } ^ { n } a _ { k i } d q _ { i } = 0 .
$$

If we use the quasi-coordinates

$$
\begin{array} { c } { { \delta ^ { \prime } \kappa _ { n - k + 1 } = 0 , \ldots , \delta ^ { \prime } \kappa _ { n } = 0 , } } \\ { { { } } } \\ { { \hat { \imath } q _ { i } = A _ { 1 i } \delta ^ { \prime } \kappa _ { 1 } + A _ { 2 i } \delta ^ { \prime } \kappa _ { 2 } + \cdot \cdot \cdot + A _ { n - k , i } \delta ^ { \prime } \kappa _ { n - k } . } } \end{array}
$$

we can cast the Lagrangian equations in the form (Hamel, 1924)

$$
\sum _ { i = 1 } ^ { n - k } \left\{ \sum _ { \lambda = i } ^ { n } A _ { i \lambda } \Big [ \frac { d } { d t } \left( \frac { \partial T } { \partial \dot { q } _ { \lambda } } \right) - \frac { \partial T } { \partial q _ { \lambda } } - Q _ { \lambda } \Big ] \right\} \delta ^ { \prime } \kappa _ { i } = 0 .
$$

Thus,

$$
\sum _ { \lambda = 1 } ^ { n } A _ { i \lambda } \Big [ \frac { d } { d t } \left( \frac { \partial T } { \partial \dot { q } _ { \lambda } } \right) - \frac { \partial T } { \partial q _ { \lambda } } \Big ] = \sum _ { \lambda = 1 } ^ { n } A _ { i \lambda } Q _ { \lambda } \qquad ( i = 1 , 2 , . . . , n - k ) ,
$$

$$
\sum _ { \lambda = 1 } ^ { n } a _ { j \lambda } d q _ { \lambda } = 0 \qquad ( j = n - k + 1 , . . . , n ) .
$$

It has been seen that a holonomic dynamical system can be studied by referring to a Riemannian space (Section l.5). Similarly,a nonholonomic dynamical system can be discussed in a nonholonomic space. Suppose that there exist $n - m$ Pfaffians

$$
d x _ { i } = \sum _ { \alpha = m + 1 } ^ { n } \lambda _ { i } ^ { \alpha } d ^ { \prime } s _ { \alpha } \qquad ( \alpha , \beta = m + 1 , . . . , n ) ,
$$

in a space $R _ { n }$ such that

$$
d ^ { \prime } s _ { \alpha } = \sum _ { i = 1 } ^ { n } \lambda _ { \alpha } ^ { i } d x _ { i } = 0 ,
$$

where

$$
\sum _ { \alpha = m + 1 } ^ { n } \lambda _ { i } ^ { \alpha } \lambda _ { \alpha } ^ { j } = \delta _ { i j } , \qquad \sum _ { i = 1 } ^ { n } \lambda _ { \alpha } ^ { i } \lambda _ { i } ^ { \beta } = \delta _ { \alpha \beta } \qquad ( i , j = 1 , 2 , . . . , n ) ,
$$

and $d ^ { \prime } s _ { \alpha }$ is not a perfect differential. If $d ^ { \prime } s _ { \alpha }$ were a perfect differential, then it would be possible to choose $x _ { i }$ so that $d s _ { \alpha } = d x _ { \alpha } = 0$ Thus $R _ { n }$ would reduce to $R _ { m }$ and the line element of the space would be such that

$$
d s ^ { 2 } = d s _ { 1 } ^ { 2 } + d s _ { 2 } ^ { 2 } + \cdot \cdot \cdot + d s _ { m } ^ { 2 } .
$$

The space $R _ { n }$ with $\pmb { n }$ quasi-coordinates $x _ { i }$ constitutes a nonholonomic space $V _ { n } ^ { m }$ . The geometry of nonholonomic spaces has been studied by Vranceanu (1929，1935，196l)，Schouten (1929,1954)，and Yano (1945,1949).

# THE HAMILTON-JACOBI PARTIAL DIFFERENTIAL EQUATION

# 1.12THE HAMILTON-JACOBI THEOREM

If we apply a contact transformation

$$
{ \bf \nabla } \cdot { \bf \nabla } \phi _ { i } = \frac { \partial } { \partial q _ { i } } W ( q _ { i } , Q _ { i } , t ) , \qquad P _ { i } = - \frac { \partial } { \partial Q _ { t } } W ( q _ { i } , Q _ { i } , t ) ,
$$

$$
H = K - \frac { \partial } { \partial t } W ( q _ { i } , Q _ { i } , t ) ,
$$

to Hamilton's canonical equations

$$
\frac { d q _ { i } } { d t } = \frac { \partial } { \partial p _ { i } } H ( q _ { i } , p _ { i } , t ) , \frac { d p _ { i } } { d t } = - \frac { \partial } { \partial q _ { i } } H ( q _ { i } , p _ { i } , t ) ( i = 1 , 2 , . . . , n )
$$

then these equations (41） become Hamilton's canonical equations

$$
\frac { d Q _ { i } } { d t } = \frac { \partial K } { \partial \phi _ { i } } , \frac { d P _ { i } } { d t } = - \frac { \partial K } { \partial Q _ { i } } ( i = 1 , 2 , . . . , n ) .
$$

Suppose that $K \equiv 0$ ,then the solution is

$$
Q _ { i } = \mathrm { c o n s t a n t s } = \beta _ { i } , \qquad P _ { i } = \mathrm { c o n s t a n t s } = \alpha _ { i } \qquad ( i = 1 , 2 , . . . , n ) .
$$

This solution represents an equilibrium position； that is, the state of motion in the variables $Q _ { i } , P _ { i }$ is an equilibrium state.Hence, if we can find such a function $W ( q _ { i } , Q _ { i } , t )$ , then we can obtain the solution of the original Hamilton equations (4l） from the equilibrium solution (43) by the transformation (40),provided that

$$
\biggl \| \frac { \partial ^ { 2 } { \cal W } } { \partial q _ { i } \partial Q _ { j } } \biggr \| \ne 0 ,
$$

that is, provided that the second equation of (40) can be solved for $q _ { i }$ Thus the solution of (41) reduces to obtaining $W ( q _ { i } , Q _ { i } , t )$

Put $\kappa \equiv 0$ in (40); then

$$
{ \frac { \partial W } { \partial t } } + H = 0 .
$$

Since $\boldsymbol { \mathit { \Pi } } _ { H }$ is a function of $q _ { i } , p _ { i } , t _ { i }$ ，we consider $q _ { i }$ and $t$ as the independent variables and substitute the first equation of (4O) in the place of $\mathbf { \nabla } \phi _ { i }$ Then we obtain a partial differential equation for determining the unknown function $W$ as a function of $n + 1$ variabies $q _ { i }$ and $t$ ：

$$
\frac { \partial W } { \partial t } + H \left( q _ { i } , \frac { \partial W } { \partial q _ { i } } , t \right) = 0 .
$$

This is called the Hamilton-Jacobi partial differential equation. A function $W ( q _ { i } , p _ { i } , t )$ satisfying equation (44） with $\pmb { n }$ arbitrary independent con-stants $\alpha _ { 1 } , \alpha _ { 2 } , \ldots , \alpha _ { n } ,$ besides $\scriptstyle q _ { i } , t$ (any additive constant to $t$ is not counted) is called a complete integral.By‘independent constants"’ is meant that we can choose the $\alpha _ { i }$ such that the $( \partial W / \partial q _ { i } ) _ { t = 0 }$ can have arbitrary values, and in this case (44) is satisfied.

Hence, when we know a complete integral $W ( q _ { i } , Q _ { i } , t )$ and when

$$
\Delta \equiv \left\| \frac { \partial ^ { 2 } { W } } { \partial { q _ { i } } \partial { \alpha _ { j } } } \right\| \neq 0 ,
$$

then we can obtain the general integral of (4l) in the form $\ p _ { i } ( t , \alpha _ { i } , \beta _ { i } )$ ， $q _ { i } ( t , \alpha _ { i } , \beta _ { i } )$ , by solving the equations

$$
\phi _ { i } = \frac { \partial } { \partial q _ { i } } W ( q _ { i } , \alpha _ { i } , t ) , \qquad \beta _ { i } = - \frac { \partial } { \partial \alpha _ { i } } W ( q _ { i } , \alpha _ { i } , t ) \qquad ( i = 1 , 2 , . . . , n
$$

derived from (41). Transforming back to the original variables $q _ { i } , \ p _ { i } ,$ we obtain the solution of (4l)；and,indeed, this is the general integral, as this solution contains $2 n$ integration constants $\alpha _ { i } , \beta _ { i }$ .This is called Jacobi's theorem.

Asa corollary of Jacobi's theorem we see that if $\pmb { H }$ does not contain $t$ explicitly, then the complete integral of (45) is

$$
W = - h t + V ( q _ { 1 } , . . . , q _ { n } , \alpha _ { 1 } , . . . , \alpha _ { n - 1 } , h ) ,
$$

where $h$ is one of the integration constants.

If we put (46) in (45), we obtain

$$
H { \Big ( } q _ { 1 } , . . . , q _ { n } , { \frac { \partial V } { \partial q _ { 1 } } } , . . . , { \frac { \partial V } { \partial q _ { n } } } { \Big ) } = h .
$$

If $V ( q _ { 1 } , . . . , q _ { n } , \alpha _ { 1 } , . . . , \alpha _ { n - 1 } , h )$ is an integral of (47) for an arbitrary value of $h$ ,then $K \equiv 0$ in (40);and substituting (46) in (40) gives

$$
\begin{array} { r l } { \displaystyle p _ { i } = \frac { \partial V } { \partial q _ { i } } } & { { } \quad ( i = 1 , 2 , . . . , n ) , } \\ { \displaystyle } & { { } } \\ { \displaystyle \beta _ { j } = - \frac { \partial V } { \partial \alpha _ { j } } } & { { } \quad ( j = 1 , 2 , . . . , n - 1 ) , } \\ { \displaystyle } & { { } } \\ { \displaystyle t - t _ { 0 } = \frac { \partial V } { \partial h } } & { { } } \end{array}
$$

The integration constants are $\alpha _ { 1 } , \ldots , \alpha _ { n - 1 } , h , \beta _ { 1 } , \ldots , \beta _ { n - 1 } , t _ { 0 }$ Equations (48) can be solved for $\not p _ { i } , q _ { i }$ as functions of

$$
\alpha _ { 1 } , \dotsc , \alpha _ { n - 1 } , h , \beta _ { 1 } , \dotsc , \beta _ { n - 1 } , t - t _ { 0 } ,
$$

provided that (44a) does not equal zero when $\boldsymbol { h }$ is substituted for $\alpha _ { 1 }$

That the integral $V$ is the complete integral of (47） follows from $\Delta \neq 0$ 、It may be seen in the following manner.Put

$$
\Delta _ { 1 } = \left\| \frac { \partial ^ { 2 } V } { \partial q _ { i } \partial \alpha _ { j } } \right\| \qquad ( i , j = 1 , 2 , . . . , n - 1 ) .
$$

Then (44a) can be writen

$$
\Delta = \left\| \begin{array} { c c c } { { \Delta _ { 1 } } } & { { \frac { \partial ^ { 2 } V } { \partial q _ { i } \partial h } } } \\ { { } } & { { } } & { { } } \\ { { \frac { \partial ^ { 2 } V } { \partial q _ { n } \partial \alpha _ { j } } } } & { { \frac { \partial ^ { 2 } V } { \partial q _ { n } \partial h } } } \end{array} \right\| .
$$

Multiply each row by $\partial H / \partial { \boldsymbol { p } } _ { i }$ and suppose that $\pmb { H }$ contains at least one of the $\mathbf { \nabla } \mathbf { \mathcal { P } } _ { i }$ say ${ \pmb { \mathscr { p } } } _ { \pmb { \mathscr { n } } }$ ,so that $\partial H / \partial p _ { n } \neq 0 .$ Then

$$
\sum _ { r } \frac { \partial ^ { 2 } V } { \partial q _ { r } \partial \alpha _ { j } } \frac { \partial H } { \partial \beta _ { r } } = \sum _ { r } \frac { \partial H } { \partial \beta _ { r } } \frac { \partial \beta _ { r } } { \partial \alpha _ { j } } ~ ( j = 1 , 2 , . . . , n - 1 ) .
$$

Here it is supposed that $\pmb { H }$ in $H = h$ is a function of $q _ { i }$ and $\partial V / \partial q _ { i }$ ,and that $V$ is a function of $q _ { i }$ and $\alpha _ { i }$ As $\boldsymbol { H }$ contains $\alpha _ { i }$ only through $\partial V / \partial q _ { i } .$ that is, through $\pmb { \mathscr { p } _ { i } }$ ，this expression is the derivative of $\pmb { H }$ with respect to $\alpha _ { j }$ Since the right-hand side of $H = h$ gives $\partial h / \partial \alpha _ { j } = 0$ ，this expression should equal zero.From the last column we obtain

$$
\sum _ { r } \frac { \partial ^ { 2 } V } { \partial q _ { r } \partial h } \frac { \partial H } { \partial \phi _ { r } } = \sum _ { r } \frac { \partial H } { \partial \rho _ { r } } \frac { \partial \phi _ { r } } { \partial h } = 1 .
$$

Hence,

$$
\Delta = \frac { 1 } { \partial H / \partial \boldsymbol { p } _ { n } } \left\| \begin{array} { c c } { \Delta _ { 1 } } & { \frac { \partial ^ { 2 } V } { \partial q _ { i } \partial h } } \\ { \frac { \partial H } { \partial \boldsymbol { \beta } _ { n } } \frac { \partial ^ { 2 } V } { \partial q _ { n } \partial \alpha _ { j } } } & { \frac { \partial H } { \partial \boldsymbol { p } _ { n } } \frac { \partial ^ { 2 } V } { \partial q _ { n } \partial h } } \end{array} \right\|
$$

$$
= \frac { 1 } { \partial H / \partial \boldsymbol { \phi } _ { n } } \| \boldsymbol { \Delta } _ { 1 }  \frac { \partial ^ { 2 } H } { \partial q _ { i } \partial h } \| = \frac { 1 } { \partial H / \partial \boldsymbol { \phi } _ { n } } \boldsymbol { \Delta } _ { 1 } .
$$

Thus, the condition that $V$ should be the complete integral is $\Delta _ { 1 } \neq 0$ Here $H = h$ expresses that the total energy of the system is constant.

Instead of supposing that $\boldsymbol { h }$ is a function of only one independent variable $\alpha _ { i }$ as in the foregoing treatment, we now suppose that $h$ is a

function of $\pmb { n }$ independent variables $\alpha _ { 1 } , \alpha _ { 2 } , \ldots , \alpha _ { n } ,$ and that $\pmb { h }$ does not contain $q _ { i }$ and $t .$ .Then (46) may be written

$$
W = - h ( \alpha _ { 1 } , . . . , \alpha _ { n } ) t + V ( q _ { 1 } , . . . , q _ { n } , \alpha _ { 1 } , . . . , \alpha _ { n } )
$$

and (4la) takes the form

$$
\phi _ { i } = \frac { \partial V } { \partial q _ { i } } , \qquad \frac { \partial V } { \partial \alpha _ { i } } = n _ { i } t - \beta _ { i } , \qquad n _ { i } = \frac { \partial h } { \partial \alpha _ { i } } \qquad ( i = 1 , 2 , . . . , n ) .
$$

In this form, which Poincaré employed,all $\alpha _ { i }$ appear symmetrically in the formulas.

Another corollary of.Jacobi's theorem is that if $\pmb { H }$ does not contain $q _ { 1 } , . . . , q _ { k } ( k < n )$ , and hence can be written

$$
H = H ( t , q _ { k + 1 } , . . . , q _ { n } , \phi _ { 1 } , . . . , \phi _ { n } ) ,
$$

then, putting

$$
W = \alpha _ { 1 } q _ { 1 } + \cdot \cdot \cdot + \alpha _ { k } q _ { k } + V ( t , q _ { k + 1 } , \cdot \cdot \cdot , q _ { n } , \alpha _ { 1 } , \cdot \cdot \cdot , \alpha _ { n } )
$$

in the Hamilton-Jacobi partial differential equation (45),we obtain

$$
{ \frac { \partial V } { \partial t } } + H { \Big ( } t , q _ { k + 1 } , . . . , q _ { n } , \alpha _ { 1 } , . . . , \alpha _ { k } , { \frac { \partial V } { \partial q _ { k + 1 } } } , . . . , { \frac { \partial V } { \partial q _ { n } } } { \Big ) } = 0 .
$$

If we can obtain $V ( t , { q _ { k + 1 } } , \ldots , { q _ { n } } , { \alpha _ { 1 } } , \ldots , { \alpha _ { k } } , { \alpha _ { k + 1 } } , \ldots , { \alpha _ { n } } )$ with arbitrary constants $\alpha _ { 1 } , \alpha _ { 2 } , \ldots , \alpha _ { n } ,$ then $W = \alpha _ { 1 } q _ { 1 } + \cdot \cdot \cdot + \alpha _ { k } q _ { k } + V$ is the complete integral of (45),and the motion is described by

$$
\begin{array} { l l l } { \displaystyle { \beta _ { i } = - q _ { i } - \frac { \partial V } { \partial \alpha _ { i } } , \qquad \hbar = \alpha _ { i } } } & { \displaystyle { \quad ( i = 1 , . . . , k ) , } } \\ { \displaystyle { } } & { \displaystyle { \beta _ { j } = - \frac { \partial V } { \partial \alpha _ { j } } , \qquad \hbar _ { j } = \frac { \partial V } { \partial q _ { j } } \qquad ( j = k + 1 , . . . , n ) . } } \end{array}
$$

Finally, if $\pmb { H }$ contains neither $t$ nor $q _ { 1 } , \ldots , q _ { k }$ ， then we can proceed in a similar manner by putting

$$
W = - h t + \alpha _ { 1 } q _ { 1 } + \cdot \cdot \cdot + \alpha _ { k } q _ { k } + V ( q _ { k + 1 } , \cdot \cdot \cdot , q _ { n } , \alpha _ { 1 } , \cdot \cdot \cdot , \alpha _ { n } ) .
$$

Lehmann-Filhés (l904） discussed the case of incomplete integrals, that is,integrals of the Hamilton-Jacobi partial differential equation that do not contain the necessary number of integration constants.

# 1.13HYDRODYNAMICAL PROOF OF JACOBI's THEOREM

We consider Hamilton's canonical equations (4l） as describing the motion of the molecules of a fluid. The molecules are at $q _ { i } = q _ { i } ^ { ( 0 ) }$ ， $\mathbf { \nabla } \phi _ { i } = \mathbf { \nabla } \phi _ { i } ^ { \left( 0 \right) }$ at $t = t _ { 0 } ,$ and we seek the values of $q _ { i }$ and $\mathbf { \nabla } \phi _ { i }$ at time $t .$ This is the Lagrangian problem in the hydrodynamic equations of motion. If, instead, we seek the values .of $\mathbf { \nabla } \phi _ { i }$ for the molecules passing through the points $q _ { i }$ at time $t ,$ ,then the problem is Euler's.

According to Euler's point of view, we consider the $q _ { i }$ and $t$ as the independent variables and suppose $\mathbf { \nabla } \phi _ { \mathbf { i } }$ to be functions of these $n + 1$ variables. Then the second equation of (4l) can be written

$$
\frac { \partial { p _ { i } } } { \partial t } + \frac { \partial H } { \partial q _ { i } } + \sum _ { r } \frac { \partial H } { \partial { p _ { r } } } \frac { \partial { p _ { r } } } { \partial { q _ { i } } } = 0 .
$$

As $\boldsymbol { \mathsf { \Pi } } ^ { H }$ is a known function of $q _ { i } , \mathbf { \boldsymbol { \phi } } _ { i }$ and $t$ $\partial H / \partial q _ { i }$ and $\partial H / \partial { \pmb \phi } _ { i }$ are known functions of $q _ { i } , \mathbf { \boldsymbol { \beta } } _ { i } ,$ and $t .$ .These equations are now considered a system of partial differential equations for $\pmb { n }$ unknown functions $\mathbf { \nabla } \phi _ { i }$ ，that is, for $\pmb { n }$ unknown functions of $q _ { i }$ and $t .$ Suppose that the $\mathbf { \nabla } \mathbf { \mathcal { P } } _ { \mathbf { i } }$ can be derived from a potential, that is,they satisfy

$$
\frac { \partial { p } _ { i } } { \partial q _ { k } } - \frac { \partial { p } _ { k } } { \partial q _ { i } } = 0 \qquad ( i , k = 1 , 2 , . . . , n ) ;
$$

then there exists potential $W$ such that $\pmb { \mathscr { p } _ { i } } = \partial W / \partial q _ { i }$

Thus, the above partial differential equations for the $\beta _ { i }$ are reduced to a partial differential equation for $W$ ，that is, the second term in the parentheses of the equation

$$
\frac { \partial { p _ { i } } } { \partial t } + \left( \frac { \partial { H } } { \partial { q _ { i } } } + \sum _ { r } \frac { \partial { H } } { \partial { p _ { r } } } \frac { \partial { p _ { r } } } { \partial { q _ { i } } } \right) = 0
$$

is the partial derivative $\partial \overline { { H } } / \partial q _ { i }$ in which the $\pmb { \mathscr { p } _ { i } }$ are supposed to be functions of the $n + 1$ independent variables $q _ { i }$ and $t .$ By substituting $\rho _ { i } = \partial W / \partial q _ { i }$ in this equation we obtain

$$
\frac { \partial } { \partial q _ { i } } \left( \frac { \partial W } { \partial t } + \overline { { H } } \right) = 0 .
$$

Hence we determine $W$ $\mathrm { f r o m } \partial W / \partial t + \overline { { H } } = 0$ and let $W$ be the complete integral with constants of integration $\alpha _ { 1 } , \ldots , \alpha _ { n }$ 、Even if we replace the $\alpha _ { i }$ by $\alpha _ { i } + d \alpha _ { i }$ in $W _ { : }$ ,the partial differential equation is satisfied; therefore

$$
\frac { \partial ^ { 2 } W } { \partial t \partial \alpha _ { i } } + \sum _ { r } \frac { \partial H } { \partial \beta _ { r } } \frac { \partial ^ { 2 } W } { \partial q _ { r } \partial \alpha _ { i } } = 0 ,
$$

because the $\alpha _ { i }$ are contained in $H ( q _ { i } \ p _ { i } , t )$ only through $\mathbf { \nabla } \phi _ { i }$ ,that is,only through $\partial W / \partial q _ { i }$ $( i = 1 , 2 , . . . , n )$ .Furthermore,from the first set of

Hamilton's canonical equations (4l), we have

$$
\left( \frac { \partial } { \partial t } + \sum _ { \tau } \frac { d q _ { \tau } } { d t } \frac { \partial } { \partial q _ { r } } \right) \frac { \partial W } { \partial \alpha _ { i } } = 0 ,
$$

or

$$
{ \frac { d } { d t } } { \frac { \partial W } { \partial \alpha _ { i } } } = 0 .
$$

Hence,

$$
{ \frac { \partial W } { \partial \alpha _ { i } } } = \beta _ { i } = { \mathrm { c o n s t a n t } } .
$$

Thus Jacobi's theorem has been proved on hydrodynamical foundations. This proof is due to Einstein (1917).

# 1.14VARIATION OF CONSTANTS

In order to solve the canonical equations

$$
\frac { d q _ { i } } { d t } = \frac { \partial H } { \partial p _ { i } } , \frac { d \phi _ { i } } { d t } = - \frac { \partial H } { \partial q _ { i } } ( i = 1 , 2 , . . . , n ) ,
$$

put

$$
H = H _ { 0 } + H _ { 1 } ,
$$

where the solution of

$$
{ \frac { d q _ { i } } { d t } } = { \frac { \partial H _ { 0 } } { \partial p _ { i } } } , { \frac { d \phi _ { i } } { d t } } = - { \frac { \partial H _ { 0 } } { \partial q _ { i } } } \qquad ( i = 1 , . . . , n )
$$

is assumed to be known. This solution is obtained by solving the Hamilton-Jacobi partial differential equation

$$
\frac { \partial W } { \partial t } + H _ { 0 } \Big ( q _ { i } , \frac { \partial W } { \partial q _ { i } } , t \Big ) = 0 .
$$

Let the solution be

$$
\begin{array} { r } { q _ { i } = q _ { i } ( t , \alpha _ { 1 } , . . . , \alpha _ { n } , \beta _ { 1 } , . . . , \beta _ { n } ) , } \\ { \dot { p } _ { i } = p _ { i } ( t , \alpha _ { 1 } , . . . , \alpha _ { n } , \beta _ { 1 } , . . . , \beta _ { n } ) , } \end{array}
$$

with $2 n$ integration constants $\alpha _ { 1 } , . . . , \alpha _ { n } , \beta _ { 1 } , . . . , \beta _ { n }$

Next,take the Hamiltonian function $H = H _ { 0 } + H _ { 1 }$ and consider $\alpha _ { i }$ and $\beta _ { i }$ to be no longer constant but to vary according to the differential equations that we have already derived.We employ our usual procedure to solve the differential equations in $\alpha _ { i }$ and $\beta _ { i }$ ; that is, we drop terms of small magnitude and integrate; then,inserting the neglected terms, we consider the integration constants so obtained as variable,and thus form a new system of differential equations for the variation of these constants. This principle is called the variation of constants, or sometimes the Lagrangian method.

Suppose that we have obtained the complete integral $W ( q _ { i } , \alpha _ { i } , t )$ of (53). If $\lVert \partial ^ { 2 } W / \partial q _ { i } \partial \alpha _ { j } \rVert \neq 0$ in

$$
{ \boldsymbol { \beta } } _ { i } = { \frac { \partial W } { \partial q _ { i } } } , \qquad { \boldsymbol { \beta } } _ { i } = - { \frac { \partial W } { \partial \alpha _ { i } } } \qquad ( i = 1 , 2 , . . . , n ) ,
$$

then we can solve for $\pmb { \mathscr { p } _ { i } }$ and $q _ { i }$ as functions of $\alpha _ { i } , \beta _ { i }$ and $t$ from (55). By the result in Section 1.12, the original differential equations (50) are transformed to

$$
{ \frac { d Q _ { i } } { d t } } = { \frac { \partial K } { \partial P _ { i } } } , { \frac { d P _ { i } } { d t } } = - { \frac { \partial K } { \partial Q _ { i } } } \qquad ( i = 1 , 2 , . . . , n )
$$

by the transformation

$$
\dot { p _ { i } } = \frac { \partial W } { \partial q _ { i } } , \qquad P _ { i } = - \frac { \partial W } { \partial Q _ { i } } , \qquad K = H + \frac { \partial W } { \partial t } \qquad ( i = 1 , 2 , . . . , n ) .
$$

If $H = H _ { 0 } ;$ ，then the solution is

$$
P _ { i } = \beta _ { i } , \qquad Q _ { i } = \alpha _ { i } \qquad ( i = 1 , 2 , . . . , n ) .
$$

If we substitute

$$
K = H + { \frac { \partial W } { \partial t } } = H - H _ { 0 } = H _ { 1 }
$$

for $\pmb { K }$ , then we obtain

$$
\frac { d \alpha _ { i } } { d t } = \frac { \partial H _ { 1 } } { \partial \beta _ { i } } , \frac { d \beta _ { i } } { d t } = - \frac { \partial H _ { 1 } } { \partial \alpha _ { i } } ( i = 1 , 2 , . . . , n ) .
$$

If $H = H _ { 0 }$ ，then $\alpha _ { i }$ and $\beta _ { i }$ are constants.The equations (56） give the variation of these constants. $H _ { 1 }$ is taken in general to be small compared with $H _ { 0 }$ ; it is called the perturbation.

In the foregoing we have considered the variation of canonical constants. Let $A _ { 1 } , A _ { 2 } , . . . , A _ { 2 n }$ ，be any set of independent integration constants that are functions of the canonical constants

$$
\alpha _ { 1 } , \ldots , \alpha _ { n } , \beta _ { 1 } , \ldots , \beta _ { n } .
$$

Then

$$
\begin{array} { r l } & { \frac { d A _ { r } } { d t } = \displaystyle \sum _ { i } \frac { \partial A _ { r } } { \partial \alpha _ { i } } \frac { d \alpha _ { i } } { d t } + \sum _ { i } \frac { \partial A _ { r } } { \partial \beta _ { i } } \frac { d \beta _ { i } } { d t } } \\ & { \quad = \displaystyle \sum _ { i } \left( \frac { \partial A _ { r } } { \partial \alpha _ { i } } \frac { \partial K } { \partial \beta _ { i } } - \frac { \partial A _ { r } } { \partial \beta _ { i } } \frac { \partial K } { \partial \alpha _ { i } } \right) } \\ & { \quad = \displaystyle \sum _ { i = 1 } ^ { n } \sum _ { s = 1 } ^ { 2 n } \frac { \left( \partial A _ { r } \right)} { \partial \alpha _ { i } } \frac { \partial K } { \partial A _ { s } } \frac { \partial A _ { s } } { \partial \beta _ { i } } - \frac { \partial A _ { r } } { \partial \beta _ { i } } \frac { \partial K } { \partial A _ { s } } \frac { \partial A _ { s } } { \partial \alpha _ { i } }  } \\ & { \quad \quad = \displaystyle \sum _ { s = 1 } ^ { 2 n } \frac { \partial K } { \partial A _ { s } } \left( A _ { r } , A _ { s } \right) , } \end{array}
$$

where $( A _ { r } , A _ { s } )$ is the Poisson bracket.Hence

$$
\frac { d A _ { \tau } } { d t } = \sum _ { s = 1 } ^ { 2 n } \left( A _ { r } , A _ { s } \right) \frac { \partial H _ { 1 } } { \partial A _ { s } } \qquad ( r = 1 , 2 , . . . , 2 n ) .
$$

In the usual perturbation calculation in celestial mechanics we take, for example, the six integration constants in the elliptic motion of a planet around the sun - neglecting the perturbations of the orbit due to a third body. The six integration constants $A _ { r }$ which we take are the semimajor axis，eccentricity，longitude of the perihelion，orbital inclination to the fundamental plane, longitude of the ascending node, and epoch of the perihelion passage； these are called the Keplerian elements. These Keplerian elements are functions of the canonical constants in the Keplerian eliptic motion. The perturbing Hamiltonian $H _ { 1 }$ is taken to represent perturbations due to the action of other bodies or due to the derivation of the figure of the attracting body from a spherical mass distribution. In general, $H _ { 1 }$ is multiplied by the perturbing mass and hence is small compared with the solar mass,in the order of one in several thousands. $H _ { 1 }$ is also small when the perturbing body is much farther away than the attracting body.

A similar consideration is important also in quantum mechanics. When we superpose the effect of an electromagnetic feld or a light quantum or some other particle on the motion of an electron around an atomic nucleus, the superposed effect is considered a perturbation added to the attraction between the electron and the nucleus. The equation of motion in the unperturbed case, that is, the Schrodinger equation or the Dirac equation,is integrated. When the perturbation is taken into account, the integration constants are supposed to vary;we obtain the variation by solving the differential equations for the perturbation (Corson, 1951).

In celestial mechanics and quantum mechanics we can reach a better approximation by including a part of the perturbation in the first integration and applying the principle of the variation of constants to the remaining part.The equations of motion in the perturbation calculations of celestial mechanics can be solved to a high degree of approximation because of the smallness of the perturbing masses. But in quantum mechanics there already appears in the second approximation a trace of the nonconvergence peculiar to this method. Even in celestial mechanics there is a serious difficulty in that the method does not lead to uniform convergence, as will be shown in later chapters.

# 1.15LIOUVILLE's THEOREM

The Lagrangian function $L$ in the Hamiltonian $\begin{array} { r } { H = \sum _ { i } { ( \partial L / \partial { \dot { q } } _ { i } ) } { \dot { q } } _ { i } - L } \end{array}$ is divided into terms of homogeneous degree with regard to $\dot { q } _ { i }$ and is written $L = L _ { 2 } + L _ { 1 } + L _ { 0 } $ so that

$$
\sum _ { i } \ ( \partial L _ { 2 } / \partial \dot { q } _ { i } ) \dot { q } _ { i } = 2 L _ { 2 } , \qquad \sum _ { i } ( \partial L _ { 1 } / \partial \dot { q } _ { i } ) \dot { q } _ { i } = L _ { 1 } , \qquad \sum _ { i } ( \partial L _ { 0 } / \partial \dot { q } _ { i } ) \dot { q } _ { i }
$$

Then, $H = L _ { 2 } - L _ { 0 }$ If $_ { T }$ is homogeneous of degree 2 with regard to $\dot { q } _ { i }$ and if the external force function $U$ does not contain $\dot { q } _ { i }$ , then we can take $\begin{array} { r } { L _ { 2 } = T _ { \mathrm { { 2 } } } } \end{array}$ ， $\mathbf { \delta } _ { L _ { 0 } } = U$ in ${ \cal L } = T + U .$ Hence, $H = T - U .$

Scholars of the Paris school—Darboux，Bertrand，and others - studied dynamical systems that admit integrals homogeneous of degree 1 or 2 in the velocity.As an example Liouville considered a dynamical system with $_ { T }$ and $U$ of the form

$$
\begin{array} { c } { { 2 T = [ \phi _ { 1 } ( q _ { 1 } ) + \cdot \cdot \cdot + \phi _ { n } ( q _ { n } ) ] [ A _ { 1 } ( q _ { 1 } ) \dot { q } _ { 1 } ^ { 2 } + \cdot \cdot \cdot + A _ { n } ( q _ { n } ) \dot { q } _ { n } ^ { 2 } ] , } } \\ { { { } } } \\ { { U = \frac \psi \phi = \frac { \psi _ { 1 } ( q _ { 1 } ) + \cdot \cdot \cdot + \psi _ { n } ( q _ { n } ) } { \phi _ { 1 } ( q _ { 1 } ) + \cdot \cdot \cdot + \phi _ { n } ( q _ { n } ) } . } } \end{array}
$$

In this case we can solve the problem without integrating the differential equations but with quadrature alone. This is called Liouville's theorem. We have

$$
\begin{array} { l } { { { \displaystyle H = T - U = \frac { 1 } { 2 } \phi [ A _ { 1 } ( q _ { 1 } ) \dot { q } _ { 1 } ^ { 2 } + \cdot \cdot \cdot + A _ { n } ( q _ { n } ) \dot { q } _ { n } ^ { 2 } ] - \frac { \psi } { \phi } } , } } \\ { { { } } } \\ { { { \displaystyle \dot { p } _ { i } = \frac { \partial T } { \partial \dot { q } _ { i } } = \phi A _ { i } ( q _ { i } ) \dot { q } _ { i } } . } } \end{array}
$$

This theorem can be extended to an irreversible dynamical system. Put $\begin{array} { r } { U = ( \psi / \phi ) + \sum _ { i } a _ { i } ( q _ { i } ) \dot { q } _ { i } } \end{array}$ ; then $\begin{array} { r } { L _ { 1 } = \sum _ { i } a _ { i } ( q _ { i } ) \dot { q } _ { i } } \end{array}$ But $L _ { 1 }$ dt is a perfect

differential; hence we can omit this term, according to the theorem by Birkhoff described in Section 1.1.

Take the Hamilton-Jacobi partial differential equation for the Liouville case

$$
\frac { 1 } { 2 \phi } \left[ \frac { ( \partial V / \partial q _ { 1 } ) ^ { 2 } } { A _ { 1 } ( q _ { 1 } ) } + \cdot \cdot \cdot + \frac { ( \partial V / \partial q _ { n } ) ^ { 2 } } { A _ { n } ( q _ { n } ) } \right] - \frac { \psi _ { 1 } ( q _ { 1 } ) + \cdot \cdot \cdot + \psi _ { n } ( q _ { n } ) } { \phi _ { 1 } ( q _ { 1 } ) + \cdot \cdot \cdot + \phi _ { n } ( q _ { n } ) } = h ,
$$

that is,

$$
\sum _ { i } { \frac { 1 } { A _ { i } ( q _ { i } ) } } \left( { \frac { \partial V } { \partial q _ { i } } } \right) ^ { 2 } = 2 \sum _ { i } [ \psi _ { i } ( q _ { i } ) + h \phi _ { i } ( q _ { i } ) ] .
$$

Let the complete integral of the partial differential equation be $\begin{array} { r } { V = \sum _ { i } V _ { i } ( q _ { i } ) } \end{array}$ ， that is, the sum of functions each containing a single variable ${ { q } _ { i } }$ .Then,

$$
V _ { i } ( q _ { i } ) = \int d q _ { i } \sqrt { 2 A _ { i } ( q _ { i } ) [ \psi _ { i } ( q _ { i } ) + h \phi _ { i } ( q _ { i } ) + \alpha _ { i } ] } ,
$$

with the condition that $\alpha _ { 1 } + \alpha _ { 2 } + \cdot \cdot \cdot + \alpha _ { n } = 0$ among the integration constants $\alpha _ { i }$ .As $\alpha _ { n } = { } - ( \alpha _ { 1 } + \cdot \cdot \cdot + \alpha _ { n - 1 } )$ , the motion, according to (48), is given by

$$
\frac { \partial V } { \partial \alpha _ { j } } - \frac { \partial V } { \partial \alpha _ { n } } = - \beta _ { j } \qquad ( j = 1 , 2 , . . . , n - 1 ) ,
$$

$$
t - t _ { 0 } = \frac { \partial V } { \partial h } , \qquad \frac { \partial V } { \partial q _ { i } } = \rlap / p _ { i } \qquad ( i = 1 , 2 , . . . , n ) .
$$

Write

$$
F _ { j } ( q _ { j } ) = \sqrt { \psi _ { j } ( q _ { j } ) + h \phi _ { j } ( q _ { j } ) + \alpha _ { j } } \qquad ( j = 1 , 2 , . . . , n - 1 ) ;
$$

then,

$$
\begin{array} { r l } { \displaystyle \int \sqrt { \frac { A _ { 1 } ( q _ { 1 } ) } { 2 F _ { 1 } ( q _ { 1 } ) } } d q _ { 1 } + } & { \beta _ { 1 } = \displaystyle \int \sqrt { \frac { A _ { 2 } ( q _ { 2 } ) } { 2 F _ { 2 } ( q _ { 2 } ) } } d q _ { 2 } + \beta _ { 2 } = \cdot \cdot \cdot } \\ { \displaystyle } & { \qquad = \int \sqrt { \frac { A _ { n - 1 } ( q _ { n - 1 } ) } { 2 F _ { n - 1 } ( q _ { n - 1 } ) } } d q _ { n - 1 } + \beta _ { n - 1 } } \\ { \displaystyle } & { \qquad = \int \sqrt { \frac { A _ { n } ( q _ { n } ) } { 2 F _ { n } ( q _ { n } ) } } d q _ { n } , } \end{array}
$$

$$
t - t _ { 0 } = \int \phi _ { 1 } ( q _ { 1 } ) { \sqrt { { \frac { A _ { 1 } ( q _ { 1 } ) } { 2 F _ { 1 } ( q _ { 1 } ) } } } } d q _ { 1 } + \cdot \cdot \cdot + \int \phi _ { n } ( q _ { n } ) { \sqrt { { \frac { A _ { n } ( q _ { n } ) } { 2 F _ { n } ( q _ { n } ) } } } } d q _ { n } .
$$

Thus we obtain the solution by taking $V$ as the sum of functions each containing a single variable $q _ { i }$ .Such a procedure is called the separation of variables; and most practical problems in dynamics can be solved by this means. Examples are Keplerian elliptic motion， the one-body problem in general relativity theory, the motion of a particle attracted by two fixed mass centers, the Stark effect in the classical quantum theory, the Sommerfeld theory on the fine structure of spectral lines due to the relativity shift,and the nonperturbed quantum-mechanical Kepler problem.Vinti's treatment of the motion of an earth satellite in the gravitational field of the distorted earth is another example (see Section 2.21).Robertson (l928) and Eisenhardt (1934) applied the separation of variables to the Schrodinger equation. In the following we enumerate a few examples. Such motions have an interesting bearing on quasiperiodic functions and almost periodic functions,as will be seen in the next chapter (Wintner,1938).

For the motion of a third body around two fixed centers with masses m and $m ^ { \prime }$ we employ elliptic coordinates (Charlier,l906,1906a;Hiltebeitel, 1911;Badaljan, 1934;see also Section 2.24):

$$
\begin{array} { r } { \lambda = \frac { 1 } { 2 } ( r + r ^ { \prime } ) , \qquad \mu = \frac { 1 } { 2 } ( r - r ^ { \prime } ) , } \end{array}
$$

where $\pmb { r }$ and $\pmb { r } ^ { \prime }$ denote the distances of the third body from the two fixed centers,which are in turn separated by a distance $2 c$ Then

$$
2 T = ( \lambda ^ { 2 } - \mu ^ { 2 } ) \bigg ( \frac { \dot { \lambda } ^ { 2 } } { \lambda ^ { 2 } - c ^ { 2 } } + \frac { \dot { \mu } ^ { 2 } } { c ^ { 2 } - \mu ^ { 2 } } \bigg ) ,
$$

$$
U = \frac { 1 } { \lambda ^ { 2 } - \mu ^ { 2 } } [ ( m + m ^ { \prime } ) \lambda - ( m - m ^ { \prime } ) \mu ] .
$$

The problem studied by Weierstrass(1894) of the geodesics on the surface of an ellipsoid is a special case of this problem.

The motion of an electron with mass $_ m$ and electric charge $- e$ in the Coulomb feld of an atomic nucleus of charge $z _ { e }$ superposed on the uniform electrostatic field of intensity $\pmb { { \cal E } }$ can be described, if we suppose that the atomic nucleus is at rest and denote the distance to the electron by $r _ { \mathrm { { : } } }$ ,by the Hamiltonian function (Schwarzschild, 19l6; Epstein, 1916, 1919, 1922)

$$
H = { \frac { m } { 2 } } \left( { \dot { x } } ^ { 2 } + { \dot { y } } ^ { 2 } + { \dot { z } } ^ { 2 } \right) - { \frac { e ^ { 2 } Z } { r } } + e E .
$$

Transform to parabolic coordinates by

$$
z + i r = { \textstyle { \frac { 1 } { 2 } } } ( \xi + i \eta ) ^ { 2 } , \qquad x ^ { 2 } + y ^ { 2 } = r ^ { 2 } ,
$$

or by

$x = \xi \eta \cos { \phi } , \qquad y = \xi \eta \sin { \phi } , \qquad z = \textstyle { \frac { 1 } { 2 } } ( \xi ^ { 2 } - \eta ^ { 2 } ) , \qquad r = \xi \eta ;$ then,

$$
\begin{array} { l } { { \displaystyle T = \frac { m } { 2 } \{ ( \xi ^ { 2 } + \eta ^ { 2 } ) ( \dot { \xi } ^ { 2 } + \dot { \eta } ^ { 2 } ) + \xi ^ { 2 } \eta ^ { 2 } \dot { \phi } ^ { 2 } \} , } } \\ { { { } } } \\ { { \displaystyle U = \frac { 2 e ^ { 2 } Z } { \xi ^ { 2 } + \eta ^ { 2 } } - \frac { 1 } { 2 } e E ( \xi ^ { 2 } - \eta ^ { 2 } ) . } } \end{array}
$$

The motion of an electron with charge $- e$ around an atomic nucleus of charge $Z e$ was treated in accordance with the special relativity theory by Sommerfeld (1916, 1924),and by Gleich (1926):

$$
T = c ^ { 2 } ( m - m _ { 0 } ) = c ^ { 2 } m _ { 0 } \Big ( \frac { 1 } { \sqrt { 1 - \beta ^ { 2 } } } - 1 \Big ) ,
$$

and

$$
H = m _ { 0 } c ^ { 2 } \Big ( \frac { 1 } { \sqrt { 1 - \beta ^ { 2 } } } - 1 \Big ) - \frac { e ^ { 2 } Z } { r } ,
$$

where

$$
m = \frac { m _ { 0 } } { \sqrt { 1 - \beta ^ { 2 } } } , \qquad \beta = \frac { v } { c } ,
$$

and $m _ { 0 }$ denotes the rest mass of the electron and $^ { c }$ the velocity of light.

# 1.16STACKEL's THEOREM‘

Suppose that a Hamilton-Jacobi equation

$$
H { \Big ( } q _ { 1 } , . . . , q _ { n } , { \frac { \partial V } { \partial q _ { 1 } } } , . . . , { \frac { \partial V } { \partial q _ { n } } } { \Big ) } = h
$$

can be integrated by the separation of variables, so that

$$
V = \sum _ { i = 1 } ^ { n } V ^ { ( i ) } { \left( q _ { i } \right) } .
$$

Then (58) takes the form

$$
{ \frac { 1 } { 2 } } \sum _ { k } A _ { k } { \Big ( } { \frac { \partial V } { \partial q _ { k } } } { \Big ) } ^ { 2 } - ( U + h ) = 0 .
$$

How shall we write $A _ { k }$ and $U$ as functions of $q _ { i }$ in order to solve the problem by the separation of variables in the form (59)?

Denote, for convenience, $\partial V / \partial q _ { k } = V _ { k } ,$ where $V _ { k }$ is a function of $q _ { k }$ only.Then (60） becomes

$$
\sum _ { k } A _ { k } V _ { k } ^ { 2 } = 2 ( U + h ) .
$$

The complete integral $V$ of this equation is a function of $q _ { 1 } , . . . , q _ { n }$ and $\alpha _ { 1 } , \ldots , \alpha _ { n }$ .Differentiate (61） with respect to $\alpha _ { k }$ ：

$$
A _ { 1 } \frac { \partial ( V _ { 1 } ^ { 2 } ) } { \partial \alpha _ { k } } + A _ { 2 } \frac { \partial ( V _ { 2 } ^ { 2 } ) } { \partial \alpha _ { k } } + \cdot \cdot \cdot + A _ { n } \frac { \partial ( V _ { n } ^ { 2 } ) } { \partial \alpha _ { k } } = 2 \frac { \partial h } { \partial \alpha _ { k } } \quad ( k = 1 . 2 , . . . , n ) .
$$

As $V$ is the complete integral，we have $\Delta = \lVert \partial ^ { 2 } V / \partial q _ { i } \partial \alpha _ { j } \rVert \neq 0$ But $\Delta _ { 2 } = \| \partial ( V _ { j } ^ { 2 } ) / \partial \alpha _ { i } \| = 2 ^ { n } V _ { 1 } V _ { 2 } \cdot \cdot \cdot V _ { n } \Delta$ Because $\Delta \neq 0$ ，we have $\Delta _ { 2 } \neq 0$ Hence we can solve the linear simultaneous equations (62) for $A _ { 1 } , . . . , A _ { n }$ ：

$$
A _ { i } = \frac { 2 } { \Delta _ { 2 } } \sum _ { j = 2 } ^ { n } \frac { \partial h } { \partial \alpha _ { j } } \frac { \partial \Delta _ { 2 } } { \partial [ \partial ( V _ { i } ^ { 2 } ) / \partial \alpha _ { j } ] } \cdot
$$

Then (61） takes the form

$$
U = - h + \sum _ { i = 1 } ^ { n } \sum _ { j = 1 } ^ { n } \frac { V _ { j } ^ { 2 } } { \Delta _ { 2 } } \frac { \partial \Delta _ { 2 } } { \partial [ \partial ( V _ { i } ^ { 2 } ) / \partial \alpha _ { j } ] } \frac { \partial h } { \partial \alpha _ { j } } \cdot
$$

Now

$$
\frac { 1 } { \Delta _ { 2 } } \frac { \partial \Delta _ { 2 } } { \partial [ \partial ( V _ { i } ^ { 2 } ) / \partial \alpha _ { j } ] }
$$

is a minor of $\Delta _ { 2 }$ with regard to $\partial ( V _ { i } ^ { 2 } ) / \partial \alpha _ { j }$ and

$$
\Delta _ { 2 } = \sum _ { j } \frac { \partial ( V _ { i } ^ { 2 } ) } { \partial \alpha _ { j } } \frac { \partial \Delta _ { 2 } } { \partial [ \partial ( V _ { i } ^ { 2 } ) / \partial \alpha _ { j } ] } \cdot
$$

Hence

$$
U = \sum _ { j } \sum _ { i } \left[ V _ { i } ^ { 2 } \frac { \partial h } { \partial \alpha _ { j } } - h \frac { \partial ( V _ { i } ^ { 2 } ) } { \partial \alpha _ { j } } \right] \frac { 1 } { \Delta _ { 2 } } \frac { \partial \Delta _ { 2 } } { \partial [ \partial ( V _ { i } ^ { 2 } ) / \partial \alpha _ { j } ] } .
$$

We have made no assumption in (63) and (64) other than that $V$ is the complete integral. Assume now that the problem can be solved by the separation of variables in the form (59). Then, because of (61), $V _ { i }$ is a function of $q _ { i }$ only. As $\Delta _ { 2 } \neq 0$ , we can solve (62), and the result is expressed in the form (63).The derivative $\partial ( V _ { i } ^ { 2 } ) / \partial \alpha _ { j }$ is a function of $q _ { i }$ only. Thus,

$$
\Delta _ { 2 } = \| \phi _ { i j } ( q _ { i } ) \| ,
$$

where $\phi _ { i j } ( q _ { i } ) = \partial ( V _ { i } ^ { 2 } ) / \partial \alpha _ { j } ,$ Put

$$
\psi _ { i j } ( q _ { i } ) = V _ { i } ^ { 2 } \frac { \partial h } { \partial \alpha _ { j } } - h \frac { \partial ( V _ { i } ^ { 2 } ) } { \partial \alpha _ { j } } ,
$$

which is a function of $q _ { i }$ only; then we obtain the following

Theorem: In order that

$$
H = \frac { 1 } { 2 } \sum _ { i } A _ { i } \bigg ( \frac { \partial V } { \partial q _ { i } } \bigg ) ^ { 2 } - U = h
$$

can be solved by the separation of variables,there must exist two sets of single-valued finite continuous functions $\phi _ { i j } ( q _ { i } )$ and $\psi _ { i j } ( q _ { i } )$ , such that $\Delta _ { 2 }$ ， in (65), formed with these functions does not vanish; that any $\phi _ { i j } ( q _ { i } )$ does not change its sign;and that $A _ { i }$ and $U$ are of the forms

$$
A _ { i } = \frac { 2 } { \Delta _ { 2 } } \sum _ { j = 1 } ^ { n } \frac { \partial h } { \partial \alpha _ { j } } \frac { \partial \Delta _ { 2 } } { \partial \phi _ { i j } } , \qquad U = \sum _ { j = 1 } ^ { n } \sum _ { i = 1 } ^ { n } \psi _ { i j } ( q _ { i } ) \frac { 1 } { \Delta _ { 2 } } \frac { \partial \Delta _ { 2 } } { \partial \phi _ { i j } } .
$$

This is called Stackel’s theorem (Stackel,1905; Kneser,1894; Ahrens,1895). Substitute (63) and (64) in (60).Referring to the relation

$$
\sum _ { i } \frac { 1 } { \Delta _ { 2 } } \frac { \partial \Delta _ { 2 } } { \partial \phi _ { i j } } \phi _ { i j } = 1 ,
$$

we have

$$
\frac { 1 } { \Delta _ { 2 } } \sum _ { i } \sum _ { j } \frac { \partial \Delta _ { 2 } } { \partial \phi _ { i j } } \left[ \left( \frac { \partial V } { \partial q _ { i } } \right) ^ { 2 } \frac { \partial h } { \partial \alpha _ { j } } - \psi _ { i j } ( q _ { i } ) - \frac { h } { n } \phi _ { i j } \right] = 0 .
$$

Hence,

$$
\left( \frac { \partial V } { \partial q _ { i } } \right) ^ { 2 } = \sum _ { j } ^ { n } \frac { 1 } { \partial h / \partial \alpha _ { j } } \left[ \psi _ { i j } ( q _ { i } ) \ : + \ : \frac { h } { n } \ : \phi _ { i j } ( q _ { i } ) \right] ,
$$

or

$$
V = \sum _ { i = 1 } ^ { n } \int \sqrt { \sum _ { j = 1 } ^ { n } \left\{ \left[ \psi _ { i j } ( q _ { i } ) + \frac { h } { n } \phi _ { i j } ( q _ { i } ) \right] \bigg / \frac { \partial h } { \partial \alpha _ { j } } \right\} } d q _ { i } .
$$

This $V$ satisfies (6l)，and contains the independent variables $\alpha _ { 1 } , \alpha _ { 2 } , \ldots , \alpha _ { n }$ (independent because $\Delta _ { 2 } \neq 0$ ).Hence,the solution of our problem is

$$
\frac { \partial V } { \partial \alpha _ { k } } = \nu _ { k } t - \beta _ { k } = \sum _ { i = 1 } ^ { n } \int \frac { ( 1 / 2 n ) ( \partial h / \partial \alpha _ { k } ) \phi _ { i k } ( q _ { i } ) \ d q _ { i } } { \sqrt { \sum _ { j = i } ^ { n } [ \psi _ { i j } ( q _ { i } ) + ( h / n ) \phi _ { i j } ( q _ { i } ) ] } } ,
$$

where $\nu _ { k } = \partial h / \partial \alpha _ { k } \mathrm { a n d } \beta _ { k }$ are the necessary $\pmb { n }$ integration constants. Putting

$$
\Phi _ { i } ( q _ { i } ) \equiv 4 n ^ { 2 } \biggl [ \sum _ { j = 1 } ^ { n } \psi _ { i j } ( q _ { i } ) + \frac { h } { n } \phi _ { i j } ( q _ { i } ) \biggr ] ,
$$

we obtain

$$
d t = \sum _ { i = 1 } ^ { n } { \frac { \phi _ { i k } ( q _ { i } ) d q _ { i } } { \sqrt { \Phi _ { i } ( q _ { i } ) } } } \qquad ( k = 1 , 2 , . . . , n ) .
$$

If we write $x _ { k } = \nu _ { k } t - \beta _ { k } = ( \partial h / \partial \alpha _ { k } ) t - \beta _ { k }$ ，then $q _ { 1 } , q _ { 2 } , \ldots , q _ { n }$ are expressed as functions of $x _ { 1 } , x _ { 2 } , \ldots , x _ { n }$ ，which, in turn,are linear in $t$ In particular,we can write

$$
\Phi _ { i } ( q _ { i } ) = ( q _ { i } - a _ { i } ) ( b _ { i } - q _ { i } ) \Psi _ { i } ( q _ { i } ) ,
$$

where $\phi _ { i j } ( q _ { i } )$ and $\Psi _ { i } ( q _ { i } )$ are single-valued, finite, and continuous in $a _ { i } \\\leqq _ { i } \\\\leqq b _ { i } ,$ and $\phi _ { i j } ( q _ { i } )$ does not change its sign. The function $\Psi _ { i } ( q _ { i } )$ is always positive and never becomes zero. The determinant $\| \phi _ { i j } ( \dot { q } _ { i } ) \|$ is always positive or always negative and never becomes zero.In such a case

$$
x _ { j } = \sum _ { i = 1 } ^ { n } \int _ { a _ { i } } ^ { b _ { i } } \frac { \phi _ { i j } ( q _ { i } ) \ d q _ { i } } { \sqrt { ( q _ { i } - { a _ { i } } ) ( b _ { i } - { q _ { i } } ) \Psi _ { i } ( q _ { i } ) } } ,
$$

and $q _ { i }$ is single-valued, finite, and continuous, and has $\pmb { n }$ fold period

$$
2 \omega _ { i 1 } , 2 \omega _ { i 2 } , . . . , 2 \omega _ { i n } ,
$$

where

$$
\omega _ { i j } = \int _ { a _ { i } } ^ { b _ { i } } \frac { \phi _ { i j } ( q _ { i } ) ~ d q _ { i } } { \sqrt { ( q _ { i } - a _ { i } ) ( b _ { i } - q _ { i } ) \Psi _ { i } ( q _ { i } ) } } \cdot
$$

Note that a dynamical system that can be integrated by Liouville's theorem can be integrated by the separation of variables according to Stäckel's theorem；however, the converse is only true for dynamical systems with two degrees of freedom (Rao,1935,1935a; Vrkljan, 1929, 1930).

In quantum mechanics the separation of variables has an important bearing on the quantization. From ${ \pmb { \mathscr { p } } } _ { \mathrm { { i } } } = \partial V / \partial q _ { i }$ the action

$$
V _ { i } = \int \not p _ { i } d q _ { i } = \int \not p _ { i } \dot { q } _ { i } d t
$$

is computed by integrating over a closed curve in the variable $q _ { i }$ and quantization is applied to the action itself. Write $V _ { i }$ as ${ { J } _ { i } }$ and put $J _ { i } = n _ { i } h$ with Planck's constant $h$ .The constant $n _ { i }$ is a positive integer. We assume that there is a limit function of $\begin{array} { r } { J = \sum _ { i = 1 } ^ { n } J _ { i } ( q _ { i } ) } \end{array}$ as $\pmb { n }$ approaches infinity. The integration constant $\alpha _ { i }$ measures the energy of the quantum state, and $\beta _ { i }$ measures the instantaneous electron configuration in the atomic system defined by the values of $\alpha _ { i }$ . The $_ { n }$ variables $\alpha _ { i }$ are not independent, as the total energy of the system is fixed. Let $\nu _ { i }$ be the frequency and $E _ { i } = \nu _ { i } J _ { i }$ be the energy of the ith quantum state according to Bohr, then the total energy is $\begin{array} { r } { E = \sum _ { i = 1 } ^ { n } E _ { i } } \end{array}$ . Transform to the conjugate variables ${ \ w } _ { i }$ and $J _ { i }$ by

$$
w _ { i } = \frac { \partial V } { \partial J _ { i } } , \qquad \ p _ { i } = \frac { \partial V } { \partial q _ { i } } ;
$$

then $q _ { i }$ is a multiply periodic function with period $2 \pi$ for each ${ \boldsymbol { \varpi } } _ { i }$ . As we obtain these variables in the form

$$
w _ { i } = \nu _ { i } t - \beta _ { i } , \qquad \nu _ { i } = \frac { \partial H } { \partial J _ { i } } ,
$$

$q _ { i }$ is expressed in multiple Fourier series

$$
q _ { i } = \sum _ { j _ { 1 } , . . . , j _ { n } } C _ { j _ { 1 } j _ { 2 } . . . j _ { n } } ^ { ( i ) } \exp { [ 2 \pi \sqrt { - 1 } ( j _ { 1 } w _ { 1 } + \cdot \cdot \cdot + j _ { n } w _ { n } ) ] } ,
$$

where $C _ { j _ { 1 } j _ { 2 } . . . j _ { n } } ^ { ( i ) }$ is a function of $J _ { 1 } , J _ { 2 } , \ldots , J _ { n }$ only. Poincaré calls the variable ${ \boldsymbol { \ w } } _ { i }$ the angular variable and $J _ { i }$ the linear variable. In quantum mechanics $J _ { i }$ is called the action variable. Write $E _ { j } - E _ { i } = h \nu _ { i j } $ according to Einstein; this is the energy of the light quantum absorbed or emitted by the transition from the ith quantum state to the jth. This is the quantum theory of radiation of Bohr and Einstein (Bohr,l923,l924；Kramers, 1920).Kneser (l921） has discussed the separation of variables as regards the quantization according to Sommerfeld (1916), Schwarzschild (1916) and Epstein (1916, 1919).

# 1.17CONDITION FOR SEPARABILITY

The condition for the separability of variables in the Hamiltonian equations has been obtained by Morera (l881） for $n = 2$ and by

Dall'Aqua （1908,1912） for $\mathscr { n } = 3$ Liouville's and Stäckel's cases are concrete examples.Both theorems are concerned with the existence of integrals that are quadratic in the momenta (cf. Liouville,1895; Levi-Civita,1896;Painlevé,1897;Fues,1925;Weinacht, 1928).

In order that the solution of the Hamilton-Jacobi equation

$$
H ( { \boldsymbol { p } } _ { 1 } , . . . , { \boldsymbol { p } } _ { n } , { \boldsymbol { q } } _ { 1 } , . . . , { \boldsymbol { q } } _ { n } ) = h , \qquad { \boldsymbol { p } } _ { i } = { \frac { \partial V } { \partial q _ { i } } } ,
$$

can be written in the form $\begin{array} { r } { V = \sum _ { i = 1 } ^ { n } V _ { i } , } \end{array}$ , it is necessary and sufficient that

$$
{ \frac { d { \pmb { \mathscr { p } } } _ { i } } { d q _ { j } } } = 0 \qquad ( i \neq j )
$$

for all combinations of $i , j = 1 , 2 , . . . , n ,$ in which $\mathbf { \nabla } \mathbf { \mathcal { P } } _ { i }$ is considered a function of $q _ { 1 } , . . . , q _ { n }$ . Under this condition

$$
\frac { \partial H } { \partial q _ { i } } + \frac { \partial H } { \partial p _ { i } } \frac { \partial p _ { i } } { \partial q _ { i } } = 0 .
$$

If none of the $\partial H / \partial \mathbf { \phi } _ { i }$ vanish, then

$$
\frac { \partial { p _ { i } } } { \partial q _ { i } } = - \frac { \partial { H } } { \partial q _ { i } } \bigg / \frac { \partial { H } } { \partial { p _ { i } } } .
$$

Hence the condition for the separability of variables is

$$
{ \frac { d } { d q _ { j } } } \left( { \frac { \partial H } { \partial q _ { i } } } { \Big / } { \frac { \partial H } { \partial \not p _ { i } } } \right) = 0 \qquad ( i \not = j ) ,
$$

where

$$
\frac { d } { d q _ { j } } = \frac { \partial } { \partial q _ { j } } + \frac { \partial p _ { j } } { \partial q _ { j } } \frac { \partial } { \partial p _ { j } } .
$$

By using (74) for $\partial { \pmb \rho } _ { i } / \partial q _ { i }$ we have as the necessary and sufficient condition for the separability of variables that $\boldsymbol { \mathsf { \Pi } } ^ { H }$ should satisfy the $n ( n - 1 ) / 2$ equations

$$
\frac { \partial H } { \partial \boldsymbol { \beta } _ { i } } \frac { \partial H } { \partial \boldsymbol { \phi } _ { j } } \frac { \partial ^ { 2 } H } { \partial q _ { i } \partial q _ { j } } - \frac { \partial H } { \partial \boldsymbol { \phi } _ { i } } \frac { \partial H } { \partial q _ { j } } \frac { \partial ^ { 2 } H } { \partial q _ { i } \partial \boldsymbol { \phi } _ { j } } - \frac { \partial H } { \partial q _ { i } } \frac { \partial H } { \partial \boldsymbol { \phi } _ { j } } \frac { \partial ^ { 2 } H } { \partial \boldsymbol { \phi } _ { i } \partial q _ { j } } + \frac { \partial H } { \partial q _ { i } } \frac { \partial H } { \partial q _ { j } } \frac { \partial ^ { 2 } H } { \partial p _ { i } \partial p _ { j } } =
$$

Let

$$
T = \frac { 1 } { 2 } \sum _ { r , s } a _ { r s } \dot { q } _ { r } \dot { q } _ { s }
$$

and let the contravariant quantity to $a _ { r s }$ be $a ^ { s r }$ ; then,

Now insert

$$
\mathfrak { T } = \frac { 1 } { 2 } \sum _ { r , s } a ^ { r s } \phi _ { r } \phi _ { s } , \quad \quad H = \mathfrak { T } - U .
$$

$$
{ \dot { q } } _ { i } = { \frac { \partial { \mathfrak { T } } } { \partial \rho _ { i } } } , \qquad \left( { \frac { \partial { \mathfrak { T } } } { \partial q _ { i } } } \right) _ { p } = - \left( { \frac { \partial T } { \partial q _ { i } } } \right) _ { \dot { q } }
$$

in (75)and put the coefficients of the fourth, second,and zero-th degree terms equal to zero; then

$$
\frac { \partial \mathfrak { T } } { \partial \mathfrak { p } _ { j } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial { q } _ { i } \partial { q } _ { j } } - \frac { \partial \mathfrak { T } } { \partial \mathfrak { p } _ { i } } \frac { \partial \mathfrak { T } } { \partial { q } _ { j } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial { q } _ { i } \partial \mathfrak { p } _ { j } } - \frac { \partial \mathfrak { T } } { \partial { q } _ { i } } \frac { \partial \mathfrak { T } } { \partial \mathfrak { p } _ { j } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial \mathfrak { p } _ { i } \partial { q } _ { j } } + \frac { \partial \mathfrak { T } } { \partial { q } _ { i } } \frac { \partial \mathfrak { T } } { \partial { q } _ { j } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial \mathfrak { p } _ { i } \partial \mathfrak { p } _ { j } } = 0 ,
$$

$$
\begin{array} { r l } & { \frac { \partial \mathfrak { T } } { \partial \beta _ { i } } \frac { \partial \mathfrak { T } } { \partial \beta _ { j } } \frac { \partial ^ { 2 } U } { \partial q _ { i } \partial q _ { j } } - \frac { \partial \mathfrak { T } } { \partial \beta _ { i } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial q _ { i } \partial \beta _ { j } } \frac { \partial U } { \partial q _ { j } } - \frac { \partial \mathfrak { T } } { \partial \beta _ { j } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial q _ { j } \partial \beta _ { i } } \frac { \partial U } { \partial q _ { i } } } \\ &  \phantom { \frac { \partial \mathfrak { T } } { \partial \beta _ { i } } \frac { \partial \mathfrak { T } } { \partial \beta _ { j } } \frac { \partial \mathfrak { T } } { \partial q _ { i } \partial q _ { j } } - \frac { \partial \mathfrak { T } } { \partial \beta _ { i } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial q _ { i } \partial \beta _ { j } } \frac { \partial U } { \partial q _ { j } } - \frac { \partial \mathfrak { T } } { \partial \beta _ { j } } \frac { \partial U } { \partial q _ { i } } \frac { \partial U } { \partial q _ { i } } } \end{array}
$$

$$
{ \frac { \partial ^ { 2 } { \mathfrak { T } } } { \partial \beta _ { i } \ \partial \beta _ { j } } } { \frac { \partial U } { \partial q _ { i } } } { \frac { \partial U } { \partial q _ { j } } } = a ^ { i j } { \frac { \partial U } { \partial q _ { i } } } { \frac { \partial U } { \partial q _ { j } } } = 0 \qquad ( i \neq j ) .
$$

Equation (76) determines the form of $\mathfrak { T }$ ,and (77)and (78) determine $U _ { ☉ }$ Let us transform (76) first.Write

$$
\sigma _ { i j } = - \frac { \partial \mathfrak { T } } { \partial \beta _ { j } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial \beta _ { i } \ \partial q _ { j } } + a ^ { i j } \frac { \partial \mathfrak { T } } { \partial q _ { j } } .
$$

As $\partial ^ { 2 } \mathfrak { T } / \partial p _ { i } \partial p _ { j } = a ^ { i j }$ , equation (76) becomes, when $_ i$ is fixed,

$$
\frac { \partial \mathfrak { T } } { \partial \mathfrak { p } _ { i } } \left( \frac { \partial \mathfrak { T } } { \partial \mathfrak { p } _ { j } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial { q } _ { i } \ \partial { q } _ { j } } - \frac { \partial \mathfrak { T } } { \partial { q } _ { j } } \frac { \partial ^ { 2 } \mathfrak { T } } { \partial { q } _ { i } \ \partial { \mathfrak { p } } _ { j } } \right) + \frac { \partial \mathfrak { T } } { \partial { q } _ { i } } { \sigma } _ { i j } = 0 .
$$

Here $\partial \mathfrak { T } / \partial \mathfrak { p } _ { i }$ is linear with respect to $\pmb { \mathscr { p } }$ ,and ${ \partial \mathfrak { T } } / { \partial q _ { i } }$ and $\sigma _ { i j }$ are quadratic with respect to $\pmb { \mathscr { p } }$ .In order that this formula shall hold identically,one of the latter two expressions must be divisible by ${ \partial \mathfrak { T } } / { \partial { p } _ { i } }$ .But

$$
\frac { \partial \mathfrak { T } } { \partial q _ { i } } = - \frac { \partial T } { \partial q _ { i } } = - \sum _ { r , s } \frac { \partial a _ { r s } } { \partial q _ { i } } \dot { q } _ { r } \dot { q } _ { s } .
$$

Hence,in order that $\partial \mathfrak { T } / \partial q _ { i }$ be divisible by $\partial \mathfrak { T } / \partial \mathfrak { p } _ { i }$ , we should have

$$
\frac { \partial a _ { r s } } { \partial q _ { i } } = 0 \qquad ( r , s \neq i ) .
$$

# Furthermore

$$
\sigma _ { i j } = { \dot { q } } _ { j } \sum _ { l } a ^ { i l } { \frac { \partial ^ { 2 } T } { \partial { \dot { q } } _ { l } \partial q _ { j } } } - a ^ { i j } { \frac { \partial T } { \partial q _ { j } } } .
$$

Hence, in order that this expression be divisible by ${ \partial \mathfrak { T } } / { \partial { p } _ { i } } = { { \dot { q } } _ { i } } ;$ the following must be true:

$$
a ^ { i j } \frac { \partial a _ { r s } } { \partial q _ { j } } = 0 , ~ \sum _ { l = 1 } ^ { n } a ^ { i l } \frac { \partial a _ { r l } } { \partial q _ { j } } - a ^ { i j } \frac { \partial a _ { r j } } { \partial q _ { j } } = 0 ,
$$

$$
\sum _ { l } a ^ { i l } { \frac { \partial a _ { j l } } { \partial q _ { j } } } - { \textstyle \frac { 1 } { 2 } } a ^ { i j } { \frac { \partial a _ { j j } } { \partial q _ { j } } } = 0 \qquad ( j , r , s \neq i ; r , s \neq j ) .
$$

Thus either (79) or (80) must be valid.

In order that all expressions ${ \partial \mathfrak { T } } / { \partial q _ { i } }$ shall be divisible by ${ \partial \mathfrak { T } } / { \partial { p } _ { \mathfrak { i } } }$ the Christoffel symbol of the first kind must be zero, i.e.,

$$
[ \stackrel { i } { \prime } \bar { \jmath } ] = 0 ,
$$

or the Riemannian curvature tensor (Bianchi,1899, 1922,1924)

$$
F _ { r s , i j } = \frac { \partial } { \partial q _ { j } } \{ _ { s } ^ { r ~ i } \} - \frac { \partial } { \partial q _ { i } } \{ _ { s } ^ { r ~ j } \} + \sum _ { l } \left( \{ _ { l } ^ { r ~ i } \} \{ _ { s } ^ { l ~ j } \} - \{ _ { l } ^ { r ~ j } \} \{ _ { s } ^ { l ~ i } \} \right)
$$

should be zero, that is,

$$
{ \cal F } _ { r s , i j } = 0 .
$$

In other words, the Riemannian curvature of the Riemann space $\begin{array} { r } { d s ^ { 2 } = \sum _ { r , s } a _ { r s } d x _ { s } d x _ { r } } \end{array}$ should be zero,i.e., the space should be differentially transformed to a Euclidean $\pmb { n }$ -dimensional flat space. Several examples with $d s ^ { 2 }$ for the expressions of $_ { T }$ which satisfy either of these conditions were given by Levi-Civita (l904) and Dall'Aqua (l908，1912） (cf. Kneser,1921; Weinacht,1928; Fues,1925).Weinacht (1928) has given several separable examples and solved the problem by referring to elliptic coordinates.

In order that the wave equation in Schrodinger's wave mechanics,

$$
\sum _ { i = 1 } ^ { n } \sqrt { H } \frac { \partial } { \partial x _ { i } } \left( \frac { H _ { i } } { \sqrt { H } } \frac { \partial \Psi } { \partial x _ { i } } \right) + k ^ { 2 } ( E - V ) \Psi = 0 ,
$$

$$
H = \prod _ { i = 1 } ^ { n } H _ { i } ( x _ { i } ) ,
$$

be solvable by the separation of variables in the form $\begin{array} { r } { \Psi = \prod _ { i = 1 } ^ { n } \Psi _ { i } ( x _ { i } ) } \end{array}$ ，

it is necessary that the coefficients of $\partial ^ { \mathbf { 2 } \mathbf { 4 } \Gamma } / \partial x _ { i } ^ { 2 }$ and ${ \partial \Psi } / { \partial x _ { i } }$ differ by a factor which is a function of only a single variable $\pmb { x _ { i } }$ .Hence

$$
\frac { \sqrt { H } } { H _ { i } } \frac { \partial } { \partial x _ { i } } \left( \frac { H _ { i } } { \sqrt { H } } \right)
$$

is a function of only a single variable $\pmb { x _ { i } }$ and

$$
H _ { i } = \sqrt { H } a _ { i } ( x _ { 1 } , x _ { 2 } , . . . , x _ { i - 1 } , x _ { i + 1 } , . . . , x _ { n } ) f _ { i } ( x _ { i } ) ,
$$

and the wave equation is of the form

$$
\sum _ { i } { \sqrt { H } } { \frac { a _ { i } } { \Psi } } { \frac { \partial } { \partial x _ { i } } } \left( f _ { i } { \frac { \partial \Psi } { \partial x _ { i } } } \right) + k ^ { 2 } ( E - V ) = 0 .
$$

This equation has a solution containing $\pmb { n }$ constants $\alpha _ { 1 } , \alpha _ { 2 } , \ldots , \alpha _ { n }$ beside $E$ . We construct

$$
\phi _ { i j } ( x _ { i } ) = \frac { \partial } { \partial \alpha _ { j } } \left[ \frac { 1 } { \Psi _ { i } } \frac { \partial } { \partial x _ { i } } \left( f _ { i } \frac { \partial \Psi _ { i } } { \partial x _ { i } } \right) \right]
$$

by choosing $\alpha _ { 1 } , \alpha _ { 2 } , \ldots , \alpha _ { n } \ s$ that $\| \phi _ { i j } ( { \pmb x } _ { i } ) \| \neq 0$ . Then the wave equation becomes

$$
\sum _ { i } { \sqrt { H } a _ { i } \phi _ { i j } } = - k ^ { 2 } \delta _ { 1 j }
$$

and ${ \sqrt { H } } a _ { i } = - k ^ { 2 } \phi ^ { i 1 }$ .Hence, $H _ { i } = - k ^ { 2 } f _ { i } ( x _ { i } ) \phi ^ { i 1 }$ where $\phi \phi ^ { i j }$ is the minor of $\left\| \phi _ { i j } \right\|$ and does not depend on $\pmb { x _ { i } }$ in the same way as $a _ { i }$ Hence ${ \sqrt { H } } \phi = c =$ constant. Thus we should have

$$
V = \sum _ { i } \phi ^ { i 1 } \chi _ { i } ( x _ { i } ) ,
$$

$$
\chi _ { i } = \alpha _ { 1 } \phi _ { i 1 } ( x _ { i } ) - \frac { 1 } { \Psi _ { i } } \frac { d } { d x _ { i } } \Big ( f _ { i } \frac { d \Psi _ { i } } { d x _ { i } } \Big ) ,
$$

or, by puting

$$
\begin{array} { l } { { \Phi _ { i j } ( x _ { i } ) = - k ^ { 2 } f _ { i } ( x _ { i } ) ^ { - 1 } \phi _ { i j } ( x _ { i } ) , } } \\ { { \ } } \\ { { F _ { i } ( x _ { i } ) = - k ^ { 2 } C ^ { - 1 / \eta } f _ { i } ( x _ { i } ) , } } \\ { { \ } } \\ { { X _ { i } ( x _ { i } ) = - k ^ { 2 } f _ { i } ( x _ { i } ) \chi _ { i } ( x _ { i } ) , } } \end{array}
$$

it is necessary that

$$
\mathrm { ~  ~ \bar { ~ \psi ~ } { ~ \psi ~ } ~ } = \mathrm { ~  ~ \bar { ~ \psi ~ } { ~ \psi ~ } ~ } \psi = \sum _ { i = 1 } ^ { n } \Phi ^ { i 1 } X _ { i } ( \dot { x } _ { i } ) , \qquad \sqrt { H } \Phi = \prod _ { i = 1 } ^ { n } ( F _ { i } ( x _ { i } ) ) ^ { - 1 } .
$$

To prove that this condition is sufficient, first note that

$$
\sum _ { i = 1 } ^ { n } \Phi ^ { i 1 } \Omega _ { i } ( x _ { i } ) = 0 ,
$$

where

$$
\Omega _ { i } ( x _ { i } ) = \frac { 1 } { F _ { i } \Psi _ { i } } \frac { d } { d x _ { i } } \left( F _ { i } \frac { d \Psi _ { i } } { d x _ { i } } \right) + k ^ { 2 } ( \alpha _ { 1 } \Phi _ { i 1 } - X _ { 1 } ) .
$$

This relation represents the circumstance that a vector $( \Psi _ { 1 } , \Psi _ { 2 } , . . . , \Psi _ { n } )$ is orthogonal to a vector $( \Phi ^ { 1 1 } , . . . , \Phi ^ { n 1 } )$ . It is known that the vector $( \Phi _ { 1 j } , . . . , \Phi _ { n j } )$ $( j = 2 , 3 , . . . , n )$ has such a property. Hence, the vector $( \Psi _ { 1 } , . . . , \Psi _ { n } )$ should be represented by a linear combination of the vectors $\Phi$ .We write the constant coefficients of this linear expression as $- k ^ { 2 } \alpha _ { 2 } , . . . , - k ^ { 2 } \alpha _ { n }$ ；then

$$
\Omega _ { i } ( x _ { i } ) = - k ^ { 2 } \sum _ { j = 1 } ^ { n } \alpha _ { j } \Phi _ { i j } ( x _ { i } ) .
$$

Hence $\Psi _ { i }$ is determined by the following equation (Robertson, 1928; Brillouin,1926,1926a,1935; Eisenhardt,1934,1948,1949;Rao, 1935, 1935a; Kemble,1937):

$$
{ \frac { 1 } { F _ { i } ( x _ { i } ) } } { \frac { d } { d x _ { i } } } \left[ F _ { i } ( x _ { i } ) { \frac { d \Psi _ { i } } { d x _ { i } } } \right] + k ^ { 2 } \biggl [ \sum _ { j = 1 } ^ { n } \alpha _ { j } \Phi _ { i j } ( x _ { i } ) - X _ { i } ( x _ { i } ) \biggr ] \Psi _ { i } = 0 .
$$

The convergence of the solution in eigenfunction expansions in the separable cases of the Schrodinger equation has been proved by Titchmarsh (1955,1958).

# HAMILTONIAN MATRIX

# 1.18HAMILTONIAN MATRIX

Operations on a matrix obey the rules for a determinant. A matrix obtained by an interchange of the rows and the columns of a matrix $M$ is called a transposed matrix and denoted by $M ^ { \prime }$ . When the number of rows equals the number of columns, the array is called a square matrix and the number of rows or columns is called the order.A matrix whose diagonal elements are all 1 while the remaining elements are all O, is a unit matrix $\boldsymbol { \varepsilon }$ or $\epsilon .$ A matrix $M ^ { - 1 }$ such that $M M ^ { - 1 } = E$ is the inverse matrix of $M .$ .The determinant formed of a square matrix $M$ is denoted by det $M .$ Obviously det $\varepsilon = 1$ .A matrix such that det $M \neq 0$ is said to be nonsingular; otherwise, singular. A matrix whose elements are all zero is a null matrix $\omega$

Two fixed nonsingular matrices $\pmb { A }$ and $B$ satisfy the symmetry relation $( A H B ) ^ { \prime } = A H B$ for any symmetric matrix $H = H ^ { \prime }$ if and only if there exists a number $s \neq 0$ such that $\boldsymbol { A } ^ { \prime } = s \boldsymbol { B }$ Put $M ^ { \prime } = A ^ { \prime } B ^ { - 1 }$ ; then the condition $( A H B ) ^ { \prime } = A H B$ is equivalent to $M H = H M ^ { \prime }$ ,where $H = H ^ { \prime }$ is arbitrary and may be chosen to be $\pmb { \cal E }$ Hence $M = M ^ { \prime }$ .Now $M H = H M$ holds for any $H = H ^ { \prime }$ if and only if $M$ is of the form $s E$ This is evident if $M$ is a diagonal matrix. If it is not diagonal, then $M = M ^ { \prime }$ is transformed by means of an orthogonal matrix $R$ into a diagonal matrix $R M R ^ { - 1 }$ ，whereas $R$ transforms the set of all matrices $H = H ^ { \prime }$ into itself. Thus $R M R ^ { - 1 }$ is a diagonal matrix and is equal to $s E .$ Finally, $s \neq 0$ ，because $s E = M = M ^ { \prime } = A ^ { \prime } B ^ { - 1 }$ is the product of two non-singular matrices. Hence $A ^ { \prime } = s B$ ， $s \neq 0$ (Wintner, 1934; Williamson, 1936,1937,1939, 1940).

Consider a matrix composed of a null matrix $\omega$ of order $_ { n }$ and a unit matrix $\epsilon$ of order $\pmb { n }$ ，such as

$$
G = \binom { \omega } { \epsilon } \quad - \epsilon \biggr ) ,
$$

in contrast to

$$
E = \left( \begin{array} { l l } { { \epsilon } } & { { \omega } } \\ { { \omega } } & { { \epsilon } } \end{array} \right) ;
$$

then,

$$
G = - G ^ { - 1 } = - G ^ { \prime } .
$$

This is the matrix formed by the coefficients of & and $d$ in a bilinear covariant expression (Section 1.7) and is naturally nonsingular.

A system of $2 n$ ordinary differential equations of the first order that are linear and homogeneous and do not contain $t$ explicitly is canonical if and only if there exists a symmetric matrix $\boldsymbol { \mathsf { \Pi } } ^ { H }$ such that the differential equations can be written in the form

$$
G { \dot { x } } = H x .
$$

In fact,this is (up to the factor 2) the matrix of the $2 n$ -ary quadratic form representing the Hamiltonian function. Since $G ^ { - 1 } = - G ,$ we can write

$$
\dot { x } = - G H x , \qquad H = H ^ { \prime } .
$$

A nonsingular matrix $c$ independent of $t$ is called Hamiltonian if the transformation $\boldsymbol { x } = C \boldsymbol { y }$ transforms any differential system of this form to a differential system of the same form (Section 1.3).

If we substitute $\boldsymbol { x } = C \boldsymbol { y }$ in the above equation, we obtain $\dot { y } = - G K y .$ where $K = - G C ^ { - 1 } G H C$ since $G = - G ^ { - 1 }$ . Hence, $C$ is a Hamiltonian matrix if and only if $\kappa$ is the matrix of a $2 n$ ary quadratic form, that is, if and only if $K = K ^ { \prime }$ whenever $H = H ^ { \prime }$ . We can write $K = K ^ { \prime }$ in the form $( A H B ) ^ { \prime } = A H B$ by putting $A = C ^ { - 1 } G$ and $B = C G$ ，so that det $A \neq 0$ det $B \neq 0$ ,i.e., $A ^ { \prime } = s B$ Hence $C ^ { \prime } G C = s G$ ，

$$
| { \bf d e t } C | = | s | ^ { n } > 0 ,
$$

if we write $\pmb { \mathscr { s } }$ instead of $- s ^ { - 1 }$

If a matrix $C$ of order $2 n$ with real elements satisfies

$$
C ^ { \prime } G C = s G ,
$$

then it is said to be Hamiltonian (Wintner, l937, 1947;van Kampen and Wintner, 1936).When $s$ is a constant different from zero, $\pmb { \mathscr { s } }$ is called the multiplier of $C$ (van Kampen and Wintner, 1936, l938; Wintner, 1937). If det $G \neq 0$ ,then $| \mathsf { d e t } C | = | s | ^ { n }$ ·

The matrix

$$
K = - G C ^ { - 1 } G H C
$$

belonging to the transformed Hamiltonian function takes the form

$$
K = s ^ { - 1 } C ^ { \prime } H C .
$$

Thus the necessary and sufficient condition (83) for a Hamiltonian matrix $C$ is that the bilinear form in cogradient variables belonging to the matrix $G$ be a relative invariant of a nonsingular linear substitution if and only if the matrix of this substitution is a Hamiltonian matrix. The group of the Hamiltonian matrices, in contrast to the rotation and reflection groups of Euclidean space, cannot be characterized by the absolute invariance of a bilinear form (Section 4.2). For example, if

$$
C = \binom { \epsilon } { \omega } \ _ { - \epsilon } ^ { \omega } ) ,
$$

then $C ^ { \prime } G C = - G \neq G$ although $\left| \operatorname* { d e t } C \right| = \left| ( - 1 ) ^ { n } \right| = 1$

Since $s \neq 0$ ,a Hamiltonian matrix is nonsingular. If $c$ is Hamiltonian, then $C ^ { \prime }$ is also Hamiltonian and has the same multiplier.When $s = 1$ ， then $C$ and $C ^ { - 1 }$ have the same eigenvalues; that is, the roots of

$$
{ \bf d e t } \left( C - \lambda E \right) = 0
$$

and the roots of det $( C ^ { - 1 } - \lambda E ) = 0$ are the same.For, by the definition of a Hamiltonian matrix, $C ^ { \prime } = G C ^ { - 1 } G ^ { - 1 }$ or $G ^ { - 1 } C ^ { \prime } G = C ^ { - 1 }$ .Hence

$$
G ^ { - 1 } ( C ^ { \prime } - \lambda E ) G = C ^ { - 1 } - \lambda E .
$$

The totality of eigenvalues is called the spectrum; it may be discrete or continuous (Section 20.1).

Take a set of $_ m$ functions $Y ( y _ { 1 } , \dots , y _ { m } )$ and consider the square matrix $A = A ( Y )$ ，of order $m$ ，and a vector $B = B ( Y )$ . A vector is a matrix with a single column consisting of the vector components. Suppose that there exists a scalar function $g = g ( Y )$ satisfying

$$
B + A { \mathrm { ~ g r a d } } f = { \mathrm { g r a d } } g ,
$$

in the whole domain of the vector space $Y$ for any scalar function $f = f ( Y )$ .Then for all functions $f$ and for a given pair $\boldsymbol { A }$ and $B$ ，there exists a function $g$ satisfying (84) if and only if $B$ has the form of the gradient of a scalar function and $A$ has the form $s E _ { : }$ ，where $\pmb { \mathscr { s } }$ is a scalar independent of $\boldsymbol { Y }$ and $\boldsymbol { \varepsilon }$ is a unit matrix of order $\pmb { m }$ . It is assumed that $A$ ， $B$ and $f , g$ all have continuous partial derivatives of first and second order.

The sufficiency of these conditions is obvious. To prove their necessity: suppose that there exists a function $g$ for any function f. Choose $f = f ( Y )$ independent of $Y$ ; then,from (84), $B$ is a gradient. Hence,if we choose $f = f ( y _ { 1 } , \dots , y _ { m } )$ as an arbitrary polynomial $q ( y _ { i } )$ of the ith vectorcomponent of $Y$ ,and denote by $A _ { i } = A _ { i } ( Y )$ the vector whose components are the elements of the ith column of $A = A ( Y )$ ，then the vector $q ( y _ { i } ) A _ { i } ( Y )$ is a gradient for every polynomial $q ( y _ { i } )$ . Hence $A ( Y )$ isa diagonal matrix, since the integrability condition is satisfied by a gradient, and the ith diagonal element of $A ( Y )$ is a function of $y _ { i }$ alone. Furthermore, since $A$ grad $f$ is a gradient for any $f ,$ we see - by choosing i and $j$ arbitrarily and putting $f ( Y ) = y _ { i } y _ { j }$ - that the diagonal elements of $A ( Y )$ are all equal and hence $A = s E$ for a constant $\pmb { \mathscr { s } }$

# 1.19CANONICAL TRANSFORMATION

Put

$$
x _ { i } = q _ { i } , \qquad x _ { n + i } = p _ { i } \qquad ( i = 1 , 2 , . . . , n )
$$

and take a vector $X = ( x _ { 1 } , . . . , x _ { 2 n } )$ ; then Hamilton's equations can be written

$$
G \dot { X } = \operatorname { g r a d } _ { x } H , \qquad H = H ( X , t ) ,
$$

where a dot denotes total differentiation with respect to the time $t _ { : }$ and the subscript of“grad’’ refers to the space in which the partial differentiations are performed.

Now transform the phase space $\boldsymbol { \cal X }$ for any fixed $t$ into a phase space $\pmb { Y }$ by means of

$$
Y = Y ( X ; t ) , \qquad Y = ( y _ { 1 } , \dots , y _ { 2 n } ) , \qquad X = ( x _ { 1 } , \dots , x _ { 2 n } ) .
$$

Assume that these $2 n$ functions have continuous partial derivatives of the second order considered as functions of $2 n$ variables and $t _ { : }$ ,and that the transformation builds a locally one-to-one correspondence between the two phase spaces for any fixed $t _ { : }$ ， the Jacobian of order $2 n$ being everywhere different from zero.A transformation satisfying these properties is called canonical if it transforms any canonical system (85) into a canonical system such that

$$
G \dot { Y } = \operatorname { g r a d } _ { Y } K , K = K ( Y , t ) .
$$

Let $c$ be the Jacobian matrix of the transformation (86) for a fixed $t _ { i }$ so that the jth element in the kth column of the nonsingular $2 n$ order square matrix $C$ is the partial derivative of $y _ { j }$ with respect to $\pmb { x } _ { k }$ .The matrix $c$ can be considered a function $C ( Y , t )$ of $\pmb { Y }$ and $t$ rather than a function of $\pmb { X }$ and $t .$ By direct differentiation we obtain

$$
\dot { Y } = C \dot { X } + Y _ { t } ,
$$

where the subscript $t$ refers to a partial differentiation with respect to $t$ furthermore

$$
X = X ( Y , t )
$$

is the inverse transformation of (86). Also,for any function $f ,$

$$
\operatorname { g r a d } _ { x } f = C ^ { \prime } \operatorname { g r a d } _ { Y } f ,
$$

where $C ^ { \prime }$ is the transposed matrix of $C$ ; finally,

$$
\mathbf { g r a d } _ { Y } Y _ { t } = C _ { t } C ^ { - 1 } .
$$

From (88) and (89) we see that (86),which is not assumed to be a constant transformation, transforms (85） into

$$
G C ^ { - 1 } ( \dot { Y } - Y _ { t } ) = C ^ { \prime } \operatorname { g r a d } _ { Y } H .
$$

Referring to the definition of $G$ ， we can put this equation in the form

$$
G { \dot { Y } } = G Y _ { t } - G C G C ^ { \prime } \operatorname { g r a d } _ { Y } H .
$$

Comparing this equation with (87),we see that (86) is a canonical transformation if and only if there exists a $K = K ( Y , t )$ for any $H ,$ such that

$$
G Y _ { t } - G C G C ^ { \prime } \operatorname { g r a d } _ { Y } H = \operatorname { g r a d } _ { Y } K ,
$$

where $H = H ( X , t )$ is expressed as a function of $\pmb { Y }$ and $t$ by (86a). Compare(91） with (84) by keeping $t$ fixed; then we see that (86) is a canonical transformation ifand only if the vector $B = G Y _ { t }$ is the gradient of a scalar function with respect to $Y$ for any fixed $t ,$ and that the matrix $A = - G C G C ^ { \prime }$ is of the form $s E$ for any fixed $t _ { : }$ where $\pmb { \mathscr { s } }$ is a scalar function independent of $\pmb { Y } _ { : }$ and $\boldsymbol { \varepsilon }$ is the unit matrix of order $2 n$ .Hence the transformation (86) is canonical if and only if there exist two scalars

$$
R = R ( Y , t ) , \qquad s = s ( t ) ,
$$

such that

$$
G Y _ { t } = \operatorname { g r a d } _ { Y } R , \quad \quad C ^ { \prime } G C = s G .
$$

If this condition is satisfied, then the Hamiltonian function for (87), because of (91), is

$$
K = s H + R .
$$

The first condition of (92) cannot be applied directly to a given transformation (86), since $\scriptstyle { R }$ is unknown. But it can be shown that there exists for a given transformation (86) a function $R$ satisfying this condition if and only if the matrix $C ^ { \prime } G C _ { \mathrm { { i } } }$ ，considered as a function of $\pmb { Y }$ and $t ,$ is independent of t.In fact, the function $R$ exists for a given transformation (86) satisfying this condition ifand only ifgrady $G Y _ { t }$ is a symmetric matrix at every point of the $( 2 n + 1 )$ -dimensional domain in question. Since we can verify that $\begin{array} { r } { \mathrm { g r a d } _ { \boldsymbol { Y } } G Y _ { t } = G \mathrm { g r a d } _ { \boldsymbol { Y } } Y _ { t } , } \end{array}$ we have grady $G Y _ { t } = G C _ { t } C ^ { - 1 }$ by (90). Hence there exists a function $R$ if and only if $G C _ { t } C ^ { - 1 }$ is a symmetric matrix, that is,is equal to the matrix $( G C _ { t } C ^ { - 1 } ) ^ { \prime }$ ; in other words, if and only if

$$
C _ { t } ^ { \prime } G C + C ^ { \prime } G C _ { t } \equiv ( C ^ { \prime } G C ) _ { t }
$$

is the null-matrix for any $\pmb { Y }$ and $t ,$ that is, ifand only if $C ^ { \prime } G C$ is a function of $\pmb { Y }$ alone.

The scalar $\pmb { \mathscr { s } }$ in the necessary and sufficient condition (92) is independent of the vector $\pmb { Y }$ in the phase space for any fixed t. This fact is due to (84).The pair of conditions (92) implies that the scalar $\pmb { \mathscr { s } }$ should be independent both of $\pmb { Y }$ and t.A transformation (86) can be shown to be canonical if and only if there exists an $s \neq 0$ independent of both $\pmb { Y }$ and $t ,$ such that the Jacobian matrix $C = C ( Y , t )$ of (86) is a Hamiltonian matrix for any $\pmb { Y }$ and $t$ with the constant $\pmb { \mathscr { s } }$ as the multiplier.This is because (86) is canonical if and only if (92) can be satisfed by a suitable choice of the functions $\pmb { R }$ and $\pmb { S } \cdot$ The first condition of (92) is satisfied if and only if $C ^ { \prime } G C$ is independent of $t$ ,that is, $\pmb { \mathscr { s } }$ is a constant by the second condition of (92), since $s \neq 0$ .Hence,a transformation (86) is canonical if and only if its Jacobian matrix $C = C ( Y , t )$ determines a linear sub-stitution under which the bilinear form in cogradient variables belonging to the skew-symmetric matrix $G$ is a relative invariant with a multiplier $s \neq 0$ and is independent both of $\pmb { Y }$ and ${ \pmb t } .$

It is clear from the definition of a canonical transformation that the set of canonical transformations defined in a common $( 2 n + 1 )$ -dimensional domain forms a group.If $R _ { 1 } = R _ { 1 } ( Y , t )$ belongs to a canonical transformation which is substituted into another canonical transformation characterized by $R _ { 2 } = R _ { 2 } ( Y , t )$ and $s _ { 2 } .$ ，then the function $\scriptstyle { R }$ characterizing the composite transformation is ${ \cal R } = s _ { 2 } { \cal R } _ { 1 } + { \cal R } _ { 2 }$ .If $\because C , R , s$ characterize a canonical transformation, then $C ^ { - 1 } , \ - R / s , 1 / s$ characterize the inverse transformation. The multiplier $\pmb { \mathscr { s } }$ is uniquely determined by the canonical transformation, while the function $R$ remains indeterminate up to an additive term which is a function of $t$ alone.

If (86）satisfies the first condition of (92) for any $t$ and $\mathbf { { \mathit { Y } } _ { i } }$ and the second condition for a fixed $t$ and any $Y _ { z }$ ，then (86) is a canonical transformation.For,if the first condition of (92) is satisfied for any $t$ and $Y _ { : }$ ， then $C ^ { \prime } G C$ is independentof $t$ ,so that the second condition of (92) holds for any $t$ and $Y$ if it holds for a single $t$ and any Y. Call a transformation (86) conseruative if it does not contain $t$ explicitly.If (86) is a contact transformation and $t _ { 0 }$ is a fixed value of $t$ ,then the conservative transformation defined by $Y = Y ( X , t _ { 0 } )$ is canonical. In fact, any conservative transformation satisfies the first condition of (92) if we put $R \equiv 0$ ; it also satisfies the second condition for any $t$ or no $t .$ 、Thus (86） need not be canonical even if the conservative transformation $Y = Y ( X , t _ { 0 } )$ is canonical for any fixed $t = t _ { 0 }$ (van Kampen and Wintner, 1936).

# 1.20PFAFF'S CRITERION

Put

$$
y _ { i } = v _ { i } , \qquad y _ { n + i } = u _ { i } \qquad ( i = 1 , 2 , . . . , n )
$$

in (86); then (86) is a canonical transformation ifand only if there exists a function $\scriptstyle { R }$ for which the Pfaffian (Section 1.7)

$$
s \sum _ { i = 1 } ^ { n } { p _ { i } d q _ { i } } - \sum _ { i = 1 } ^ { n } { u _ { i } d v _ { i } } + R d t
$$

is a perfect differential by (86). Consider a Pfaffian

$$
{ \scriptstyle { \frac { 1 } { 2 } } } s X ^ { \prime } G d X - { \scriptstyle { \frac { 1 } { 2 } } } Y ^ { \prime } G d Y + R d t ,
$$

where an expression such as $Z ^ { \prime } A W$ denotes the bilinear form belonging to the square matrix $\pmb { A }$ and the vectors $z$ and $W _ { ; }$ ，so that $Z ^ { \prime } A W$ is

considered as the product of three matrices. Whether the transformation (86) be canonical or not, the difference of the two Pfaffians (94) and (95) is a perfect differential of the function

$$
s \sum _ { i = 1 } ^ { n } { p _ { i } q _ { i } } - \sum _ { i = 1 } ^ { n } u _ { i } v _ { i } .
$$

Now (95) is a perfect differential in virtue of (86) if and only if (86) is canonical.In fact,(95) becomes a Pfaffian

$$
{ \scriptstyle { \frac { 1 } { 2 } } } ( s X ^ { \prime } G - Y ^ { \prime } G C ) ~ d X + ( R - { \scriptstyle { \frac { 1 } { 2 } } } Y ^ { \prime } G Y _ { t } ) ~ d t
$$

by (86),where $\pmb { Y }$ and $\textstyle \boldsymbol { Y } _ { t }$ are considered as functions of $X$ and $t .$ This last Pfaffian is a perfect differential of a function of $2 n + 1$ variables $X$ and $t$ if and only if (i) the partial derivatives of the covariant vector

$$
{ \scriptstyle { \frac { 1 } { 2 } } } ( s X ^ { \prime } G - Y ^ { \prime } G C )
$$

with respect to $t$ is the covariant gradient of the scalar $R \mathrm { ~ - ~ } _ { 2 } ^ { 1 } Y ^ { \prime } G Y _ { t }$ with respect to the vector $X$ and (ii） the Stokes alternating derivative of this covariant vector with respect to the vector $X$ is identically zero. It can be shown that conditions (i） and (ii） are equivalent to the two conditions of (92).

Actually, condition (i) is satisfied if and only if

$$
\begin{array} { r } { - \frac 1 2 ( Y ^ { \prime } G C ) _ { t } = ( \mathrm { g r a d } _ { x } R ) ^ { \prime } - \frac 1 2 [ \mathrm { g r a d } _ { x } \left( Y ^ { \prime } G Y _ { t } \right) ] ^ { \prime } , } \end{array}
$$

or, when transposed,

$$
\begin{array} { r } { - \frac { 1 } { 2 } ( C ^ { \prime } G ^ { \prime } Y ) _ { t } = \operatorname { g r a d } _ { X } R - \frac { 1 } { 2 } C ^ { \prime } G Y _ { t } + \frac { 1 } { 2 } C _ { t } ^ { \prime } G Y , } \end{array}
$$

that is, if and only if

$$
C ^ { \prime } G Y _ { t } = C ^ { \prime } \mathrm { g r a d } _ { \mathrm { Y } } R .
$$

Since this can be writen in the form of the first condition of (92), condition (i) is equivalent to this first condition of (92). Furthermore, since an alternating derivative is transformed by any transformation (86) as a tensor,and since the alternating derivative of $X ^ { \prime } G$ with respect to $\ b X$ is $G$ by definition, then the alternating derivative of $Y ^ { \prime } G C$ with respect to $X$ is $C ^ { \prime } G C$ and condition (ii） is satisfied if and only if the matrix ${ \scriptstyle \frac { 1 } { 2 } } ( s G - C ^ { \prime } G C )$ is identically zero.Hence,condition (ii） is equivalent to the second condition of (92) (van Kampen and Wintner 1936; Wintner 1947).

# I.2IPOLAR FACTORIZATION

A matrix with complex elements is called symplectic according to Weyl (1939). If two symplectic matrices $A = \left( a _ { j k } \right)$ and $B = ( b _ { j k } )$ are such that $a _ { j k } = b _ { j k }$ for all $j$ and $k$ ，then $A = B$ Let $\bar { A } = ( \bar { a } _ { j k } )$ be a conjugate matrix of $A ( a _ { j k } )$ such that $a _ { k j } = \bar { a } _ { j k } .$ ，where a bar means the complex conjugate, so that $\scriptstyle A ^ { \prime } = { \overline { { A } } }$ and $\overline { { A ^ { \prime } } } = A$ The matrix $\pmb { A }$ determines a bilinear form

$$
A = A ( x , y ) = \sum _ { k , j } a _ { j k } y _ { j } x _ { k }
$$

and

$$
A ( x _ { j } ) = { \frac { \partial A ( x , y ) } { \partial y _ { j } } } = \sum _ { k } a _ { j k } x _ { k } ,
$$

that is,

$$
A ( x ) = ( a ) ( x ) .
$$

If $a _ { k j } = \bar { a } _ { j k }$ ，then $A ( x , y )$ is called a Hermitian form.The Hermitian form $A ( x , \bar { x } ) = \overline { { { A ( x , \bar { x } ) ^ { \prime } } } } = \overline { { { A ^ { \prime } ( x , \bar { x } ) } } }$ is real. If $A ( x , { \bar { x } } )$ equals zero when all $_ x$ are equal to zero and never changes its sign,then it is called definite. If the sign is positive, i.e., $A ( x , { \bar { x } } ) > 0$ ，then it is called a Hermitian form. The product of two bilinear forms $A ( x , y )$ and $B ( x , y )$ is defined by

$$
\begin{array} { l } { { \displaystyle { \cal C } = \sum _ { l } \displaystyle \frac { \partial A } { \partial x _ { l } } \displaystyle \frac { \partial B } { \partial y _ { l } } = \sum _ { l } \sum _ { j } a _ { j l } y _ { j } \sum _ { k } b _ { l k } x _ { k } } } \\ { { = \sum _ { j , k } y _ { j } x _ { k } \sum _ { l } a _ { j l } b _ { l k } = ( c _ { j k } ) , } } \end{array}
$$

where

$$
c _ { j k } = \sum _ { l } a _ { j l } b _ { l k } .
$$

Operate with

$$
B ( { \boldsymbol x } _ { l } ) = \sum _ { k } b _ { l k } { \boldsymbol x } _ { k }
$$

on

$$
A ( x _ { j } ) = \sum _ { l } a _ { j l } x _ { l } .
$$

Then we obtain

$$
A B ( x _ { j } ) = \sum _ { l , k } a _ { j l } b _ { l k } x _ { k } = \sum _ { k } x _ { k } \sum _ { l } a _ { j l } b _ { l k } = \sum _ { k } c _ { j k } x _ { k } ,
$$

that is, a bilinear form $c$ ; also $( A B ) ^ { \prime } = B ^ { \prime } A ^ { \prime }$ ,and $\overline { { ( A B ) } } = \bar { A } \bar { B }$

The simplest Hermitian form is the unit-form $\begin{array} { r } { E = E ( x , y ) = \sum \bar { x } y . } \end{array}$ We have $E ( x , \bar { x } ) = \textstyle \sum ( x ) ^ { 2 }$ .A form $U$ such that $\overline { { U } } ^ { \prime } U = E _ { \mathrm { { ; } } }$ $\overline { { U ^ { \prime } } } = U ^ { - 1 }$ ， $U \neq 0$ , is called unitary.

Take

$$
R ( z , t ) = \sum _ { j , k } t _ { j k } t _ { j } z _ { k } , \qquad x _ { j } = \frac { \partial R ( z , t ) } { \partial t _ { j } } ,
$$

and transform a Hermitian form $\pmb { A }$ to $R ^ { - 1 } A R$ Then $X = A ( x , { \bar { x } } )$ is transformed to $Z = \overline { { { R ^ { \prime } } } } A R ( z , \bar { z } ) = X ,$ where $X$ is a Hermitian form. For, $\pmb { X }$ equals zero if all $_ x$ are zero and $z$ equals zero if all $_ z$ are zero. $X$ is positive for all values of $_ x$ and $z$ is positive for all $z .$ Hence,

$$
\overline { { Z ^ { \prime } } } = \overline { { ( \vec { R } ^ { \prime } A R ) ^ { \prime } } } = Z .
$$

Thus,a transformation $R$ transforms a Hermitian form $\pmb { A }$ to a Hermitian form $\overline { { R ^ { \prime } A R } }$ Let a transformation of a Hermitian form $\pmb { A }$ to a Hermitian form $R ^ { - 1 } A R$ be $R = U ,$ $\overline { { R ^ { \prime } } } = R ^ { - 1 }$ ; then the new Hermitian form gives a new Hermitian transformation.

Let the $N$ transformations of a group $G _ { n }$ of a finite order $\pmb { n }$ be $A _ { l }$ $( l = 1 , 2 , . . . , N )$ ,and $H$ be any Hermitian form.The Hermitian form

$$
\mathbf { \Pi } _ { \mathbf { H } } = \sum _ { l } \overline { { A _ { l } ^ { \prime } } } H A _ { l } ( \pmb { x } , \pmb { \bar { x } } )
$$

is an absolute invariant by all transformations of $G _ { n }$ . If the invariant of $G _ { n }$ is $\boldsymbol { \varepsilon }$ ,then $G _ { n }$ is formed exclusively by unitary transformations and is itself unitary. Consider a transformation $_ { T }$ such that ${ \overline { { T ^ { \prime } } } } = T .$ Let $\pmb { \sigma }$ be a real parameter and $i = \sqrt { - 1 }$ . Then it can be proved that a transformation

$$
\begin{array} { r } { U = ( \sigma E + i T ) ^ { - 1 } ( \sigma E - i T ) } \\ { = ( \sigma E - i T ) ( \sigma E + i T ) ^ { - 1 } } \end{array}
$$

is unitary. A unitary transformation forms a unitary group. It can be shown that this group is transitive (Section 4.27; Weyl, 1923, 1931, 1939;Wintner,1929).

A Hermitian transformation $\pmb { H }$ and its transform $U ^ { - 1 } H U$ generated by a unitary transformation are considered to be equivalent. Take $\dot { H } = ( h _ { j k } ) , \dot { h _ { j k } } = \bar { h } _ { k j } , \overline { { H ^ { \prime } } } = H ;$ then det $H \neq 0$ Let $\boldsymbol { r }$ be a root of the characteristic equation $\Delta = \Delta ( r ) = \operatorname* { d e t } \left( H - r E \right) = 0 .$ ，There exists at least one point $\xi$ such that $\begin{array} { r } { \sum _ { k } h _ { j k } \xi _ { k } = r \xi _ { j } } \end{array}$ or $H ( \xi ) = r \xi$ . Hence,a Hermitian transformation keeps at least one point $\xi$ invariant in the space of $\pmb { \xi }$

By a suitable unitary transformation a Hermitian form of order $\pmb { n }$ can be decomposed into a Hermitian form $H _ { n - 1 }$ of order $n - 1$ and a Hermitian form $h _ { n n } x _ { n } y _ { n }$ of order $_ { n }$ , as can be proved by considering a unitary transformation $W = [ w _ { j k } ]$ and its subtransformation

$$
\begin{array} { r } { W _ { j k } = \partial | W | / \partial w _ { j k } , } \end{array}
$$

such that $\overline { { W ^ { \prime } } } W = E ,$ $W ^ { \prime - 1 } = \overline { { W } } _ { \mathrm { { i } } }$ that is, $( \operatorname * { d e t } W ) ^ { - 1 } W _ { j k } = { \bar { w } } _ { j k }$

A matrix $C = \left( c _ { j k } \right)$ is called normal if $c _ { j k } = 0$ for $j \neq k$ or $C$ is of the form $\begin{array} { r } { C = \sum _ { j } c _ { j j } x _ { j } y _ { j } . } \end{array}$ f $R ^ { - 1 } A R$ formed of two matrices $R$ and $A$ is normal, then $R$ is called a normalizing matrix of $\pmb { A }$ .When the characteristic equation for the transformation has only simple roots, then there always exists a normalizing matrix for $\pmb { A }$ .When the characteristic equation has multiple roots,then $A$ cannot generally be normalized. Jordan (Turnbull and Aitken,1932;Wedderburn,1934) has proved that any transformation $\pmb { A }$ of finite order $_ { n }$ can be normalized. If the characteristic equation $\Delta = \Delta ( r ) = \operatorname* { d e t } \left( A - r E \right) = 0$ has a root $\pmb { r }$ of multipicity $\pmb { \mathscr { p } }$ ，then the minor determinants of $\Delta$ of orders $n - p + 1$ ， $n - \phi + 2 , . . . , n - 1 , n$ vanish for this value of $_ r$ 、This property holds also for any Hermitian matrix, as well as for any unitary matrix.

It can be proved that the matrix obtained by multiplying a Hermitian matrix by itself $m$ times is also Hermitian,and that fora given Hermitian matrix $\pmb { H }$ there exists one and only one Hermitian matrix defined by $A ^ { m } = H .$

Let $P$ be an arbitrary matrix with det $P \neq 0$ ； then $H = \bar { P } ^ { \prime } P$ is Hermitian. Conversely,any Hermitian matrix can be put in the form $H = \bar { P } ^ { \prime } P .$ . The matrix $P$ is of the form $P = U \sqrt { H }$ with a unitary matrix $\boldsymbol { U }$ (Autonne, 1902).

A Hermitian form does not in general form a group. If a group G has a Hermitian form $\boldsymbol { H }$ as an absolute invariant, then the group $H ^ { 1 / 2 } { \tt G } H ^ { - 1 / 2 }$ is unitary and has the Hermitian $\boldsymbol { \varepsilon }$ as its absolute invariant. It can be proved that a group that has two distinct Hermitian forms as its invariants is decomposable.

When we limit ourselves to the real domain, then a Hermitian form $H = ( h _ { j k } )$ - such that $h _ { j k } = h _ { k j } = \bar { h } _ { j k }$ 一is real and symmetric at the same time;and a unitary transformation $U$ — such that $\overline { { U } } = U$ —is orthogonal, $U ^ { \prime } U = E$ 、An orthogonal group is transitive in the real domain. Thus a Hermitian form in the real domain has at least an orthogonal normal form, that is,a real unitary form.A multiple root of a real Hermitian form is also a real Hermitian form (Autonne, l902; Williamson, 1935).

In virtue of this result of polar factorization it can be seen that there exists for any nonsingular matrix $A$ one and only one positive-definite and symmetric matrix $P = P ^ { \prime }$ and one and only one orthogonal matrix $R = R ^ { \prime - 1 }$ such that $A = P R$ 、It can be proved that both these polar factors $P$ and $R$ of any Hamiltonian matrix $C$ are Hamiltonian matrices (Wintner, 1934).

In fact,if we substitute $C = P R$ (where $P = P ^ { \prime }$ ， $R ^ { \prime } = R ^ { - 1 }$ ）inthe formula (83),with $\vert \operatorname* { d e t } C \vert = \vert s \vert ^ { n } > 0$ ，then we obtain

$$
R ^ { - 1 } P G P R = s G .
$$

Hence

$$
P _ { \mathrm { i } } R _ { \mathrm { 1 } } = P _ { \mathrm { 2 } } R _ { \mathrm { 2 } } ,
$$

where

$$
\begin{array} { r l } { R _ { 1 } = G , \quad } & { R _ { 2 } = \pm R G R ^ { - 1 } , } \\ { P _ { 1 } = P , \quad } & { P _ { 2 } = \pm s R _ { 2 } P ^ { - 1 } R _ { 2 } ^ { - 1 } \quad } & { ( \pm s = | s | ) . } \end{array}
$$

Orthogonal matrices form a group containing $\scriptstyle { R }$ and $\pm G ,$ since

$$
G ^ { \prime } = - G = G ^ { - 1 } ,
$$

so that $R _ { 1 }$ and $R _ { 2 }$ are orthogonal matrices. On the other hand, $P _ { 1 }$ and $P _ { 2 }$ are positive-definite matrices, because the reciprocal matrix of a positivedefinite matrix $P$ is positive-definite and remains so after an orthogonal transformation and multiplication by a positive number $\pm s = | s |$ · Accordingly, $R _ { 1 } = R _ { 2 }$ and $P _ { 1 } = P _ { 2 }$ ， owing to the uniqueness of the polar factorization of the nonsingular matrix $P _ { 1 } R _ { 1 } = P _ { 2 } R _ { 2 }$ .Hence

$$
{ \cal G } = \pm R G R ^ { - 1 } , ~ { \cal P } = \pm s G { \cal P } ^ { - 1 } { \cal G } ^ { - 1 } ,
$$

where

$$
R ^ { - 1 } = R ^ { \prime } , \qquad P ^ { \prime } = P .
$$

Consequently

$$
C ^ { \prime } G C = s G , \qquad | \mathbf { d e t } C | = | s | ^ { n } > 0
$$

is satisfied by $C = R$ and by $C = P$ , if we choose the multiplier $\pmb { \mathscr { s } }$ equal to $\pm 1 , \pm s ,$ respectively. Thus $R$ and $P$ are Hamiltonian matrices.

Since the product of two Hamiltonian matrices is Hamiltonian,it follows that it is sufficient for normalization to know those positive-definite or orthogonal Hamiltonian matrices, that is, those matrices that represent dilatations and rotations or reflections in a Euclidean space of $2 n$ dimensions. The projection of an orthogonal transformation of the $2 n -$ dimensional phase space on the $\pmb { n }$ -dimensional coordinate space need not be an orthogonal transformation of this $\pmb { n }$ -dimensional space.Write a Hamiltonian matrix $c$ in the form

$$
C = { \binom { \alpha \beta } { \gamma \delta } } ,
$$

so that $\alpha , \beta , \gamma ,$ δ are matrices of order $_ n$ ,and substitute the defnition of $G$ of Section 1.18 and of this matrix $c$ in condition (83） with $C ^ { \prime } = C ^ { - 1 }$ of $2 n$ -ary orthogonality； then, $\gamma = - \beta$ ， $\delta = \alpha$ if $s > 0$ ，and $\gamma = \beta$ ， $\delta = - \alpha$ if $s < 0$ Hence,if $c$ is a Hamiltonian matrix, it is a $2 n$ -ary orthogonal matrix if and only if it has either of the particular forms

$$
C = \left( \begin{array} { r r } { { \alpha } } & { { \beta } } \\ { { - \beta } } & { { \alpha } } \end{array} \right) \qquad ( s = 1 ) ,
$$

or

$$
C = { \binom { \alpha } { \beta } } \quad { \overset { \beta } { \_ } } \quad \quad ( s = - 1 ) .
$$

Williamson (l936a) extended such polar representation to singular matrices.He also (1935) discussed the nonsingular pencils of Hermitian matrices.

# I.22PFAFFIAN DYNAMICAL SYSTEM

Birkhoff (1928) noticed that Hamiltonian equations in dynamics can be regarded as a special type arising from the more general Pfaffian variational principle

$$
\delta \int _ { t _ { 0 } } ^ { t _ { 1 } } \left( \sum _ { j = 1 } ^ { n } P _ { j } \dot { p } _ { j } + Q \right) d t = 0 ,
$$

in which the integral is linear in allof the frst derivatives with arbitrary functions $\ P _ { 1 } , . . . , P _ { n } ,$ $Q$ of the variables $\pmb { / _ { 1 } , \ldots , \pmb { / _ { n } } }$ as coefficients, and $\pmb { n }$ is even. In fact, the Hamiltonian equations are derived from the variational problem

$$
\delta \int _ { t _ { 0 } } ^ { t _ { 1 } } \left( \sum _ { j = 1 } ^ { n } \dot { p } _ { j } \dot { q } _ { j } - H \right) d t = 0 ,
$$

or

$$
\delta \int _ { t _ { 0 } } ^ { t _ { 1 } } { \cal L } d t = 0 , \qquad { \cal L } = { \cal L } _ { 0 } + { \cal L } _ { 1 } + { \cal L } _ { 2 } ,
$$

$$
H = - L + \sum _ { j = 1 } ^ { n } { / } { \hat { q } } _ { j } { \hat { q } } _ { j } ,
$$

where

$$
\frac { d q _ { i } } { d t } = \frac { \partial H } { \partial \dot { p } _ { i } } , \frac { d \dot { p } _ { i } } { d t } = - \frac { \partial H } { \partial q _ { i } } ( i = 1 , 2 , . . . , n ) .
$$

If we develop this general Pfaffan variational principle explicitly, then we obtain

$$
\sum _ { j = 1 } ^ { n } \left( { \frac { \partial P _ { i } } { \partial { \dot { p } } _ { j } } } - { \frac { \partial P _ { j } } { \partial p _ { i } } } \right) { \frac { d p _ { j } } { d t } } - { \frac { \partial Q } { \partial p _ { i } } } = 0 \qquad ( i = 1 , 2 , . . . , n ) .
$$

These equations are those of a degenerate Lagrangian problem with $\begin{array} { r } { L _ { 2 } = 0 , L _ { 1 } = \sum _ { j } P _ { j } \dot { p } _ { j } , L _ { 0 } = Q } \end{array}$ , so that there exists a particular integral $Q =$ constant, which reduces to the energy integral in the Hamiltonian case.

The conservative Hamiltonian principle

$$
\delta \int \left( \sum _ { i = 1 } ^ { n } \dot { p _ { i } } \frac { d q _ { i } } { d t } - H ( \dot { p } _ { 1 } , . . . , \dot { p } _ { n } , q _ { 1 } , . . . , q _ { n } ) \right) d t = 0
$$

has been generalized by Birkhoff (1928) to the Pfaffian principle

$$
\delta \int \bigg ( \sum _ { i = 1 } ^ { 2 n } R _ { i } d x _ { i } - H d t \bigg ) = 0 ,
$$

or

$$
\sum _ { j = 1 } ^ { 2 n } \left( { \frac { \partial R _ { i } } { \partial x _ { j } } } - { \frac { \partial R _ { j } } { \partial x _ { i } } } \right) { \frac { d x _ { j } } { d t } } = { \frac { \partial H } { \partial x _ { i } } } \qquad ( i = 1 , 2 , \ldots , 2 n ) ,
$$

where

$$
H = H ( x _ { 1 } , . . . , x _ { 2 n } ) , \qquad R _ { i } = R _ { i } ( x _ { 1 } , . . . , x _ { 2 n } )
$$

are $2 n + 1$ given functions of $2 n$ coordinates, and

$$
\operatorname* { d e t } \left| \frac { \partial R _ { i } } { \partial x _ { j } } - \frac { \partial R _ { j } } { \partial x _ { i } } \right| \neq 0 .
$$

Equations (97),which admit the integral $H =$ constant,are invariant for any coordinate transformation.Most of the essential properties of the Hamiltonian system hold also for the Pfaffian cases.

The variational equations (Section 4.2O） belonging to the given periodic solution $x _ { i } = x _ { i } ( t ) ~ ( i = 1 , 2 , . ~ . ~ . , 2 n )$ ，

$$
\begin{array} { r l } & { \displaystyle \sum _ { j = 1 } ^ { 2 n } \left( \frac { \partial R _ { i } } { \partial x _ { j } } - \frac { \partial R _ { j } } { \partial x _ { i } } \right) \frac { d \xi _ { i } } { d t } + \displaystyle \sum _ { j = 1 } ^ { 2 n } \sum _ { l = 1 } ^ { 2 n } \left( \frac { \partial ^ { 2 } R _ { i } } { \partial x _ { l } \partial x _ { j } } - \frac { \partial ^ { 2 } R _ { j } } { \partial x _ { l } \partial x _ { i } } \right) \xi _ { l } \frac { d x _ { j } } { d t } } \\ & { \quad \quad \quad \quad \quad = \displaystyle \sum _ { j = 1 } ^ { 2 n } \frac { \partial ^ { 2 } H } { \partial x _ { j } \partial x _ { i } } \xi _ { j } \qquad ( i = 1 , 2 , . . . , 2 n ) } \end{array}
$$

can be written

$$
\frac { d \xi _ { i } } { d t } = \sum _ { j = 1 } ^ { 2 n } a _ { i j } ( t ) \xi _ { j } \qquad ( j = 1 , 2 , . . . , 2 n ) .
$$

Interchange $_ i$ and $j ;$ and write $\eta$ in place of $\xi$ in (98); then,

$$
\begin{array} { r l } & { \displaystyle \sum _ { i = 1 } ^ { 2 n } \left( \frac { \partial R _ { i } } { \partial x _ { j } } - \frac { \partial R _ { j } } { \partial x _ { i } } \right) \frac { d \eta _ { i } } { d t } + \displaystyle \sum _ { i = 1 } ^ { 2 n } \sum _ { l = 1 } ^ { 2 n } \left( \frac { \partial ^ { 2 } R _ { i } } { \partial x _ { l } \partial x _ { j } } - \frac { \partial ^ { 2 } R _ { j } } { \partial x _ { l } \partial x _ { i } } \right) \eta _ { l } \frac { d x _ { i } } { d t } } \\ & { \quad \quad \quad \quad = \displaystyle - \sum _ { i = 1 } ^ { 2 n } \frac { \partial ^ { 2 } H } { \partial x _ { i } \partial x _ { j } } \eta _ { i } \qquad ( j = 1 , 2 , . . . , 2 n ) . } \end{array}
$$

Furthermore, the expression

$$
\sum _ { i = 1 } ^ { 2 n } \sum _ { j = 1 } ^ { 2 n } \sum _ { l = 1 } ^ { 2 n } \bigg ( \frac { \partial ^ { 2 } R _ { i } } { \partial x _ { l } \partial x _ { j } } - \frac { \partial ^ { 2 } R _ { j } } { \partial x _ { l } \partial x _ { i } } \bigg ) \bigg ( \xi _ { j } \eta _ { i } \frac { d x _ { l } } { d t } - \xi _ { l } \eta _ { i } \frac { d x _ { j } } { d t } - \xi _ { j } \eta _ { l } \frac { d x _ { i } } { d t } \bigg )
$$

is the sum of six trilinear forms, three of which are the negatives of the three others.Multiplying (98） by $\pmb { \eta _ { i } }$ and (100） by $\xi _ { j } .$ ，we obtain $_ { 4 n }$ relations.Adding these, we obtain

$$
\begin{array} { r l r } {  { \sum _ { i = 1 } ^ { 2 n } \sum _ { j = 1 } ^ { 2 n } \Big ( \frac { \partial R _ { i } } { \partial x _ { j } } - \frac { \partial R _ { j } } { \partial x _ { i } } \Big ) \Big ( \frac { d \xi _ { j } } { d t } \eta _ { i } + \frac { d \eta _ { i } } { d t } \xi _ { j } \Big ) } } \\ & { } & { + \sum _ { i = 1 } ^ { 2 n } \sum _ { j = 1 } ^ { 2 n } \sum _ { l = 1 } ^ { 2 n } \Big ( \frac { \partial ^ { 2 } R _ { i } } { \partial x _ { l } \partial x _ { j } } - \frac { \partial ^ { 2 } R _ { j } } { \partial x _ { l } \partial x _ { i } } \Big ) \Big ( \xi _ { l } \eta _ { i } \frac { d x _ { j } } { d t } + \xi _ { j } \eta _ { l } \frac { d x _ { i } } { d t } \Big ) = 0 . } \end{array}
$$

The sum of the two vanishing expressions (l01) and (102) is the time derivative of the bilinear covariant

$$
\sum _ { i = 1 } ^ { 2 n } \sum _ { j = 1 } ^ { 2 n } s _ { i j } ( t ) \xi _ { j } ( t ) \eta _ { i } ( t ) ,
$$

where

$$
{ s } _ { i j } ( t ) = \frac { \partial { R } _ { i } } { \partial { x } _ { j } } - \frac { \partial { R } _ { j } } { \partial { x } _ { i } } ,
$$

which is associated with the Pfaffian

$$
\sum _ { i = 1 } ^ { 2 n } R _ { i } d x _ { i } - H d t .
$$

The value of this bilinear covariant (Section 1.7) is independent of $t$ for two arbitrary solutions $\xi , \eta$ of the variational equations belonging to the solution $x _ { i } = x _ { i } ( t ) ~ ( i = 1 , 2 , . . . , 2 n )$

It follows that the characteristic equation associated with (98) or (99) is always reciprocal. This is Wintner's (l93l） generalized form of Birkhoff's theorem. The skew-symmetric nonsingular bilinear form (103)

is the Pfaffian generalization of Poincaré's bilinear differential invariant corresponding to the Hamiltonian-type integral invariant

$$
\iint \sum _ { i = 1 } ^ { n } \delta p _ { i } \delta q _ { i } .
$$

The integral invariants of the Pfaffian systems have been studied by Feraud (1929).

The property of polar factorization can be extended to such Pfaffian dynamical systems.In the linear conservative case this has the form

$$
{ \dot { y } } = S ^ { - 1 } F y ,
$$

where $F = F ^ { \prime }$ and $s$ is nonsingular and equal to $- S ^ { \prime }$ but not necessarily equal to $G$ ， so that (lO4) is more general than the equations

$$
\dot { x } = - G H x .
$$

It can be proved that these Pfaffian systems may be reduced to the Hamiltonian. If $y = T x ,$ where $_ { T }$ is a nonsingular matrix, then (104) is equivalent to

$$
\dot { x } = ( T ^ { \prime } S T ) ^ { - 1 } ( T ^ { \prime } F T ) x .
$$

Now $H = T ^ { \prime } F T = H ^ { \prime }$ since $F = F ^ { \prime }$ .If $S = - S ^ { \prime }$ and det $\boldsymbol { s } \neq 0$ ，then there exists a nonsingular matrix $_ { T }$ such that $T ^ { \prime } S T = G = - G ^ { - 1 }$ (Weyl, 1931). Using this matrix $_ { T }$ in $y = T x ,$ ,we obtain (104a).

The definition of $G$ gives, for any $H = H ^ { \prime }$ and any integer $m \geq 0$

$$
G ( - G H ) ^ { m } G ^ { - 1 } = - G G ( - H G ) ^ { m - 1 } H G ^ { - 1 } = ( - 1 ) ^ { m } ( H G ) ^ { m }
$$

and

$$
[ ( G H ) ^ { m } ] ^ { \prime } = ( H ^ { \prime } G ^ { \prime } ) ^ { m } = ( - 1 ) ^ { m } ( H G ) ^ { m } .
$$

Hence,

$$
G ( - G H ) ^ { m } G ^ { - 1 } = [ ( G H ) ^ { m } ] ^ { \prime } .
$$

Consequently

$$
G [ \exp { ( - G H ) } ] G ^ { - 1 } = [ \exp { ( G H ) } ] ^ { \prime } ,
$$

where exp $\pmb { A }$ is defined for any $\pmb { A }$ by means of the exponential series. Since exp $( - A ) = ( \exp A ) ^ { - 1 }$ , it is clear that $C ^ { \prime } G C = s G _ { \cdot }$

$$
| { \bf d e t } C | = | s | ^ { m } > 0 ,
$$

is satisfied by $C = \exp { ( G H ) } , s = 1$ Hence, exp $( G H )$ is for any $H = H ^ { \prime }$ a Hamiltonian matrix of determinant $+ 1$ (Williamson, 1939, 1940).

On the other hand,any solution ${ \boldsymbol x } = { \boldsymbol x } ( t )$ of $\dot { \boldsymbol { x } } = B \boldsymbol { x } ,$ where $\boldsymbol { B }$ is independent of $t$ ,is related to the vector ${ \pmb x } ( 0 )$ of the initial constant by the linear transformation

$$
\begin{array} { r } { { \boldsymbol x } ( t ) = L ( t ) { \boldsymbol x } ( 0 ) , } \end{array}
$$

where $L ( t )$ denotes the matrix exp $( B t )$ . If we apply this to (104a), with $H = H ^ { \prime }$ and $B = - G H ,$ then—since $- t H$ is,like $H ,$ real and symmetric for $- \infty < t < + \infty$ - we obtain this result: the cyclic transformation group $L ( t )$ generated by the infinitesimal transformation (104a) consists of Hamiltonian matrices and det $L ( t ) = + 1$ for any finite $t$ (see Section 4.26 et seq. on the Lie groups).

Consider such interesting subgroups of $C$ as

$$
C _ { 1 } = { \binom { \alpha } { \omega } } , \quad \omega \biggr ) , \qquad C _ { 2 } = { \binom { \omega } { \gamma } } \quad \beta \biggr ) .
$$

We obtain the usual condition of contragradiency for $C _ { 1 }$

$$
\alpha ^ { \prime } = s \delta ^ { - 1 } , \qquad s \neq 0 ;
$$

and for $C _ { 2 }$

$$
\beta ^ { \prime } = - s \gamma ^ { - 1 } , \qquad s \neq 0 ,
$$

as the necessary and sufficient condition for the Hamiltonian character of the matrices (Wintner,1934). (See Section 4.14).

# REFERENCES

Ahrens, W. 1895Ueber eine Gattung n-fach periodischer Functionen von n reellen Veranderlichen,Dissertation,otock.   
Appell, P. 1892 J.reine angew. Math., 110, 37.   
Autonne, L. 1902 Rendiconti Cir. Mat.Palermo,16, 104.   
Badaljan, G. 1934 Russian Astron.J.,11,376.   
Bianchi, L. 1899 Vorlesungen iber Diferentialgeometrie, Teubner,Leipzig. 1922 Lezioni di Geometria Diferenziale,I,Spoerri, Pisa. 1923 Ibid.,II, Part 1. 1924 Ibid.,II, Part 2.   
Birkhoff, G. D. 1917 Trans. Am. Math. Soc., 18,199. 1927 Am.J. Math., 49,1. 1928 Dynamical Systems,Am. Math. Soc., New York.   
Bohr, N. 1923 Z. Physik, 13, 117. 1924 Proc.Cambridge Phil. Soc.,22, Supplement.   
Boltzmann, L. 1922 Vorlesungen iber die Prinzipien der Mechanik,I,I, 2nded., Barth, Leipzig.   
Bolza, O. 1909 Vorlesungen iber Variationsrechnung, Teubner, Leipzig.   
Brillouin, L. 1926 Compt. Rend.,183, 24. 1926aJ. de Phys.,7, 353. 1935Quantenstatistik. Springer, Berlin.   
Caratheodory, C. 1930 Rendiconti Accad.Lincei, ser.6,12, 353. 1935 Variationsrechnung, Teubner,Leipig. 1937 Geometrische Optik,Springer,Berlin.   
Cartan, E. 1922 Lecons sur les Invariants Intégraux,Hermann, Paris. 1934 LesEspaces de Finsler,Gauthier-Villars,Paris. 1937 Théorie des Groupes Finis et Continus,Gauthier-Villars,Paris.   
Charlier, C. V. L. 1906 Mechanik des Himmels, I,II, W.de Gruyter,Leipzig. Second rev. ed.published as Die Mechanik des Himmels fur Vorlesung,I,II, W. de Gruyter,Leipzig, 1927. 1906a Arkiv Mat. Astron.Fys., 2, no. 29.   
Corson, E. M. 1951 Perturbation Methods in the Quantum Mechanics of n-Electron Systems, Blackie, London.   
Dall'Aqua, F. A. 1908 Math. Annalen,66, 394. 1912Rendiconti Cir.Mat. Palermo,33,341.   
Darboux, G. 1890Theorie des Surfaces,III,Book 6,Chap.V,Gauthier-Villars,Paris.   
Datzeff, A. 1936Compt.Rend.203,300.   
de Donder, T. 1901 Rendiconti Cir.Mat.Palermo,15,65. 1902 Ibid.,16,155. 1930Theorie Invariantive du Calcul des Variations,Gauthier-Villars, Paris.   
Dirac, P. A. M. 1925 Proc.Roy.Soc.London,ser.A,109,642.   
Dziobek, O. 1900 Astron.Nachr.,152, 33.   
Eddington, A. S. 1923The Mathematical Theory of Relativity,Cambridge University Press, London.   
Einstein, A. 1917 Sitzber. preuss. Akad. Wiss. Berlin 1917, 606.   
Eisenhardt, L.P. 1934 Annals of Math.,35,284; Phys. Rev., 45, 427. 1948 Phys.Rev., 74, 87. 1949 Proc.Natl. Acad. Sci.U.S.,35,412,490.   
Epstein, P. 1916Ann. Physik,50,489; 51,168. 1919 Ibid.,58,553. 1922 Z. Physik.8,211,305; 9,92.   
Feraud, L. 1929Trans. Am. Math. Soc., 32,817.   
Forsyth, A. R. 1927" Calculus of Variations, Cambridge University Press,London.   
Fues, E. 1925 Z.Physik,34,788.   
Gleich, G. v. 1926 Z.Physik,35,7.   
Goursat, E. 1908 J. Math. pures appl., ser.6, 4, 333. 1915 Ibid.,ser.6, 11,241. 4922 Compt.Rend.,174,1089. 1923Probleme de Pfaff,Gauthier-Villars,Paris.   
Hadamard, J. 1910"Calcul des Variations, Hermann, Paris.   
Hamel, G. 1924 Math. Annalen, 92,33.   
Hertz, H. von, 1910 Prinzipien der Mechanik in neuen Zusammenhang dargestellt, Barth, Leipzig.   
Herzberger, M. 1931"Strahlenoptik, Springer,Berlin.   
Heun, K. 1901Arch. Math.Phys.,ser.3,2,57, 298.   
Hilbert, D. 1906 Math. Annalen, 62,351.   
Hiltebeitel, A. M. 1911 Am.J. Math.,33, 337.   
Jacobi, C. G. J. 1884Vorlesungen üiber Dynamik, Reimer, Berlin.   
Kemble, E. C. 1937 The Fundamental Principles of Quantum Mechanics, McGraw-Hill, New York.   
Kneser, A. 1894 Math. Annalen, 45,446. 1925 Lehrbuch der Variationsrechnung, Vieweg, Brunswick.   
Kneser, H. 1921 Math. Annalen, 84, 277.   
Koschmieder, G. 1933Variationsrechnung, Gruyter, Berlin.   
Kramers, H. A. 1920 Z.Physik,3,199.   
Lehmann-Filhes,R. 1904Astron. Nachr.,165,209.   
Levi-Civita, T. 1896 Annali Mat. pura appl., ser.2,24, 255. 1904 Math.Annalen,59,383. 1923 Derabsolute Differentialkalkul,Springer,Berlin. 1925 Lezioni di Calcolo Differenziale Assoluto,Zanichelli,Bologna. 1936 Recueil Math.,1, 123.   
Levi-Civita, T., and Amaldi, U. 1927Lezioni di Meccanica Razionale, II, Part 2, 318. Zanichelli, Bologna.   
Lewis, D. C. 1937 Bull. Am.Math. Soc.,43, 277.   
Lie, S. 1922 Gesammelte Werke, III, 295, Teubner, Leipzig.   
Liouville, R. 1895 Acta Math.,19,251   
Miyahara, S. 1954Pub. Astron. Soc. Japan, 6,9.   
Morera, G. 1881 Atti Accad. Torino,16, 276.   
Murnaghan, F. D. 1931 Proc. Natl. Acad. Sci.U.S.,17, 128.   
Painleve, P. 1894 J. Math. pures appl., ser. 4, 10,5. 1897 Compt.Rend.,124,221.   
Poincaré, H. 1899Acta Math.,13,1.Reprinted in Oeuvres de Henri Poincaré, VII,286. Gauthier-Villars,Paris,1952. 1893Méthodes Nouvellesdela MécaniqueCeleste,II,Gauthier-Villars,Paris.   
Prange, G. 1935 Die allgemeine Integrationsmethode der analytischen Mechanik, Teubner, Leipzig.   
Rao, B. S.Medhava. 1935 Math. Annalen,111,459. 1935a Z.Physik,93,320.   
Reed, F. W. 1918 Am. J. Math., 40, 97.   
Ricci, A., and Levi-Civita, T. 1900 Math.Annalen,54,125.   
Robertson, H. P. 1928 Math. Annalen, 98, 749.   
Schouten, J. A., and v. d. Kulk, W. 1949Pfaff's Problem and its Generalizations, Clarendon,Oxford.   
Schouten, J.A. 1929Math.Z.,30,149.   
Schwarzschild, K. 1916 Sitzber. preuss. Akad.Wiss. Berlin,1916, 548.   
Sommerfeld, A. 1916 Ann.Physik,51,1,125. 1924 Atombau und Spektralinen, Vieweg, Brunswick.   
Stäckel, P. 1891J.reine angew. Math., 107, 319. 1905Ibid.,128,222.   
Synge, J. L. 1926Phil. Trans.Roy. Soc.London,ser.A,226,31. 1960Ecodiaics,ssicalcsgli   
Taylor, A. E. 1935 Bull. Am. Math. Soc.,41, 735. 1937J. Math. pures appl., ser.9, 16,15.   
Titchmarsh, E. C. 1955 Proc.London Math. Soc.,ser.3,5, 1. 1958 Eigenfunction Expansions Associated with Second Order Differential Equations, Part I1, Clarendon, Oxford.   
Tonelli, L. 1921Fondamenti di Calcolo di Variazioni,I,Zanichelli, Bologna. 1923 Ibid., I1.   
Turnbull, W. H., and Aitken, A. C. 1932 An Introduction to the theory of Canonical Matrices, Blackie, London.   
Ura, T. 1946 Proc.Imp.Acad. Japan,22,1. 1951 Bull.Astron., ser.2, 16,333.   
Van Kampen, E. R. and Wintner, A. 1936 Am.J.Math.,58,851. 1938 Trans. Am.Math. Soc.,44,168.   
Vranceanu, G. 1929 Annali Mat. pura appl., ser. 4, 6,9. 1935 Les Espaces Non-Holonomes,Gauthier-Villars, Paris. 1961 Vorlesungen iber Differentialgeometrie,II,Akad.Verlag,Berlin.   
Vrkljan, V. S. 1929 Z. Physik,59,718. 1930Ibid.,65,280.   
Weber, H. v. 1900Vorlesungen iber das Pfafsche Problem, Teubner, Leipzig.   
Wedderburn, J. H. M. 1934 Lectures on Matrices，Amer. Math.Soc. Colloq. Publ.，New York.   
Weierstrass, K. 1894Gesammelte Werke, I, 257, Mayer u. Muller, Berlin.   
Weinacht, J. 1928 Math. Annalen, 91,279.   
Weitzenbock, R. 1923Invariantentheorie, Noordhoff, Groningen.   
Weyl, H. 1923 Mathematische Analyse des Raumproblems,Springer,Berlin. 1931 Gruppentheorie und Quantenmechanik,2nd edition, Hirzel, eiig. 1939 The Classical Groups,Princeton University Press,Princeton, N.J.   
Williamson, J. 1935 Am.J.Math., 57,475. 1936 Ibid., 58, 141. 1936a Bull.Am.Math.Soc.,41,118. 1937 Am.J.Math.,59,599. 1939 Bull.Am.Math.Soc.,45, 920. 1940 Am. J. Math., 62, 881.   
Wintner, A. 1929 Spektraltheorie der unendlichen atrizen,S. Hirzel,eiig. 1930 Berichte Math.-Phys.Kl. sachs.Akad.Wiss, 82,3. 1931 Am.J.Math., 53,623. 1934 Annali Mat. pura appl., ser.4,13,104. 1936 Quart.J. Math.,7, 214. 1937 Proc.Edinburgh Math. Soc.,ser.2,5, 90. 1938 Am.J. Math.,60,463. 1947 The Analytical Foundation ofCelestial Mechanics,Princeton University Press,Princeton, N.J.   
Yano, K. 1945 Proc. Imp. Acad.Japan,21, 171,248, 261. 1949 J.Faculty Sci. Univ. Tokyo, 6, 1.

# Quasi-Periodic Motion

# SIMPLY PERIODIC MOTION

# 2.1LEBESGUE'S INTEGRALS

A collection of definite elements related to their totality by a certain law is called a set.Any two sets that can be made to correspond to each other are equivalent.A set of natural numbers is said to have the power of denumerable numbers.A set of rational numbers and a set of algebraic numbers determined as the roots of an algebraic equation with rational coefficients have the power of denumerable numbers.By representing such a number as the coordinate of a point on the straight line interval between O and l,we can find a number belonging to this set in any small but finite segment in this interval. Such being the case, the rational numbers and the algebraic numbers are said to be euerywhere dense in the interval. But the set of all points in the interval has the power of the continuum, because such a set cannot be made to correspond to any set of rational or algebraic numbers.

Describe a circle with a small but finite radius $\pmb { r }$ around a point $\pmb { P }$ The region inside this circle is called the $\pmb { r }$ -neighborhood.If there is no point belonging to the set $M$ in the neighborhood of $P _ { i }$ ，then $P$ is said to be isolated.If there are infinitely many points of the set $M$ other than $P$ in the $\pmb { r }$ -neighborhoodof $P$ then $P$ is called an accumulation point or a limit point of the set $M .$ A set consisting of accumulation points is called a derived set.A set containing its derived set is said to be closed.A set coinciding with its derived set is called a perfect set.A perfect set cannot have the power of denumerable numbers but has the power of the continuum according to Cantor.

A sum set (union, Vereinigungsmenge) of the sets $M _ { 1 } , M _ { 2 } , . . . , M _ { k }$ is a set $M$ containing all points that are in any of these $k$ sets.A product set of the sets $M _ { 1 } , M _ { 2 } , . . . , M _ { k }$ (intersection,Durchschnit） is a set of points that are common to all these $k$ sets.A set that is a part of a set is called a subset. A subset $M _ { 1 }$ of $M$ (written $M _ { 1 } \subset M$ ） consisting of all points not contained in a subset $M _ { 2 }$ of $M$ is called the complement of $M _ { 2 }$ in $M$ when $M _ { 1 } + M _ { 2 } = M .$ A complementary set of a closed set is an open set.

If there is a number larger or smaller than any member of the set $M ,$ then the number is called the upper bound or the lower bound of $M .$ If the upper and the lower bound both exist, then the set $M$ is said to be bounded.There should be at least one accumulation point for any bounded set consisting of an infinite number of elements (Bolzano-Weierstrass theorem). If there is only one accumulation point in a bounded set, then the set is said to be convergent and the accumulation point is called the limit. The largest and smallest accumulation points are denoted respectively by ${ \mathfrak { s } } \mathfrak { T } \overline { { \operatorname { i m } } } M ^ { \prime } $ or“ lim sup $M ^ { \prime \prime }$ and“lim $M ^ { \prime \prime }$ or“ lim inf M," and are called the superior and inferior limits, respectively. A set consisting of only interior points of a set $M$ is said to be open.A bounded closed set can be covered by a finite number of open sets.This is Heine-Borel’s covering theorem.

If all subsets consisting of an infinite number of elements selected from a set $M$ possess accumulation points, then the set $M$ is called compact. A number $\| { x , y } \|$ can be made to correspond to a pair of two points $_ { x }$ and $y$ ，such that $\left\| x , x \right\| = 0 , \left\| x , y \right\| = \left\| y , x \right\| > 0$ for $x \neq y$ ,and

$$
\| x , y \| + \| y , z \| \geq \| x , z \| .
$$

This number is called the distance of the two points,and such a set is said to be metrical. If the set of points $( x _ { 1 } , x _ { 2 } , \ldots , x _ { n } )$ is metrical, then the set constitutes a metrical space.When

$$
\left\| x , y \right\| = [ ( x _ { 1 } - y _ { 1 } ) ^ { 2 } + ( x _ { 2 } - y _ { 2 } ) ^ { 2 } + \cdot \cdot \cdot + ( x _ { n } - y _ { n } ) ^ { 2 } ] ^ { 1 / 2 } ,
$$

the space is called Euclidean. The necessary and sufficient condition for a set $M$ of a Euclidean space $R _ { n }$ to be compact is that a finite number $\pmb { A }$ can be found such that $\left| x _ { i } \right| < A$ $( i = 1 , 2 , . . . , n )$ ，for any point $( x _ { 1 } , x _ { 2 } , \ldots , x _ { n } )$ of $M .$

If every point of a set is itself an accumulation point, then the set is called dense in itself. A perfect set is dense in itself.A set $M$ is nowhere dense in a set $N$ if $M$ is not dense in any subset of $N .$ If a set $M$ cannot be represented by a sum of its closed subsets, then $M$ is called connected. A sum of connected sets that are nonoverlapping-i.e.，any two of which have no common point — is connected.Any two points $\scriptstyle { a ^ { \prime } }$ and $ { \boldsymbol { a } } ^ { \prime \prime }$ of a connected set $M$ can be connected by a finite number of sequences $a ^ { \prime } = a _ { 0 } , a _ { 1 } , . . . , a _ { l - 1 } , a _ { l } = a ^ { \prime \prime }$ ,belonging to the set $M$ such that

$$
\begin{array} { r } { \left\| a _ { i - 1 } , a _ { i } \right\| \le \rho \qquad ( i = 1 , 2 , . . . , l ) . } \end{array}
$$

A closed connected set is called a continuum.A connected set is dense in itself. A continuum is a perfect set.

If there is in $M$ a denumerably infinite number of subsets that are dense in themselves,then $M$ is said to be separable.A compact set is separable.Any set of a Euclidean space is separable.If there exists an $\pmb { n }$ for a given $\epsilon$ such that $\| a _ { n ^ { \prime } } , a _ { n ^ { \prime \prime } } \| < \epsilon$ for $n ^ { \prime } \geq n$ $n { ' } \stackrel { } { = } n$ ，then the sequence $\left\{ a _ { n } \right\}$ is said to be a Cauchy sequence. If a Cauchy sequence from a set $M$ possesses an accumulation point belonging to $M _ { : }$ ，then it is called complete.A complete set is closed and a compact closed set is complete. A closed subset of a Euclidean space is complete.

Let $\pmb { A }$ be a system of sets $M$ in an $\pmb { n }$ -dimensional metrical space $R _ { n }$ Let a function $\phi ( M )$ be such that: (i) $\phi ( M ) \geq 0$ for all $M$ belonging to $A$ ； (ii) $\phi ( M ) = 0$ for any empty set, i.e., for any set containing no element; (iii) $\phi ( M ) \cong \phi ( M _ { 1 } )$ for a subset $M _ { 1 }$ of $M$ that is, for $M _ { 1 } \subset M$ ； and (iv) $\phi ( { \cal M } ) \le \phi ( { \cal M } _ { 1 } ) + \phi ( { \cal M } _ { 2 } ) + \cdot \cdot \cdot$ for $M _ { 1 } + M _ { 2 } + \cdot \cdot \cdot = M ,$ where $M _ { 1 } , M _ { 2 } , .$ are a denumerably infinite number of sets in the system $\pmb { A }$ ，Then the function $\phi ( M )$ is called a measure function. Let $\begin{array} { r } { m ( J ) = \prod _ { k = 1 } ^ { n } \left( b _ { k } - a _ { k } \right) } \end{array}$ be the measure function of an open domain $J$ of an $\pmb { n }$ -dimensional space $a _ { k } < x _ { k } < b _ { k }$ $( k = 1 , 2 , . . . , n )$ . Let an arbitrary set $M$ in the metrical space $R _ { n }$ be covered by at most a denumerable number of open sets, by various methods. The sum set of such open sets contains $M .$ The sum of the measure functions of such open sets may differ according to the method of covering. Call the lower bound of these sums the outer measure; that is, the lower bound of the outer measures of all open sets containing $M$ is called the outer measure of $M$ and denoted by $m _ { a } ( M )$ . Let a set $M$ be covered by an open set $o$ The difference between the outer measure of $O$ and the outer measure of the complementary set of $M , O \mathrm { ~ - ~ } M ,$ is called the inner measure $m _ { i } ( M )$ ; that is, $m _ { a } ( O ) - m _ { a } ( O - M ) = m _ { i } ( M )$ . In general: $m _ { i } ( M ) \ \leqq m _ { a } ( M )$ .If $M _ { 1 } \subset M ,$ then $m _ { i } ( M _ { 1 } ) \ \leq m _ { i } ( M )$ .A set $M$ such that

$$
m _ { i } ( M ) = m _ { a } ( M ) = m ( M )
$$

is called measurable according to Lebesgue.

Any set constructed by a finite or a denumerable number of operations such as addition,subtraction,multiplication - finite and infinite一 from measurable sets is measurable.Such sets are called Borel sets.The operations just mentioned constitute a field called the Borel feld.

Let $M _ { 1 } + M _ { 2 }$ and $M _ { 1 } M _ { 2 }$ be respectively the sum set and the product set of two sets $M _ { 1 }$ and $M _ { 2 }$ . We have

$$
\begin{array} { r } { m _ { a } ( M _ { 1 } + M _ { 2 } ) + m _ { a } ( M _ { 1 } M _ { 2 } ) \leq m _ { a } ( M _ { 1 } ) + m _ { a } ( M _ { 2 } ) , } \\ { m _ { i } ( M _ { 1 } + M _ { 2 } ) + m _ { i } ( M _ { 1 } M _ { 2 } ) \geq m _ { i } ( M _ { 1 } ) + m _ { i } ( M _ { 2 } ) . } \end{array}
$$

If $M _ { 1 }$ and $M _ { 2 }$ are both measurable, that is, if

$$
m _ { i } ( M _ { 1 } ) = m _ { a } ( M _ { 1 } ) = m ( M _ { 1 } ) , ~ m _ { i } ( M _ { 2 } ) = m _ { a } ( M _ { 2 } ) = m ( M _ { 2 } ) ,
$$

then

$$
m _ { i } ( M _ { 1 } + M _ { 2 } ) = m _ { a } ( M _ { 1 } + M _ { 2 } ) , \qquad m _ { i } ( M _ { 1 } M _ { 2 } ) = m _ { a } ( M _ { 1 } M _ { 2 } ) ,
$$

and the sum set and the product set are measurable. We have

$$
m ( M _ { 1 } + M _ { 2 } ) + m ( M _ { 1 } M _ { 2 } ) = m ( M _ { 1 } ) + m ( M _ { 2 } ) .
$$

In particular, if $M _ { 1 }$ and $M _ { 2 }$ are nonoverlapping,i.e., possess no common point, then

$$
m ( M _ { 1 } + M _ { 2 } ) = m ( M _ { 1 } ) + m ( M _ { 2 } ) , m ( M _ { 1 } M _ { 2 } ) = 0 .
$$

The sum set and the product set of a denumerably infinite number of measurable sets are both measurable.A measure cannot be negative. The measure of an empty set is zero. The measure of the set containing all points between O and 1 is one. The measure of the set containing a denumerably infinite number of points, such as rational numbers or algebraic numbers, is zero.

For a point $^ { a }$ of $M _ { : }$ i.e., $a \in M ,$ ，the set of values of $f ( a )$ is called a function $f .$ For various sequences $\left\{ a _ { n } \right\}$ converging to a point $_ { a }$ of $M$ ,the point function lim $\scriptstyle \operatorname* { s u p } _ { n \to \infty } f ( a _ { n } )$ is denoted by $G ( a ; f , M )$ ,and

$$
\operatorname* { l i m } _ { n \to \infty } { \mathrm { i n f } } f ( a _ { n } )
$$

by $g ( a ; f , M )$ .If $G ( a ; f , M ) = g ( a ; f , M )$ , then $f$ is said to be continuous at $a \in M .$ A function that is continuous at all points of a set $M$ - that is, a function for which $G ( f , M ) = g ( f , M )$ —is said to be continuous in $M .$ If there exists a $\rho > 0$ for a given $\epsilon > 0$ such that $| f ( a ^ { \prime } ) - f ( a ^ { \prime \prime } ) | < \epsilon$ for any two points $\scriptstyle { a ^ { \prime } }$ and $\pmb { a } ^ { \prime \prime }$ of $M$ whose distance apart is less than $\rho$ ， then the bounded function $f$ is said to be uniformly continuous in $M$

If $M$ is compact and closed, then any function continuous in $M$ is uniformly continuous.The function

$$
\omega ( a ; f , M ) = G ( a ; f , M ) - g ( a ; f , M )
$$

is called the fluctuation of $f$ in $M .$ In order that $\omega ( a ; f , M ) = 0$ it is necessary and sufficient that $f$ should be continuous in M. Otherwise, $f$ is said to be discontinuous.

If the discontinuity point of $f$ is compact in $M _ { i }$ ，then the finite but discontinuous function $f$ is said to be totally discontinuous; if it is not compact, then it is said to be pointwise discontinuous.A function continuous on a perfect bounded set $M$ is called a function of Baire's class 0.A continuous function represented as the limit function of a sequence $\{ f _ { \mathfrak { n } } \}$ of continuous functions $f _ { n }$ is a function of Baire's class 1. If $f _ { n }$ belongs to Baire's class $\pmb { \alpha }$ but $f = \operatorname* { l i m } _ { n \to \infty } f _ { n }$ does not belong to any Baire's class lower than $\pmb { \alpha }$ ，then $f$ is said to be of Baire's class $\propto$ ，A discontinuous function which is represented as the limit of functions of Baire's class $\pmb { \alpha }$ is said to be of Baire's class $\propto + ~ 1$ .

Let $f ( x )$ be a nonnegative bounded function of $_ x$ Draw a graph with coordinates $_ x$ and $y$ such that $y = f ( x )$ . The set of values of $y$ such that $0 \leq y \leqq f ( x )$ for a measurable $_ n$ dimensional set $M$ of $_ x$ is called an ordinate set and denoted by $\Omega \{ M ; f ( x ) \}$ . Denote the measure of this measurable space of $x , y$ by $\mu$ ; then

$$
\mu _ { a } [ \Omega \{ M ; f ( x ) \} ] = { \overline { { \int _ { M } f ( x ) ~ d x } } } , \qquad \mu _ { i } [ \Omega \{ M ; f ( x ) \} ] = \underbrace { \int _ { M } f ( x ) ~ d x } _ { \qquad }
$$

Call these integrals the upper and lower Lebesgue integrals of $f$ over the set $M .$ If $\Omega \{ M ; f ( x ) \}$ is measurable, then these two integrals coincide. If the measure is finite, then $f ( x )$ is called integrable or summable in Lebesgue's sense. $\mu [ \Omega \{ M ; f ( x ) \} ] = \int _ { M } f ( x ) \ d x$ is called a Lebesgue integral of $f$ on $M .$

When $f ( x )$ is a constant, that is, $f = h > 0$ ，then $\Omega ( M ; h )$ is an open ordinate set represented by a cylinder of height $h$ with base $M .$ The $( n + 1 )$ -dimensional measure of this cylinder is $\mu [ \Omega \{ M ; h \} ] = h { \cdot } m ( M )$ It is equal to zero for $m ( M ) = 0$ For $h = \infty$ and for a measurable $M$ with $m ( M ) > 0$ ，the measure of $\Omega \{ M ; \infty \}$ is measurable with infinite measure. If $m ( M ) = 0 $ then

$$
\mu [ \Omega \{ M ; \infty \} ] = \operatorname * { l i m } _ { k \to \infty } \mu [ \Omega \{ M ; k \} ] = \operatorname * { l i m } _ { k \to \infty } [ k \cdot m ( M ) ] = 0 ;
$$

that is, an ordinate set erected over a set of measure zero is itself of measure zero.

Denote a subset of $M _ { : }$ ， such that $f < \alpha$ ,by $M ( f < \alpha ) = M _ { \alpha }$ . It can be shown that $M _ { \alpha }$ is measurable for all $\alpha \geq 0$ Since $M ( f > 1 )$ ， $M ( f > 2 ) , \ldots$ are measurable, $M ( x = \infty )$ is measurable. If $M ( f = 0 )$ is measurable then $M ( f \geq \alpha )$ is measurable for all nonnegative $\pmb { \alpha }$ .The set $M ( f < \alpha )$ is also measurable. Functions such as $f$ are said to be measurable according to Lebesgue. Thus the measurability of a function can be deduced from the measurability of an ordinate set. Conversely，the existence of the integral $\int _ { M } f ( x ) \ d x$ can be deduced, if the measure is finite, from the measurability of the function $f ( x )$ . Since $m ( M _ { \infty } ) = 0$ (as is now evident),this function $f$ becomes infinite only on sets of measure zero,if $f$ is integrable on $M .$

Let $f ( x )$ be a measurable nonpositive function defined on a set $M$ of $_ { x }$ with finite measure. Construct the mirror image of this function with respect to $y = 0$ .Denote the ordinate set of a nonnegative function $- f$ by $\Omega ^ { + } \{ M ; - f \}$ ,and let its mirror image be $\Omega ^ { - } \{ M ; f \}$ .If $\Omega ^ { - } \{ M ; f \}$ is measurable, then take

$$
\int _ { M } f d x = - \mu [ \Omega ^ { - } \{ M ; f \} ] .
$$

Since

$$
\mu [ \Omega ^ { - } \{ M ; f \} ] = \mu [ \Omega ^ { + } \{ M ; - f \} ] ,
$$

we obtain

$$
\int _ { M } f d x = - \int _ { M } \left( - f \right) d x .
$$

Hence the properties for a nonnegative function can be extended to those for a nonpositive function,and consequently to those for any function with arbitrary sign. Thus it can be shown that $M ( f \lesseqqgtr \ - \alpha )$ ， $M ( f < - \alpha )$ ， $M ( f \gtrapprox - \alpha )$ ， $M ( f > - \alpha )$ are measurable if $f \lessgtr 0$ is integrable on $M .$

Consider a function which can have either sign on a measurable set $M$ with finite measure. Denote $f ^ { + } = \mathrm { m } \mathbf { a x } ( f , 0 ) , f ^ { - } = \mathrm { m i n } ( 0 , f )$ ； then, $f = f ^ { + } + f ^ { - }$ . Put

$$
\Omega ^ { + } \{ f ^ { + } \} = \Omega ^ { + } \{ f \} , \qquad \Omega ^ { - } \{ f ^ { - } \} = \Omega ^ { - } \{ f \} .
$$

$$
\begin{array} { l } { { \displaystyle \mu _ { a } [ \Omega ^ { + } \{ f \} ] - \mu _ { i } [ \Omega ^ { - } \{ f \} ] = \overline { { { \displaystyle \int _ { M } f d x } } } , } } \\ { { \displaystyle \mu _ { i } [ \Omega ^ { + } \{ f \} ] - \mu _ { a } [ \Omega ^ { - } \{ f \} ] = \int _ { M } f d x } } \\ { { \displaystyle \mu _ { i } [ \Omega ^ { + } \{ f \} ] - \mu _ { a } [ \Omega ^ { - } \{ f \} ] = \int _ { M } f d x } } \end{array}
$$

are called the upper and lower integrals of $f ,$ respectively. If these two integrals are equal, then

$$
\int _ { M } f d x = \mu [ \Omega ^ { + } \{ f \} ] - \mu [ \Omega ^ { - } \{ f \} ] = \int _ { M } f ^ { + } \ d x + \int _ { M } f ^ { - } \ d x
$$

is called the integral off.For $f$ to be integrable it is necessary that $f ^ { + }$ and $f ^ { - }$ are both integrable.We say that $f$ is measurable on $M$ if $\Omega ^ { + } \{ f \}$ and $\Omega ^ { - } \{ f \}$ are both measurable.

The sum of a finite number of measurable functions $f _ { 1 } , f _ { 2 } , \ldots , f _ { n }$ on $M ,$ for which the operation $\textstyle \sum _ { t = 1 } ^ { \infty } f _ { i }$ is applicable, is measurable on a set $M ^ { * } \subset M .$ There exists $\operatorname* { l i m } _ { n \to \infty } f _ { n } ,$ which is on a measurable set and is measurable on the set. Suppose that an infinite sequence of finite and integrable functions $f _ { 1 } , f _ { 2 } , \ldots$ on $M$ converges to a finite function $f \circ \mathbf { n } M$ (except sets of measure zero). Let $f _ { 1 } + f _ { 2 } + \cdot \cdot \cdot + f _ { n } = S _ { n }$ ； then the $S _ { n }$ are integrable. If there exists an integrable function $\Psi$ that is the upper bound of the absolute values of all $S _ { n } { \mathrm { : } }$ then $f$ is integrable and

$$
\int _ { M } f d x = \int _ { M } \sum _ { n = 1 } ^ { \infty } f _ { n } d x = \sum _ { n = 1 } ^ { \infty } \int _ { M } f _ { n } d x .
$$

Thus a convergent infinite series of finite integrable functions can be integrated term by term.If, in particular, there is a constant $\pmb { c }$ independent of $_ { n }$ such that $| S _ { n } | < c ;$ ,that is,if the partial sums $S _ { n }$ are uniformly bounded, then the series can be integrated term by term.

Lebesgue defined an auxiliary function $f _ { n }$ for an integer $_ { n }$ corresponding to a nonnegative measurable function $f$ on $M$ by the condition $f _ { n } = f$ for any point $P$ where $f \leqslant n$ ,and $f _ { n } = n$ for any point $P$ where $f > n$ The function $f _ { n }$ is bounded and measurable.We have

$$
\int _ { M } f d P = \operatorname* { l i m } _ { n \to \infty } \int _ { M } f _ { n } d P .
$$

If this limit is finite,then $f ,$ according to Lebesgue, is summable on $M .$ $\operatorname { I f } f$ is nonpositive, a definition of summability can still be made. If $M$ is the sum of a finite or a denumerably infinite number of nonoverlapping sets $M _ { 1 }$ ， $M _ { 2 } , \ldots$ ，then the integral on $M$ is equal to the sum of integrals on $M _ { 1 } , M _ { 2 } , \dots$ Thus the sum of summable functions is summable,and the integral of the sum equals the sum of the integrals of each function. $\operatorname { I f } f$ is summable on a set $M$ and $M ^ { * } \subset M ,$ then the integral offon $M ^ { * }$ tends to zero with the measure of $M ^ { * }$ .If,further, the functions of a sequence $f _ { 1 } , f _ { 2 } , \ldots , f _ { n } , \ldots .$ are summable on a set $M _ { : }$ ，if their absolute values are smaller than a positive summable function $\Phi$ independent of $\pmb { n }$ ,and if the sequence admits a limit function $F _ { : }$ ，then $F$ is summable and

$$
\operatorname* { l i m } _ { n \to \infty } \int _ { M } f _ { n } d P = \int _ { M } F d P .
$$

This theorem is due to Lebesgue.

Let a function $f$ be defined on a measurable set $M$ with finite measure and be integrable. Consider a function $\bar { f }$ which coincides with $f$ on $M$ but is equal to zero outside $M ,$ i.e., on a complementary set of $M .$ A set function on a set such that $F ( U ) = \int _ { U } \bar { f } d x$ with respect to an arbitrary measurable set $U$ in $R _ { n }$ is measurable and is called an indefinite integral of $\bar { f } .$ Let a function $\pmb { F }$ of finite values on a set $\boldsymbol { U }$ with finite measure be such that $F ( U ) = F ( U _ { 1 } ) + F ( U _ { 2 } ) + \cdot \cdot \cdot$ for $U = U _ { 1 } + U _ { 2 } + \cdots$ ,where $U$ is the sum set of denumerably infinite nonoverlapping measurable sets $U _ { 1 } , U _ { 2 } , \dots$ .This property is called the complete additivity of the set function $F ( U )$ . Let ${ \pmb F }$ be such that with a given positive $\epsilon$ there exists a constant δ so that $| F ( U ) | < \epsilon$ for $m ( U ) < \delta$ Then $F$ is said to be totally continuous. An indefinite integral has three properties: $( i )$ , it has a finite value for any finite set of finite measure; $( i i )$ , it/is completely additive; and $( i i i )$ , it is totally continuous. If the inequality

$$
g \cdot m ( U ) \leq \int _ { U } f d x = F ( U ) \leq G \cdot m ( U )
$$

(the mean value theorem)， holds in a set $U$ of an integrable function $f ,$ then

$$
g \leq { \frac { F ( U ) } { m ( U ) } } \leq G .
$$

Let the set function $F ( U )$ possess the above three properties. In a sequence of sets $U _ { 1 }$ $, U _ { 2 } , \ldots$ which converges to $X$ for $n \to \infty$ , we consider a set $K _ { n }$ containing $X$ and one of $U _ { n }$ .If $m ( U _ { n } ) / m ( K _ { n } ) \ \geq \ \alpha$ for a positive $\pmb { \alpha }$ ，and if $\operatorname* { l i m } _ { n \to \infty } \left[ F ( U _ { n } ) / m ( U _ { n } ) \right]$ exists，then the limit is called the derivative of $F ( U )$ at $X ,$ and ${ \pmb F }$ is differentiable at $x .$ Consider at each point $\pmb { X }$ of a set $M$ of finite outer measure an admissble sequence of closed sets $V$ convergent to $X .$ Then finite or denumerably infinite sets $V _ { 1 } , V _ { 2 } , \ldots$ can be chosen from the sequence so that: (i), $V _ { 1 } , V _ { 2 } , \ldots$ are nonoverlapping; (ii), $M$ can be covered (except sets of measure zero) by the sets $V _ { 1 } , V _ { 2 } , \ldots$ so that $\begin{array} { r } { m _ { a } [ M - M ( \sum _ { n } V _ { n } ) ] = 0 } \end{array}$ ; and (iii), for a given $\epsilon > 0$ we have $\begin{array} { r } { \sum _ { n } m ( V _ { n } ) < m _ { a } ( M ) + \epsilon } \end{array}$ This is called Vitali’s covering theorem. On the basis of this theorem Lebesgue has shown that a finite, totally continuous, and completely additive set function ${ \pmb F }$ is almost everywhere differentiable (i.e., except for sets of measure zero) and is equal to an indefinite integral of its derivative.

If a function $\Phi$ can be found such that

$$
\Phi ( J ) = \Phi ( J _ { 1 } ) + \cdot \cdot \cdot + \Phi ( J _ { n } ) \quad \mathrm { f o r } \quad J = J _ { 1 } + J _ { 2 } + \cdot \cdot \cdot + J _ { n } ,
$$

with nonoverlapping sets $\boldsymbol { J } _ { 1 } , \boldsymbol { J } _ { 2 } , \ldots$ . and that with a given $\epsilon > 0$ a constant δ can be chosen such that

$$
\biggr | \sum _ { i = 1 } ^ { n } \Phi ( J _ { i } ) \biggr | < \epsilon \qquad \mathrm { i f } \qquad \sum _ { i = 1 } ^ { n } m ( J _ { i } ) < \delta ,
$$

then $\Phi$ is called an interval function. The displayed properties are called complete additivity and total continuity，respectively.Let $f$ be an integrable function in $a \leq x \leq b$ ，and $a \leq \alpha < \beta \leq b$ be a closed interval $J = [ \alpha , \beta ]$ .Then

$$
\int _ { J } f d x = \int _ { \alpha } ^ { \beta } f d x ,
$$

and

$$
\int _ { \alpha } ^ { \beta } f d x = \int _ { \alpha } ^ { \gamma } f d x + \int _ { \gamma } ^ { \beta } f d x
$$

for $\alpha < \gamma < \beta .$ .The indefinite integral, owing to the relation

$$
\Phi ( J ) = \Phi [ \alpha , \beta ] = \int _ { \alpha } ^ { \beta } f d x ,
$$

is represented by an interval function and can be written

$$
\Phi ( J ) = F ( \beta ) - F ( \alpha ) , F = \int _ { \gamma } ^ { x } f d x .
$$

Consider $a \equiv x _ { 1 } < x _ { 1 } ^ { \prime } < x _ { 2 } < \cdot \cdot \cdot < x _ { n } < x _ { n } ^ { \prime } \equiv b$ contained in the interval ${ \cal J } ^ { o } = [ a , b ]$ . If $\begin{array} { r } { \left| \sum _ { i = 1 } ^ { n } \Phi ( x _ { i } , x _ { i } ^ { \prime } ) \right| } \end{array}$ is bounded, then

$$
\Big | \sum _ { i = 1 } ^ { n } \{ F ( { \pmb x } _ { i } ^ { \prime } ) - F ( { \pmb x } _ { i } ) \} \Big |
$$

is bounded. The function is then said to be of bounded variation on $J ^ { o }$ ·

The integral of a bounded function $f ( { \pmb x } )$ in the interval $[ a , b ]$ depends on the interval. This interval function obeys the mean value theorem relative to $f ( { \pmb x } )$ and has the property of complete additivity. Such an integral is called a Riemann integral, which is an ordinary integral. $\mathbf { I f } f ( { \pmb x } )$ is integrable in the Riemann sense then $f ( x )$ is measurable and the Lebesgue integral of $f$ coincides with the Riemann integral (Vallée Poussin,1916;Hahn,1921; Baire,1925;Hobson,1926; Sierpinski,1928, 1934；Schlesinger and Plessner, 1926； Kestelman, 1937； Saks, 1937; Lebesgue，1930；Halmos，1950；McShane，1947；Thielman，1953; Natanson, 1955, 1960; Whitney, 1957; Bourbaki,1963).

# 2.2FOURIER SERIES

If we can determine $N = N ( \epsilon )$ independent of $_ { x }$ for a given $\epsilon$ such that

$$
| f _ { n + 1 } ( x ) + \cdot \cdot \cdot + f _ { n + k } ( x ) | < \epsilon ,
$$

with an integer $k$ and an integer $n > N$ for all values of $_ { x }$ in the given interval, then the sum $\textstyle \sum _ { n } f _ { n } ( x )$ is said to be uniformly convergent in that interval. If $\textstyle \sum _ { n } f _ { n } ( x )$ is uniformly convergent and $f _ { n } ( x )$ is continuous at $\boldsymbol { x } = \boldsymbol { x } _ { 0 } ,$ ，then $\begin{array} { r } { F ( { \boldsymbol { x } } ) = \sum _ { n } f _ { n } ( { \boldsymbol { x } } ) } \end{array}$ is also continuous at $\scriptstyle x = x _ { 0 }$ and is summable term by term, that is,

$$
\operatorname* { l i m } _ { x = x _ { 0 } } \sum _ { n } f _ { n } ( x ) = \sum _ { n = 0 } ^ { \infty } \operatorname* { l i m } _ { x = x _ { 0 } } f _ { n } ( x ) .
$$

If a series is uniformly convergent and each of its terms is integrable, then $F ( { \pmb x } )$ is also integrable and summable term by term. If each term of $\textstyle \sum _ { n } f _ { n } ( x )$ is differentiable and $\textstyle \sum _ { n } f _ { n } ^ { \prime } ( x )$ is uniformly convergent, (where $f ^ { \prime } ( x )$ is the derivative o $\mathbf { f } f ( { \pmb x } ) )$ and if $\textstyle \sum _ { n } f _ { n } ( x )$ is uniformly convergent at least at one point, then $\textstyle \sum _ { n } f _ { n } ( x )$ is uniformly convergent everywhere, and the derived function of $\pmb { F } ( \pmb { x } )$ is $\begin{array} { r } { F ^ { \prime } ( x ) = \overline { { \sum } } _ { n } f _ { n } ^ { \prime } ( x ) } \end{array}$ ; that is, the series is differentiable term by term (Knopp, 1931).

A series of the form

$$
f ( x ) = { \frac { a _ { 0 } } { 2 } } + \sum _ { k = 1 } ^ { \infty } { \bigl ( } a _ { k } \cos k x + b _ { k } \sin k x { \bigr ) }
$$

is called a trigonometric series. If the series is uniformly convergent it represents a periodic function of period $2 \pi$ .Then a sum of finite number of terms of (1） is also called a trigonometric series and represents a singlevalued continuous function. If the absolute value of such a function $f ( x )$ is integrable, then

$$
a _ { k } = { \frac { 1 } { \pi } } \int _ { 0 } ^ { 2 \pi } f ( t ) \cos k t d t , \qquad b _ { k } = { \frac { 1 } { \pi } } \int _ { 0 } ^ { 2 \pi } f ( t ) \sin k t d t ;
$$

these are called the Fourier coefficients. When $k = 0$ , the function equals $a _ { 0 } / 2$ In such cases the expression of the form (1) is called a Fourier series.

Let $f ( x )$ be continuous in a closed interval $[ a , b ]$ and let ${ \boldsymbol { x } } _ { 1 } , { \boldsymbol { x } } _ { 2 }$ be any two points in this interval.Write the maximum of $| f ( x _ { 2 } ) - f ( x _ { 1 } ) |$ ,with $| x _ { 2 } - x _ { 1 } | \leq \delta$ as $\omega ( \delta )$ . The quantity $\omega ( \delta )$ is called the modulus of continuity of $f ( x )$ in $[ a , b ]$ . If $\omega ( \delta ) \ \leqq M \delta$ (where $M$ is a constant),it is said to satisfy the Lipschitz condition.If the modulus of continuity $\omega ( \delta )$ of a function $f ( x )$ satisfies the Dini-Lipschitz condition

$$
\operatorname* { l i m } _ { \delta  0 } \omega ( \delta ) \log ( 1 / \delta ) = 0 ,
$$

then the Fourier series of $f ( x )$ converges uniformly to $f ( x )$

If $| f ( x ) |$ is integrable in $[ a , b ]$ , then

$$
\operatorname* { l i m } _ { k \to \infty } \int _ { a } ^ { b } f ( x ) \sin k x d x = 0 , \qquad \operatorname* { l i m } _ { k = \infty } \int _ { a } ^ { b } f ( x ) \cos k x d x = 0 .
$$

$\operatorname { I f } f ( x )$ is continuous and stepwise differentiable in $[ 0 , 2 \pi ]$ - that is,if $f ( { \pmb x } )$ has at most a finite number of discontinuity points in any finite interval - and if $\left| f ^ { \prime } ( x ) \right|$ is integrable, then

$$
\operatorname* { l i m } _ { k \to \infty } k a _ { k } = 0 , \qquad \operatorname* { l i m } _ { k \to \infty } k b _ { k } = { \frac { f ( + 0 ) - f ( - 0 ) } { \pi } } \cdotp
$$

Thus, a Fourier series is integrable term by term.

$\operatorname { I f } f ( x )$ is continuous in $[ 0 , 2 \pi ]$ and is such that $f ( 0 ) = f ( 2 \pi )$ , then the finite sum of trigonometric functions

$$
T _ { n } ( x ) = { \frac { a _ { 0 } } { 2 } } + \sum _ { k = 1 } ^ { n } { \bigl ( } \alpha _ { k } \cos k x + \beta _ { k } \sin k x { \bigr ) }
$$

exists for a given $\epsilon > 0$ ,such that the relation $| f ( x ) - T _ { n } ( x ) | \leq \epsilon$ would hold uniformly in $[ 0 , 2 \pi ]$ . This is called the Weierstrassian approximation theorem. If $f ( x )$ is continuous in $[ a , b ]$ ， then for a given $\epsilon$ there exists a polynomial $P ( { \pmb x } )$ such that $| f ( { \pmb x } ) - P ( { \pmb x } ) | \le \epsilon$ uniformly in $[ a , b ]$ . This is another form of the Weierstrassan approximation theorem, using not a trigonometric series but by a series of polynomials.

The Fourier coefficients $a _ { k }$ and $b _ { k }$ can be obtained by minimizing $\int _ { 0 } ^ { 2 \pi } ( f ( x ) - T _ { n } ( x ) ) ^ { 2 } d x$ among thesum of trigonometrical functions of the form (3), or by

$$
\operatorname* { l i m } _ { n \to \infty } \int _ { 0 } ^ { 2 \pi } ( f ( x ) - T _ { n } ( x ) ) ^ { 2 } d x = 0 .
$$

This is sometimes called the minimum property of the Fourier coefficients. $\mathbf { I f } f ( { \pmb x } )$ is a quadratically integrable function, then

$$
{ \frac { a _ { 0 } } { 2 } } + \sum _ { k = 1 } ^ { \infty } { ( a _ { k } ^ { 2 } + b _ { k } ^ { 2 } ) }
$$

converges to an integral

$$
{ \frac { 1 } { \pi } } \int _ { 0 } ^ { 2 \pi } ( f ( x ) ) ^ { 2 } d x .
$$

This is called the completeness theorem of Fourier series.Accordingly, if the Fourier coefficients $\operatorname { o f } f ( { \pmb x } )$ are all zero, then $f ( x )$ is identically zero,and two such series with the same corresponding Fourier coefficients are identical.

The sum

$$
S _ { n } = { \frac { a _ { 0 } } { 2 } } + \sum _ { k = 1 } ^ { n } a _ { k }
$$

is called the Fourier partial sum. Then

$$
\begin{array} { l } { { \displaystyle S _ { n } ( \xi ) = \frac { 1 } { 2 \pi } \int _ { \xi - \pi } ^ { \xi + \pi } f ( x ) \frac { \sin { ( 2 n + 1 ) } \frac { x - \xi } { 2 } } { \sin { \frac { x - \xi } { 2 } } } d x } } \\ { { \displaystyle ~ = \frac { 1 } { \pi } \int _ { 0 } ^ { \pi / 2 } \left[ f ( \xi + 2 t ) + f ( \xi - 2 t ) \right] \frac { \sin { ( 2 n + 1 ) } t } { \sin { t } } d t . } } \end{array}
$$

Riemann proved that this $S _ { n } ( \xi )$ depends only on the property of $f ( x )$ at $x = \xi$ and that the integral on the right-hand side of this equation (if the lower limit $o$ of the integral is replaced by a fixed S） converges uniformly to zero as $\pmb { n }$ approaches infinity, for all values of $\xi$

In general, when $\int _ { a } ^ { b } { \bar { f } } ( x ) \ d x$ exists, $f ( x )$ is said to belong to class $L _ { 1 }$ ； when $\int _ { a } ^ { b } | f ( x ) | ^ { 2 } d x$ exists, $f ( { \pmb x } )$ is said to belong to class $L _ { 2 }$ .The integrals are taken in the Lebesgue sense. If $[ a , b ]$ is finite, then $L _ { 2 }$ contains $L _ { 1 }$ ； because, if $| f ( { \pmb x } ) |$ is large, then so is $| f ( x ) | ^ { 2 }$ . But $L _ { 2 }$ and $L _ { 1 }$ are not correlated in the infinite interval $[ - \infty , + \infty ]$ .If $f ( x )$ and $g ( x )$ both belong to class $L _ { 2 }$ ，then $f ( { \pmb x } ) { \pmb g } ( { \pmb x } )$ belongs to $L _ { 1 }$ and

$$
\left( \int _ { a } ^ { b } \left| f ( x ) g ( x ) \right| d x \right) ^ { 2 } \leq \int _ { a } ^ { b } | f ( x ) | ^ { 2 } d x \int _ { a } ^ { b } | g ( x ) | ^ { 2 } d x .
$$

This is called the Schwarz inequality.In fact, since $( | f ( { \pmb x } ) | + \lambda | g ( { \pmb x } ) | ) ^ { 2 }$ is integrable, the inequality $2 | f ( x ) g ( x ) | \leq | f ( x ) | ^ { 2 } + | g ( x ) | ^ { 2 }$ gives

$$
\begin{array} { l } { 0 \displaystyle \equiv \int _ { a } ^ { b } ( | f ( x ) | + \lambda | g ( x ) | ) ^ { 2 } d x } \\ { \displaystyle } \\ { = \int _ { a } ^ { b } | f ( x ) | ^ { 2 } d x + 2 \lambda \int _ { a } ^ { b } | f ( x ) g ( x ) | d x + \lambda ^ { 2 } \int _ { a } ^ { b } | g ( x ) | ^ { 2 } d x , } \end{array}
$$

a quadratic expression in $\lambda ,$ in which the discriminant cannot be positive.

We now introduce the Riesz-Fischer theorem :

If $\scriptstyle \sum _ { n } ^ { + \infty } = - \infty \ \left| a _ { n } \right| ^ { 2 }$ is convergent, then there exists a function $f ( { \pmb x } )$ in $[ - \pi , + \pi ]$ ,belonging to class $L _ { 2 }$ ,such that

$$
\operatorname* { l i m } _ { n \to \infty } \int _ { - \pi } ^ { \pi } { \bigg | } f ( x ) - \sum _ { k = - n } ^ { n } a _ { k } e ^ { i k x } { \bigg | } ^ { 2 } d x = 0 .
$$

Suppose that we are given a sequence of functions $\{ f _ { \pi } ( x ) \}$ of the class $L _ { 2 }$ in $[ a , b ]$ or in $[ - \infty , + \infty ]$ and that

$$
\operatorname* { l i m } _ { m , n \to \infty } \int _ { a } ^ { b } | f _ { m } ( x ) - f _ { n } ( x ) | ^ { 2 } d x = 0 .
$$

Then there exists a function $f ( x )$ of class $L _ { 2 }$ that satisfies

$$
\operatorname* { l i m } _ { n \to \infty } \int _ { a } ^ { b } | f _ { n } ( x ) - f ( x ) | ^ { 2 } d x = 0 .
$$

In such a case $f _ { n } ( x )$ is said to converge to $f ( x )$ in the mean,and is written $f ( x ) = \operatorname { l . i . m . } _ { n  \infty } { \mathcal { f } } _ { n } ( x )$ . This means that we exclude a set of points of measure zero. It does not mean that $f ( x )$ converges at all points $_ x$

By stating that a sequence of functions $\{ \phi _ { \mathfrak { n } } ( { \mathfrak { x } } ) \}$ is orthogonal we mean that the functions are of class $L _ { 2 }$ in $[ a , b ]$ or $[ - \infty , + \infty ]$ and satisfy $\int \phi _ { m } ( x ) { \overline { { \phi _ { n } ( x ) } } } d x = 0$ $( m \neq n )$ in that interval, where $\overline { { \phi ( { \pmb x } ) } }$ denotes the conjugate complex function of $\phi ( { \pmb x } )$ .If $\int | \phi _ { n } ( x ) | ^ { 2 } d x = 1$ ，then the sequence is said to be normal. The minimum value of

$$
\int \left| { f ( x ) - \sum _ { k = - n } ^ { n } a _ { k } \phi _ { k } ( x ) } \right| ^ { 2 } d x
$$

is attained at $a _ { k } = \int f \phi _ { k } d x .$ There exists a function $f ( x )$ of class $L _ { 2 }$ such that $\begin{array} { r } { \operatorname* { l i m } _ { n \to \infty } \int | f _ { n } - ^ { \bullet } f | ^ { 2 } d x = 0 . \operatorname { I f } f ( x ) } \end{array}$ is of class $L _ { 2 }$ and $\{ \phi _ { \mathfrak { n } } \}$ is a sequence of normal functions, then there exists a function $g ( x )$ of class $L _ { 2 }$ such that

$$
g ( x ) \sim f ( x ) - \sum _ { n = 1 } ^ { \infty } \phi _ { n } ( x ) \int f ( x ) { \overline { { \phi _ { n } ( x ) } } } d x .
$$

Furthermore, $\int g ( x ) { \overline { { \phi _ { n } ( x ) } } } \ d x = 0$ for all values of $\pmb { n }$ .If $f ( x )$ belongs to class $L _ { 2 }$ and

$$
f ( x ) \sim \sum _ { n = 1 } ^ { \infty } \phi _ { n } ( x ) \int f ( x ) { \overline { { \phi _ { n } ( x ) } } } \ d x ,
$$

then the sequence of functions $\{ \phi _ { \mathfrak { n } } \}$ is said to be closed or complete. In order that $\{ \phi _ { \mathfrak { n } } \}$ be complete,it is necessary and suffcient that for a given $\epsilon > 0$ there should exist a function $f ( { \pmb x } )$ of class $L _ { 2 }$ ,an integer $N _ { : }$ and a sum $\begin{array} { r } { \sum _ { k = 1 } ^ { N } a _ { k } \phi _ { k } ( x ) } \end{array}$ such that

$$
\int { \Big | } f ( x ) - \sum _ { k = 1 } ^ { N } a _ { k } \phi _ { k } ( x ) { \Big | } ^ { 2 } d x < \epsilon .
$$

Also, in such a case,

$$
\int | f ( x ) | ^ { 2 } d x = \sum _ { n = 1 } ^ { \infty } \left| \int f \bar { \phi } _ { n } d x \right| ^ { 2 } .
$$

This is called the Parseval theorem.

In the theories of perturbation in celestial mechanics we usually employ trigonometric series at present. Those theorems and properties are indispensable for criticism of the theories discussed in the following chapters (Schlesinger and Plessner,1926；Zygmund, 1935；Hobson, 1926；Tonelli,1928；Kaczmarz and Steinhaus, 1935;Lebesgue,1906; Vallée Poussin,1919; Bernstein,1926;Hilb and Riesz,1924;Rogosinski, 1930；Denjoy，l94l， 1949；Hardy and Rogosinski， 1950； Natanson, 1955,1960; Kolmogorov and Fomin, 1961).

# 2.3LAGRANGE'S THEOREM

Let $f ( x )$ bea regular function inside a domain $\pmb { A }$ bounded bya simple closed curve $C$ in a complex plane of $\pmb { x } _ { ; }$ and $\theta ( z )$ be a regular function inside and on the boundary of $C$ Let $^ { a }$ be a zero point of $\theta ( z )$ inside $C ,$ such that $^ { a }$ is a simple root of $\theta ( z ) = 0$ .By a“simple curve” we mean a continuous curve without any double or any multiple point； that is,a curve that does not intersect itself. Let $_ { x }$ be a given point inside $\pmb { A }$ and $| \theta ( x ) | < | \theta ( z ) |$ for all points $_ z$ inside $C .$ Then,by Cauchy’s theorem in the theory of analytic functions, the complex integral

$$
{ \frac { 1 } { 2 \pi i } } \int _ { c } f ( z ) { \frac { \theta ^ { \prime } ( z ) } { \theta ( z ) } } d z
$$

represents the sum of the residues inside $C$ 、In fact, if we write the development of $\theta ( z )$ around $^ { a }$

$$
\theta ( z ) = A _ { 1 } ( z - a ) ^ { \tau } + A _ { 2 } ( z - a ) ^ { \tau + 1 } + \cdot \cdot \cdot ,
$$

then

$\theta ^ { \prime } ( z ) = A _ { 1 } r ( z - a ) ^ { r - 1 } + \cdot \cdot \cdot , \qquad f ( z ) = f ( a ) + ( z - a ) f ^ { \prime } ( a ) + \cdot \cdot \cdot$ ：， and, in addition,

$$
\frac { f ( z ) \theta ^ { \prime } ( z ) } { \theta ( z ) } - \frac { \eta f ( a ) } { z - a }
$$

is regular at $^ { a }$

Hence

$$
{ \frac { 1 } { 2 \pi i } } \int _ { c } { \frac { d z } { z - a } } = 1 , \qquad { \frac { 1 } { 2 \pi i } } \int _ { c } { \frac { d z } { ( z - a ) ^ { \tau } } } = 0 \qquad ( r \neq 1 ) ,
$$

and, hence, the residue of $f ( z ) \theta ^ { \prime } ( z ) / \theta ( z )$ is $\gamma f ( a )$ . From this follows the theorem. In particular, if we put $f ( z ) = 1$ , then the integral

$$
{ \frac { 1 } { 2 \pi i } } \int _ { c } { \frac { \theta ^ { \prime } ( z ) } { \theta ( z ) } } d x
$$

gives the total number of zero points of $\theta ( z )$ inside $C$

In the statement of this theorem we put $\theta ( z ) \ : - \ : \theta ( { \pmb x } )$ in place of $\theta ( z )$ Then, since $| \theta ( x ) | < | \theta ( z ) |$ on $C _ { s }$ ，we can easily show that the total number of zero points of $\theta ( z ) - \theta ( { \boldsymbol { x } } )$ inside $C$ is

$$
\begin{array} { c } { { { \frac { 1 } { 2 \pi i } } \displaystyle \int _ { c } { \frac { \theta ^ { \prime } ( z ) ~ d z } { \theta ( z ) _ { , } - ~ \theta ( x ) } } = { \frac { 1 } { 2 \pi i } } \displaystyle \left[ \int _ { c } { \frac { \theta ^ { \prime } ( z ) } { \theta ( z ) } } d z + \theta ( x ) \displaystyle \int _ { c } { \frac { \theta ^ { \prime } ( z ) } { ( \theta ( z ) ) ^ { 2 } } } d z + \cdot \cdot \cdot \right] } } \\ { { = { \frac { 1 } { 2 \pi i } } \displaystyle \int _ { c } { \frac { \theta ^ { \prime } ( z ) ~ d z } { \theta ( z ) } } . } } \end{array}
$$

This integral represents the number of zero points of $\theta ( z ) \ : - \ : \theta ( { \boldsymbol { x } } )$ inside $C .$ By our assumption, $\theta ( z )$ has only one root - indeed,one simple root 一 $^ { a }$ inside $C .$ Thus $\theta ( z ) - \theta ( { \boldsymbol { x } } )$ has only a simple zero point $z = x$ inside $C .$ Hence,by Cauchy's theorem,

$$
f ( x ) = { \frac { 1 } { 2 \pi i } } \int _ { c } { \frac { f ( z ) \theta ^ { \prime } ( z ) \ d z } { \theta ( z ) \ - \ \theta ( x ) } } \cdotp
$$

We expand the integrand, since $| \theta ( { \pmb x } ) | < \theta ( z )$ , in the form

$$
\begin{array} { l } { f ( x ) = \displaystyle \frac { 1 } { 2 \pi i } \left[ \displaystyle \sum _ { n = 0 } ^ { \infty } ( \theta ( x ) ) ^ { n } \int _ { c } \frac { f ( z ) \theta ^ { \prime } ( z ) } { ( \theta ( z ) ) ^ { n + 1 } } d z \right] } \\ { = \displaystyle \sum _ { n = 0 } ^ { \infty } A _ { n } ( \theta ( x ) ) ^ { n } , \qquad A _ { n } = \displaystyle \frac { 1 } { 2 \pi i } \int _ { c } \frac { f ( z ) \theta ^ { \prime } ( z ) \ d z } { ( \theta ( z ) ) ^ { n + 1 } } . } \end{array}
$$

Or, from integration by parts,

$$
A _ { n } = { \frac { 1 } { 2 \pi i n } } \int _ { C } { \frac { f ^ { \prime } ( z ) } { ( \theta ( z ) ) ^ { n } } } d z .
$$

Now we write $\theta ( z ) = ( z - a ) \theta _ { 1 } ( z )$ . The function $\theta _ { 1 } ( z )$ is regular on and inside $c$ and does not vanish. Hence,

$$
{ \cal A } _ { n } = \frac { 1 } { 2 \pi i n } \int _ { \cal C } \frac { f ^ { \prime } ( z ) ~ d z } { ( z - a ) ^ { n } ( \theta _ { 1 } ( z ) ) ^ { n } } = \frac { 1 } { n ! } \frac { d ^ { n - 1 } } { d a ^ { n - 1 } } \left[ \frac { f ^ { \prime } ( a ) } { ( \theta _ { 1 } ( a ) ) ^ { n } } \right] ,
$$

because

$$
\frac { 1 } { 2 \pi i } \int _ { c } \frac { f ( z ) ~ d z } { ( z - a ) ^ { n } } = \frac { 1 } { ( n - 1 ) ! } f ^ { ( n - 1 ) } ( a ) .
$$

Especially for $n = 0$ , using Cauchy's residue theorem again gives

$$
A _ { 0 } = \frac { 1 } { 2 \pi i } \int _ { c } \frac { f ( z ) \theta ^ { \prime } ( z ) } { \theta _ { 1 } ( z ) } \frac { d z } { z - a } = f ( a ) .
$$

Thus, if we put

$$
\theta _ { 1 } ( z ) = \frac { 1 } { \phi ( z ) } , \qquad \theta ( x ) = t , \qquad \frac { z - a } { \phi ( z ) } = \theta ( z ) ,
$$

then

$$
| \theta ( x ) | < \left| \frac { z - a } { \phi ( z ) } \right| \qquad \mathrm { o n } ~ C .
$$

This leads to the following theorem of Lagrange (Whittaker and Watson, 1924;Goursat, 1918):

Let $f ( z ) , \phi ( z )$ be regular on and inside a closed curve $C$ around $^ { a }$ and let $t$ be such that $| t \phi ( z ) | < | z - a |$ holds for all points $z$ on $C$ Then the equation for x, $x = a + t \phi ( x )$ , admits one root inside $C$ Any function regular on and inside $C$ can be expanded in an integral power series of $\pmb { t }$ in the form

$$
f ( x ) = f ( a ) + \sum _ { n = 1 } ^ { \infty } \frac { t ^ { n } } { n ! } \frac { d ^ { n - 1 } } { d a ^ { n - 1 } } [ f ^ { \prime } ( a ) ( \phi ( a ) ) ^ { n } ] .
$$

# 2.4ASYMPTOTIC MOTION AND LIBRATION

Consider a dynamical system with one degree of freedom

$$
{ \frac { d q } { d t } } = { \frac { \partial H } { \partial \dot { p } } } , { \frac { d \dot { p } } { d t } } = - { \frac { \partial H } { \partial q } } , H = { \scriptstyle { \frac { 1 } { 2 } } } A \dot { p } ^ { 2 } - U ( q ) .
$$

The corresponding Hamilton-Jacobi partial diferential equation is

$$
A ( q ) \bigg ( \frac { \partial V } { \partial q } \bigg ) ^ { 2 } = 2 ( U + \alpha ) ,
$$

and the solution is

$$
V = \int \ { \sqrt { \frac { 2 ( U + \alpha ) } { A ( q ) } } } d q , \qquad t - \ t _ { 0 } = { \frac { \partial V } { \partial a } } = \int { \frac { d q } { \sqrt { 2 ( U + \alpha ) A ( q ) } } } \cdotp
$$

Take as the independent variable in place of $t$ a new variable called a mechanical variable, which increases monotonically and continuously with $t$ and becomes infinite together with t.In our case the mechanical variable is $q$ ; writing $2 ( U + \alpha ) A ( q ) = F ( q )$ gives

$$
\left( { \frac { d q } { d t } } \right) ^ { 2 } = F ( q ) .
$$

By our assumption, $F ( q )$ is continuous; it either does or does not vanish for some values of $q$

First suppose that $F ( q )$ does not vanish and is always positive. If $F ( q )$ were negative, then the motion would be imaginary. Further, $F ( q )$ is supposed to be an even function of $q$ ,and to remain finite for any finite value of $\pmb q$ ,and,in particular, to be periodic with period $2 \pi$ with regard to $q$ As $F ( q ) \neq 0$ , it can be expanded by the Weierstrassian approximation theorem of Section 2.2 in a Fourier series

$$
( F ( q ) ) ^ { - 1 / 2 } = A _ { 0 } + A _ { 1 } \cos q + A _ { 2 } \cos 2 q + \cdot \cdot \cdot . .
$$

By our assumption this does not vanish, so that

$$
| A _ { 0 } | > | A _ { 1 } | + | A _ { 2 } | + \cdots ,
$$

which is a suffcient condition. Hence

$$
t - t _ { 0 } = \int ( F ( q ) ) ^ { - 1 / 2 } d q = A _ { 0 } q + A _ { 1 } \sin q + { \textstyle \frac { 1 } { 2 } } A _ { 2 } \sin 2 q + \cdot \cdot \cdot ,
$$

and

$$
| A _ { 0 } | > | A _ { 1 } | + \textstyle { \frac { 1 } { 2 } } | A _ { 2 } | + \cdot \cdot \cdot .
$$

Since the frst term is the largest, we integrate this expresson with respect to $q$ from 0 to $2 \pi _ { : }$ obtaining $2 T = 2 \pi A _ { 0 }$ .By Lagrange's theorem of Section 2.3 we can expand $q$ in the form

$$
q = \frac { t - t _ { 0 } } { A _ { 0 } } + \sum _ { j = 1 } A _ { j } \sin { \left( j \frac { t - t _ { 0 } } { A _ { 0 } } \right) } ,
$$

if $| \phi ( q ) | < | t - t _ { 0 } - A _ { 0 } q |$ ，where $t - t _ { 0 } = A _ { 0 } q + \phi ( q )$ . Hence,when $\pmb { t }$ increases by ${ 2 T } _ { \ O }$ ，the variable $q$ increases by $2 \pi$ .Thus $q$ ,regarded as an angular variable,rotates through the whole circumference of a circle from $q = 0$ to $q = 2 \pi$ This is called the case of revolution.

Next, suppose that $F ( q )$ can become zero. $q$ regarded as an angular variable cannot take all values but it oscillates between two values of $q$ smaller than $2 \pi$ ； that is, it cannot pass through the value $^ { a }$ for which $F ( a ) = 0$ In fact, if we put $F ( q ) = ( q - a ) ^ { m } \phi ( a ) , \phi ( a ) \neq 0$ ，then $\phi ( a )$ can be expanded in a Taylor series and

$$
\frac { d q } { ( q - a ) ^ { m / 2 } } \left[ C _ { 0 } + C _ { 1 } ( q - a ) + C _ { 2 } ( q - a ) ^ { 2 } + \cdot \cdot \cdot \right] = \pm d t .
$$

In particular, if $\pmb { m }$ is odd, then

$$
\frac { 2 } { ( q - a ) ^ { m / 2 - 1 } } \left[ \frac { C _ { 0 } } { m - 2 } + \frac { C _ { 1 } } { m - 4 } ( q - a ) + \cdot \cdot \cdot \right] = \mp ( t - t _ { 0 } ) ;
$$

if $_ { \pmb { m } }$ is even, then

$$
_ { \imath / 2 - 1 } \log { ( q - a ) } + \frac { 2 } { ( q - a ) ^ { m / 2 - 1 } } \left[ \frac { C _ { 0 } } { m - 2 } + \frac { C _ { 1 } } { m - 4 } ( q - a ) + \cdot \cdot \cdot \right]
$$

When $q$ approaches the value $^ { a }$ ,the absolute value of the left-hand side increases; hence, $t$ increases, provided that $m / 2 \geq 1$ . The variable $q$ does not approach $^ { a }$ for any finite value of $t .$ Suppose that $q$ does not pass through $\pmb { a }$ This is called a case of asymptotic motion. If $m = 1$ and $^ { a }$ is a simple root of $F ( q ) = 0 .$ then

$$
q - a = A _ { 1 } ( t - t _ { 0 } ) ^ { 2 } + A _ { 2 } ( t - t _ { 0 } ) ^ { 4 } + \cdot \cdot \cdot .
$$

For $q = a _ { \mathrm { : } }$ $t = t _ { 0 }$ ； thus $q$ reaches $^ { a }$ for a finite value of $t$ but never through $_ { a }$ ; it turns back and follows the same route in the opposite sense. Such a point is called an ordinary point.

Let two consecutive roots of $F ( q ) = 0$ be $_ { a }$ and $b$ . Then

$$
\left( \frac { d q } { d t } \right) ^ { 2 } = ( q - a ) ^ { m } ( b - q ) ^ { n } \psi ( q ) , \qquad \psi ( a ) \ne 0 , \qquad \psi ( b ) \ne 0 ;
$$

or

$$
\left( \frac { d q } { d w } \right) ^ { 2 } = \epsilon ^ { 2 } ( q - a ) ^ { m } ( b - q ) ^ { n } , \left( \frac { d w } { d t } \right) ^ { 2 } = \frac { 1 } { \epsilon ^ { 2 } } \psi ( q ) .
$$

Take w such that $w = 0$ for $t = 0$ , and suppose that $d t$ and dw are of the same sign. The variable ${ \pmb w }$ is the mechanical variable. If $q$ is initially between $^ { a }$ and $b$ ，then $q$ always lies between $^ { a }$ and $b$

Let $m = n = 1$ ，then

$$
\frac { d q } { d w } = \epsilon \sqrt { ( q - a ) ( b - q ) } \quad \mathrm { a n d } \quad q = a \cos ^ { 2 } \frac { \epsilon w } { 2 } + b \sin ^ { 2 } \frac { \epsilon w } { 2 } .
$$

Hence, $q$ is a periodic function of $\boldsymbol { \omega }$ with period $2 \pi / \epsilon$ with respect to $\boldsymbol { \omega }$ The variable $q$ is in this case a periodic function of $t$ ,also. Actually,

$$
\int _ { 0 } ^ { w } \frac { \epsilon d w } { \sqrt { \psi ( q ) } } = t .
$$

The variable $q$ does not change when w increases by $2 \pi / \epsilon$ ，while $t$ increases by $2 T .$ Since $q$ is a periodic function of $\epsilon w$ ，

$$
\begin{array} { l } { { \displaystyle T = \int _ { 0 } ^ { \pi } \frac { \epsilon d w } { \sqrt { \psi ( a \cos ^ { 2 } { ( \epsilon w / 2 ) } + b \sin ^ { 2 } { ( \epsilon w / 2 ) } ) } } } } \\ { { \displaystyle ~ = \int _ { a } ^ { b } \frac { d q } { \sqrt { ( q - a ) ( b - q ) \psi ( q ) } } . } } \end{array}
$$

In order that the periods with respect to ${ \boldsymbol w }$ and to $t$ be equal, $\epsilon$ should equal ${ \pi } / T .$ Thus $q$ is periodic with respect to $t$ ； from the Weierstrassian approximation theorem,

$$
q = { \scriptstyle \frac { 1 } { 2 } } B _ { 0 } + \sum _ { j = 1 } ^ { \infty } B _ { j } \cos j \frac { \pi t } { T }  .
$$

In this case $q$ does not make the whole revolution on the circumference of a circle； it oscillates between two limits smaller than $2 \pi$ around $\begin{array} { r } { q = \frac { 1 } { 2 } B _ { 0 } } \end{array}$ . This is called a libration. In a region where $F ( q )$ is positive between two consecutive simple roots of $F ( q ) = 0$ ,there isa libration. It is necessary to state in what variable the libration takes place. The motion with regard to $q$ is a libration, but in our case it is also a revolution with regard to w (Hagihara, 1944;Brown, l932). The phenomenon of libration is very important in celestial mechanics.The moon librates around its axis of rotation with respect to the line connecting the moon and the earth; this is called the physical libration and causes more than half the surface area of the moon to be visible from the earth.The perisaturnia of some satellites of Saturn librate around the perisaturnia or the nodes of other satellite orbits.The librations of the satellites of Jupiter are most remarkable.Librations also occur in the motion of some asteroids.

If $_ m$ or $\boldsymbol { n }$ or both equal or exceed 2,an asymptotic motion takes place. The moving point approaches the limit point but never passes through it in the limit as $t$ approaches infinity. When two simple zero points $q = a$ and $q = b$ coincide and become a double zero point, then the moving point $C$ comes to rest at that double point if the domain of real motion degenerates to this single point；but the real motion is asymptotic from both sides of the double zero point if the domain of imaginary motion degenerates to this single point. When the root is multiple in an odd order, then the moving point is either at rest at a point in the domain of real motion or approaches it asymptotically. Furthermore,in the domain between the largest real root and $+ \infty$ and in the domain between the smallest real root and $- \infty$ , the motion is asymptotic for $q = + \infty$ and $q = - \infty$ ,respectively. The motion, beginning with $q = + \infty$ or $q = - \infty$ at $t = - \infty$ ,reaches the largest or the smallest real simple zero point in a finite time and returns to the initial point for $t = + \infty$ . When the real zero point is multiple, the motion is asymptotic to that point.

In order to study the motion at $q = \pm \infty$ ,we write $q = 1 / u _ { \colon }$ as in the theory of analytic functions, and study the motion at $u = 0$ Put

$$
F ( q ) = C _ { n } q ^ { n } + C _ { n - 1 } q ^ { n - 1 } + \cdot \cdot \cdot + C _ { 0 } ;
$$

then,

$$
d t = - \frac { d u } { u ^ { 2 - n / 2 } \sqrt { C _ { n } + C _ { n - 1 } u + \cdot \cdot \cdot + C _ { 0 } u ^ { n } } } \cdot
$$

If $n \neq 2$ ， the motion is asymptotic at $u = 0$ or $q = \infty$ .If $n = 2$ ，the motion is also asymptotic.But,if the domain of real motion extends from the largest real simple zero point to $+ \infty$ ，or from $- \infty$ to the smallest

real simple zero point, then we can transform the real domain to a real finite domain by

$$
q = \frac { \alpha v + \beta } { \gamma v + \delta }
$$

as in the theory of elliptic functions,modular functions,and automorphic functions, the motion is a libration with regard to the variable $v$ in that domain, that is, the singular character at $q = \infty$ with regard to $v$ is only an apparent one. For example, if we take $F ( q ) = ( q - a ) ( b - q )$ ，then if we write $q = c + 1 / v$ $( a < c < b )$ ,the motion is a libration between $- 1 / ( c - a )$ and $+ 1 / ( b - c )$ with regard to $v$

When $F ( q )$ is a third-degree polynomial with respect to $q , \psi ( q )$ may become zero and the above condition is not satisfed.When $F ( q )$ isa fourth degree polynomial and has four real zero points $a < b < c < d ,$ then the real motion of libration takes place in the real domain in which the initial position is situated. But, if the domain extends to values smaller than $^ { a }$ or larger than $d ,$ the motion is asymptotic at $q = - \infty$ or $q = + \infty$ . The polynomial $F ( q )$ can then be transformed to its canonical form

$$
{ \cal F } ( q ) = 4 q ^ { 3 } - g _ { 2 } q - g _ { 3 } ,
$$

in the theory of elliptic functions, where $g _ { 2 }$ and $g _ { 3 }$ are the invariants of quartic forms. One zero point is driven to infinity (Hagihara, 1931; Appell and Lacour, 1922; Halphen, 1886).

More generally, when $F ( q )$ is an algebraic polynomial, the behavior of the motion can be discussed as it is in the theory of hyperelliptic functions (Appell and Goursat, 1895；Baker, 1897, i907；Bliss,1933; Osgood,1932; Roberts and Hartford, 1938; see also Sections 6.23, 6.25).

# CONDITIONALLY PERIODIC MOTIONS

# 2.5MULTIPLY PERIODIC FUNCTIONS

When a real function $\pmb { F } ( \pmb { x } )$ satisfies the condition $F ( { \boldsymbol { \mathscr { x } } } ) = F ( { \boldsymbol { \mathscr { x } } } + { \boldsymbol { \omega } } )$ with a real constant $\omega$ for all real values of $_ x$ ,the function $\pmb { F } ( \pmb { x } )$ is said to have a period $\omega$ . With any positive or negative integer $\pmb { \tau }$ we have

$$
\begin{array} { r } { F ( { \pmb x } ) = F ( { \pmb x } + { \pmb \tau } { \pmb \omega } ) ; } \end{array}
$$

that is, if $\pmb { \omega }$ is a period, then its positive integral multiple or its negative integral multiple is also a period. Suppose that $\boldsymbol { F } ( \boldsymbol { x } )$ has another real period $\omega ^ { \prime }$ .If $F ( { \pmb x } ) = F ( { \pmb x } + { \pmb \omega } ^ { \prime } )$ , then $\scriptstyle { \tau ^ { \prime } \omega ^ { \prime } }$ with any positive or negative integer $\scriptstyle { \pmb { \tau } } ^ { \prime }$ is also a period.Hence, $\tau \omega + \tau ^ { \prime } \omega ^ { \prime }$ is also a period.

Assume that $F ( x )$ has no infinitely small period. If $\boldsymbol { F } ( \boldsymbol { x } )$ has two real periods $\omega$ and $\omega ^ { \prime }$ , then we can choose $\tau \omega + \tau ^ { \prime } \omega ^ { \prime }$ as small as we please by taking two integers $\boldsymbol { \tau }$ and $\tau ^ { \prime }$ suitably,as will bedescribedin Kronecker's theorem in Section 2.8. But, since by our assumption $\boldsymbol { F } ( \boldsymbol { x } )$ has no infinitely small period,the two periods $\omega$ and $\omega ^ { \prime }$ must be commensurable; that is, they must be in the ratio of two integers. Consider now the ratio ${ \pmb \beta } / q$ of two mutually prime integers. If $\pmb { \omega }$ is a period, then any other period must be in the form $\scriptstyle { \phi \omega } / { q }$ If we choose $\pmb { \tau }$ and $\scriptstyle \tau ^ { \prime }$ such that

$$
\tau q + \tau ^ { \prime } p = 1 ,
$$

then $\tau \omega + \tau ^ { \prime } \hbar \omega / q = \omega / q$ and $\omega / q$ is also a period. By assumption there should be the smallest number $\omega / q _ { 1 }$ among all periods of the form $\omega / q$ Hence,there should be necessarily a smallest period if there is no infinitely small period.

Next we consider a function $F ( x _ { 1 } , x _ { 2 } , \ldots , x _ { n } )$ of $\pmb { n }$ variables $x _ { 1 } , x _ { 2 } , \ldots , x _ { n } ,$ and suppose that when $x _ { 1 } , x _ { 2 } , \ldots , x _ { n }$ increase by $\omega _ { i 1 } , \omega _ { i 2 } , . . . , \omega _ { i n } ,$ respectively, the function $F ( x )$ has the same value. Then we say that $\pmb { F } ( \pmb { x } )$ is multiply periodic with the period $\mathbf { \Psi } ( \omega _ { i } ) = ( \omega _ { i 1 } , \omega _ { i 2 } , . . . , \omega _ { i n } )$ . The result of multiplying every element $\omega _ { i k } - k = 1 , 2 , . . . , n -$ of $\left( \omega _ { i } \right)$ by a fixed integer is also a multiple period. If $\left( \omega _ { i } \right)$ and $\left( \omega _ { j } \right)$ are both multiple periods of $\pmb { F } ( \pmb { x } )$ ， then $( \omega _ { i } + \omega _ { j } ) = ( \omega _ { i 1 } + \omega _ { j 1 } , . . . , \omega _ { i n } + \omega _ { j n } )$ is also a multiple period $\operatorname { o f } F ( x )$ $\begin{array} { r } { ( \tau \omega _ { i } + \tau ^ { \prime } \omega _ { j } ) = ( \tau \omega _ { i 1 } + \tau ^ { \prime } \omega _ { j 1 } , } \end{array}$ $\tau \omega _ { i n } + \tau ^ { \prime } \omega _ { j n } )$ is also a multiple period. Generally,

$$
\left( \sum _ { k = 1 } ^ { m } \tau _ { k } \omega _ { k 1 } , \sum _ { k = 1 } ^ { m } \tau _ { k } \omega _ { k 2 } , . . . , \sum _ { k = 1 } ^ { m } \tau _ { k } \omega _ { k n } \right) \qquad ( m \leq n )
$$

is also a multiple period.When there is no relation of the form

$$
\begin{array} { r } { \frac { \tau _ { 1 } } { \omega _ { 1 j } } + \frac { \tau _ { 2 } } { \omega _ { 2 j } } + \cdot \cdot \cdot + \frac { \tau _ { m } } { \omega _ { m j } } = 0 ( j = 1 , 2 , . . . , m ) , } \end{array}
$$

with positive or negative integers $\tau _ { 1 } , \tau _ { 2 } , . . . , \tau _ { m }$ (all of which do not vanish at the same time),then the multiple periods $\left( \omega _ { i } \right)$ (all of which do not vanish at the same time for $i = 1 , 2 , . . . , m )$ , are said to be independent.When a multiple period $\left( \omega _ { l } \right)$ is connected with other multiple periods $\left( \omega _ { i } \right)$ by a linear homogeneous relation, then the multiple period $\left( \omega _ { l } \right)$ is said to belong to a field of multiple periods $\left( \omega _ { i } \right)$ ,and the $_ m$ multiple periods $\left( \omega _ { i } \right)$ are called the primitive periods of order $_ m$ (Esclangon,1902,1903,1904, 1916).

In general, a system admitting the operations of addition and multi-