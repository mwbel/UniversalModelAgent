# Chapter 2

# Introduction to Vectors

# 2.1 Scalars and Vectors

A quantity that has magnitude only is called a scalar; it can be represented by a number with an associated sign. For example, distance and electric charge are scalars.

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/c9c3996eac9db8a60c78c0cfbfa6964656ff4c43f66d5e489617b85db26b5ea2.jpg)  
Figure 2.1

Consider two points A and $\mathcal { B }$ .The distance between them is $A B .$ which is $\ Q$ scalar;if this distance is associated with the direction of $A$ to $\mathcal { B }$ it becomes a vector,AB.A vector can be thought of as a scalar with an associated direction (this is necessary but not sufficient; vectors must also satisfy the law of addition given in the following section)；or, in this example,AB can be regarded as a displacement of amount AB in the direction $A$ to $\mathcal { B }$

Notation for vectors varies widely. I prefer to use an arrow, e.g., $\overrightarrow { A B }$ n this text a vector will be denoted by a symbol in boldface type. The modulus, or magnitude of a vector a is its scalar value and this will be written as lal, or simply $\pmb { \mathcal { L } } .$ it is always positive.A unit vector has unit modulus; it will be writen with a “cap” above it, e.g., a. a is the unit vector along a; it may be referred to as the direction of a or as the line of action of a. A vector with zero modulus is a nullor zero vector and is simply written $a _ { 0 } = 0$ Considering AB as $\mathtt { a }$ displacement from $A$ to $\mathcal { B }$ we have

$$
\mathbf { A B } + \mathbf { B A } = \mathbb { 0 } \quad { \mathrm { o r } } \quad \mathbf { A B } = - \mathbf { B A } .
$$

Vectors may be used in any of three senses:

1. Free vectors. Two free vectors, AB and CD,are equal if they have the same modulus and parallel directions. (See Figure 2.1.) 2. Localized vectors. These have their lines of action passing through some point. Examples are the force acting at a point and the velocity of $\hat { \bf { a } }$ point. 3. Position vectors. Let $o$ be any fixed origin and $\mathcal { P }$ a point that may vary.

The vector

$$
{ \mathfrak { Q P } } = { \mathfrak { r } } \qquad .
$$

is the position vector of $P$ with respect to $o$ The symbol $\pmb { \Sigma }$ will usually denote a position vector; it is always anchored to some origin. (See Figure 2.2.)

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/87a54f4a21fae666669872e6d80164fe35e3856f172e6c6c541f34c0bd014f63.jpg)  
-- Figure 2.2

The possibility of three different senses may seem confusing, but in practice the context makes it quite clear which one is relevant.

From the definition,a vector may be multiplied by a scalar, only its modulus being affected (unless the scalar is negative, when the direction is reversed). For example, 2AB has the same direction as AB but twice its modulus. So we can write

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/d838de7bfb2a853d5e83c2024a0fc633ede7ba0c580f131fe9a3d8292bb6e68f.jpg)

$$
\boldsymbol { \mathfrak { z } } = \boldsymbol { \mathfrak { r } } \hat { \boldsymbol { \mathfrak { z } } } ,
$$

an expression which will be used rather frequently, and

$$
\begin{array} { r } { \mathbb { k } \mathbb { r } = ( \mathbb { k } \mathbb { r } ) \mathbb { r } . } \end{array}
$$

It follows from the known properties of scalars that

$$
k x = \tt x k
$$

and

$$
( k + 1 ) 5 = k 5 + 1 5 .
$$

# 2.2The Law of Addition

Considering AB as a displacement, we see that two vectors AB and BC can be added to give AC. Any two free vectors can be added as shown in Figure 2.3.

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/54f0cc17f52fae86ebf7d4f78ec9454190b0fedb2a711e203308ab996b8778c3.jpg)  
Figure 2.3

It follows from geometrical constructions (which are left to the reader) that

$$
\mathbf { a } + \mathbf { b } = \mathbf { b } + \mathbf { a } \quad ( { \mathrm { c o m m a t a } } \mathbf { i } ) \mathbf { c } { \mathrm { i } } { \mathrm { v e } } \ \mathbf { i } \mathbf { a } \mathbf { w } )
$$

and

$$
( \mathbf { a } + \mathbf { b } ) + \mathbf { c } = \mathbf { a } + ( \mathbf { b } + \mathbf { c } ) \quad ( \mathbf { a s s o c i a t i v e \ l a w } ) .
$$

Subtraction can be considered in a similar way to addition, since $A B = - B A$

To illustrate the use of vector addition, consider the straight line $A D$ with direction i. Let $\mathcal { B }$ and $C$ be any two points on $A D$ and let their position vectors with respect to any origin be $\pmb { \tau } _ { 0 }$ and $\mathbf { x }$ Then

$$
O C = O B + B C
$$

$$
{ \mathfrak { r } } = { \mathfrak { r } } _ { \mathfrak { I } } \div \lambda { \hat { \mathfrak { I } } } ,
$$

where $\lambda$ ,the length $B C$ is a scalar. If $\lambda$ varies, $C$ traces out the whole line, so that (2.2.1) is the vector equation of the line $A D$ Now suppose that $\boldsymbol { C }$ is moving along the line at a constant speed $\pmb { \mathcal { V } }$ ,and that at time $t = 0$ ,itisat $\mathcal { B }$ Then at time ${ \big . } { \big . }$ $\mathcal { B } \mathcal { C } = \lambda = \mathfrak { v t }$ and

$$
\begin{array} { r } { \mathfrak { r } = \mathfrak { r } _ { \mathbb { 0 } } \div \mathfrak { v } \tplus . } \end{array}
$$

Conversely,if equations of motion yield this solution for the motion of $C$ ,then it must be moving in a straight line with constant speed.

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/3346c83bce48035d04563424bb80fa1044e37482b174202b2e6e6b1deaa1de27.jpg)  
Figure 2.4

If i,j,and $\pmb { \Sigma }$ are three coplanar vectors, and i and $\hat { \bf J }$ are neither parallel nor antiparallel (pointing in opposite directions), then unique scalars $\mathscr { X }$ and $\mathcal { Y }$ exist such that

$$
\mathbf { r } = x \hat { \mathbf { z } } + y \hat { \mathbf { \bar { z } } } .
$$

(See Figure 2.5.) Similarly, if we have another vector $\hat { \bf k }$ which does not lie in this plane, and let $\pmb { \Sigma }$ be any (three-dimensional) vector, then unique scalars $\boldsymbol { \mathscr { X } }$ $y .$ and $z$ exist such that

$$
{ \mathfrak { x } } = x { \hat { \mathbf { z } } } + y { \hat { \mathbf { z } } } + z { \hat { \mathbf { x } } } .
$$

r is said to be resolved along these three directions, and $\boldsymbol { \mathscr { Z } } , \boldsymbol { \mathscr { Y } }$ and $\textit { \textbf { z } }$ are its components. Resolution can be regarded as the reverse of additin.

i,j,and $\hat { \bf k }$ can be any three directions, but in the applications in this text they will be mutually perpendicular, forming a right-handed triad. This is such that if k points away, then a rotation through $9 9 ^ { \circ }$ fromito $\hat { \mathfrak { J } }$ will be clockwise.

Let r have components $x , y ,$ and $z$ with respect to some triad; then it may be written without ambiguity as

$$
\mathbf { z } = \{ { \mathfrak { x } } , { \mathfrak { z } } , z \} .
$$

If another triad is chosen r will have different components. One great strength of the vector notation is that it is independent of any particular system of reference. Beware of resolving too early; once a vector has been resolved, there are three quantities to cope with instead of one. Also, if a different triad is needed later, then all the paraphernalia of a change of origin and a rotation of axes are needed to change the components.

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/1e5ad2d8f6117a6933bb41ea254a7d9d347440b9d5b7dba31fdcdfabed29f60c.jpg)  
Figure 2.5a

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/9585cebacd23a1db57bbf4488e9002e4c2223b3bfbb3ff8a27e33b85fa1b1886.jpg)  
Figure 2.5b

Let $\widehat { \mathfrak { Q } } \mathfrak { L } = \mathfrak { L }$ make an angle $\pmb { \mathcal { E } }$ with $\pmb { \mathcal { Q } } \ b { x }$ (the $\mathscr { X }$ axis). Then let

$$
\cos \alpha = { \frac { x } { r } } = { \mathrm { i } } .
$$

If $\operatorname { \mathcal { T } 2 }$ and $\mathfrak { n }$ are similarly defined, then $\vec { \hat { \iota } } , \ m$ ,and $\mathfrak { T }$ are the direction cosines of rwith respect to this triad.Since

$$
\xi ^ { 2 } + \pi ^ { 2 } + n ^ { 2 } = 1
$$

the vector $( \vec { \mathbf { \xi } } , \vec { \mathbf { \xi } } ) \vec { \mathbf { \xi } } , \vec { \mathbf { \xi } } \vec { \mathbf { \xi } } \vec { \mathbf { \xi } } \vec { \mathbf { \xi } }$ has unit modulus and so is the unit vector r. In various notations we may write

$$
\begin{array} { r } { \Omega \mathbb { P } = \mathbf { r } = x \hat { \mathbf { z } } + y \hat { \mathbf { J } } + z \hat { \mathbf { k } } = ( x , y , z ) = ( r \hat { l } , r m , r n ) = r ( \hat { l } , m , n ) = r \hat { \mathbf { r } } . } \end{array}
$$

f ${ \bf r } _ { \mathrm { i } } = \{ { x _ { \mathrm { i } } , y _ { \mathrm { i } } , z _ { \mathrm { i } } } \}$ and ${ \bf r } _ { 2 } = \{ x _ { 2 } , y _ { 2 } , z _ { 2 } \}$ ,then

$$
{ \mathfrak { r } } _ { 1 } + { \mathfrak { r } } _ { 2 } = ( x _ { 1 } + x _ { 2 } , y _ { 1 } + y _ { 2 } , z _ { 1 } + z _ { 2 } ) .
$$

This form of the addition law must hold for any vector. Before anything can be called a vector,it must be shown to have magnitude and direction and to obey the vector addition law. A good example of something that has magnitude and direction but is not a vector isa finite rotation. The direction is that of the axis ofrotation, and the magnitude is the angle through which the rotation turns, but two finite rotations cannot be added according to the vector addition law. Infinitesimal rotations, however, can be added in this way.

# Problems

1. Construct geometrically: $0 A + O B$ ， $0 A + 3 0$ $- \Omega \mathbb { A } + \mathbb { B } \mathbb { O }$

2. State the necessary and sufficient conditions for the sum of two vectors to vanish.

3.Show by similar triangles,or otherwise,that I(a+b)=la+lb.

4.Show that the resolution of a vector along any three directions (where no two are parallel or antiparalel and all three do not lie in a plane) is unique. (Consider the consequences if this were not so),

5. Find the moduli and direction cosines of the following vectors:

$$
( - a , 0 , 0 ) , \quad ( 1 , 1 , - 1 ) , \quad ( 0 , 1 , - 1 ) , \quad ( x , y , z )
$$

and of

$$
[ ( - a , \dot { b } , 0 ) + ( a , \dot { b } , 0 ) ] , \quad [ ( 2 , 3 , - 5 ) + ( 3 , - 2 , 4 ) ]
$$

and

$$
[ \{ x , y , z \}  \{ p , q , r \} ] .
$$

All symbols may be assumed to represent positive numbers.

6. Assuming that forces are vectors, find the resultant of the two forces: P1 along (l1,m1,ni） and P along(l,m2,n2).

7. Find the necessary and sufficient conditions for the sum of three nonzero vectors to vanish.

8.Show that

(a) $\frac { \left\{ \mathbf { O A } + \mathbf { O B } \right\} } { 2 }$ is the mid-point of AB:   
(b) $\frac { ( n \mathbf { O A } + m \mathbf { O B } ) } { ( m + n ) }$ is the center of mass of masses n at A and m at B; （9 $\frac { \left. 0 \mathrm { A } + 0 \mathrm { B } + 0 \mathbb { C } \right. } { 3 }$

9. Prove by a vector method that the medians of a triangle are concurrent.

10. Two triangles $A B C$ and $A ^ { \prime } B ^ { \prime } C ^ { \prime }$ have centroids $G$ and $G ^ { \prime }$ Prove that

$$
\mathbf { A } \mathbf { A } ^ { \prime } + \mathbf { B } \mathbf { B } ^ { \prime } + \mathbf { C } \mathbf { C } ^ { \prime } = 3 \mathbf { G } \mathbf { G } ^ { \prime }
$$

11. Express the vector equation of the line (2.2.1), in cartesians, and eliminate the parameter $\lambda$ ，

12. Show that the equation of a plane can be written

$$
{ \mathbf { x } } = { \mathbf { x } } _ { 0 } + \lambda \hat { { \mathbf { a } } } \div \mu \hat { { \mathbf { b } } } ,
$$

where $\yen 0$ is a point in the plane, a and $\hat { \bf \delta }$ are any two nonparallel directions in the plane, and $\lambda$ and $\mu$ are variable scalars. Show that this is equivalent to the usual cartesian equation of the plane.

13. If $\scriptscriptstyle \mathcal { Q } _ { \mathfrak { L } }$ points toward the vernal equinox, and $\beta z$ toward the celestial north pole, show that the direction cosines of a point $\{ \alpha , \delta \}$ are

$$
( \cos \alpha \cos \delta , \quad \sin \alpha \cos \delta , \quad \sin \delta ) .
$$

Find the direction cosines of the following points: the north pole of the ecliptic; the winter solstice; $( 1 2 3 ) , + 4 5 ^ { \circ } )$ ； $( 1 5 7 3 , - 3 0 ^ { \circ } )$ ： $( 4 1 3 5 \mathrm { m } 5 3 . 2 \mathrm { s } ,$ $- 5 8 ^ { \circ } \mathbb { 1 } \mathbb { 6 } ^ { \prime } 3 \mathbb { 1 } ^ { \prime \prime } )$ .In each case verify that the sum of the squares of the direction cosines is one.

14.What is the equation of the plane of the ecliptic in the reference system of problem 13?

15.If Ox points toward the vernal equinox, and Oz toward the north pole of the ecliptic, find the direction cosines of the celestial north and south poles, the autumnal equinox, and $\{ \ G _ { 2 } , 0 ^ { \circ } \}$

# 2.3 The Scalar Product

The angle between two vectors is conventionally taken to lie between 0 and $2 8 0 ^ { \circ }$ Let the angle betwen $\pmb { \hat { e } }$ and b be $\boldsymbol { \theta }$ ；then their scalar or “dot” product is defined by

$$
a \cdot \vec { \supset } = a \vec { \supset } \cos \theta .
$$

From the definition and the commutative law for scalars we have

$$
{ \mathrm { a } } \cdot { \mathrm { b } } = a b \cos \theta = b a \cos \theta = { \mathrm { b } } \cdot { \mathrm { a } } ,
$$

so the commutative law holds for the scalar product. $a \cdot b$ may be considered as the length of a multiplied by the projected length of b on a (i.e., $\overrightarrow { b } \cos \theta )$ . Then, since the projection of $( \gamma + c )$ on a is the sum of the separate projections of b and c on a, it follows that

$$
a \cdot ( b + c ) = a \cdot b + a \cdot c
$$

or that the distributive law holds. We aiso have,from the properties of scalars,

$$
m ( { \mathrm { a } } \cdot { \mathrm { b } } ) = ( m { \mathrm { a } } ) \cdot { \mathrm { b } } = { \mathrm { a } } \cdot ( m { \mathrm { b } } ) .
$$

(2.3.1) should be accepted as an arbitrary definition. Neither reason nor excuse need be offered for it, but there are plenty of reasons why it turns out tobe useful in practice,and that iswhat matters.

One of the most important uses of the scalar product is in the resolution of vectors.In particular if i,j, and $\hat { \bf k }$ form a rectangular triad, the components ofrin this triad are $z = \sum \limits _ { i = 1 } ^ { n } z \cdot j$ and $\boldsymbol { \underline { { z } } } \cdot \hat { \boldsymbol { \mathbf { k } } }$ Since the directions are mutually perpendicular,we have

$$
\hat { { \bf 1 } } \cdot \hat { { \bf k } } = \hat { { \bf k } } \cdot \hat { { \bf j } } = \hat { { \bf j } } \cdot \hat { { \bf i } } = \hat { { \bf j } } .
$$

The square of a vector, $\tt r ^ { 2 }$ , is defined by

$$
\begin{array} { r } { \mathtt { r } ^ { 2 } = \mathtt { r } \cdot \mathtt { r } = \mathtt { r } \mathtt { r } = \mathtt { r } ^ { 2 } . } \end{array}
$$

In particular

$$
\hat { \bar { \bf z } } ^ { 2 } = \hat { \bar { \bf 3 } } ^ { 2 } = \hat { \bf \bar { k } } ^ { 2 } = { \bf \bar { 1 } } .
$$

Writingr=xi+yj+zk,we fnd

$$
{ \tt r } ^ { 2 } = x ^ { 2 } \div y ^ { 2 } + z ^ { 2 } ,
$$

which is the usual formula for finding the modulus of a vector.

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/fe438710da0edf8b6dbe552fd86f7334aa3c80744f1ade0a25e75ea689b2b271.jpg)

# Problems

1. If the angle between AB and CD is $\boldsymbol { \vartheta }$ , What are the angles between AB and DC and between BA and DC?

2. Find a formula for the cosine of the angle between the two vectors $\{ x _ { 1 } , y _ { 1 } , z _ { 1 } \}$ and $\{ x _ { 2 } , y _ { 2 } , z _ { 2 } \}$

3. Prove by a vector method that for any triangle

$$
a ^ { 2 } = b ^ { 2 } + c ^ { 2 } - 2 b c \cos A .
$$

4. Solve, for the unknown vector $\mathbf { x } .$ the equations:

(a) $\mathbf { z } \cdot \mathbf { \hat { a } } = \boldsymbol { \hat { \mathbb { 3 } } } .$ (b) (c） $\begin{array} { l } { \mathbf { x } \cdot \mathbf { a } = \mathbf { x } \cdot \mathbf { b } ( \mathrm { w h e r e ~ } \mathbf { a } \neq \mathbf { b } ) . } \\ { \mathbf { x } \cdot \mathbf { a } = \emptyset . } \end{array}$

5.Evaluate $( a \div b ) \cdot ( a - b )$ when $a = \tilde { \mathfrak { z } }$ Hence prove that the diagonals of a rhombus are perpendicular.

6.a, $\mathbf { \hat { \bar { \rho } } }$ ,and $\pmb { \varepsilon }$ are three nonzero and non-coplanar vectors, and no two of them are parallel or antiparallel. Show that if

$$
\mathbf { x } \cdot \hat { \mathbf { a } } = \mathbf { x } \cdot \mathbf { b } = \mathbf { x } \cdot \varepsilon = 0
$$

then ${ \bf x } = \hat { \bf y }$

7. Assuming that forces are vectors, what relation must exist between $\mathcal { P }$ and $Q$ such that forces $P$ along $( 1 , 0 , 0 )$ and $Q$ along $\{ 0 , 1 , 0 \}$ have zero resultant along $\{ \boldsymbol { \mathbf { \mathit { l } } } , \boldsymbol { \mathfrak { M } } , \boldsymbol { \widehat { \mathbf { \mathit { l } } } } \} ;$

8. Show by a vector method that the altitudes of a triangle intersect in a point.

9. Show by a vector method that the perpendicular bisectors of a triangle intersect in a point.

10. Show that the equation of $\mathtt { a }$ plane can be writen

$$
\left( \boldsymbol { \mathfrak { r } } - \boldsymbol { \dot { \mathfrak { r } } } _ { \mathfrak { V } } \right) \cdot \hat { \boldsymbol { \mathfrak { z } } } = 0 .
$$

Interpret i, and show that this equation is equivalent to that of problem 12, Section 2.2.

11. Find the shortest distances from the.origin to the line

$$
{ \mathfrak { r } } = { \mathfrak { r } } _ { \mathfrak { V } } + \lambda { \hat { \mathfrak { z } } }
$$

and to the plane

$$
\{ \mathbf { r } - \mathbf { r } _ { \Omega } \} \circ \widehat { \mathbf { z } } = 0 .
$$

12. Find the shortest distances from the point $\Re _ { \Im }$ ,where $\mathbb { O P } _ { 1 } = \mathtt { r } _ { 1 }$ ,to the line and plane of problem 11.

13. Interpret the equation

$$
\left\{ \mathfrak { r } - \mathfrak { r } _ { \mathfrak { G } } \right\} ^ { 2 } = \mathfrak { a } ^ { 2 } ,
$$

where r is a variable vector.

# 2.4 The Vector Product

To rotate from a vector a to another vector b, the right-handed convention is adopted,so that a unique direction is associated with the rotation,as shown in Figure 2.6. Let this direction be i； then the vector or “cross” product is defined by

$$
\mathbf { a } \times \mathbf { \dot { b } } = ( a \mathbf { \dot { \delta } } \sin \theta ) \mathbf { \hat { i } } ,
$$

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/1268758e96550e2ccd99c35138fa6be6dc051b9fd6b4a4ebbb2a79e7cdbdaf14.jpg)  
Figure 2.6

where θ is the angle between them. The order is important because, from the definition, it followsthat

$$
\hat { \mathbf { a } } \times \hat { \mathbf { b } } = - \hat { \mathbf { b } } \times \hat { \mathbf { a } }
$$

so that the vector product is not commutative. (Notation for the vector product differs, but the symbol “X” seems to be nearly universal now. The author regrets the passing of the notation aAb which is much safer than a ×b, since X has so many uses; in particular it is fatally easy to start to treat X as a variablel)

The distributive law does hold; i.e.,

$$
a \times ( b + c ) = a \times b + a \times c .
$$

There are several proofs of this, all of which require careful reasoning. The proof given here is based on that given in Rutherford's Vector Methods (Ref. 7).

Let a plane perpendicular to a be called π,and let the projections of b and C on π be b'and c'. Then the projection of (b+c) on π is (b'+c'). Now the length of b' is bsinθ, whereθ is the angle between a and b; so we have

$$
\ a \times b ^ { \prime } = a \times b
$$

and similarly,

$$
{ \hat { \mathbf { a } } } \times { \mathbf { c } } ^ { \prime } = { \hat { \mathbf { a } } } \times { \mathbf { c } } { \quad { \mathrm { a n d } } \quad } { \hat { \mathbf { a } } } \times ( \mathbf { b } ^ { \prime } + { \mathbf { c } } ^ { \prime } ) = { \mathbf { a } } \times ( \mathbf { b } + { \mathbf { c } } ) .
$$

Now a is perpendicular to $b ^ { 2 }$ ,s0 $\mathbf { a } \times \mathbf { \tilde { s } } ^ { \prime }$ lies in $\mathfrak { F }$ and is $\pmb { \mathcal { Q } }$ times the length of $b ^ { \prime }$ Similarly, $\widehat { \sf z } \times \widehat { \sf z } ^ { \prime }$ lies in $\mathcal { T }$ and is $\pmb { \alpha }$ times the length of $c ^ { \prime }$ Then it follows that $\{ \mathbf { a } \times \mathbf { \dot { \delta } } \mathbf { \dot { \delta } } \div \mathbf { a } \times \mathbf { c } ^ { \prime } \}$ lies in $\pi$ is $^ a$ times the length of $( b ^ { \prime } + c ^ { \prime } )$ , and is perpendicular to $( b ^ { \prime } + c ^ { \prime } )$ .Then

$$
\mathbf { a } \times \mathbf { b } ^ { \prime } \div \mathbf { a } \times \mathbf { c } ^ { \prime } = \mathbf { a } \times ( \mathbf { b } ^ { \prime } \div \mathbf { c } ^ { \prime } ) ,
$$

from which the result follows.

It is important to remember that

$$
\boldsymbol { \mathfrak { x } } \times \boldsymbol { \mathfrak { z } } = \boldsymbol { \mathfrak { z } }
$$

and that if i, j,and k form the conventional right-handed rectangular triad, then

$$
\hat { \bf j } \times \hat { \bf k } = \hat { \bf i } , \quad \hat { \bf k } \times \hat { \bf i } = \hat { \bf j } , \quad \hat { \bf i } \times \hat { \bf j } = \hat { \bf k } , \quad \hat { \bf k } \times \hat { \bf j } = - \hat { \bf i } , \quad \mathrm { e t c } .
$$

It follows from this and from the distributive law that if the components of b and $\pmb { \mathfrak { c } }$ are $( \dot { b } _ { z } , \dot { b } _ { y } , \dot { b } _ { z } )$ and $\{ c _ { z } , c _ { 3 } , c _ { z } \}$ ,then

$$
{ \bf b } \times { \bf c } = ( b _ { y } c _ { z } - b _ { z } c _ { y } , ~ b _ { z } c _ { x } - b _ { x } c _ { z } , ~ b _ { x } c _ { y } - b _ { y } c _ { x } )
$$

or, in determinant notation,

$$
\bar { \boldsymbol { \mathbf { b } } } \times \boldsymbol { \mathbf { \mathit { \mathbf { c } } } } = \left| \begin{array} { c } { \hat { \mathbf { 1 } } \hat { \mathbf { J } } \hat { \mathbf { k } } } \\ { \hat { b } _ { x } \hat { b } _ { y } \hat { b } _ { z } } \\ { c _ { x } c _ { y } c _ { z } } \end{array} \right| .
$$

The triple scalar product is defined by a·(b × c)， and is often writen as $| \sum \limits _ { i = 1 } ^ { 3 } a _ { i } \theta _ { i } c |$ $\mathsf { a } = \{ a _ { x } , a _ { y } , a _ { z } \}$ ,then by (2.4.2),

$$
[ \mathbf { a } , \mathbf { b } , \mathbf { c } ] = \left| \begin{array} { l l } { a _ { x } \ a _ { y } \ a _ { z } } \\ { b _ { x } \ b _ { y } \ \bar { b } _ { z } } \\ { c _ { x } \ c _ { y } \ c _ { z } } \end{array} \right| .
$$

The absolute value of the triple scalar product is equal to the volume of the parallelepiped with edges a, b,and c. A simple proof of this follows. In the notation of Figure 2.7 we have

$$
{ \bf \hat { b } } \times { \bf c } = \bar { b } c \sin \theta \ \hat { \bf p } ,
$$

where p is the unit vector perpendicular to b and c, as shown. Then

$$
{ \begin{array} { r l } & { [ \mathbf { a } , \mathbf { b } , \mathbf { c } ] = b c { \sin \theta } { \hat { \mathbf { p } } } \cdot \mathbf { a } } \\ & { \qquad = b c { \sin \theta } a \cos \phi } \\ & { \qquad = { \mathrm { a r e a ~ o f ~ b a s e ~ t i m e s ~ t i n e ~ p e r p e n d i c u l a r ~ h e i g h } } } \\ & { \qquad = { \mathrm { t h e ~ v o l u m e ~ o f ~ t h e ~ p a r a l l e p i p e d } } . } \end{array} }
$$

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/5c3422ceb73a33b17975e2df5b6eedaca3e5f5362aa2d726275e463fc34237ed.jpg)  
Figure 2.7

It follows from this or from the properties of determinants that

$$
[ { \mathsf { a } } , { \mathsf { b } } , { \mathsf { c } } ] = [ { \mathsf { b } } , { \mathsf { c } } , { \mathsf { a } } ] = [ { \mathsf { c } } , { \mathsf { a } } , { \mathsf { b } } ] = - [ { \mathsf { c } } , { \mathsf { b } } , { \mathsf { a } } ] = - [ { \mathsf { a } } , { \mathsf { c } } , { \mathsf { b } } ] = - [ { \mathsf { b } } , { \mathsf { a } } , { \mathsf { c } } ] ,
$$

and that the triple scalar product of three nonzero vectors vanishes if any two are paralel or antiparallel or if all three vectors lie in a plane.

The triple or continued vector product is

$$
a \times ( b ) = ( c )
$$

in which the inclusion of parentheses is essential. It can be shown (most simply, if not most elegantly,by resolving the vectors) that

$$
\mathbf { a } \times ( \mathbf { b } \times \mathbf { c } ) = ( \mathbf { a } \cdot \mathbf { c } ) \mathbf { b } - ( \mathbf { a } \cdot \mathbf { b } ) \mathbf { c } .
$$

This formula is important and should be memorized.

Let P be any point on the line of action of a vector Q, and let OP = r. Then the moment of Qabout O is r × Q. The reader should verify that this is independent of the position of P along Q.

It is important to get the feel of the geometry of the vector product, as well as thealgebra,for it is largely through this that vectors have such considerable practical use. Various uses of the product will appear shortly, but one will be pointed out now. If a vector equation contains some term that you do not like,you have only to multiply vectorially by its direction to eliminate it.For instance, the general equation of motion for a central orbit has the form

$$
\frac { d ^ { 2 } { \bf \vec { r } } } { d \vec { \bf z } ^ { 2 } } = - { \bf \vec { \nabla } } \hat { \bf r } ,
$$

where f is some scalar function. Vectorial multiplication by r gives

$$
\begin{array} { r } { \tt { r } \times \frac { \vec { a } ^ { 2 } \vec { z } } { \vec { a } \vec { z } ^ { 2 } } = 0 , } \end{array}
$$

which yields an important integral for any central orbit. Another example of the elimination of unwanted terms occurs in a consideration of the equation of the plane. This may be put in the form

$$
{ \mathfrak { r } } = { \mathfrak { r } } _ { \ 0 } + \lambda { \hat { \mathbf { a } } } + \mu { \hat { \mathbf { b } } } ,
$$

where $\pmb { \tau } _ { \emptyset }$ is any point in the plane and a and $\hat { \bf 5 }$ lie in the plane. $\lambda$ and $\mu$ are variable scalars that can be eliminated by scalar multiplication by $\hat { a } \times \hat { b }$ Then we have

$$
( { \mathfrak { r } } - { \mathfrak { r } } _ { 0 } ) \cdot ( { \hat { \mathbf { a } } } \times { \hat { \mathbf { b } } } ) = 0 .
$$

# Problems

1. Let i point toward the vernal equinox, $\hat { \mathbf { k } }$ toward the celestial north pole, m toward the summer solstice, and n toward the north pole of the ecliptic. Express iin terms of n and $\hat { \mathbf { k } }$ m in terms of $\hat { \mathfrak { A } }$ and $\hat { \bf k }$ $\hat { \pmb { \mathfrak { X } } }$ in terms of i and $\hat { \mathbf { k } }$ ； $\hat { \bf k }$ in terms of i and n.

2. Prove the formula for the triple vector product.

3.If $a + b + c = 0$ show that

$$
{ \bf b } \times { \bf c } = { \bf c } \times { \bf a } = { \bf a } \times { \bf b } ,
$$

and interpret geometrically.

4.What is the locus of r such that $[ \pmb { \updownarrow } , \pmb { \mathrm { a } } , \mathbf { b } ] = \pmb { \updownarrow } ?$

5. If $\vec { \mathbf { \ell } } _ { 3 } ^ { \ \prime \ } \vec { \mathbf { \ell } } _ { 3 } ^ { \ \prime \ }$ ,and $\mathfrak { T }$ are three nonzero scalars, and

$$
l a \div \pi ^ { 2 } b \div \pi c = 0 ,
$$

show that a, b, and $\pmb { \mathfrak { L } }$ are coplanar. If also

$$
{ \bf \bf \bf \Phi } _ { i } ^ { \prime } { \bf \Phi } _ { \bf { a } } + \eta { \bf \Phi } _ { i } ^ { \prime } { \bf \Phi } _ { \bf { b } } + \eta { \bf \Phi } _ { i } ^ { \prime } { \bf \Phi } _ { \bf { c } } = { \bf \Phi } _ { j } ,
$$

show that

$$
\frac { i } { j ^ { j } } = \frac { \pi } { m ^ { \prime } } = \frac { \pi } { n ^ { \prime } } .
$$

6. If $\mathbf { x }$ , a, and b are coplanar, show that usually $\mathbf { x }$ can be expressed as

$$
x = \lambda a + i t ^ { \frac { 3 } { 2 } } .
$$

What is the exception to this?

7. Evaluate $( a + b ) \times ( a - b )$

8. Choosing simple (but not trivial) numerical values for the components of a, $b$ and $\pmb { \mathbb { c } } .$ verify numerically that

$$
\begin{array} { c } { { \tt a } \cdot ( { \tt b } + { \tt c } ) = { \tt a } \cdot { \tt b } + { \tt a } \cdot { \tt c } , \quad { \tt a } \times ( { \tt b } + { \tt c } ) = { \tt a } \times { \tt b } + { \tt a } \times { \tt c } , } \\ { { \tt a } \times ( { \tt b } \times { \tt c } ) = ( { \tt a } \cdot { \tt c } ) { \tt b } - ( { \tt a } \cdot { \tt b } ) { \tt c } . \quad } \end{array}
$$

Find the values of

$$
{ \mathrm { i } } \mathtt { a } \times \mathtt { c } { \mathrm { i } } , \quad [ \mathtt { a } , \mathtt { b } , \mathtt { c } ] , \quad ( \mathtt { a } \times \mathtt { b } ) \times \mathtt { c } , \quad ( \mathtt { a } \times \mathtt { b } ) \cdot ( \mathtt { a } \times \mathtt { c } ) , \quad ( \mathtt { a } \times \mathtt { b } ) \times ( \mathtt { a } \times \mathtt { c } ) .
$$

$g$ Find the equation to the plane containing the heads of the vectors $a , b$ and $\pmb { \mathrm { c } }$

10. Show that $( \mathbf { a } \times \mathbf { b } ) \cdot ( \mathbf { c } \times \mathbf { d } ) = ( \mathbf { b } \cdot \mathbf { d } ) ( \mathbf { c } \cdot \mathbf { a } ) - ( \mathbf { b } \cdot \mathbf { c } ) ( \mathbf { d } \cdot \mathbf { a } ) .$

11. Show that

$$
\begin{array} { r l r } & { } & { ( { \bf a } \times { \bf b } ) \times ( { \bf c } \times { \bf d } ) = [ { \bf a } , { \bf b } , { \bf d } ] { \bf c } - [ { \bf a } , { \bf b } , { \bf c } ] { \bf d } } \\ & { } & { \qquad = ( { \bf c } \times { \bf d } ) \times ( { \bf b } \times { \bf a } ) \qquad } \\ & { } & { \qquad = [ { \bf c } , { \bf d } , { \bf a } ] { \bf d } - [ { \bf c } , { \bf d } , { \bf b } ] { \bf a } . } \end{array}
$$

Hence show that

$$
[ a , b , c ] \mathrm { d } = [ \mathrm { d } , \mathrm { b } , \mathrm { c } ] \mathrm { a } + [ \mathrm { a } , \mathrm { d } , \mathrm { c } ] \mathrm { b } + [ \mathrm { a } , \mathrm { b } , \mathrm { d } ] \mathrm { c }
$$

and deduce the conditions for the unique resolution of the vector d along the three directions a, b, and c.

12.Show that

$$
[ \mathbf { a } , \mathbf { b } , \mathbf { c } ] ( \mathbf { f } \times \mathbf { g } ) = \left| \begin{array} { l } { \mathbf { a } \qquad \mathbf { b } \qquad \mathbf { c } } \\ { \mathbf { f } \cdot \mathbf { a } \ \mathbf { f } \cdot \mathbf { b } \ \mathbf { f } \cdot \mathbf { c } } \\ { \mathbf { g } \cdot \mathbf { a } \mathbf { g } \cdot \mathbf { b } \ \mathbf { g } \cdot \mathbf { c } } \end{array} \right| .
$$

Hence, generalize equation (2.4.2).

13.Solve for X the equation X × a= b, where a·b=0.

14. Solve the equation X+a(X·b)=c. (Try ·b.)

15. Solve the simultaneous equations

$$
\alpha { \mathbf { X } } + \beta { \mathbf { Y } } = { \mathbf { a } } \quad { \mathrm { a n d } } \quad { \mathbf { X } } \times { \dot { \mathbf { Y } } } = { \mathbf { b } } \qquad { \mathrm { w h e r e ~ } } { \mathbf { a } } \cdot { \mathbf { b } } = 0 .
$$

16. Solve the equation $x \times a + ( X \cdot b ) c = d$

# 2.5The Velocity of a Vector

A vector can vary as a function of a scalar, such as time,or as a function of another vector,such as position. Provided that the variation is suitably continuous (and this will normally be taken for granted),the vector can be differentiated. Here we shall mostly be considering vectors that vary with time, and the subject of differentiation will be approached in relation to the kinematics of a particle.

Let AB represent the path of a particle $\mathcal { P }$ ,and let $P$ and $P ^ { i }$ be positions of the particle at times $\pmb { \xi }$ and $\ddagger + \delta \vec { \tau }$ $\delta \sharp$ is a small time interval, and the arc $P F ^ { \prime \prime }$ can be considered as linear because we are shortly to take the limit $\delta t  0$ Choose any origin $\boldsymbol { \theta }$ and let $\Omega \mathbb { P } = \mathfrak { r }$ and ${ \tt O P } ^ { \prime } = \tt \tt \tt \ " + \delta \tt \& \mathrm { \tt \to \tt G }$ ，so that $P P ^ { I } = \delta \pi$ Let $\scriptstyle { \hat { \tau } }$ be the unit vector along $\mathbf { \overline { { P } } } \mathbf { \overline { { \mathcal { P } } } } ^ { 3 }$ ； in the limit, this will be the tangent at $P$ Then

$$
\delta \mathfrak { r } = \widehat { \mathfrak { T } } \delta \mathfrak { s }
$$

where s is distance measured along the curve. Taking the limit we have

$$
\operatorname* { l i m } _ { \delta t \to 0 } { \frac { \mathbf { P } \mathbf { P } ^ { t } } { \delta t } } = \operatorname* { l i m } _ { \delta t \to 0 } { \frac { \delta \mathbf { r } } { \delta t } } = { \frac { d { \mathbf { r } } } { d t ^ { t } } } = { \hat { \boldsymbol { \tau } } } { \frac { d s } { d t ^ { t } } } .
$$

This is the velocity of the particle at $\mathbf { \xi } _ { p }$ It may also be written $\dot { \pmb { \mathrm { \mathfrak { r } } } }$ or $\mathbf { \^ v }$ It has magnitude and direction,but we have to show that it obeys the vector law of addition,in order to establish that it isa vector.

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/fee9f4109206013e40944d8e7331e9e4292997fec5d4ecf9c51d53fe27b13193.jpg)  
Figure 2.8

Let $\mathcal { P }$ have its position changed in time $\delta \mathbf { \vec { \hat { z } } }$ by two increments, $\delta _ { 1 } \tau$ and $\delta _ { 2 } \tt { r }$ These are vectors, so that the total displacement is $\delta \mathbf { z } = \delta _ { \textrm { I } } \mathbf { z } + \delta _ { \textrm { 2 } } \mathbf { z }$ Dividing by

St and taking the limit, we get

$$
\begin{array} { r } { \frac { d \mathfrak { r } } { d \mathfrak { t } } = \frac { d \mathfrak { r } _ { 1 } } { d \mathfrak { t } } + \frac { d \mathfrak { r } _ { 2 } } { d \mathfrak { t } } , } \end{array}
$$

which can be written as

$$
\begin{array} { r } { \mathbb { v } = \mathbb { v } _ { 1 } \neq \mathbb { v } _ { 2 } . } \end{array}
$$

This is the required result.

The components of $\{ \dot { \bar { \tau } } \}$ are $\dot { \mathfrak { X } } , \dot { \mathfrak { Y } } , \dot { z }$ and those of $\hat { \pmb { \tau } }$ are $\{ d \bar { \alpha } / d s , d \bar { y } / d s , d z / d s \}$

It is convenient to adopt the modern convention using “velocity” only as   
the vector $\blacktriangledown$ and “speed”as the scalar value of the velocity. Thus the speed of   
$P$ is $\frac { d s } { d t }$

It is very important to realize that ${ \left| \frac { d \mathbf { \hat { x } } } { d \mathbf { \hat { z } } } \right| } , ~ \mathrm { o r ~ } ~ { \left| \mathbf { \hat { r } } \right| } ,$ is not in general the same $\mathrm { a s } \ \frac { d \vert \Im \vert } { d t } , \ \mathrm { o r } \ \frac { d \Im } { d t }$ or $\dot { \tau }$ The former is the speed of $\mathcal { P }$ while the latter is only the component of the velocity of $P$ along the radius vector: in general there will also be $^ \mathrm { a }$ transverse component of velocity,at right angles to the radius vector. We have

$$
\boldsymbol { \mathfrak { x } } \cdot \boldsymbol { \mathfrak { x } } = \boldsymbol { \mathfrak { z } } ^ { 2 } .
$$

Differentiating each side, we get

$$
\mathbf { z } \cdot \dot { \mathbf { z } } = r \dot { \mathbf { r } } ,
$$

an important relation that will be used frequently in this text.

$\pmb { \Sigma }$ is $\hat { \mathbf { x } } _ { \pmb { \nu } }$ position vector and the path traced out by $\mathcal { P }$ is the locus (or orbit, for our purposes) of $P$ is a localized vector, but if we let $0 \mathbf { \hat { z } } _ { 2 } = \mathbf { \hat { z } }$ ,then $Q$ traces out a path called the hodograph of the motion. The rate of change of $0 \mathbf { Q }$ measures the acceleration of $\mathcal { P }$ which is a vector. It may be written $\vec { a } ^ { 2 } \vec { \mathfrak { x } } / \bar { a } \vec { \mathfrak { z } } ^ { 2 }$ ， $\sin \beta \pi / \sin \beta \pi \sin \beta$

In Figure 2.8 consider the product $\mathbb { O P } \times \mathbb { O P } ^ { \prime } = \mathfrak { F } \times \delta \mathfrak { r }$ The modulus is equal to twice the area of the triangle $O P _ { 2 } ^ { \alpha }$ , and the rate of change of this area is the areal velocity of $o p$ The areal velocity is therefore

$$
\frac { 1 } { 2 } | x \times \frac { 1 } { 2 } | .
$$

The direction of $\sum X { \dot { \bar { \mathbf { z } } } }$ is perpendicular to the plane containing $\pmb { \Sigma }$ and i. If the motion takes place in a plane, then this direction is constant. Consider Kepler's first two laws: the frst specifes motion in a plane for a planet, and the second states that the areal velocity is constant. So, for Keplerian motion, we have

$$
\mathbf { r } \times \dot { \hat { \mathbf { z } } } = \mathbf { \hat { \mathbf { z } } } ,
$$

where h is a constant vector. Note that (2.5.2) is the frst integral of (2.4.6) and that this means that the acceleration is entirely directed along r.

# Problems

1. Solve the following equations of motion, and interpret their solutions.

$$
{ \begin{array} { r l } & { { \mathrm { ( a ) ~ } } { \dot { \bar { \mathbf { r } } } } = { \hat { \mathbf { a } } } \qquad { \mathrm { ( b ) ~ } } { \ddot { \bar { \mathbf { r } } } } = { \mathbf { b } } \qquad { \mathrm { ( c ) ~ } } { \mathbf { r } } \times { \dot { \bar { \mathbf { r } } } } = { \boldsymbol { \mathbb { 0 } } } \qquad { \mathrm { ( d ) ~ } } { \mathbf { r } } \cdot { \dot { \bar { \mathbf { r } } } } = c } \\ & { \qquad { \mathrm { ( e ) ~ } } | { \ddot { \bar { \mathbf { r } } } } | = { \dot { r } } \qquad { \mathrm { ( f ) ~ } } { \mathbf { r } } \times { \ddot { \bar { \mathbf { r } } } } = { \boldsymbol { \mathbb { 0 } } } \qquad { \mathrm { ( g ) ~ } } { \dot { \bar { \mathbf { r } } } } = { \mathbf { r } } \times { \ddot { \bar { \mathbf { r } } } } . } \end{array} }
$$

2.Show that $\frac { d \vec { \langle } a \mathbf { r \rangle } } { d t } = \mathbf { r } \frac { d \vec { a } } { d \vec { \tau } } + a \frac { d \vec { \mathbf { x } } } { d \vec { \tau } } ,$

3.Show that $\frac { d ( { \bf a } \cdot { \bf b } ) } { d \xi } = { \bf a } \cdot \frac { d { \bf b } } { d \xi } + { \bf b } \cdot \frac { d \hat { \bf a } } { d \xi } .$

4. Show that $\frac { d ( { \bf a \times b } ) } { d t } = { \bf a \times \frac { d b } { d t } + \frac { d a } { d t } \times b } .$

5.Describe the hodograph of uniform circular motion with radius $a$ and speed $v$ Hence show that the acceleration is $( y ^ { 2 } / a )$ directed toward the center.

6.A particle describes an equiangular spiral with constant areal velocity $A$ Show that for the motion $\mathfrak { F } = 2 A \cot \theta$ where $\theta$ is the (constant） angle between the radius vector and the tangent, and solve this equation to show how r varies with the time.

# 2.6Angular Velocity

A vector can change its direction as well as its modulus, so that it is possible to differentiate a unit vector, ${ \hat { \bar { \mathbf { i } } } } ,$ say.Let ibe rotated through a small angle, $\delta \theta$ the new vector being $\hat { \bar { \mathbf { z } } } + \delta \hat { \bar { \mathbf { z } } }$ Since this must still be a unit vector, $\hat { \mathfrak { Z } } \circ \delta \hat { \mathbf { 1 } } = \mathbb { 0 }$

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/e21ae773d51ecce9d1af880ab882239ea8ee7f6826c575d6303b83c94167be20.jpg)  
Figure 2.9

(Squares of small quantities, such as $\delta \hat { \mathbf { z } } ^ { 2 }$ , are, of course, neglected.） Let the

direction of the rotation, taken in the right-handed sense, be $\hat { \bf k }$ Then the direction of 8i is $\hat { \mathbf { k } } \times \hat { \mathbf { \mathsf { \mathbf { \mathbf { \mathbf { \mathbf { \mathbf { \varepsilon } } } } } } } }$ and its length is $\delta \theta$ s0

$$
\delta \hat { \mathbf { z } } = \delta \hat { \theta } \hat { \mathbf { z } } \times \hat { \mathbf { z } } ,
$$

and

$$
\begin{array} { r } { \frac { d \hat { \bar { \mathbf { i } } } } { d \hat { t } } = \left( \hat { \mathbf { k } } \frac { d \hat { \theta } } { d \hat { t } } \right) \times \hat { \mathbf { i } } } \\ { = \omega \times \hat { \mathbf { i } } , } \end{array}
$$

where w is an angular velocity. It is useful to remember that the derivative of aunit vector is always perpendicular to that vector.

More generally, let OP =r be any vector,making an angle θ with k, the axis of rotation. Then, since Pis at a distance r sinθ from k, the rate of change ofr due solely to the angular velocity w is w ×r.

W has magnitude and direction.To show that it is a vector,we must prove that it obeys the vector addition law. Suppose r to be subjected to two infinitesimal rotations,δ0about k1 and 802 about k2. Taking these in order, we have $\pmb { \Sigma }$ becoming first

$$
{ \mathfrak { r } } + \delta { \mathfrak { r } } _ { \mathtt { j } } = { \mathfrak { r } } + \delta \hat { \theta } _ { \mathtt { j } } \hat { \mathbf { k } } _ { \mathtt { j } } \times { \mathfrak { r } }
$$

and then

$$
\mathbf { r } + \delta \mathbf { r } _ { 1 2 } = ( \mathbf { r } + \delta \theta _ { 1 } \hat { \mathbf { k } } _ { 1 } \times \mathbf { r } ) + \delta \theta _ { 2 } \hat { \mathbf { k } } _ { 2 } \times ( \mathbf { r } + \delta \theta _ { 1 } \hat { \mathbf { k } } _ { 1 } \times \mathbf { r } ) .
$$

Neglecting the second order small quantity δ0802 (this is all right since we are to take a limit；it is this step which is not possible when dealing with fnite rotations), we have

$$
\delta \mathbf { r } _ { 1 2 } = \delta \theta _ { \mathrm { i } } \hat { \mathbf { k } } _ { 1 } \times \mathbf { r } + \delta \theta _ { 2 } \hat { \mathbf { k } } _ { 2 } \times \mathbf { r } = \delta \pmb { \hat { \varepsilon } _ { 2 1 } } ,
$$

showing that infinitesimal rotations are commutative. Dividing by St and taking the limit,we see that the same is true for angular velocities. If

$$
\hat { \bf x } _ { \mathrm { i } } \frac { d \hat { \theta } _ { 1 } } { d \hat { t } } = \omega _ { \mathrm { i } } \quad \mathrm { a } \hat { \bf x } _ { 2 } \frac { d \hat { \theta } _ { 2 } } { d \hat { t } } = \omega _ { 2 }
$$

then

$$
\dot { \boldsymbol { \mathfrak { r } } } = \{ \omega _ { \perp } + \omega _ { 2 } \} \times \boldsymbol { \mathfrak { r } } ,
$$

so that the effect is the same as that produced by a single angular velocity (w1+w2). Hence angular velocities are vectors; they can be added according to the vector law and they can also be resolved.

# Problems

1. Find expressions for the modulus and direction of the angular velocity of the mean Sun.

2. Find the angular velocity of the apparent Sun, assuming that the Earth's orbit is circular,with constant angular velocity. Resolve this angular velocity along an equatorial system of axes and hence find the components of the Sun's motion on the celestial sphere along these axes. Compare the component in right ascension with the angular velocity of the mean Sun.

3. Assuming the Earth to be fixed and the celestial sphere (of unit radius) to be rotating around the Earth, consider the apparent motion of the stars as seen by an observer at latitude l. If 2 points to the observer's zenith (the direction vertically above him),resolve the motion of a star at declination $\delta$ along and at right angles to 么.

# 2.7 Rotating Axes

Writing $\pmb { \tilde { \underline { { s } } } } = \pmb { \tilde { \tau } } \hat { \pmb { \tilde { \mathbf { z } } } }$ and differentiating, we have

$$
\dot { \bar { x } } = \dot { \bar { y } } \hat { \bar { z } } \frac { 1 } { \bar { z } } \hat { \frac { d \hat { \mathbf { r } } } { d \bar { t } } } .
$$

This expresses f as the sum of two vectors, one along and one perpendicular to r. The first gives the rate of change of the length of $\pmb { \Sigma }$ and the second can be considered as being due to a rotation.

Frequently (as on the Earth) we observe phenomena with respect to rotating axes. Suppose a vector $\pmb { \Sigma }$ to bebservedtohaverates ofchange $\frac { d \pmb { \xi } } { d t }$ with respect to afxed frare f eference $\pmb { \mathcal { F } } _ { 1 }$ and $\frac { \partial \mathfrak { x } } { \partial \sharp }$ wit repet t a rarne ${ \boldsymbol { { F } } } _ { 2 }$ rotating with respect to $\pmb { \xi } _ { 1 }$ with angular velocity $\omega$ ，which need not be constant. The formula (2.7.1) applies to a simple case of this, where the axes of $F _ { 2 }$ are rotating with $\pmb { \Sigma }$ ,so that $\frac { { \hat { \sigma } } \mathbf { r } } { \mathbf { \sigma } { \hat { \alpha } } t }$ can be written instead of $\dot { \gamma } \hat { \mathbf { r } }$ Then (2.7.1)can be writen in the form

$$
\frac { d \mathfrak { x } } { d \xi } = \frac { \partial \mathfrak { x } } { \partial \xi } \div \omega \times \mathfrak { r } .
$$

This is a general result, as we shall now show.

Let $\hat { \mathfrak { T } } , \hat { \mathbf { J } }$ and $\hat { \mathbf { k } }$ form an orthogonal triad rigidly attached to $\boldsymbol { { \mathcal { F } } _ { 2 } }$ ；then

$$
\begin{array} { r } { \mathbf { \tilde { r } } = \sum ( \mathbf { r } \cdot \hat { \mathbf { l } } ) \hat { \mathbf { l } } , } \end{array}
$$

where the summation is over $\hat { \mathfrak { Z } } , \hat { \mathfrak { z } }$ and $\hat { \bf k }$ Then

$$
\frac { d \mathbf { r } } { d t } = \sum \left\{ \frac { d ( \mathbf { r } \cdot \hat { \mathbf { i } } ) } { d t } \hat { \mathbf { i } } \right\} + \sum \left\{ ( \mathbf { r } \cdot \hat { \mathbf { i } } ) \frac { d \hat { \mathbf { i } } } { d t } \right\} .
$$

But

$$
\frac { d \widehat { \sf z } } { d \widehat { \sf z } } = \omega \times \widehat { \sf z }
$$

s0

$$
\sum \limits _ { i = 0 } \left\{ ( \frac { 1 } { i } \cdot \hat { 1 } ) \frac { d \hat { 1 } } { d t } \right\} = \omega \times \sum \limits _ { i = 1 } ( \frac { 1 } { i } \cdot \hat { 1 } ) ^ { \hat { 1 } }
$$

To find $\frac { \partial \pmb { x } } { \partial t }$ w treat $\widehat { \tt z } , \widehat { \tt z } ,$ and $\hat { \Bbbk }$ as coustnt vectors s ctat

$$
\frac { \partial \pmb { \varepsilon } } { \partial t } = \sum \left\{ \frac { d \{ \pmb { \{ r \cdot \hat { \mathbf { i } } } } \}  { d \hat { \pmb { \varepsilon } } ^ { \pm } } \hat { \bf { i } } \right\} .
$$

Hence (2.7.2) follows at once. It may be written in the notation of operators as

$$
\frac { d } { d t } = \frac { \partial } { \partial t } + \omega \times .
$$

As an illustration of the use of this formula we shall find the components of velocity and acceleration in polar coordinates. Here $\mathfrak { Q } \mathfrak { L } = \mathfrak { r }$ makes an angle $\theta$ with some fixed direction.Let i point in the radial direction (along OP) and $\hat { \Im }$ in the transverse direction (i.e., in the direction of increasing $\boldsymbol { \vartheta }$ ，and let $\hat { \mathbf { 1 } } \times \hat { \mathbf { 3 } } = \hat { \mathbf { 3 } }$ (See Figure 2.10.） Then the angular velocity of OP is $\dot { \theta } \hat { \mathbf { k } }$ and

$$
\frac { d \hat { \mathbf { x } } } { d \hat { \mathbf { z } } } = \dot { \vec { \mathbf { \tau } } } \hat { \hat { \mathbf { z } } } + r \dot { \theta } \hat { \vec { \mathbf { j } } } .
$$

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/99b2c80dc855ccb6a74e631e68e76d4cc1dc69cc71aa4375ca15682e7c5542de.jpg)  
Figure 2.10

This gives the radial and transverse components of velocity. Similarly,

$$
\begin{array} { r } { \frac { d ^ { 2 } \mathfrak { r } } { d \mathfrak { t } ^ { 2 } } = \left( \frac { d } { d \mathfrak { t } } \right) ( \mathfrak { r } \mathfrak { T } + \mathfrak { r } \dot { \theta } \mathfrak { I } ) } \end{array}
$$

$$
{ \begin{array} { r l } & { = \left( { \frac { \partial } { \partial t } } + { \dot { \theta } } { \hat { \mathbf { k } } } \times \right) \left( { \dot { r } } { \hat { \mathbf { i } } } + r { \dot { \theta } } { \hat { \mathbf { j } } } \right) } \\ & { = { \ddot { r } } { \hat { \mathbf { i } } } + \left( { \dot { r } } { \dot { \theta } } + r { \ddot { \theta } } \right) { \hat { \mathbf { j } } } + { \dot { \theta } } { \hat { \mathbf { k } } } \times \left( { \dot { r } } { \hat { \mathbf { i } } } + r { \dot { \theta } } { \hat { \mathbf { j } } } \right) } \\ & { = ( { \ddot { r } } - r { \dot { \theta } } ^ { 2 } ) { \hat { \mathbf { i } } } + \left( r { \ddot { \theta } } + 2 { \dot { r } } { \dot { \theta } } \right) { \hat { \mathbf { j } } } . } \end{array} }
$$

This formula,which it is useful to memorize,gives the radial and transverse components of acceleration. The transverse component can be written as

$$
\frac { 1 } { r } \frac { d } { d t ^ { 2 } } ( r ^ { 2 } \dot { \theta } ) .
$$

The term in the parentheses is easily verified to be twice the areal velocity of $\mathcal { P }$ ：

Consider the situation when $\textbf { z }$ particle travels in a circle with constant speed. Taking the center of the circle as origin,we have

$$
\frac { d ^ { 2 } \tt { r } } { d ^ { 2 } \tt { t } ^ { 2 } } = - \tt { e } ^ { \tt { i } \cdot \tt { \dot { E } } ^ { 2 } \hat { \tt { i } } . }
$$

This acceleration is a consequence of the kinematics of the particle. If an observer is moving round with the particle,and, through ignorance or prejudice or for convenience, he likes to work in terms of the kinematics with respect to his rotating axes,then he will be unable to account for what he observes unless he applies to every particle a “centrifugal acceleration” ${ \mathfrak { p } } { \mathfrak { \hat { \theta } } } ^ { 2 } { \hat { \mathbf { \mathbb { 1 } } } } .$ Alternatively he can introduce a fictitious force, the “centrifugal force,”which would produce thisacceleration.

Asan example of its application consider a conical pendulum consisting of amass $\gamma _ { \mathcal { R } }$ at $\mathcal { P }$ suspended from $o$ bya light string oflength $\gamma ^ { * }$ and traveling in a circle with constant angular velocity about the vertical such that OP makes a constant angle with the vertical. The radius of the circle is rsin,so that the centrifugal force C,acting as shown in Figure 2.11 is equal to mr sin 02, where $\dot { \boldsymbol { \theta } }$ is the angular velocity of $\mathcal { P }$ about thevertical.With this force included, the problem becomes one of statics.Resolving the forces at right angles to the string (to eliminate the tension),we have

$$
m g \sin \phi = C \cos \phi = \pi r \sin \phi \cos \phi \dot { \theta } ^ { 2 }
$$

$$
{ \dot { \theta } } ^ { 2 } = { \frac { g } { r \cos \phi } } .
$$

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/3cab65424b3689013813270ea2288ef4510fffbd4384f44447afa76dfecdbeff.jpg)  
Figure 2.11

As another illustration of the use of equation (2.7.3) consider the motion observed from $\clubsuit$ point fixed on the surface of the Earth.Let $C$ be the center of the Earth,and let someone at $o$ be observing the motion of a point $\pmb { \mathcal { P } }$ Let $\mathbb { C O } = \mathfrak { r o }$ and $\mathbb { Q } \mathbb { P } = \mathbb { r }$ usually $\gamma$ is small compared with $\mathfrak { P } _ { \mathfrak { V } }$ Let the angular velocity of the Earth be $\omega \hat { \mathbf { z } }$ The velocity of $P$ with respect to nonrotating axes is

$$
\begin{array} { c } { { \displaystyle { \frac { d \mathbb { P } } { d t } } = \frac { \vec { d } } { d t } \big ( \boldsymbol { \mathrm { r } } _ { 0 } + \boldsymbol { \mathrm { r } } \big ) } } \\ { { \displaystyle { \phantom { \frac { d \mathbb { P } } { d t } } } = \frac { \partial \boldsymbol { \mathrm { r } } } { \partial t } + \omega \hat { \mathbf { z } } \times \boldsymbol { \mathrm { r } } _ { 0 } + \omega \hat { \mathbf { z } } \times \boldsymbol { \mathrm { r } } . } } \end{array}
$$

The acceleration of $\pmb { \mathscr { P } }$ is

$$
\begin{array} { r l } & { \frac { d ^ { 2 } \mathbb { P } } { d t ^ { 2 } } = \left( \frac { \partial } { \partial t } + \omega \hat { { \mathbf z } } \times \right) \frac { d \mathbb { P } } { d t } } \\ & { \quad \cdot \quad = \frac { \partial ^ { 2 } { \mathbf r } } { \partial t ^ { 2 } } + 2 \omega \hat { { \mathbf z } } \times \frac { \partial { \mathbf r } } { \partial t } + \omega ^ { 2 } \hat { { \mathbf z } } \times ( \hat { { \mathbf z } } \times { \mathbf r } _ { 0 } ) + \omega ^ { 2 } \hat { { \mathbf z } } \times ( \hat { { \mathbf z } } \times { \mathbf r } ) . } \end{array}
$$

The usual equation of motion of $P$ will contain $d ^ { 2 } \mathbb { P } / d t ^ { 2 }$ and terms dealing with the forces acting on $P$ If the equation is considered in terms of $\partial ^ { 2 } \mathbb { P } / \partial \ t ^ { 2 }$ or 0²r/0t²,or in terms of what the person at O observes, then we can replace $d ^ { 2 } \geq / d t ^ { 2 }$ on one side of the equation by $\partial ^ { 2 } \mathbb { P } / \partial t ^ { 2 }$ provided we add the terms

$$
- 2 \omega \hat { \mathbf z } \times \frac { \partial \mathbf r } { \partial t } - \omega ^ { 2 } \hat { \mathbf z } \times ( \hat { \mathbf z } \times \mathbf r _ { 0 } ) - \omega ^ { 2 } \hat { \mathbf z } \times ( \hat { \mathbf z } \times \mathbf r )
$$

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/55b9173834daa739752a9e00a6d68a67935e571b9ae73169bb42ec164612458e.jpg)  
Figure 2.12

to the other side,among the terms dealing with the forces acting on $P$ （Usually the last two terms can be neglected,since $\omega$ is small.)Again,the terms in (2.7.6) can be interpreted as being due to fctitious forces: the first is known as the Coriolis force; the final two make up the centrifugal force. An example concerning them will be given in Section 3.8.

# Problems

1. What conditions are necessary for $\ddot { \boldsymbol { r } } = [ \ddot { \boldsymbol { \mathfrak { r } } } ] ?$

2.If $\mathcal { P }$ moves in a plane and its areal velocity about $o$ is constant, show that the transverse acceleration of $\mathcal { P }$ is zero.

3. Solve the simultaneous equations

$$
\ddot { \tau } - r \dot { \theta } ^ { 2 } = 0 \quad \mathrm { a n d } \quad 2 \dot { r } \dot { \theta } + r \ddot { \theta } = 0 .
$$

What can be deduced from these?

4.A particle moves in an ellipse with semiaxes $\pmb { \mathcal { Q } }$ and $\acute { \theta }$ with constant areal velocity $\pmb { A }$ about the center of the ellipse. Find the components of its velocity and acceleration in cartesian and polar coordinates. Find also the period of the orbit.

# 2.8 The Gradient of a Scalar

Consider a scalar function of position. This may be written $\textstyle { \widehat { \xi } } ( { \mathcal { Z } } , { \mathcal { Y } } , z )$ or $f ( \underline { { r } } )$ We shall suppose that it is defined and is differentiable within the region of space with which we are concerned. If we move from $\{ x , y , z \}$ ，to $\{ { \mathfrak { x } } + \delta { \mathfrak { x } } , { \mathfrak { y } } , z \}$ ，the change in $f$ can be written $( \partial f / \partial x ) \delta x$ ,where now $\partial / \partial x$ represents conventional partial differentiation with respect to $\pmb { \mathcal { Z } }$ so that for this differentiation, $\Im$ and z are assumed to be constant. This could be written in the form

$$
\begin{array} { l } { \displaystyle \frac { \partial f } { \partial x } \delta x = \left( \frac { \partial f } { \partial x } \hat { \mathbf { i } } + \frac { \partial f } { \partial y } \hat { \mathbf { j } } + \frac { \partial f } { \partial z } \hat { \mathbf { k } } \right) \cdot \hat { \mathbf { i } } \delta x } \\ { \displaystyle ~ = \nabla f \cdot \hat { \mathbf { i } } \delta x . } \end{array}
$$

Similarly,a change from $\{ x , y , z \}$ to $\{ x , y + \delta y , z \}$ produces a change in $\pmb { \hat { \hat { \beta } } }$ of $\sqrt { f } = \Im \delta y$ and $a$ change from $\pmb { \Sigma }$ to $r + \delta \mathbf { r }$ produces $\hat { \bf { \varepsilon } }$ change $\nabla f \cdot \delta \mathbf { \boldsymbol { \mathbf { \mathbf { \mathit { z } } } } }$

The vector with components $( \partial f / \partial x , \partial f / \partial y , \partial f / \partial z )$ is called the gradient f $f$ It is written as “grad $f ^ { - 1 7 }$ or $\sqrt [ 6 ] { 7 } ) ^ { 5 / 7 }$ ；the symbol $^ { 6 6 } \nabla ^ { \mathfrak { n } }$ is called “Nabla”or “Del.” Since

$$
\frac { \partial } { \partial x } ( f _ { 1 } + f _ { 2 } ) = \frac { \partial f _ { 1 } } { \partial x } + \frac { \partial f _ { 2 } } { \partial x }
$$

there is no difficulty in establishing that $\nabla f$ obeys the vector law of addition;   
hence it is definitely a vector.

Consider a curve $C$ and the values that $f$ takes along this curve. The rate of change of $f$ along $C$ with respect to arc length $\pmb { \mathscr { s } }$ is

$$
\begin{array} { c } { \frac { d f } { d s } = \frac { \partial f } { \partial x } \cdot \frac { d x } { d s } + \frac { \partial f } { \partial y } \cdot \frac { d y } { d s } \div \frac { \partial f } { \partial z } \cdot \frac { d z } { d s } } \\ { = \widehat { \tau } \cdot \nabla f , } \end{array}
$$

from Section 2.5. Consider a surface with equation ${ \mathfrak { f } } \{ x , y , z \} = \cos$ nstant. Along any line on the surface df $\beta s = 0$ so that $\hat { \boldsymbol { \mathfrak { F } } } \cdot \nabla \hat { \boldsymbol { \mathfrak { f } } } = \mathbb { 0 }$ Therefore $\nabla f$ is perpendicular to the surface.

$\overrightarrow { V } f$ is an example of a field vector, i.e., a vector that is a function of position.

# Problems

1. Prove that $\sqrt { 3 } r ^ { n } = \pi z ^ { n - 2 }$ .

2. Show that the components of $\nabla$ in two-dimensional polar coordinates are

$$
\frac { \partial } { \partial \mathbf { r } } \quad \mathtt { a } \_ { \mathtt { a } } \_ { \mathtt { d } } \_ { \mathtt { r } } \cdot \frac { \partial } { \partial \theta } .
$$

3. Find the components of $\overrightarrow { \mathbf { V } }$ in cylindrical coordinates $\{ \uparrow , \phi , z \}$ and in spherical polar coordinates $\{ \mathfrak { F } , \hat { \mathfrak { G } } , \hat { \phi } \}$

4.If $f$ and $\mathscr { G }$ are scalar functions of $\pmb { \Sigma }$ show that

$$
\nabla \left( { \frac { f } { g } } \right) = { \frac { g \nabla f - f \nabla g } { g ^ { 2 } } } .
$$

# 2.9Spherical Trigonometry

We conclude this chapter with a short account of basic spherical trigonometry approached through vectors.

Spherical trigonometry is set on the surface of a sphere that is assumed to have unit radius.All lines with which we are concerned are portions of great circles. If two points are not at opposite ends of a diameter, then there is one and only one great circle passing through them.A spherical triangle ABCis defined by three points on the surface of the sphere, the sides of the triangle being the appropriate parts of the great circles through the points. If $o$ is the center of the sphere,

$$
\angle B O C = a , \angle C O A = b , \mathrm { a n d } \angle A O B = c .
$$

The angle between the planes $A O B$ and $A O C$ is $A$ and $B$ and $C$ are similarly defined.

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/f0b1e106941e2acfd74e4eadf6a6eee454b7c3afc8e0407d1487cfa0557ec120.jpg)  
Figure 2.13

Let $\hat { { \bf 2 } } , \hat { { \bf 3 } } ,$ and $\hat { \bf k }$ be the unit vectors ( $\mathtt { a o t }$ mutually perpendicular) along OA, OB,and OC. $\{ \widehat { \tt 3 } \times \widehat { \tt 3 } \}$ is a vector with magnitude sin $c$ and direction perpendicular to the plane AOB.Similarly, $\{ \hat { \mathbf { 1 } } \times \hat { \mathbf { k } } \}$ has magnitude sin $b$ and direction perpendicular to the plane AOC.So the angle between these two vectors is $A$ Hence

$$
( { \hat { \bf { i } } } \times { \hat { \bf { j } } } ) \cdot ( { \hat { \bf { i } } } \times { \hat { \bf { k } } } ) = \sin { \theta } \sin { c } \cos { A } .
$$

But

$$
\begin{array} { r l } & { ( \hat { \mathbf { \mathfrak { z } } } \times \hat { \mathbf { \mathfrak { z } } } ) \cdot ( \hat { \mathbf { \mathfrak { z } } } \times \hat { \mathbf { \mathfrak { z } } } ) = \hat { \mathbf { \mathfrak { z } } } \cdot [ \hat { \mathbf { \mathfrak { z } } } \times ( \hat { \mathbf { \mathfrak { z } } } \times \hat { \mathbf { \mathfrak { z } } } ) ] } \\ & { \qquad = \hat { \mathbf { \mathfrak { z } } } \cdot [ \hat { \mathbf { \mathfrak { z } } } ( \hat { \mathbf { \mathfrak { z } } } \cdot \hat { \mathbf { \mathfrak { x } } } ) - \hat { \mathbf { \mathfrak { z } } } ( \hat { \mathbf { \mathfrak { z } } } \cdot \hat { \mathbf { \mathfrak { z } } } ) ] } \\ & { \qquad = ( \hat { \mathbf { \mathfrak { z } } } \cdot \hat { \mathbf { \mathfrak { x } } } ) - ( \hat { \mathbf { \mathfrak { z } } } \cdot \hat { \mathbf { \mathfrak { x } } } ) ( \hat { \mathbf { \mathfrak { z } } } \cdot \hat { \mathbf { \mathfrak { z } } } ) } \\ & { \qquad = \cos \alpha - \cos { c } \cos { \hat { \mathfrak { z } } } . } \end{array}
$$

Hence

$$
\cos a = \cos { \dot { b } } \cos c + \sin b \sin c \cos A .
$$

By the definition of the vector product,

$$
\begin{array} { r l } & { \sin A = \frac { \left\{ \left( \hat { \mathbf { 1 } } \times \hat { \mathbf { j } } \right) \times \left( \hat { \mathbf { 1 } } \times \hat { \mathbf { k } } \right) \right\} } { \left\{ \hat { \mathbf { 1 } } \times \hat { \mathbf { j } } \right\} \left\{ \mathbf { 1 } \times \hat { \mathbf { k } } \right\} } } \\ & { \qquad = \frac { \left\{ - \hat { \mathbf { 1 } } \left[ \hat { \mathbf { j } } , \hat { \mathbf { 1 } } , \hat { \mathbf { k } } \right] + \hat { \mathbf { j } } \left[ \hat { \mathbf { 1 } } , \hat { \mathbf { i } } , \hat { \mathbf { k } } \right] \right\} } { \sin b \sin c } . } \\ & { \qquad = \frac { \left\{ \hat { \mathbf { 1 } } , \hat { \mathbf { j } } , \hat { \mathbf { k } } \right\} } { \sin b \sin c } . } \end{array}
$$

Hence

$$
{ \frac { \sin A } { \sin a } } = { \frac { \sin B } { \sin { \mathfrak { z } } } } = { \frac { \sin C } { \sin c } } = { \frac { 6 \operatorname { v o l } ( O A B C ) } { \sin a \sin b \sin c } } .
$$

These equations are the fundamental formulas of spherical trigonometry. Two other formulas are frequently used; they can be derived from (2.9.1） and (2.9.2) by elementary trigonometry. Theyare

$$
\sin a \cos B = \cos b \sin c - \sin b \cos c \cos A
$$

and

$$
\cos a \cos C = \sin a \cot \dot { b } - \sin C \cot \ddot { B } .
$$

Asan illustration of the use of these formulas,consider the problem of expressing right ascension and declination in terms of celestial longitude and latitude.

Let $X$ be a star at $\{ \alpha , \delta \}$ or $\{ \lambda , \beta \}$ Let $\mathfrak { N }$ and $\mathcal { P }$ be the celestial north pole and the north pole of the ecliptic,and let the great circles through these points and $X$ meet the celestial equator and ecliptic in $A$ and $\mathcal { B }$ ,respectively, as shown in Figure 2.14,where the great circles through $P { \ C } _ { \frac { 1 } { 2 } }$ and $N { \mathfrak { O } }$ are also drawn. $\cos 3$ is the pole of the great circle $\mathcal P N$ so the angles $P M \cap$ and $\frac { \pi } { 2 } \int \frac { \pi } { 2 } d \Omega$ are both equal to $9 0 ^ { \circ }$

Now

$$
\begin{array} { r } { \mathfrak { P A } = \angle \mathfrak { P N A } = \alpha } \end{array}
$$

so

$$
\angle P N X = 9 0 ^ { \circ } \div \alpha .
$$

Similarly,

$$
\angle \widehat { N } \widehat { P } \bar { X } = 9 0 ^ { \circ } - \lambda .
$$

AIso,

$$
\begin{array} { l } { { \bar { P } \tilde { i } \tilde { \mathcal { Y } } = \varepsilon , } } \\ { { \bar { N } \bar { X } = 9 0 ^ { \circ } - \delta , } } \end{array}
$$

and

$$
\mathcal { P } \ X _ { 4 } = 9 0 ^ { \circ } - \beta .
$$

![](../chunks/Fundamentals of Celestial Mechanics (John M. A. Danby) (z-library.sk, 1lib.sk, z-lib.sk)_part_0001_pages_0001-0040/ocr/images/04228e4bb06c0c0dd00f7d33873db9cabd6fece480dd232595d0baf6dd73ab5d.jpg)  
Figure 2.14

Applying the formulas (2.9.1), (2.9.2),and (2.9.3) to the triangle PNX, we have

$$
\cos P X = \cos \beta X \cos P \mathcal { N } + \sin \beta X \sin P \mathcal { N } \cos P N X ,
$$

$$
\sin { P X } \sin { P P X } = \sin { P X } \sin { P N X } ,
$$

$$
\begin{array} { c } { \sin \beta = \sin \delta \cos \epsilon - \cos \delta \sin \epsilon \sin \alpha , } \\ { \cos \beta \cos \lambda = \cos \delta \cos \alpha , } \\ { \cos \beta \sin \lambda = \sin \delta \sin \epsilon + \cos \delta \cos \epsilon \sin \alpha . } \end{array}
$$

These expressions can also be found quickly without using spherical trigonometry. If we take axes, $\hat { \bar { \mathbf { 1 } } } , \hat { \bar { \mathbf { 3 } } } ,$ and $\hat { \bf k }$ withipointing toward $m$ and $\hat { \bf z }$ toward

N,and let these be changed to I, m,and n by a rotation about Op through the angle ε,so that n points toward P, m lies in the ecliptic (it is the summer solstice) and $\hat { \mathfrak { z } } = \hat { \mathfrak { z } }$ then

$$
\begin{array} { r } { \hat { \mathbf { m } } = \hat { \mathbf { j } } \cos \epsilon + \hat { \mathbf { k } } \sin \epsilon , \quad } \\ { \hat { \mathbf { n } } = - \hat { \mathbf { j } } \sin \epsilon + \hat { \mathbf { k } } \cos \epsilon . } \end{array}
$$

But the components of OX along these two sets of axes are

$$
( \cos \delta \cos \alpha , \cos \delta \sin \alpha , \sin \delta )
$$

and

$$
( \cos \beta \cos \lambda , \cos \beta \sin \lambda , \sin \beta ) ,
$$

and equations (2.9.5) follow at once. So, although it is useful to be familiar with the methods of spherical trigonometry,you should be on the lookout for methods that are quicker and perhaps safer.

Rotation matrices can often be used in preference to spherical trigonometry, especially in computations. The following is an elegant derivation of several formulas of spherical trigonometry using rotation matrices.(I am indebted to B.G. Marsden for this method.） First, make certain that you thoroughly understand the matrices. (See Appendix B.) For the triangle of Figure 2.13, start with axes such that the xy-plane is in the plane OBCand the x-axis points along OB.The sequence of rotations:R(a),P(-C),R(-b),will result in the new x-axis pointing along OA with the new y-axis on the great circle AC. The identical result can be generated using the rotations: P(B),R(c),P(A-π).So we have the identity

$$
R ( - b ) { \mathcal { P } } ( - C ) R ( a ) = P ( A - \pi ) R ( c ) P ( B ) .
$$

Substitute,multiply the matrices,and then compare components!

# Problem

Using the triangle PNX,derive expressions for Q and δ in terms of X and β. Check these expressions, using the alternative method indicated above.
