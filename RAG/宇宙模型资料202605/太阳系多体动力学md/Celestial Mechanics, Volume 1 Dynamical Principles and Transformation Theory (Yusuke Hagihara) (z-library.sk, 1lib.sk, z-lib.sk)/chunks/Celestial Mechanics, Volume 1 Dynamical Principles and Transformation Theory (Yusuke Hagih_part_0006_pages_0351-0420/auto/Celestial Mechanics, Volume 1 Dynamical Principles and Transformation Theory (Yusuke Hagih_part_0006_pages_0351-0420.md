Thus we have six functions

$$
H , H _ { 1 } , H _ { 2 } , H _ { 3 } , \Phi , \Pi
$$

that are mutually in involution.

A distinguished function should be in involution with any function belonging to the same group. Let $f$ be such a function. It should satisfy

$$
( H _ { 4 } , f ) = ( H _ { 5 } , f ) = ( H _ { 6 } , f ) = ( H _ { 7 } ^ { \prime } , f ) = ( H _ { 8 } ^ { \prime } , f ) = ( H _ { 9 } ^ { \prime } , f ) = 0 .
$$

We have two functions

$$
\begin{array} { l } { f = \Psi = \frac 1 2 ( H _ { 1 } ^ { 2 } + H _ { 2 } ^ { 2 } + H _ { 3 } ^ { 2 } ) \qquad \mathrm { a n d } } \\ { \qquad \quad } \\ { f = \Pi = ( M H _ { 4 } - H _ { 7 } ^ { \prime } ) ^ { 2 } + ( M H _ { 5 } - H _ { 8 } ^ { \prime } ) ^ { 2 } + ( M H _ { 6 } - H _ { 9 } ^ { \prime } ) ^ { 2 } } \end{array}
$$

satisfying these equations. The two functions $\Psi$ and $\Pi$ together with $H$ are the required three distinguished functions.Hence, the six functions mutually in involution are $H , H _ { 1 } , H _ { 2 } , H _ { 3 } , \Psi , \Pi$ (Hagihara, 1938).

On the other hand,according to Lie's theorem, the order of a system of differential equations of order $2 m$ is reduced (when there are $\boldsymbol { \mathscr { p } }$ integrals mutually in involution） to order $2 ( m - { p } )$ .Hence, the order of the differential equations of the $_ n$ -body problem is reduced to $6 n - 1 2$ ， because there exist six integrals mutually in involution.Note that the number of the known Eulerian integrals is ten.This difference between twelve and ten is explained by the existence of two other integrals: the integral for the elimination of time (Section 4.4),and the integral for the elimination of nodes discovered by Jacobi (l842,1844；see also Section 5.4).

In the plane $_ { n }$ -body problem,the order of the differential equations is $_ { 4 n }$ .There are six corresponding classical integrals:

$$
H = T - U , \qquad H _ { 1 } = - \sum _ { j = 1 } ^ { n } y _ { 2 j - 1 } , \qquad H _ { 2 } = - \sum _ { j = 1 } ^ { n } y _ { 2 j } ,
$$

$$
\begin{array} { l } { { \displaystyle H _ { 6 } = \sum _ { j = 1 } ^ { n } \big ( x _ { 2 j } y _ { 2 j - 1 } - x _ { 2 j - 1 } y _ { 2 j } \big ) , } } \\ { ~ } \\ { { \displaystyle H _ { 7 } = \sum _ { j = 1 } ^ { n } \big ( m _ { 2 j - 1 } x _ { 2 j - 1 } - y _ { 2 j - 1 } t \big ) , } } \\ { { \displaystyle H _ { 8 } = \sum _ { j = 1 } ^ { n } \big ( m _ { 2 j } x _ { 2 j } - y _ { 2 j } t \big ) . } } \end{array}
$$

The function group is of order $r = 6$ .Table 2 gives the matrix repre-sentation of this function group.

TABLE 2   

<table><tr><td></td><td>H</td><td>H</td><td>H</td><td>H6</td><td>H</td><td>H</td></tr><tr><td>H</td><td>0</td><td>0</td><td>0</td><td>0</td><td>0</td><td>0</td></tr><tr><td>H</td><td>0</td><td>0</td><td>0</td><td>-H2</td><td>-M</td><td>0</td></tr><tr><td>H2</td><td>0</td><td>0</td><td>0</td><td>H</td><td>0</td><td>-M</td></tr><tr><td>H6</td><td>0</td><td>H</td><td>-H</td><td>0</td><td>H</td><td>-H7</td></tr><tr><td>H</td><td>0</td><td>M</td><td>0</td><td>-Hg</td><td>0</td><td>0</td></tr><tr><td>H</td><td>0</td><td>0</td><td>M</td><td>H</td><td>0</td><td>0</td></tr></table>

The order of the determinant formed of this matrix is four; therefore $m = 2$ . That is, there are four independent functions mutually in involution and two distinguished functions.The functions mutually in involution are $H , H _ { 1 } , H _ { 2 } , X _ { 3 }$ ； and the distinguished functions are $H$ and $X _ { i }$ where

$$
X = M H _ { 6 } - H _ { 2 } H _ { 7 } + H _ { 1 } H _ { 8 } .
$$

Thus,the order of the differential equations is reduced to $4 n - 8$ The order is reduced by eight, but there are only six known classical integrals；this difference is due to two integrals:the integral for the elimination of time,and the integral expressing the fact that the longi-tude of perihelion appears only in the difference of longitudes (Hagihara, 1944).

It is known that with cyclic coordinates,a Hamiltonian system possesses an integral common to the whole class of potential functions. Schmeidler (l963) obtained a general criterion for the existence of such an integral, on the basis of Lie's idea of transformation groups and function groups.He applied this criterion to problems of stellar dynamics and showed that the integral could not exist (see Chapter 14).

# 4.14PARAMETRIZATION

The classical reduction of the differential equations for a dynamical system described in the foregoing sections can be carried out owing to the existence of a function group of the first integrals (Section 4.10). By using the Hamiltonian matrices (Sections l.18-l.22) van Kampen and Wintner (l938） treated a more general problem—in which known functions or hypersurfaces in the phase space cannot be represented by first integrals -by substituting invariantive relations for the first integrals (Section 4.6).In particular, the known hypersurfaces need not constitute a continuous family but may be isolated.The restricted three-body problem can be reduced by substituting for these first integrals certain combinations ofcomplicated invariantive-relations systems.This substitution is based on a distinct idea of the parametrization of an invariantiverelations system.The parametrization is symmetric with respect to the coordinates and momenta. Thus the reduction has a formal advantage when an invariantive relation is a first integral; it can be carried out by canonical transformations,as has been shown in Sections l.18-1.22.

A matrix with $k$ rows and $\iota$ columns is called a $( k , l )$ -matrix.The product $\boldsymbol { C D }$ ofa $( k , l )$ -matrix $C$ and an $( l , m )$ -matrix $_ D$ is a $( k , m )$ -matrix. The transposed matrix of a $( k , l )$ -matrix is an $( l , k )$ -matrix. A $( k , 1 ) \cdot$ matrix is a $k \mathrm { . }$ -vector.A $( k , l )$ -matrix with all its $k l$ elements zero is denoted by $0 _ { l } ^ { k }$ .A zero $k .$ -vector $0 _ { 1 } ^ { k }$ is written as $0 ^ { k }$ .Total differentiation with respect to $t$ is denoted by a dot; partial differentiation with respect to $t$ is denoted by a subscript $t .$ If a scalar $c = c ( Z , t )$ is a function of $t$ and a $k$ -vector $Z = Z ( t )$ , then

$$
\dot { c } = c _ { t } + \dot { Z } ^ { \prime } \mathrm { g r a d } _ { z } c ,
$$

where the prime denotes the transposed matrix.

Consider

$$
\begin{array}{c} G = { \binom { 0 _ { n } ^ { n } } { E ^ { n } } } \quad - { E ^ { n } }  \\ { { E ^ { n } } } \quad { 0 _ { n } ^ { n } }  \end{array} ) , \quad \quad G = - G ^ { \prime } = - G ^ { - 1 } ,
$$

where $E ^ { n }$ is a unit matrix with $\boldsymbol { n }$ rows and columns,and a canonical dynamical system with $_ { n }$ degrees of freedom

$$
G \dot { X } = \operatorname { g r a d } _ { X } h , \qquad h = h ( q _ { 1 } , . . . , q _ { n } , \phi _ { 1 } , . . . , \phi _ { n } , t ) ,
$$

where

$$
X = ( x _ { 1 } , . . . , x _ { n } , x _ { n + 1 } , . . . , x _ { 2 n } ) = { \binom { Q } { P } } ,
$$

$$
Q = { \binom { q _ { 1 } } { \vdots } } = { \binom { x _ { 1 } } { \vdots } } , \qquad P = { \binom { \hbar _ { 1 } } { \vdots } } = { \binom { x _ { n + 1 } } { \vdots } } .
$$

Suppose that $\textstyle K ( X , t )$ isa $( 2 n - m )$ -vector，where $m \leq 2 n$ ，in the $( 2 n + 1 )$ -dimensional $( X , t )$ -domain; then $2 n - m$ is called the rank of $\operatorname { g r a d } _ { X } K$ .Then

$$
K = K ( X , t ) = 0 ^ { 2 n - m }
$$

determines a nonempty set $\Lambda ( t )$ in the $2 n$ -dimensional $X$ -space for a fixed $t .$ Thus $\Lambda ( t )$ is a locally $_ m$ -dimensional manifold for a fixed $t .$

We choose a $2 n$ -vector $F = F ( Y , t )$ dependent on $t _ { : }$ and an $m$ -vector $Y = ( y _ { 1 } , \dots y _ { m } )$ , such that

$$
X = F ( Y , t )
$$

is a local parametrization of the manifold $\Lambda ( t )$ and $m$ is the rank of a $( 2 n , m )$ -matrix

$$
J = J ( Y , t ) = \operatorname { g r a d } _ { Y } F .
$$

A curve

$$
X ( t ) = F ( Y ( t ) , t )
$$

in the $2 n$ dimensional phase space obeys the relation

$$
{ \dot { x } } = F _ { t } + J { \dot { Y } }
$$

(where $J = J ( Y , t ) = \operatorname { g r a d } _ { Y } { \mathcal { F } }$ is the Jacobian),as an identity in t. Thus, if every solution $X ( t )$ of (80） that lies on $\Lambda ( t )$ for a single $t$ lies on $\Lambda ( t )$ for euery $t$ ,then the $2 n - m$ relation (82) is called an invariantive system of the differential equations (80).The parametrization (83) of (80） is called an invariantive system of (80).An invariantive system does not determine $F = F ( Y , t )$ uniquely, since $m =$ rank of the Jacobian $J$ is the only restriction imposed on the choice of the parametrization (83).

If a given solution $X ( t )$ satisfies (82),then there exists just one path $Y = Y ( t )$ ， such that (83a) is satisfied in the space of the parameter vector $Y .$ 、This unique $Y ( t )$ satisfies

$$
G J { \dot { Y } } = - G F _ { t } + \operatorname { g r a d } _ { x } h
$$

identicall when (83) is substituted. Hence a unique vector $L = L ( Y , t )$ in the $( m + 1 )$ -dimensional $( Y , t )$ -space-defined so that

$$
\mathbf { \nabla } \cdot G J L = \mathbf { \nabla } - G F _ { t } + \mathbf { g r a d } _ { x } \ : h
$$

is satisfied identically by (83)- belongs to this parametrization. Thus $Y ( t )$ and $L ( Y , t )$ are uniquely determined,and

$$
\dot { Y } = L ( Y , t ) .
$$

If this system of equations has a solution $Y ( t )$ such as (83a),then and only then is $X ( t )$ the solution of the given system of equations satisfying the invariantive system (82).

# 4.15SEPARATING PARAMETRIZATION

Define an $\pmb { m }$ -vector $\pmb { R }$ and a scalar $_ r$ by

$$
\begin{array} { r } { R = R ( Y , t ) = \frac { 1 } { 2 } J ^ { \prime } G F , \qquad r = r ( Y , t ) = \frac { 1 } { 2 } F _ { t } ^ { \prime } G F ; } \end{array}
$$

then it can be proved (van Kampen and Wintner, l938) that a matrix function $J ^ { \prime } G J$ in $( Y , t )$ is represented in the $( Y , t )$ -domain by an alternating derivative,that is, the curl

$$
J ^ { \prime } G J = ( \mathrm { g r a d } _ { Y } R ) - ( \mathrm { g r a d } _ { Y } R ) ^ { \prime } ,
$$

and a vector function $J G F _ { t }$ in $( Y , t )$ is represented by

$$
J ^ { \prime } G F _ { t } = R _ { t } - \mathrm { g r a d } _ { Y } r .
$$

Since the Jacobian $\boldsymbol { J }$ isa $( 2 n , m )$ -matrix, $J ^ { \prime } G J$ is a skew-symmetric $( m , m )$ -matrix, and $2 l$ is the rank of $J ^ { \prime } G J$ for an integer $\boldsymbol { l }$ such that $2 l \le m$ . This integer $\boldsymbol { l }$ is determined uniquely by the invariantive system (82),because the matrix $N = N ( X , t ) = \operatorname* { g r a d } _ { X } K$ is a $( 2 n - m , 2 n )$ -matrix, and $N G N ^ { \prime }$ is a skew-symmetric $( 2 n - m , 2 n - m )$ -matrix.

The $m$ -vector $Y = ( y _ { 1 } , \dots y _ { m } )$ can be split into a $2 l .$ -vector $\mathbf { \nabla } \mathbf { Y } ^ { 0 }$ and an $( m - 2 l )$ -vector $\gamma *$ such that

$$
( y _ { 1 } , . . . , y _ { m } ) = Y = { \binom { Y ^ { 0 } } { Y ^ { * } } } ,
$$

$$
Y ^ { 0 } = ( y _ { 1 } , . . . , y _ { 2 l } ) , \qquad Y ^ { * } = ( y _ { 2 l + 1 } , . . . , y _ { m } ) .
$$

If there exists a (2l,2l)-matrix $S ^ { 0 } = S ^ { \circ } ( Y , t )$ such that the $( m , m )$ matrix $J ^ { \prime } G J$ is represented in the form

$$
J ^ { \prime } G J = \binom { S ^ { 0 } } { 0 _ { 2 l } ^ { m - 2 l } } \begin{array} { l l } { { 0 _ { m - 2 l } ^ { 2 l } } } \\ { { 0 _ { 2 l } ^ { m - 2 l } } } \end{array} ,
$$

then the parametrization (83) is called a separating parametrization.

If the parametrization (83) of the invariantive system (82） of the differential equation (80) is a separating parametrization, then the first $2 l$ of the $_ m$ differential equations (85) are of the form

$$
\dot { Y } ^ { \circ } = L ^ { \circ } ( Y ^ { \circ } , t ) ;
$$

that is, these $2 l$ differential equations are separated from the remaining $m - 2 l$ differential equations defining the components $Y ^ { * }$ of the $m -$ vector $Y = ( y _ { 1 } , . . . , y _ { 2 l } , y _ { 2 l + 1 } , . . . , y _ { m } )$ .The differential equations are of the form

$$
\dot { Y } ^ { \ast } = L ^ { \ast } ( Y ^ { \ast } , Y ^ { 0 } , t ) .
$$

When the solution $Y ^ { \circ } ( t )$ of the equations (89) is known, the corresponding function $Y ^ { * } ( t )$ is the solution of

$$
\dot { Y } ^ { * } = L ^ { * } ( Y ^ { * } , t ) ,
$$

where

$$
L ^ { * } ( Y ^ { * } , t ) = L ^ { * } ( Y ^ { * } , Y ^ { 0 } ( t ) , t )
$$

is a known function of $t$ and the $( m - 2 l )$ -vector $Y ^ { * }$ . Thus the equation (85) is split into two separate systems (89) and (90). The $_ m$ -vector $Y ( t )$ determined by $Y ^ { \circ } ( t )$ and $Y ^ { * } ( t )$ gives the solution (83a) of equation (80).In the case of separating parametrization, the $2 l .$ -vector $Y ^ { * }$ is determined by the system of $2 l$ differential equations forming a nondegenerate Pfaffian dynamical system (Section 1.22) of $\boldsymbol { l }$ degrees of freedom in the sense used by Birkhoff (1927).

If $S ^ { \circ }$ exists in (88),then det $S ^ { 0 } \ne 0$ and $S ^ { 0 } = - ( S ^ { 0 } ) ^ { \prime }$ ， since $J ^ { \prime } G J$ is skew-symmetric and $2 l$ is its rank. In such a case $S ^ { \circ }$ can be represented by the alternating derivative

$$
S ^ { 0 } ( Y ^ { 0 } , t ) \equiv S ^ { 0 } = ( \mathrm { g r a d } _ { Y } \circ R ^ { 0 } ) - ( \mathrm { g r a d } _ { Y ^ { 0 } } R ^ { 0 } ) ^ { \prime } ,
$$

and

$$
J ^ { \prime } G F _ { t } = \operatorname { g r a d } _ { Y } h + { \binom { R _ { t } ^ { 0 } } { 0 ^ { m - 2 l } } } \quad \begin{array} { l } { - \operatorname { g r a d } _ { Y } \circ h ^ { 0 } } \\ { 0 ^ { m - 2 l } } \end{array} \biggr ) ,
$$

where

$$
R ^ { 0 } = R ^ { 0 } ( Y ^ { 0 } , t ) , \qquad h ^ { 0 } = h ^ { 0 } ( Y ^ { 0 } , t )
$$

are both $2 l .$ vectors. In the particular case of separating parametrization, the functions $J ^ { \prime } G J$ and $J ^ { \prime } G F _ { t } - \operatorname { g r a d } _ { Y } h$ (which are functions of $t$ and the $_ m$ -vector $Y$ ）are independent of the $( m - 2 l )$ -vector $Y ^ { * }$ of (87).

It can be proved (van Kampen and Wintner, l938) that there always exists a separating parametrization of an invariantive system. Since we know

$$
J ^ { \prime } G J \dot { Y } + J ^ { \prime } G F _ { t } - \mathrm { g r a d } _ { Y } h = 0 ^ { m } ,
$$

we obtain

$$
\left[ \left( \mathrm { g r a d } _ { Y } R \right) - \left( \mathrm { g r a d } _ { Y } R \right) ^ { \prime } \right] \dot { Y } + R _ { t } - \mathrm { g r a d } _ { Y } \left( r + h \right) = 0 ^ { m } ,
$$

where $R$ and $r$ are given by (86) and

$$
h = h ( Y , t ) = h ( F ( Y , t ) , t ) .
$$

Let $\pmb { \omega }$ be an $( m + 1 )$ -ary Pfaffian

$$
\omega = R ^ { \prime } d Y + ( r + h ) d t ;
$$

then the foregoing equation (91） is equivalent to the Euler-Lagrange equations belonging to the variational problem with fixed end points

$$
\delta \int \omega \equiv \delta \int \{ [ R ( Y , t ) ] ^ { \prime } d Y + [ r ( Y , t ) + h ( Y , t ) ] d t \} = 0 .
$$

This can be expressed by

$$
\delta \int \left[ - \textstyle { \frac { 1 } { 2 } } Y ^ { 0 \prime } G ^ { 0 } d Y ^ { 0 } + h ^ { 0 } ( Y ^ { 0 } , Y ^ { * } , t ) d t \right] = 0 ,
$$

where the Pfaffian $\omega$ is represented in the $( 2 l + 1 )$ -ary normal form

$$
- { \textstyle \frac { 1 } { 2 } } Y ^ { 0 \prime } G ^ { 0 } d Y ^ { 0 } + h ^ { 0 } d t ,
$$

and

$$
G ^ { 0 } = { \binom { 0 _ { l } ^ { l } } { E _ { l } } } \quad \quad { \boldsymbol { 0 } } _ { l } ^ { l } { \bf \Phi } = - ( G ^ { 0 } ) = - ( G ^ { 0 } ) ^ { - 1 } .
$$

The Euler-Lagrange equation belonging to the new variational problem is

$$
\binom { G ^ { 0 } } { 0 _ { 2 l } ^ { m - 2 l } } \binom { 0 2 l } { m - 2 l } \binom { \dot { Y } ^ { 0 } } { \dot { Y } ^ { * } } - \binom { \mathrm { g r a d } _ { Y ^ { 0 } } h ^ { 0 } } { \mathrm { g r a d } _ { Y ^ { * } } h ^ { 0 } } = 0 ^ { m } .
$$

Hence

$$
J ^ { \prime } G J = \binom { G ^ { 0 } } { 0 _ { 2 l } ^ { m - 2 l } } \ \begin{array} { l } { 0 _ { m - 2 l } ^ { 2 l } } \\ { 0 _ { 2 l } ^ { m - 2 l } } \end{array}
$$

Thus, the condition (88) characterizing the separating parametrization is satisfied.

A canonical parametrization is a separating parametrization for which the corresponding separated system of $2 l$ differential equations-that is, the system (89)-is a canonical dynamical system of $\mathbf { \xi } _ { l }$ degrees of freedom; accordingly the equation

$$
[ ( \mathrm { g r a d } _ { \mathrm { Y } ^ { 0 } } R ^ { 0 } ) - ( \mathrm { g r a d } _ { \mathrm { Y } ^ { 0 } } R ^ { 0 } ) ^ { \prime } ] \dot { Y } ^ { 0 } = - R _ { t } ^ { 0 } + \mathrm { g r a d } _ { \mathrm { Y } ^ { 0 } } h ^ { 0 }
$$

is of the particular form

$$
G ^ { \scriptscriptstyle 0 } \dot { Y } ^ { \scriptscriptstyle 0 } = \mathrm { g r a d } _ { \scriptscriptstyle Y ^ { \scriptscriptstyle 0 } } h ^ { \scriptscriptstyle 0 } ,
$$

where

$$
\mathbf { g r a d } _ { Y ^ { * } } h ^ { 0 } = 0 ^ { m - 2 l } .
$$

Thus,the invariantive system (82) of equations (80) always admits a canonical parametrization. The construction of such canonical parametrization of a given invariantive system depends on the point transformation that transforms the Pfaffian $\omega$ into its normal form (92). This point transformation is determined by the solution of a complete system of partial differential equations (see Section 4.9). In general, the solution can be obtained not by mere quadrature but by successive approximations.

# 4.16FIRST INTEGRALS

A scalar function $g = g ( X , t )$ is a first integral of the system (80), if and only if $g = 0$ is satisfied identically by this system of differential equations. If the equation

$$
K ( X , t ) = C
$$

with a constant $( 2 n - m )$ -vector $C$ is an invariantive system for any $C$ in the $( 2 n - m )$ -dimensional domain, then $\dot { K } = 0 ^ { 2 n - m }$ is an identity because of the system of differential equations.When the invariantive system consists of only first integrals, if the $2 n - m$ first integrals repre-sented by (94), which are mutually independent, form a function group (see Section 4.1O) in the sense of Lie, then this function group contains $n - l$ independent first integrals in involution and nothing more (see Section 4.5；also see van Kampen and Wintner， 1938；Williamson, 1940). Denote by $k$ the rank of $N G N ^ { \prime }$ ，where $N = \operatorname { g r a d } _ { x } K$ It can be proved that,if （94） consists of $2 n - m$ first integrals of the canonical system (80） of $\boldsymbol { n }$ degrees of freedom，and if these first integrals are independent and form a function group, then the maximum number $j$ of the first integrals in involution that belong to the function group is equal to $2 n - m - k$ (see Sections 4.10, 4.11).

Consider the three-body problem in which $\xi _ { i } , \ \eta _ { i } , \ \zeta _ { i } \ ( i = 1 , 2 , 3 )$ are the rectangular coordinates of the mass $\mu _ { i }$ referred to the inertial system, and $\Xi _ { i } , \mathrm { H } _ { i } , Z _ { i }$ are the conjugate momenta $\mu _ { i } \dot { \xi } _ { i } , \mu _ { i } \dot { \eta } _ { i } , \mu _ { i } \dot { \zeta } _ { i }$ The system of differential equations of motion admits, besides the energy integral, nine classical integrals

$$
\begin{array} { r l } & { \begin{array} { l l l } & { \displaystyle \mathcal { F } _ { 1 } - \sum _ { i = 1 } ^ { 3 } ( \mathbb { H } \xi _ { i } - Z _ { 0 : i } ) , } & & { \displaystyle \int _ { 0 } - \sum _ { i = 1 } ^ { 3 } ( Z _ { 1 } \xi _ { i } - \mathbb { E } \xi _ { i } ) , } \\ & { \displaystyle \int _ { \xi _ { 0 } } - \sum _ { i = 1 } ^ { 3 } ( Z _ { 1 } \eta _ { i } - \mathbb { H } \xi _ { i } ) , } & & { \displaystyle \int _ { \xi _ { 1 } } - \sum _ { i = 1 } ^ { 3 } \mathbb { E } \xi _ { i } , } \end{array} } \\ & { \begin{array} { l } { \displaystyle \mathcal { F } _ { 3 } - \sum _ { i = 1 } ^ { 3 } ( \xi _ { 1 } , \eta _ { i } - \mathbb { H } , \xi _ { i } ) , } & & { \displaystyle \int _ { \xi _ { 1 } } - \sum _ { i = 1 } ^ { 3 } \mathbb { E } \xi _ { i } , } \\ & { \displaystyle \int _ { \xi _ { 1 } } - \sum _ { i = 1 } ^ { 3 } \mathbb { H } _ { \xi _ { i } } , } \end{array} } \\ & { \begin{array} { l } { \displaystyle \mathcal { F } _ { 4 } - \sum _ { i = 2 } ^ { 3 } \mathbb { H } , } \\ & { \displaystyle \int _ { \xi _ { 1 } } - \sum _ { i = 1 } ^ { 3 } ( \mu _ { i } \xi _ { i } - \mu \mathbb { E } ) , } \end{array} } \\ & { \begin{array} { l } { \displaystyle \mathcal { F } _ { 2 } - \sum _ { i = 1 } ^ { 3 } ( \mu _ { i } \xi _ { i } - \mu \mathbb { E } ) , } \\ & { \displaystyle \int _ { \xi _ { 2 } } - \sum _ { i = 1 } ^ { 3 } ( \mu _ { i } \xi _ { i } - \mu \mathbb { E } ) , } \end{array} \quad \begin{array} { l } { \displaystyle \mathcal { F } _ { 6 } - \sum _ { i = 1 } ^ { 3 } ( \mu _ { i } \eta _ { i } - \mathbb { E } \mathbb { H } \xi _ { i } ) , } \\ { \displaystyle \int _ { \xi _ { 1 } } - \sum _ { i = 1 } ^ { 3 } ( \mu _ { i } \xi _ { i } - \mu \mathbb { Z } _ { \xi _ { 1 } } ) . } \end{array} } \\ &  \begin{array} { l }  \displaystyle \int _ { \xi _ { 2 } } ( \mu _ { i } \xi _   \end{array} \end{array}
$$

The $( 2 n - m )$ -vector $\kappa$ defines a 9-vector with components $f _ { 1 } , \ldots , f _ { 9 } ,$ Thus $n = 9$ ， $m = 9$ ；hence $l = k$ $j = 9 - k$ ，where $k = 4$ Hence $l = 4$ and $j = 5$ ; these are respectively the degree of freedom and the maximum number of the first integrals in involution contained in the function group generated by $f _ { 1 } , \ldots , f _ { 9 }$ of the separated system (93). The Poisson brackets (see Sections 4.12, 4.13) are represented by

$$
\begin{array} { l } { { ( f _ { 1 } , f _ { 2 } ) = f _ { 3 } , ~ ( f _ { 2 } , f _ { 4 } ) = - f _ { 6 } , ~ ( f _ { 3 } , f _ { 4 } ) = f _ { 5 } , } } \\ { { ~ ( f _ { 2 } , f _ { 7 } ) = - f _ { 9 } , ~ ( f _ { 3 } , f _ { 7 } ) = f _ { 8 } , ~ ( f _ { 4 } , f _ { 7 } ) = \displaystyle \sum _ { i = 1 } ^ { 3 } \mu _ { i } . } } \end{array}
$$

Thus,

$$
\left| \left( f _ { \rho } , f _ { \sigma } \right) \right| = \left( \begin{array} { c c c } { { F _ { 1 } } } & { { F _ { 2 } } } & { { F _ { 3 } } } \\ { { F _ { 2 } } } & { { 0 } } & { { \displaystyle \sum _ { i = 1 } ^ { 3 } \mu _ { i } E ^ { 3 } } } \\ { { } } & { { } } & { { } } \\ { { F _ { 3 } } } & { { - \displaystyle \sum _ { i = 1 } ^ { 3 } \mu _ { i } E ^ { 3 } } } & { { O } } \end{array} \right) ,
$$

where

$$
F _ { i } = \left( - f _ { 3 i } \qquad O \qquad f _ { 3 i } \qquad - f _ { 3 i - 1 } \right) \qquad ( i = 1 , 2 , 3 ) .
$$

In the plane problem, $\zeta _ { i } = Z _ { i } = 0$ $( i = 1 , 2 , 3$ ). Hence we have five integrals $f _ { 3 } , f _ { 4 } , f _ { 5 } , f _ { 7 } , f _ { 8 }$ .Thus, $n = 6$ ， $2 n - m = 5$ and $l = k + 1$ ， $j = 5 - k$

The 5-rowed skew-symmetric matrix is of rank 4;and $k = 2 , l = 3$ ， $j = 3$

# INTEGRAL INVARIANTS

# 4.17INTEGRALS AND INTEGRAL INVARIANTS

Suppose that, in the system of differential equations

$$
{ \frac { d x _ { 1 } } { X _ { 1 } } } = { \frac { d x _ { 2 } } { X _ { 2 } } } = \cdot \cdot \cdot = { \frac { d x _ { n } } { X _ { n } } } = d t ,
$$

the functions $X _ { i }$ ，together with their derivatives,are single-valued and continuous,and do not contain $t$ explicitly. The trajectory $x _ { i } = f _ { i } ( t )$ $( i = 1 , 2 , . . . , n )$ in the $\pmb { n }$ dimensional space $( x _ { 1 } , x _ { 2 } , \ldots , x _ { n } )$ represents the solution of (95） that passes through $x _ { i } = x _ { i } ^ { 0 } ( i = 1 , 2 , . ~ . ~ . , n )$ at $t = t _ { 0 }$ . Take a subspace $E _ { p } ^ { ( 0 ) } \ { \mathsf { o f } } \ p$ dimensions in this space and suppose that the trajectory that was in this subspace $E _ { p } ^ { ( 0 ) }$ at $t = t _ { 0 }$ is transferred to a $\pmb { \mathscr { p } }$ -dimensional subspace $E _ { p }$ at time t. A homogeneous expression of degree $\pmb { \mathscr { p } }$ with regard to the differentials

$$
\omega = \sum _ { \alpha _ { 1 } , \alpha _ { 2 } , \cdot \cdot \cdot , \alpha _ { p } } A _ { \alpha _ { 1 } \alpha _ { 2 } \cdot \cdot \cdot \alpha _ { p } } d x _ { \alpha _ { 1 } } d x _ { \alpha _ { 2 } } \cdot \cdot \cdot d x _ { \alpha _ { p } }
$$

is called a Pfaffian of degree $\pmb { \mathscr { p } }$ (Section l.7). If the integral $I _ { p } = \int \omega$ taken over this Pfaffian has the same value for any $E _ { p }$ and $E _ { p } ^ { ( 0 ) }$ ,then $I _ { p }$ is called an integral invariant of degree $\pmb { \mathscr { p } }$ (Section i.2).Here an integral invariant is meant in the sense used by Poincaré.The index $\pmb { \mathscr { p } }$ can take any value between l and $_ { n }$ (Poincaré,1889,1899;Weber,1900;Goursat, 1908,1912,1915,1921；de Donder,1901,1902,1927；Cartan,1922; Schouten and van der Kulk, 1949).

It is evident from this definition that an integral invariant remains an integral invariant after any transformation of the variables. Let $f _ { 1 } , f _ { 2 } , \ldots , f _ { n - 1 }$ be $n - 1$ independent integrals of

$$
X ( f ) = X _ { 1 } { \frac { \partial f } { \partial x _ { 1 } } } + X _ { 2 } { \frac { \partial f } { \partial x _ { 2 } } } + \cdot \cdot \cdot + X _ { n } { \frac { \partial f } { \partial x _ { n } } } = 0 .
$$

Then we have $n - 1$ integrals of (95): $f _ { i } = c _ { i } ( i = 1 , 2 , . . . , n - 1 )$ and $t = f _ { n } + c _ { n }$ ，where the $c _ { i }$ are constants and $f _ { i }$ depend only on $x _ { 1 } , x _ { 2 } , \ldots , x _ { n }$ Take new variables $y _ { 1 } = f _ { 1 } , y _ { 2 } = f _ { 2 } , . . . , y _ { n - 1 } = f _ { n - 1 }$ ， $z = f _ { n }$ ； then (95) is transformed into the system

$$
{ \frac { d y _ { 1 } } { 0 } } = { \frac { d y _ { 2 } } { 0 } } = \cdot \cdot \cdot = { \frac { d y _ { n - 1 } } { 0 } } = { \frac { d z } { 1 } } = d t ,
$$

whose general integrals are

$$
y _ { 1 } = c _ { 1 } , \qquad y _ { 2 } = c _ { 2 } , . . . , \qquad y _ { n - 1 } = c _ { n - 1 } , \qquad z = t + c _ { n } .
$$

The original Pfaffian $\pmb { \omega }$ is changed to a Pfaffian $\omega$ of $d y _ { 1 } , d y _ { 2 } , . . . , d y _ { n - 1 } ;$ $d z$ ； the coefficients $A$ of the differentials in $\omega$ are now functions of $y _ { 1 } , y _ { 2 } , . . . , y _ { n - 1 }$ ， $_ z$ Hence,an integral invariant $I _ { p } = \int \omega$ is transformed to an integral invariant $\int { \varpi \frac { } { } }$ with regard to (97). If we increase $z$ by $t \mathrm { ~ - ~ } t _ { 0 }$ by keeping $y _ { 1 } , y _ { 2 } , . . . , y _ { n - 1 }$ unaltered in the subspace $E _ { p } ^ { ( 0 ) }$ ，then the points in $E _ { p } ^ { ( 0 ) }$ are displaced to the points in $E _ { p }$ . In order that $\int \varpi$ may not depend on $t$ -that is, in order that $\int \varpi \mathrm { m a y }$ be an integral invariant with regard to (97)-it is necessary and sufficient that $\eqcirc$ should be invariant under the change of variable from $z$ to $z + t$ in $\pmb { \varpi }$ in other words, that the coefficients of $\eqcirc$ should not depend on $_ z$ But the system (97) does not alter its form even if we take $n - 1$ arbitrary independent functions of $y _ { 1 } , y _ { 2 } , . . . , y _ { n - 1 }$ as our new variables. Hence, there must be an infinite number of such integral invariants of degree $\pmb { \mathscr { p } }$

As a corollary, consider $f _ { 1 } , f _ { 2 } , . . . , f _ { r } ~ ( r < n )$ ， which are integrals of (95） independent of $t _ { : }$ and a Pfaffian $\omega$ of degree $\pmb { \mathscr { p } }$ , in which the coefficients of $d f _ { 1 } , d f _ { 2 } , \ldots , d f _ { \tau }$ are functions of $f _ { 1 } , f _ { 2 } , \ldots , f _ { r }$ only. Then $\int \omega$ is an integral invariant of degree $\pmb { r }$ If, for example, $f$ is an integral of (97), then $\int$ df is an integral invariant. Further,suppose that

$$
\int { \left( \sum _ { i , j } B _ { i j } d x _ { i } d x _ { j } \right) ^ { 1 / 2 } }
$$

becomes

$$
\int \Big ( \sum _ { i , j } B _ { i j } ^ { \prime } d y _ { i } d y _ { j } \Big ) ^ { 1 / 2 }
$$

by this transformation and that $B _ { i j } ^ { \prime }$ do not depend on $z$ 、Then this is an integral invariant. Let $\int \omega _ { p }$ and $\int . \omega _ { q }$ be two integral invariants with regard to (95).In the transformation from (95) to (97),the coefficients in these integral invariants do not contain $_ z$ Hence, the situation is the same for the product, and $\int \omega _ { p } \omega _ { q }$ is also an integral invariant.

Denote the differential of (96) formally by

$$
\omega ^ { \prime } = \sum d A _ { \alpha _ { 1 } \alpha _ { 2 } \cdot \cdot \cdot \alpha _ { p } } d x _ { \alpha _ { 1 } } d x _ { \alpha _ { 2 } \cdot \cdot \cdot } d x _ { \alpha _ { p } }
$$

and call it a differential form.Put it in another form

$$
\omega ^ { \prime } = \sum B _ { \alpha _ { 1 } \alpha _ { 2 } \cdot \cdot \cdot \alpha _ { p } \alpha _ { p + 1 } } d x _ { \alpha _ { 1 } } d x _ { \alpha _ { 2 } } \cdot \cdot \cdot d x _ { \alpha _ { p } } d x _ { \alpha _ { p + 1 } } .
$$

$\mathbf { I f } \ p$ is even, then

$$
B _ { \alpha _ { \mathrm { i } } \alpha _ { 2 } \cdots \alpha _ { p } } = \frac { \partial A _ { \alpha _ { 1 } \alpha _ { 2 } \cdots \alpha _ { p } } } { \partial x _ { \alpha _ { p + 1 } } } + \frac { \partial A _ { \alpha _ { 2 } \cdots \alpha _ { p + 1 } } } { \partial x _ { \alpha _ { 1 } } } + \cdot \cdot + \frac { \partial A _ { \alpha _ { p + 1 } \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 1 } } } { \partial x _ { \alpha _ { p } } } ,
$$

and if $\pmb { \mathscr { p } }$ is odd, then

$$
B _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p + 1 } } = { \frac { \partial A _ { \alpha _ { 1 } \alpha _ { 2 } \cdot \cdot \cdot \alpha _ { p } } } { \partial x _ { \alpha _ { p + 1 } } } } - { \frac { \partial A _ { \alpha _ { 2 } \cdot \cdot \cdot \alpha _ { p + 1 } } } { \partial x _ { \alpha _ { 1 } } } } + \cdot \cdot \cdot - { \frac { \partial A _ { \alpha _ { p + 1 } \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 1 } } } { \partial x _ { \alpha _ { p } } } } .
$$

In particular, if $\omega$ is a Pfaffian of degree one,

$$
\omega = A _ { 1 } d x _ { 1 } + A _ { 2 } d x _ { 2 } + \cdot \cdot \cdot + A _ { n } d x _ { n } ,
$$

then

$$
\omega ^ { \prime } = \sum _ { i , j } \left( \frac { \partial A _ { i } } { \partial x _ { j } } - \frac { \partial A _ { j } } { \partial x _ { i } } \right) d x _ { i } d x _ { j } .
$$

Exchanging $d x _ { i } \ d x _ { j }$ with $d x _ { j } \ \delta x _ { i } - \ \delta x _ { j } \ d x _ { i }$ gives the bilinear covariant of Section 1.4,

$$
\omega ^ { \prime } = \sum _ { i , j } \Big ( \frac { \partial A _ { i } } { \partial x _ { j } } - \frac { \partial A _ { j } } { \partial x _ { i } } \Big ) ( d x _ { j } \delta x _ { i } - \delta x _ { j } d x _ { i } ) .
$$

By the above theorem,it is necessary and sufficient that $\partial A _ { i } / \partial x _ { j } - \partial A _ { j } / \partial x _ { i }$ should be independent of $z$ for this expression $\omega ^ { \prime }$ to be an integral invariant.

Let the points on a trajectory, which starts at time $t _ { 0 }$ from the subspace $E _ { \mathcal { P } + 1 } ^ { ( 0 ) }$ , embedded in an $\pmb { n }$ dimensionalspace,occupythesubspace $E _ { p + 1 }$ at time $t .$ The subspaces $E _ { p + 1 } ^ { ( 0 ) }$ and $E _ { p + 1 }$ are bounded by the subspaces $E _ { p } ^ { ( 0 ) }$ and $E _ { p }$ ,respectively.By a generalization ofStokes's theorem, the integrals $\int \omega ^ { \prime }$ extended over $E _ { p + 1 } ^ { ( 0 ) }$ and $E _ { p + 1 }$ are equal to $\int \omega$ extended over $\mathbf { } E _ { p } ^ { ( 0 ) }$ and $E _ { p }$ ，respectively. If $\int \omega$ is an integral invariant, then the integrals $\int \omega$ extended over $E _ { p } ^ { ( 0 ) }$ and $E _ { p }$ have the same value. Hence, the integrals $\int \omega ^ { \prime }$ extended over $E _ { p + 1 } ^ { ( 0 ) }$ and $E _ { p + 1 }$ havethesamevalue;thatis,theyare integral invariants. Thus,a new integral invariant ${ \cal { I } } ^ { \prime } = \int \omega ^ { \prime }$ is derived from an integral invariant with regard to (95).

The integral $\int \omega$ extended over $E _ { p }$ is called an absolute integral invariant. When and only when $E _ { p }$ is a closed space,an integral invariant over it is said to be a relative integral invariant and written as $J .$ .The above integrals $\int \omega ^ { \prime }$ ,extended over the spaces $E _ { p + 1 } ^ { ( 0 ) }$ and $E _ { p + 1 }$ ,respectively,are equal to ω extended over the closed subspaces $E _ { p } ^ { ( 0 ) }$ and $E _ { p }$ bounding $E _ { p + 1 } ^ { ( 0 ) }$ and $E _ { p + 1 } ,$ respectively. Since $\int \omega$ is a relative integral invariant by definition, $\int \omega ^ { \prime }$ is an absolute integral invariant. The necessary and sufficient condition that $\int \omega$ be a relative but not an absolute integral invariant is that $\int \omega ^ { \prime }$ should be an absolute integral invariant. Conversely, if $\int \omega ^ { \prime }$ is an absolute integral invariant, then $\int \omega$ is a relative or an absolute integral invariant.A perfect differential is zero when integrated over a closed space; therefore,an integral of the sum of the integrand $\omega$ of a relative integral invariant and any perfect differential of the same degree extended over a closed space is also a relative integral invariant.

# 4.18CONDITION FOR AN INTEGRAL INVARIANT

Let us find the condition for (96) to be an absolute integral invariant with regard to (95). Denote the integral $\int \omega$ extended over $E _ { p }$ by $I _ { p } ( t )$ It is required that $d I _ { p } / d t = 0$ in order that $\int \omega$ shall be an integral invariant. As $t$ increases by $\delta t$ ，the variable $x _ { i }$ increases to

$$
x _ { i } ^ { \prime } = x _ { i } + X _ { i } ( x _ { 1 } , . . . , x _ { n } ) \ \delta t \qquad ( i = 1 , 2 , . . . , n ) .
$$

By taking $\pmb { \mathscr { p } }$ independent variables $u _ { 1 } , u _ { 2 } , \dotsc , u _ { p }$ we can write

$$
\begin{array} { r l r } & { } & { I _ { p } ( t ) = \displaystyle { \int \sum { A _ { \alpha _ { 1 } } . . . \alpha _ { p } \frac { \partial { x _ { \alpha _ { 1 } } } } { \partial { u _ { 1 } } } . . . \frac { \partial { x _ { \alpha _ { p } } } } { \partial { u _ { p } } } d u _ { 1 } \cdot \cdot \cdot d u _ { p } } } , } \\ & { } & { \mathrm { } _ { p } ( t + \delta t ) = \displaystyle { \int \sum { A _ { \alpha _ { 1 } } ^ { \prime } . . . \alpha _ { p } \frac { \partial { x _ { \alpha _ { 1 } } ^ { \prime } } } { \partial { u _ { 1 } } } . . . \frac { \partial { x _ { \alpha _ { p } } ^ { \prime } } } { \partial { u _ { p } } } d u _ { 1 } } \cdot \cdot \cdot d u _ { p } } . } \end{array}
$$

$A ^ { \prime }$ is the result of substituting $\pmb { x } _ { i } ^ { \prime }$ for $x _ { i }$ in $A _ { i }$ . We also have

$$
{ \cal A } _ { \beta _ { 1 } \cdot \cdot \cdot \beta _ { p } } ^ { \prime } = { \cal A } _ { \beta _ { 1 } \cdot \cdot \cdot \beta _ { p } } + X ( { \cal A } _ { \beta _ { 1 } \cdot \cdot \cdot \beta _ { p } } ) \delta t ,
$$

$$
\frac { \partial x _ { \alpha } ^ { \prime } } { \partial u _ { j } } = \frac { \partial x _ { \alpha } } { \partial u _ { j } } + \ : \delta t \sum _ { i } \frac { \partial X _ { \alpha } } { \partial x _ { i } } \frac { \partial x _ { i } } { \partial u _ { j } } \qquad ( \alpha , j = 1 , . . . , \beta ) ,
$$

where

$$
X ( f ) = X _ { 1 } { \frac { \partial f } { \partial x _ { 1 } } } + X _ { 2 } { \frac { \partial f } { \partial x _ { 2 } } } + \cdot \cdot \cdot + X _ { n } { \frac { \partial f } { \partial x _ { n } } } .
$$

Hence, the coefficient of

$$
\frac { \partial x _ { \alpha _ { 1 } } } { \partial u _ { 1 } } \cdot \cdot \cdot \frac { \partial x _ { \alpha _ { p } } } { \partial u _ { p } } \delta t
$$

in $I _ { p } ( t + \delta t )$ is

$$
\begin{array} { l } { { \displaystyle { \cal X } ( A _ { \alpha _ { 1 } } . . . _ { \alpha _ { p } } ) + \sum _ { i } \Big ( A _ { i \alpha _ { 2 } } . . . _ { \alpha _ { p } } \frac { \partial { \cal X } _ { i } } { \partial x _ { \alpha _ { 1 } } } + A _ { \alpha _ { l i } } . . . _ { \alpha _ { p } } \frac { \partial { \cal X } _ { i } } { \partial x _ { \alpha _ { 2 } } } + \cdot \cdot \cdot } } \\ { { \displaystyle ~ + ~ A _ { \alpha _ { 1 } } . . . _ { \alpha _ { p - 1 } i } \frac { \partial { \cal X } _ { i } } { \partial x _ { \alpha _ { p } } } \Big ) } . } \end{array}
$$

In order that $I _ { p } ( t + \delta t ) - I _ { p } ( t )$ shall be zero, it is necessry and sufficient that the quantity (lOO) be zero for any combination of the indices $\alpha _ { 1 } , \dotsc , \alpha _ { p }$ In order that $\int \omega$ shall be a relative integral invariant with regard to (95), it is necessary that the expression (l00) integrated over a closed space of $\boldsymbol { \mathscr { p } }$ dimensions be zero.

From the integral invariant of order $_ { n }$ ，

$$
I _ { n } = \int M ( x _ { 1 } , x _ { 2 } , . . . , x _ { n } ) d x _ { 1 } d x _ { 2 } . . . d x _ { n } ,
$$

we form the expression

$$
X ( M ) + \sum _ { i = 1 } ^ { n } M { \frac { \partial X _ { i } } { \partial x _ { i } } } = 0 ,
$$

or

$$
{ \frac { \partial } { \partial x _ { 1 } } } \left( X _ { 1 } M \right) + { \frac { \partial } { \partial x _ { 2 } } } \left( X _ { 2 } M \right) + \cdot \cdot \cdot + { \frac { \partial } { \partial x _ { n } } } \left( X _ { n } M \right) = 0 .
$$

Hence,for (lOl） to be an integral invariant with regard to (95） it is necessary and sufficient for $M$ to be the last multiplier of the differential equations (95)． That is, (95) becomes a perfect differential when multiplied by $M$ on the left-hand side (Poincaré,1899).

If

$$
\int M d x _ { 1 } d x _ { 2 } \cdot \cdot \cdot d x _ { n } , \qquad \int M ^ { \prime } d x _ { 1 } d x _ { 2 } \cdot \cdot \cdot d x _ { n } ,
$$

are two integral invariants, then $M / M ^ { \prime }$ is an integral of (95). For, we obtain

$$
{ \frac { \partial } { \partial x _ { 1 } } } \left( X _ { 1 } { \frac { M } { M ^ { \prime } } } \right) + { \frac { \partial } { \partial x _ { 2 } } } \left( X _ { 2 } { \frac { M } { M ^ { \prime } } } \right) + \cdot \cdot \cdot + { \frac { \partial } { \partial x _ { n } } } \left( X _ { n } { \frac { M } { M ^ { \prime } } } \right) = 0
$$

from

$$
\begin{array} { r l } & { \frac { \partial } { \partial x _ { 1 } } \left( X _ { 1 } M \right) + \cdot \cdot \cdot + \frac { \partial } { \partial x _ { n } } \left( X _ { n } M \right) = 0 , } \\ & { } \\ & { \frac { \partial } { \partial x _ { 1 } } \left( X _ { 1 } M ^ { \prime } \right) + \cdot \cdot \cdot + \frac { \partial } { \partial x _ { n } } \left( X _ { n } M ^ { \prime } \right) = 0 . } \end{array}
$$

In particular, if $\begin{array} { r } { \sum _ { i = 1 } ^ { n } \partial X _ { i } / \partial x _ { i } = 0 } \end{array}$ ，then letting $M = 1$ gives the ndimensional volume $\int d x _ { 1 } d x _ { 2 } \cdot \cdot \cdot d x _ { n }$ as an integral invariant. A dynamical system with such a relation is called a Liouville dynamical system.Since the volume is invariant, the dynamical system is represented by an incompressible fluid motion

$$
\sum _ { i \mathop { = } 1 } ^ { n } { \frac { \partial X _ { i } } { \partial x _ { i } } } = \mathop { \mathrm { d i v } } \mathbf { X } = 0 ,
$$

where $\mathbf { x }$ is a vector with components $X _ { 1 } , X _ { 2 } , . . . , X _ { n }$

When $X _ { i }$ does not contain $t$ in the Pfaffian of order $n - 1$ ， we have

$$
\begin{array} { c } { { \Omega _ { n - 1 } = X _ { 1 } d x _ { 2 } d x _ { 3 } \cdot \cdot \cdot d x _ { n } \pm X _ { 2 } d x _ { 3 } \cdot \cdot \cdot d x _ { n } d x _ { 1 } + \cdot \cdot \cdot } } \\ { { { } } } \\ { { \pm X _ { n } d x _ { 1 } d x _ { 2 } \cdot \cdot \cdot d x _ { n - 1 } , } } \end{array}
$$

where the plus sign is taken for $\pmb { n }$ odd and the minus sign for $\scriptstyle n$ even. An integral of $X ( f ) = 0$ is also an integral of $\Omega _ { n - 1 } d f = 0 .$ since

$$
\Omega _ { n - 1 } d f = \pm \ X ( f ) \ d x _ { 1 } d x _ { 2 } \cdot \cdot \cdot d x _ { n } .
$$

Conversely, an integral of $\Omega _ { n - 1 } d f = 0$ is also an integral of $X ( f ) = 0$ · Suppose that transforming from (95) to (97） gives

$$
\Omega _ { n - 1 } = { \cal K } d y _ { 1 } d y _ { 2 } . \cdot \cdot d y _ { n - 1 } .
$$

This occurs when and only when MK does not depend on $z$ or when $\int M \Omega _ { n - 1 }$ is an integral invariant if $M \Omega _ { n - 1 }$ is a differential； that is, $\int M \Omega _ { n - 1 }$ is an integral invariant if and only if

$$
{ \frac { \partial ( M X _ { 1 } ) } { \partial x _ { 1 } } } + { \frac { \partial ( M X _ { 2 } ) } { \partial x _ { 2 } } } + \cdot \cdot \cdot + { \frac { \partial ( M X _ { n } ) } { \partial x _ { n } } } = 0 .
$$

Hence，we can obtain an integral invariant $\int M \Omega _ { n - \dot { 1 } }$ from the last multiplier of (95). Here it is assumed that $M$ and $\Omega _ { n - 1 }$ contain neither $_ z$ nor $d z$ after the transformation.

Conversely,by constructing an expression analogous to (loo） from

$$
\begin{array} { l } { { \omega _ { n - 1 } = A _ { 1 } d x _ { 2 } d x _ { 3 } \cdot \cdot \cdot d x _ { n } \pm A _ { 2 } d x _ { 3 } \cdot \cdot \cdot d x _ { n } d x _ { 1 } + \cdot \cdot \cdot } } \\ { { \ } } \\ { { \pm A _ { n } d x _ { 1 } d x _ { 2 } \cdot \cdot \cdot d x _ { n - 1 } , } } \\ { { \ } } \end{array}
$$

we obtain

$$
\begin{array} { l } { { X ( A _ { 1 } ) d x _ { 2 } \cdot \cdot \cdot d x _ { n } \pm \cdot \cdot \cdot \pm \ X ( A _ { n } ) d x _ { 1 } \cdot \cdot \cdot d x _ { n - 1 } } } \\ { { \ } } \\ { { + A _ { 1 } [ d X _ { 2 } d x _ { 3 } \cdot \cdot \cdot d x _ { n } + d x _ { 2 } d X _ { 3 } \cdot \cdot \cdot d x _ { n } + \cdot \cdot \cdot ] } } \\ { { \ } } \\ { { \pm \ A _ { 2 } [ d X _ { 3 } \cdot \cdot \cdot d x _ { n } d x _ { 1 } + d x _ { 3 } d X _ { 4 } \cdot \cdot \cdot d x _ { 1 } + \cdot \cdot \cdot ] } } \\ { { \ } } \\ { { \vdots } } \\ { { \pm \ A _ { n } [ d X _ { 1 } d x _ { 2 } \cdot \cdot \cdot d x _ { n - 1 } + d x _ { 1 } d X _ { 2 } \cdot \cdot \cdot d x _ { n - 1 } + \cdot \cdot \cdot ] , } } \end{array}
$$

where the coefficient of $d x _ { 2 } d x _ { 3 } \cdot \cdot \cdot d x _ { n }$ , for example, is

$$
X ( A _ { 1 } ) - A ( X _ { 1 } ) + A _ { 1 } H ,
$$

with

$$
A = \sum _ { i = 1 } ^ { n } A _ { i } { \frac { \partial } { \partial x _ { i } } } , \qquad H = \sum _ { i = 1 } ^ { n } { \frac { \partial X _ { i } } { \partial x _ { i } } } .
$$

Letting all coefficients equal zero, we see that the necessary and sufficient condition for $\int \omega _ { n - 1 }$ to be an integral invariant is that

$$
X ( A ( f ) ) - A ( X ( f ) ) + H A ( f ) = 0
$$

for any function $f .$

Furthermore,

$$
X ( f ) = 0 , \quad \quad A ( f ) + M f = 0 \quad \quad \mathrm { w i t h } \quad \quad M = \sum _ { i = 1 } ^ { n } { \frac { \partial A _ { i } } { \partial x _ { i } } }
$$

form a system in involution, since the necessary and sufficient condition is that

$$
X ( A ( f ) + M f ) - A ( X ( f ) ) - M X ( f ) = 0
$$

should be a consequence of $X ( f ) = 0$ ， $A ( f ) + M f = 0$ From (103), this becomes

$$
X ( M ) f - H A ( f ) = [ X ( M ) + H M ] f = 0 .
$$

The relation $X ( M ) + H M = 0$ expresses that $M$ is a last multiplier, since the derived form of $\omega _ { n - 1 }$ is $M d x _ { 1 } d x _ { 2 } \cdot \cdot \cdot d x _ { n }$

The system $X ( f ) = 0$ ， $A ( f ) + M f = 0$ is obtained by finding those integrals of $X ( f ) = 0$ that are also the multipliers for $\omega _ { n - 1 }$ . We have

$$
( \omega _ { n - 1 } f ) ^ { \prime } = \omega _ { n - 1 } ^ { \prime } f + \omega _ { n - 1 } d f = [ A ( f ) + M f ] d x _ { 1 } \cdot \cdot \cdot d x _ { n } .
$$

We obtain the equations $X ( f ) = 0 { \it \Omega }$ $A ( f ) = 0$ at the same time by finding those integrals of $X ( f ) = 0$ that satisfy also the relation $\omega _ { n - 1 } d f = 0$ .Hence, if the integral $\int \omega _ { n - 1 }$ is an integral invariant, the linear equations $( \omega _ { n - 1 } f ) ^ { \prime } = 0 , X ( \check { f } ) = 0$ form a system in involution, and consequently the equations $\omega _ { n - 1 } d f = 0 \mathrm { _ { ; } }$ ， $X ( f ) = 0$ form a complete system of linear homogeneous equations.

# 4.19INTEGRAL INVARIANTS ALONG TRAJECTORIES

Write equations (95) in the form $d x _ { i } = X _ { i }$ dt $( i = 1 , 2 , . . . , n )$ ,where $X _ { i }$ contains $x _ { 1 } , x _ { 2 } , \ldots , x _ { n }$ but not $t$ .Consider the second system

$$
d x _ { i } = \lambda X _ { i } d t \qquad ( i = 1 , 2 , . . . , n ) ,
$$

where $\lambda$ is a function of $x _ { 1 } , x _ { 2 } , \ldots , x _ { n }$ .The integrals $f _ { 1 } , f _ { 2 } , \ldots , f _ { n - 1 }$ of the first system are also the integrals of the second system. Only the nth integral $f _ { n }$ contains t. Hence, the nth integral of the second system contains $\lambda$ .The trajectory has the same form in both systems, but there is a difference in the way it is described in terms of $t .$ If the coefficients in a Pfaffian $\omega$ of order $\pmb { \mathscr { p } }$ with respect to $d f _ { 1 } , d f _ { 2 } , \ldots , d f _ { n - 1 }$ are functions $\mathrm { o f } f _ { 1 } , f _ { 2 } , \dotsc , f _ { n - 1 }$ only, then $\int \omega$ is an integral invariant whatever $\lambda$ may be. Such an integral invariant is called an integral invariant along the trajectory and denoted by $I _ { p } ^ { l }$ . We have seen that the necessary and sufficient condition for $\int \Omega$ to be an integral invariant in the transformation from (95) to (97) is that the coefficients of $\Omega$ do not depend on $_ z$ Also,whatever the factor $\lambda$ is, $\Omega$ must be independent of $z$ and $d z$ for $\int \Omega$ to be an integral invariant. That is, $\Omega$ must contain neither $z$ nor $d z$ ， for $\int \Omega$ to be an integral invariant along the trajectory.

In order that an integral invariant $\int \Omega$ with regard to (95) shall be also an integral invariant along the trajectory, it is necessary and suffcient that $\int \Omega$ should still be an integral invariant even if we write $\lambda X _ { i }$ for $X _ { i ; \ l }$ ， whatever may be the value of $\lambda$ .Write $\lambda X _ { i }$ for $X _ { i }$ in the condition (100).

In order that this rewritten condition hold foranyfunction $\lambda$ , itis necessary and sufficient to have

$$
\sum _ { i = 1 } ^ { n } A _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 1 } i } X _ { i } = 0
$$

for all combinations of $\phi - 1$ indices taken arbitrarily out of the $\pmb { n }$ indices. When we take $d \pmb { x } _ { \alpha _ { 1 } } \cdot \cdot \cdot d \pmb { x } _ { \alpha _ { p - 1 } }$ out of each term of

$$
\begin{array} { l } { { A _ { \alpha _ { 1 } } . . . . _ { \alpha _ { p } - 1 ^ { i } } d x _ { \alpha _ { 1 } } . . . d x _ { \alpha _ { p } - 1 } d x _ { i } , } } \\ { { . } } \end{array}
$$

we obtain a Pfaffian of order one

$$
\omega _ { \alpha _ { 1 } \cdot \cdot \cdot \cdot \alpha _ { p - 1 } } = \sum _ { i = 1 } ^ { n } A _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 1 } i } d x _ { i } .
$$

By taking all combinations of $\phi - 1$ out of the $_ n$ indices we obtain the Pfaffan of order one associated with

$$
\Omega = \sum A _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p } } d x _ { \alpha _ { 1 } } \cdot \cdot \cdot d x _ { \alpha _ { p } } .
$$

When we substitute (95) in these last two expressions, they are reduced to zero by reason of (lO4); that is,such equations are satisfied by a function of one variable satisfying $n - 1$ integrals $f _ { i } = c _ { i } ( i = 1 , 2 , . ~ . ~ . , n - 1 )$ ， of the system (95). Hence, $\omega _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 1 } } = 0$ is a result of

$$
d f _ { 1 } = 0 , . . . , d f _ { n - 1 } = 0 ,
$$

and the Pfaffian of order one associated with $\Omega$ is a linear homogeneous expression with respect to $d f _ { 1 } , . . . , d f _ { n - 1 }$ 、Also,(104） is the condition for an integral invariant along the trajectory,and (lOO） gives the condition for an integral invariant in general. It can be proved,as in the general case, that if $\int \Omega$ is an integral invariant along the trajectory then so is $\int \Omega ^ { \prime }$

By applying Poincaré's method we can obtain an integral invariant of order $\pmb { \mathscr { p } } - 1$ along the trajectory from a general integral invariant of order $\boldsymbol { \mathscr { p } }$ not necessarily along the trajectory. Let an integral invariant in general, not along the trajectory, be

$$
I _ { p } = \int \omega _ { p } = \int \sum A _ { \alpha _ { 1 } \cdots \alpha _ { p } } d x _ { \alpha _ { 1 } } \cdot \cdot \cdot d x _ { \alpha _ { p } } ,
$$

and put

$$
C _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 1 } } = \sum _ { i = 1 } ^ { n } A _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 1 } i } X _ { i }
$$

then it does not necessarily follow that all $C _ { \alpha _ { 1 } \cdots \alpha _ { p - 1 } }$ are zero. If we write

$$
\omega _ { p - 1 } = \sum C _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 1 } } d x _ { \alpha _ { 1 } } \cdot \cdot \cdot d x _ { \alpha _ { p - 1 } } ,
$$

then $\int \omega _ { p - 1 }$ is an integral invariant along the trajectory. That this is an integral invariant follows from the fact that the expression (l00) formed for ωp-1s

$$
X ( C _ { \alpha _ { 1 } \cdots \alpha _ { p - 1 } } ) + \sum _ { i = 1 } ^ { n } \left( C _ { i \alpha _ { 2 } \cdots \alpha _ { p - 1 } } { \frac { \partial X _ { i } } { \partial x _ { \alpha _ { 1 } } } } + \cdot \cdot \cdot + C _ { \alpha _ { 1 } \cdots \alpha _ { p - 2 ^ { i } } } { \frac { \partial X _ { i } } { \partial x _ { \alpha _ { p - 1 } } } } \right) = 0
$$

is the result of equating the expression (100) formed for $\omega _ { p }$ to zero. It is

$$
X ( C _ { \alpha _ { 1 } \cdots \alpha _ { p - 1 } } ) = \sum _ { i } X _ { i } X ( A _ { \alpha _ { 1 } \cdots \alpha _ { p - 1 } i } ) + \sum _ { i } \sum _ { j } A _ { \alpha _ { 1 } \cdots \alpha _ { p - 1 } i } X _ { j } { \frac { \partial X _ { i } } { \partial x _ { j } } } .
$$

This can be proved if we substitute in $X ( A _ { \alpha _ { 1 } } . . . { \alpha _ { p } } _ { - 1 } i )$ the expression obtained by equating the expression (l00) formed for $\omega _ { p }$ to zero, and if at the same time we substitute in $C _ { \alpha _ { 1 } \cdots \alpha _ { p - 1 } }$ the expression in terms of such $A _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 1 } i }$ Hence, $\int \omega _ { p - 1 }$ is an integral invariant. That it is also an integral invariant along the trajectory can be proved by the fact that the sum in

$$
\sum _ { i = 1 } ^ { n } C _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 2 } i } X _ { i } = \sum _ { i } \sum _ { j } A _ { \alpha _ { 1 } \cdot \cdot \cdot \alpha _ { p - 2 } j i } X _ { i } X _ { j }
$$

becomes zero by exchanging i and $j$ on the right-hand side,and equation (104).is then satisfied.

De Donder(190l)applied the theory ofintegral invariants to the theory of turbulence.

# 4.20 VARIATIONAL EQUATIONS

Let $x _ { i } = x _ { i } ^ { ( 0 ) } ( t ) ~ ( i = 1 , 2 , . ~ . ~ . , n )$ be a particular solution of the system of differential equations

$$
\frac { d x _ { i } } { d t } = X _ { i } \qquad ( i = 1 , 2 , . . . , n ) .
$$

Put $x _ { i } = x _ { i } ^ { ( 0 ) } + \xi _ { i } ( i = 1 , 2 , . . . , n )$ ， and substitute in (l05)； then we have the differential equations for the variation $\xi _ { i }$ .The system of

differential equations obtained by neglecting higher-order terms with regard to $\xi _ { i }$ ，

$$
\frac { d \xi _ { i } } { d t } = \sum _ { j = 1 } ^ { n } \frac { \partial X _ { i } } { \partial x _ { j } } \bigg | _ { x = x ^ { ( 0 ) } } \xi _ { j } \qquad ( i = 1 , 2 , . . . , n ) ,
$$

are called the variational equations. Let a solution of (105) be

$$
x _ { i } = \phi _ { i } ( t , c _ { 1 } , c _ { 2 } , . . . , c _ { p } ) ,
$$

and call a solution corresponding to $x _ { i } = x _ { i } ^ { ( 0 ) }$ with

$$
c _ { 1 } = c _ { 2 } = \cdot \cdot \cdot = c _ { p } = 0
$$

the generating solution; then (106) has particular solutions

$$
\xi _ { i } = \frac { \partial \phi _ { i } } { \partial c _ { j } } \qquad ( i = 1 , 2 , . . . , n ; j = 1 , 2 , . . . , p ) .
$$

Thus,when we know solutions of (l05) containing integration constants, we can obtain a particular solution of (lO6). If we know an integral of (105), $F ( x _ { 1 } , x _ { 2 } , . . . , x _ { n } ) =$ constant, then we have the integrals

$$
F [ \phi _ { 1 } ( t ) , \phi _ { 2 } ( t ) , . . . , \phi _ { n } ( t ) ] = \mathrm { c o n s t a n t }
$$

for (105), and

$$
F [ \phi _ { 1 } ( t ) + \xi _ { 1 } , \phi _ { 2 } ( t ) + \xi _ { 2 } , . . . , \phi _ { n } ( t ) + \xi _ { n } ] = \mathrm { c o n s t a n }
$$

for (106). From these two integrals we obtain an integral of (106)

$$
\left. { \frac { \partial F } { \partial x _ { 1 } } } \right| _ { x = x ^ { ( 0 ) } } \xi _ { 1 } + \cdot \cdot \cdot + \left. { \frac { \partial F } { \partial x _ { n } } } \right| _ { x = x ^ { ( 0 ) } } \xi _ { n } = { \mathrm { c o n s t a n t } } ,
$$

where $\partial F / \partial x _ { i } \big | _ { x = x ^ { ( 0 ) } }$ is the result of substituting $x _ { i } ^ { ( 0 ) } = \phi _ { i } ( t )$ after the differentiation.

Poincaré (l899） has found a close connection between an integral of (106) and an integral invariant with regard to (lO5) in general. Let $F ( \xi _ { 1 } , \xi _ { 2 } , . . . , \xi _ { n } ) =$ constant be an integral of (lO6). It is a homogeneous function with respect to $\xi _ { i }$ and depends on $x _ { i } . F$ can be considered linear homogeneous with respect to $\xi _ { i }$ ； if not, we can still construct a linear homogeneous expression by forming a power of expression $F$ with a suitable index. Thus,

$$
\int F ( d x _ { 1 } , d x _ { 2 } , \ldots , d x _ { n } )
$$

is an integral invariant with regard to (lO5). In fact, the integrand is supposed to be infinitely small and linear homogeneous with respect to $d x _ { i }$ Let $F _ { 0 }$ be the infinitely short arc of a curve with the values of $( x _ { 1 } , x _ { 2 } , \ldots , x _ { n } )$ and $( x _ { 1 } + \xi _ { 1 } , x _ { 2 } + \xi _ { 2 } , . . . , x _ { n } + \xi _ { n } )$ at $t = 0$ as the coordinates of the two ends, then the integral (lO7） extended over this short arc is nothing but $F ( \xi _ { 1 } , \xi _ { 2 } , . . . , \xi _ { n } )$ . Since this expression is an integral, its values on $F _ { 0 }$ and on a curve $F$ consisting of the points at time $t$ should be equal. If $F _ { 0 }$ and $F$ are both finite, we divide these curves into infinitely short arcs and extend (l07） over the corresponding arcs on each of the curves. Then the integral (lO7） extended over an arc of $\pmb { F }$ equals the integral (l07） extended over the corresponding arc of $F _ { 0 }$ Hence,(lO7) is an integral invariant.

Conversely,if (lO7） is an integral invariant of order one, then $F ( \xi _ { 1 } , \xi _ { 2 } , . . . , \xi _ { n } )$ is an integral of (106). Indeed, the integral (107), being of equal values for $F _ { 0 }$ and for $F$ , has the same value $F ( \xi _ { 1 } , \xi _ { 2 } , . . . , \xi _ { n } )$ when the ends of $F _ { 0 }$ are $x _ { i }$ and $x _ { i } + \xi _ { i }$ .But,as this is an integral invariant, it is a constant. Hence,it is an integral of (106).

Now we extend this notion to the integral invariants of order two.Take two particular solutions $\xi _ { i }$ and $\xi _ { i } ^ { \prime } ~ ( i = 1 , 2 , . . . , n )$ of the variational equations (lO6).There exists a function $F ( x _ { i } , \xi _ { i } , \xi _ { i } ^ { \prime } )$ that is independent of $t$ whatever the two particular solutions are; in other words, $\pmb { F }$ is an integral of the systems of differential equations

$$
\frac { d \xi _ { j } } { d t } = \sum _ { i = 1 } ^ { n } \frac { \partial X _ { j } } { \partial x _ { i } } \Big | _ { x = x ^ { ( 0 ) } } \xi _ { i } , \frac { d \xi _ { j } ^ { \prime } } { d t } = \sum _ { i = 1 } ^ { n } \frac { \partial X _ { j } } { \partial x _ { i } } \Big | _ { x = x ^ { ( 0 ) } } \xi _ { i } ^ { \prime } ,
$$

for $\xi _ { i }$ and $\xi _ { i : } ^ { \prime }$ , respectively. In particular, if we take $F$ to be of the form

$$
\sum _ { i , j } A _ { i j } ( \xi _ { i } \xi _ { j } ^ { \prime } - \xi _ { j } \xi _ { i } ^ { \prime } ) ,
$$

where $A _ { i j }$ are functions of $_ x$ only, then the double integral

$$
I = \int \int \sum A _ { i j } d x _ { i } d x _ { j }
$$

is an integral invariant with regard to (105).

To prove this theorem,construct for $F _ { 0 }$ an infinitely small parallelogram with its vertices at the points $x _ { i } , x _ { i } + \xi _ { i } , x _ { i } + \xi _ { i } ^ { \prime } , x _ { i } + \xi _ { i } + \xi _ { i } ^ { \prime }$ at time $t = 0$ For $\pmb { F }$ we construct another infinitely small parallelogram with its vertices at the same coordinates at time $t .$ Then $\boldsymbol { \mathit { I } }$ is equal to

$$
\sum _ { i , j } A _ { i j } ( \xi _ { i } \xi _ { j } ^ { \prime } - \xi _ { j } \xi _ { i } ^ { \prime } )
$$

and,being an integral of (l08) by our assumption, should have equal values for $\boldsymbol { F }$ and $F _ { 0 }$ .If $F$ and $F _ { 0 }$ have finite areas,we divide them into infinitely small parts and proceed in the same way as in the above case of integral invariants of order one. The value of $\boldsymbol { \mathit { I } }$ extended over any one of the parts of $F _ { 0 }$ equals the value of $\boldsymbol { \mathit { I } }$ extended over the corresponding part of $F .$ .Thus, its values for $F _ { 0 }$ and $F$ are equal. Hence, $\boldsymbol { \mathit { I } }$ is an integral invariant. The converse of this theorem can be proved in the same way as in the foregoing case.

Integral invariants of order three can be obtained from three particular solutions $\xi _ { i } , \ \xi _ { i } ^ { \prime } , \ \xi _ { i } ^ { \prime \prime }$ ， respectively，of the three systems of variational equations

$$
\frac { d \xi _ { j } } { d t } = \left. \sum _ { i } \frac { \partial X _ { j } } { \partial x _ { i } } \right| _ { x = x ^ { ( 0 ) } } \xi _ { i } , \frac { d \xi _ { j } ^ { \prime } } { d t } = \left. \sum _ { i } \frac { \partial X _ { j } } { \partial x _ { i } } \right| _ { x = x ^ { ( 0 ) } } \xi _ { i } ^ { \prime } ,
$$

$$
\frac { d \xi _ { j } ^ { \prime \prime } } { d t } = \sum _ { i } \frac { \partial X _ { j } } { \partial x _ { i } } \bigg | _ { x = x ^ { ( 0 ) } } \xi _ { i } ^ { \prime \prime } .
$$

Since these systems admit the integral

$$
\sum _ { i , j , k } A _ { i j k } { \left| \begin{array} { l l l } { \xi _ { i } } & { \xi _ { i } ^ { \prime } } & { \xi _ { i } ^ { \prime \prime } } \\ { \xi _ { j } } & { \xi _ { j } ^ { \prime } } & { \xi _ { j } ^ { \prime \prime } } \\ { \xi _ { k } } & { \xi _ { k } ^ { \prime } } & { \xi _ { k } ^ { \prime \prime } } \end{array} \right| } ,
$$

it follows that the integral

$$
\iiint \sum _ { i , j , k } A _ { i j k } d x _ { i } d x _ { j } d x _ { k } ,
$$

where $\boldsymbol { A } _ { i j k }$ are functions of $_ x$ only, is an integral invariant of (105).

In general, let there be $\pmb { \mathscr { p } }$ $\left( { \pmb { \mathscr { p } } } < { \pmb { \mathscr { n } } } \right)$ integrals of $( 1 0 6 ) \colon \Phi _ { 1 } , \Phi _ { 2 } , . . . , \Phi _ { p } \mathrm { . }$ suppose that they are functions of $_ x$ only. Denote $q$ first-order integral invariants with regard to (l05) by $\int F _ { 1 } ( d x _ { i } ) , \ldots , \int F _ { q } ( d x _ { i } )$ Then $F _ { 1 } ( \xi _ { i } ) , . . . , F _ { q } ( \xi _ { i } )$ are integrals of (l06)， linear homogeneous with respect to $\xi _ { i }$ .Takea function

$$
\Theta ( \Phi _ { 1 } , . . . , \Phi _ { p } , F _ { 1 } , . . . , F _ { q } ) \equiv \Theta [ \Phi _ { k } , F _ { l } ] ,
$$

that is linear homogeneous with respect to $F$ ; then $\Theta [ \Phi _ { k } , F _ { l } ( \xi _ { i } ) ]$ is a new integral of (l06), linear homogeneous with respect to $\xi _ { i }$ Hence, $\int \Theta [ \Phi _ { k } , F _ { l } ( d x _ { i } ) ]$ is a first-order integral invariant with regard to (105).

Similarly, let

$$
\iint F _ { 1 } ( d x _ { i } d x _ { j } ) , . . . , \iint F _ { q } ( d x _ { i } d x _ { j } )
$$

be $q$ second-order integral invariants and $\Theta [ \Phi _ { k } , F _ { l } ]$ be a function of $\Phi$ and $F .$ ， and linear homogeneous with respect to $F _ { \cdot }$ Then

$$
\Theta [ \Phi _ { k } , F _ { l } ( \xi _ { i } \xi _ { j } ^ { \prime } - \xi _ { j } \xi _ { i } ^ { \prime } ) ]
$$

is an integral of (106) and

$$
\int \int \Theta [ \Phi _ { k } , F _ { l } ( d x _ { i } d x _ { j } ) ]
$$

is a second-order integral invariant with regard to (105).

More generally, let $\int F _ { 1 }$ and $\int F _ { 2 }$ be integral invariants of order $\pmb { \mathscr { p } }$ and $q$ ， respectively， and iet $\xi _ { i } ^ { ( 1 ) } , \ldots , \xi _ { i } ^ { ( p + q ) }$ be ${ p + q }$ integrals of (106). Denote by $F _ { 1 } ^ { \prime }$ the result of substituting for the determinant formed of $\boldsymbol { \mathscr { p } }$ differentials in $F _ { 1 }$ the determinant formed of $\pmb { \mathscr { p } }$ solutions $\xi _ { i } ^ { ( 1 ) } , \ldots , \xi _ { i } ^ { ( p ) }$ Similarly, denote by $F _ { 2 } ^ { \prime }$ the result of substituting, for $q$ differentials in $F _ { 2 }$ ， $q$ solutions $\xi _ { i } ^ { ( p + 1 ) } , \ldots , \xi _ { i } ^ { ( p + q ) }$ .Then $F _ { 1 } ^ { \prime } \cdot F _ { 2 } ^ { \prime }$ is an integral of (106).We make arbitrary permutations of the ${ \pmb q }$ solutions $\xi _ { i } ^ { ( j ) }$ . If we assign a plus or minus sign according to the odd or even number of the permutations and add the result, then the sum is also an integral of (lO6). There are $( \pmb { \mathscr { p } } + \pmb { q } ) ! / ( \pmb { \mathscr { p } } ! \pmb { q } ! )$ such independent permutations. This is a determinant of order $\phi + q$ formed of $\textit { \textbf { p } } + \textit { q }$ solutions $\xi _ { i } ^ { ( 1 ) } , \ldots , \xi _ { i } ^ { ( p + q ) }$ ,and its integral is linear homogeneous. The result is an integral invariant of order ${ p + q }$ ，with regard to (lO5).This is how Poincaré constructed integral invariants of various orders.

Consider, instead of (105), the system of canonical equations

$$
\frac { d x _ { i } } { d t } = \frac { \partial F } { \partial y _ { i } } , \frac { d y _ { i } } { d t } = - \frac { \partial F } { \partial x _ { i } } \qquad ( i = 1 , 2 , . . . , n ) .
$$

The variational equations are now

$$
\begin{array} { l } { { \displaystyle \frac { d \xi _ { i } } { d t } = \sum _ { j } \frac { \partial ^ { 2 } F } { \partial y _ { i } \partial x _ { j } } \xi _ { j } + \sum _ { j } \frac { \partial ^ { 2 } F } { \partial y _ { i } \partial y _ { j } } \eta _ { j } , } } \\ { { \displaystyle \frac { d \eta _ { i } } { d t } = - \sum _ { j } \frac { \partial ^ { 2 } F } { \partial x _ { i } \partial x _ { j } } \xi _ { j } - \sum _ { j } \frac { \partial ^ { 2 } F } { \partial x _ { i } \partial y _ { j } } \eta _ { j } \qquad ( i = 1 , 2 , . . . , n ) . } } \end{array}
$$

Denote another solution of (110) by $\xi _ { i } ^ { \prime } , \eta _ { i } ^ { \prime }$ ; then,

$$
\sum _ { i = 0 } ^ { n } \frac { d } { d t } \left( \eta _ { i } ^ { \prime } \xi _ { i } - \xi _ { i } ^ { \prime } \eta _ { i } \right) = \sum _ { i = 1 } ^ { n } \left( \eta _ { i } ^ { \prime } \frac { d \xi _ { i } } { d t } - \xi _ { i } ^ { \prime } \frac { d \eta _ { i } } { d t } - \eta _ { i } \frac { d \xi _ { i } ^ { \prime } } { d t } + \xi _ { i } \frac { d \eta _ { i } ^ { \prime } } { d t } \right) = 0 .
$$

Hence, we have an integral of (llO) in the form

$$
\sum _ { i = 1 } ^ { n } { \bigl ( } \xi _ { i } \eta _ { i } ^ { \prime } - \eta _ { i } \xi _ { i } ^ { \prime } { \bigr ) } = { \mathrm { c o n s t a n t . } }
$$

If $\xi _ { i }$ and $\eta _ { i }$ are integrals of (llO), then so are $d \xi _ { i } / d t$ and $d \eta _ { i } / d t$ The integral invariant obtained in this way,

$$
I _ { 1 } = \int { ( d x _ { 1 } d y _ { 1 } + \cdot \cdot \cdot + d x _ { n } d y _ { n } ) } ,
$$

is of order two and has a very important application.

The integral formed of four sets of particular solutions

$$
\sum _ { i , j } { \left| \begin{array} { l l l l } { \xi _ { i } } & { \xi _ { i } ^ { \prime } } & { \xi _ { i } ^ { \prime \prime } } & { \xi _ { i } ^ { \prime \prime } } \\ { \eta _ { i } } & { \eta _ { i } ^ { \prime } } & { \eta _ { i } ^ { \prime \prime } } & { \eta _ { i } ^ { \prime \prime } } \\ { \xi _ { j } } & { \xi _ { j } ^ { \prime } } & { \xi _ { j } ^ { \prime \prime } } & { \xi _ { j } ^ { \prime \prime } } \\ { \eta _ { j } } & { \eta _ { j } ^ { \prime } } & { \eta _ { j } ^ { \prime \prime } } & { \eta _ { j } ^ { \prime \prime } } \end{array} \right| } = \mathrm { c o n s t a n t }
$$

is also an integral of (1lO)； the corresponding integral invariant with regard to (109),

$$
I _ { 2 } = \int \sum _ { i , j } d x _ { i } d y _ { i } d x _ { j } d y _ { j } ,
$$

is of order four, where the sum is extended over the $n ( n - 1 ) / 2$ combinations of $_ i$ and $j$ Similarly,

$$
I _ { 3 } = \int \sum _ { i , j , k } d x _ { i } d y _ { i } d x _ { j } d y _ { j } d x _ { k } d y _ { k }
$$

extended over the $n ( n - 1 ) ( n - 2 ) / 6$ combinations of $i , j , k$ is a sixthorder integral invariant.In addition,the equations (lO6） admit the integral expressed by the sum of determinants

$$
\begin{array} { l } { \displaystyle \sum _ { \alpha _ { 1 } , \cdots , \alpha _ { p } } \| \xi _ { \alpha _ { 1 } } \eta _ { \alpha _ { 1 } } \xi _ { \alpha _ { 2 } } \eta _ { \alpha _ { 2 } } \cdot \cdot \cdot \xi _ { \alpha _ { p } } \eta _ { \alpha _ { p } } \| = \mathrm { c o n s t a n t } } \\ { \displaystyle ( \alpha _ { 1 } , \alpha _ { 2 } , \ldots , \alpha _ { p } = 1 , 2 , \ldots , n ) . } \end{array}
$$

Hence,we can construct $\pmb { n }$ similar integral invariants $I _ { 1 } , I _ { 2 } , . . . , I _ { n }$ of order twice the index; they are not necessarily independent. This is how Poincaré constructed integral invariants of higher orders.

Let an integral of (105) be $\Phi ( x _ { i } , y _ { i } , t ) = $ constant. Then the equation

$$
\sum _ { i } { \frac { \partial \Phi } { \partial x _ { i } } } \xi _ { i } + \sum _ { i } { \frac { \partial \Phi } { \partial y _ { i } } } \eta _ { i } = { \mathrm { c o n s t a n t } }
$$

is an integral of (105). Since, identically,

$$
\begin{array} { r } { \sum _ { i } \frac { d } { d t } \left( \frac { \partial \Phi } { \partial x _ { i } } \right) \xi _ { i } + \sum _ { i } \frac { d } { d t } \left( \frac { \partial \Phi } { \partial y _ { i } } \right) \eta _ { i } + \sum _ { i } \frac { \partial \Phi } { \partial x _ { i } } \left( \sum _ { j } \frac { \partial ^ { 2 } F } { \partial y _ { i } \partial x _ { j } } \xi _ { j } + \sum _ { j } \frac { \partial ^ { 2 } F } { \partial y _ { i } \partial y _ { j } } \right. } \\ { \left. - \sum _ { i } \frac { \partial \Phi } { \partial y _ { i } } \left( \sum _ { j } \frac { \partial ^ { 2 } F } { \partial x _ { i } \partial x _ { j } } \xi _ { j } + \sum _ { j } \frac { \partial ^ { 2 } F } { \partial x _ { i } \partial y _ { j } } \eta _ { j } \right) : \right. } \end{array}
$$

therefore,

$$
\begin{array} { l } { \displaystyle \frac { d } { d t } \left( \frac { \partial \Phi } { \partial x _ { i } } \right) = - \sum _ { j } \frac { \partial ^ { 2 } F } { \partial y _ { i } \partial x _ { j } } \frac { \partial \Phi } { \partial x _ { j } } + \sum _ { j } \frac { \partial ^ { 2 } F } { \partial x _ { i } \partial x _ { j } } \frac { \partial \Phi } { \partial y _ { j } } , } \\ { \displaystyle \frac { d } { d t } \left( \frac { \partial \Phi } { \partial y _ { i } } \right) = - \sum _ { j } \frac { \partial ^ { 2 } F } { \partial y _ { i } \partial y _ { j } } \frac { \partial \Phi } { \partial x _ { j } } + \sum _ { j } \frac { \partial ^ { 2 } F } { \partial x _ { i } \partial y _ { j } } \frac { \partial \Phi } { \partial y _ { j } } . } \end{array}
$$

Hence, $\xi _ { i } = \partial \Phi / \partial y _ { i }$ ， $\eta _ { i } = - \partial \Phi / \partial x _ { i }$ is a solution of (106). Furthermore, if $\Phi =$ constant, $\Phi _ { 1 } =$ constant are both solutions of (105), then, by substituting the expressions for $\xi _ { i } , \eta _ { i }$ just obtained in

$$
\sum _ { i } { \frac { \partial \Phi } { \partial x _ { i } } } \xi _ { i } + \sum _ { i } { \frac { \partial \Phi } { \partial y _ { i } } } \eta _ { i } = \mathrm { c o n s t a n t } ,
$$

we obtain

$$
( \Phi , \Phi _ { 1 } ) \equiv \sum _ { i } \left( \frac { \partial \Phi } { \partial x _ { i } } \frac { \partial \Phi _ { 1 } } { \partial y _ { i } } - \frac { \partial \Phi } { \partial y _ { i } } \frac { \partial \Phi _ { 1 } } { \partial x _ { i } } \right) = \mathrm { c o n s t a n t } .
$$

This is Poisson's theorem. Since $\xi _ { i } ^ { \prime } = \partial \Phi _ { 1 } / \partial y _ { i } , \eta _ { i } ^ { \prime } = - \partial \Phi _ { 1 } / \partial x _ { i }$ constitute a particular solution of (l06), there follows the integral

$$
\sum _ { i } \left( \xi _ { i } \eta _ { i } ^ { \prime } - \xi _ { i } ^ { \prime } \eta _ { i } \right) = \mathrm { c o n s t a n t } .
$$

This is the formula (ll1） which we have just obtained. The integral invariant $I _ { 1 }$ of (112) is merely another form of Poisson's theorem.

Let $\Phi$ $, \Phi _ { 1 } , \Phi _ { 2 } , \Phi _ { 3 }$ be four integrals of (l05).Let their Jacobian with respect to $x _ { i } , ~ x _ { j } , ~ y _ { i } , ~ y _ { j }$ be $\Delta _ { i j }$ The sum $\textstyle \sum _ { i , j } \Delta _ { i j }$ of $\Delta _ { i j }$ with all possible combinations of $i , j$ is also an integral of (lO6),that is,the integral invariant $I _ { 2 }$ .By a similar correspondence we can generalize Poisson's theorem to $I _ { 2 } , I _ { 3 } , . . . , I _ { n }$

4.2IINTEGRAL INVARIANTS OF THE $\pmb { n }$ -BODY PROBLEM

The energy integral of the differential equations for the $\boldsymbol { n }$ -body problem

$$
\frac { d x _ { i } } { d t } = \frac { \partial H } { \partial y _ { i } } , \frac { d y _ { i } } { d t } = - \frac { \partial H } { \partial x _ { i } } ~ ( i = 1 , 2 , . . . , 3 n )
$$

is

$$
H = \sum _ { i = 1 } ^ { 3 n } { \frac { 1 } { 2 m _ { i } } } y _ { i } ^ { 2 } - U = \mathrm { c o n s t a n t } .
$$

Let a solution of (113) be

$$
x _ { i } = \phi _ { i } ( t ) , \qquad y _ { i } = m _ { i } \phi _ { i } ^ { \prime } ( t ) .
$$

Then another solution is written with an arbitrary constant $c$ in the form

$$
x _ { i } = \phi _ { i } ( t + c ) , ~ y _ { i } = m _ { i } \phi _ { i } ^ { \prime } ( t + c ) .
$$

A particular solution of the variational equations is

$$
\xi _ { i } = c \phi _ { i } ^ { \prime } ( t ) = c \frac { y _ { i } } { m _ { i } } , \qquad \eta _ { i } = c m _ { i } \phi _ { i } ^ { \prime \prime } ( t ) = c \frac { \partial U } { \partial x _ { i } } .
$$

Hence

$$
\xi _ { i } = \frac { y _ { i } } { m _ { i } } , \qquad \eta _ { i } = \frac { \partial U } { \partial x _ { i } } \qquad ( i = 1 , 2 , . . . , 3 n ) ,
$$

constitute a particular solution of the variational equations, and the variational equations admit an integral

$$
\sum _ { i } { \frac { y _ { i } \eta _ { i } } { m _ { i } } } - \sum _ { i } { \frac { \partial U } { \partial x _ { i } } } \xi _ { i } = \mathrm { c o n s t a n t } ,
$$

corresponding to (114). The corresponding integral invariant is

$$
\int \omega _ { 0 } = \int \sum _ { i } \left( { \frac { 1 } { m _ { i } } } y _ { i } d y _ { i } - { \frac { \partial U } { \partial x _ { i } } } d x _ { i } \right) .
$$

Similarly， the particular solutions of the variational equations corresponding to the first integrals of the center of mass,i.e.,

$$
H _ { 1 } = - \sum _ { j = 1 } ^ { n } y _ { 3 j - 2 } , \qquad H _ { 2 } = - \sum _ { j = 1 } ^ { n } y _ { 3 j - 1 } , \qquad H _ { 3 } = - \sum _ { j = 1 } ^ { n } y _ { 3 j } ,
$$

are

$$
- \sum _ { j = 1 } ^ { n } \eta _ { 3 j - 2 } , \qquad - \sum _ { j = 1 } ^ { n } \eta _ { 3 j - 1 } , \qquad - \sum _ { j = 1 } ^ { n } \eta _ { 3 j } .
$$

The corresponding integral invariants are

$$
\begin{array} { c } { { { \displaystyle \int \omega _ { 1 } = - \int \sum _ { j = 1 } ^ { n } d y _ { 3 j - 2 } , \quad \quad \displaystyle \int \omega _ { 2 } = - \int \sum _ { j = 1 } ^ { n } d y _ { 3 j - 1 } , } } } \\ { { { \displaystyle \int \omega _ { 3 } = - \int \sum _ { j = 1 } ^ { n } d y _ { 3 j } } . } } \end{array}
$$

Corresponding to the integral of angular momentum

$$
H _ { 4 } = \sum _ { j = 1 } ^ { n } { \left( { x _ { 3 j } y _ { 3 j - 1 } } - { x _ { 3 j - 1 } y _ { 3 j } } \right) } ,
$$

the particular solution of the variational equations is

$$
\sum _ { j = 1 } ^ { n } ( x _ { 3 j } \eta _ { 3 j - 1 } - y _ { 3 j } \xi _ { 3 j - 1 } - x _ { 3 j - 1 } \eta _ { 3 j } + y _ { 3 j - 1 } \xi _ { 3 j } ) ,
$$

and the corresponding integral invariant is

$$
\int \omega _ { 4 } = \int \sum _ { j = 1 } ^ { n } ( x _ { 3 j } d y _ { 3 j - 1 } - y _ { 3 j } d x _ { 3 j - 1 } - x _ { 3 j - 1 } d y _ { 3 j } + y _ { 3 j - 1 } d x _ { 3 j } ) .
$$

Similarly

$$
\begin{array} { l } { { \displaystyle \int \omega _ { 5 } = \int \sum _ { j = 1 } ^ { n } \big ( x _ { 3 j - 2 } d y _ { 3 j } - y _ { 3 j - 2 } d x _ { 3 j } - x _ { 3 j } d y _ { 3 j - 2 } + y _ { 3 j } d x _ { 3 j - 2 } \big ) } , }  \\ { { \displaystyle \int \omega _ { 6 } = \int \sum _ { j = 1 } ^ { n } \big ( x _ { 3 j - 1 } d y _ { 3 j - 2 } - y _ { 3 j - 1 } d x _ { 3 j - 2 } - x _ { 3 j - 2 } d y _ { 3 j - 1 } + y _ { 3 j - 2 } d x _ { 3 j - 1 } \big ) } . }  \end{array}
$$

We also obtain from (115), putting $i \equiv \alpha$ (mod 3), another particular solution

$$
\begin{array} { r l } & { ( t ) \ + \ c _ { \alpha } + k _ { \alpha } t , \qquad y _ { i } = m _ { i } \phi _ { i } ^ { \prime } ( t ) \ + m _ { i } k _ { \alpha } \qquad ( i = 1 , 2 , . . . , 3 n ) , } \\ & { \qquad c _ { 0 } ^ { 2 } + c _ { 1 } ^ { 2 } + c _ { 2 } ^ { 2 } = c ^ { 2 } , \qquad k _ { 0 } ^ { 2 } + k _ { 1 } ^ { 2 } + k _ { 2 } ^ { 2 } = k ^ { 2 } . } \end{array}
$$

Supposing the constants $\pmb { c }$ and $k$ to be infinitely small,we obtain the solutions of the variational equations

$$
\xi _ { \alpha } = 1 , \qquad \eta _ { \alpha } = 0 ,
$$

and

$$
\xi _ { \alpha } = t , \qquad \eta _ { \alpha } = m _ { \alpha } \qquad ( \alpha = 3 j - 2 , 3 j - 1 , 3 j ) .
$$

Corresponding to the integrals

$$
\sum _ { j = 1 } ^ { n } \eta _ { 3 j - 2 } = \mathrm { c o n s t a n t } , \qquad \sum _ { j = 1 } ^ { n } \eta _ { 3 j - 1 } = \mathrm { c o n s t a n t } , \qquad \sum _ { j = 1 } ^ { n } \eta _ { 3 j } = \mathrm { c o n s }
$$

$$
\sum _ { j = 1 } ^ { n } \eta _ { 3 j - 2 } t - \sum _ { j = 1 } ^ { n } m _ { 3 j - 2 } \xi _ { 3 j - 2 } = \mathrm { c o n s t a n t } ,
$$

$$
\sum _ { j = 1 } ^ { n } \eta _ { 3 j - 1 } t - \sum _ { j = 1 } ^ { n } m _ { 3 j - 1 } \xi _ { 3 j - 1 } = \mathrm { c o n s t a n t } ,
$$

$$
\sum _ { j = 1 } ^ { n } \eta _ { 3 j } t - \sum _ { j = 1 } ^ { n } m _ { 3 j } \xi _ { 3 j } = \mathrm { c o n s t a n t } ,
$$

of the variational equations, we obtain the respective integral invariants

$$
\begin{array} { l } { { \displaystyle \int \omega _ { 7 } = \int \sum _ { j = 1 } ^ { n } m _ { 3 j - 2 } \big ( \delta x _ { 3 j - 2 } - t \delta y _ { 3 j - 2 } - y _ { 3 j - 2 } \big ) \big ( \delta t \big ) } , }  \\ { { \displaystyle \int \omega _ { 8 } = \int \sum _ { j = 1 } ^ { n } m _ { 3 j - 1 } \big ( \delta x _ { 3 j - 1 } - t \delta y _ { 3 j - 1 } - y _ { 3 j - 1 } \delta t \big ) , } } \\ { { \displaystyle \int \omega _ { 9 } = \int \sum _ { j = 1 } ^ { n } m _ { 3 j } \big ( \delta x _ { 3 j } - t \delta y _ { 3 j } - y _ { 3 j } \delta t \big ) . } } \end{array}
$$

A very interesting integral invariant is obtained when $U$ is homo-geneous, of degree $\pmb { \mathscr { p } }$ : the Newtonian law of gravitation corresponds to $\begin{array} { r } { \mathbf { \nabla } \phi = - 1 } \end{array}$ . Equations (ll3） remain unaltered when we multiply $\scriptstyle t$ by $\lambda ^ { 2 - p }$ ， $x _ { i }$ by $\lambda ^ { 2 } , y _ { i }$ by $\lambda ^ { p }$ ,where $\lambda$ is a constant. If we take $\lambda$ to be equal to $1 + \epsilon$ (very close to l),then we have a particular solution of the variational equations

$$
\xi _ { i } = 2 \phi _ { i } - ( 2 - \hbar ) t \phi _ { i } ^ { \prime } , \eta _ { i } = \hbar p m _ { i } \phi _ { i } ^ { \prime } - ( 2 - \hbar ) m _ { i } t \phi _ { i } ^ { \prime } ,
$$

or

$$
\xi _ { i } = 2 x _ { i } - ( 2 - \phi ) t \frac { y _ { i } } { m _ { i } } , \qquad \eta _ { i } = \phi y _ { i } - ( 2 - \phi ) t \frac { \partial U } { \partial x _ { i } } .
$$

From these we derive the particular solution

$$
\begin{array} { c } { { \displaystyle \sum _ { i = 1 } ^ { 3 n } \left( 2 x _ { i } \eta _ { i } - \rlap / p y _ { i } \xi _ { i } \right) = ( 2 - \rlap / p ) t \sum _ { i = 1 } ^ { 3 n } \left( \frac { y _ { i } \eta _ { i } } { m _ { i } } - \frac { \partial U } { \partial x _ { i } } \xi _ { i } \right) } } \\ { { = \mathrm { c o n s t a n t } } } \end{array}
$$

of the variational equations.This solution cannot be obtained by differentiating any known integral of (l13). The corresponding integral invariant is

$$
\int { \omega _ { 1 0 } } = \int { \sum _ { i = 1 } ^ { 3 n } { { { \left( { 2 { x _ { i } } d { y _ { i } } - \beta { y _ { i } } d { x _ { i } } } \right) } } + { \left( { \dot { p } } - 2 \right) } t } } \int { \sum _ { i = 1 } ^ { 3 n } { \left( { \frac { { { y _ { i } } d { y _ { i } } } } { { { m _ { i } } } } - \frac { { \partial U } } { { \partial { x _ { i } } } } d { x _ { i } } } \right) } } .
$$

As this expression contains $t$ explicitly,this integral invariant is different in character from those already found. The second integral is writen

$$
\int d \left[ \sum _ { i = 1 } ^ { 3 n } \left( \frac { y _ { i } ^ { 2 } } { 2 m _ { i } } - U \right) \right] ;
$$

there is a perfect differential under the integral sign. The sum is equal to the energy constant,by (114). $\int \omega _ { 1 0 }$ is linear and is an integral extended over an arbitrary arc of a curve.Let the values of the energy integral at the two ends of the arc be $C _ { 0 }$ and $C _ { 1 }$ ，respectively. This arc is what we have called $F _ { 0 }$ in the foregoing discussion； since it is deformed continuously to $F _ { : }$ ， the values of $C _ { 0 }$ and $C _ { 1 }$ do not alter.Hence

$$
\omega _ { 1 0 } = \int \sum _ { i = 1 } ^ { 3 n } { ( 2 x _ { i } d y _ { i } - p y _ { i } d x _ { i } ) - ( p - 2 ) t ( C _ { 1 } - C _ { 0 } ) } .
$$

The first term on the right-hand side varies as $F$ is deformed, but the variation is proportional to $t .$ If $C _ { 0 } = C _ { 1 }$ at both ends of this curve, then $\int \omega _ { 1 0 }$ is a constant. If this curve is closed, then the same situation occurs. Hence, this is a relative integral invariant.But in the case of a closed curve the integral remains unaltered when we put a perfect differential under the integral sign. Adding $\sum _ { i = 1 } ^ { 3 n }$ $( x _ { i } d y _ { i } + y _ { i } d x _ { i } )$ after multiplying by a constant factor, we see that $\int \sum _ { i = 1 } ^ { 3 n } y _ { i } d x _ { i }$ and $\int \sum _ { i } ^ { 3 n } { x _ { i } } d y _ { i }$ are both relative integral invariants. We have shown how we can get an absolute integral invariant of order two from a relative integral invariant of order one.The former invariant in the present case is

$$
I _ { 1 } = \int \int \sum _ { i = 1 } ^ { 3 n } d x _ { i } d y _ { i } ,
$$

which we have already derived. The differential $\begin{array} { r } { \sum _ { i = 1 } ^ { 3 n } \left( 2 x _ { i } d y _ { i } - \phi y _ { i } d x _ { i } \right) } \end{array}$ isa perfect differential when $\hbar = - 2$ ,that is,when the attractive force is inversely proportional to the cube of the mutual distance (instead of the square as in Newtonian law),and the invariant takes the form

$$
\int \sum _ { i = 1 } ^ { 3 n } ( 2 x _ { i } d y _ { i } - p y _ { i } d x _ { i } ) = \sum _ { i = 1 } ^ { 3 n } 2 x _ { i } y _ { i } .
$$

Hence, $\sum _ { i = 1 } ^ { 3 n } 2 x _ { i } y _ { i }$ is linear with respect to $t .$ ,But this is equal to

$$
\frac { d } { d t } \sum _ { i = 1 } ^ { 3 n } m _ { i } x _ { i } ^ { 2 } ,
$$

and

$$
\sum _ { i = 1 } ^ { 3 n } m _ { i } x _ { i } ^ { 2 }
$$

is quadratic with respect to $t _ { \ast }$ , as was found by Jacobi. In general,

$$
\sum _ { i \mathop { = } 1 } ^ { 3 n } ( 2 x _ { i } d y _ { i } - / { y } y _ { i } d x _ { i } )
$$

is not a perfect differential; it equals

$$
\int \sum _ { i = 1 } ^ { 3 n } ( 2 x _ { i } d y _ { i } + y _ { i } d x _ { i } ) - 3 t ( C _ { 1 } - C _ { 0 } )
$$

in the Newtonian case, $\begin{array} { r } { p = - 1 } \end{array}$ . Even if we form Poisson's brackets using this new integral and the clasical ten integrals, there occurs no additional new integral,as can be seen from a simple computation. Indeed, this integral is not in involution with any known integral except the energy integral $H .$ ，Hence, the order of the differential equations cannot be reduced by such an integral.

# 4.22CARTAN'S EXTERIOR MULTIPLICATION

Cartan (1922,1945) used the notation

$$
\left| \begin{array} { l l } { u _ { i } } & { u _ { j } } \\ { v _ { i } } & { v _ { j } } \end{array} \right| = [ u _ { i } \quad u _ { j } ] , \qquad \left| \begin{array} { l l l } { u _ { 1 } } & { u _ { 2 } } & { u _ { 3 } } \\ { v _ { 1 } } & { v _ { 2 } } & { v _ { 3 } } \\ { w _ { 1 } } & { w _ { 2 } } & { w _ { 3 } } \end{array} \right| = [ u _ { 1 } \quad u _ { 2 } \quad u _ { 3 } ] , \ldots ,
$$

and called these (following Grassmann and Plucker) the exterior multiplication forms.With the linear forms

$$
f ( u ) = \sum _ { i = 1 } ^ { n } { a _ { i } u _ { i } } , \qquad f ^ { \prime } ( u ) = \sum _ { i = 1 } ^ { n } { a _ { i } ^ { \prime } u _ { i } } ,
$$

we obtain

$$
\left[ f ( u ) , f ^ { \prime } ( u ) \right] = \left| \begin{array} { l l } { f ( u ) } & { f ^ { \prime } ( u ) } \\ { f ( v ) } & { f ^ { \prime } ( v ) } \end{array} \right| = \sum _ { i , j } a _ { i } a _ { j } ^ { \prime } \left| \begin{array} { l l } { u _ { i } } & { u _ { j } } \\ { v _ { i } } & { v _ { j } } \end{array} \right| = \sum _ { i , j } a _ { i } a _ { j } ^ { \prime } [ u _ { i } u _ { j } ] ,
$$

if we regard this quantity as a product of two linear forms where the order of the factors in the partial products has not been altered,and such that the product of two identical variables is zero and the product of two different variables changes sign when the order of the factors is reversed. Thus, to any alternating bilinear form corresponds a form of the second degree with a single series of variables but with exterior multiplication; conversely, to any quadratic form with exterior multiplication there corresponds an alternating bilinear form.

For the case of three variables,

$$
\begin{array} { c } { { \left[ u _ { 1 } u _ { 2 } u _ { 3 } \right] = - [ u _ { 2 } u _ { 1 } u _ { 3 } ] = - [ u _ { 1 } u _ { 3 } u _ { 2 } ] = - [ u _ { 3 } u _ { 2 } u _ { 1 } ] } } \\ { { { } } } \\ { { { } } } \\ { { { } = [ u _ { 2 } u _ { 3 } u _ { 1 } ] = [ u _ { 3 } u _ { 1 } u _ { 2 } ] . } } \end{array}
$$

In general, an exterior multiplication form of order $\pmb { n }$ is writen

$$
A [ u _ { 1 } u _ { 2 } \cdot \cdot \cdot u _ { n } ] .
$$

An exterior multiplication form $[ F \Phi \Psi ]$ can be defined from exterior multiplication forms $F , \Phi , \Psi$ of arbitrary orders, the degree of the product being the sum of the degrees of the factors. The product is zero if the sum of the degrees exceeds $_ n$ .For example, from

$$
F = [ u _ { 1 } u _ { 2 } ] + [ u _ { 3 } u _ { 4 } ] + \cdot \cdot \cdot + [ u _ { 2 s - 1 } u _ { 2 s } ]
$$

we obtain

$$
\begin{array} { l } { { \displaystyle \frac { 1 } { 2 ! } [ F ^ { 2 } ] = [ u _ { 1 } u _ { 2 } u _ { 3 } u _ { 4 } ] + [ u _ { 1 } u _ { 2 } u _ { 5 } u _ { 6 } ] + \cdot \cdot \cdot + [ u _ { 2 s - 3 } u _ { 2 s - 2 } u _ { 2 s - 1 } u _ { 2 s } ] } , }  \\ { { \mathrm { } } } \\ { { \displaystyle \frac { 1 } { 3 ! } [ F ^ { 3 } ] = [ u _ { 1 } u _ { 2 } u _ { 3 } u _ { 4 } u _ { 5 } u _ { 6 } ] + \cdot \cdot \cdot , } } \end{array}
$$

$$
\begin{array} { c } { { \frac { 1 } { s ! } [ F ^ { s } ] = [ u _ { 1 } u _ { 2 } u _ { 3 } \cdot \cdot \cdot u _ { 2 s - 1 } u _ { 2 s } ] , } } \\ { { { } } } \\ { { { } } } \\ { { \frac { 1 } { + 1 ) ! } [ F ^ { s + 1 } ] = 0 . } } \end{array}
$$

From $\omega _ { \delta } = \textstyle \sum _ { i } a _ { i } \delta x _ { i }$ we derive

$$
{ \omega } _ { \delta } ^ { \prime } = \sum _ { i } [ \delta a _ { i } \ \delta x _ { i } ] = \sum _ { i , j } \Big ( \frac { \partial a _ { j } } { \partial x _ { i } } - \frac { \partial a _ { i } } { \partial x _ { j } } \Big ) [ \delta x _ { i } \ \delta x _ { j } ] .
$$

This is called the exterior derivative of the Pfaffian $\omega _ { \delta }$ . From Stokes's formula in the present notation,

$$
\int _ { { \cal C } } \omega _ { \delta } = \int \int _ { s } \omega _ { \delta } ^ { \prime } ,
$$

the necessary and sufficient condition for $\omega ^ { \prime } = 0$ is that $\omega$ should be a perfect differential.

To a bilinear form

$$
\Omega = \sum _ { i , j } a _ { i j } [ \delta x _ { i } \delta x _ { j } ]
$$

corresponds a bilinear alternating form

$$
\sum _ { i , j } a _ { i j } ( \delta x _ { i } \delta x _ { j } ^ { \prime } - \delta x _ { j } \delta x _ { i } ^ { \prime } ) .
$$

Corresponding to a trilinear form

$$
\begin{array} { r }  { { \bf \Phi } ^ { \prime } ( \delta \mathrm {  ~ \delta } \delta ^ { \prime } \mathrm {  ~ \delta } \delta ^ { \prime \prime } ) = \displaystyle \sum _ { i , j } [ \delta a _ { i j } ( \delta ^ { \prime } x _ { i } \mathrm {  ~ \delta } \delta ^ { \prime \prime } x _ { j } - \delta ^ { \prime } x _ { j } \mathrm {  ~ \delta } \delta ^ { \prime \prime } x _ { i } ) - \delta ^ { \prime } a _ { i j } ( \delta x _ { i } \mathrm {  ~ \delta } \delta ^ { \prime \prime } x _ { j } - \delta x _ { j } \mathrm {  ~ \delta } \delta ^ { \prime \prime } x } \\ { + \ \delta ^ { \prime \prime } a _ { i j } ( \delta x _ { i } \mathrm {  ~ \delta } \delta ^ { \prime } x _ { j } - \delta x _ { j } \mathrm {  ~ \delta } \delta ^ { \prime } x _ { i } ) ] , \qquad } \end{array}
$$

there is the trilinear alternating exterior derived form:

$$
\Omega _ { \delta } ^ { \prime } = \sum _ { i , j } [ \delta a _ { i j } \ \delta x _ { i } \ \delta x _ { j } ] = \sum _ { i , j , k } \left( { \frac { \partial a _ { i j } } { \partial x _ { k } } } + { \frac { \partial a _ { j k } } { \partial x _ { i } } } + { \frac { \partial a _ { k i } } { \partial x _ { j } } } \right) [ \delta x _ { i } \ \delta x _ { j } \ \delta x _ { k } ] .
$$

This is the exterior differential of $\Omega$ . If an exterior multiplication form of any degree is exteriorly derived, then the degree increases by one.A term $A [ \delta x _ { i } \ \delta x _ { j } \cdot \cdot \cdot \delta x _ { l } ]$ generates a term $[ \delta A \ \delta x _ { i } \ \delta x _ { j } \cdot \cdot \cdot \delta x _ { l } ]$ by exterior derivation.From a constant $_ m$ and an exterior multiplication form $\Omega$ ， we obtain $( m \Omega ) ^ { \prime } = [ d m \cdot \Omega ] + m \Omega ^ { \prime }$ . From two exterior multiplication forms $\Omega$ and $\Pi$ we obtain

$$
[ \Omega \Pi ] ^ { \prime } = [ \Omega ^ { \prime } \Pi ] + [ \Omega \Pi ^ { \prime } ]
$$

if the degree of $\Omega$ is even, and

$$
[ \Omega \Pi ] ^ { \prime } = [ \Omega ^ { \prime } \Pi ] - [ \Omega \Pi ^ { \prime } ]
$$

if the degree is odd. In particular,

$$
[ \Omega ^ { p } ] ^ { \prime } = \pmb { \mathscr { p } } [ \Omega ^ { p - 1 } \Omega ^ { \prime } ]
$$

if the degree of $\Omega$ is even.

The derivative of the exterior derivative $\Omega ^ { \prime }$ of an exterior multiplication form is identically zero.Cartan (l945） called this Poincaré's theorem. The term of $\Omega ^ { \prime }$ corresponding to an arbitrary term $a [ \delta x _ { 1 } \cdot \cdot \cdot \delta x _ { p } ]$ of $\Omega$ is $[ \delta a \ \delta x _ { 1 } \cdot \cdot \cdot \delta x _ { p } ]$ . If $^ { a }$ depends only on $x _ { 1 } , \dotsc , x _ { p }$ ，then the last term and its derivatives are zero. If $^ { a }$ does not depend on $x _ { 1 } , \dotsc , x _ { p } ,$ then - by a change of variables so that $^ { a }$ becomes equal to ${ \pmb x } _ { p + 1 }$ - the exterior derivative of a term $[ \delta x _ { p + 1 } \delta x _ { 1 } \cdot \cdot \cdot \delta x _ { p } ]$ is zero,because,the coefficient of this term being unity, the derivative contributes nothing toward the formation of the exterior derivative.

Conversely, if the derivative of a differential form $\Omega$ is zero, then the form $\Omega$ can be regarded as the derivative of a form $\Pi$ whose degree is one less than the degree of $\Omega$ 、To prove this theorem we refer to the lemma:if the derivative of a form $\Omega$ is zero and this form does not contain the differential $\phantom { } \delta x _ { n }$ ， then the coefficients are all independent of $x _ { n }$ . In fact a term such as $A [ \delta x _ { 1 } \delta x _ { 2 } \cdot \cdot \cdot \delta x _ { p } ]$ generates in the derivation a term $[ \delta A \ \delta x _ { 1 } \ \delta x _ { 2 } \cdot \cdot \cdot \delta x _ { p } ]$ ,or,by expansion, several terms one of which is $( \partial A / \partial x _ { n } ) [ \delta x _ { n } \ \delta x _ { 1 } \cdot \cdot \cdot \delta x _ { p } ]$ . This last term cannot cancel by combining with another term, since no term of $\Omega$ contains $\ S x _ { n }$ .Hence $\partial A / \partial x _ { n } = 0 ,$ because $\Omega ^ { \prime } = 0$

To prove the theorem, let $\Omega _ { 0 }$ be the result of substituting $x _ { n } = x _ { n } ^ { 0 }$ $\delta x _ { n } = 0$ in $\Omega$ Evidently $\Omega _ { 0 } ^ { \prime } = 0$ if $\Omega ^ { \prime } = 0$ .Suppose that the theorem is true for $n - 1$ variables and that it is possible to find a form $\Pi _ { 0 }$ con-structed with the variables $x _ { 1 } , \ldots , x _ { n - 1 }$ such that $\Pi _ { 0 } ^ { \prime } = \Omega _ { 0 }$ .Divide $\Omega$ and $\Pi$ into the parts containing $\phantom { } \delta x _ { n }$ and the parts independent of $\phantom { } \delta x _ { n }$ ， so that

$$
\Omega = \Omega _ { 1 } + [ \delta x _ { n } \Omega _ { 2 } ] , ~ \Pi = \Pi _ { 1 } + [ \delta x _ { n } \Pi _ { 2 } ] .
$$

The term containing $\phantom { } \delta x _ { n }$ in $\Pi ^ { \prime }$ is

$$
\Pi ^ { \prime } = \left[ \delta x _ { n } { \frac { \partial \Pi _ { 1 } } { \partial x _ { n } } } \right] - [ \delta x _ { n } \Pi _ { 2 } ^ { \prime } ] + \cdot \cdot \cdot .
$$

Choose $\Pi _ { 2 }$ arbitrarily and determine $\Pi _ { 1 }$ by the condition that $( i ) \ \Pi _ { 1 }$ reduces to $\Pi _ { 0 }$ for $x _ { n } = x _ { n } ^ { 0 } .$ and $( i i ) \partial \Pi _ { 1 } / \partial x _ { n } = \Pi _ { 2 } ^ { \prime } + \Omega _ { 2 } ,$ so that $\Pi _ { 1 }$ can be evaluated by quadrature. The form $\Pi ^ { \prime } - \Omega$ does not contain $\ S x _ { n }$ after similar terms are cancelled and reduces to zero if we put $x _ { n } = x _ { n } ^ { 0 }$ in the coefficients.Because of the lemma, all coefficients are independent of $x _ { n }$ .Hence the difference $\Pi ^ { \prime } - \Omega$ should be identically zero.

This theorem shows that if the exterior derivative of a linear form $\Omega$ is zero then $\Omega$ is an exact differential.If $\Omega$ is a quadratic form with three variables

$$
\Omega = P [ \delta y \delta z ] + Q [ \delta z \ \delta x ] + R [ \delta x \ \delta y ] ,
$$

then the condition

$$
\frac { \partial P } { \partial x } + \frac { \partial Q } { \partial y } + \frac { \partial R } { \partial z } = 0
$$

is necessary and sufficient in order that $\Omega$ shall be regarded as the derivative of a linear form,that is,in order that we can find three functions $A , B , C$ satisfying

$$
{ \frac { \partial C } { \partial y } } - { \frac { \partial B } { \partial z } } = P , { \frac { \partial A } { \partial z } } - { \frac { \partial C } { \partial x } } = Q , { \frac { \partial B } { \partial x } } - { \frac { \partial A } { \partial y } } = R .
$$

By the rank of a quadratic form we mean the number of quadratic terms necessary to express the quadratic form as their sum.To find the rank of a general quadratic form we need only see how many independent equations the actual equations would reduce to, if we equated all the partial derivatives of the quadratic form to zero. Similarly the rank is defined for an exterior multiplication form $\begin{array} { r } { F = \sum _ { i , j } a _ { i j } [ u _ { i } u _ { j } ] } \end{array}$ The equations obtained by equating all its partial derivatives to zero are called the associated system.Note that

$$
\frac { \partial [ u _ { 1 } u _ { 2 } ] } { \partial u _ { 1 } } = u _ { 2 } , \quad \quad \frac { \partial [ u _ { 1 } u _ { 2 } ] } { \partial u _ { 2 } } = - u _ { 1 } , \quad \quad \frac { \partial [ u _ { 1 } u _ { 2 } ] } { \partial u _ { i } } = 0
$$

If $a _ { 1 2 } \neq 0$ in the coefficients of $F ( u )$ , then

$$
\begin{array} { c } { { \displaystyle \frac { 1 } { a _ { 1 2 } } \left[ \frac { \partial F } { \partial u _ { 1 } } \frac { \partial F } { \partial u _ { 2 } } \right] = \frac { 1 } { a _ { 1 2 } } \left[ ( a _ { 1 2 } u _ { 2 } + a _ { 1 3 } u _ { 3 } + \cdot \cdot \cdot + a _ { 1 n } u _ { n } ) \right. } } \\ { { \displaystyle \phantom { \frac { 1 } { a _ { 1 2 } } } \times \left. ( a _ { 2 1 } u _ { 1 } + a _ { 2 3 } u _ { 3 } + \cdot \cdot \cdot + a _ { 2 n } u _ { n } ) \right] } } \end{array}
$$

has the same coefficients as $F$ in the terms combining $\pmb { u } _ { 1 }$ or ${ \boldsymbol { u } } _ { 2 }$ , so that

$$
F ( u ) \ - \frac { 1 } { a _ { 1 2 } } \left[ \frac { \partial F } { \partial u _ { 1 } } \frac { \partial F } { \partial u _ { 2 } } \right] \equiv F ^ { \prime } ( u )
$$

contains only $u _ { 3 } , \ldots , u _ { n }$ If in addition $a _ { 3 4 } ^ { \prime } \neq 0$ ,then

$$
F ^ { \prime } ( u ) - \frac { 1 } { a _ { 3 4 } ^ { \prime } } \left[ \frac { \partial F ^ { \prime } } { \partial u _ { 3 } } \frac { \partial F ^ { \prime } } { \partial u _ { 4 } } \right] = F ^ { \prime \prime } ( u )
$$

contains only $u _ { 5 } , \ldots , u _ { n }$ . Continuing this process, we obtain

$$
F ( u ) = \frac { 1 } { a _ { 1 2 } } \left[ \frac { \partial F } { \partial u _ { 1 } } \frac { \partial F } { \partial u _ { 2 } } \right] + \frac { 1 } { a _ { 3 4 } ^ { \prime } } \left[ \frac { \partial F ^ { \prime } } { \partial u _ { 3 } } \frac { \partial F ^ { \prime } } { \partial u _ { 4 } } \right] + \frac { 1 } { a _ { 5 6 } ^ { \prime \prime } } \left[ \frac { \partial F ^ { \prime \prime } } { \partial u _ { 5 } } \frac { \partial F ^ { \prime \prime } } { \partial u _ { 6 } } \right] + \cdot \cdot \cdot ,
$$

or

$$
F ( U ) = [ U _ { 1 } U _ { 2 } ] + [ U _ { 3 } U _ { 4 } ] + \cdot \cdot \cdot + [ U _ { 2 s - 1 } U _ { 2 s } ] ( 2 s \equiv n ) ,
$$

where

$$
\frac { \partial F } { \partial u _ { 1 } } = U _ { 1 } , \qquad \frac { \partial F } { \partial u _ { 2 } } = a _ { 1 2 } U _ { 2 } , \qquad \frac { \partial F ^ { \prime } } { \partial u _ { 3 } } = U _ { 3 } , \qquad \frac { \partial F ^ { \prime } } { \partial u _ { 4 } } = a _ { 3 4 } ^ { \prime } U _ { 4 } ,
$$

$$
{ \frac { \partial F ^ { \prime \prime } } { \partial u _ { 5 } } } = U _ { 5 } , ~ { \frac { \partial F ^ { \prime \prime } } { \partial u _ { 6 } } } = a _ { 5 6 } ^ { \prime \prime } U _ { 6 } , . ~ . ~ . .
$$

This is called the canonical form of $F .$ 、We see that the rank of $F$ should always be even. The associated system is $U _ { 1 } = U _ { 2 } = \cdot \cdot \cdot = U _ { 2 s } = 0$

If a given exterior multiplication form $\Omega$ is invariant with regard to the system of differential equations

$$
{ \frac { d x _ { 1 } } { X _ { 1 } } } = { \frac { d x _ { 2 } } { X _ { 2 } } } = \cdot \cdot \cdot = { \frac { d x _ { n } } { X _ { n } } } ,
$$

then $\Omega$ can be expressed by $n - 1$ independent integrals $y _ { 1 } , y _ { 2 } , . . . , y _ { n - 1 }$ and their differentials,by the theorem of Section 4.17.The situation is the same for the exterior derivative $\Omega ^ { \prime }$ .The system of linear differential equations associated with two exterior multiplication forms $\Omega$ and $\Omega ^ { \prime }$ is a consequence of the equations

$$
d y _ { 1 } = 0 , d y _ { 2 } = 0 , . . . , d y _ { n - 1 } = 0 ,
$$

and hence a consequence of (l16). In other words, in order that (116) shall admit $\Omega ( \delta )$ as an invariant form, it is necessary that the system associated with $\Omega$ and $\Omega ^ { \prime }$ be satisfied when the variables $\delta x _ { 1 } , . . . , \delta x _ { n }$ are replaced by $X _ { 1 } , . . . , X _ { n }$ ，respectively. The system of equations expressed by perfect differentials in the form (1l7) is called the characteristic system with respect to (116).

Conversely,suppose that the above condition for perfect differentials is satisfied. Since the system associated with $\Omega ( d )$ is satisfied when (116) is taken into account, it is satisfied when the equivalent system (li7) is taken into account.Hence $\Omega ( d )$ , considered as an exterior multiplication form of $d { \pmb x } _ { i }$ ,can be expressed uniquely by $d y _ { 1 } , . . . , d y _ { n - 1 } .$ ，the coefficients being functions of $_ x$ that can be expressed in terms of $y _ { 1 } , \dotsc , y _ { n - 1 }$ and $x _ { n } ,$ if $X _ { n } \neq 0$ Thus

$$
\Omega = \textstyle \sum { A _ { i _ { 1 } } . . . _ { i _ { p } } [ d y _ { i _ { 1 } } . . . d y _ { i _ { p } } ] } .
$$

Only the term in $[ d x _ { n } d y _ { i _ { 1 } } \cdot \cdot \cdot d y _ { i _ { p } } ]$ has the coefficient $\partial A _ { i _ { 1 } } . . . _ { i _ { p } } / \partial x _ { n }$ By hypothesis $\Omega ^ { \prime }$ should be expressed by means of the variables $d y _ { i }$ only. Hence

$$
\frac { \partial A _ { i _ { 1 } } \cdot \cdot \cdot i _ { p } } { \partial x _ { n } } = 0 ,
$$

that is, $\Omega$ is expressed by the first integrals of the given system and their differentials.This is an invariant form.Thus the equations of the characteristic system of $\Omega$ reduce to the equations of the associated system of $\Omega$ joined with the equations of the associated system of $\Omega ^ { \prime }$

In particular, if $\Omega$ is a perfect differential, that is,if $\Omega ^ { \prime } = 0$ ,then the characteristic equation of a differential form $\Omega$ coincides with the associated system of form $\Omega$

The characteristic equation of a relative integral invariant $\int \Omega$ coincides with the associated system of the derived form $\Omega ^ { \prime }$ ,because a relative invariant leads to an absolute invariant $\int \Omega ^ { \prime }$ ，where $\Omega ^ { \prime }$ isa perfect differential. From the linear integral invariant in dynamics

$$
\int \omega _ { \delta } = \int \sum _ { i } \beta _ { i } \delta q _ { i } - H \delta t ,
$$

we obtain

$$
\omega _ { \delta } ^ { \prime } = \sum _ { i } \left[ \delta p _ { i } \delta q _ { i } \right] - [ \delta H \delta t ] .
$$

The associated system of form $\omega ^ { \prime }$ is

$$
\frac { \partial \omega ^ { \prime } } { \partial ( \delta q _ { i } ) } = 0 , \frac { \partial \omega ^ { \prime } } { \partial ( \delta p _ { i } ) } = 0 , \frac { \partial \omega ^ { \prime } } { \partial ( \delta t ) } = 0 ;
$$

that is, replacing δ by $d$

$$
- \ d p _ { i } \ - \frac { \partial H } { \partial q _ { i } } d t = 0 , \qquad d q _ { i } - \frac { \partial H } { \partial p _ { i } } d t = 0 , \qquad d H - \frac { \partial H } { \partial t } d t = 0 .
$$

This is the result already obtained in Chapter 1.

An electromagnetic field defined by the exterior form

$$
\begin{array} { r l } & { \Omega = H _ { x } [ \delta y \ \delta z ] + H _ { y } [ \delta z \ \delta x ] + H _ { z } [ \delta x \ \delta y ] + E _ { x } [ \delta x \ \delta t ] } \\ & { ~ + E _ { y } [ \delta y \ \delta t ] + E _ { z } [ \delta z \ \delta t ] } \end{array}
$$

satisfies the condition that $\Omega ^ { \prime }$ is zero. Thus,

$$
^ { ' } = \bigg ( { \frac { \partial H _ { x } } { \partial x } } + { \frac { \partial H _ { y } } { \partial y } } + { \frac { \partial H _ { z } } { \partial z } } \bigg ) [ \delta x \ \delta y \ \delta z ] + \bigg ( { \frac { \partial H _ { x } } { \partial t } } + { \frac { \partial E _ { z } } { \partial y } } - { \frac { \partial E _ { y } } { \partial z } } \bigg ) [ \delta y \ \delta z \ \delta t ]
$$

$$
+ \left( { \frac { \partial H _ { y } } { \partial t } } + { \frac { \partial E _ { x } } { \partial z } } - { \frac { \partial E _ { z } } { \partial x } } \right) [ \delta z \ \delta x \ \delta t ] + \left( { \frac { \partial H _ { z } } { \partial t } } + { \frac { \partial E _ { y } } { \partial x } } - { \frac { \partial E _ { x } } { \partial y } } \right) [ \delta x \ \delta y \ \delta t ] .
$$

Equating to zero the four coefficients of $\Omega ^ { \prime }$ ,we obtain the classical four equations of Maxwell

$$
\operatorname { d i v } H = 0 , \frac { \partial H } { \partial t } + \operatorname { c u r l } E = 0 ,
$$

If the exterior differential of a form $\pmb { \omega }$ of degree $\pmb { \mathscr { p } }$ is zero,then the integral $\int \omega$ extended over the boundary of an arbitrary domain of $( \pmb { \mathscr { p } } + 1 )$ dimensions is zero.In this case the form is said to be closed. Let $\theta _ { \alpha } \equiv A _ { \alpha _ { i } } d x _ { i } = 0 ( \alpha = 1 , 2 , . . . , r )$ be a differential system of linear equations where $\theta _ { \alpha }$ are supposed to be independent. The set of exterior forms

$$
\Phi = [ F _ { 1 } \phi _ { 1 } ] + [ F _ { 2 } \phi _ { 2 } ] + \cdot \cdot \cdot + [ F _ { h } \phi _ { h } ]
$$

-where $\phi _ { i }$ are homogeneous exterior forms under the condition that the sum of the degrees of two factors in any term $[ F _ { i } \phi _ { i } ]$ is the same for all terms on the right-hand side- is called the ring (Van der Waerden, 1955) determined by the $h$ homogeneous exterior forms $F _ { 1 } , F _ { 2 } , \ldots , F _ { h }$ of $\boldsymbol { n }$ variables. Any form $\Phi$ of the ring determined by $h$ given forms

$$
F _ { 1 } , F _ { 2 } , . . . , F _ { h }
$$

vanishes for any solution of the system $F _ { 1 } = 0 , F _ { 2 } = 0 , . . . , F _ { h } = 0 $ But in order that an exterior form $\Phi$ may vanish for all solutions of the system of linear independent equations $F _ { 1 } = 0 , F _ { 2 } = 0 , . . . , F _ { h } = 0$ itis necessary and sufficient that $\Phi$ belongs to the ring determined by the forms $F _ { i }$ The necessary and sufficient condition for $\Phi$ to belong to the ring is that the form $[ F _ { 1 } F _ { 2 } \cdot \cdot \cdot F _ { h } \Phi ]$ be zero.

In order that a system of $r$ linear differential equations shall be completely integrable,it is necessary and sufficient that in the neighborhood of any point in space the exact differential $d \theta _ { \alpha }$ belong to the ring determined by the forms $\theta _ { \alpha }$ .A differential system is said to be closed if the exterior differentials $d \theta _ { \alpha }$ belong to the ring determined by $\theta _ { \alpha }$ The necessary and sufficient condition for the complete integrability of the system

$$
\theta _ { \alpha } \equiv A _ { \alpha _ { i } } d x _ { i } = 0 \qquad ( \alpha = 1 , 2 , . . . , r )
$$

is that

$$
[ \theta _ { 1 } \theta _ { 2 } \cdot \cdot \cdot \cdot \theta _ { r } d \theta _ { \alpha } ] = 0 \qquad ( \alpha = 1 , 2 , . . . , r ) .
$$

Attempts to solve differential systems lead to the study of the solutions of closed differential systems.

The exterior product of two exterior invariant forms is evidently an invariant form.Hence, if an invariant exterior form $\Omega$ is known, we can derive a series of other invariant forms, that is, $\Omega ^ { \acute { \prime } }$ and all forms deducible from $\Omega$ and $\Omega ^ { \prime }$ ,by exterior multiplication.

If $\Omega$ is an absolute invariant form of an even degree, then two series of absolute invariant forms

$$
[ \Omega ^ { p } ] , \qquad [ \Omega ^ { p - 1 } \Omega ^ { \prime } ] \qquad ( \rlap { / } { p } = 1 , 2 , . . . ) ,
$$

can be deduced. The derivative of a form of the first series is equal to a form of the second series. The derivative of a form of the second series is zero. If $\Omega$ is an absolute invariant of an odd degree, then we obtain two series

$$
[ \Omega ^ { \prime p } ] , \qquad [ \Omega \Omega ^ { \prime p - 1 } ] \qquad ( \not p = 1 , 2 , \ldots ) .
$$

The derivative of a form of the frst series is zero,and the derivative of a form of the second series is equal to a form of the first series.

Consider a relative integral invariant $\int \Omega$ and suppose that $\Omega$ is of an even degree.Then only one new invariant, that is, the absolute invarint $\int \Omega ^ { \prime }$ ,can be derived.If $\Omega$ is of an odd degree, then we obtain a series of relative integral invariants $\int \Omega \Omega ^ { \prime } ^ { p - 1 }$ and a series of absolute integral invariants $\int \Omega ^ { \prime } \ d \mathbf { p }$ .The relative invariant $\int \Omega \Omega ^ { \prime } { } ^ { p - 1 }$ leads to the absolute invariant $\int ^ { \infty } \Omega ^ { \prime } ^ { p }$ by derivation.

For example, take the relative invariant of dynamics

$$
\int \omega = \int \sum _ { i = 1 } ^ { n } \dot { p } _ { i } \delta q _ { i } - H \delta t .
$$

The relative integral invariants derivable from this invariant are $\int _ { . } \omega \omega ^ { \prime p - 1 } \left( \phi = 1 , 2 , . . . , n \right)$ ，and the absolute integral invariants are $\int \omega ^ { \prime p } \ \left( \boldsymbol { \phi } = 1 , 2 , . . . , n \right)$ . Hence there exists an invariant, relative or absolute,of any given degree, lower than or equal to $2 n$

Consider a form $\pmb { F }$ of degree $\pmb { \mathscr { p } }$ .Denote by $\xi , \xi ^ { \prime } , \ldots , \xi ^ { ( n - p + 1 ) }$ the linear forms with indeterminate coefficients

$$
\xi = \sum _ { i = 1 } ^ { n } \xi _ { i } u _ { i } , \xi ^ { \prime } = \sum _ { i = 1 } ^ { n } \xi _ { i } ^ { \prime } u _ { i } ,
$$

The exterior product $[ F \xi \xi ^ { \prime } \cdot \cdot \cdot \xi ^ { ( n - p + 1 ) } ]$ is of degree $\pmb { n }$ and hence of the form $\Phi [ u _ { 1 } u _ { 2 } \cdot \cdot \cdot u _ { n } ]$ The coefficient $\Phi$ is linear with respect to each series of coefficients $\xi$ and is alternating. It corresponds to an exterior form of degree $n - \phi$ with respect to the variables $\xi _ { 1 } , \xi _ { 2 } , . . . , \xi _ { n }$ This is defined as the adjoint form of $\pmb { F }$

If we apply a linear substitution to the variables $\pmb { u }$ and a linear substitution to the variables $\xi$ so that the expression

$$
\xi _ { 1 } u _ { 1 } + \xi _ { 2 } u _ { 2 } + \cdot \cdot \cdot + \xi _ { n } u _ { n }
$$

is conserved, then the expression $\Phi [ u _ { 1 } u _ { 2 } \cdot \cdot \cdot u _ { n } ]$ is conserved. In other words, the adjoint form is multiplied by the determinant of the linear substitution applied to the variables $\boldsymbol { u }$

An exterior form of degree $\pmb { \mathscr { p } }$ is called a monomial if it can be expressed as an exterior product of $\boldsymbol { \mathscr { p } }$ linear forms.Consider the system of linear equations obtained by equating to zero all partial derivatives of order $\phi - 1$ of that exterior form, that is, the associated system.If a linear substitution with nonvanishing determinant is applied to the form $F ( u _ { 1 } , u _ { 2 } , \ldots , u _ { p } )$ to transform it into the form $\Phi ( u _ { 1 } , u _ { 2 } , . . . , u _ { p } )$ , then the same process transforms the associated system of $\pmb { F }$ into the associated system of $\Phi$ . If an exterior monomial form is linearly transformed so that it contains only a single term $a [ u _ { 1 } u _ { 2 } \cdot \cdot \cdot u _ { p } ]$ ， then the associated system consists of $\pmb { \mathscr { p } }$ independent equations $u _ { 1 } = 0 , u _ { 2 } = 0 , . . . , u _ { p } = 0 .$ Conversely, if the associated system of a form ${ \pmb F }$ of degree $\pmb { \mathscr { p } }$ is reduced to $\pmb { \mathscr { p } }$ independent equations, then the associated system can take the form ${ \bf \chi } _ { 1 } ^ { \prime } = 0 , u _ { 2 } = 0 , . . . , u _ { p } = 0$ by a linear substitution,but none of the nonvanishing coefficients of the form can contain indices other than $1 , 2 , \ldots , \boldsymbol { \mathscr { p } }$ 、Hence the form should be $a [ u _ { 1 } u _ { 2 } \cdot \cdot \cdot u _ { p } ]$ ，a monomial. The necessary and sufficient condition for an exterior form of degree $\pmb { \mathscr { p } }$ to be a monomial is that the associated system be of rank $\pmb { \mathscr { p } }$

The adjoint form of $F = F _ { 1 } + F _ { 2 }$ is the sum of the adjoint forms $\Phi _ { 1 }$ and $\Phi _ { 2 }$ .The adjoint form of $a F$ is $a \Phi$ ,where $^ { a }$ is a numerical constant. To calculate the adjoint form it is suffcient to calculate the adjoint form of a monomial such as $F = [ u _ { \alpha _ { 1 } } u _ { \alpha _ { 2 } } \cdot \cdot \cdot u _ { \alpha _ { p } } ]$ . Then $\Phi = [ \xi _ { \alpha _ { p + 1 } } \cdot \cdot \cdot \xi _ { \alpha _ { n } } ] _ { \mathrm { \ell } }$ ， where the indices $\alpha _ { p + 1 } , \ldots , \alpha _ { n }$ are those indices $1 , 2 , \ldots , n$ that do not appear in the sequence $\alpha _ { 1 } , \alpha _ { 2 } , \ldots , \alpha _ { p } ,$ ,and these indices are arranged in an order such that the total sequence $\alpha _ { 1 } , \ldots , \alpha _ { p } , \alpha _ { p + 1 } , \ldots , \alpha _ { n }$ is even.

If $F$ is of degree $n - 1$ ,then the adjoint form is of the first degree.We can suppose that the adjoint form is $\xi _ { n }$ so that $\pmb { F }$ can be put in the form $F = [ u _ { 1 } u _ { 2 } \cdot \cdot \cdot u _ { n - 1 } ]$ If $\boldsymbol { F }$ is of degree $n - 2$ ，then $\Phi$ is of the second degree, and is given by $\Phi = [ \xi _ { 1 } \xi _ { 2 } ] + \cdot \cdot \cdot + [ \xi _ { 2 s - 1 } \xi _ { 2 s } ]$

$$
\{ u _ { 3 } u _ { 4 } u _ { 5 } \cdot \cdot \cdot u _ { n } \} + [ u _ { 1 } u _ { 2 } u _ { 5 } u _ { 6 } \cdot \cdot \cdot u _ { n } ] + \cdot \cdot \cdot + [ u _ { 1 } u _ { 2 } \cdot \cdot \cdot u _ { 2 s - 2 } u _ { 2 s + 1 } \cdot \cdot \cdot u _ { n } ] .
$$

Suppose that $f _ { 1 } , f _ { 2 } , \ldots . . . , f _ { h }$ are $h$ independent linear forms. Let $\pmb { F }$ be an arbitrary exterior form.In order that $F$ may vanish when there are relations $f _ { 1 } = 0 , f _ { 2 } = 0 , . . . , f _ { h } = 0$ among the variables, it is necessary and sufficient that $[ F f _ { 1 } f _ { 2 } \cdot \cdot \cdot f _ { h } ] = 0 .$ For,if any term of $F$ contains at least one of the variables $u _ { 1 } , u _ { 2 } , \ldots , u _ { h }$ as a factor, then the product $\left[ F u _ { 1 } \cdot \cdot \cdot u _ { h } \right]$ is zero.Conversely,if this product is zero,then an arbitrary term of $F$ must contain at least one of the variables $u _ { 1 } , u _ { 2 } , \ldots , u _ { h }$ ，otherwise the multiplication of the term by $\left[ u _ { 1 } u _ { 2 } \cdot \cdot \cdot u _ { h } \right]$ will give a nonzero product that we cannot cancel by combining it with any other product.

Consider an exterior quadratic form $\pmb { F }$ in $_ n$ variables $u _ { 1 } , . . . , u _ { n }$ Suppose that the variables are connected by a linear relation

$$
f \equiv a _ { 1 } u _ { 1 } + a _ { 2 } u _ { 2 } + \cdot \cdot \cdot + a _ { n } u _ { n } = 0 .
$$

The form $F _ { : }$ where $u _ { n }$ is expressed in terms of $u _ { 1 } , \ldots , u _ { n - 1 }$ by means of this linear relation, is of a certain rank corresponding to the number of linearly independent equations of the associated system, namely

$$
{ \frac { \partial F } { \partial u _ { 1 } } } - { \frac { a _ { 1 } } { a _ { n } } } { \frac { \partial F } { \partial u _ { n } } } = 0 , . . . , { \frac { \partial F } { \partial u _ { n - 1 } } } - { \frac { a _ { n - 1 } } { a _ { n } } } { \frac { \partial F } { \partial u _ { n } } } = 0 , \quad f = 0 .
$$

If there are $\pmb { \mathscr { p } }$ linearly independent relations

$$
f _ { 1 } = \sum _ { i = 1 } ^ { n } a _ { i } u _ { i } = 0 , \mathcal { f } _ { 2 } = \sum _ { i = 1 } ^ { n } a _ { i } ^ { \prime } u _ { i } = 0 , . . . , \mathcal { f } _ { p } = \sum _ { i = 1 } ^ { n } a _ { i } ^ { ( p - 1 ) } u _ { i } =
$$

then the associated system of $F$ is

$$
{ \left| \begin{array} { l l l l l } { { \frac { \partial F } { \partial u _ { 1 } } } } & { { \frac { \partial F } { \partial u _ { 2 } } } } & { { } } & { { \frac { \partial F } { \partial u _ { n } } } } \\ { a _ { 1 } } & { a _ { 2 } } & { { } } & { a _ { n } } \\ { a _ { 1 } ^ { \prime } } & { { a _ { 2 } ^ { \prime } } } & { { } } & { a _ { n } ^ { \prime } } \\ { \vdots } & { { } } & { { } } & { { } } \\ { a _ { 1 } ^ { ( p - 1 ) } } & { { a _ { 2 } ^ { ( p - 1 ) } } } & { { } } & { { a _ { n } ^ { ( p - 1 ) } } } \end{array} \right| } = 0 , ~ f _ { 1 } = 0 , ~ f _ { 2 } = 0 , \ldots , ~ f _ { p } = 0
$$

The rank $2 s ^ { \prime }$ of the form $F _ { : }$ ，when the variables are connected by the $\pmb { \mathscr { p } }$ linearly independent relations, is twice the largest exponent for which the form $[ f _ { 1 } f _ { 2 } . \cdot \cdot f _ { p } F ^ { s ^ { \prime } } ]$ is not zero.

If we define the bilinear alternating form

$$
\Phi ( \xi , \xi ^ { \prime } ) = \sum _ { i , j } a _ { i j } \xi _ { i } \xi _ { j } ^ { \prime }
$$

by the equation

$$
s [ F ^ { s - 1 } ( \xi _ { 1 } u _ { 1 } + \cdot \cdot \cdot + \xi _ { n } u _ { n } ) ( \xi _ { 1 } ^ { \prime } u _ { 1 } + \cdot \cdot \cdot + \xi _ { n } ^ { \prime } u _ { n } ) ] = \Phi ( \xi , \xi ^ { \prime } ) [ F ^ { s } ] ,
$$

then the exterior quadratic form $\begin{array} { r } { \Phi ( \xi ) = \sum _ { i , j } a _ { i j } [ \xi _ { i } \xi _ { j } ] } \end{array}$ is equal to the adjoint form of $F ^ { s - 1 } / ( s - 1 ) !$ ,multiplied by a constant factor.It is an absolute covariant of $\pmb { F }$ in the sense that if we apply a linear substitution to the variables $u _ { 1 } , \ldots , u _ { n }$ and a linear substitution to the variables $\xi _ { 1 } , . . . , \xi _ { n }$ such that $\xi _ { 1 } u _ { 1 } + \cdot \cdot \cdot + \xi _ { n } u _ { n }$ is conserved,and if $\pmb { F } ( \pmb { u } )$

and $\Phi ( \xi )$ become respectively equal to $\bar { F } ( \bar { u } )$ and $\overline { { \Phi } } ( \xi )$ through these two substitutions, then

$$
s [ \bar { F } ^ { s - 1 } \bigl ( \bar { \xi } _ { 1 } \bar { u } _ { 1 } + \cdot \cdot \cdot + \bar { \xi } _ { n } \bar { u } _ { n } \bigr ) \bigl ( \bar { \xi } _ { 1 } ^ { \prime } \bar { u } _ { 1 } + \cdot \cdot \cdot + \bar { \xi } _ { n } ^ { \prime } \bar { u } _ { n } \bigr ) ] = \overline { { { \Phi } } } ( \bar { \xi } \bar { \xi } ^ { \prime } ) [ \bar { F } ^ { s } ] .
$$

In particular, if $\pmb { F }$ is of the canonical form

$$
{ \cal F } = [ u _ { 1 } u _ { 2 } ] + \cdot \cdot \cdot + [ u _ { 2 s - 1 } u _ { 2 s } ] ,
$$

then

$$
\Phi = [ \xi _ { 1 } \xi _ { 2 } ] + \cdot \cdot \cdot + [ \xi _ { 2 s - 1 } \xi _ { 2 s } ] .
$$

This leads to the general identity

$$
\biggl [ \frac { F ^ { s - p } } { ( s - p ) ! } ( \xi _ { 1 } u _ { 1 } + \cdot \cdot \cdot + \xi _ { n } u _ { n } ) ( \xi _ { 1 } ^ { \prime } u _ { 1 } + \cdot \cdot \cdot + \xi _ { n } ^ { \prime } u _ { n } ) \cdot \cdot \cdot \cdot +
$$

$$
\left( \xi _ { 1 } ^ { ( 2 p - 1 ) } u _ { 1 } + \cdot \cdot \cdot + \xi _ { n } ^ { ( 2 p - 1 ) } u _ { n } \right) \bigg ] = \frac { \Phi ^ { ( p ) } ( \xi , \xi ^ { \prime } , \cdot \cdot , \xi ^ { ( 2 p - 1 ) } ) } { \rlap / p ! } \bigg [ \frac { F ^ { s } } { s ! } \bigg ] ,
$$

where the exterior form of degree $\pmb { \mathscr { p } }$ corresponding to the multilinear alternating form $\Phi ^ { ( p ) }$ is equal to $[ \Phi ^ { ( p ) } ( \xi ) ]$ .If $\pmb { \hat { p } } = 2$ , then,by taking the terms in $[ \xi _ { i } \xi _ { j } ^ { \prime } \xi _ { k } ^ { \prime \prime } \xi _ { l } ^ { \prime \prime \prime } ]$ , we obtain

$$
\left[ \frac { F ^ { s - 2 } } { ( s - 2 ) ! } u _ { i } u _ { j } u _ { k } u _ { l } \right] = ( a _ { i j } a _ { k l } + a _ { i k } a _ { l j } + a _ { i l } a _ { j k } ) \left[ \frac { F ^ { s } } { s ! } \right] ,
$$

where $\alpha _ { i j }$ are defined by

$$
\left[ \frac { F ^ { s - 1 } } { ( s - 1 ) ! } u _ { i } u _ { j } \right] = a _ { i j } \Bigl [ \frac { F ^ { s } } { s ! } \Bigr ] .
$$

Consider a form of degree $2 s - 1$ ：

$$
\left[ \frac { F ^ { s - 2 } } { ( s - 2 ) ! } u _ { i } u _ { j } u _ { k } \right] - \left[ \frac { F ^ { s - 1 } } { ( s - 1 ) ! } ( a _ { i j } u _ { k } + a _ { j k } u _ { i } + a _ { k i } u _ { j } ) \right] .
$$

Multiply exteriorly by any one of the variables $u _ { 1 } , \dotsc , u _ { 2 s } ,$ ， for example by ${ \boldsymbol { \mu } } _ { l }$ ；then,by (ll9)，the product is zero.Hence the form itself is identically zero. Since $u _ { i } , u _ { j } , u _ { k }$ can be replaced by any three linear forms of the variables, we obtain the following

Theorem : Consider linear forms $f _ { 1 } , \ldots , f _ { p }$ and put

$$
\Big [ \frac { F ^ { s - 1 } } { ( s - 1 ) ! } f _ { i } f _ { j } \Big ] = a _ { i j } \Big [ \frac { F ^ { s } } { s ! } \Big ] ( i , j = 1 , 2 , . . . , p ) ,
$$

then,

$$
\left[ \frac { F ^ { s - 2 } } { ( s - 2 ) ! } f _ { i } f _ { j } f _ { k } \right] = \left[ \frac { F ^ { s - 1 } } { ( s - 1 ) ! } ( a _ { i j } f _ { k } + a _ { j k } f _ { i } + a _ { k i } f _ { j } ) \right] .
$$

Consider $\boldsymbol { \mathscr { p } }$ linearly independent relations $f _ { 1 } = 0 , f _ { 2 } = 0 , . . . , f _ { p } = 0 .$ We can suppose that they are reduced to the forms

$$
u _ { 1 } = 0 , u _ { 2 } = 0 , . . . , u _ { p } = 0 ,
$$

on the single condition that the frst $\pmb { \mathscr { p } }$ variables $u _ { 1 } , . . . , u _ { p }$ can be exchanged among themselves. Thus an arbitrary linear substitution can be applied to the variables $\xi$ on the single condition that the last $2 s - \pmb { \mathscr { p } }$ variables $\xi _ { p + 1 } , \ldots , \xi _ { 2 s }$ can be exchanged among themselves. Put

$$
\left[ { \frac { F ^ { s - 1 } } { ( s - 1 ) ! } } u _ { i } u _ { j } \right] = a _ { i j } \Bigl [ { \frac { F ^ { s } } { s ! } } \Bigr ] \qquad ( i , j = 1 , 2 , . . . , \rlap / p )
$$

and drop the terms in $\xi _ { p + 1 } , . . . , \xi _ { 2 s }$ in $\Phi$ ; then,

$$
\overline { { \Phi } } = \sum _ { i , j = 1 } ^ { p } a _ { i j } [ \xi _ { i } \xi _ { j } ] .
$$

Let $2 q$ be the rank of the form $\overline { { \Phi } }$ .By suitable linear substitution on $\xi _ { 1 } , . . . , \xi _ { p } .$ ，we can reduce $\overline { { \Phi } }$ to

$$
\Phi = [ \xi _ { 1 } \xi _ { 2 } ] + \cdot \cdot \cdot + [ \xi _ { 2 q - 1 } \xi _ { 2 q } ] .
$$

Replacing $\xi _ { 1 } , . . . , \xi _ { p }$ by linear combinations of $\xi _ { p + 1 } , . . . , \xi _ { 2 s } ,$ we obtain $\Phi$ in the form

$$
\begin{array} { l } { { \Phi = [ \xi _ { 1 } \xi _ { 2 } ] + \cdot \cdot \cdot + [ \xi _ { 2 q - 1 } \xi _ { 2 q } ] + [ \xi _ { 2 q + 1 } \xi _ { p + 1 } ] + \cdot \cdot \cdot + [ \xi _ { p } \xi _ { 2 p - 2 q } ] } } \\ { { \mathrm { } } } \\ { { \mathrm { } + [ \xi _ { 2 p - 2 q + 1 } \xi _ { 2 p - 2 q + 2 } ] + \cdot \cdot \cdot + [ \xi _ { 2 s - 1 } \xi _ { 2 s } ] . } } \end{array}
$$

Then ${ \pmb F }$ takes the form

$$
\begin{array} { l } { { F = [ u _ { 1 } u _ { 2 } ] + \cdot \cdot \cdot + [ u _ { 2 q - 1 } u _ { 2 q } ] + [ u _ { 2 q + 1 } u _ { p + 1 } ] + \cdot \cdot \cdot + [ u _ { p } u _ { 2 p - 2 q } ] } } \\ { { \ \qquad + \cdot \cdot \cdot + [ u _ { 2 s - 1 } u _ { 2 s } ] . } } \end{array}
$$

Hence,if there are $\pmb { \mathscr { p } }$ relations $u _ { 1 } = 0 , u _ { 2 } = 0 , . . . , u _ { p } = 0 $ ，then the rank of $F$ is reduced by $2 p - 2 q$ 、From this we obtain the following

Theorem: Consider linearly independent forms $f _ { 1 } , f _ { 2 } , \ldots , f _ { p }$ and $\textstyle { \frac { 1 } { 2 } } p ( p - 1 )$ quantities $a _ { i j }$ defined by

$$
\left[ \frac { F ^ { s - 1 } } { ( s - 1 ) ! } f _ { i } f _ { j } \right] = a _ { i j } \Bigl [ \frac { F ^ { s } } { s ! } \Bigr ] ,
$$

and the exterior quadratic form of $\pmb { \mathscr { p } }$ variables $\xi _ { 1 } , . . . , \xi _ { p }$

$$
\Phi ( \xi ) = \sum _ { i , j = 1 } ^ { p } a _ { i j } [ \xi _ { i } \xi _ { j } ] .
$$

If the rank of this form is $2 q$ ,then the rank of the form $\pmb { F }$ is reduced by $2 p - 2 q$ when the variables are connected by $\pmb { \mathscr { p } }$ relations

$$
f _ { 1 } = 0 , f _ { 2 } = 0 , . . . , f _ { p } = 0 .
$$

If,in addition, we apply to the $\pmb { \mathscr { p } }$ given linear forms a linear substitution such that $\Phi$ reduces to the canonical form

$$
\Phi = [ \xi _ { 1 } \xi _ { 2 } ] + \cdot \cdot \cdot + [ \xi _ { 2 q - 1 } \xi _ { 2 q } ] ,
$$

then the form $\pmb { F }$ reduces to the canonical form

$$
\begin{array} { l } { { F = [ f _ { 1 } f _ { 2 } ] + \cdot \cdot \cdot + [ f _ { 2 q - 1 } f _ { 2 q } ] + [ f _ { 2 q + 1 } f _ { p + 1 } ] + \cdot \cdot \cdot + [ f _ { p } f _ { 2 p - 2 q } ] } } \\ { { \ \qquad + \cdot \cdot \cdot + [ f _ { 2 s - 1 } f _ { 2 s } ] , } } \end{array}
$$

where $f _ { p + 1 } , \ldots , f _ { 2 s }$ denote the new linear forms conveniently chosen so that they are independent of one another and of the given forms.

Suppose that a system of differential equations

$$
\frac { d x _ { 1 } } { d t } = X _ { 1 } , . . . , \frac { d x _ { n } } { d t } = X _ { n }
$$

admits an integral invariant of the maximum degree $\pmb { n }$

$$
\Omega = M [ ( \delta x _ { 1 } - X _ { 1 } \delta t ) ( \delta x _ { 2 } - X _ { 2 } \delta t ) \cdot \cdot \cdot ( \delta x _ { n } - X _ { n } \delta t ) ] .
$$

The required coidition is that $\Omega ^ { \prime } = 0$ ,that is,

$$
\frac { \partial M } { \partial t } + \frac { \partial ( M X _ { 1 } ) } { \partial x _ { 1 } } + \cdot \cdot \cdot + \frac { \partial ( M X _ { n } ) } { \partial x _ { n } } = 0 .
$$

The coefficient $M$ is called the Jacobi last multiplier (Section 4.18). This condition indicates that the form $\Omega$ can be expressed by means of $\pmb { n }$ independent first integrals $y _ { 1 } , y _ { 2 } , \ldots , y _ { n }$ of (123) and their differentials; that is,

$$
M [ ( \delta x _ { 1 } - X _ { 1 } \delta t ) \cdot \cdot \cdot ( \delta x _ { n } - X _ { n } \delta t ) ] = H ( y _ { 1 } , \cdot \cdot \cdot , y _ { n } ) [ \delta y _ { 1 } \cdot \cdot \cdot \delta y _ { n } ] .
$$

Let $\omega _ { 1 } = 0 , . . . , \omega _ { r } = 0$ be a system of Pfaffians completely integrable, of which we know an invariant form of the maximum degree $r _ { : }$ i.e., $\Omega = M [ \omega _ { 1 } \omega _ { 2 } \cdot \cdot \cdot \omega _ { \tau } ]$ . If we know $r - 1$ first integrals $y _ { 1 } , \dotsc , y _ { r - 1 }$ of the system， then, by equating $y _ { 1 } , \dotsc , y _ { r - 1 }$ each to an arbitrary constant, we can replace the given system by a single system $\omega _ { \tau } = 0$ , and obtain

$$
\Omega = N [ \delta y _ { 1 } \cdot \cdot \cdot \delta y _ { r - 1 } \omega _ { r } ] .
$$

The coefficient $N$ can be calculated from $M .$ Thus $N \omega _ { \tau }$ is an invariant form for a single equation $\omega _ { r } = 0$ ; that is, $N \omega _ { \tau }$ is a perfect differential, and the integration can be carried out by quadrature.

If this Pfaffan system admits an infinitesimal transformation

$$
A f = \xi _ { 1 } { \frac { \partial f } { \partial x _ { 1 } } } + \cdot \cdot \cdot + \xi _ { n } { \frac { \partial f } { \partial x _ { n } } } ,
$$

then we can deduce from an invariant differential form $\Omega$ another invariant form $A ( \Omega )$ .Let $\Omega = [ \omega _ { 1 } \cdot \cdot \cdot \omega _ { r } ]$ . We construct

$$
\omega _ { 1 } ( A ) , \omega _ { 2 } ( A ) , . . . , \omega _ { \tau } ( A )
$$

and suppose that $\omega _ { 1 } ( A ) = 1$ ， $\omega _ { 2 } ( A ) = \cdot \cdot \cdot = \omega _ { r } ( A ) = 0$ ，are the results of a suitable transformation. Suppose that $\Omega$ is of the third degree,and consider the corresponding trilinear differential form $\Omega ( \delta , \delta ^ { \prime } , \delta ^ { \prime \prime } )$ Replace δ by the symbol $A$ for the infinitesimal transformation； then, we obtain $\Omega ( A , \delta ^ { \prime } , \delta ^ { \prime \prime } )$ - with two series of differentials $\delta ^ { \prime }$ and $\delta ^ { \prime \prime }$ 丨to which corresponds an exterior quadratic form $\Omega ( A , \delta )$ . If $\Omega$ is expressed in terms of the first integrals $y _ { i }$ and their differentials,then $\Omega ( A , \delta )$ is expressed in terms of $y _ { i }$ and $\delta { y } _ { i }$ . Thus, from any invariant form we can derive another invariant form whose degree is diminished by one:

$$
\Omega ( A , \delta ) = \xi _ { 1 } \frac { \partial \Omega } { \partial ( \delta x _ { 1 } ) } + \cdot \cdot \cdot + \xi _ { n } \frac { \partial \Omega } { \partial ( \delta x _ { n } ) } \cdot
$$

Hence, if we know an infinitesimal transformation, we can deduce from the invariant form $\Omega ( \delta )$ a new invariant form $\Omega ( A , \delta )$ by means of this formula.

Let $2 n$ be the rank of the exterior derivative $\omega ^ { \prime }$ ,and $f$ and $g$ be two first integrals of its characteristic system.The two differential forms $[ \omega ^ { \prime n - 1 } \ d f d g ]$ and $[ \omega ^ { \prime n } ]$ are invariants of the maximum degree $2 n$ Hence these two forms differ only by a factor that is a first integral. Put

$$
\frac { 1 } { ( n - 1 ) ! } [ \omega ^ { \prime } { } ^ { n - 1 } d f d g ] = \frac { 1 } { n ! } ( f , g ) [ \omega ^ { \prime } { } ^ { n } ] ,
$$

where $( f , g )$ is a Poisson bracket and is an alternating bilinear form with respect to the partial derivatives of $f$ and $g$

If the condition $( f , g ) = 0$ is satisfed, then the rank of $\omega ^ { \prime }$ is $2 n - 4$ In this case we say that $f$ and $g$ are in involution. If this condition is not satisfied,then,by the definition of $( f , g )$ , the form $\omega ^ { \prime } - [ d f d g ] / ( f , g )$ is of rank $2 n - 2$ ,and its nth power is

$$
[ \omega ^ { \prime \prime } ] - \frac { n } { ( f , g ) } [ \omega ^ { \prime \prime - 1 } d f d g ] = 0 .
$$

If （124） is applied to $\begin{array} { r } { \omega ^ { \prime } = \sum _ { i } \left[ \delta \beta _ { i } \delta q _ { i } \right] - \left[ \delta H \delta t \right] } \end{array}$ in dynamics, and if the terms $[ \delta p _ { 1 } \delta q _ { 1 } \delta p _ { 2 } \cdot \cdot \cdot \delta q _ { n } ]$ are equated on both sides, then we obtain

$$
( f , g ) = \sum _ { i } \bigg ( \frac { \partial f } { \partial \phi _ { j } } \frac { \partial g } { \partial q _ { i } } - \frac { \partial f } { \partial q _ { j } } \frac { \partial g } { \partial \phi _ { i } } \bigg ) ,
$$

as in Chapter 1.

From (120) we obtain the identity

$$
\begin{array} { l } { \displaystyle { \frac { 1 } { ( n - 1 ) ! } \left[ \omega ^ { \prime n - 1 } \{ ( f , g ) d h + ( g , h ) d f + ( h , f ) d g \} \right] } } \\ { \displaystyle { \ } } \\ { \displaystyle { \ = \frac { 1 } { ( n - 2 ) ! } \left[ \omega ^ { \prime n - 2 } d f d g d h \right] } . } \end{array}
$$

By exterior derivation,and noting that the exterior derivative of the right-hand member is zero, we see that

$$
[ \omega ^ { \prime \prime - 1 } d ( f , g ) d h ] + [ \omega ^ { \prime \prime - 1 } d ( g , h ) d f ] + [ \omega ^ { \prime \prime - 1 } d ( h , f ) d g ] = 0 .
$$

This is Jacobi's identity (Section 4.3).

If there are two linear invariant forms $\varpi _ { 1 }$ and $\varpi _ { 2 }$ , then the quantity $\pmb { \alpha }$ defined by

$$
n [ \omega ^ { \prime n - 1 } \varpi _ { 1 } \varpi _ { 2 } ] = \alpha [ \omega ^ { \prime n } ]
$$

is a first integral. It reduces to $( y _ { 1 } , y _ { 2 } )$ if $\varpi _ { 1 }$ and $\varpi _ { 2 }$ are differentials of two first integrals $y _ { 1 }$ and $y _ { 2 }$

Suppose that the characteristic equations for $\omega ^ { \prime }$ admit two infinitesimal transformations $A _ { 1 } f$ and $A _ { 2 } f .$ Then

$$
\varpi _ { 1 } = \omega ^ { \prime } ( A _ { 1 } , \ S ) , \qquad \varpi _ { 2 } = \omega ^ { \prime } ( A _ { 2 } , \ S ) .
$$

Apply to both sides of (l25) the operation that carries an invariant form $\Omega ( \delta )$ to the form $\Omega ( A _ { 1 } , \delta )$ . Then,

$$
n ( n - 1 ) [ \omega ^ { \prime n - 2 } \varpi _ { 1 } \varpi _ { 2 } ] - n [ \omega ^ { \prime n - 1 } \varpi _ { 1 } ] \varpi _ { 2 } ( A _ { 1 } ) = n \alpha [ \omega ^ { \prime n - 1 } \varpi _ { 1 } ] .
$$

Since $[ \omega ^ { \prime n - 1 } \varpi _ { 1 } ]$ is not zero,

$$
\alpha = - \varpi _ { 2 } ( A _ { 1 } ) = \omega ^ { \prime } ( A _ { 1 } , A _ { 2 } ) = \varpi _ { 1 } ( A _ { 2 } ) .
$$

Thus, if we apply the generalized theorem of Poisson-Jacobi to the two invariant forms $\omega ^ { \prime } ( A _ { 1 } , \delta )$ and $\omega ^ { \prime } ( A _ { 2 } , \delta )$ , we can derive the first integral $\omega ^ { \prime } ( A _ { 1 } , A _ { 2 } )$ by twice performing on $\omega ^ { \prime }$ the operation corresponding to the infinitesimal transformations $A _ { 1 } f$ and $A _ { 2 } f .$

After these general considerations we now apply the theory to the reduction of the differential equations for the $\pmb { n }$ -body problem.

Suppose that we reduce the equations to the order $6 n - 6$ by using the six integrals for the center of mass.The equations then admit four infinitesimal transformations $A _ { i } f ( i = 0 , 4 , 5 , 6 )$ . Corresponding to these transformations are four integrals $H _ { i }$ $( i = 0 , 4 , 5 , 6 )$ .The infinitesimal transformation corresponding to the integral invariant ${ \boldsymbol { \omega } } _ { 1 0 }$ (Section 4.21) is

$$
A _ { 1 0 } f = \sum _ { i = 1 } ^ { 3 n } \left[ x _ { i } { \frac { \partial f } { \partial x _ { i } } } - { \frac { \dot { p } } { 2 } } y _ { i } { \frac { \partial f } { \partial y _ { i } } } \right] + \left[ 1 + { \frac { \dot { p } } { 2 } } \right] t { \frac { \partial f } { \partial t } } .
$$

The corresponding linear invariants $\omega _ { i } = \omega ^ { \prime } ( A _ { i } , \delta )$ are

$$
\omega _ { 0 } = \delta H _ { 0 } , \qquad \omega _ { 4 } = \delta H _ { 4 } , \qquad \omega _ { 5 } = \delta H _ { 5 } , \qquad \omega _ { 6 } = \delta H _ { 6 } ,
$$

$$
\omega _ { 1 0 } = \mathrm { ~ - ~ } \sum _ { i = 1 } ^ { 3 n } m _ { i } \Bigl ( x _ { i } ~ \delta y _ { i } + \frac { \dot { p } } { 2 } y _ { i } ~ \delta x _ { i } \Bigr ) + \Bigl ( 1 + \frac { \dot { p } } { 2 } \Bigr ) t ~ \delta H _ { 0 } + p H _ { 0 } ~ \delta t .
$$

We have

$$
\omega _ { 1 0 } ^ { \prime } = A _ { 1 0 } ( \omega ^ { \prime } ) = \Big ( 1 - \frac { \rlap / p } { 2 } \Big ) \omega ^ { \prime } .
$$

If we compute $a _ { i j } = \omega ^ { \prime } ( A _ { i } , A _ { j } )$ by (125) and (126), then we obtain

$$
\begin{array} { r l } & { \Phi ( \xi ) = \displaystyle \rlap / { / } H _ { 0 } [ \xi _ { 1 0 } \xi _ { 0 } ] + \frac { \beta - 2 } { 2 } [ \xi _ { 1 0 } ( H _ { 4 } \xi _ { 4 } + H _ { 5 } \xi _ { 5 } + H _ { 6 } \xi _ { 6 } ) ] } \\ & { \qquad + \ H _ { 4 } [ \xi _ { 5 } \xi _ { 6 } ] + H _ { 5 } [ \xi _ { 6 } \xi _ { 4 } ] + H _ { 6 } [ \xi _ { 4 } \xi _ { 5 } ] . } \end{array}
$$

The rank is four. Let

$$
\begin{array} { r l } & { \xi _ { 0 } ^ { \prime } = \hbar H _ { 0 } \xi _ { 0 } + \frac { \displaystyle \dot { p } - \displaystyle 2 } { \displaystyle 2 } ( H _ { 4 } \xi _ { 4 } + H _ { 5 } \xi _ { 5 } + H _ { 6 } \xi _ { 6 } ) . } \\ & { \xi _ { 4 } ^ { \prime } = \alpha _ { 1 } \xi _ { 4 } + \alpha _ { 2 } \xi _ { 5 } + \alpha _ { 3 } \xi _ { 6 } , \qquad \xi _ { 5 } ^ { \prime } = \beta _ { 1 } \xi _ { 4 } + \beta _ { 2 } \xi _ { 5 } + \beta _ { 3 } \xi _ { 6 } , } \\ & { \xi _ { 6 } ^ { \prime } = \xi _ { 0 } , \qquad \xi _ { 1 0 } ^ { \prime } = \xi _ { 1 0 } . } \end{array}
$$

Then we obtain the canonical form

$$
\Phi = [ \xi _ { 1 0 } ^ { \prime } \xi _ { 0 } ^ { \prime } ] + [ \xi _ { 4 } ^ { \prime } \xi _ { 5 } ^ { \prime } ] ,
$$

where $\propto$ and $\beta$ are chosen so that

$$
\alpha _ { 2 } \beta _ { 3 } - \alpha _ { 3 } \beta _ { 2 } = H _ { 4 } , \qquad \alpha _ { 3 } \beta _ { 1 } - \alpha _ { 1 } \beta _ { 3 } = H _ { 5 } , \qquad \alpha _ { 1 } \beta _ { 2 } - \alpha _ { 2 } \beta _ { 1 } = H _ { 6 } ,
$$

and with further conditions

$$
\alpha _ { 1 } \beta _ { 1 } + \alpha _ { 2 } \beta _ { 2 } + \alpha _ { 3 } \beta _ { 3 } = 0 ,
$$

$$
\alpha _ { 1 } ^ { 2 } + \alpha _ { 2 } ^ { 2 } + \alpha _ { 3 } ^ { 2 } = \beta _ { 1 } ^ { 2 } + \beta _ { 2 } ^ { 2 } + \beta _ { 3 } ^ { 2 } = \sqrt { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } } .
$$

If we define five linear forms $\varpi _ { 0 } , \varpi _ { 4 } , \varpi _ { 5 } , \varpi _ { 6 } , \varpi _ { 1 0 } ,$ by

$$
\begin{array} { r l } & { \xi _ { 0 } \omega _ { 0 } + \xi _ { 4 } \omega _ { 4 } + \xi _ { 5 } \omega _ { 5 } + \xi _ { 6 } \omega _ { 6 } + \xi _ { 1 0 } \omega _ { 1 0 } } \\ & { \qquad = \xi _ { 0 } ^ { \prime } \varpi _ { 0 } + \xi _ { 4 } ^ { \prime } \varpi _ { 4 } + \xi _ { 5 } ^ { \prime } \varpi _ { 5 } + \xi _ { 6 } ^ { \prime } \varpi _ { 6 } + \xi _ { 1 0 } ^ { \prime } \varpi _ { 1 0 } , } \end{array}
$$

then we obtain

$$
\begin{array} { l } { { \displaystyle { \varpi _ { 0 } = \frac { 2 } { \dot { p } - 2 } \frac { 2 } { 2 } \frac { H _ { 4 } d H _ { 4 } + H _ { 5 } d H _ { 5 } + H _ { 6 } d H _ { 6 } } { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } } } , } } \\ { { \displaystyle { \varpi _ { 4 } = \frac { \alpha _ { 1 } d H _ { 4 } + \alpha _ { 2 } d H _ { 5 } + \alpha _ { 3 } d H _ { 6 } } { \sqrt { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } } } } , } } \\ { { \displaystyle { \varpi _ { 5 } = \frac { \beta _ { 3 } d H _ { 4 } + \beta _ { 2 } d H _ { 5 } + \beta _ { 3 } d H _ { 6 } } { \sqrt { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } } } } , } } \\ { { \displaystyle { \varpi _ { 6 } = d H _ { 6 } - \frac { 2 \beta H _ { 0 } } { \beta - 2 } \frac { H _ { 4 } d H _ { 4 } + H _ { 5 } d H _ { 5 } + H _ { 6 } d H _ { 6 } } { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } } } , } } \\ { { \displaystyle { \varpi _ { 6 } = d H _ { 0 } - \frac { 2 \beta H _ { 0 } } { \beta - 2 } \frac { H _ { 4 } d H _ { 4 } + H _ { 5 } d H _ { 5 } + H _ { 6 } d H _ { 6 } } { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } } } , } } \\ { { \displaystyle { \varpi _ { 1 0 } = \omega _ { 1 0 } } } } \end{array}
$$

and

$$
\omega ^ { \prime } = [ \varpi _ { 1 0 } \varpi _ { 0 } ] + [ \varpi _ { 4 } \varpi _ { 5 } ] + [ \omega _ { 1 } \varpi _ { 6 } ] + [ \omega _ { 2 } \omega _ { 3 } ] + \cdot \cdot \cdot .
$$

Hence

$$
\begin{array} { r l r } { \omega ^ { \prime } = \displaystyle \frac { 2 } { \beta - 2 } \left[ \omega _ { 1 0 } \frac { H _ { 4 } d H _ { 4 } + H _ { 5 } d H _ { 5 } + H _ { 6 } d H _ { 6 } } { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } } \right] } & { } & \\ { \cdot + \frac { H _ { 4 } [ d H _ { 5 } d H _ { 6 } ] + H _ { 5 } [ d H _ { 6 } d H _ { 4 } ] + H _ { 6 } [ d H _ { 4 } d H _ { 5 } ] } { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } } + \Omega , } \end{array}
$$

where

$$
\begin{array} { l } { { \Omega = \displaystyle \left[ \omega _ { \mathrm { 1 } } \Big ( d H _ { \mathrm { 0 } } - \frac { 2 \beta H _ { \mathrm { 0 } } } { \beta - 2 } \frac { H _ { \mathrm { 4 } } d H _ { \mathrm { 4 } } + H _ { \mathrm { 5 } } d H _ { \mathrm { 5 } } + H _ { \mathrm { 6 } } d H _ { \mathrm { 6 } } } { H _ { \mathrm { 4 } } ^ { \mathrm { 2 } } + H _ { \mathrm { 5 } } ^ { \mathrm { 2 } } + H _ { \mathrm { 6 } } ^ { \mathrm { 2 } } } \Big ) \right] } } \\ { { + \left( \omega _ { \mathrm { 2 } } \omega _ { \mathrm { 3 } } \right) + \cdot \cdot \cdot . } } \end{array}
$$

By the theorem relative to (l21), the rank of $\Phi$ is four. Since there are five integral invariants, the rank of $\omega ^ { \prime }$ is reduced by $2 \times 5 - 4 = 6$ and the order of the differential equations is reduced from $6 n - 6$ to $6 n - 1 2$ ; that is, we obtain the integrals by puting each of $H _ { 0 } , H _ { 4 } , H _ { 5 }$ $H _ { 6 }$ equal to arbitrary constants. However, the corresponding characteristic system contains arbitrary parameters.

There is a proces for reducing the number of the degree offreedom by avoiding the introduction of arbitrary parameters. Equate the exterior derivative of the right-hand member of (l27) to zero; then we obtain

$$
\Omega ^ { \prime } = \biggl [ \frac { H _ { 4 } d H _ { 4 } + H _ { 5 } d H _ { 5 } + H _ { 6 } d H _ { 6 } } { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } } \Omega \biggr ] ,
$$

by referring to $\omega _ { 1 0 } ^ { \prime } = \frac { \ d H _ { 1 } } { \ d z ^ { 2 } } ( 2 - \hbar \omega ^ { \prime } )$ .This shows that the exterior derivative of a quadratic form

$$
\begin{array} { c } { { \displaystyle { \frac { 1 } { \sqrt { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + ~ H _ { 6 } ^ { 2 } } } } \Omega = \frac { 2 } { 2 ~ - ~ \hat { p } } \left( \frac { 1 } { \sqrt { H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + ~ H _ { 6 } ^ { 2 } } } \omega _ { 1 0 } \right) ^ { \prime } } } \\ { { - \frac { H _ { 4 } [ d H _ { 5 } d H _ { 6 } ] + H _ { 5 } [ d H _ { 6 } d H _ { 4 } ] + H _ { 6 } [ d H _ { 4 } d H _ { 5 } ] } { ( H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + ~ H _ { 6 } ^ { 2 } ) ^ { 3 / 2 } } ~ , } } \end{array}
$$

is zero.The first term on the right-hand side is zero because it is an exact exterior derivative.For the second term,consider a vector $O S$ of length $\gamma = ( H _ { 4 } ^ { 2 } + H _ { 5 } ^ { 2 } + H _ { 6 } ^ { 2 } ) ^ { 1 / 2 }$ . This is the angular momentum of the whole system with respect to the center of mass.Let $d \sigma$ be the surface element described by the point $S$ and $\alpha _ { 1 } , \alpha _ { 2 } , \alpha _ { 3 }$ be the direction cosines of the normal to this surface element. Then

$$
[ d H _ { 5 } d H _ { 6 } ] = \alpha _ { 1 } d \sigma , \qquad [ d H _ { 6 } d H _ { 4 } ] = \alpha _ { 2 } d \sigma , \qquad [ d H _ { 4 } d H _ { 5 } ] = \alpha _ { 3 } d \sigma .
$$

The second term on the right-hand side of (127a) becomes

$$
- { \frac { \alpha _ { 1 } H _ { 4 } + \alpha _ { 2 } H _ { 5 } + \alpha _ { 3 } H _ { 6 } } { \gamma ^ { 3 } } } d \sigma = - { \frac { \cos \psi d \sigma } { \gamma } } ,
$$

where $\psi$ is the angle between the normal and the vector $O S .$ The righthand member of (l27b) equals the solid angle subtended by $d \sigma$ at the center of mass. Denote by $\theta$ and $\phi$ the colatitude and the longitude of $S$ ； then this right-hand member becomes $- \sin \theta [ d \theta d \phi ]$ ,that is,the exterior derivative of cos $\theta \ : d \phi$ .Let

$$
\varpi = \frac { 2 } { 2 - \beta } \frac { \omega _ { 1 0 } } { \gamma } + \cos { \theta } d \phi ;
$$

then the characteristic equation of the relative integral invariant $\int \varpi$ is

$$
\begin{array} { c } { { H _ { 0 } - { \displaystyle \frac { 2 \phi } { \beta - 2 } } H _ { 0 } { \displaystyle \frac { d \gamma } { \gamma } } = 0 , \omega _ { 4 } = 0 , \omega _ { 5 } = 0 , \omega _ { 6 } = 0 , } } \\ { { \omega _ { 1 } = 0 , . . . , \omega _ { 7 } = 0 , . . . , \omega _ { 1 0 } = 0 . } } \end{array}
$$

# ADIABATIC INVARIANTS

# 4.23ADIABATIC INVARIANTS

Ehrenfest (l9l7) and Burgers (l9l7) showed that the mean value of the variation of the phase integral (Section 2.7)

$$
\scriptstyle J = \oint \ p \ d q
$$

becomes zero in multiply-periodic motions of atomic systems in classical quantum theory, when the system is submitted to slowly varying perturbations.This invariance of the phase integral for infinitely slow variation has been called the adiabatic invariance by these two workers, and also by Born (l926).Sommerfeld (l922） assumed that the time variation of the mechanical system is nonsystematic,rather than assuming zero time-average.But a slow variation does not necessarily satisfy this assumption. Kneser (l924) supposed that the mechanical system contains a slowly varying parameter $^ { a }$ ,such that $a = f ( t )$ for $0 < t < T _ { : }$ and,by extending the interval to $0 < t < l T _ { : }$ ，proved that the total variation of the phase integral $J ( t = l T ) - J ( t = 0 )$ tends to zero as $l  \infty$ , for the case of one degree of freedom. The proof of Fermi and Persico (1926) is not satisfactory.Born and Fock (l928), using Schrodinger's equation in quantum mechanics as a basis, estimated the order for the deviation from invariance which tends to zero asymptotically for $T \to \infty$

Let the eigenfunctions of the operator $H ( s )$ be $\phi _ { 1 } ( q , s ) , \phi _ { 2 } ( q , s ) , . . . ,$ which satisfy

$$
H ( s ) \phi _ { n } ( q , s ) = W _ { n } ( s ) \phi _ { n } ( q , s ) \qquad ( n = 1 , 2 , \ldots ) .
$$

The Schrodinger equation

$$
H \psi _ { n } + \frac { h } { 2 \pi i } \frac { \partial \psi _ { n } } { \partial t } = 0 \qquad ( n = 1 , 2 , . . . )
$$

is satisfied by the complete, normalized orthogonal system of functions $\psi _ { 1 } ( q , t ) , \psi _ { 2 } ( q , t ) , . . . ,$ with the initial condition

$$
\psi _ { n } ( q , 0 ) = \phi _ { n } ( q , 0 ) ( n = 1 , 2 , . . . ) .
$$

We have

$$
\dot { q } = \frac { 2 \pi i } { h } ( H q - q H ) , ~ \dot { p } = \frac { 2 \pi i } { h } ( H \rlap / p - \rlap / p H ) .
$$

These equations are satisfied by

$$
q = U ^ { \dagger } q ^ { 0 } U , \qquad \hbar = U ^ { \dagger } p ^ { 0 } U , \qquad H = U ^ { \dagger } H ^ { 0 } U ,
$$

with unitary matrix $U _ { : }$ ，where $U ^ { \dagger }$ denotes the transposed conjugate matrix of $U _ { : }$ and $H ^ { 0 }$ ， $q ^ { 0 } , p ^ { 0 }$ are the initial values of $H , q , \phi$ ,respectively. $U$ should satisfy

$$
\frac { h } { 2 \pi i } \dot { U } + H ^ { \circ } U = 0 .
$$

The solution is of the form $U = V Y$ with unitary matrices $V$ and Y. Let $s = t / T$ ； then,

$$
\begin{array} { r } { \frac { d Y } { d s } = - \frac { 2 \pi i T } { h } W Y + i Q Y , } \\ { Q = - i \frac { d V ^ { \dagger } } { d s } V = i V ^ { \dagger } \frac { d V } { d s } . } \end{array}
$$

Referring to the matrix elements

$$
\begin{array} { l l } { { U _ { m n } = \displaystyle \int \phi _ { m } ^ { * } ( q , 0 ) \psi _ { n } ( q , t ) \rho d q , \quad } } & { { \quad V _ { m n } = \displaystyle \int \phi _ { m } ^ { * } ( q , 0 ) \phi _ { n } ( q , s ) \rho d q , } } \\ { { { } } } & { { { } } } \\ { { { \displaystyle Y _ { m n } = \int \phi _ { m } ^ { * } ( q , s ) \psi _ { n } ( q , t ) \rho d q } , } } & { { } } \end{array}
$$

where a superscript \* means the conjugate complex and $\rho$ denotes the density function in $q$ -configuration space, we obtain

$$
\frac { d Y _ { m n } } { d s } = { } - \frac { 2 \pi i T } { h } W _ { m } Y _ { m n } + i \sum _ { k } Q _ { m k } Y _ { k n } .
$$

Let

$$
\begin{array} { l } { \displaystyle \omega _ { k } ( s ) = \frac { 2 \pi } { h } \int _ { 0 } ^ { s } W _ { k } ( s ) \ d s , } \\ { \displaystyle c _ { m n } ( s ) = Y _ { m n } e ^ { i T \omega _ { m } } , \qquad c _ { m n } ( 0 ) = \delta _ { m n } . } \end{array}
$$

Assume that $d ( \omega _ { m } - \omega _ { n } ) / d s = 2 \pi \nu _ { m n } ( s )$ admits only a finite number of zero points of the rth order such that

$$
\frac { 1 } { | 2 \pi \nu _ { m n } ( s ) | } < \frac { A } { | s - s _ { 0 } | ^ { r } }
$$

with a finite positive constant $A$ in the neighborhood of a zero point ${ \mathfrak { s } } _ { 0 } .$ that $| Q _ { m n } ( s ) | \leqq M _ { m n }$ with a fnite positive constant matrix $M _ { m n }$ ，and that the real and the imaginary parts of $Q _ { m n } ( s ) / \nu _ { m n } ( s )$ are piecewise monotonic with a finite number of discontinuity points. Then Born and Fock proved that $c _ { m n } - \delta _ { m n }$ is of order $T ^ { - 1 / ( r + 1 ) }$ for a finite $\pmb { \mathscr { s } }$ and an infinite $_ { T }$ such that

$$
c _ { m n } = \delta _ { m n } + { \cal O } \big ( T ^ { - 1 / ( r + 1 ) } \big ) .
$$

Suppose，with Levi-Civita (l928)，that the system of differentiah equations

$$
\frac { d x _ { i } } { d t } = X _ { i } ( x _ { 1 } , x _ { 2 } , . . . , x _ { n } , a _ { 1 } , a _ { 2 } , . . . , a _ { r } ) \qquad ( i = 1 , 2 , . . . , n ) ,
$$

containing the parameters $a _ { 1 } , a _ { 2 } , \ldots , a _ { r } ;$ but not $t$ explicitly, admits the integrals

$$
\begin{array} { r l } & { f _ { j } ( x _ { 1 } , x _ { 2 } , . . . , x _ { n } , a _ { 1 } , a _ { 2 } , . . . , a _ { r } ) = c _ { j } \qquad ( j = 1 , 2 , . . . , n - 1 ) , } \\ & { } \\ & { f _ { n } ( x _ { 1 } , x _ { 2 } , . . . , x _ { n } , a _ { 1 } , a _ { 2 } , . . . , a _ { r } ) = t - t _ { 0 } , } \end{array}
$$

where $a _ { \rho }$ are all constant. An integral curve of (128) is the trajectory of a point moving in the $\pmb { n }$ dimensional space $R _ { n }$ ， and the trajectory is represented by the intersection of the $n - 1$ hypersurfaces,that is,the $( n - 1 )$ -dimensional continuum represented by the first set of $n - 1$ equations of (129) embedded in $R _ { n }$ ·

When the parameter $a _ { \rho }$ varies as a function of $t$ ，the variables

$$
t _ { 0 } , c _ { 1 } , . . . , c _ { n - 1 }
$$

of (129) are functions of $t$ . By the principle of the variation of constants we must integrate

$$
\begin{array} { l l } { \displaystyle \frac { d c _ { j } } { d t } = \sum _ { \rho = 1 } ^ { r } \frac { \partial f _ { j } } { \partial a _ { \rho } } \frac { d a _ { \rho } } { d t } } \\ { \displaystyle \frac { d t _ { 0 } } { d t } = - \sum _ { \rho = 1 } ^ { r } \frac { \partial f _ { n } } { \partial a _ { \rho } } \frac { d a _ { \rho } } { d t } } & { ( j = 1 , 2 , . . . , n - 1 ) . } \end{array}
$$

For the coefficients of the time derivatives of $a _ { \rho }$ we substitute the solution of (128), and the result is of the form

$$
\frac { \partial f _ { i } } { \partial a _ { \rho } } = \phi _ { i \rho } ( t - t _ { 0 } , c _ { 1 } , . . . , c _ { n - 1 } , a _ { 1 } , . . . , a _ { r } )
$$

$$
( i = 1 , 2 , . . . , n ; \rho = 1 , 2 , . . . , r ) ;
$$

also,(130) is transformed to

$$
\begin{array} { l } { { d c _ { j } = \displaystyle \sum _ { \rho = 1 } ^ { r } \phi _ { i \rho } d a _ { \rho } , } } \\ { { { } } } \\ { { d t _ { 0 } = \displaystyle - \sum _ { \rho = 1 } ^ { r } \phi _ { n \rho } d a _ { \rho } ~ ( j = 1 , 2 , . . . , n - 1 ) . } } \end{array}
$$

If we know $a _ { \rho } ( t )$ ,then,when (130) is solved, $c _ { j } ( t )$ should be a functional of $a _ { \rho } ( t )$ . Let the domain of $t$ be $T .$ ，and the two ends be $t _ { 0 } + T _ { 0 }$ and $t _ { 0 } + T _ { 1 }$ .Take a distribution function

$$
F ( t - t _ { 0 } , c _ { 1 } , . . . , c _ { n - 1 } , a _ { 1 } , . . . , a _ { r } )
$$

and consider the mean value of an arbitrary function

$$
J ( t - t _ { 0 } , c _ { 1 } , . . . , c _ { n - 1 } , a _ { 1 } , . . . , a _ { r } ) colon
$$

$$
\bar { J } = \int _ { t _ { 0 } + T _ { 0 } } ^ { t _ { 0 } + T _ { 1 } } J F d t \biggl / \int _ { t _ { 0 } + T _ { 0 } } ^ { t _ { 0 } + T _ { 1 } } F d t ,
$$

where the integration is to be carried out with regard to $t$ appearing explicitly in the integrands.By applying this operation to (l31) we obtain

$$
d c _ { j } = \sum _ { \rho = 1 } ^ { r } \bar { \phi } _ { j \rho } d a _ { \rho } , d t _ { 0 } = - \sum _ { \rho = 1 } ^ { r } \bar { \phi } _ { n \rho } d a _ { \rho } ;
$$

the formulas still contain $t _ { 0 } , c _ { 1 } , . . . , c _ { n - 1 } , a _ { 1 } , . . . , a _ { r } ,$ but the parameters $a _ { 1 } , \ldots , a _ { r }$ are now considered independent variables. This happens when the parameters $a _ { 1 } , \ldots , a _ { r }$ vary slowly. An invariant expression of the system of differential equations of the form (l33）which were obtained by taking the mean values is called an adiabatic invariant (Levi-Civita, 1928,1928a; Kneser,1924; Graf,1936; Mineur,1937; Mattioli, 1932; Geppert, 1928,1929;Lenard,1959).In order that

$$
J ( t _ { 0 } , c _ { 1 } , . . . , c _ { n - 1 } , a _ { 1 } , . . . , a _ { r } )
$$

shall be an adiabatic invariant, it must satisfy the condition

$$
\sum _ { j = 1 } ^ { n - 1 } \frac { \partial J } { \partial c _ { j } } \bar { \phi } _ { j \rho } - \frac { \partial J } { \partial t _ { 0 } } \bar { \phi } _ { n \rho } + \frac { \partial J } { \partial a _ { \rho } } = 0 \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

If一 referring to (l34)- we can show that the differential coefficients of all orders with respect to $t$ of the mean values defined by (132) are zero,then the function is said to be invariant in the mean with regard to (128),where the word “in the mean" is taken in the sense of Section 2.2. According to the definition commonly accepted by physicists an adiabatic invariant is a function of the integration constants and the parameters of (l28) that is invariant in the mean for all values of

$$
d a _ { \rho } / d t \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

From such a definition, we obtain,

$$
\frac { d J } { d t } = \sum _ { \rho = 1 } ^ { r } \bigg ( \sum _ { i = 1 } ^ { n - 1 } \frac { \partial J } { \partial c _ { i } } \phi _ { i \rho } - \frac { \partial J } { \partial t _ { 0 } } \phi _ { n \rho } + \frac { \partial J } { \partial a _ { \rho } } \bigg ) \frac { d a _ { \rho } } { d t } .
$$

As $J$ does not depend on $t$ explicitly, this definition requires the validity of (134). This notion was conceived by Ehrenfest (19l7) and developed by Burgers (l9l7). It deals with invariance in the mean rather than ordinary invariance. An external electromagnetic field, for example, in quantum mechanics varies very slowly and slightly compared to variations within the atom itself, so we can quantize with respect to action variables which are considered to be adiabatically invariant.

The mean value defined by (l32) depends on $t _ { 0 }$ . Now suppose that it does not. Then the domain $T _ { \ast }$ also,does not depend on $t _ { 0 }$ .We consider with Levi-Civita (l928) the mean value taken over the infinite space

$$
[ J ] = \operatorname* { l i m } _ { \tau \to \infty } \Bigg \{ \int _ { t _ { 0 } } ^ { t _ { 0 } + T } J F d t \Bigg / \int _ { t _ { 0 } } ^ { t _ { 0 } + T } F d t \Bigg \} .
$$

The definition of adiabatic invariants is narrower,and the adiabatic invariant $\boldsymbol { J }$ must identically satisfy the condition

$$
\sum _ { i = 1 } ^ { n - 1 } \frac { \partial J } { \partial c _ { i } } \left[ \phi _ { i \rho } \right] + \frac { \partial J } { \partial a _ { \rho } } = 0 \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

As any function of one or more than one adiabatic invariants is also an adiabatic invariant,our problem is to find independent particular solutions of the equations (l36).The number of such solutions is at most $n - 1$ and they express the relation between the parameters and

$$
c _ { 1 } , \ldots , c _ { n - 1 } .
$$

In particular, an integral $f _ { i } ( x _ { 1 } , . . . , x _ { n } ) = c _ { i }$ independent of the parameters is an adiabatic invariant.

The first $n - 1$ integrals (l29） determine the trajectory $s$ in the $\pmb { n }$ -dimensional space.Suppose that $f _ { i }$ is single-valued or at least a finitenumber-valued function along $S _ { i }$ ,and suppose in addition that,if the domain $_ { T }$ of $t$ over which to take the mean extends to infinity $f _ { i }$ has the property of ergodicity - because we have no information as to its behavior in the infinite domain. That is,almost all trajectories defined by (128) fill up densely a certain continuum $\Phi$ .The words “ almost all" are used (in the sense of point-set theory) to mean that we exclude such values of the integration constants as the Lebesgue measure zero (Sections 2.9,2.l9).As we shall see later,Poincaré and Carathéodory proved that such a situation occurs when the trajectories are in a domain of $R _ { n }$ with finite measure,and the last multiplier $M$ for (128) is either positive or zero (at most) at points of measure zero in that domain. This is called the quasi-ergodic theorem (see Dragoni, 1934,1934a).

If we make this additional assumption, then the trajectory fills up densely the $( n - m )$ -dimensional continuum $\Phi$ if $_ m$ of the $\pmb { n }$ equations (l29) are single-valued (or, at most, finite-number-valued) along the whole trajectory and the remaining $n - m$ equations are infinite-numbervalued. For convenience we assume that $\Phi$ is a closed continuum. The number $_ { n }$ is called the primitivity order.When $m = 0$ ,the space $\Phi$ is called primitive. By this assumption of quasi-ergodicity in topology，we can replace the operations extending over $t$ of (135) by the operations with respect to the space $\Phi$

Consider the case $m = 1$ . There is only one integral,

$$
f ( x _ { 1 } , . ~ . ~ . , x _ { n } , a _ { 1 } , . ~ . ~ . , a _ { \tau } ) = c ,
$$

and it determines an $( n - 1 )$ -dimensional hypersurface $\Phi$ . Assume that ${ \pmb F } =$ constant. Suppose that an element $d \Phi _ { 0 }$ of $\Phi$ at $t = t _ { 0 }$ is displaced to $d \Phi _ { 1 }$ at $t = t _ { 1 }$ ,and define a single-valued density function $\pmb { \kappa }$ such that $\kappa _ { 0 } d \Phi _ { 0 } = \kappa _ { 1 } d \Phi _ { 1 }$ . The quantity $\kappa _ { 0 }$ is the value of $\pmb { \kappa }$ where $t = t _ { 0 } { \mathrm { : } }$ and $\kappa _ { 1 }$ is the value where $t = t _ { 1 }$ .There is one and only one function $\pmb { \kappa }$ with this property. For, if there were another single-valued function $\lambda$ with this property， then $\kappa d \Phi$ and $\lambda d \Phi$ would be invariant because of (l28). $\mathrm { o } \mathfrak { n }$ the assumption that $f$ is a finite-number-valued function, the ratio $\kappa { : } \lambda$ ,being an integral of (l28),should be a function of $f ,$ that is, should be equal to a constant on the hypersurface $\Phi$ .Hence, there is only one such $\pmb { \kappa }$

Now consider besides $f = c$ another hypersurface $f = c + d c$ and call it $\Phi ^ { \prime }$ .Let $P ^ { \prime }$ be the point of intersection of the normal to $\Phi$ at a point $P$ of $\Phi$ with the hypersurface $\Phi ^ { \prime }$ . The direction cosines of the normal are

$$
\begin{array} { l } { \displaystyle \frac { 1 } { \Gamma _ { 1 n } } \frac { \partial f } { \partial x _ { i } } \qquad ( i = 1 , 2 , . . . , n ) , } \\ { \displaystyle } \\ { \Gamma _ { 1 n } ^ { 2 } = \sum _ { i = 1 } ^ { n } \Big ( \frac { \partial f } { \partial x _ { i } } \Big ) ^ { 2 } . } \end{array}
$$

If we denote a line element of the normal by $d ^ { \prime } n$ ,then $\Gamma _ { 1 n } d ^ { \prime } n = d c _ { 3 }$ and the components of $d ^ { \prime } n$ are

$$
d ^ { \prime } x _ { i } = { \frac { 1 } { \Gamma _ { 1 n } ^ { 2 } } } { \frac { \partial f } { \partial x _ { i } } } d c \qquad ( i = 1 , 2 , . . . , n ) .
$$

Let the volume element situated between $\Phi$ and $\Phi ^ { \prime }$ be $d \tau = d \Phi d ^ { \prime } n$ · As the trajectories are on this hypersurface, by (l28), then

$$
d \tau = d \Phi _ { 1 } d ^ { \prime } n _ { 1 } .
$$

Hence,

$$
\kappa d \Phi d c = \kappa \Gamma _ { 1 n } d \tau = \kappa _ { 1 } d \Phi _ { 1 } d c = [ \kappa \Gamma _ { 1 n } ] _ { 1 } d \tau _ { 1 } ,
$$

that is, $M d \tau = M _ { 1 } d \tau _ { 1 } ,$ where $M = \kappa  { \Gamma _ { 1 n } }$ . In other words, $\int M d \tau$ is an integral invariant with regard to (l28). Hence, $M$ is the last multiplier by the theorem in Section 4.18 due to Poincaré and must satisfy

$$
\sum _ { i = 1 } ^ { n } { \frac { \partial M } { \partial x _ { i } } } X _ { i } + M \sum _ { i = 1 } ^ { n } { \frac { \partial X _ { i } } { \partial x _ { i } } } = 0 .
$$

Hence, if $M$ is the last multiplierof (128), then $\kappa = M / \Gamma _ { 1 n }$

Assume here that the solution of (l37） is finite-number-valued and does not become zero on $\Phi$ except at a finite number of points and that its sign is positive; then, instead of (l35),we have

$$
[ J ] = \int _ { \Phi } J { \frac { M } { \Gamma _ { 1 n } } } d \Phi \biggl / \int _ { \Phi } { \frac { M } { \Gamma _ { 1 n } } } d \Phi .
$$

If $F \not \equiv 1$ , then

$$
[ J ] = \int _ { \Phi } J F \frac { M } { \Gamma _ { 1 n } } d \Phi \biggl / \int _ { \Phi } F \frac { M } { \Gamma _ { 1 n } } d \Phi .
$$

Hence, the adiabatic invariant (l36) can be writen

$$
\begin{array} { r } { \frac { \partial J } { \partial c } \displaystyle \int _ { \Phi } \frac { \partial f } { \partial a _ { o } } F \frac { M } { \Gamma _ { 1 n } } d \Phi + \frac { \partial J } { \partial a _ { o } } \displaystyle \int _ { \Phi } F \frac { M } { \Gamma _ { 1 n } } d \Phi = 0 } \\ { ( \rho = 1 , 2 , . . . , r ) . } \end{array}
$$

Geppert (1928,l929),by studying the condition for integrability of this equation,derived the necessary and sufficient condition for the existence of adiabatic invariants in a dynamical system of primitivity order $m = 1$ to be

$$
\begin{array} { r l r } {  { [ \frac { \partial f } { \partial a _ { \rho } } \frac { \partial \log M F } { \partial a _ { \lambda } } ] - [ \frac { \partial f } { \partial a _ { \rho } } ] [ \frac { \partial \log M F } { \partial a _ { \lambda } } ] } } \\ & { } & \\ & { } & { = [ \frac { \partial f } { \partial a _ { \lambda } } \frac { \partial \log M F } { \partial a _ { \rho } } ] - [ \frac { \partial f } { \partial a _ { \lambda } } ] [ \frac { \partial \log M F } { \partial a _ { \rho } } ] } \\ & { } & { ~ ( \lambda , \rho = 1 , 2 , . . . , r ) . } \end{array}
$$

In particular, if $\begin{array} { r } { \sum _ { i = 1 } ^ { n } \partial X _ { i } / \partial x _ { i } = 1 } \end{array}$ ， the dynamical system is called Liouville's； the $\pmb { n } .$ dimensional volume $V = \int d x _ { 1 } , \ldots , d x _ { n }$ is now an integral invariant of the system of differential equations. It occurs in the motion of an incompressible fluid.In this case $M =$ constant and $F = 1$ · Such a dynamical system has been studied by Gibbs and Hertz in analytical dynamics and statistical mechanics.We can prove that an adiabatic invariant exists by taking the mean value. In general, when MF does not depend on the parameters $a _ { 1 } , \ldots , a _ { r }$ we call it the general Liouville case； there then exists an adiabatic invariant.Hence, there exists an integral invariant

$$
V = \int _ { \Phi } M F d x _ { 1 } \cdot \cdot \cdot d x _ { n }
$$

extended over the inside of the hypersurface $\Phi$ . In the general Liouville case,because of (l40),equations (l38) can be written in the form

$$
\frac { \partial J } { \partial c } \frac { \partial V } { \partial a _ { \rho } } - \frac { \partial J } { \partial a _ { \rho } } \frac { \partial V } { \partial c } = 0 \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

Thus

$$
J = V = \int M F d x _ { 1 } \cdot \cdot \cdot d x _ { n }
$$

is an adiabatic invariant in the general Liouville case.

For a general value of $m$ , if there exists an independent integral

$$
f _ { i } ( x _ { 1 } , . . . , x _ { n } ; a _ { 1 } , . . . , a _ { r } ) = c _ { i } \qquad ( i = 1 , 2 , . . . , m ) ,
$$

then,by a similar argument, the density function $\kappa$ is $M / \Gamma _ { m n }$ ，where $\Gamma _ { m n } ^ { 2 }$ is the square of the matrix $( \partial ( f _ { 1 } , . . . , f _ { m } ) / \partial ( x _ { 1 } , . . . , x _ { m } ) )$ ; and the differential equations for the adiabatic invariants corresponding to (138) are written

$$
\begin{array} { r } { \displaystyle \sum _ { i = 1 } ^ { m } \frac { \partial J } { \partial c _ { i } } \int _ { \Phi } \frac { \partial f _ { i } } { \partial a _ { \rho } } F \frac M { \Gamma _ { m n } } d \Phi + \frac { \partial J } { \partial a _ { \rho } } \int _ { \Phi } \frac M { \Gamma _ { m n } } d \Phi = 0 } \\ { ( \rho = 1 , 2 , . . . , r ) . } \end{array}
$$

Now consider the Hamiltonian canonical equations

$$
\frac { d q _ { i } } { d t } = \frac { \partial H } { \partial p _ { i } } , \frac { d \phi _ { i } } { d t } = - \frac { \partial H } { \partial q _ { i } } ~ ( i = 1 , 2 , . . . , n ) ,
$$

where $H$ does not contain $t$ explicitly. There exists the integral $H = h$ Assume that there exists no integral other than $H = h$ and that the hypersurface $H = h$ is closed and that the dynamical system is quasiergodic (as is evident from topological transformations of the motion of molecules in a cube - see Section 2.9).Then the integral $\int F M d x _ { 1 } \cdot \cdot \cdot d x _ { n }$ extended over the volume in the phase space $\Phi _ { 2 n }$ bounded by $H = h$ is an adiabatic invariant；in particular, if $F =$ constant in a Liouville dynamical system, then the volumeis an integralinvariant.If,inaddition, (142) has $m$ integrals $f _ { i } = c _ { i }$ mutually in involution, then

$$
( f _ { \alpha } , f _ { \beta } ) = 0 ( \alpha , \beta = 1 , 2 , . . . , m ) ,
$$

and

$$
( f _ { \alpha } , H ) = 0 \qquad ( \alpha = 1 , 2 , . . . , m ) .
$$

Let the $_ m$ solutions of ${ \dot { f } } _ { i } = c _ { i }$ with respect to the $\pmb { \mathscr { p } } _ { \alpha }$ be

$$
\qquad p _ { \alpha } = p _ { \alpha } ( { p } _ { m + 1 } , \ldots , { p } _ { n } , q _ { 1 } , \ldots , q _ { n } , c _ { 1 } , \ldots , c _ { m } ) \qquad ( \alpha = 1 , 2 , \ldots , m )
$$

and the result of the substitution of these equations in $\boldsymbol { H }$ be $\mathfrak { S }$ By Lie's theorem (Section 4.8), the canonical equations (l42) are reduced to the differential equations of order $2 ( n - m )$ ：

$$
\frac { d q _ { i } } { d t } = \frac { \partial \underline { { \sf { S } } } } { \partial \underline { { \theta } } _ { i } } , \frac { d \underline { { \phi } } _ { i } } { d t } = - \frac { \partial \underline { { \sf { S } } } } { \partial q _ { i } } ( i = m + 1 , . . . , n ) ,
$$

and $q _ { \alpha }$ are obtained from

$$
{ \frac { d q _ { \alpha } } { d t } } = { \frac { \partial \Im } { \partial / _ { \alpha } } } \qquad ( \alpha = 1 , 2 , . . . , m ) .
$$

We call such a $2 ( n - m )$ -dimensional space the reduced phase space. If the reduced phase space is closed and the dynamical system is Liouville's, then the volume $W ( h , c _ { 1 } , \ldots , c _ { m } )$ of this phase space is an integral invariant. If $H$ and $f _ { \alpha }$ both contain the parameters $a _ { 1 } , \ldots , a _ { r } ,$ and hence, $\mathfrak { H }$ contains $2 m + r$ parameters $q _ { 1 } , . . . , q _ { m } ; c _ { 1 } , . . . , c _ { m } ; a _ { 1 } , . . . , a _ { r } ,$ all of which vary slowly, and if the trajectory is quasi-ergodic in the $2 ( n - m )$ 1 dimensional closed space, then the volume $W ( h , c _ { 1 } , \ldots , c _ { r } )$ of the reduced phase space is an adiabatic invariant. Since $H = h$ ，we rewrite it $f _ { 0 } = c _ { 0 }$ ; then

$$
( f _ { \alpha } , f _ { \beta } ) = 0 ( \alpha , \beta = 0 , 1 , 2 , . . . , m ) .
$$

Let the result of substitution of these $\pmb { m }$ integrals for $\pmb { \mathscr { p } } _ { 1 } , \ldots , \pmb { \mathscr { p } } _ { m }$ in the $m + 1$ functions $f _ { \alpha }$ be $F _ { \alpha }$ ; then,

$$
{ \frac { d q _ { i } } { d t } } = { \frac { \partial F _ { \alpha } } { \partial p _ { i } } } , { \frac { d p _ { i } } { d t } } = - { \frac { \partial F _ { \alpha } } { \partial q _ { i } } } \qquad ( i = m + 1 , . . . , n ; \alpha = 0 , 1 , . . . , m )
$$

All these equations give the same trajectory. There exist adiabatic invariants $W _ { 1 } , . . . , W _ { m }$ ，which are the respective $2 ( n - m )$ -dimensional volumes corresponding to $F _ { 1 } , . . . , F _ { m }$ taken for $F _ { 0 }$ or $\mathfrak { S }$ in the above discussion.Hence, such a dynamical system given by canonical equations of primitivity order $m + 1$ has $m + 1$ adiabatic invariants,if the $_ m$ independent integrals are mutually in involution.

A special case $n = m + 1$ contains the case in which the solution can be obtained by separation of the variables according to Stäckel's theorem. Suppose that the functions $\phi _ { \alpha \beta }$ (which appeared in our proof of Stäckel's theorem in Section 1.16） are functions of $q _ { \beta }$ only.Then we have $\pmb { n }$ integrals

$$
F _ { \alpha } = \sum _ { \beta = 0 } ^ { n - 1 } \phi ^ { \alpha \beta } \bigl ( { \textstyle \frac { 1 } { 2 } } \beta _ { \beta } ^ { 2 } - U _ { \beta } \bigr ) \qquad ( \alpha = 0 , 1 , . . . , n - 1 ) ,
$$

where $U _ { \beta }$ is a function of a single variable $q _ { \beta }$ only,and this function $F _ { \alpha }$ is a function of a single variable $q _ { \alpha }$ only.Hence the $F _ { \alpha }$ are mutually in involution. The reduced phase space is now a plane, and there exist $\pmb { n }$ adiabatic invariants

$$
W _ { i } = \int / p _ { i } d q _ { i } \qquad ( i = 0 , 1 , . . . , n - 1 ) .
$$

This proves that the action in quantum mechanics is an adiabatic invariant (Section 2.7). The proof does not necessitate our assuming that the periods of the component oscillations are mutually in irrational ratios as in the case studied by Burgers (l9l7).Thus we have proved the fundamental theorem of quantum mechanics. We have assumed quasiergodicity in our proof; this assumed property is valid in any Liouville dynamical system,as will be shown in a later chapter on the fundamentals of the statistical mechanics for Boltzmann's theory.We have proved that the coordinates and the momenta in Stäckel's case are expressed by quasi-periodic functions (Section 2.lO).Hence, the motion in this case is quasi-periodic.

Lenard (l959） has shown that a positive constant $M > 0$ can be found such that,for a change $\Delta Q$ of anadiabaticinvariant $Q , | \Delta Q | < \epsilon ^ { n } M$ for all small values of $\epsilon$ ; he states that the adiabatic invariant is of the nth order. There are many such invariants that are known to be constant to a low order but are actually so to all orders. Kulsrud (1957) proved this proposition for the harmonic oscilltor and Lenard (i959) for nonlinear oscillators.

The theory of adiabatic invariants has been applied to the fall of meteoric swarms or cosmic dust,and to the motion of celestial bodies of varying mass as affected by the ejection or accretion of their masses (Levi-Civita,1934).

# 4.24ADIABATIC APPROXIMATION

A function $\textstyle J ( p , q , \lambda )$ was called by Arnold (1963) an adiabatic invariant of a dynamical system with the Hamiltonian

$$
H ( p , q , \lambda ) \qquad ( p = p _ { 1 } , p _ { 2 } , . . . , p _ { n } ; q = q _ { 1 } , q _ { 2 } , . . . , q _ { n } ) ,
$$

on the following condition: for any $\kappa > 0$ there exists a $\mu _ { 0 } > 0$ ，such that, if $0 < \mu < \mu _ { 0 } ;$ ，then for any $t$ in the interval $0 < t < 1 / \mu$ there exists the inequality $| J ( t ) - J ( 0 ) | < \kappa$

The linear oscillatory system occupies an exceptional position: the frequency of its oscillation does not depend on the amplitude,and there are well-defined resonant frequencies. In nonlinear systems the frequency changes with amplitude, and oscillations do not grow without limit as in the linear case； that is, the condition of resonance in the nonlinear case changes as the amplitude changes.

Consider a nonlinear oscillatory system with one degree of freedom with the analytic Hamiltonian function $H ( p , q , \lambda )$ depending on slow time $\lambda = \mu t$ periodically, that is, $H ( p , q , \lambda + 2 \pi ) = H ( p , q , \lambda )$ . In the zero-th approximation, $\lambda =$ constant and the system is autonomous, i.e., independent of an explicit appearance of $t .$ Also, the system is integrable. Periodic motions of the system can be described by the action $I$ and angle w. We have

$$
{ \boldsymbol { \beta } } = { \frac { \partial { \boldsymbol { S } } } { \partial { \boldsymbol { q } } } } , \qquad { \boldsymbol { w } } = { \frac { \partial { \boldsymbol { S } } } { \partial { \boldsymbol { I } } } } ,
$$

with the generating function

$$
S ( I , q ) = \int _ { H = h } ^ { q } \ d h \ d h \phi d q ,
$$

where $H _ { 0 } ( I ) = h$ is the inverse function of

$$
I ( h ) = { \frac { 1 } { 2 \pi } } \int _ { H = h } \beta d q ,
$$

and $\pmb { \mathscr { p } }$ is the value derived from $H ( p , q ) = h$ .The variation of $\boldsymbol { I }$ and w with time is given by

$$
\dot { w } = \omega ( I ) = \frac { \partial H _ { 0 } } { \partial I } , I = \mathrm { c o n s t a n t } .
$$

This approximation is valid during the time $t \sim 1$

The transformation $\begin{array} { r } { p , q  I , } \end{array}$ w being canonical, the change of $\boldsymbol { I }$ and ${ \boldsymbol w }$ is determined by the canonical equations with the Hamiltonian

$$
H ( I , w , \lambda ) = H _ { 0 } ( I ) + \mu H _ { 1 } ( I , w , \lambda ) ,
$$

where $H _ { 1 } = \partial S / \partial \lambda$ is a single-valued function of period $2 \pi$ with respect to w and $\lambda$ In the space of $\dot { p } , q , \lambda$ the equation $\boldsymbol { I } =$ constant determines a two-dimensional torus and the angular variables ${ \boldsymbol w }$ and $\lambda$ are the latitude and longitude on this torus (Figure l).In the conservative approximation $\mu = 0$ ,every point of the torus moves along the meridian $\lambda =$ constant with angular velocity $\omega ( I , \lambda )$ depending on the longitude. For $\mu \neq 0$ a slow motion $\lambda = \mu$ is added across the meridian; the motion now has two frequencies. But in the approximation the phase point remains on the invariant torus $\boldsymbol { I } =$ constant.This approximation is called adiabatic. The true motion is near the adiabatic approximation during $t \sim 1 / \mu$

![](images/56d62c7240a3dceb69548e793383a78dcdca73d5be171a4804aabd8b8ca77896.jpg)  
FIGURE I

In the linear system, $\omega ( I ) =$ constant, $H _ { 0 } ( I ) = I \omega$ In the nonlinear system,

$$
\bar { \omega } ( I ) = \frac { 1 } { 2 \pi } \oint \omega ( I , \lambda ) d \lambda = \frac { d \bar { H } _ { 0 } } { d I } , \qquad \bar { H } _ { 0 } ( I ) = \frac { 1 } { 2 \pi } \oint H _ { 0 } ( I , \lambda ) d \lambda .
$$

On the assumption that

$$
\frac { d ^ { 2 } \bar { H } _ { 0 } } { d I ^ { 2 } } = \frac { d \bar { \omega } } { d I } \neq 0
$$

we can prove the perpetual adiabatic invariance of $\boldsymbol { \mathit { I } }$ (Arnold, 1963).

In the case of the perturbed Hamiltonian (143) we obtain many invariant tori, real but not approximate, near the tori $\pmb { I } =$ constant, for small $\pmb { \mu }$ .They are two-dimensional and divide the three-dimensional phase space ${ p , q , \lambda }$ into thin layers.It can be proved that, if the initial point $( \ p _ { 0 } , q _ { 0 } , \lambda )$ lies between two such tori $T _ { 1 }$ and $T _ { 2 }$ ，then the entire trajectory ${ \mathfrak { p } } ( t ) , q ( t ) , \lambda ( t )$ lies wholly between $T _ { 1 }$ and $T _ { 2 }$ ; more precisely, we can find a constant $\mu _ { 0 } > 0$ for any $\kappa > 0$ such that, if $| \mu | < \mu _ { 0 } ;$ then any point $( \ p _ { 0 } , q _ { 0 } , \lambda _ { 0 } )$ lies between two invariant tori $T _ { 1 }$ and $T _ { 2 } ,$ and

$$
| I ( \phi _ { 1 } , q _ { 1 } , \lambda _ { 1 } ) - I ( \phi _ { 2 } , q _ { 2 } , \lambda _ { 2 } ) | < \kappa
$$

for $\not p _ { 1 } , q _ { 1 } , \lambda _ { 1 }$ belonging to $T _ { 1 }$ and $\phi _ { 2 } , q _ { 2 } ,$ $\lambda _ { 2 }$ belonging to $T _ { 2 }$

Let us introduce a new time, $T = w$ .Then the integral curves of the Hamiltonian system in space $I , w , \lambda$ are invariantly connected with the differential form

$$
I d w - H ( I , w , \mu t ) d t = - { \frac { 1 } { \mu } } ( H d \lambda - \mu I d w ) .
$$

Consider $H , \lambda ,$ w instead of $I , w ,$ $t$ as the independent variables. Solving (143) with respect to $I ,$ we obtain

$$
 I ( H , \lambda , w ) = I _ { 0 } ( H , \lambda ) + \mu I _ { 1 } ( H , \lambda , w ) + \cdot \cdot \cdot .
$$

Put

$$
{ \boldsymbol { \dot { p } } } ^ { \prime } = { \boldsymbol { H } } , \qquad { \boldsymbol { q } } ^ { \prime } = \lambda , \qquad { \boldsymbol { T } } = { \boldsymbol { w } } , \qquad { \boldsymbol { K } } = \mu { \boldsymbol { I } } .
$$

Then,

$$
\begin{array} { c } { { K ( \phi ^ { \prime } , q ^ { \prime } , T ) = \mu I _ { 0 } ( \phi ^ { \prime } , q ^ { \prime } ) + \mu ^ { 2 } I _ { 1 } ( \phi ^ { \prime } , q ^ { \prime } , T ) + \cdot \cdot \cdot , } } \\ { { { } } } \\ { { H d \lambda - \mu I d w = \phi ^ { \prime } d q ^ { \prime } - K ( \phi ^ { \prime } , q ^ { \prime } , T ) d T , } } \end{array}
$$

so that the systems with the Hamiltonians (143) and (145) are equivalent. The variables $\pmb { P }$ and $Q$ are introduced by the canonical transformation

$$
{ \boldsymbol { \rlap / p } } ^ { \prime } = { \frac { \partial S } { \partial { \boldsymbol { q } } ^ { \prime } } } , \qquad { \boldsymbol { Q } } = { \frac { \partial S } { \partial { \boldsymbol { P } } } } ,
$$

with the generating function

$$
S ( P , q ^ { \prime } ) = \int ^ { q ^ { \prime } } H _ { 0 } ( I , \lambda ) \ d \lambda .
$$

The Hamiltonian is transformed to

$$
k ( P , Q , T ) = \mu k _ { 0 } ( P ) + \mu ^ { 2 } k _ { 1 } ( P , Q , T ) + \cdot \cdot \cdot ,
$$

where $I = k _ { 0 } ( P )$ is a function inverse to $H _ { 0 } ( I )$

Hence,if the frequency $\omega ( I , \lambda ) = \partial H _ { 0 } / \partial I$ does not vanish, there exist analytic functions $P , \ Q , \ T$ of the variables $I , \ w , \ \lambda$ independent of $\pmb { \mu } ,$ such that (i) $P , Q \mathrm { ~ - ~ } \lambda , T \mathrm { ~ - ~ } w$ are periodic with period $2 \pi$ with respect to ${ \pmb w }$ and $\lambda$ ； (ii) the canonical equations with the Hamiltonian (143) are equivalent to the equations

$$
\frac { d P } { d T } = - \frac { \partial k } { \partial Q } , \frac { d Q } { d T } = \frac { \partial k } { \partial P } ,
$$

with the Hamiltonian (146) which is of period $2 \pi$ with respect to both $Q$ and $_ { T }$ and analytic in the complex neighborhood of the toroidal layer $\left| P - I _ { 0 } \right| \leqslant r$ ；and (iii） the function $k _ { 0 } ( P )$ of (146） is inverse to $\textstyle { \overline { { H } } } _ { 0 } ( I )$ ， so that $\overrightarrow { H } _ { 0 } ( k _ { 0 } ( P ) ) = P$

From this theorem we can show the perpetual adiabatic invariance of action.

Theorem: Let an oscillatory system be described by the analytic Hamiltonian (l43),in terms of action-angle variables. Suppose that $\partial H _ { 0 } / \partial I \neq 0$ and $\partial ^ { 2 } H _ { 0 } / \partial I ^ { 2 } \neq 0$ in the region $\left| I - I _ { 0 } \right| \leqslant r$ everywhere. If for any $\kappa > 0$ we can find a constant $\mu _ { 0 } > 0$ such that $| \mu | < \mu _ { 0 }$ and $\left| I ( 0 ) - I _ { 0 } \right| \le r - \kappa _ { : }$ then $| I ( t ) - I ( 0 ) | < \kappa$ will hold for

$$
- \infty < t < + \infty .
$$

In fact, the system with the Hamiltonian (l46) can be reduced to a set of canonical equations with two degrees of freedom

$$
{ \frac { d P } { d \tau } } = \tau { \frac { \partial k } { \partial Q } } , { \frac { d R } { d \tau } } = - { \frac { \partial k } { \partial T } } , { \frac { d Q } { d \tau } } = { \frac { \partial k } { \partial P } } , { \frac { d T } { d \tau } } = 1 ,
$$

with'the Hamiltonian

$$
R + k ( P , Q , T ) = R + \mu k _ { 0 } ( P ) + \mu ^ { 2 } k _ { 1 } ( P , Q , T ) \cdot \cdot \cdot _ { \cdotp } 
$$

The Hamiltonian is now of the degenerateform

$$
H ( p , q ) = H _ { 0 } ( p _ { 0 } ) + \mu H _ { 1 } ( p , q ) + \cdot \cdot \cdot ,
$$

where $\pmb { \mathscr { p } } = ( \pmb { \mathscr { p } } _ { 0 } , \pmb { \mathscr { p } } _ { 1 } )$ with a vector $\scriptstyle { \pmb { \mathscr { p } } } _ { 0 }$ of $n _ { 0 }$ dimensions (corresponding to the fast-varying coordinates $q _ { 0 , }$ ）and a vector ${ \pmb { \mathscr { p } } } _ { 1 }$ of $\pmb { n } _ { 1 }$ dimensions, $\boldsymbol { n } = \boldsymbol { n } _ { 0 } + \boldsymbol { n } _ { 1 }$ (corresponding to the slow-varying coordinates $q _ { 1 , }$ ).The solution with this Hamiltonian (l47) in the zero-th approximation is

$$
\begin{array} { l l r } { { \dot { \boldsymbol { { p } } } _ { 0 } = 0 , \quad } } & { { \dot { \boldsymbol { { q } } } _ { 0 } = \omega _ { 0 } ( \boldsymbol { { p } } _ { 0 } ) = \displaystyle \frac { \partial H _ { 0 } } { \partial { p _ { 0 } } } , } } \\ { { \dot { \boldsymbol { { p } } } _ { 1 } = 0 , \quad } } & { { \dot { \boldsymbol { { q } } } _ { 1 } = \mu \xi _ { 1 } ( \boldsymbol { { p } } ) = \mu \displaystyle \frac { \partial \overline { { { H } } } _ { 1 } } { \partial { p _ { 1 } } } , } } \end{array}
$$

where $\omega _ { 1 } = \mu \xi _ { 1 }$ for the slow frequencies.

Assume that the secular part of the perturbation averaged over the fast motion in the coordinates $q _ { 0 }$ ，

$$
\overline { { { H } } } _ { 1 } = \frac { 1 } { ( 2 \pi ) ^ { n _ { 0 } } } \oint H _ { 1 } d q _ { 0 } ,
$$

is independent of the phase $q _ { 1 }$ of the slow motion $\overline { { H } } _ { 1 } = \overline { { H } } _ { 1 } ( \mathfrak { p } _ { 1 } )$ ,and that the determinants of the frequencies with respect to momenta $\scriptstyle { \pmb { \mathscr { p } } } _ { 0 }$ and $\pmb { \mathscr { p } _ { 1 } }$ do not become identically zero; that is,

$$
\left\| \frac { \partial \omega _ { 0 } } { \partial { \boldsymbol { \beta } } _ { 0 } } \right\| = \left\| \frac { \partial ^ { 2 } { \boldsymbol { H } } _ { 0 } } { \partial \boldsymbol { \beta } _ { 0 } ^ { 2 } } \right\| \not \equiv 0 , \qquad \left\| \frac { \partial \xi _ { 1 } } { \partial \boldsymbol { \beta } _ { 1 } } \right\| = \left\| \frac { \partial ^ { 2 } \boldsymbol { \bar { H } } _ { 1 } } { \partial \boldsymbol { \beta } _ { 1 } ^ { 2 } } \right\| \not \equiv 0 .
$$

Arnold (1963) has proved that for sufficiently small $\left| \mu \right|$ for the majority of initial conditions - that is, all except for sets of Lebesgue measure zero 一 the motion determined by the canonical equations with the

Hamiltonian (l47） is conditionally periodic (Section 2.7） and，for $- \infty < t < + \infty$ ,is near the conditionally periodic motion (l48).This is, according to Arnold, because almost every vector

$$
\omega = ( \omega _ { 1 } , \omega _ { 2 } , . . . , \omega _ { n } )
$$

satisfies the inequalities

$$
| ( \omega , k ) | \geq K | k | ^ { - \nu } \qquad ( | k | = | k _ { 1 } | + \cdot \cdot \cdot + | k _ { n } | ; \nu = n + 1 )
$$

for all integers $k \neq 0$ for some $K ( \omega ) > 0$ ,as will be discussed in a later chapter.

Thus we come to the following conclusion: If the function (l46） is analytic for $| \mu | < \bar { \mu }$ in the region

$$
| \Im Q , T | \le \rho , \qquad | P - P _ { 0 } | \le r
$$

and in this region

$$
\left| k _ { 0 } \right| \le M , \qquad \left| k _ { 1 } + ( \mu ) \cdot \cdot \cdot \right| \le M , \qquad \left| \frac { d ^ { 2 } k _ { 0 } } { d P _ { 0 } ^ { 2 } } \right| \ge \theta > 0 ,
$$

then for any $\kappa > 0$ we can find a constant $\mu _ { 0 } ( \kappa , \bar { \mu } , r , \rho , M , \theta ) > 0$ with the property that if $| \mu | < \mu _ { 0 } ;$ ，then the real toroidal layer $F _ { : }$ ，where $\left| P - P _ { 0 } \right| \le r ,$ is filled with invariant tori with accuracy up to the remainder of measure smaller than $\kappa m ( F )$ , and each of these invariant tori is located at a distance less than $\kappa$ from some torus $P =$ constant. Hence the theorem on the perpetual adiabatic invariance of action is proved.

We confine ourselves for the moment to a conservative dynamical system with two degrees of freedom $X ,$ ，Y.Assume that the change of the coordinate $X$ only slightly affects the state of the system; then the system can be regarded as having approximately one degree of freedom but depending on the slowly varying parameter $\mu X .$ .The action variable $I _ { Y }$ takes the part of an adiabatic invariant.

Lemma : Let $X , ~ Y , ~ P _ { x } , ~ P _ { \mathrm { { y } } }$ be canonically conjugate variables,and consider the Hamiltonian $H ( \mu X , Y , P _ { X } , P _ { Y } )$ .If $\mu$ is small, then the state of the system changes little during the variation of $\pmb { X }$ by a first-order quantity. If we fix $\pmb { X }$ and $P _ { x }$ ，then the Hamiltonian is of the form $H _ { \mathrm { Y } } ( Y , P _ { \mathrm { Y } } )$ , and the action variable is $I _ { \mathbf { Y } } ( Y , P _ { \mathbf { Y } } )$ . The value of $I _ { \tt Y }$ depends also on $\mu X$ and $P _ { x }$ 、For the Hamiltonian $H ( \mu X , Y ; P _ { X } , P _ { Y } )$ , the action $I _ { Y }$ is an adiabatic invariant in the sense that the change of

$$
I _ { Y } [ \mu X ( t ) , Y ( t ) , P _ { X } ( t ) , P _ { Y } ( t ) ]
$$

during the time $t \sim 1 / \mu$ is small,and of order $\mu$ . This Hamiltonian can be written $H = H _ { x } ( \mu X , P _ { X } , I _ { Y } )$ . For an approximation the system is described by the Hamiltonian $H _ { x } ( \mu X , P _ { x } )$ . If it is periodic with one degree of freedom, then we introduce the action variable $I _ { x } \sim 1 / \mu$ .In the adiabatic approximation, the motion consists of fast oscillations of $Y$ with frequence $\omega _ { Y }$ and slow oscillations of $X$ with frequency ${ \pmb { \omega } } _ { { \pmb { X } } }$ of order $\mu$ ,in which $I _ { x }$ and $\mu I _ { x }$ are conserved.

Suppose that the Hamiltonian is reduced to the form (145).That $I _ { Y }$ and $\mu I _ { x }$ are perpetual adiabatic invariants is easily concluded from the existence of two-dimensional invariant tori, since these tori divide the three-dimensional energy level $H = h$

Let the Hamiltonian function $H ( x , Y , P _ { x } , P _ { y } )$ ，where $x = \mu X ,$ be analytic and,for a fixed $_ x$ , let the momenta $P _ { x }$ determine the oscillatory system with the variables $I _ { \mathtt { Y } } ( x , P _ { x } , h )$ and $w _ { \mathrm { Y } } ( x , Y , P _ { x } , P _ { \mathrm { Y } } )$ . Then there exists an analytic substitution, changing $x , Y , P _ { x } , P _ { y }$ to

$$
x ^ { \prime } , w ^ { \prime } , P ^ { \prime } , I ^ { \prime }
$$

such that: (i) the functions o $\mathrm { f } \ x , \ Y , P _ { x } , P _ { \mathrm { y } }$ admit period $2 \pi$ with regard to ${ \boldsymbol w }$ ，and for $\mu \to 0$ the variables $x ^ { \prime } , \ w ^ { \prime } , \ P ^ { \prime } , \ I ^ { \prime }$ are transformed to $x , w _ { \mathrm { Y } } , P _ { x } , I _ { \mathrm { Y } }$ ； (ii) along the integral curves of the canonical equations

$$
\frac { d P _ { X } } { d t } = \triangledown - \frac { \partial H } { \partial X } , \frac { d P _ { Y } } { d t } = \triangledown - \frac { \partial H } { \partial Y } , \frac { d X } { d t } = \frac { \partial H } { \partial P _ { X } } , \frac { d Y } { d t } = \frac { \partial H } { \partial P _ { Y } } ,
$$

the canonical equations

$$
{ \frac { d P ^ { \prime } } { d w ^ { \prime } } } = - { \frac { \partial K } { \partial x ^ { \prime } } } , \qquad { \frac { d x ^ { \prime } } { d w ^ { \prime } } } = { \frac { \partial K } { \partial P ^ { \prime } } }
$$

are satisfied with the Hamiltonian $K ( P ^ { \prime } , x ^ { \prime } , w ^ { \prime } , h )$ depending on the parameter $h$ ; and (ii) the function $\kappa$ is of the form $K = - \mu I ^ { \prime }$ where

$$
{ \cal I } ^ { \prime } = { \cal I } _ { 0 } ( P ^ { \prime } , x ^ { \prime } , h ) + \mu { \cal I } _ { 1 } ( P ^ { \prime } , x ^ { \prime } , w ^ { \prime } , h ) + \cdot \cdot \cdot
$$

is analytic with period $2 \pi$ with respect to $w ^ { \prime }$ and

$$
I _ { 0 } ( P ^ { \prime } , x ^ { \prime } , h ) = I _ { Y } ( x ^ { \prime } , P ^ { \prime } , h ) .
$$

From $H ( x , Y , P _ { X } , P _ { Y } ) = h _ { ! }$ we have

$$
P _ { \mathrm { Y } } = P _ { \mathrm { Y } } ( x , P _ { X } , Y , h ) .
$$

Assume that $\begin{array} { r } { 2 \pi I _ { \tt Y } ( { \boldsymbol { x } } , { \boldsymbol { P } } _ { \boldsymbol { x } } , h ) = \oint P _ { \tt Y } d X } \end{array}$ ；then this relation determines $h ( x , P _ { x } , I _ { \mathrm { Y } } )$ . The generating function $P ^ { \prime } X + S ( x , P ^ { \prime } , Y , I ^ { \prime } )$ , where

$$
S = \int ^ { \tau } P _ { \mathrm { { r } } } [ x , P ^ { \prime } , Y , h ( x , P ^ { \prime } , I ^ { \prime } ) ] d Y ,
$$

gives the canonical transformation $X , Y , P _ { x } , P _ { y }  X ^ { \prime } , w ^ { \prime } , P ^ { \prime } , I ^ { \prime } ;$ according to the formulas

$$
X ^ { \prime } = X + \frac { \partial S } { \partial P ^ { \prime } } , \qquad w ^ { \prime } = \frac { \partial S } { \partial I ^ { \prime } } , \qquad P _ { X } = P ^ { \prime } + \mu \frac { \partial S } { \partial x } , \qquad P _ { Y } = \frac { \partial S } { \partial Y } .
$$

Assume that $\scriptstyle x ^ { \prime } = \mu X ^ { \prime }$ . Since

$$
H \{ x , Y , P ^ { \prime } , P _ { Y } [ x , P ^ { \prime } , Y , h ( x , P ^ { \prime } , I ^ { \prime } ) ] \} = h ( x , P ^ { \prime } , I ^ { \prime } ) ,
$$

then

$$
\begin{array} { l } { { H \{ x , Y , P _ { X } , P _ { Y } [ x , P ^ { \prime } , Y , h ( x , P ^ { \prime } , I ^ { \prime } ) ] \} = h ( x , P ^ { \prime } , I ^ { \prime } ) } } \\ { { \ } } \\ { { \qquad + \ \mu \frac { \partial S } { \partial x } \frac { \partial H } { \partial P _ { X } } + \cdot \cdot \cdot . . } } \end{array}
$$

Substituting the new variables for $\pmb { X }$ and $\pmb { Y } _ { : }$ ，we obtain

$$
H ( x , Y , P _ { X } , P _ { Y } ) = h ( x ^ { \prime } , P ^ { \prime } , I ^ { \prime } ) + \mu \frac { \partial S } { \partial x } \frac { \partial H } { \partial P _ { X } } - \mu \frac { \partial S } { \partial P ^ { \prime } } \frac { \partial H } { \partial x } + \cdot \cdot \cdot ,
$$

or

$$
\begin{array} { r l } & { H ( x , Y , P _ { X } , P _ { Y } ) = H ^ { \prime } ( x ^ { \prime } , P ^ { \prime } , I ^ { \prime } , w ^ { \prime } ) } \\ & { \qquad = h _ { 0 } ( x ^ { \prime } , P ^ { \prime } , I ^ { \prime } ) + \mu h _ { 1 } ( x ^ { \prime } , P ^ { \prime } , I ^ { \prime } , w ^ { \prime } ) + \cdot \cdot \cdot , } \end{array}
$$

where $\mu h _ { 1 } + \cdots$ is analytic, with period $2 \pi$ with respect to $w ^ { \prime }$

Taking $P ^ { \prime } , X ^ { \prime } , - H , t , w ^ { \prime }$ ,instead of $P ^ { \prime } , X ^ { \prime } , I ^ { \prime } , w ^ { \prime } , t$ as the new independent variables,and taking $\boldsymbol { h }$ as a parameter and $w ^ { \prime }$ as time, gives the new Hamiltonian as $I ^ { \prime } ( X ^ { \prime } , P ^ { \prime } , w ^ { \prime } , h )$ , where $\scriptstyle { I ^ { \prime } }$ is determined by

$$
H ^ { \prime } ( \mu X ^ { \prime } , P ^ { \prime } , I ^ { \prime } , w ^ { \prime } ) = h .
$$

The coordinate $t$ is cyclical. By dropping $- \pmb { H }$ and $t$ we obtain a nonautonomous system with one degree of freedom.Multiply $X ^ { \prime }$ and $- \boldsymbol { I ^ { \prime } }$ by the constant $\mu$ ，giving $\mu X ^ { \prime } = x ^ { \prime }$ and $- \mu I ^ { \prime } = K .$ ，The derivatives of $\scriptstyle { \boldsymbol { x } } ^ { \prime }$ and $P ^ { \prime }$ with respect to $w ^ { \prime }$ are determined by the canonical equations (149)with the Hamiltonian $\kappa$

These forms of $\boldsymbol { \mathit { \Pi } } _ { H }$ and $\scriptstyle { H ^ { \prime } }$ show that $\scriptstyle { { \cal { I } } ^ { \prime } }$ is of the form (150); thus the lemma is proved.

According to (150),the Hamiltonian $\kappa$ is of the form (145). By our assumption, the inequalities (l44) are satisfied. Consequently the argument in the proof of perpetual adiabatic invariance can be applied. Hence the existence of the invariant tori and the perpetual adiabatic invariance of $I _ { Y }$ and $\mu I _ { x }$ are proved,on the assumption that the mean value $\overline { { ( \omega _ { \mathbf { Y } } / \omega _ { X } ) } }$ of the ratio $\omega _ { Y } / \omega _ { X }$ depends on $I _ { \tt Y }$ for a fixed total energy $h$

Thus the method of adiabatic approximation is justified in this case of two degrees of freedom.

Arnold applied the idea to the problem of magnetic traps, that is, the motion of a charged particle in a magnetic field.Assume that the instantaneous radius of the helix along which the particle moves is small compared with the distance at which the field changes by a noticeable amount.Arnold has shown that in axially-symmetric magnetic traps the adiabatic invariant $W _ { \perp } / B$ is conserved perpetually， where $B$ is the magnetic field intensity and $W _ { \perp }$ is the kinetic energy of the motion transverse to the field.

Suppose that the magnetic field $\boldsymbol { B }$ is derived from the vector potential $A _ { , }$ ，which has only one component $A _ { \phi } = A ( r , z )$ in polar coordinates. Then the intensity is

$$
B _ { \tau } = - { \frac { \partial A } { \partial z } } , B _ { z } = { \frac { 1 } { r } } { \frac { \partial ( r A ) } { \partial r } } ,
$$

and the lines of force are given by $r A =$ constant and $\phi =$ constant. The Lagrangian function for unit charge and unit mass is

$$
L = { \textstyle \frac { 1 } { 2 } } ( \dot { r } ^ { 2 } + \dot { z } ^ { 2 } + r ^ { 2 } \dot { \phi } ^ { 2 } ) + r A \dot { \phi } ,
$$

and the Hamiltonian is

$$
H = \frac { 1 } { 2 } \left[ \rlap { / } p _ { \tau } ^ { 2 } + \rlap { / } p _ { z } ^ { 2 } + \frac { ( \rlap { / } p _ { \phi } - r A ) ^ { 2 } } { r ^ { 2 } } \right] .
$$

Since $\phi$ is cyclical, the value of $\hbar _ { \phi } = M$ is constant. There remains to be studied the plane motion in the field with potential

$$
U ( r , z ) = \frac { ( M - r A ) ^ { 2 } } { 2 r ^ { 2 } } .
$$

This potential represents a potential ditch with zero bottom along the line of force $r A = M .$ In the neighborhood of this line we have

$$
U ( r , z ) = { \textstyle \frac { 1 } { 2 } } B ^ { 2 } y ^ { 2 } + \cdot \cdot \cdot ,
$$

where $y$ is the distance to a line of force.

We introduce the coordinates $_ x$ and $y$ by

$$
d r ^ { 2 } + d z ^ { 2 } = [ 1 + y k ( x ) ] ^ { 2 } d x ^ { 2 } + d y ^ { 2 } ,
$$

where $k ( x )$ is the curvature of the line of force at the point $( x , 0 )$ . The new Hamiltonian is

$$
\begin{array} { c } { { H = \displaystyle \frac { 1 } { 2 } \left. \frac { \rlap / p _ { x } ^ { 2 } } { [ 1 \ + y k ( x ) ] ^ { 2 } } + \rlap / p _ { y } ^ { 2 } \right. + U , } } \\ { { U ( x , y ) = \textstyle \frac { 1 } { 2 } B ( x ) y ^ { 2 } + \cdot \cdot \cdot . . } } \end{array}
$$

![](images/614368bc3e56be54c3ce116ea375867a35e9cba193a69e3f5beff8c106d30fb7.jpg)  
FIGURE 2

We are interested in the case in which the radius of the Larmor spiral described by the charge around the lines of force is very small compared with the characteristic dimension of the field；that is,we consider the values of $M$ and the total energy $h$ such that $U \leq h$ determines a strip of width $\sim \mu$ around the line of force $r A = M$ (Figure 2). Introduce new variables $X , Y , P _ { x } , P _ { \mathrm { y } }$ by $x = \mu X , y = \mu Y , \dot { p } _ { x } = \mu P _ { x } , \dot { p } _ { y } = \mu P _ { x }$ ；then the Hamiltonian becomes

$$
\begin{array} { c } { { \displaystyle H ^ { \prime } = \frac 1 2 \left\{ \frac { P _ { X } ^ { 2 } } { [ 1 + \mu Y k ( \mu X ) ] ^ { 2 } } + P _ { Y } ^ { 2 } \right\} + U ^ { \prime } , } } \\ { { \displaystyle I ^ { \prime } ( \mu X , Y ) = \frac 1 2 B ^ { 2 } ( \mu X ) Y ^ { 2 } + \cdot \cdot \cdot . } } \end{array}
$$

or

$$
H ^ { \prime } = \frac { P _ { X } ^ { 2 } + P _ { Y } ^ { 2 } + B ^ { 2 } ( x ^ { 2 } ) Y ^ { 2 } } { 2 } + \mu H _ { 1 } ( x , Y , P _ { X } , P _ { Y } ) + \cdot \cdot \cdot
$$

of the form similar to the foregoing.

We select the initial values of $y , \dot { x } , \dot { y }$ in such a manner that $H \sim \mu ^ { 2 }$ ， $\mu \ll 1$ ,and $y , { \dot { x } } , { \dot { y } } \sim \mu$ We fix $Y , { \dot { X } } , { \dot { Y } } ,$ choose $y = \mu Y , \dot { x } = \mu \dot { X } , \dot { y } = \mu \dot { Y } ,$ and put $\mu$ equal to zero. If the magnetic field is analytic and $B ( x ) > 0 _ { ; }$ ， $B ( { \pmb x } )  \infty$ for $| x |  \infty$ , then for any $\kappa > 0$ we have $\mu _ { 0 } > 0$ such that, if $| \mu | < \mu _ { 0 }$ ，then $| I _ { \mathrm { Y } } ( t ) - I _ { \mathrm { Y } } ( 0 ) | < \kappa$ for all $- \infty < t < + \infty$ ，where $I _ { Y } = ( P _ { Y } ^ { 2 } + B ^ { 2 } Y ^ { 2 } ) / ( 2 B )$ Hence,a particle forwhich ${ { I } _ { Y } } \ne 0$ is locked in a bounded region if the field $B$ grows indefinitely as $x \to \infty$ .This is the phenomenon of the trap with magnetic mirrors.We see that when $Y = 0$ ，then $\dot { \phi } = 0$ and ${ \dot { y } } ^ { 2 } / 2 = W _ { \perp } ,$ so that $\mu ^ { 2 } I _ { \mathrm { Y } } = W _ { \perp } / B$ .Hence,in an axially-symmetric magnetic trap,the magnetic moment $W _ { \perp } / B$ isa perpetual adiabatic invariant.

This picture of the magnetic trap applies to the motion of charged particles around the geomagnetic lines of force in the Van Allen belt, or that of charged particles along the magnetic lines of force in the solar chromosphere between pairs of sunspots.

The generalization of the theorem to multiparametric problems can be made on the assumption that

$$
| ( k , \xi ) | > K | k | ^ { - \upsilon } , \qquad | k | > 0 , \qquad \nu = n + 1 , \qquad K > 0 .
$$

The extension to the case of many degrees of freedom has yet to be accomplished.

# 4.25EXISTENCE OF ADIABATIC INVARIANTS

Kasuga (l961) has given a precise formulation of the adiabatic theorem in the general case and proved it by the operator method in the classical mechanics of von Neumann (1932) and Koopman (1931).

According to Kasuga a quantity $I ( p , q , s )$ , which is an integral of

$$
\frac { d \phi } { d t } = \mathrm { ~ - } \frac { \partial H } { \partial q } , \frac { d q } { d t } = \frac { \partial H } { \partial \phi } , H = H ( \phi , q , s ) ,
$$

where $\boldsymbol { \mathscr { p } } = ( \mathscr { p } _ { 1 } , \mathscr { p } _ { 2 } , . . . , \mathscr { p } _ { n } ) , \mathscr { q } = ( q _ { 1 } , q _ { 2 } , . . . , q _ { n } )$ ，for every fixed $s ,$ $( 0 \leq s \leq 1 ) ,$ ,is called an adiabatic invariant of (153）if $I ( p , q , t / \lambda )$ is conserved, asymptotically for $\lambda \to + \infty$ ，along almost all trajectories of the system

$$
\frac { d \phi } { d t } = \ - \frac { \partial H } { \partial q } , \frac { d q } { d t } = \frac { \partial H } { \partial \phi } , H = H ( \phi , q , t / \lambda ) ;
$$

that is (if we let $t ^ { \prime } = t / \lambda )$ , of the system

$$
\frac { d \phi } { d t ^ { \prime } } = - \lambda \frac { \partial H } { \partial q } , \frac { d q } { d t ^ { \prime } } = \lambda \frac { \partial H } { \partial \substack { \partial p } } , H = H ( p , q , t ^ { \prime } ) ,
$$

in the whole interval of time $0 \leq t \leq \lambda$ ,that is, $0 \leq t ^ { \prime } \leq 1$ .The fundamental case of the adiabatic theorem in celestial mechanics is the case in which (l53) admits no single-valued Lebesgue measurable integral other than the energy integral for almost all $s , 0 \leq s \leqq 1$ .In this case the phase volume

$$
\bar { \mathfrak { S } } ( \phi , q , s ) = \iint d \phi d q ,
$$

where the integral extended over the domain $I _ { E , s }$ in the phase space enclosed by the energy surface $S _ { E , s } = \{ ( \phi , q ) \mid E ( \phi , q , s ) = E \}$ of (153) passing through $( \pmb { \mathscr { p } } , \pmb { q } )$ , is the only adiabatic invariant.

Kasuga first determined the region $\pmb { D }$ in a $( 2 n + 1 )$ -dimensional space $R ^ { 2 n + 1 }$ such that the adiabatic theorem can be stated,and studied the properties of the phase volume function $\textstyle { \bar { \mathfrak { I } } } ( p , q , s )$ in $D$

Let $B$ be a subset of a Euclidian space or a subset of a Hilbert space (von Neumann 1929； Stone, 1932;Hille,l948)； then the closure of $\boldsymbol { B }$ in the space is denoted by $\bar { B }$ ,and the set of all inner points of $\pmb { B }$ by $B ^ { 0 }$ · A subset of $B$ is called relatively open in $B$ if it can be considered as an intersection of an open set in the Euclidian space with $B$ .We denote by $C ^ { m } ( B )$ the set of all $_ m$ -times continuously differentiable functions on $B$ 一 noting that $\bar { B } ^ { 0 } \supset B$ — and by $C _ { 0 } ^ { m } ( B )$ the set of all $_ { m } .$ -times continuously differentiable functions on $\boldsymbol { B }$ vanishing outside compact sets contained in $B$ By $\pmb { K }$ we denote the point set

$$
\begin{array} { r l r } & { } & { \{ ( { \boldsymbol { \phi } } , { \boldsymbol { q } } , { \boldsymbol { s } } ) ~ | ~ - \infty ~ < { \boldsymbol { p } } _ { i } < + \infty , - \infty < { \boldsymbol { q } } _ { i } < + \infty , i = 1 , 2 , \ldots , n , } \\ & { } & \\ & { } & { \qquad \quad \alpha \leq { \boldsymbol { s } } \leq b \} } \end{array}
$$

in $R ^ { 2 n + 1 }$ with two fixed numbers $^ { a }$ and $b , a < b$

Assume that the point set in $R ^ { 2 n + 1 }$ ，where the real-valued function $H ( p , q , s )$ is defined, contains a subset $G$ of $\kappa$ relatively open in $\pmb { K } _ { : }$ ， satisfying

$$
\sum _ { i = 1 } ^ { n } \left[ \left( { \frac { \partial H } { \partial p _ { i } } } \right) ^ { 2 } + \left( { \frac { \partial H } { \partial q _ { i } } } \right) ^ { 2 } \right] \neq 0
$$

on $G$ and the following condition:For each $s , a \leqq s \leqq b .$ ，there is an open interval

$$
\Gamma _ { s } = \{ E | E _ { s } < E < E _ { s } ^ { \prime } \} , \qquad - \infty \leq E _ { s } < E _ { s } ^ { \prime } = + \infty ,
$$

on the $E$ -line such that the set

$$
S _ { E , s } = \{ ( \phi , q ) \mid H ( \phi , q , s ) = E , ( \phi , q ) \in G _ { s } \}
$$

in the open set $G _ { s } = \{ ( p , q ) \mid ( p , q , s ) \in G \}$ is nonempty and is a closed $( 2 n - 1 )$ -dimensional manifold of $R ^ { 2 n }$ enclosing a domain $I _ { E , s }$ in $R ^ { 2 n }$ for each $E \in \Gamma _ { s }$ and $G _ { s } = \mathbf { U } _ { E \in \Gamma _ { s } }$ (the neighborhood of $S _ { E , s }$ for which $E \in \Gamma _ { s . }$ ).

Assume further that there are two numbers $J _ { 1 } ^ { * }$ and $J _ { 2 } ^ { * }$ independent of $\pmb { \mathscr { s } }$ such that $J _ { s } < J _ { 1 } ^ { * } < J _ { 2 } ^ { * } < J _ { s } ^ { \prime }$ for all $\pmb { \mathscr { s } }$ in the interval $a \leq s \leq b$ ·

Put

$$
S ( J , s ) = \{ ( p , q ) \mid ( p , q ) \in G _ { s } , { \vec { \mathbb { S } } } ( p , q , s ) = J \}
$$

and

$$
I ( s ) = I ( J _ { 1 } ^ { \ast } , J _ { 2 } ^ { \ast } , s ) = \{ ( p , q ) \mid ( p , q ) \in G _ { s } , J _ { 1 } ^ { \ast } < \mathfrak { F } ( p , q , s ) < J _ { 2 } ^ { \ast } \} .
$$

It can be seen that

$$
\Im ( E , s ) = \int _ { I _ { E , s } } d / s d q \in C ^ { \prime } ( \Lambda ) , \qquad \hat { \Im } ( / s , q , s ) = \Im ( H ( p , q , s ) , s ) ,
$$

and ${ \partial \Im } / { \partial E } > 0$ for $( E , s ) \in \Lambda _ { : }$ ， and that there is a unique measure $m _ { J , s }$ defined for all Borel subsets (Section 2.1) of each $S ( J , s )$ , such that

$$
{ \frac { d } { d J } } \int _ { I ( J _ { 1 } ^ { \star } , J , s ) } f ( \beta , q ) \ d \beta \ d q = \int _ { S ( J , s ) } f ( \beta , q ) \ d m _ { J , s } ,
$$

for every function $f ( \pmb { \mathscr { p } } , q ) \in C _ { 0 } ^ { 0 } [ I ( s ) ]$ ，where $m _ { J , s }$ denotes the Lebesgue measure of $ { \boldsymbol { S } } (  { \boldsymbol { J } } ,  { \boldsymbol { s } } )$ . For any integrable function $f ( \pmb { \mathscr { p } } , \pmb { q } )$ on $I ( s )$ ，the function $f ( \pmb { \mathscr { p } } , \pmb { q } )$ is integrable on the measure space $( S ( J , s ) , m _ { J , s } )$ for almost all $J \left( J _ { 1 } ^ { * } < J < J _ { 2 } ^ { * } \right)$ ,and

$$
\int _ { S ( J , s ) } f ( \pmb { \mathscr { p } } , q ) ~ d m _ { J , s }
$$

is integrable with respect to $\boldsymbol { J }$ in the interval $J _ { 1 } ^ { * } < J < J _ { 2 } ^ { * }$ and

$$
\begin{array} { l } { { \displaystyle \int _ { I ( s ) } f ( { \boldsymbol { p } } , { \boldsymbol { q } } ) \ d { \boldsymbol { p } } \ d { \boldsymbol { q } } = \int _ { J _ { 1 } ^ { \star } } ^ { J _ { 2 } ^ { \star } } \left( \int _ { S ( J , s ) } f ( { \boldsymbol { p } } , { \boldsymbol { q } } ) \ d m _ { J , s } \right) d J , } } \\ { { \displaystyle \int _ { S ( J , s ) } d m _ { J , s } = 1 . } } \end{array}
$$

Kasuga then proved the theorem that

$$
\int _ { S ( J , s ) } { \frac { \partial { \bar { \mathfrak { S } } } } { \partial s } } \left( \phi , q , s \right) d m _ { J , s } = 0
$$

for $J _ { 1 } ^ { * } < J < J _ { 2 } ^ { * }$ and $a \leq s \leq b$

He then studied the flows defined by (l53) and the infinitesimal generator $i A$ of the unitary group $\{ U _ { t } \mid - \infty < t < + \infty \}$ associated with one of the flows,and proved the adiabatic theorem for a multidimensional and multiparametric case.

Let $( X , m )$ be a measure space where $_ m$ is a finite, separable,and complete measure (Section 2.1） defined on a Borel field in $X .$ Aoneparameter group $\{ \mathfrak { T } _ { t } \ | \ - \infty < t < \ + \infty \}$ of a one-to-one mapping ${ \mathfrak { T } } _ { t }$ of $X$ onto $\boldsymbol { \chi }$ is called a flow on $( X , m )$ . A measurable function $f ( P )$ on $( X , m )$ is called an invariant function of a fow $\{ \mathfrak { T } _ { t } \}$ on $( X , m )$ if

$$
f ( \mathfrak { T } _ { t } ( P ) ) = f ( P )
$$

almost everywhere on $( X , m )$ for every fixed $\scriptstyle t$ 、It is called a strictly invariant function of a flow $\{ \mathfrak { T } _ { t } \}$ on $( X , m )$ if it is defined everywhere on $X$ and if $f ( \mathfrak { T } _ { t } ( P ) ) = f ( P )$ for all $( P , t )$ such that $P \in X _ { \mathrm { { i } } }$ $- \infty < t < + \infty$ A measure-preserving and measurable flow $\{ \mathfrak { T } _ { t } \}$ on $( X , m )$ is ergodic in the sense used by von Neumann (l932) if and only if all its invariant functions are equivalent, that is, coincident almost everywhere, to constants on $( X , m )$ . If a flow $\{ \mathfrak { T } _ { t } \}$ on $( X , m )$ is measure-preserving and measurable, then we can associate with it a one-parameter group $\{ \mathfrak { U } _ { t } \ | \ - \infty < t < \ + \infty \}$ of unitary transformations (Section 1.18) ${ \mathfrak { U } } _ { t }$ on $L ^ { 2 } ( X , m )$ by

$$
\mathfrak { U } _ { t } f ( P ) = f ( \mathfrak { T } _ { t } ( P ) ) , \qquad f \in L ^ { 2 } ( X , m ) , \qquad P \in X ;
$$

and ${ \mathfrak { U } } _ { t }$ is a continuous function of $t$ in the strong topology of ${ \mathfrak { U } } _ { t }$ . (See a later chapter on the ergodic theorem.)

If $X$ is a Lebesgue measurable subset of a Euclidian space $R ^ { \tau }$ ,and $\pmb { m }$ is the Lebesgue measure of $R ^ { \tau }$ defined for all Lebesgue measurable subsets of $X ,$ a flow on $( X , m )$ is called a flow on $X .$

Consider the Hamiltonian system with a parameter ${ \pmb S } .$ The canonical equations are

$$
\frac { d \phi } { d t } = \mathrm { ~ - } \frac { \partial H } { \partial q } , \frac { d q } { d t } = \frac { \partial H } { \partial \phi } , H = H ( \phi , q , s ) .
$$

By assumption, the solution of this system,

$$
\begin{array} { r } { \dot { p } = \dot { p } ( t , \dot { p } ^ { 0 } , q ^ { 0 } , s ) , \qquad q = q ( t , \dot { p } ^ { 0 } , q ^ { 0 } , s ) , } \end{array}
$$

in the open set $I ( s )$ for a fixed $s$ $\mathit { \Pi } ^ { \prime } a \ \leq \ s \ \leq \ b \ \mathrm { , }$ ）with the initial conditions

$$
( \phi , q ) = ( \phi ^ { 0 } , q ^ { 0 } ) , \qquad ( \phi ^ { 0 } , q ^ { 0 } ) \in I ( s ) , \qquad { \mathrm { a t ~ } } t = 0
$$

can be uniquely prolonged for the whole interval $- \infty < t < + \infty$ and

$$
\left( \phi ( t , \phi ^ { 0 } , q ^ { 0 } , s ) , q ( t , \phi ^ { 0 } , q ^ { 0 } , s ) \right) \in S ( J , s ) \qquad { \mathrm { f o r ~ } } - \infty < t < + \infty
$$

if $( \phi ^ { 0 } , q ^ { 0 } ) \in S ( J , s )$

Denote by $T _ { t } ^ { ( s ) }$ the one-to-one mapping of $I ( s )$ onto $I ( s )$ ：

$$
\left( \phi ^ { 0 } , q ^ { 0 } \right) \to \left( \phi ( t , \phi ^ { 0 } , q ^ { 0 } , s ) , q ( t , \phi ^ { 0 } , q ^ { 0 } , s ) \right) ;
$$

then $\{ T _ { t } ^ { \mathfrak { ( s ) } } | - \infty < t < + \infty \}$ constitutes a flow $F _ { s }$ on $I ( s )$ for $a \leq s \leq b$ The flow $F _ { s }$ on $I ( s )$ is measure-preserving and measurable. Since $T _ { t } ^ { \left( s \right) }$ transforms $S ( J , s )$ onto $S ( J , s )$ , the mapping $T _ { t } ^ { \left( s \right) }$ induces a one-to-one mapping $T _ { t } ^ { ( J , s ) }$ of $S ( J , s )$ onto $S ( J , s )$ , such that

$$
\{ T _ { t } ^ { ( J , s ) } \mid - \infty < t < + \infty \}
$$

constitutes a flow $F _ { J , s } ,$ ， which is measure-preserving and measurable, on the measure space $( S ( J , s ) , m _ { J , s } )$ for $J _ { 1 } ^ { * } < J < J _ { 2 } ^ { * }$ ， $a \leq s \leq b$ . Kasuga proved the equivalence for any fixed $s$ of two conditions: (i) that every invariant function $f ( \pmb { \mathscr { p } } , \pmb { q } )$ of the flow on $I ( s )$ is equivalent on $I ( s )$ to a function of the form $\phi ( H ( \pmb { \mathscr { p } } , q , s ) )$ , where $\phi ( E )$ is a measurable function