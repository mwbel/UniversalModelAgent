# 第五章 分支程序设计

在解决实际问题的过程中，处理进行到某一步骤后，经常会因具体条件的需要把下面的处理分成两支来进行。例如，让小学生做一道数学题，得到的答案可能是对的，也可能是错误的。如果答对了，可以再出更难些的题目让他去练习；如果错了，就要给他讲解。一般说来，用计算机处理这类问题，需要执行一种特殊结构的程序，即这一章要介绍的分支结构的程序。

# 第一节分支结构的程序

例5.1 下面是一个能让计算机判断从键盘上输入的数是否符合要求的程序。

5REM 输入数据  
10 PRINT"请输入下面算术题的答案："  
20 PRINT  
30 INPUT $" 3 4 + 4 3 = " ;$ X  
35REM 判断并输出结果  
40 PRINT  
50 LET $\mathbf { S } = 3 4 + 4 3$   
60 IF $\mathbf { S } = \mathbf { X }$ THENPRINT"对了，非常好!"：END  
70 PRINT"不对！应该是"；S  
80 END

运行这个程序，计算机执行10，20,30语句时，屏幕上会显示出

请输入下面算术题的答案：

$$
3 4 + 4 3 =
$$

询问 $3 4 + 4 3$ 等于多少，并等待从键盘上输入答案。如果键入了正确答案，计算机会执行60语句行中的PRINT"对了，非常好!"：END,在屏幕上输出

对了，非常好！并结束程序的运行；如果键入了错误的答案，计算机会执行语句70,在屏幕上输出

不对！应该是77

然后结束程序的运行。为什么运行同一个程序会产生两种不同的结果呢？其中60语句行起了非常重要的作用。

# 一、条件语句

例5.1里程序中的60语句行叫做条件语句。条件语句的格式是$\textbf { I F } <$ 关系式 $>$ THEN $<$ 语句组 $>$

像例5.1中的 $\displaystyle \mathbf { S } = \mathbf { X }$ 这样表示条件的式子，叫做关系式。关系式由用关系运算符连接起来的表达式组成。BASIC语言中的关系运算符与数学中常用的略有不同，一般用 $> =$ 表示“大于或等于”，用 $< =$ 表示“小于或等于”，用 $< >$ 表示“不等于”。其他关系运算符与数学符号相同，即用 $=$ 表示“相等”，用 $>$ 表示“大于”，用 $<$ 表示“小于”。

计算机执行条件语句时，先判断关系式给出的条件是否成立，如果成立就执行THEN后面的语句组 (THEN后面可以有用冒号隔开的多个语句)；如果条件不成立，就执行这个条件语句下面的语句行，而不执行THEN后面的语句。这样就把处理过程依条件是否成立而分成了两支。

英语单词IF有“如果”的意思，THEN有“那么就”的意思。从字面上看，60语句行的意思是：“如果 $\mathbf { S } = \mathbf { X }$ ,那么就输出‘对了，非常好！’，然后结束”，程序运行时也确实如此。

例5.1中60语句行的最后是END语句，所以程序进人这个分支，显示出信息后就结束了。如果THEN后面没有END语句，情况又会怎样呢？请看下面的例题。

例5.2某单位每月向各用户收煤气费，计费办法如下：4人以下和4人的用户，每户收6元；超过4人的用户，每超出1人加收1.2元。设计一个程序，按输人的人数计算应收费的金额。

分析：据题意可知，无论人数是否超过4人，都要收6元。所以程序的一个分支应该是只要输入了人数，就输出“应收6元"；输人人数超过4人的另一个分支，应在6元的基础上增加每人1.2元的金额。先试编一个程序。

程序：

10 LET $\mathbf { Y } = 6$   
20 INPUT"人数 $\mathbf { \Lambda } = \mathbf { \Lambda } ^ { \prime \prime } ; \mathbf { N }$   
30IF $\mathbf { N } { > } 4$ THEN LET $\mathbf { Y } = \mathbf { Y } + \left( \mathbf { N } - 4 \right) * 1 . 2$   
40 PRINT“应收 ${ \pmb \mathscr { n } }$ ；Y；"元"   
50 END

运行这个程序，执行到语句20,屏幕上显示出“人数 $=$ ”时，先输入一个小于4的人数，例如 3,屏幕上会显示出

应收6元 1

再运行一次，输入一个大于4的人数，例如7,这时屏幕上会显示出

]

两次的结果都是正确的。用不同的值输入时都能输出正确的结果，这说明当输入的数大于4时，语句30中的关系式 $\mathbf { N } { > } 4$ 成立，计算机执行THEN后面的赋值语句，把Y的值增加了应增的金额，然后也执行语句40,输出了处理的结果。

从这个例子可以看到，当条件语句的THEN后面没有END语句时，如果条件成立，计算机执行完THEN后面的语句序列后，继续按行号的顺序向下执行。

# 二、分支结构的程序

用条件语句编程序解决实际问题的过程中，依照一定的条件分别处理时，每一个分支可能有较多的步骤。这时，需要使用GOTO语句构成完整的分支结构。请看下面的例子。

例5.3某公司批发软磁盘。购货数量不足500片时，按定价每片4.5元收款，购货数量达到或超过500片时，按定价的 $8 5 \%$ 收款。据此，可以编出下面这样的程序。

10 HOME  
20 INPUT“请输入购货数量："；N  
30 IF $\mathbf { N } { < } 5 0 0$ THEN LET ${ \bf P } { = } 4 . 5$ : GOTO 50  
40 LET $\mathbf { P } { = } 4 . 5 { * } 8 5 / 1 0 0$   
50 LET ${ \mathrm { T L } } { = } \mathbf { P } { * } \mathbf { N } $   
60 PRINT  
70 PRINT"数量",”单价",”金额"  
80 PRINT N,P, TL  
90 END

程序中30语句行是一个条件语句。THEN后面是用冒号隔开的两个语句，后一个叫做GOTO语句。GOTO语句的语句定义符取自英语go to,意为“到……去”。语句定义符后面是程序中的某个语句行的行号。从字面上看，GOTO语句的意思是：“到xx行去继续执行”。

![](../chunks/易照华《天体力学基础》南京大学出版社_part_0002_pages_0071-0140/auto/images/045511238e9a4c208d588f8f484d978207189b05f89620ffe00d53939e4fa27b.jpg)  
图5.1

计算机执行GOTO语句时，就是让程序转到GOTO后面的行号指定的语句行去继续执行。

GOTO语句的格式是

GOTO $<$ 行号 $>$

有了GOTO语句的控制，运行这个程序输人了小于500的数时，计算机会先执行30语句行中 THEN后面的LET语句，把表示单价的变量P的值变成4.5,然后执行后面的GOTO50,转到语句行50去按照每片4.5元的单价计算货款金额。然后再顺序执行60、70、80等语句行。

如果没有这个GOTO50,输入了小于 500的数后，尽管THEN后面的LET语句把变量P的值变成了4.5,由于下面接着执行的是语句行40,又会把变量P的值变成定价的 $8 5 \%$ 。这样计算出来的金额就不符合规定了。

这个程序在计算机上运行的流程如图5.1所示，图中的菱形框表示判断。

上述几个例子中的程序，都是由条件语句把处理过程分成两个分支，计算机依照不同的条件选择其中的一支进行处理，然后再沿程序的主干方向继续运行。我们把这类程序叫做分支结构的程序，简称分支程序。

# 第二节分支程序设计

用计算机解决实际问题时，分支程序的应用很普遍。这一节主要介绍一下分支程序设计的基本方法。

# 一、分支程序设计

例5.4设计一个程序，用来判断从键盘上输入的是否是正数。如果是正数，输出时加上正号，否则照原数输出。

分析：根据题意，程序可以用图5.2(a)所示的框图来描述。图5.2(a)中的“输入一个数”的操作用INPUT语句可以很容易地实现；判断“是正数吗?”的操作可以用一个条件语句来完成;对两种数的输出操作，可以安排不同的PRINT语句。

程序：

10 REM输入一个数  
20 INPUT $\mathbf { \overrightarrow { x } } = \mathbf { \prime }$ ; X  
30REM判断是否正数  
40 IF $\mathbf { X } < = 0$ THEN GOTO 70  
50 REM 输出正数  
60 PRINT $" + "$ ; X : END  
70 REM 输出非正数  
80 PRINT X  
90 END

设计这个程序的过程中,写到40 IF $\mathbf { X } { < } { = } 0$ THEN时，暂不设

![](../chunks/易照华《天体力学基础》南京大学出版社_part_0002_pages_0071-0140/auto/images/f256f3a9b54fe0c11da029d135998cd5995592c5dc03ee25097933846f6af7c4.jpg)  
图5.2(a)

![](../chunks/易照华《天体力学基础》南京大学出版社_part_0002_pages_0071-0140/auto/images/baeb4147d05f6c56337be4b37031a35e8f03a35d943959528ab30a2bbfa0df2d.jpg)  
图5.2(b)

计THEN后面的处理步骤，而是先设计条件 $\mathbf { X } < = 0$ 不成立时那个分支的程序段，即先写出输入的数是正数时的处理步骤

60 PRINT ${ \bf \ " } + { \bf \ " } ; { \bf X } : { \bf E N D }$

然后再考虑条件 $\mathbf { X } { < } = 0$ 成立时的分支，返回去写完40语句行的其余部分GOTO 70和语句70、语句80等语句行，完成输入的数不是正数时要进行的处理步骤。

设计分支结构的程序时，常常采用这种方法，即程序产生分支时，先设计其中的一个分支，然后再设计另一个分支。两个分支必须有同一个出口 (本例的出口是END语句)。

语句40的THEN后面可以省略GOTO而只留下行号70,于是条件语句就变成了另外一种形式，即

# IF $<$ 关系式 $>$ THEN $<$ 行号 $>$

计算机执行这种形式的条件语句，当关系式表示的条件成立时，程序转到THEN后面的行号指定的语句行去执行，否则执行这个条件语句下面的语句行。

经过进一步分析还可以发现，去掉60语句行中的 $\mathbf { \mu } ^ { \ast } \mathbf { X } : \mathbf { E N D } ^ { \prime }$ ，程序的功能不变，框图如图5.2(b)所示。最后得到比较简明的程序：

10REM输入一个数  
20 INPUT $\mathbf { \ " } \mathbf { X } = \mathbf { \ " }$ ; X  
30 REM 判断  
40 IF $\mathbf { X } < = 0$ THEN 70  
50 PRINT $" + "$ ；  
60 REM 输出结果  
70 PRINT X  
80 END  
下面再看一个例子。

![](../chunks/易照华《天体力学基础》南京大学出版社_part_0002_pages_0071-0140/auto/images/cb38d3417e4674dab6eb45340fac8f829163eec88f88c4f3774635d54016b80a.jpg)  
图5.3 (a)

例5.5 设计一个程序，用来输出从键盘上输入的三个不同数中的最大数。

分析：根据题目的要求，程序可以用图5.3(a)所示的粗框图来描述，整个处理过程分为三个模块M1、M2、M3。其中M1和M3模块比较简单，对于M1模块，可用INPUT语句来实现其功能；对于M3模块，可以用PRINT语句来实现其功能。M2模块可以细化成图 5.3(b)所示的框图。根据这两个框图，可以画出图5.3(c)所示的程序流程图。

程序：用BASIC语句分别实现图5.3(c)中各部分的功能，可编程序如下。

100 REM 输人三个数A,B,C

110 INPUT"请顺序输入A,B,C的值"；A,B,C  
120 REM从A,B中找最大数存入MAX  
$1 3 0 ~ \mathrm { { I F } }$ $\mathbf { A } { > } \mathbf { B }$ THEN LET $\mathbf { M A X } { = } \mathbf { A }$ : GOTO 150  
140 LET $\mathbf { M A X } { = } \mathbf { B }$   
150 REM 从MAX,C中找最大数存人MAX  
160 IF $\mathbf { C } { \mathrm { > } } \mathbf { M } \mathbf { A } \mathbf { X }$ THEN LET $\mathbf { M A X } { = } \mathbf { C }$   
170REM 输出最大数  
180 PRINT"最大数是:"；MAX  
190 END

![](../chunks/易照华《天体力学基础》南京大学出版社_part_0002_pages_0071-0140/auto/images/ca7093b72e5a61c7c46aa534329c381d750e6db0dbfc770b59ac3cb8539611b4.jpg)  
图5.3 (b)

![](../chunks/易照华《天体力学基础》南京大学出版社_part_0002_pages_0071-0140/auto/images/e1cb59c8688d273a385af47d6f5905e0c3d359b9d27f66117356d9befa451d8c.jpg)  
图5.3 (c)

在设计第二个模块的过程中，写到130 IF $\mathbf { A } { > } \mathbf { B }$ THEN时，暂不设计后面的处理步骤，而是先设计条件 $\mathbf { A } { > } \mathbf { B }$ 不成立时那个分支的程序段，即先写出

140 LET $\mathbf { M A X } { = } \mathbf { B }$

这个语句行。完成了这个分支的设计以后，再考虑条件 $\mathbf { A } { > } \mathbf { B }$ 成立时的那个分支，即返回来再接着完成130语句行的其余部分LET $\mathbf { M A X } { = } \mathbf { A }$ $:$ GOTO150。设计语句160 时也采用相同的方法。

# 二、逻辑判断

在分支程序里，利用条件语句对关系式的判断，可以处理一些较简单的问题。实际解决问题时，给出的条件往往比较复杂，需要先进行逻辑运算才能确定条件是否成立。请先看一个例子。

例5.6某小学评选优秀学生。学习方面的条件是 $\textcircled{1}$ 语文、算术这两科的成绩必须都不低于90分，或者 $\textcircled{2}$ 这两科的总分不低于185分。利用下面这个程序，可以在输入全班所有学生的各科成绩时，让计算机输出符合条件学生的姓名和各科成绩。

10 INPUT"本班人数："；N

20 FOR $\mathbf { I } = 1$ TO N

30 INPUT“姓名：语文，算术成绩:"；N\$,CH,MA

40IF $\mathrm { C H } > = 9 0 $ AND $\mathbf { M A } > = 9 0$ OR $\mathbf { C H } + \mathbf { M A } > = 1 8 5$ THEN PRINT "姓名:"; N\$,

"语文"；CH,"算术"；MA

50 NEXT I

60 END

运行这个程序时，先输人本班学生数，计算机根据学生数自动确定循环次数。每次执行循环体时，先从键盘上输入学生的姓名，然后输入各科成绩。40语句行进行具体的判断，如果够条件就输出学生的姓名和各科成绩，否则执行50语句，决定输入下一个学生的信息或结束程序的运行。

在这个程序里，40语句中的关系式之间使用了AND、OR等符号。在BASIC语言里，这类符号叫做逻辑运算符。用逻辑运算符把关系式连接起来构成的式子叫做逻辑表达式，简称逻辑式。逻辑式表示的是逻辑运算。有关逻辑运算的知识，我们将在第十一章里讲解。在这里只介绍一些最基本的内容。

# 1.三种逻辑运算

例 5.6中用到的AND和OR 这两个逻辑运算符，分别表示“与”运算和“或”运算。

(1）“与”运算用AND把关系式连接起来表示的逻辑运算叫做“与”运算。例如，$\mathrm { C H } > = 9 0 $ AND $\mathbf { M A } > = 9 0$ 表示的就是两个关系式的“与”运算。英文单词AND在这里有“并且”的意思。因此， $\mathrm { C H } > = 9 0 $ AND $\mathbf { M A } > = 9 0$ 这个逻辑式的意思是：“CH的值不小于90并且MA的值也不小于 $9 0 ^ { \eta }$ 。“与”运算的意义是：只有参加运算的所有关系式都成立时，整个运算的逻辑式才成立。

(2）“或”运算用OR把关系式连接起来表示的逻辑运算叫做“或”运算。例如， $\mathbf { A } > = 9 5$ OR $\mathbf { B } > = 8 5$ 表示的就是两个关系式的“或”运算。英文单词OR在这里有“或者”的意思。因此， $\mathbf { A } > = 9 5$ OR $\mathbf { B } > = 8 5$ 这个逻辑式的意思是：“A的值不小于95,或者B的值不小于 $8 5 "$ 。“或”运算的意义是：只要参加运算的逻辑式中有一个成立，整个“或”运算的逻辑式就成立。

(3）“非”运算除了AND和OR以外，常用的还有一种“非”运算。“非”运算用英文单词NOT表示。例如，NOT $\mathbf { M A } > = 8 0$ 表示对 $\mathbf { M A } > = 8 0$ 这个关系式进行的“非”运算。NOT表示否定，相当于“不是”。逻辑式NOT $\mathbf { M A } > = 8 0$ 的意思是：“不是MA的值不小于 $8 0 ^ { \circ }$ ，也就是“MA的值小于 $8 0 ^ { \circ }$ 。“非”运算的意义是：参加运算的关系式如果成立，整个“非”运算的逻辑式就不成立；反之，参加运算的关系式如果不成立，整个“非”运算的逻辑式就成立。

逻辑运算的结果还可以再参加逻辑运算。例如， $\mathrm { N O T } ( \mathrm { A } < = 5 $ AND $\mathbf { A } { > } 0$ OR $\mathbf { A } { > } 1 0 0 )$ 以及这个例子里的 $\mathrm { C H } > = 9 0 $ AND $\mathbf { M A } > = 9 0$ OR $\mathbf { C H } + \mathbf { M A } > = 1 8 5$ 等。运算中，执行三种逻辑运算的优先顺序是，先执行“非”运算，其次执行“与”运算，最后执行“或”运算。如果使用了括号，优先进行括号里的运算。

# 2.逻辑值

在BASIC语言里，关系式也叫逻辑式。逻辑式的成立与否，是根据它的逻辑值来确定的。逻辑式只有“成立”和“不成立”两种可能。相应地，逻辑值只有真（true)和假(false)两个。在BASIC语言里，一般用1表示真，用O表示假。用PRINT命令或 PRINT语句都可以输出逻辑式的值。例如，用PRINT $5 { < } 9$ 可以输出1（表示 $5 { < } 9$ 成立)，用PRINT $3 + 4 = 1 2$ 可以输出0（表示 $3 + 4 = 1 2$ 不成立)，等等。

例5.7 设计一个程序，让计算机判断键盘上输入的年份是不是闰年。

分析：由历法可知，能被100整除但不能被400整除的年份，和不能被4整除的年份都不是闰年。让计算机据此对输入的年份进行逻辑判断，就可以解决题目中的问题。实际判断时，根据$\mathbf { I N T } \left( \mathbf { Y } / \mathbf { X } \right)$ 是否等于 $\mathbf { Y } / \mathbf { X }$ 就可以确定Y是否能被X整除。程序可用图5.4中框图描述。

程序：

10 REM 输入年份   
20 INPUT“年份:"；Y   
30 REM 判断   
40IF $\mathbf { Y } / 4 < > \mathbf { I N T } ( \mathbf { Y } / 4 )$ OR $\mathbf { Y } / 1 0 0 { = } \mathbf { I N T } ( \mathbf { Y } / 1 0 0 )$ AND Y/400<>INT(Y/400) THEN 70   
50 REM 输出   
60 PRINT Y；"年是闰年":GOTO 80   
70 PRINTY；"年不是闰年"   
80 END

从这个例子中可以看出，编写分支程序时，适当运用逻辑式，可以使条件语句简单、明确，从而使程序易编、易读。下面再利用逻辑式重编一下例5.5中的程序。

例5.8 重编例5.5的程序。

![](../chunks/易照华《天体力学基础》南京大学出版社_part_0002_pages_0071-0140/auto/images/c0061e72481c5ca88312ededb51b998ebd9cb763f26bf265766a9dc261cbd1e9.jpg)

分析：输入的三个数如果满足 $\mathbf { A } { > } \mathbf { B }$ 并且 图5.4$\mathbf { A } { > } \mathbf { C } ,$ ，那么A就是最大的；否则，如果满足$\mathbf { B } { > } \mathbf { A }$ 并且 $\mathbf { B } { > } \mathbf { C } $ ，那么B就是最大的；如果以上条件都不满足，那么C就是最大的。

程序：

100 REM 输入数据  
110 INPUT"A, B, $\mathbf { C } = \mathbf { \ " }$ ; A, B, C  
120 REM 找最大数存入MA  
130 IF $\mathbf { A } { > } \mathbf { B }$ AND $\mathbf { A } { > } \mathbf { C }$ THEN LET MA= A : GOTO 170  
140IF $\mathbf { B } > \mathbf { A }$ AND $\mathbf { B } { > } \mathbf { C }$ THEN LET MA $=$ B : GOTO 170  
150 LET $\mathbf { M A } { = } \mathbf { C }$   
160 REM 输出结果  
170 PRINT“最大数是:"；MA  
180 END

# 第三节应用程序举例

判断 $\mathbf { I N T } ( \mathbf { A } / \mathbf { B } ) { = } \mathbf { A } / \mathbf { B }$ 是否成立，可以确定A能否被B整除。请看下面的例题。  
例5.9 让计算机判断从键盘上输入的两个整数中第一个能否被第二个整除。

程序：

10 INPUT“被除数，除数 $\mathbf { \mu } = \mathbf { \ " } ; \mathbf { X }$ ,D   
20 IF $\mathbf { X } / \mathbf { D } { = } \mathbf { I N T } ( \mathbf { X } / \mathbf { D } )$ THENPRINT“能整除"：GOTO 40   
30 PRINT"不能整除"   
40 END

运行这个程序，输入X和D的值，如果X能被D整除，屏幕上会显示出“能整除”，否则会显示出“不能整除”。

第四章已介绍过，利用INT(X)和 ${ \bf R N D } ( { \bf X } )$ 函数,可以使表达式 $\mathbf { I N T } ( \mathbf { R N D } ( \mathbf { X } ) ^ { * } ( \mathbf { M } - \mathbf { N } ) + \mathbf { N } )$ 产生N到M之间的随机整数。请看下面的例子。

例5.10设计一个出10道10以内整数相加的题，帮助小学生练习加法运算的程序。要求在练习时指出错误，并按每题10分给出最后的成绩。

分析：设计程序时应注意利用表达式 $\mathbf { I N T } ( \mathbf { R N D } ( 1 ) ^ { * } 1 0 )$ 产生10以内的整数；利用条件语句判断键入的答案是否正确；设置执行10次的循环来完成练习10道题所需的操作。

程序：

10 LET $\mathbf { C } { = } \mathbf { 0 }$ : LET $\mathbf { N } { = } \mathbf { 0 }$ : HOME （C统计成绩，N计答对题数)   
20 FOR $\mathbf { I } = 1$ TO 10 (确定循环次数）   
30 LET $\mathbf { A } { = } \mathbf { I N T } ( \mathbf { R N D } ( 1 ) { * } 1 0 )$ (产生一个10以内的随机整数)   
40 LET $\mathbf { B } { = } \mathbf { { I N T } } ( \mathbf { R N D } ( 1 ) { * } 1 0 )$ (产生一个10以内的随机整数)   
$5 0 ~ \mathrm { P R I N T } \mathrm { ~ I ~ ; ~ } ^ { \prime \prime } ~ \prime \prime , \mathrm { ~ A ~ } ; ^ { \prime \prime } + ^ { \prime \prime } ; \mathrm { ~ B ~ } ; ^ { \prime \prime } = ? ^ { \prime \prime } ;$ (显示问题）   
60 INPUT X （输人答案）   
70 IF $\mathbf { A } + \mathbf { B } < > \mathbf { X }$ THENPRINT"错了！"：GOTO 90（判断，指出错误)   
80 LET ${ \bf C = C + 1 0 }$ : LET ${ \bf N } = { \bf N } + 1$ :PRINT"对了！”（对了加10分，计数器加1)   
90 NEXT I （循环出口）   
100 PRINT"答对题数:"；N （输出结果)   
110 PRINT“得分:"；C （输出结果)   
120 END

例5.11某年级有150个学生，设计一个程序，统计一次数学考试的成绩，输出85分以上、75分到85分、75分以下这三个分数段的得分人数和占全体学生数的百分比。

分析：判断得分是否在75分到85分这个分数段，可用一个“与”运算的逻辑式当作条件。

程序：

(设置循环初始条件)

100REM设置计数变量  
110 LET $\mathbf { H } { = } \mathbf { 0 }$ : LET $\mathbf { M } = \mathbf { 0 }$ : LET ${ \bf L } = { \bf 0 }$   
200 FOR $\mathbf { I } = 1$ TO 150  
210 REM读入一个分数  
220 READ C  
300 REM统计各分数段的人数  
310 IF $\mathbf { C } { > } 8 5$ THEN LET $\scriptstyle \mathbf { H } = \mathbf { H } + 1$ : GOTO 340

320 IF $\mathrm { C } > = 7 5$ AND $\mathbf { C } < = 8 5$ THEN LET M=M+1 : GOTO 340

(循环终端)

330 LET ${ \bf L } = { \bf L } + 1$   
340 NEXT I  
400REM计算百分比  
410 LET $\mathrm { R H } { = } \mathrm { I N T } ( \ \mathrm { H } / 1 5 0 { * } 1 0 0 0 { + } . 5 ) \ / 1 0 0 0 { ^ { \ast } } 1 0 0$   
420 LET $\mathbf { R M } = \mathbf { I N T } ( \mathbf { \Delta M } / 1 5 0 * 1 0 0 0 + . 5 ) \mathbf { \Delta } / 1 0 0 0 ^ { * } 1 0 0$   
430 LET $\mathrm { R L } = \mathrm { I N T } ( \mathrm { \ L } / 1 5 0 * 1 0 0 0 + . 5 ) \ / 1 0 0 0 ^ { * } 1 0 0$   
500 REM输出各分数段的人数和百分比  
510 PRINT $\because 8 5$ 分以上的人数：”；H,"占全年级的”；RH; $\% \%$   
520 PRINT“75-85分的人数：”；M,"占全年级的”；RM; $" \% "$   
530 PRINT“75分以下的人数：”；L,"占全年级的”；RL; $" \% "$   
600 END  
700REM设置150个数据  
710 DATA 57,83,94,87,90,（数据从略）

BASIC语言中，两个字符串可以比较大小，比较时遵循以下规则：

（1）单个字符按各自的ASCII代码的大小来比较。例如,数字0到9的ASCII代码是 48,$4 9 , \cdots , ~ 5 7$ ；大写字母从A到 $\mathbf { Z }$ 的 ASCII代码是65,66,，90。其他字符的 ASCI代码可查书后的附录。

(2）比较两个字符串时，从第一个字符开始比较，第一个字符大的字符串较大。例如,$' ^ { \prime } \mathbf { A B C D ^ { \prime \prime } { < } ^ { \prime } { B X Y ^ { \prime \prime } } }$ ， $" 9 9 " > " 2 0 0 0 0 "$ ,等等；如果第一个字符相同,则比较两个字符串的第二个字符，……，依此类推。例如，” $\mathbf { B A C Y Z ^ { \prime \prime } \Sigma < ^ { \prime \prime } B B C F ^ { \prime \prime } }$ ， $" \mathrm { \bf { X } } \mathrm { \bf { Y } } \mathrm { \bf { Z } } \mathrm { \bf { U } } ^ { \prime \prime } > " \mathrm { \bf { X } } \mathrm { \bf { Y } } \mathrm { \bf { X } } \mathrm { \bf { Y } } \mathrm { \bf { Y } } ^ { \prime \prime }$ ,等等。

(3）如果两个字符串长度不等，但前面的字符一一对应相同，那么长一些的字符串较大。例如， $\prime _ { \mathrm { \bf ~ A B C D E F ^ { \prime \prime } > A B C D ^ { \prime \prime } } }$ ， $\mathbf { \ " A } \ \mathbf { B } \mathbf { B } ^ { \prime \prime } { < } \mathbf { " A }$ BBCC",等等。

(4）如果两个字符串长度相等，组成字符串的所有字符也一一对应相同，则两个字符串相等。例如, $'  \ g _ { \mathrm { H } * \mathrm { B } ^ { \prime \prime } } = " \ g _ { \mathrm { H } * \mathrm { B } ^ { \prime \prime } }$ ， $" \mathrm { \bf ~ A B } ^ { \prime \prime } + " \mathrm { \bf ~ C D } ^ { \prime \prime } = "$ ABCD",等等。

例5.12 从10个字符串中选一个最小的输出。

分析：要解决这个问题，可以用DATA语句设置10个字符串。先把第一个字符串存入一个变量 SS,并假定它是最小的。然后依次读取其余的9个字符串，与 S\$中的字符串进行比较，如果读来的字符串更小就取而代之。待第10个字符串比较完了，S\$中存放的就是10个字符串中最小的。

程序：

100 REM读取第一个字符串

110 READ S\$

120 REM 读取其余的字符串并比较

130 FOR $\mathbf { I } = 1$ TO 9  
140 READ N\$  
$1 5 0 ~ \mathrm { { I F } }$ $\mathbf { N S } { < } \mathbf { S } \mathbf { S }$ THEN LET $\mathbf { S } \mathbb { S } = \mathbf { N } \mathbb { S }$ (如果读来的更小就取代S\$中原有的串)  
160 NEXT I  
170输出结果  
180 PRINT"最小的字符串是:"；S\$  
190 END  
200.REM 设置数据  
210 DATA LET,PRINT, INPUT,END,REM  
220 DATA SQR,INT,RND, IF,THEN

# 本章小结

1．本章介绍了分支程序。分支程序的核心是条件语句。条件语句也叫IF-THEN语句。计算机执行条件语句时，先判断表示条件的关系式是否成立，如果成立就执行THEN后面的语句组，使处理进入一个分支，如果不成立就执行条件语句下面的那个语句行，使处理进入另一个分支；在分支程序里，由条件语句控制计算机选择两个分支之一进行处理，无论选择了哪个分支，处理结束后都要回到程序的主干上来。也就是说，分支程序只有一个入口和一个出口。

2．编写分支程序时，一般应先设计IF-THEN语句下面条件不满足时的那一个分支，然后再设计条件满足时THEN后面的那一个分支。需要时，注意用GOTO语句使分支程序有唯一的出口。

3．用关系式 $\mathbf { X } / \mathbf { Y } { = } \mathbf { I N T } ( \mathbf { X } / \mathbf { Y } )$ 是否成立，可以判断X是否能被Y整除。

4．适当用逻辑表达式表示条件语句中的条件，可以使程序结构清晰，易读、易懂。

5．字符串可以比大小。

6．框图也叫流程图。画程序框图时常用的符号及功用如下表所示。

<table><tr><td rowspan=1 colspan=1>符号</td><td rowspan=1 colspan=1>名称</td><td rowspan=1 colspan=1>功                                                   用</td></tr><tr><td rowspan=1 colspan=1>U</td><td rowspan=1 colspan=1>起止框</td><td rowspan=1 colspan=1>用于框图的起始和终止。只有一个出口或入口，表示程序的开始和结束</td></tr><tr><td rowspan=1 colspan=1></td><td rowspan=1 colspan=1>输入/输出框</td><td rowspan=1 colspan=1>表示输入或输出数据。有一个入口和一个出口</td></tr><tr><td rowspan=1 colspan=1>□</td><td rowspan=1 colspan=1>处理框</td><td rowspan=1 colspan=1>表示某种操作。有一个入口和一个出口</td></tr><tr><td rowspan=1 colspan=1>></td><td rowspan=1 colspan=1>判断框</td><td rowspan=1 colspan=1>表示进行判断。有一个入口和两个出口，程序运行时根据判断的结果选择其中的一个出口</td></tr><tr><td rowspan=1 colspan=1>D</td><td rowspan=1 colspan=1>循环初始框</td><td rowspan=2 colspan=1>二者共同表示循环结构，其间为循环体</td></tr><tr><td rowspan=1 colspan=1></td><td rowspan=1 colspan=1>循环终止框</td></tr><tr><td rowspan=1 colspan=1>←</td><td rowspan=1 colspan=1>流程线</td><td rowspan=1 colspan=1>表示程序的执行方向</td></tr></table>

# 习题五

1．举出日常生活中需要用分支过程处理的例子，用框图描述处理的步骤。

2．用适当的语句描述下列处理过程。

（1）如果方程 $a x ^ { 2 } + b x + c = 0$ 的判别式小于0,输出“无实根”并结束程序的运行，否则输出“有实根”后结束程序的运行。  
(2）如果变量A的值不是100,输出“错了,”，然后输出“应该是100!”，否则输出“对了，”，然后也输出“应该是 $1 0 0 ! ^ { \mathfrak { n } }$ 。  
(3）如果变量 $\mathbf { X }$ 的值小于100,输出它的值，否则输出时前面加上“\*\*\*”作为标志。  
(4)如果条件 $\mathbf M < > \mathbf N$ 成立，让计算机执行从语句500开始的一段程序，否则先输出$\mathbf { \omega ^ { * } M } = \mathbf { N } \mathbf { \omega ^ { * } }$ ，再继续执行。

3．阅读下列程序，写出运行结果。

(1) 10 READ X,Y 20 IF $\mathbf { X } { > } \mathbf { Y }$ THEN PRINT " $\mathbf { X } = \mathbf { \ " }$ ; X : GOTO 40 30 PRINT $\mathbf { \ " } \mathbf { Y } = \mathbf { \ " }$ ; Y 40 END 50 DATA 1.9988,8.2034

(2) 10 READ X, Y 20IF $\mathbf { X } { > } \mathbf { Y }$ THEN PRINT " $\mathbf { X } = \mathbf { \ " }$ ; X : END 30 PRINT $\mathbf { \Omega } ^ { \prime \prime } \mathbf { Y } { = } ^ { \prime \prime } \vdots \mathbf { Y }$ 40 END 50 DATA 9988,8034

(3）10 LET $\mathbf { A } { = } 3 3 : \mathrm { L E T } \ \mathbf { B } { = } 9 9$ 20 IF $\mathbf { A } { < } \mathbf { B }$ THEN LET $\mathbf { T } = \mathbf { A }$ : LET A=B : LET B=T30 LET $\mathbf { A } { = } \mathbf { A } { - } \mathbf { B }$ 40 PRINT " $\mathbf { A } = "$ ； A50 END

(4)10 LET $\mathbf { A } { = } 1 3 3 : \mathbf { L E T } \ \mathbf { B } { = } 9 9$ 20IF $\mathbf { A } { < } \mathbf { B }$ THEN LET $\mathbf { T } = \mathbf { A }$ : LET $\mathbf { A } { = } \mathbf { B }$ : LET B=T 30 LET $\mathbf { A } { = } \mathbf { A } { - } \mathbf { B }$ 40 PRINT " $\mathbf { A } = { \boldsymbol { \prime } }$ ;A 50 END

(5)10 LET $\mathbf { A } { = } 3 3 : \mathbf { L E T } \ \mathbf { B } { = } 9 9$ 20IF $\mathbf { A } { < } \mathbf { B }$ THEN LET $\mathbf { T } = \mathbf { A }$ : LET $\mathbf { A } = \mathbf { B }$ : LET B=T : GOTO 40 30 LET $\mathbf { A } { = } \mathbf { A } { - } \mathbf { B }$ 40 PRINT " $\mathbf { A } = \mathbf { \ " }$ ; A 50 END

(6)10 LET $\mathbf { A } { = } 1 0 0 : \mathbf { L E T } \ \mathbf { B } { = } 9 9$ 20IF $\mathbf { A } { < } \mathbf { B }$ THEN LET $\mathbf { T } = \mathbf { A }$ : LET $\mathbf { A } { = } \mathbf { B }$ : LET B=T : GOTO 40 30 LET $\mathbf { A } { = } \mathbf { A } { - } \mathbf { B }$ 40 PRINT " $\mathbf { A } = { \boldsymbol { \prime } }$ ; A 50 END

(7)10 LET $\mathbf { A } { = } 3 3 : \mathbf { L E T } \ \mathbf { B } { = } 9 9$ 20 IF $\mathbf { A } { < } \mathbf { B }$ THEN 40 30 LET $\mathbf { A } { = } \mathbf { A } { - } \mathbf { B }$ 40 PRINT $\mathbf { \Omega } ^ { \prime \prime } \mathbf { A } { = } ^ { \prime \prime } \vdots \mathbf { A }$ 50 END

(8）10 LET A=200 : LET $\mathbf { B } = 9 9$ 20 IF $\mathbf { A } { < } \mathbf { B }$ THEN 4030 LET $\mathbf { A } { = } \mathbf { A } { - } \mathbf { B }$ 40 PRINT " $\mathbf { A } = "$ ； A50 END

(9)10 LET $\mathbf { X } { = } 1 0 0$ 20 LET $\scriptstyle \mathbf { X } = \mathbf { X } + 9 9$ $3 0 ~ \scriptstyle \mathbf { I F }$ $\scriptstyle \mathbf { X } = \mathbf { X } + 9 9$ THEN PRINT ${ \bf { \ " } } \bf { X } \mathrm { = } \bf { X } \mathrm { + } 9 9 = \bf { " }$ ; X : GOTO 6040 PRINT" AFTER 10, $\mathbf { X } = 1 0 0 ^ { \prime \prime }$ 50 PRINT "AFTER 20, $\mathbf { X } { = } ^ { \prime \prime } ; \mathbf { X } , \mathbf { \mu } ^ { \prime \prime } \mathbf { X } { + } 9 9 { = } ^ { \prime \prime } ; \mathbf { X } { + } 9 9$ 60 END

4．设计一个程序，用来解一元二次方程 $a x ^ { 2 } + b x + c = 0$ 。要求 $\textcircled{1}$ 从键盘上输入a， $^ { b }$ 和 $c$ 的值;$\textcircled{2}$ 当方程无实数解时，屏幕上能给出提示信息。

5．设计一个程序，检查键盘上输入的三个数是否为三角形的三条边长。如果是，求出三角形的面积，否则输出“不是三角形”。

6．某小学一年级有200名学生参加数学百题赛、语文百词赛和朗诵比赛，如果有两项得分不低于90,另一项得分不低于80就可以获得优秀奖。设计一个程序处理这个问题。

7．设计一个程序，找出1000以内所有能被13整除并能被3整除的数。

8．设计一个程序，产生100个0到2之间的随机数，统计并输出大于1与不大于1的数所占的百分比。

9.设计一个程序，产生10道随机整数乘法题，要求乘数与被乘数大于1且小于100。

10．编一段有如下处理功能的程序：如果字符串变量B\$中的内容不是Y,输出“NO”，否则输出“YES”。无论输出的是“YES”还是 $\mathbf { \mu } ^ { \mathfrak { s } } \mathbf { N } \mathbf { O } ^ { \mathfrak { n } }$ ，最后都输出 SEE YOU NEXT TIME！。

11．设计一个程序，从20个字符串中找到并输出最大的一个。
