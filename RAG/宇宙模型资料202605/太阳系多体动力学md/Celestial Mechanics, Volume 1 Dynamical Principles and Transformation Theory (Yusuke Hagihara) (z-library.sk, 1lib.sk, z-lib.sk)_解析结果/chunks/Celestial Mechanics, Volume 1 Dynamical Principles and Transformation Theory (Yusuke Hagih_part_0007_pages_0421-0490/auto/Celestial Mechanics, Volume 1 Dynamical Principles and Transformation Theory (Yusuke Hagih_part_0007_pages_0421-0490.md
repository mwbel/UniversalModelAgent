of $\pmb { { \cal E } }$ for the interval $\mathfrak { E } ( J _ { 1 } ^ { * } , s ) < E < \mathfrak { E } ( J _ { 2 } ^ { * } , s )$ ,and (ii) that the flow $F _ { \boldsymbol { \jmath } , s }$ on $( S ( J , s ) , m _ { J , s } )$ is ergodic for almost all $\boldsymbol { J }$ in the interval

$$
J _ { 1 } ^ { * } < J < J _ { 2 } ^ { * } .
$$

Suppose that either of these two conditions is satisfied for almost all $\pmb { \mathscr { s } }$ in $a \leq s \leq b$ 、If an invariant function $f ( \boldsymbol { p } , \boldsymbol { q } , \boldsymbol { s } )$ of the flow $\pmb { F }$ on $_ D$ belongs to $L ^ { 2 } ( D )$ ,then

$$
\int _ { D } \overline { { f ( \boldsymbol { p } , \boldsymbol { q } , \boldsymbol { s } ) } } \frac { \partial \overline { { \mathfrak { S } } } } { \partial \boldsymbol { s } } ( \boldsymbol { p } , \boldsymbol { q } , \boldsymbol { s } ) d \boldsymbol { p } d \boldsymbol { q } d \boldsymbol { s } = 0 .
$$

Now consider the one-parameter group $\{ U _ { t } \}$ of unitary transforma-tions on $L ^ { 2 } ( D )$ associated with the flow ${ \pmb F }$ on $_ D$ . Define an operator $\ b { A f }$ by

$$
\left\| { \frac { U _ { t } f - f } { i t } } - A f \right\| _ { D } \to 0 \qquad ( t \to 0 )
$$

for all $f \in L ^ { 2 } ( D )$ for which such an $A f$ exists. Then $\pmb { A }$ is a self-adjoint operator in the sense used by von Neumann on $L ^ { 2 } ( D )$ ,and $U _ { t } = e ^ { i A t }$ in the sense of the operator calculus (see a later chapter on the ergodic theorem; Stone, l932). Denote the domain of $\pmb { A }$ by $\mathfrak { D } ( A ) . \operatorname { I f } f \in C _ { 0 } ^ { 1 } ( D ^ { 0 } )$ ， we define $f = 0$ on $D - D ^ { 0 }$ .If $f \in C _ { 0 } ^ { 1 } ( D ^ { 0 } )$ , then $f \in { \mathfrak { D } } ( A )$ and

$$
A f = - i ( f , H ) = i ( H , f ) ,
$$

where $( H , f )$ is the Poisson bracket.

Denote by $N$ the set of all invariant functions of the flow ${ \pmb F }$ on $D$ belonging to $L ^ { 2 } ( D )$ . The set $N$ coincides with the set

$$
\{ f | f \in L ^ { 2 } ( D ) , f = U _ { t } f , - \infty < t < + \infty \} = \{ f | f \in \mathfrak { D } ( A ) , A f = 0 \} .
$$

Hence $\boldsymbol { N }$ is a closed linear subspace of $L ^ { 2 } ( D )$ . Denote the orthogonal complement of $N$ in $L ^ { 2 } ( D )$ by $N ^ { \bot }$ .Then it can be shown that $A [ C _ { 0 } ^ { 1 } ( D ^ { 0 } ) ]$ ， the image of $C _ { 0 } ^ { 1 } ( D ^ { 0 } )$ by $\pmb { A }$ ,is contained in $N ^ { \bot }$ and is dense in $N ^ { \bot }$ .For any $\epsilon > 0$ ，thereisa function $f _ { 0 } ( \pmb { \mathscr { p } } , q , s ) \in C _ { 0 } ^ { 1 } ( D ^ { 0 } )$ such that

$$
\bigg \| ( H , f _ { 0 } ) - \frac { \partial \tilde { \mathfrak { s } } } { \partial s } \bigg \| _ { D } < \epsilon .
$$

Let ${ \pmb a } ^ { \prime }$ and ${ \pmb b } ^ { \prime }$ be two numbers such that $a \leq a ^ { \prime } < b ^ { \prime } \leq b$ and

$$
( b ^ { \prime } - a ^ { \prime } ) < ( J _ { 2 } ^ { \ast } - J _ { 1 } ^ { \ast } ) / ( 2 M ) ,
$$

and put

$$
J _ { 2 } = J _ { 2 } ^ { * } - M ( b ^ { \prime } - a ^ { \prime } ) , ~ J _ { 1 } = J _ { 1 } ^ { * } + M ( b ^ { \prime } - a ^ { \prime } ) .
$$

Then the solution of (154a) passing through $( \phi ^ { 0 } , q ^ { 0 } , a ^ { \prime } )$ , where

$$
( p ^ { 0 } , q ^ { 0 } ) \in I ( J _ { 1 } , J _ { 2 } , a ^ { \prime } )
$$

can be prolonged in $_ D$ to the interval $a ^ { \prime } \leq t \leq b ^ { \prime }$ for every $\lambda > 0$

When $a \\\\\\\equiv a ^ { \prime } \ < b ^ { \prime } \ \\\\\\\\\\\\\\\\\\\\\\\\\equiv b$ $( \phi ^ { 0 } , q ^ { 0 } ) \in I ( a ^ { \prime } )$ , and $\lambda > 0$ , we define

$$
\Delta ( a ^ { \prime } , b ^ { \prime } , p ^ { \prime } , q ^ { \prime } , \lambda ) = \operatorname * { m a x } _ { a ^ { \prime } \leq t \leq b ^ { \prime } } | \mathfrak { F } \{ \tilde { p } ( t , p ^ { 0 } , q ^ { 0 } , a ^ { \prime } , \lambda ) , \tilde { q } ( t , p ^ { 0 } , q ^ { 0 } , a ^ { \prime } , \lambda ) , t \}
$$

$$
- \ : \Im ( \phi ^ { 0 } , q ^ { 0 } , a ^ { \prime } ) | , \quad
$$

if the solution $\begin{array} { r } { \ p = \tilde { \ p } ( t , \ p ^ { 0 } , q ^ { 0 } , a ^ { \prime } , \lambda ) } \end{array}$ ， $q = \tilde { q } ( t , \boldsymbol { \mathscr { p } } ^ { 0 } , \boldsymbol { q } ^ { 0 } , \boldsymbol { a } ^ { \prime } , \lambda )$ of (154a) can be continued to $t = b ^ { \prime }$ in $D$ ; we put

$$
\Delta ( a ^ { \prime } , b ^ { \prime } , p ^ { 0 } , q ^ { 0 } , \lambda ) = + \infty
$$

if this solution cannot be continued to $t = b ^ { \prime }$ in $D$ . When

$$
a \leq a ^ { \prime } < b ^ { \prime } \leq b , \qquad \lambda > 0 , \delta > 0 ,
$$

we denote the subset of $I ( a ^ { \prime } )$ ，

$$
\{ ( p ^ { 0 } , q ^ { 0 } ) \mid ( p ^ { 0 } , q ^ { 0 } ) \in I ( a ^ { \prime } ) , \Delta ( a ^ { \prime } , b ^ { \prime } , p ^ { 0 } , q ^ { 0 } , \lambda ) < \delta \} ,
$$

by $L ( a ^ { \prime } , b ^ { \prime } , \lambda , \delta )$ . The subset $L ( a ^ { \prime } , b ^ { \prime } , \lambda , \delta )$ is an open set in $R ^ { 2 n }$

Denote the $_ m$ -dimensional Lebesgue measure of a measurable set in $R ^ { m }$ by $\mu _ { m } [ \ ]$ . Then,for any fixed $\delta ( > 0 )$ , we have

$$
\mu _ { 2 n } [ I ( J _ { 1 } , J _ { 2 } , a ^ { \prime } ) - L ( a ^ { \prime } , b ^ { \prime } , \lambda , \delta ) ] \to 0 \qquad ( \lambda \to + \infty ) .
$$

The adiabatic theorem of Kasuga states that

$$
\mu _ { 2 n } [ I ( J _ { 1 } , J _ { 2 } , a ) - L ( a , b , \lambda , \delta ) ] \to 0 \qquad ( \lambda \to + \infty ) ,
$$

for any fixed $J _ { 1 } , J _ { 2 } ;$ ，δ such that $J _ { 1 } ^ { * } < J _ { 1 } < J _ { 2 } < J _ { 2 } ^ { * }$ and $\delta > 0$

# LIE GROUPS

# 4.26LIE'S FUNDAMENTAL THEOREMS

Consider a set of independent functions

$$
f _ { i } ( x _ { 1 } , x _ { 2 } , . . . , x _ { n } ; a _ { 1 } , a _ { 2 } , . . . , a _ { r } ) \qquad ( i = 1 , 2 , . . . , n ) ,
$$

of $\pmb { n }$ independent variables $x _ { 1 } , x _ { 2 } , \ldots , x _ { n }$ containing $_ r$ parameters $a _ { 1 } , a _ { 2 } , \ldots , a _ { r } ;$ ，continuous of class $C ^ { 2 }$ in $_ x$ and in $\pmb { a }$ By assumption, the Jacobian of $f _ { i }$ with respect to $_ { x }$ is not zero. If there is no relation of the form

$$
\sum _ { \sigma = 1 } ^ { r } \phi _ { \sigma } ( a _ { 1 } , a _ { 2 } , . . . , a _ { r } ) { \frac { \partial f _ { i } } { \partial a _ { \sigma } } } = 0 ( i = 1 , 2 , . . . , n )
$$

with nonzero functions $\phi _ { \sigma } ( a _ { 1 } , a _ { 2 } , . . . , a _ { \tau } )$ ， then and only then are $a _ { 1 } , a _ { 2 } , \ldots , a _ { \tau }$ said to be the essential parameters. Any transformation by an $\pmb { r }$ parameter group

$$
x _ { i } ^ { \prime } = f _ { i } ( x _ { 1 } , x _ { 2 } , . . . , x _ { n } ; a _ { 1 } , a _ { 2 } , . . . , a _ { \tau } ) \qquad ( i = 1 , 2 , . . . , n )
$$

transforms a point $P ( x _ { 1 } , . . . . , x _ { n } )$ of an $\pmb { n }$ dimensional manifold to a point $P ^ { \prime } ( x _ { 1 } ^ { \prime } , \ldots , x _ { n } ^ { \prime } )$ ；that is, $T _ { a } { \pmb x } = { \pmb x } ^ { \prime }$ ， $T _ { a } ^ { - 1 } x ^ { \prime } = x$ ，The composite transformation $T _ { b } T _ { a } = T _ { c }$ is determined uniquely; thus,

$$
T _ { c } ( T _ { b } T _ { a } ) = ( T _ { c } T _ { b } ) T _ { a } .
$$

A system with these properties is called a semi-group. If it contains an identity such as $T _ { a } I = I T _ { a } = T _ { a }$ and an inverse element such as $T _ { a } ^ { - 1 } T _ { a } = T _ { a } T _ { a } ^ { - 1 } = I ,$ then it is called a group. From (l55) we obtain

$$
x _ { j } = f _ { j } ^ { \prime } ( x _ { i } ^ { \prime } , . . . , x _ { n } ^ { \prime } , a _ { 1 } , . . . , a _ { r } ) \qquad ( j = 1 , 2 , . . . , n ) .
$$

The transformations (155) define a continuous transformation group $_ { T }$ (Lie,1888,1890,1891, 1893,1893a,1896).

From (155),

$$
\hat { \delta } x _ { i } ^ { \prime } = \sum _ { \rho } f _ { i \rho } ( x , a ) \ \hat { \delta } a _ { \rho } \qquad ( i = 1 , 2 , . . . , n ) ,
$$

or

$$
\delta x _ { i } ^ { \prime } = \sum _ { \rho } f _ { i \rho } \{ x ^ { \prime } , a \} \delta a _ { \rho } ( i = 1 , 2 , . . . , n ) .
$$

This is an infinitesimal transformation $T _ { a } ^ { - 1 } T _ { a + \delta a }$ According to Lie it is written

$$
\sum _ { \rho } \left( \sum _ { i } { f _ { i \rho } \{ x ^ { \prime } , a \} } \frac { { \partial { f ^ { \prime } } } } { { \partial { x _ { i } ^ { \prime } } } } \right) \frac { { \delta { a _ { \rho } } } } { { \delta t } } .
$$

We write the relation $T _ { \flat } T _ { a } = T _ { c }$ in the form

$$
c _ { \rho } = \psi _ { \rho } ( a , b ) \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

Since $T _ { b } T _ { a + \delta a } = T _ { c + \delta c : }$ we have

$$
T _ { c } ^ { - 1 } T _ { c + \delta c } = T _ { a } ^ { - 1 } T _ { b } ^ { - 1 } T _ { b } T _ { a + \delta a } = T _ { a } ^ { - 1 } T _ { a + \delta a } .
$$

Thus, $T _ { a } ^ { - 1 } T _ { a + \delta a }$ is invariant for the transformation (157). This leads to the relation

$$
\sum _ { \rho } \left( \sum _ { i } f _ { i \rho } \{ x ^ { \prime } , a \} \frac { \partial f ^ { \prime } } { \partial x _ { i } ^ { \prime } } \right) \frac { \delta a _ { \rho } } { \delta t } = \sum _ { \sigma } \left( \sum _ { i } f _ { i \sigma } \{ x ^ { \prime } , c \} \frac { \partial f ^ { \prime } } { \partial x _ { i } ^ { \prime } } \right) \frac { \delta c _ { \sigma } } { \delta t } .
$$

From (157) we have

$$
\delta c _ { \sigma } = \sum _ { \rho } \phi _ { \sigma \rho } ( b , a ) \ \delta a _ { \rho } = \sum _ { \rho } \phi _ { \sigma \rho } \{ a , c \} \ \delta a _ { \rho } .
$$

Substituting this expression for $\delta c _ { \sigma }$ in (156b） makes the infinitesimal transformation (l56a) become

$$
\sum _ { \rho } \left( \sum _ { i } f _ { i \rho } \{ x ^ { \prime } , a \} \frac { \partial f ^ { \prime } } { \partial x _ { i } ^ { \prime } } \right) \frac { \partial a _ { \rho } } { \partial t } = \sum _ { \sigma } \left[ \left( \sum _ { i } f _ { i \sigma } \{ x ^ { \prime } , c \} \frac { \partial f ^ { \prime } } { \partial x _ { i } ^ { \prime } } \right) \cdot \sum _ { \rho } \phi _ { \sigma \rho } \{ a , c \} \frac { \partial a _ { \rho } } { \partial t } \right]
$$

We fix $c .$ .Then $b$ is given by (157),and the $\mathcal { f } _ { i \sigma } \{ x ^ { \prime } , c \}$ become functions of $\scriptstyle { \boldsymbol { x } } ^ { \prime }$ only. We can put

$$
f _ { i \sigma } \{ x ^ { \prime } , c \} = \xi _ { \sigma i } ( x ^ { \prime } ) , \phi _ { \sigma \rho } \{ a , c \} = \psi _ { \sigma \rho } ( a ) ,
$$

$$
\sum _ { i } \xi _ { \sigma i } ( x ^ { \prime } ) \frac { \partial f ^ { \prime } } { \partial x _ { i } ^ { \prime } } = X _ { \sigma } ^ { \prime } f ^ { \prime } .
$$

This gives an important relation:

$$
\sum _ { \rho } \left( \sum _ { i } f _ { i \rho } \{ x ^ { \prime } , a \} \frac { \partial f ^ { \prime } } { \partial x _ { i } ^ { \prime } } \right) \frac { \partial a _ { \rho } } { \partial t } = \sum _ { \sigma } \left( \sum _ { \rho } \psi _ { \sigma \rho } ( a ) \frac { \delta a _ { \rho } } { \delta t } \right) X _ { \sigma } ^ { \prime } f ^ { \prime } .
$$

These infinitesimal transformations $X _ { 1 } f _ { 3 } \ldots , X _ { r } f$ constitute a ring with the operations of addition and multiplication,which is called an infinitesimal ring.

We have $( \pmb { x } ^ { \prime } ) = ( \pmb { x } ) S _ { t } , ( \pmb { x } ) = ( \pmb { x } ^ { \prime } ) S _ { t } ^ { - 1 }$ ; accordingly

$$
( x ^ { \prime } + \delta x ^ { \prime } ) = ( x ^ { \prime } ) S _ { t } ^ { - 1 } S _ { t + \delta t } .
$$

Hence, the infinitesimal transformation $T _ { a } ^ { - 1 } T _ { a + \delta a }$ can be formed from the basic transformations $X _ { 1 } f _ { 3 } \ldots , X _ { \tau } f ,$ linearly with the coefficients

$$
\sum _ { \tau } \psi _ { 1 \tau } ( a ) \ \delta a _ { \tau } , . . . , \sum _ { \tau } \psi _ { r \tau } ( a ) \ \delta a _ { \tau } .
$$

Write (157) in the form $a _ { \rho } ^ { \prime } = \psi _ { \rho } ( a , b )$ and let $T _ { b } T _ { a } = T _ { a ^ { \prime } }$ .If $T _ { c } T _ { a ^ { \prime } } = T _ { a ^ { \prime \prime } ; }$ then $T _ { c } T _ { b } T _ { a } = T _ { a ^ { \prime \prime } }$ But since $T _ { c } T _ { b } = T _ { d }$ ，therefore, $T _ { d } T _ { a } = T _ { a ^ { \prime \prime } } $ This is called a left-hand group. It is the second parameter group of Lie. Write (157) in the form $a _ { \rho } ^ { \prime } = \psi _ { \rho } ( b , a )$ and let $T _ { a } T _ { b } = T _ { a ^ { \prime } }$ .If $T _ { a ^ { \prime } } T _ { c } = T _ { a ^ { \prime \prime } }$ then $T _ { a } T _ { b } T _ { c } = T _ { a ^ { \prime \prime } }$ Since $T _ { b } T _ { c } = T _ { d } { : }$ ，therefore $T _ { a } T _ { d } = T _ { a ^ { \prime \prime } }$ This is Lie's first parameter group; it is a right-hand group. The parameter $\pmb { b }$ is essential in these groups. Cartan (1933, l937) introduced the notion of equipollence by means of a geometrical illustration. Changing $T _ { a } ^ { - 1 } T _ { a + \delta a }$ to $T _ { a ^ { \prime } } ^ { - 1 } T _ { a ^ { \prime } + \delta a ^ { \prime } }$ by means of (158), we obtain

$$
\sum _ { \rho } \left( \sum _ { \tau } \psi _ { \rho \tau } ( a ) \ \delta a _ { \tau } \right) X _ { \rho } f = \sum _ { \rho } \left( \sum _ { \tau } \psi _ { \rho \tau } ( a ^ { \prime } ) \ \delta a _ { \tau } ^ { \prime } \right) X _ { \rho } f
$$

for the second parameter group. Since the basic infinitesimal transformations are linearly independent, therefore

$$
\sum _ { \tau } \psi _ { \rho \tau } ( a ) \ \delta a _ { \tau } = \sum _ { \tau } \psi _ { \rho \tau } ( a ^ { \prime } ) \ \delta a _ { \tau } ^ { \prime } \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

That is, the $\pmb { r }$ Pfaffians $\begin{array} { r } { \sum _ { \tau } \psi _ { \rho \tau } ( a ) \ \delta a _ { \tau } } \end{array}$ are invariant for the second parameter group.

Construct an $\pmb { r }$ -dimensional parameter space with coordinates $a _ { 1 } , a _ { 2 } , \ldots , a _ { r } .$ Put $\begin{array} { r } { \sum _ { \tau } \psi _ { \rho ^ { \prime } \tau } ( a ) \ \delta a _ { \tau } = \epsilon _ { \rho \rho ^ { \prime } } \ \delta t . } \end{array}$ ，where $\epsilon _ { \rho \rho } = 1$ ， $\epsilon _ { \rho \rho ^ { \prime } } = 0$ $\left( \rho \neq \rho ^ { \prime } \right)$ . Such a vector feld is invariant for the second parameter group；it is the field of the infinitesimal increment defined by the infinitesimal transformations

$$
A _ { \rho } f = \sum _ { \tau } \alpha _ { \rho \tau } ( a ) \frac { \partial f } { \partial a _ { \tau } } \qquad ( \rho = 1 , 2 , . . . , r ) ,
$$

with the determinant $\| \psi _ { \sigma \tau } \| \neq 0$ ，where $\alpha _ { \rho \tau } ( a )$ is the minor of this determinant. Since any infinitesimal transformation $\ b { A f }$ is formed linearly with constant coefficients from $A _ { 1 } f _ { 3 } \ldots , A _ { r } f ,$ we obtain

$$
( A _ { \sigma } , A _ { \rho } ) f = \sum _ { \tau } c _ { \sigma \rho \tau } A _ { \tau } f \qquad ( \sigma , \rho = 1 , 2 , . . . , r )
$$

with the commutator $( A _ { \sigma } , A _ { \rho } )$ (Section 4.2).

If the second parameter group $T _ { c } T _ { a ^ { \prime } } = T _ { a ^ { \prime \prime } }$ is applied to the first parameter group $T _ { a } T _ { b } = T _ { a ^ { \prime } }$ ，then $T _ { c } T _ { a } T _ { b } = T _ { a ^ { \prime \prime } }$ Let $T _ { c } T _ { a } = T _ { a _ { 1 } ; }$ $T _ { a _ { 1 } } T _ { b } = T _ { a _ { 2 } }$ ；then $T _ { c } T _ { a } T _ { b } = T _ { a _ { 2 } }$ 、Hence, if transformations from the first and the second parameter groups are denoted by $\scriptstyle A _ { b }$ and $B _ { c }$ ， respectively, then $A _ { b } B _ { c } = B _ { c } A _ { b }$ .Thus, $B _ { c } = A _ { b } ^ { - 1 } B _ { c } A _ { b } ,$ $A _ { b } = B _ { c } ^ { - 1 } A _ { b } B _ { c }$ ； that is, any transformation of one of the parameter groups is invariant for transformations of the other parameter group.Hence, $A _ { b } ^ { - 1 } A _ { b + \delta b }$ is invariant for $B _ { c }$ .We see that the infinitesimal transformations of the first parameter group are formed by linear combinations with constant coefficients from $A _ { 1 } f _ { 3 } \ldots , A _ { r } f .$ Let an infinitesimal transformation of the second parameter group be $B f ,$ and let $B f$ operate on $A _ { \rho } f _ { \mathrm { : } }$ ；then we obtain $A _ { \rho } f + \ ( A _ { \rho } , B ) \delta t$ .Since this is invariant for the second parameter group, therefore $( A _ { \rho } , B _ { \sigma } ) = 0$ $( \rho , \sigma = 1 , 2 , . . . , r )$ ·

Let $A _ { \rho } f$ be a basic infinitesimal transformation of the first parameter group, and let $\delta _ { \rho }$ be the displacement due to $A _ { \rho } f _ { ; }$ ；then,by(159) and (160),

$$
( A _ { \sigma } , A _ { \tau } ) = \sum _ { \rho } \left( \frac { \hat { \delta } _ { \sigma } \alpha _ { \tau \rho } } { \delta t } - \frac { \hat { \delta } _ { \tau } \alpha _ { \sigma \rho } } { \delta t } \right) \frac { \delta f } { \delta \alpha _ { \rho } } = \sum _ { \rho } \sum _ { \kappa } c _ { \sigma \tau \kappa } \alpha _ { \kappa \rho } \frac { \partial f } { \partial \alpha _ { \rho } } .
$$

Multiply by $\psi _ { \lambda \rho }$ and sum over $\pmb { \rho }$ ； then,remembering that the matrix $\pmb { \alpha }$ is the reciprocal of the matrix $\psi$ ,we obtain

$$
\sum _ { \rho } \left( \psi _ { \lambda \rho } \frac { \hat { \delta } _ { \sigma } \alpha _ { \tau \rho } } { \delta t } - \psi _ { \lambda \rho } \frac { \hat { \delta } _ { \tau } \alpha _ { \sigma \rho } } { \delta t } \right) = c _ { \sigma \tau \lambda } .
$$

With $\begin{array} { r } { \sum _ { \rho } \psi _ { \lambda \rho } \alpha _ { \tau \rho } = \epsilon _ { \lambda \tau } , \sum _ { \rho } \psi _ { \lambda \rho } \alpha _ { \sigma \rho } = \epsilon _ { \lambda \sigma } . } \end{array}$ we have

$$
\begin{array} { r } { \displaystyle \sum _ { \rho } { \left( \alpha _ { \sigma \rho } \frac { \delta _ { \tau } \psi _ { \lambda \rho } } { \delta t } - \alpha _ { \tau \rho } \frac { \delta _ { \sigma } \psi _ { \lambda \rho } } { \delta t } \right) } = \displaystyle \sum _ { \rho } { \sum _ { \kappa } { \left( \alpha _ { \sigma \rho } \alpha _ { \tau \kappa } \frac { \partial \psi _ { \lambda \rho } } { \partial a _ { \kappa } } - \alpha _ { \tau \rho } \alpha _ { \sigma \kappa } \frac { \partial \psi _ { \lambda \rho } } { \partial a _ { \kappa } } \right) } } } \\ { = c _ { \sigma \tau \lambda } = \displaystyle \sum _ { \rho } { \sum _ { \kappa } { \alpha _ { \sigma \rho } \alpha _ { \tau \kappa } \left( \frac { \partial \psi _ { \lambda \rho } } { \partial a _ { \kappa } } - \frac { \partial \psi _ { \lambda \kappa } } { \partial a _ { \rho } } \right) } } . } \end{array}
$$

Hence

$$
\frac { \partial \psi _ { \lambda \rho } } { \partial a _ { \sigma } } - \frac { \partial \psi _ { \lambda \sigma } } { \partial a _ { \rho } } = \sum _ { \kappa } \sum _ { \tau } c _ { \kappa \tau \lambda } \psi _ { \kappa \rho } \psi _ { \tau \sigma } .
$$

This is called Maurer's relation.

If the group contains at least a transformation (l55） that maps a point $( x _ { 1 } , . . . , x _ { n } )$ of the $\pmb { n }$ dimensional manifold to any other point $( x _ { 1 } ^ { \prime } , \ldots , x _ { n } ^ { \prime } )$ of that manifold, then the group is said to be transitive. When there are exactly $_ { n }$ independent equations among

$$
X _ { 1 } f = 0 , . . . , X _ { r } f = 0 ,
$$

then the transformation of the point $( x _ { 1 } , . . . , x _ { n } )$ is transitive.Otherwise it is intransitive.If $n = r _ { \mathrm { { i } } }$ ，then the system of $_ { x }$ can be transformed to the system of $^ { a }$ under certain conditions. Such a group is said to be simply transitive.

By combining $\begin{array} { r } { P _ { \rho } = \sum _ { \tau } \psi _ { \rho \tau } ( a ) \delta a _ { \tau } ( \rho = 1 , 2 , . . . , r ) } \end{array}$ and the basic infinitesimal transformations

$$
X _ { \rho } ^ { \prime } f ^ { \prime } = \sum _ { i } \xi _ { \rho i } ( x ^ { \prime } ) \frac { \partial f ^ { \prime } } { \partial x _ { i } ^ { \prime } } ,
$$

we have seen that the transformation $T _ { a } ^ { - 1 } T _ { a + \delta a }$ satisfies

$$
\delta x _ { i } ^ { \prime \prime } = \sum _ { \rho } { P _ { \rho } \xi _ { \rho i } ( x ^ { \prime } ) } \qquad ( i = 1 , 2 , . . . , n ) .
$$

If $x ^ { \prime }$ is regarded as a function of $^ { a }$ ,then it should satisfy

$$
d x _ { i } ^ { \prime } - \sum _ { \rho , \sigma } \psi _ { \sigma \rho } ( a ) \xi _ { \sigma i } ( x ^ { \prime } ) d a _ { \rho } = 0 \qquad ( i = 1 , 2 , . . . , n ) ,
$$

or

$$
\frac { \partial x _ { i } ^ { \prime } } { \partial a _ { \rho } } = \sum _ { \sigma } \psi _ { \sigma \rho } ( a ) \xi _ { \sigma i } ( x ^ { \prime } ) \qquad ( i = 1 , 2 , . . . , n ; \rho = 1 , 2 , . . . , r ) .
$$

Hence,a transformation of an $\boldsymbol { r }$ -parameter group should satisfy this system (163) of partial differential equations. This is the first fundamental theorem of $\pmb { L i e }$

Substituting (l62) in the relation

$$
d F = \sum _ { i } { \frac { \partial F } { \partial x _ { i } ^ { \prime } } } d x _ { i } ^ { \prime } + \sum _ { \rho } { \frac { \partial F } { \partial a _ { \rho } } } d a _ { \rho }
$$

we obtain

$$
\frac { \partial F } { \partial a _ { \rho } } + \sum _ { i , \sigma } \psi _ { \sigma \rho } ( a ) \xi _ { \sigma i } ( x ^ { \prime } ) \frac { \partial F } { \partial x _ { i } ^ { \prime } } = 0 ( \rho = 1 , 2 , . . . , r ) ,
$$

or, with the definition of the matrix $\pmb { \alpha }$

$$
\sum _ { i } \xi _ { \rho i } ( x ^ { \prime } ) \frac { \partial F } { \partial x _ { i } ^ { \prime } } + \sum _ { \sigma } \alpha _ { \rho \sigma } ( a ) \frac { \partial F } { \partial a _ { \sigma } } = 0 \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

Since $x _ { i } ^ { \prime } = f _ { i } ( x , a )$ satisfies (l62） with the integration constants $^ { a }$ ，the relation $x _ { i } = f _ { i } ^ { \prime } ( x ^ { \prime } , a )$ satisfies (165).As these $_ { n }$ functions $f _ { i } ^ { \prime }$ are independent with regard to $\scriptstyle { x ^ { \prime } }$ ，the equations （l65）admit $_ { n }$ independent integrals $f _ { i } ^ { \prime }$ .There are $r$ independent equations for $n + r$ variables in the form (l65).Hence,(l65) is a complete system and can be integrated completely.

Conversely,it can be shown that if the transformation (155) containing the identical elements $a _ { 1 } ^ { 0 } , \ldots , a _ { r } ^ { 0 }$ of the parameters satisfies the Pfaffian system (l62）which is completely integrable with respect to $x ^ { \prime }$ and $^ { a }$ ， such that $\| \psi _ { \sigma \rho } ( a ^ { 0 } ) \| \neq 0$ ， then the transformation gives a transformation group.

According to (162） the transformation $T _ { a } ^ { - 1 } T _ { a + \delta a }$ coincides with the infinitesimal transformation $\begin{array} { r } { \sum _ { \sigma } P _ { \sigma } ( a ) X _ { \sigma } ^ { \prime } } \end{array}$ Let $( a _ { 1 } , \ldots , a _ { r } )$ represent a point $\pmb { A }$ and $( a _ { 1 } ^ { 0 } , \ldots , a _ { r } ^ { 0 } )$ a point $A ^ { 0 }$ of the parameter space.Divide the arc $A A ^ { 0 }$ joining $A ^ { 0 }$ to $\pmb { A }$ into segments by the points $A ^ { 1 } , A ^ { 2 } , . . . , A ^ { n - 1 }$ on the arc.Then $T _ { A ^ { 0 } } ^ { - 1 } T _ { A }$ is divided into factors

$$
T _ { A ^ { 0 } } ^ { - 1 } T _ { A ^ { 1 } } , T _ { A ^ { 1 } } ^ { - 1 } T _ { A ^ { 2 } } , . . . , T _ { A ^ { n } - 1 } ^ { - 1 } T _ { A ^ { n } } .
$$

Each of these factors can become an infinitesimal transformation,if we increase the number of these segments indefinitely. If the arc is written

$$
P _ { \sigma } ( a ) = c _ { \sigma } d t \qquad ( \sigma = 1 , 2 , . . . , r ) ,
$$

then $T _ { A ^ { 0 } } ^ { - 1 } T _ { A }$ is said to be generated (erzeugt） by the infinitesimal transformation $\textstyle \sum _ { \sigma } c _ { \sigma } X _ { \sigma }$ .Referring to the definition of $P _ { \rho }$ ， we obtain， from (166),

$$
\frac { d a _ { o } } { d t } = \sum _ { \sigma } c _ { \sigma } \alpha _ { \sigma \rho } ( a ) \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

The integral curve of these equations is a trajectory that admits the infinitesimal transformation

$$
\sum _ { \rho } \sum _ { \sigma } c _ { \sigma } a _ { \sigma \rho } ( a ) \ : \frac { \partial f } { \partial a _ { \rho } } = \sum _ { \sigma } c _ { \sigma } A _ { \sigma } f ,
$$

and passes through $A ^ { 0 }$ . Thus $A ^ { 0 }$ is transformed to $A$ by the transformation $\textstyle \sum _ { \sigma } c _ { \sigma } A _ { \sigma }$ operating in the parameter space. At the same time the infinitesimal transformation $\textstyle \sum _ { \sigma } c _ { \sigma } X _ { \sigma }$ should operate in the $_ { x }$ -manifold, where

$$
X _ { \sigma } f = \sum _ { i } \xi _ { \sigma i } ( x ) { \frac { \partial f } { \partial x _ { i } } } , \qquad \delta x _ { i } = \delta t \cdot \sum _ { \rho } c _ { \rho } \xi _ { \rho i } ( x ) \qquad ( i = 1 , 2 , \ldots , n ) .
$$

Formula (163) shows that

$$
\sum _ { \rho } \psi _ { i \rho } ( a ) \ \delta a _ { \rho } = c _ { i } \ \delta t ,
$$

which is derived from (155).

Suppose that the points $A ^ { \circ } , B ^ { \circ } , C ^ { \circ } , \ldots .$ are transformed respectively to $A , B , C , \ldots$ by $\textstyle \sum _ { \sigma } c _ { \sigma } A _ { \sigma }$ for the same variation of $t .$ 、Then the transformations

$$
T _ { A ^ { 0 } } ^ { - 1 } T _ { A } , \qquad T _ { B ^ { 0 } } ^ { - 1 } T _ { B } . \qquad T _ { C ^ { 0 } } ^ { - 1 } T _ { C } , . . .
$$

all coincide and generate the infinitesimal transformation $\textstyle \sum _ { \sigma } c _ { \sigma } X _ { \sigma }$ Denote by $A ^ { \circ } , B ^ { \circ } , C ^ { \circ } , \ldots$ ，the successive images of the point $( a ^ { 0 } )$ corre-sponding to the identical element, and by $A , B , C , \ldots$ the corresponding images of the point $( a )$ . Then $T _ { a ^ { 0 } } ^ { - 1 } T _ { a }$ or $T _ { a }$ should be among the transformations (168).Since $T _ { A ^ { 0 } } { } ^ { - 1 } T _ { A } = T _ { a } ,$ therefore $T _ { A } = T _ { A ^ { 0 } } T _ { a }$ Thus, from two transformations $T _ { A ^ { 0 } }$ and $T _ { a }$ belonging to (l55） we obtain the transformation $T _ { A }$ belonging to (155). The point $\boldsymbol { A ^ { 0 } }$ is an arbitrary point near the point $( a ^ { 0 } )$ ,and $( a )$ is obtained from $( a ^ { 0 } )$ by the transformation $\textstyle \sum _ { \sigma } c _ { \sigma } A _ { \sigma }$ Hence, by choosing $c _ { \sigma }$ suitably, we can reach any point $( a )$ in the neighborhood of $( a ^ { 0 } )$ .For,integrating (167) from the point $( a ^ { 0 } )$ gives

$$
a _ { \rho } = a _ { p } ^ { 0 } + \sum _ { \sigma } t c _ { \sigma } \alpha _ { \sigma \rho } ( a ^ { 0 } ) + \cdot \cdot \cdot \qquad ( \rho = 1 , 2 , . . . , r ) ,
$$

Thus $( a _ { \rho } )$ is a function of $\lambda _ { \sigma } = t c _ { \sigma }$ The Jacobian of $a _ { \rho }$ with respect to $\lambda _ { \sigma }$ at $\lambda _ { \sigma } = 0$ is $\| \alpha _ { \sigma \rho } ( a ^ { 0 } ) \|$ and does not vanish. By solving these series we obtain

$$
t c _ { \sigma } = \sum _ { \rho } \psi _ { \sigma \rho } ( a ^ { 0 } ) ( a _ { \rho } - a _ { \rho } ^ { 0 } ) + \cdot \cdot \cdot \quad \quad ( \sigma = 1 , 2 , . . . , r ) .
$$

Hence $T _ { a }$ is generated by the infinitesimal transformation $\textstyle \sum _ { \sigma } c _ { \sigma } X _ { \sigma }$ if $( a )$ is in the neighborhood of $( a ^ { 0 } )$

Since by definition $\begin{array} { r } { \delta f ^ { \prime } = X ^ { \prime } f ^ { \prime } \delta t , X ^ { \prime } f ^ { \prime } = \sum _ { i } \xi _ { \sigma i } ( x ^ { \prime } ) \ \partial f ^ { \prime } / \partial x . } \end{array}$ ，therefore $d f ^ { \prime } / d t = X ^ { \prime } f ^ { \prime }$ ， $d ^ { 2 } f ^ { \prime } / d t ^ { 2 } = X ^ { \prime } X ^ { \prime } f ^ { \prime } = X ^ { \prime 2 } f ^ { \prime }$ ,.... Denote the values of $f ^ { \prime } , d f ^ { \prime } / d t , d ^ { 2 } f ^ { \prime } / d t ^ { 2 } , . . .$ at $t = 0 \ b y f , X f , X ^ { 2 } f , . . . ,$ respectively; then

$$
f ^ { \prime } = f + \frac { t } { 1 ! } X f + \frac { t ^ { 2 } } { 2 ! } X ^ { 2 } f + \cdot \cdot \cdot .
$$

If the infinitesimal transformations $X _ { 1 } f _ { 3 } \ldots , X _ { r } f$ belonging to the foregoing group satisfy the conditions of Lie's theorem， then any transformation belonging to the group in the neighborhood of the identical element is generated by

$$
X f = e _ { 1 } X _ { 1 } f + \cdot \cdot \cdot + e _ { r } X _ { r } f .
$$

Let $t = 1$ ; then such a transformation is writen

$$
f ( x ^ { \prime } ) = f ( x ) + { \frac { 1 } { 1 ! } } \sum _ { \rho } e _ { \rho } X _ { \rho } f + { \frac { 1 } { 2 ! } } \sum _ { \rho , \sigma } e _ { \rho } e _ { \sigma } X _ { \rho } X _ { \sigma } f + \cdot \cdot \cdot .
$$

The $\pmb { r }$ constants $e _ { 1 } , \ldots , e _ { \tau }$ are called the canonical parameters. If $f ( x ^ { \prime } )$ satisfies

$$
\frac { d f ( x ^ { \prime } ) } { d t } = \sum _ { \rho } \lambda _ { \rho } X _ { \rho } ^ { \prime } f ( x ^ { \prime } ) ,
$$

without $\delta t$ being put equal to zero, then the infinitesimal transformation $( \lambda _ { 1 } X _ { 1 } f + \cdot \cdot \cdot + \lambda _ { \tau } X _ { \tau } f ) \delta t$ generates a finite transformation

$$
f ( x ^ { \prime } ) = f ( x ) + \frac { 1 } { 1 ! } t \sum _ { \rho } \lambda _ { \rho } X _ { \rho } f + \frac { 1 } { 2 ! } t ^ { 2 } \sum _ { \rho , \sigma } \lambda _ { \rho } \lambda _ { \sigma } X _ { \rho } X _ { \sigma } f + \cdot \cdot \cdot .
$$

Here $X _ { 1 } f , . . . , X _ { \tau } f$ are called the generators of the infinitesimal transformations.

The transformation (155) is represented in general by

$$
X _ { \rho } f = \sum _ { i = 1 } ^ { n } \xi _ { \rho i } ( x ) { \frac { \partial f } { \partial x _ { i } } } \quad \quad ( \rho = 1 , 2 , . . . , r ) .
$$

Integrating the differential equations

$$
{ \frac { d x _ { 1 } ^ { \prime } } { \displaystyle { \sum _ { \rho = 1 } ^ { r } \lambda _ { \rho } \xi _ { \rho 1 } ( x ^ { \prime } ) } } } = \cdot \cdot \cdot = { \frac { d x _ { n } ^ { \prime } } { \displaystyle { \sum _ { \rho = 1 } ^ { r } \lambda _ { \rho } \xi _ { \rho n } ( x ^ { \prime } ) } } } = d t
$$

from $x _ { i } ^ { \prime } = x _ { i }$ for $t = 0$ ,we obtain

$$
x _ { i } ^ { \prime } = \phi _ { i } ( x _ { 1 } , . . . , x _ { n } , \lambda _ { 1 } t , . . . , \lambda _ { r } t ) \qquad ( i = 1 , 2 , . . . , n ) .
$$

This is in the required canonical form,and is written

$$
x _ { i } ^ { \prime } = \phi _ { i } ( x _ { 1 } , \ldots , x _ { n } , u _ { 1 } , \ldots , u _ { r } ) \qquad ( i = 1 , 2 , \ldots , r ) ,
$$

where $\lambda _ { \rho } t = u _ { \rho }$

The equations (165) can be put in the form $X _ { \sigma } ^ { \prime } F + A _ { \sigma } F = 0 \ :$ where

$$
\sum _ { \rho } \alpha _ { \sigma \rho } ( a ) \frac { \partial { \cal F } } { \partial x _ { \rho } } = A _ { \sigma } { \cal F } ,
$$

and are satisfied by the transformation (155). $A _ { \sigma } F$ is the first parameter group. Since this group is a complete system (Section 4.9),the commutator $( X _ { \sigma } ^ { \prime } F + A _ { \sigma } F , X _ { \tau } ^ { \prime } F + A _ { \tau } F )$ is a linear combination of

$$
X _ { \rho } ^ { \prime } F + A _ { \rho } F \qquad ( \rho = 1 , 2 , . . . , r ) ,
$$

that is,

$$
( X _ { \sigma } ^ { \prime } F + A _ { \sigma } F , X _ { \tau } ^ { \prime } F + A _ { \tau } F ) = \sum _ { \rho } \phi _ { \sigma \tau \rho } ( X _ { \rho } ^ { \prime } F + A _ { \rho } F ) .
$$

Since

$$
( X _ { \sigma } ^ { \prime } F + A _ { \sigma } F , X _ { \tau } ^ { \prime } F + A _ { \tau } F ) = ( X _ { \sigma } ^ { \prime } F , X _ { \tau } ^ { \prime } F ) + ( A _ { \sigma } F , A _ { \tau } F ) ,
$$

the commutator is divided into

$$
( X _ { \sigma } ^ { \prime } , X _ { \tau } ^ { \prime } ) = \sum _ { \rho } \phi _ { \sigma \tau \rho } X _ { \rho } ^ { \prime } F , \qquad ( A _ { \sigma } , A _ { \tau } ) = \sum _ { \rho } \phi _ { \sigma \tau \rho } A _ { \rho } F \eqno ( \sigma , \tau _ { - } ) ,
$$

Since $\| \alpha _ { \sigma \rho } ( a ) \| \neq 0$ ，the second relation determines $\phi _ { \sigma \tau \rho }$ uniquely as functions of $^ a$ Differentiating the first relation with respect to $a _ { \lambda }$ we obtain

$$
\sum _ { \rho } \frac { \partial \phi _ { \sigma \tau \rho } } { \partial a _ { \lambda } } X _ { \rho } ^ { \prime } F = 0 .
$$

Since the $X ^ { \prime } F$ are linearly independent, we should have

$$
\frac { \partial \phi _ { \sigma \tau \rho } } { \partial a _ { \lambda } } = 0 , \mathrm { t h a t i s , } \phi _ { \sigma \tau \rho } = \mathrm { c o n s t a n t . }
$$

Thus

$$
( X _ { \sigma } , X _ { \tau } ) f = \sum _ { \rho } c _ { \sigma \tau \rho } X _ { \rho } f \qquad ( \sigma , \tau = 1 , 2 , . . . , r ) .
$$

This is the second fundamental theorem of $\ L i e ; c _ { \sigma \tau \rho }$ are the structure constants. Conversely, if $r$ independent infinitesimal transformations

$$
X _ { 1 } f _ { 3 } \ldots , X _ { r } f
$$

satisfy (l74)， then these transformations belong to the $r$ -parameter group, and any transformation of this group in the neighborhood of the identical element is generated by $\textstyle \sum _ { \rho } c _ { \rho } X _ { \rho } f .$

There are relations among the $r ^ { 3 }$ structure constants. Since

$$
( X _ { \sigma } , X _ { \tau } ) + ( X _ { \tau } , X _ { \sigma } ) = 0 ,
$$

we have

$$
\sum _ { \rho } { ( c _ { \sigma \tau \rho } + c _ { \tau \sigma \rho } ) X _ { \rho } f } = 0 .
$$

Since $X _ { \rho } f$ are linearly independent, we also have

$$
c _ { \sigma \tau \rho } + c _ { \tau \sigma \rho } = 0 .
$$

Substituting $X _ { \sigma } f , X _ { \tau } f , X _ { \rho } f$ in Poisson's identity (Section 4.3), we obtain

$$
( ( X _ { \sigma } , X _ { \tau } ) , X _ { \rho } ) \ + \ ( ( X _ { \tau } , X _ { \rho } ) , X _ { \sigma } ) \ + \ ( ( X _ { \rho } , X _ { \sigma } ) , X _ { \tau } ) \equiv 0 ,
$$

or

$$
\begin{array} { l } { \displaystyle  \sum _ { \lambda } \sum _ { \kappa } [ ( c _ { \sigma \tau \lambda } c _ { \lambda \rho \kappa } + c _ { \tau \rho \lambda } c _ { \lambda \sigma \kappa } + c _ { \rho \sigma \lambda } c _ { \lambda \tau \kappa } ) ] \equiv 0 } \\ { \displaystyle ( \rho , \sigma , \tau = 1 , 2 , . . . , r ) . } \end{array}
$$

Equations (l75） represent a plane manifold in the $r ^ { 3 }$ -dimensional space,while the equations (l76) represent a quadratic manifold. Each point on the intersection $\mathfrak { M }$ of these two manifolds corresponds to the structure constant of the group. Substituting $\bar { X } _ { 1 } f , . . . , \bar { X } _ { r } f \mathrm { f o r }$

$$
X _ { 1 } f , . . . , X _ { r } f
$$

by means of

$$
\bar { X } _ { \rho } f = a _ { \rho 1 } X _ { 1 } f + \cdot \cdot \cdot + a _ { \rho r } X _ { r } f \qquad ( \rho = 1 , 2 , . . . , r ) ,
$$

we obtain

$$
X _ { \sigma } f = \bar { a } _ { 1 \sigma } \bar { X } _ { 1 } f + \cdot \cdot \cdot + \bar { a } _ { r \sigma } \bar { X } _ { r } f \qquad ( \sigma = 1 , 2 , . . . , r ) ,
$$

since the $X _ { \rho } f$ are mutually independent. From the relations

$$
\begin{array} { l } { { ( \displaystyle \bar { X } _ { \tau } , \displaystyle \bar { X } _ { \kappa } ) f = \sum _ { \lambda , \mu , \nu } a _ { \tau \lambda } a _ { \kappa \mu } c _ { \lambda \mu \nu } \displaystyle \bar { X } _ { \nu } f } } \\ { { { } } } \\ { { { } = \displaystyle \sum _ { \lambda , \mu , \nu , \rho } a _ { \tau \lambda } a _ { \kappa \mu } \bar { a } _ { \rho \nu } c _ { \lambda \mu \nu } \displaystyle \bar { X } _ { \rho } f = \sum _ { \rho } \bar { c } _ { \tau \kappa \rho } \displaystyle \bar { X } _ { \rho } f , } } \end{array}
$$

we obtain

$$
\bar { c } _ { \tau \kappa \rho } = \sum _ { \lambda , \mu , \nu } a _ { \tau \lambda } a _ { \kappa \mu } \bar { a } _ { \rho \nu } c _ { \lambda \mu \nu } \qquad ( \rho , \tau , \kappa = 1 , 2 , . . . , r ) .
$$

Thus to each linear transformation (l77） there corresponds a linear transformation of $c _ { \tau \kappa \rho }$ of (178). Conversely,by solving (178) we obtain

$$
c _ { \tau \kappa \rho } = \sum _ { \lambda , \mu , \nu } \bar { a } _ { \lambda \tau } a _ { \mu \kappa } \bar { a } _ { \nu \rho } c _ { \lambda \mu \nu } \qquad ( \rho , \tau , \kappa = 1 , 2 , . . . , r ) .
$$

Since (177) constitute a group, the transformations (l78) also con-stitute a group. This group of $c _ { \tau \kappa \rho }$ is said to be induced by the group $X _ { \sigma } f .$ The constants $\bar { c } _ { \tau \kappa \rho }$ and ${ c } _ { \lambda \mu \nu }$ related through (178) are not essentially different.A point of $\mathfrak { M }$ is simply transformed to another·point of $\mathfrak { M }$ Two groups with the same structure constants are said to be equivalent.

For proving the converse of this theorem- that is, that there exists an $\pmb { r }$ -parameter group (174) for the $r ^ { 3 }$ constants satisfying (175) and (176)- it is sufficient to show the existence of $\psi _ { \sigma \rho } ( a )$ ， such that $\| \psi _ { \sigma \rho } ( a ) \| \neq 0$ satisfies Maurer's relations (161)

$$
\frac { \partial \psi _ { \rho \sigma } } { \partial a _ { \tau } } - \frac { \partial \psi _ { \rho \tau } } { \partial a _ { \sigma } } = \sum _ { \lambda , \kappa } c _ { \lambda \kappa \rho } \psi _ { \lambda \sigma } \psi _ { \kappa \tau } \qquad ( \rho , \sigma , \tau = 1 , 2 , . . . , r ) .
$$

This relation is merely the integrability condition for the Pfaffan (162), which can be written, similarly to (158a),

$$
d f ( x ^ { \prime } ) = \sum _ { \rho } P _ { \rho } X _ { \rho } ^ { \prime } f ( x ^ { \prime } ) , \qquad P _ { \rho } = \sum _ { \sigma } \psi _ { \rho \sigma } ( a ) \ d a _ { \sigma } .
$$

Let $a _ { 1 } , \ldots , a _ { r }$ be the canonical parameters. The finite transformations of the corresponding group are obtained by the infinitesimal transformation $\textstyle ( \sum _ { i } a _ { i } X _ { i } f ) \delta t$ from $t = 0$ to $t = 1$ . If we continue it from $t = 1$ to $t = 1 + \delta t$ ，then we obtain the finite transformations with the canonical parameters $a _ { 1 } ( 1 + \delta t ) , \dotsc , a _ { r } ( 1 + \delta t )$ .From(180) we obtain

$$
\delta f ( x ^ { \prime } ) = \sum _ { \rho , \sigma } \psi _ { \rho \sigma } ( a ) a _ { \sigma } X _ { \rho } ^ { \prime } f ( x ^ { \prime } ) \delta t .
$$

On the other hand,

$$
\delta f ( x ^ { \prime } ) = \sum _ { \rho } a _ { \rho } X _ { \rho } ^ { \prime } f ( x ^ { \prime } ) \delta t .
$$

Hence

$$
\sum _ { \sigma } \psi _ { \rho \sigma } ( a ) a _ { \sigma } = a _ { \rho } \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

Multiply Maurer's relation by $a _ { \tau }$ and sum over $\pmb { \tau }$ ； then, referring to (181),we obtain

$$
\sum _ { \tau } \frac { \partial \psi _ { \rho \sigma } } { \partial a _ { \tau } } a _ { \tau } - \sum _ { \tau } \frac { \partial \psi _ { \rho \tau } } { \partial a _ { \sigma } } a _ { \tau } = \sum _ { \lambda , \kappa } c _ { \lambda \kappa \rho } \psi _ { \lambda \sigma } a _ { \kappa } .
$$

Write

$$
U f = a _ { 1 } { \frac { \partial f } { \partial a _ { 1 } } } + \cdot \cdot \cdot + a _ { r } { \frac { \partial f } { \partial a _ { r } } } ;
$$

then, from (181),

$$
U \psi _ { \rho \sigma } = \epsilon _ { \rho \sigma } - \psi _ { \rho \sigma } + \sum _ { \lambda , \kappa } c _ { \lambda \kappa \rho } \psi _ { \lambda \sigma } a _ { \kappa } \qquad ( \rho , \sigma = 1 , 2 , . . . , r ) .
$$

Put $a _ { \rho } = \lambda _ { \rho } t$ ; then (182） becomes

$$
\frac { \partial ( t \psi _ { \rho \sigma } ) } { \partial t } = \epsilon _ { \rho \sigma } + \sum _ { \lambda , \kappa } c _ { \lambda \kappa \rho } ( t \psi _ { \lambda \sigma } ) \lambda _ { \kappa } ,
$$

since

$$
\frac { \partial \psi _ { \rho \sigma } } { \partial t } = \sum _ { \tau } \frac { \partial \psi _ { \rho \sigma } } { \partial a _ { \tau } } \lambda _ { \tau } = t ^ { - 1 } U \psi _ { \rho \sigma } .
$$

Denote by $L$ the matrix $( l _ { \rho \lambda } )$ , such that $\begin{array} { r } { \sum _ { \kappa } c _ { \lambda \kappa \rho } \lambda _ { \kappa } = l _ { \rho \lambda } , } \end{array}$ and by $\Psi = t \psi$ the matrix $( \Psi _ { \rho \sigma } ) = t ( \psi _ { \rho \sigma } )$ ; then the $r ^ { 3 }$ equations become

$$
\frac { \partial \Psi } { \partial t } = L \Psi + I ,
$$

where $\Psi$ reduces to a null matrix for $t = 0$ If $\lambda _ { 1 } , . . . , \lambda _ { \tau }$ are given, then this matrix equation determines $\Psi$ with the initial condition that $\Psi = 0$ for $t = 0$ in the form

$$
\Psi = t I + \frac { 1 } { 2 ! } t ^ { 2 } L + \frac { 1 } { 3 ! } t ^ { 3 } L ^ { 2 } + \cdot \cdot \cdot .
$$

Hence,

$$
\psi = I + \frac { t } { 2 ! } L + \frac { t ^ { 2 } } { 3 ! } L ^ { 2 } + \cdot \cdot \cdot .
$$

If we write $\begin{array} { r } { \sum _ { \kappa } c _ { \sigma \kappa \rho } a _ { \kappa } = A _ { \rho \sigma } } \end{array}$ and denote the matrix of $A _ { \rho \sigma } = t l _ { \rho \sigma }$ by $\pmb { A }$ ， then the $\psi _ { \rho \sigma }$ are given as functions of $^ { a }$ by means of

$$
\psi = I + \frac { 1 } { 1 ! } A + \frac { 1 } { 2 ! } A ^ { 2 } + \cdot \cdot \cdot , A ^ { m } = t ^ { m } L ^ { m } .
$$

It can be shown that this matrix $\psi$ satisfies Maurer's relation (161).

Denote the matrices of $\psi _ { \rho \sigma }$ and $\alpha _ { \rho \sigma }$ by $\psi$ and $\pmb { \alpha }$ ，respectively. By definition, $\psi _ { \alpha } = I .$ Then (l83) is written in the form

$$
\psi = \frac { e ^ { t L } - 1 } { t L }
$$

and the transposed reciprocal in the form

$$
\alpha ^ { \prime } = \frac { t L } { e ^ { t L } - 1 }
$$

in the neighborhood of $t = 0$ .With Bernoulli's numbers $B _ { 1 } , B _ { 2 } , \ldots ,$ it is known that

$$
\frac { x } { e ^ { x } - 1 } = 1 - \frac { x } { 2 } + \frac { 1 } { 2 ! } B _ { 1 } x ^ { 2 } - \frac { 1 } { 4 ! } B _ { 2 } x ^ { 4 } + \cdot \cdot \cdot .
$$

Thus

$$
\alpha ^ { \prime } = I - \frac { 1 } { 2 } A + \frac { 1 } { 2 ! } B _ { 1 } A ^ { 2 } - \frac { 1 } { 4 ! } B _ { 2 } A ^ { 4 } + \cdot \cdot \cdot .
$$

Since the elements of the transposed matrix are $\begin{array} { r } { A _ { \rho \sigma } ^ { \prime } = - \sum _ { \tau } a _ { \tau } c _ { \tau \rho \sigma } } \end{array}$ we have

$$
\alpha = I - \frac { 1 } { 2 } A ^ { \prime } + \frac { 1 } { 2 ! } B _ { 1 } A ^ { \prime 2 } - \frac { 1 } { 4 ! } B _ { 2 } A ^ { \prime 4 } + \cdot \cdot \cdot ,
$$

or, by writing the matrix of $c _ { \tau \rho \sigma }$ as $C _ { \tau }$ ，

$$
\alpha = I + \frac { 1 } { 2 } \sum _ { \tau } a _ { \tau } C _ { \tau } + \frac { 1 } { 2 ! } B _ { 1 } \Bigl ( \sum _ { \tau } a _ { \tau } C _ { \tau } \Bigr ) ^ { 2 } - \frac { 1 } { 4 ! } B _ { 2 } \Bigl ( \sum _ { \tau } a _ { \tau } C _ { \tau } \Bigr ) ^ { 4 } + \cdot \cdot \cdot ,
$$

where

$$
A ^ { \prime } = - \sum _ { \tau } a _ { \tau } C _ { \tau } , \qquad C _ { \tau } = ( c _ { \tau \rho \sigma } ) ,
$$

with fixed $\pmb { \tau }$ .Thus,the infinitesimal transformations $A _ { 1 } f _ { 3 } \ldots , A _ { r } f$ are determined from the matrix $\pmb { \alpha }$

This proof is due to Schur (l89l).This is the foundation of Baker's method of solving a system of linear differential equations with variable coefficients (Baker,1902,1904;Hagihara,1940),which will be applied in Chapter l2 (Volume 3） to several problems in celestial mechanics. It is to be noticed that the series (l84) is convergent if $\pmb { A }$ ,and accordingly $t _ { : }$ ， is sufficiently small. Since the elements $\psi _ { \rho \sigma }$ are convergent series arranged in powers of $a _ { 1 } , \ldots , a _ { r }$ ,so are the elements $\alpha _ { \rho \sigma }$ of the reciprocal matrix.

Lie's series are applied by Grobner (l96O) to the numerical solution of differential equations,and by Knapp (l964） to the computation of perturbation (Section 10.44).

# 4.27ADJOINT GROUPS

A finite continuous group containing at least one transformation that transforms every point of a region in a space $R _ { n } ( x _ { 1 } , \ldots , x _ { n } )$ to another point of the space is said to be transitive. If for a given set of values of $x _ { 1 } , . . . , x _ { n } , x _ { 1 } ^ { \prime } , . . . , x _ { n } ^ { \prime }$ there is at least one set of values of $a _ { 1 } , \ldots , a _ { r }$ satisfying (l55)，then the transformation group (l55） is transitive. Otherwise it is intransitive.Hence,a transitive group should contain at least $\pmb { n }$ essential parameters.For $n = r$ there exists in general a transformation that transforms one arbitrary point to another arbitrary point. Thus, there is no transformation that makes a general point invariant, except the identity transformation. Such a group is called simply transitive. (For the same group definitions in another context, see discussion preceding the first fundamental theorem of Lie, Section 4.26.) Since the finite group of $r$ independent infinitesimal transformations

$$
X _ { \rho } f = \sum _ { i = 1 } ^ { n } \xi _ { \rho i } ( x ) \ { \frac { \partial f } { \partial x _ { i } } } \qquad ( \rho = 1 , 2 , . . . , r )
$$

can be written

$$
x _ { i } ^ { \prime } = x _ { i } + \sum _ { \rho = 1 } ^ { r } e _ { \rho } \xi _ { \rho i } ( x ) + \cdot \cdot \cdot ,
$$

it is sufficient for this group to be transitive that none of the nth order determinants $\| \xi _ { \rho i } \|$ be zero. Denote the rank of the matrix $( \xi _ { \rho i } )$ by $m \textless n$ ； then $_ m$ transformations out of $X _ { \rho } f$ are independent and the remaining transformations are linear combinations of these. These $\pmb { m }$ transformations form a complete system (Section 4.9).Hence the system admits $n - m$ independent solutions $\Omega _ { 1 } ( { \pmb x } ) , . . . , \Omega _ { n - m } ( { \pmb x } )$ .Thus there are $n - m$ relations between $_ { x }$ and $\scriptstyle { \pmb x } ^ { \prime }$ ; that is,

$$
\Omega _ { s } ( x ^ { \prime } ) = \Omega _ { s } ( x ) \qquad ( s = 1 , 2 , . . . , n - m ) .
$$

Hence,in order for the group to be transitive,it is necessary and sufficient that $m$ of $X _ { 1 } f = 0 , . . . , X _ { r } f = 0$ be independent. If the group is transitive it contains no invariant.If it is intransitive, then the solutions common to the $r$ transformations $X _ { \rho } f = 0$ are the only invariants, and

$$
\Omega _ { 1 } ( x ) = C _ { 1 } , . . . , \Omega _ { n - m } ( x ) = C _ { n - m }
$$

divide the $\pmb { n }$ dimensional space into $_ m$ -dimensional regions $M _ { m }$ that are invariant for $X _ { 1 } f = 0 , . . . , X _ { \tau } f = 0$ ，This invariant manifold $M _ { m }$ can be determined by eliminating the given transformation group from the finite equations (155).

Since a point of the space $R _ { n }$ can belong to one and only one of the $\infty ^ { n - m }$ manifolds (186),we say that any manifold decomposes (zerlegen) the space $R _ { n }$ ； this decomposition is invariant for any transformation of $X _ { 1 } f = 0 , . . . , X _ { \tau } f = 0 \quad$ ，Even a transitive group can decompose $R _ { n }$ into $\infty ^ { n - m }$ manifolds of the form (186).Such manifolds are invariant for the transformation group as a whole, but each individual manifold may be interchanged by a single transformation of the group. If a group decomposes $R _ { n }$ invariantly into $\infty ^ { n - m }$ $m$ -dimensional manifolds $M _ { m }$ in this manner, then the group is called imprimitive. Otherwise it is primitive. An intransitive group is imprimitive.A primitive group is transitive.

The condition for imprimitivity is that the complete system of $_ m$ independent equations $Y _ { 1 } f = 0 , . . . , Y _ { m } f = 0$ admits all transformations of the group. This system of equations defines an $_ m$ -dimensional manifold $M _ { m }$ .If the system admits all transformations of the group, then $M _ { m }$ is invariant for those transformations. The integer $\pmb { m }$ is called the order of imprimitivity (Section 4.23).The necessary and sufficient condition for the order of imprimitivity to be $_ m$ is that the complete system of equations $X _ { 1 } f = 0 , . . . , X _ { m } f = 0$ admits $n - m$ independent solutions.

Consider in $m$ independent infinitesimal transformations

$$
X _ { \rho } f = \sum _ { i = 1 } ^ { n } \xi _ { \rho i } { \frac { \partial f } { \partial x _ { i } } } \qquad ( \rho = 1 , 2 , . . . , m ) ,
$$

a set of $\infty ^ { n - m }$ infinitesimal transformations represented by

$$
e _ { 1 } X _ { 1 } f + \cdot \cdot \cdot + e _ { m } X _ { m } f
$$

with $_ m$ arbitrary constants $e _ { 1 } , \ldots , e _ { m }$ . The transformed set can be written

$$
\sum _ { \rho = 1 } ^ { m } e _ { \rho } \sum _ { i = 1 } ^ { n } \xi _ { \rho i } ( x ) \ \frac { \partial f } { \partial x _ { i } } = \sum _ { \rho = 1 } ^ { m } e _ { \rho } ^ { \prime } \sum _ { i = 1 } ^ { n } \xi _ { \rho i } ( x ^ { \prime } ) \ \frac { \partial f } { \partial x _ { i } ^ { \prime } } .
$$

Hence, if

$$
\sum _ { \rho = 1 } ^ { m } e _ { \rho } X _ { \rho } f = \sum _ { \sigma = 1 } ^ { m } e _ { \sigma } ^ { \prime } X _ { \sigma } ^ { \prime } f ,
$$

then the set of transformations $\scriptstyle \sum _ { \rho = 1 } ^ { m } e _ { \rho } X _ { \rho } f$ is invariant for the transformation of $_ { x }$ to $\scriptstyle { \pmb x } ^ { \prime }$ ; that is, $S T _ { e } S ^ { - 1 } = T _ { e ^ { \prime } ; }$ or $S T _ { e } = T _ { e ^ { \prime } } S .$

Let $Y f$ be a one-parameter group for the transformation of the parameter $t$ -from $_ x$ to $x _ { i } ^ { \prime } = f _ { i } ( x _ { 1 } , . . . , x _ { n } , t )$ . Then $x _ { i } ^ { \prime } = x _ { i } + t Y + \cdot \cdot \cdot _ { ; }$ and $X _ { \rho } ^ { \prime } f = X _ { \rho } f + t ( Y , X _ { \rho } ) f + \cdot \cdot \cdot$ . In order that this transformation may belong to the set of those transformations, we should have

Hence

$$
( Y , X _ { \rho } ) f = \sum _ { j = 1 } ^ { m } g _ { \rho j } X _ { j } f \qquad ( \rho = 1 , 2 , . . . , m ) .
$$

$$
\frac { \partial ( X _ { \rho } ^ { \prime } f ) } { \partial t } = \sum _ { j = 1 } ^ { m } g _ { \rho j } X _ { j } ^ { \prime } f \qquad ( \rho = 1 , 2 , . . . , m ) .
$$

Since

$$
\begin{array} { r l r } {  { \frac { \partial } { \partial t } \sum _ { \rho } e _ { \rho } ^ { \prime } X _ { \rho } ^ { \prime } f = \sum _ { \rho } \frac { d e _ { \rho } ^ { \prime } } { d t } X _ { \rho } ^ { \prime } f + \sum _ { \rho } e _ { \rho } ^ { \prime } \sum _ { j } g _ { \rho j } X _ { j } ^ { \prime } f } } \\ & { } & \\ & { } & { = \sum _ { \rho } \{ \frac { d e _ { \rho } ^ { \prime } } { d t } + \sum _ { j } g _ { j \rho } e _ { j } ^ { \prime } \} X _ { \rho } ^ { \prime } f , } \end{array}
$$

therefore

$$
\frac { d e _ { \rho } ^ { \prime } } { d t } + \sum _ { j } g _ { j \rho } e _ { j } ^ { \prime } = 0 .
$$

The values of $\boldsymbol { e } _ { \rho }$ are determined by integrating (188） from $e _ { \rho } ^ { \prime } = e _ { \rho }$ at $t = 0$

If $X _ { 1 } f _ { 3 } \ldots , X _ { r } f$ satisfies (188)，that is,if $\textstyle \sum _ { \rho } e _ { \rho } X _ { \rho } f$ constitutes an $\boldsymbol { r }$ -parameter group and all transformations of this set of transformation groups make the one-parameter group $Y f$ invariant, then

$$
( Y , X _ { \rho } ) f = \sum _ { \sigma = 1 } ^ { r } g _ { \rho \sigma } X _ { \sigma } f \qquad ( \rho = 1 , 2 , . . . , r ) .
$$

The coefficients $g _ { \rho \sigma }$ are given by (188) in terms of $\boldsymbol { e } _ { \rho }$ Then $r + 1$ infinitesimal transformations $X _ { 1 } f , . . . , X _ { r } f ,$ Yf constitute an $( r + 1 )$ parameter group,and the $\boldsymbol { r }$ transformations $X _ { 1 } f , . . . , X _ { \tau } f$ constitute its $\pmb { r }$ parameter subgroup. Let $x _ { i } ^ { \prime } = \psi _ { i } ( x _ { 1 } , \ldots , x _ { n } ; a _ { 1 } , \ldots , a _ { r + 1 } )$ be the finite transformations of the $( r + 1 )$ -parameter group; then the set of transformations $\textstyle \sum _ { \rho } e _ { \rho } X _ { \rho } f$ is invariant for the change of $_ { x }$ to $\mathbf { \Delta } _ { \mathbf { \boldsymbol { x } } ^ { \prime } }$

A similar situation exists for infinitesimal transformations $Y _ { 1 } f _ { 3 } \ldots , Y _ { m } f$ instead ofa single Yf. If $X _ { 1 } f , . . . , X _ { \tau } f$ are invariant for all transformations of an $( r + m )$ -parameter group, then they constitute an invariant subgroup (Lie,l888). Then (189） holds for all such $Y _ { i } f .$ Hence,if $X _ { 1 } f , . . . , X _ { \tau } f$ are contained in $X _ { 1 } f , . . . , X _ { r } f , Y _ { 1 } f , . . . , Y _ { m } f ,$ and all commutators $( Y _ { i } , Y _ { k } ) f$ are linear combinations of $X _ { 1 } f , . . . , X _ { r } f$ with constant coefficients, then $X _ { 1 } f , . . . , X _ { r } f$ constitute an invariant subgroup. In particular, if the transformation group $( X _ { \sigma } , X _ { \rho } )$ is an $\boldsymbol { r }$ -parameter group,then the group is the same as the group $X _ { 1 } f , . . . , X _ { r } f .$ If it contains less than $\pmb { r }$ parameters, then the group is an invariant subgroup of $X _ { 1 } f , \ldots , X _ { r } f .$ It can be shown that the group $X _ { 1 } f , . . . , X _ { r } f$ constitutes an invariant subgroup if mutually independent infinitesimal transformations $\boldsymbol { e } _ { 1 } X _ { 1 } \boldsymbol { f } + \cdot \cdot \cdot + \boldsymbol { e } _ { \tau } X _ { \tau } \boldsymbol { f } ,$ also independent of all commutators $( X _ { \rho } , X _ { \sigma } )$ ， are added to $( X _ { \rho } , X _ { \sigma } )$ . Actually, $( X _ { \sigma } , X _ { \rho } ) f$ contains at most $\boldsymbol { r }$ -parameter groups, as it belongs to $X _ { 1 } f _ { 3 } \ldots , X _ { r } f .$ Since

$$
( ( X _ { \sigma } , X _ { \rho } ) , ( X _ { \lambda } , X _ { \mu } ) ) = \sum _ { \tau , \kappa } c _ { \sigma \rho \tau } c _ { \lambda \mu \kappa } ( X _ { \tau } , X _ { \kappa } )
$$

from (182), the transformations $( X _ { \sigma } , X _ { \rho } )$ constitute a group. Since

$$
( X _ { \tau } , ( X _ { \rho } , X _ { \sigma } ) ) = \sum _ { \lambda } c _ { \rho \sigma \lambda } ( X _ { \tau } , X _ { \lambda } ) ,
$$

these transformations are invariant for $X _ { 1 } f _ { 3 } \ldots , X _ { r } f .$ This theorem will be applied in Section 4.28 for simple groups.

If infinitesimal transformations $Y _ { 1 } f _ { 3 } \ldots , Y _ { p } f$ and $Z _ { 1 } f _ { 3 } \ldots , Z _ { q } f$ of two invariant subgroups of the same group $G _ { r }$ have no infinitesimal transformation in common, then the $( Y _ { i } , Z _ { j } )$ should all vanish identically. If they have any common transformation, then the common infinitesimal transformation generates invariant subgroups of $G _ { \tau }$ . Actually， the $( Y _ { i } , Z _ { j } )$ are linear combinations of the infinitesimal transformations of the invariant subgroup with constant coefficients. If they have no transformation in common, then these combinations should be zero. If the $( Y _ { i } , Z _ { j } )$ are all zero, then the $Y _ { i }$ and $Z _ { j }$ are said to be permutable (vertauschbar). This theorem will be applied also in Section 4.28 for semisimple groups.

Let the independent transformations $Y _ { 1 } f _ { 3 } \ldots , Y _ { r } f$ belonging to the group $X _ { 1 } f _ { 3 } \ldots , X _ { \tau } f$ be such that

$$
\begin{array} { c } { { ( Y _ { \sigma } , Y _ { \rho } ) f = \displaystyle \sum _ { \tau } c _ { \sigma \rho \tau } ^ { \prime } Y _ { \tau } f , ( X _ { \sigma } , X _ { \rho } ) f = \displaystyle \sum _ { \tau } c _ { \sigma \rho \tau } X _ { \tau } f , } } \\ { { { } } } \\ { { { \displaystyle Y _ { \sigma } f = \displaystyle \sum _ { \rho } h _ { \sigma \rho } X _ { \rho } f . } } } \end{array}
$$

Then,

$$
( Y _ { \sigma } , Y _ { \rho } ) f = \sum _ { \lambda , \mu , \tau } h _ { \sigma \lambda } h _ { \rho \mu } c _ { \lambda \mu \tau } X _ { \tau } f = \sum _ { \lambda , \tau } h _ { \lambda \tau } c _ { \sigma \rho \lambda } ^ { \prime } X _ { \tau } f .
$$

Hence

$$
c _ { \sigma \rho \tau } ^ { \prime } = \frac { 1 } { D } \sum _ { \lambda } \bigg \{ \frac { \partial D } { \partial h _ { \tau \lambda } } \sum _ { \mu , \nu } h _ { \sigma \mu } h _ { \rho \nu } c _ { \mu \nu \lambda } \bigg \} , \quad \quad D = \| h _ { \rho \sigma } \| .
$$

If arbitrary $Z _ { 1 } f _ { 3 } \ldots , Z _ { r } f$ are such that $\begin{array} { r } { ( Z _ { \sigma } , Z _ { \rho } ) f = \sum _ { \tau } c _ { \sigma \rho \tau } ^ { \prime } Z _ { \tau } f } \end{array}$ with the constants $c _ { \sigma \rho \tau } ^ { \prime }$ satisfying these relations, then $Z _ { \rho } f$ and $X _ { \sigma } f$ are said to be of equal structure (gleichzusammengesetzt). If $Y _ { 1 } f _ { 3 } \ldots , Y _ { r - q } f$ can be chosen so that, for the infinitesimal transformations

$$
Z _ { \rho } f = \sum _ { \sigma = 1 } ^ { r - q } h _ { \rho \sigma } Y _ { \sigma } f \qquad ( \rho = 1 , 2 , . . . , r )
$$

of the $( r - q )$ -parameter group, none of the minor determinants of the $( r - q ) \operatorname { t h }$ order of $\| h _ { \rho \sigma } \|$ can be made zero, and, at the same time, chosen so that $\begin{array} { r } { ( Z _ { \sigma } , Z _ { \rho } ) = \sum _ { \tau = 1 } ^ { \tau } c _ { \sigma \rho \tau } Z _ { \tau } f } \end{array}$ are satisfied identically, then the group $X _ { 1 } f , \dotsc , X _ { \tau } f$ is said to be isomorphic for $q = 0$ ， and homomorphic for $q > 0$ to $Y _ { 1 } f _ { 3 } \ldots , Y _ { r - q } f .$ Two isomorphic or homomorphic groups are of equal structure, that is, have the same structure constants.

If $\textstyle \sum _ { \rho } c _ { \rho } X _ { \rho } f$ formed of the infinitesimal transformations $X _ { \rho } f$ of an $\pmb { r - }$ parameter group $x _ { i } ^ { \prime } = f _ { i } ( x _ { 1 } , \ldots , x _ { n } ; a _ { 1 } , \ldots , a _ { r } )$ can be written in the form (l87) for a transformation of $_ { x }$ to $\scriptstyle { \pmb x } ^ { \prime }$ as new variables, then

$$
\ell _ { \tau } ^ { ' } = \sum _ { \sigma } \rho _ { \tau \sigma } ( a _ { 1 } , . . . , a _ { r } ) e _ { \sigma } \qquad ( \tau = 1 , 2 , . . . , r ) ,
$$

where $e ^ { \prime }$ is independent of $_ x$ . Substituting $\pmb { x } _ { i } ^ { \prime \prime } = f _ { i } ( \pmb { x } ^ { \prime } , \pmb { b } )$ gives

$$
\sum _ { \rho } \ e _ { \rho } ^ { \prime } X _ { \rho } ^ { \prime } f = \sum _ { \sigma } e _ { \rho } ^ { \prime \prime } X _ { \rho } ^ { \prime \prime } f , \qquad e _ { \sigma } ^ { \prime \prime } = \sum _ { \tau } \rho _ { \sigma \tau } ( b _ { 1 } , . . . , b _ { r } ) e _ { \tau } ^ { \prime } .
$$

Since $\boldsymbol { x } _ { i } ^ { \prime } = f _ { i } ( \boldsymbol { x } , \boldsymbol { a } )$ form a group, we can write

$$
x _ { i } ^ { \prime \prime } = f _ { i } ( x , c ) , \qquad c _ { \rho } = \phi _ { \rho } ( a _ { 1 } , . . . , a _ { r } , b _ { 1 } , . . . , b _ { r } ) .
$$

Hence,

$$
\sum _ { \rho } e _ { \rho } X _ { \rho } f = \sum _ { \sigma } e _ { \sigma } ^ { \prime \prime } X _ { \sigma } ^ { \prime \prime } f ,
$$

and

$$
\epsilon _ { \sigma } ^ { \prime \prime } = \sum _ { \tau } \rho _ { \sigma \tau } ( c _ { 1 } , . . . , c _ { r } ) e _ { \tau } = \sum _ { \tau } \rho _ { \sigma \tau } ( \phi _ { 1 } ( a , b ) , . . . , \phi _ { r } ( a , b ) ) e _ { \tau } .
$$

Thus $\begin{array} { r } { e _ { \sigma } ^ { \prime } = \sum _ { \tau } \rho _ { \sigma \tau } ( a ) e _ { \tau } } \end{array}$ form a group, and

$$
\ell _ { \sigma } ^ { \prime \prime } = \sum _ { \tau , \kappa } \rho _ { \sigma \tau } ( b _ { 1 } , . . . , b _ { r } ) \rho _ { \tau \kappa } ( a _ { 1 } , . . . , a _ { r } ) e _ { \kappa } ,
$$

$$
\rho _ { \sigma \tau } ( \phi _ { 1 } ( a , b ) , \ldots , \phi _ { r } ( a , b ) ) \equiv \sum _ { \kappa } \rho _ { \kappa \tau } ( a _ { 1 } , \ldots , a _ { r } ) \rho _ { \sigma \kappa } ( b _ { 1 } , \ldots , b _ { r } ) .
$$

Hence, the group (190) belongs to a group $\pmb { x } _ { i } ^ { \prime } = f _ { i } ( \pmb { x } , \pmb { a } )$ or to a group of infinitesimal transformations $X _ { 1 } f , . . . , X _ { \tau } f .$ This last group is called the adjoint group.

In other words, let $S T _ { e } S ^ { - 1 } = T _ { e ^ { \prime } }$ be the first transformation, and $S _ { 1 } T _ { e } { , } S _ { 1 } ^ { - 1 } = T _ { e } { , }$ be the second. Then

$$
T _ { e ^ { \prime \prime } } = S _ { 1 } S T _ { e } S ^ { - 1 } S _ { 1 } ^ { - 1 } = S _ { 2 } T _ { e } S _ { 2 } ^ { - 1 } ,
$$

with $S _ { 2 } = S _ { 1 } S$ If $S T _ { e } S ^ { - 1 } = T _ { e ^ { \prime } } , S T _ { e ^ { \prime } } S ^ { - 1 } = T _ { e ^ { \prime \prime } } ;$ then

$$
T _ { e ^ { \prime \prime } } T _ { e ^ { \prime } } = S T _ { e ^ { \prime } } S ^ { - 1 } S T _ { e } S ^ { - 1 } = S T _ { e ^ { \prime } } T _ { e } S ^ { - 1 } ;
$$

that is, $T _ { e }$ is in automorphism. Thus $T _ { e }$ constitutes a group, and every transformation of the adjoint group is homomorphic to the given group.Hence, if a canonical parameter $\boldsymbol { e } _ { \rho }$ admits the transformations of an adjoint group,then the structure constants are invariant. Consequently, if the infinitesimal transformations of an adjoint group are written in the form

$$
E _ { \mu } f = \sum _ { \lambda , \kappa } c _ { \lambda \mu \kappa } e _ { \lambda } { \frac { \partial f } { \partial e _ { \kappa } } } \qquad ( \mu = 1 , 2 , . . . , r ) ,
$$

then $\lambda _ { 1 } E _ { 1 } f + \cdot \cdot \cdot + \lambda _ { \tau } E _ { \tau } f$ is a general infinitesimal transformation of the adjoint group and satisfes the relation

$$
( E _ { \mu } , E _ { \nu } ) f = \sum _ { \rho } c _ { \mu \nu \rho } E _ { \rho } f \qquad ( \mu , \nu = 1 , 2 , . . . , r ) .
$$

If $E _ { 1 } f _ { 3 } \ldots , E _ { \tau } f$ are not independent, but $\begin{array} { r } { \sum _ { \rho } g _ { \rho } E _ { \rho } f \equiv 0 } \end{array}$ ,then, from

$$
\sum _ { \rho } g _ { \rho } \sum _ { \sigma , \tau } c _ { \sigma \rho \tau } e _ { \sigma } \frac { \partial f } { \partial e _ { \tau } } = 0 ,
$$

we obtain

$$
\sum _ { \rho } g _ { \rho } c _ { \sigma \rho \tau } = 0 \qquad ( \sigma , \tau = 1 , 2 , . . . , r ) .
$$

Hence

$$
\Bigl ( X _ { \sigma } f , \sum _ { \rho } g _ { \rho } X _ { \rho } f \Bigr ) = \sum _ { \tau } \Bigl ( \sum _ { \rho } g _ { \rho } c _ { \sigma \rho \tau } \Bigr ) X _ { \tau } f \equiv 0 .
$$

Thus, $\textstyle \sum _ { \rho } g _ { \rho } X _ { \rho } f$ is permutable with all infinitesimal transformations $X _ { \rho } f .$ Conversely, if $X _ { 1 } f , . . . , X _ { r } f$ contain allinfinitesimal transformations that are permutable with all $X _ { \sigma } f$ in $\begin{array} { r } { \sum _ { \rho } g _ { \rho } X _ { \rho } f , } \end{array}$ then $\begin{array} { r } { \sum _ { \rho } g _ { \rho } E _ { \rho } f \equiv 0 . } \end{array}$ Such a $\textstyle \sum _ { \rho } g _ { \rho } X _ { \rho } f$ is called the distinguished (ausgezeichnete) infinitesimal transformation of the group.A distinguished infinitesimal transformation is invariant for the transformation $\boldsymbol { x } _ { i } ^ { \prime } = f _ { i } ( \boldsymbol { x } , \boldsymbol { a } )$ ; that is,

$$
\sum _ { \rho } g _ { \rho } X _ { \rho } f = \sum _ { \rho } g _ { \rho } ^ { \prime } X _ { \rho } ^ { \prime } f .
$$

In the corresponding adjoint group, $\begin{array} { r } { \sum _ { \rho } g _ { \rho } E _ { \rho } f = 0 . } \end{array}$ Hence, if there are just $m$ such distinguished transformations among $X _ { \rho } f$ of the transformation group $G _ { \tau ; \tau }$ then $r \mathrm { ~ - ~ } m$ among $E _ { \sigma } f$ are not independent. The adjoint group has $r \mathrm { ~ - ~ } m$ essential parameters. Thus, since $\textstyle \sum _ { \rho } g _ { \rho } X _ { \rho } f$ is invariant for the group $G _ { r }$ ，this group $G _ { r }$ is imprimitive.Conversely,if $G _ { \tau }$ is primitive in $x _ { 1 } , . . . , x _ { n } ,$ then $G _ { r }$ does not contain any distinguished infinitesimal transformation. The adjoint group of an $\boldsymbol { r }$ parameter primitive group contains $\pmb { r }$ essential parameters.Conversely, $G _ { r }$ has a primitive group, that is, an adjoint group.

The transformation (155) with $\boldsymbol { r }$ essential parameters satisfies

$$
\frac { \partial x _ { i } ^ { \prime } } { \partial a _ { \rho } } = \sum _ { \sigma } \psi _ { \sigma \rho } ( a ) \xi _ { \sigma i } ( x ^ { \prime } ) .
$$

Since $\| \psi _ { \sigma \rho } \| \neq 0 _ { ; }$ ，these equations can be solved in the form

$$
\xi _ { \sigma i } ( \pmb { x } ^ { \prime } ) = \sum _ { \rho } \alpha _ { \sigma \rho } ( a ) \frac { \partial \pmb { x } _ { i } ^ { \prime } } { \partial a _ { \rho } } ,
$$

and the transformation (155) is generated by $\boldsymbol { r }$ independent infinitesimal transformations

$$
X _ { \rho } f = \sum _ { i = 1 } ^ { n } \xi _ { \rho i } ( x _ { 1 } , . . . , x _ { n } ) \frac { \partial f } { \partial x _ { i } } \quad \quad ( \rho = 1 , 2 , . . . , r ) .
$$

The $\pmb { r }$ infinitesimal transformations of the corresponding parameter group

$$
A _ { \rho } f = \sum _ { \sigma } \alpha _ { \rho \sigma } ( a ) \ : \frac { \partial f } { \partial a _ { \sigma } }
$$

are independent. It has been seen that their structure constants are the same as those of $X _ { \rho } f ;$ that is,if

$$
( X _ { \rho } , X _ { \sigma } ) f = \sum _ { \tau } c _ { \rho \sigma \tau } X _ { \tau } f ,
$$

then

$$
( A _ { \rho } , A _ { \sigma } ) f = \sum _ { \tau } c _ { \rho \sigma \tau } A _ { \tau } f .
$$

We have seen also that the finite transformation

$$
x _ { i } ^ { \prime } = F _ { i } ( x _ { 1 } , \dots , x _ { n } , \lambda _ { 1 } t , . . . , \lambda _ { r } t )
$$

with the canonical form

$$
x _ { i } ^ { \prime } = x _ { i } + \sum _ { \sigma } e _ { \sigma } \xi _ { \sigma i } ( x ) + \cdot \cdot \cdot
$$

of the transformation (155) is obtained by integrating

$$
\frac { d x _ { 1 } ^ { \prime } } { \sum _ { \rho } \lambda _ { \rho } \xi _ { \rho 1 } ^ { \prime } } = \cdot \cdot \cdot = \frac { d x _ { n } ^ { \prime } } { \sum _ { \rho } \lambda _ { \rho } \xi _ { \rho n } ^ { \prime } } = d t ,
$$

with the initial condition $x _ { 1 } ^ { \prime } = x _ { 1 } , . . . , x _ { n } ^ { \prime } = x _ { n }$ for $t = 0$ Multiply (194) by $\lambda _ { \sigma }$ and sum over $\pmb { \sigma }$ ,then from these differential equations we obtain

$$
\frac { d x _ { i } ^ { \prime } } { d t } = \sum _ { \rho , \sigma } \lambda _ { \rho } \alpha _ { \rho \sigma } ( a ) \frac { \partial x _ { i } ^ { \prime } } { \partial a _ { \sigma } } .
$$

Hence, regarded as functions of $t _ { : }$ ， the parameters $a _ { 1 } , \ldots , a _ { r }$ must satisfy

$$
\frac { d a _ { \rho } } { d t } = \sum _ { \sigma } \lambda _ { \sigma } \alpha _ { \sigma \rho } ( a ) .
$$

The initial condition is that $^ { a }$ should be $a ^ { 0 }$ ，obtained by putting $\boldsymbol { x } = \boldsymbol { x } ^ { \prime }$ for $t = 0$ in the equations (155). Integrating (l96) with this initial condition,we obtain

$$
a _ { \rho } = \Phi _ { \rho } ( \lambda _ { 1 } t , . . . , \lambda _ { \tau } t ) .
$$

Putting these in (155)，we obtain the required canonical form (195). Write this as

$$
a _ { \rho } = \Phi _ { \rho } ( u _ { 1 } , \ldots , u _ { r } ) , \qquad u = \lambda t .
$$

Then the canonical transformation is of the form

$$
\begin{array} { r } { x _ { i } ^ { \prime } = f _ { i } ( x _ { 1 } , \dots , x _ { n } ; \Phi _ { 1 } ( u ) , \dots , \Phi _ { r } ( u ) ) . } \end{array}
$$

In other words, the system of equations in $a _ { 1 } , \dotsc , a _ { r } , t$ admits the infinitesimal transformation

$$
A f = \lambda _ { 1 } A _ { 1 } f + \cdot \cdot \cdot + \lambda _ { r } A _ { r } f + \frac { \partial f } { \partial t } ,
$$

and can be integrated with respect to $^ { a }$ with the initial condition that $\ a = a ^ { 0 }$ for $t = 0$ ,where it is assumed that $\| \psi _ { \rho \sigma } \| \neq 0$ for $\scriptstyle a = a ^ { 0 }$

The computation is simplifed if we use the adjoint group (Lie, 1893, 1893a). Substituting ${ \pmb x } _ { i } ^ { \prime } = f _ { i } ( { \pmb x } )$ in

$$
\sum _ { \rho } e _ { \rho } \sum _ { i } \xi _ { \rho i } ( x ) \frac { \partial f } { \partial x _ { i } } = \sum _ { \rho } e _ { \rho } X _ { \rho } f ,
$$

we obtain

$$
\sum _ { \rho } e _ { \rho } ^ { \prime } X _ { \rho } ^ { \prime } f = \sum _ { \sigma } e _ { \sigma } X _ { \sigma } f .
$$

Accordingly

$$
e _ { \sigma } ^ { \prime } = \sum _ { \tau } \rho _ { \sigma \tau } ( a ) e _ { \tau }
$$

defines the adjoint group. If the integral (l97) of the system (l96) is known， then the canonical form of the adjoint group is obtained by substituting (l97) in (198). The result is of the form

$$
\boldsymbol { e } _ { \sigma } ^ { \prime } = \sum _ { \tau } \rho _ { \sigma \tau } ( \Phi _ { 1 } ( \lambda t ) , . . . , \Phi _ { \tau } ( \lambda t ) ) \boldsymbol { e } _ { \tau } .
$$

If the integral (l97） is not known,then we determine the canonical form by solving

$$
\frac { d \boldsymbol { e } _ { 1 } ^ { \prime } } { \underset { \rho } { \sum } \lambda _ { \rho } \boldsymbol { e } _ { \rho 1 } ^ { \prime } } = \cdot \cdot \cdot = \frac { d \boldsymbol { e } _ { r } ^ { \prime } } { \underset { \rho } { \sum } \lambda _ { \rho } \boldsymbol { e } _ { \rho r } ^ { \prime } } = d t ,
$$

with the initial condition that $e ^ { \prime } = e$ for $t = 0$ ，because the adjoint group is generated by

$$
E _ { \rho } f = \sum _ { \sigma , \tau } c _ { \sigma \rho \tau } e _ { \sigma } \frac { \partial f } { \partial e _ { \tau } } , \qquad e _ { \rho \sigma } ^ { \prime } = \sum _ { \tau } c _ { \tau \rho \sigma } e _ { \tau } ^ { \prime }
$$

and satisfies

$$
( E _ { \sigma } , E _ { \rho } ) f = \sum _ { \tau } c _ { \sigma \rho \tau } E _ { \tau } f .
$$

Equations (l99) can be solved by mere algebraic operations, since they have constant coeffcients. Thus the canonical form of the adjoint group is determined in the form

$$
\boldsymbol { e } _ { \mu } ^ { \prime } = \sum _ { \nu } \sigma _ { \mu \nu } ( \lambda _ { 1 } t , . . . , \lambda _ { \tau } t ) \boldsymbol { e } _ { \nu } ,
$$

where $\sigma _ { \mu \nu }$ contain $\lambda t$ only in the combination $\lambda t = u$ 、This will be identical with (198) if we substitute (197); that is,

$$
\rho _ { \mu \nu } ( a _ { 1 } , . . . , a _ { r } ) = \sigma _ { \mu \nu } ( \lambda _ { 1 } t , . . . , \lambda _ { r } t )
$$

is an identity. This is the integral of (196).It satisfies (197) identically for $a = a ^ { 0 }$ ， $t = 0$ It is when the $\boldsymbol { r }$ parameter group does not contain the distinguished infinitesimal transformation that the equations (199） can be solved algebraically. If the group contains $ { l }$ independent distinguished transformations,that is,if $X _ { 1 } f , . . . , X _ { \tau } f$ contain $\mathbf { \xi } _ { l }$ transformations $\begin{array} { r } { Z f = \sum _ { i } \xi _ { i } ( { \pmb x } ) \ \partial f / \partial { \pmb x } _ { i } } \end{array}$ that are permutable with each of $X _ { 1 } f , \ldots , X _ { r } f ,$ then just $\mathbf { \xi } _ { l }$ independent integrations should be carried out besides algebraic operations (Lie, l927). The adjoint group constitutes, then, an $( r - l )$ -parameter group and obeys the relation (l95). The structure constants $c _ { \rho \sigma \tau }$ should satisfy Lie's third fundamental theorem. Conversely, if $r ^ { 3 }$ structure constants $c _ { \rho \sigma \tau }$ are given, then $X _ { 1 } f , . . . , X _ { r } f$ need not be considered, since the structure constants give the $\boldsymbol { r }$ parameter group.

Baker's matrix method of integrating a system of linear differential equations is founded on this use of the adjoint group (Baker, l902,1904; Hagihara, 1940).

# 4.28SIMPLE AND SEMISIMPLE GROUPS

A group that does not contain any invariant subgroup is called simple. Otherwise it is nonsimple (Lie,1893,i927; Cartan,1930,1933; Chevalley, 1951；see also Weyl, 1923,1923a,1924,1931,1939;van der Waerden, 1955,1959；Speiser,1923；Pontrjagin,1939;Grobner,1960).A simple group contains only one homomorphic subgroup, namely, the identity transformation.

Consider a nonsimple group. We choose the infinitesimal transformations of the group $G _ { r }$ so that $X _ { m + 1 } f _ { 3 } \ldots , X _ { \tau } f$ generate an $( r - m )$ parameter group.Then the structure of $G _ { r }$ is such that

$$
( X _ { \mu } , X _ { \nu } ) f = \sum _ { \sigma = 1 } ^ { m } c _ { \mu \nu \sigma } X _ { \sigma } f + \sum _ { \tau = 1 } ^ { r - m } c _ { \mu , \nu , m + \tau } X _ { m + \tau } f
$$

$$
( X _ { \mu } , X _ { m + k } ) f = \sum _ { \tau = 1 } ^ { r - m } c _ { \mu , m + k , m + \tau } X _ { m + \tau } f ,
$$

$$
( X _ { m + k } , X _ { m + j } ) f = \sum _ { \stackrel { \scriptstyle \tau = 1 } { \tau = 1 } } ^ { \stackrel { \scriptstyle \tau - m } { } } c _ { m + k , m + j , m + \tau } X _ { m + \tau } f
$$

Let the $_ m$ -parameter group homomorphic to $G _ { \tau }$ determined by the invariant subgroup be

$$
( \bar { X } _ { \mu } , \bar { X } _ { v } ) f = \sum _ { \sigma = 1 } ^ { m } c _ { \mu v \sigma } \bar { X } _ { \sigma } f \qquad ( \mu , \nu = 1 , 2 , . . . , m ) ,
$$

and suppose that $\overline { { X } } _ { q + 1 } f , . . . , \overline { { X } } _ { m } f$ $( 0 < q < m )$ generate an invariant subgroup composed of other transformations besides the identity transformation. Then $\bar { X } _ { q + 1 } f , . . . , \bar { X } _ { m } f , X _ { m + 1 } f , . . . , X _ { r } f$ generate an $( r - q )$ - parameter invariant subgroup $G _ { \tau - q }$ containing the $( r - m )$ parameter invariant subgroup $G _ { r - m }$ .Hence, in order that the $\pmb { m }$ parameter group $G _ { m }$ ,homomorphic to the given $\pmb { r }$ -parameter group $G _ { \tau }$ $( r > m )$ ， shall be simple,it is necessary and sufficient that it be possible to obtain the structure of $G _ { r }$ so that the invariant subgroup of the highest order $r \mathrm { ~ - ~ } m$ corresponds to the identity transformation.

We can derive from the structure of the $\boldsymbol { r }$ -parameter group the structure of a sequence_of simple groups by choosing the subgroups ${ \overline { { G } } } _ { 1 } , { \overline { { G } } } _ { 2 } , \dots$ of $G _ { r }$ so that $\overline { { G } } _ { 1 }$ is invariant in $G _ { r }$ but is not contained in any larger invariant subgroup of $\overline { { G } } _ { k }$ ,and so that $\overline { { G } } _ { k + 1 }$ is invariant in $\overline { { G } } _ { k }$ but is not contained in any larger invariant subgroup of $\overleftarrow { G } _ { k }$ . Then $\overline { { G } } _ { 1 }$ gives the structure of the simple group that is isomorphic to $G _ { r } ,$ and $\overline { { G } } _ { 2 }$ gives the structure of the simple group that is isomorphic to $\overline { { G } } _ { 1 }$ , and so on. The numbers of the essential parameters of such simple groups in this sequence are $\boldsymbol { r } - \boldsymbol { r } _ { 1 } , \boldsymbol { r } _ { 1 } - \boldsymbol { r } _ { 2 } , \boldsymbol { r } _ { 2 } - \boldsymbol { r } _ { 3 } , \ldots ,$ respectively. These are called the indices of the simple groups; since each index is smaller than the preceding one, the process will end at the stage where $\overline { { G } } _ { m }$ ,for example, consists of only the identity transformation and $\overline { { G } } _ { m - 1 }$ is simple. Thus we get $\pmb { m }$ such subgroups and the number of the essential parameters is $\boldsymbol { \mathscr { r } } .$

This sequence of $G _ { \tau } , \overline { { G } } _ { 1 } , \overline { { G } } _ { 2 } , . . . , \overline { { G } } _ { m }$ of the group $G _ { r }$ is called the composition series (Kompositionsreihe). The sequence of invariant subgroups only is called the normal series (Normalreihe). If no other subgroup can be inserted between any two of the subgroups in a sequence, that is, if no refinement (Verfeinerung) can be made, then the sequence is a composition series.Two normal series are isomorphic and can be refined. This is Schreier's theorem (Schreier,1926,1927).Since $\overline { { G } } _ { 1 }$ is not necessarily the only subgroup in $G _ { r }$ with the same property, there may be more than one composition series - not necessarily of the same property.

Let the differences between the essential parameters of $G _ { r }$ and those of the largest subgroup containing $\overline { { G } } _ { 1 }$ (but contained in $G _ { \tau }$ be $l _ { 1 }$ . In general, let the differences between the essential parameters of $\overrightharpoon { G } _ { k - 1 }$ and those of the largest subgroup containing $\widehat { G } _ { k }$ but contained in $\widehat { G } _ { k - 1 }$ be $l _ { k }$ The numbers $l _ { 1 } , l _ { 2 } , \ldots$ are the same (if we disregard the order) for any composition series $G _ { r } , { \overrightarrow { G } } _ { 1 } , { \overrightarrow { G } } _ { 2 } , \dots .$ This is Lie-Jordan's theorem.

Let $G _ { m }$ be the invariant subgroup of $G _ { \tau }$ ， $m < r ,$ and its basicinfinitesimal transformations be $X _ { 1 } f _ { 3 } \ldots , X _ { m } f = 0 \quad$ . In general,

$$
( \tau = 1 , 2 , . . . , m ; \lambda = 1 , 2 , . . . , r ; \kappa = m + 1 , . . . , r ) .
$$

Substituting in (176), we obtain

$$
\begin{array} { c } { { c _ { \rho \sigma \lambda } c _ { \lambda \mu \tau } + c _ { \sigma \mu \lambda } c _ { \lambda \rho \tau } + c _ { \mu \rho \lambda } c _ { \lambda \sigma \tau } = 0 } } \\ { { { } } } \\ { { ( \rho , \sigma , \tau , \lambda , \mu = m + 1 , . . . , r ) } } \end{array}
$$

Hence,there exists the group $H _ { r - m }$ of these structure constants, according to Lie's third fundamental theorem. Since these groups are.all isomorphic, each of them is called the factor group of $G _ { r }$ determined by the invariant subgroup $G _ { m }$ ,and is written $F _ { r - m } = G _ { r } / G _ { m }$ .The subgroup $G _ { m }$ is called the normal divisor (Normalteiler).Let its basic infinitesimal transformations be $Y _ { m + 1 } f , \dots . , Y _ { r } f ,$ and add $Y _ { 1 } f = 0 , . . . , Y _ { m } f = 0$ to them； then the groups $G _ { r }$ and $F _ { \tau - m }$ are homomorphic and $G _ { m }$ corresponds to the identity transformation in $F _ { \neg m }$ . Conversely,if $F _ { r - m }$ is homomorphic to $G _ { \tau } ,$ then $F _ { \tau - m }$ is the factor group of $G _ { \tau }$ determined by the invariant subgroup $G _ { r }$ that corresponds to the identity transformation in $F _ { \neg m }$ If $F _ { s }$ is the largest invariant subgroup of $F _ { r - m }$ ,then and only then is the corresponding invariant subgroup $G _ { s + m }$ of $G _ { r }$ the largest. If $F _ { \tau - m }$ is simple, then and only then is $G _ { m }$ the largest invariant subgroup. Hence, the necessary and suffcient condition for the factor group $G _ { r } / G _ { m }$ to be simple is that $G _ { m }$ should be the largest invariant subgroup of $G _ { r }$ If $F _ { \tau - m }$ is not simple, then it contains the composition series

$$
F _ { r - m } , F _ { r - m _ { 1 } } , . . . , F _ { r - m _ { q } } ;
$$

the corresponding subgroups $G _ { r - m _ { 1 } + m } , \ldots , G _ { r - m _ { 1 } + m _ { q } } , G _ { m } , 1$ constitute the composition series. Hence the invariant subgroup of $G _ { r }$ is one of the composition series of $G _ { r }$

Suppose that $G _ { r }$ contains the composition series $G _ { r } , \bar { G } _ { 1 } , \bar { G } _ { 2 } , . . . , \bar { G } _ { \rho } , 1 ,$ and that the order of $\overline { { G } } _ { \alpha }$ is $r _ { \alpha }$ .As has just been seen, $F _ { \alpha } = \overline { { G } } _ { \alpha } / \overline { { G } } _ { \alpha + 1 }$ is simple. Thus there are simple factor groups $F _ { \tau - \tau _ { 1 } } , F _ { \tau _ { 1 } - \tau _ { 2 } } , . . . , F _ { \tau _ { \rho } }$ that are, respectively， of order $r - r _ { 1 } , r _ { 1 } - r _ { 2 } , . . . , r _ { \rho - 1 } - r _ { \rho } , r _ { \rho }$ Let the second composition series of $G _ { r }$ be $G _ { r } , \vec { G } _ { 1 } ^ { \prime } , \vec { G } _ { 2 } ^ { \prime } , . . . , \vec { G } _ { \rho } ^ { \prime } , 1$ ；then the numbers of elements are equal for these two composition series, according to Lie-Jordan's theorem. Hence,if $H _ { m }$ is the common subgroup of $\overline { { G } } _ { 1 }$ and $\overline { { G } } _ { 1 } ^ { \prime }$ , it must be the largest invariant subgroup of the two groups. If the orders of the subgroups in the second composition series are $\pmb { r } _ { 1 } ^ { \prime } , \ldots , \pmb { r } _ { \rho } ^ { \prime }$ ，then $r = r _ { 1 } + r _ { 1 } ^ { \prime } - m$ . Suppose that the basic infinitesimal transformations of $G _ { r }$ are so chosen that the basic infinitesimal transformations of $H _ { m }$ are $X _ { 1 } f _ { 3 } \ldots , X _ { m } f .$ Then $X _ { 1 } f , . . . , X _ { m } f , X _ { m + 1 } f , . . . , X _ { r _ { 3 } } f$ are the base of $\overline { { G } } _ { 1 }$ ; and $X _ { 1 } f , . . . , X _ { m } f , X _ { r _ { 1 } + 1 } f , . . . , X _ { r } f$ are the base of $\overline { { G } } _ { 1 } ^ { \prime }$ .The infinitesimal transformations

$$
X _ { 1 } f , . . . , X _ { m } f , X _ { m + 1 } f , . . . , X _ { r _ { 1 } } f , X _ { r _ { 1 } + 1 } f , . . . , X _ { r } f
$$

are the base of $G _ { r }$ .However, the three indices of the structure constants of the factor group $G _ { r } / \overline { G } _ { 1 }$ are $m + 1 , . . . , r ,$ as has just been seen, and those of the factor group $\overline { { G } } _ { 1 } / H _ { m }$ are $m + 1 , . . . , r _ { 1 }$ The factor groups $\overline { { G } } _ { 1 } ^ { \prime } / H _ { m }$ and $G _ { r } / \overline { { G } } _ { 1 } ^ { \prime }$ must have the same structure constants as the former ones.Thus, if $\overline { { G } } _ { 1 }$ and $\overline { { G } } _ { 1 } ^ { \prime }$ are the largest invariant subgroups of $G _ { r }$ with the common invariant subgroup $H _ { m }$ ，then the factor groups $G _ { \tau } / G _ { 1 }$ and $\overline { { G } } _ { 1 } ^ { \prime } / H _ { m }$ are isomorphic,and hence $G _ { r } / \overline { { G } } _ { 1 } ^ { \prime }$ and $\overline { { G } } _ { 1 } / H _ { m }$ are also isomorphic.

It can be shown that,if $G _ { r }$ has two composition series,then the simple factor groups of these two composition series are isomorphic if we disregard the order. Indeed, let $H$ be the largest invariant subgroup of $G _ { \tau } ^ { \prime } , \overline { { G } } _ { 1 } .$ $\overline { { G } } _ { 1 } ^ { \prime }$ ,and let the order be $h$ If $H _ { \mathrm { { ; } } }$ ， $H _ { 1 }$ ， $H _ { 2 } , \ldots$ are a composition series of subgroups of $H _ { ; }$ then $G _ { r } , \bar { G } _ { 1 } , H , H _ { 1 } , . . .$ and $G _ { r } , \ : \overline { { G } } _ { 1 } ^ { \prime } , H , H _ { 1 } , \ldots .$ are two composition series of subgroups of $G _ { r }$ ，These two composition series have the same structure constants, as just stated. Next,

$$
G _ { r } , { \bar { G } _ { 1 } } , { \bar { G } _ { 2 } } , \ldots \qquad \mathrm { a n d } \qquad G _ { r } , { \bar { G } _ { 1 } } , H , H _ { 1 } , \ldots .
$$

give_the same series of simple groups. Similarly, $G _ { \tau } , \overline { { G } } _ { 1 } ^ { \prime } , \overline { { G } } _ { 2 } ^ { \prime } , \dots .$ and $G _ { \tau } , \vec { G } _ { 1 } ^ { \prime } , H , H _ { 1 } , \ldots$ give the same series of simple groups. Hence, the simple factor groups of the two composition series in question are isomorphic as a whole. This is Holder-Engel’s theorem.

The subgroups of an Abelian group are normal divisors. If the structure is written by means of addition, then the group and the subgroups are called modulus,and the factor group the remainder class modulus (Restklassenmodul).

If the group $G _ { \tau }$ consisting of $X _ { 1 } f , . . . , X _ { \tau } f$ is an $\pmb { r }$ parameter group, then $( X _ { \rho } , X _ { \sigma } ) f$ is the invariant subgroup of $G _ { r }$ ，according to Section 4.27. Hence, in the adjoint group $E _ { r }$ consisting of $E _ { 1 } f _ { 3 } \ldots , E _ { r } f _ { 3 }$ the commutator $( E _ { \rho } , E _ { \sigma } ) f$ generates the invariant subgroup of the adjoint group.This invariant subgroup is called the derived group of $G _ { \tau }$ A derived group of $G _ { \tau }$ includes also a derived group of itself. Such a derived group is called the second derived group. In general, the mth derived group can be defined in a similar manner. All derived groups are invariant for $G _ { r }$ . If the first derived group of $G _ { \tau }$ coincides with itself, then all derived groups coincide. In such a case they are said to be perfect in the sense of point-set theory (Section 2.1). The order of the mth derived group cannot be higher than $m - 1$ . Hence, the order of the qth derived group may coincide with that of the $( q + 1 ) \operatorname { t h }$ . Then the qth derived group may or may not consist of only the identity transformation. A group $G _ { \tau }$ whose derived group consists only of the identity transformation is called integrable (auflosbar).A group that has no such derived group is called nonintegrable (unauflosbar).Every subgroup ofan integrable group is integrable.

An $( r - m )$ -parameter group homomorphic to an $\pmb { r }$ -parameter integrable group is itselfintegrable. Hence, the adjoint group of an integrable group is integrable.

The difference between the order of $G _ { r }$ and the order of its derived group is equal to the number of independent invariants of the adjoint group of $G _ { r }$ For,let $\boldsymbol { e } _ { 1 }$ be an invariant of the adjoint group; then

$$
E _ { \mu } e _ { 1 } = \sum _ { \sigma } e _ { \sigma } c _ { \sigma \mu 1 } = 0 .
$$

Conversely,if the derived group is determined by $X _ { m + 1 } f , \ldots , X _ { \tau } f ,$ then $c _ { \sigma \mu \tau } = 0 ( \sigma , \mu = 1 , 2 , . . . , r ; \tau = 1 , 2 , . . . , m )$ , where $e _ { 1 } , e _ { 2 } , \ldots , e _ { m }$ are the invariants of the adjoint group. An integrable group $G _ { \tau }$ of order $r$ contains an invariant subgroup $G _ { \tau - 1 }$ of order $r - 1 ; G _ { \tau - 1 }$ contains an invariant subgroup $G _ { \tau - 2 }$ of order $\ r - 2$ ; in general, an invariant subgroup of order $\boldsymbol { r } \gets \boldsymbol { r } _ { 1 }$ contains an invariant subgroup of order $r - r _ { 1 } - 1 $ 、 Thus the infinitesimal transformations of $G _ { r }$ can be written

$$
( X _ { \sigma } , X _ { \sigma + \rho } ) f = \sum _ { \tau = 1 } ^ { \sigma } c _ { \sigma , \sigma + \rho , \tau } X _ { \tau } f
$$

$$
( \sigma = 1 , 2 , . . . , r ; \rho = 1 , 2 , . . . , r - \sigma ) .
$$

We propose to determine all second-order groups that contain a transformation $\textstyle \sum _ { \tau } e _ { \tau } X _ { \tau } f$ of $G _ { \tau }$ We search for $\lambda$ such that, with two constants $\pmb { \rho }$ and $\pmb { \omega }$ , we may form

$$
\left( \sum _ { \tau } e _ { \tau } X _ { \tau } , \sum _ { \sigma } \lambda _ { \sigma } X _ { \sigma } \right) f = \rho \sum _ { \tau } e _ { \tau } X _ { \tau } f + \omega \sum _ { \sigma } \lambda _ { \sigma } X _ { \sigma } f .
$$

Let $X _ { 1 } f$ be the required group, then, for $\lambda _ { 1 } = 0$ ，

$$
\Big ( X _ { 1 } , \sum _ { \sigma = 2 } ^ { \tau } \lambda _ { \sigma } X _ { \sigma } \Big ) f = \sum _ { \sigma , \tau } \lambda _ { \sigma } c _ { 1 \sigma \tau } X _ { \tau } f = \rho X _ { 1 } f + \omega \sum _ { \tau = 2 } ^ { \tau } \lambda _ { \sigma } X _ { \sigma } f .
$$

Hence, $\lambda$ can be obtained from

$$
\sum _ { \sigma = 2 } ^ { r } c _ { 1 \sigma \tau } \lambda _ { \sigma } = \omega \lambda _ { \tau } ,
$$

with the roots $\omega$ of $\| c _ { 1 \sigma \tau } - \omega \| = 0$ .Then $\pmb { \rho }$ is given by

$$
\rho = \sum _ { \sigma = 2 } ^ { \tau } \lambda _ { \sigma } c _ { 1 \sigma 1 } .
$$

If $\rho = 0$ in particular, that is, if we search for $\textstyle \sum _ { \sigma } \lambda _ { \sigma } X _ { \sigma }$ invariant for $\textstyle \sum _ { \tau } e _ { \tau } X _ { \tau }$ ，then $\omega$ is determined by the characteristic equation or Killing's equation

$$
\Delta ( \omega ) = \left. \sum _ { \tau = 1 } ^ { r } e _ { \tau } c _ { \tau \lambda \kappa } - \omega \right. = 0 .
$$

Write this equation in the form

$$
( - 1 ) ^ { r } \Delta ( \omega ) = \omega ^ { r } - \psi _ { 1 } ( \epsilon ) \omega ^ { r - 1 } + \psi _ { 2 } ( \epsilon ) \omega ^ { r - 2 } + \cdot \cdot \cdot + ( - 1 ) ^ { r - 1 } \psi _ { r - 1 } ( \epsilon
$$

Then $\psi _ { i } ( e )$ is a homogeneous polynomial of degree $_ i$ in $e _ { 1 } , \ldots , e _ { r } ,$ such that

$$
\begin{array} { c } { { \psi _ { 1 } ( e ) = \displaystyle \sum _ { \sigma , \tau } e _ { \sigma } c _ { \sigma \tau \tau } , } } \\ { { { } } } \\ { { [ \psi _ { 1 } ( e ) ] ^ { 2 } - 2 \psi _ { 2 } ( e ) = \displaystyle \sum _ { \sigma , \tau , \lambda , \mu } e _ { \sigma } e _ { \tau } c _ { \sigma \lambda \mu } c _ { \tau \mu \lambda } , } } \end{array}
$$

If we transform by $\begin{array} { r } { X _ { \rho } ^ { \prime } f = \sum _ { \sigma } h _ { \rho \sigma } X _ { \sigma } f , } \end{array}$ such that $\begin{array} { r } { e _ { \sigma } = \sum _ { \rho } h _ { \rho \sigma } e _ { \rho } ^ { \prime } , } \end{array}$ $\begin{array} { r } { ( X _ { \rho } ^ { \prime } , X _ { \sigma } ^ { \prime } ) f = \sum _ { \tau } c _ { \rho \sigma \tau } ^ { \prime } X _ { \tau } ^ { \prime } f } \end{array}$ then we obtain

$$
\sum _ { \lambda } h _ { \lambda \sigma } c _ { \rho \tau \lambda } ^ { \prime } = \sum _ { \mu , \nu } h _ { \rho \mu } h _ { \tau \nu } c _ { \mu \nu \sigma } \qquad ( \sigma , \rho , \tau = 1 , 2 , . . . , r ) .
$$

If the new characteristic equation is $\Delta ^ { \prime } ( \omega )$ , then

$$
\Delta ( \omega ) = \Delta ^ { \prime } ( \omega ) ,
$$

and

$$
\psi _ { i } ^ { \prime } ( e _ { 1 } , . . . , e _ { r } ) = \psi _ { i } \Bigl ( \sum _ { \rho } h _ { \rho 1 } e _ { 1 } ^ { \prime } , . . . , \sum _ { \rho } h _ { \rho r } e _ { r } ^ { \prime } \Bigr )
$$

It can be shown by the third fundamental theorem of Lie that $\psi _ { 1 } ( e ) , . . . , \psi _ { r - 1 } ( e )$ are invariant for the transformations of the adjoint group.

Killing calld the number of independent functions among

$$
\psi _ { 1 } ( e ) , . . . , \psi _ { r - 1 } ( e )
$$

the rank of $G _ { \tau }$ .The function $\psi _ { i } ( e )$ is the sum of the principal minor determinants of order $r - i$ of the characteristic equation for $\omega = 0$ ,that is,of $\Delta ( 0 )$ . Hence the rank is either equal to or lower than the order of the principal minor of $\Delta ( 0 )$ . It equals at most the number of roots that are identically zero of the characteristic equation. The nonzero roots of the characteristic equation, as functions of $e _ { 1 } , \ldots , e _ { r } $ ，are the invariants of the adjoint group; and the number of the nonzero roots that are functionally independent is the rank of the group, because the number of independent functions $\psi _ { i }$ equals the number of independent nonzero roots.

The root of $\psi _ { 1 } ( e )$ of the characteristic equation is always zero. Since the Poisson bracket of (20l） is of the form $( S , T ) = S T - T S$ and the traces of $s \tau$ and $\boldsymbol { \tau } s$ are equal, the trace of $S T - T S$ is zero.A trace (spur) is the sum of the elements in the principal diagonal of a matrix. Corresponding to a root $\omega ^ { ( \alpha ) }$ of $\omega$ there is always a root $- \omega ^ { ( \alpha ) }$ . For the zero roots of $\omega$ the group $X _ { 1 } f , . . . , X _ { r } f$ is Abelian.

The characteristic equation of an integrable group derived from (200) is written

$$
\Delta ( \omega ) = - \omega \prod _ { \sigma = 1 } ^ { \tau - 1 } \Big ( \sum _ { \tau = 1 } ^ { \tau } e _ { \tau } c _ { \tau \sigma \sigma } - \omega \Big ) ,
$$

and is decomposed into linear factors in $\omega$ ， $e _ { 1 } , \ldots , e _ { \tau }$ . Since $\textstyle \sum _ { \sigma } e _ { \sigma } C _ { \sigma i i }$ $( i = 1 , 2 , . . . , r )$ are the invariants of the adjoint group, they are equal to zero for a derived group. Hence the rank of the derived group of an integrable group is zero.If the rank of a derived group is zero, then and only then is the original group integrable.

Cartan (l933) proved that the necessary and sufficient condition for $G _ { r }$ to be integrable is that the transformations of the derived group make identically zero the coefficient $\psi _ { 2 } ( e )$ of the characteristic equation.The group is integrable if

$$
\sum _ { \tau } c _ { \rho \sigma \tau } { \frac { \partial { \psi } _ { 2 } } { \partial e _ { \tau } } } \equiv 0 \qquad ( \rho , \sigma = 1 , 2 , . . . , r ) .
$$

In other words, the necessary and suffcient condition for integrability is derived by substituting this relationship in the equation for $\psi _ { 2 }$ ,that is,

$$
\sum _ { \lambda , \mu , \nu } c _ { \rho \lambda \mu } c _ { \sigma \mu \nu } c _ { \tau \nu \lambda } = \sum _ { \lambda , \mu , \nu } c _ { \rho \mu \lambda } c _ { \sigma \nu \mu } c _ { \tau \lambda \nu } \qquad ( \rho , \sigma , \tau = 1 , 2 , . . . , r ) .
$$

A group that contains no invariant subgroup other than the identical element is called semisimple. Since a simple group contains no invariant subgroup, it is also semisimple.

In order that a group be semisimple,it is necessary and sufficient that $\partial \psi _ { 2 } / \partial e _ { \tau } = 0$ shall always be associated with $e _ { 1 } = \cdot \cdot \cdot = e _ { r } = 0 $ Hence, $\psi _ { 2 } ( e )$ should be written as the sum of $r$ squares. For a group to be semisimple, it is therefore necessary and suffcient that the discriminant of the quadratic term $\psi _ { 2 } ( e )$ shall not be zero; in other words, the rank of the determinant formed by the coefficients of $e _ { 1 } , \ldots , e _ { r }$ of the quadratic form must be $\boldsymbol { r }$

Suppose that a semisimple group $G _ { r }$ contains an invariant subgroup $g _ { m }$ . Since $\psi _ { 2 }$ is a function of $e _ { 1 } , \ldots , e _ { m }$ only, the equations

$$
{ \frac { \partial \psi _ { 2 } } { \partial e _ { 1 } } } = \cdot \cdot \cdot = { \frac { \partial \psi _ { 2 } } { \partial e _ { m } } } = 0
$$

determine another invariant subgroup $g ^ { \prime }$ or $G _ { r }$ The transformations common to $g _ { m }$ and $g ^ { \prime }$ determine the integrable invariant subgroup of $G _ { r }$ · Hence $e _ { 1 } = \cdot \cdot \cdot = e _ { r } = 0$ should be derived from

$$
{ \frac { \partial \psi _ { 2 } } { \partial e _ { 1 } } } = \cdot \cdot \cdot = { \frac { \partial \psi _ { 2 } } { \partial e _ { m } } } = e _ { m + 1 } = \cdot \cdot \cdot = e _ { r } = 0 .
$$

Thus $\partial \psi _ { 2 } / \partial e _ { 1 } , . . . , \partial \psi _ { 2 } / \partial e _ { m }$ are independent of $e _ { 1 } , \ldots , e _ { m }$ . The subgroup $g ^ { \prime }$ contains $r \mathrm { ~ - ~ } m$ infinitesimal transformations $X _ { m + 1 } ^ { \prime } f _ { 3 } \ldots , X _ { r } ^ { \prime } f .$ Hence it is written in the form

$$
\psi _ { 2 } = \phi ( e _ { 1 } ^ { \prime } , . . . , e _ { m } ^ { \prime } ) + \chi ( e _ { m + 1 } ^ { \prime } , . . . , e _ { r } ^ { \prime } ) ,
$$

where $\phi$ and $\chi$ are both quadratic forms with nonzero discriminants. Thus the two invariant subgroups $g _ { m }$ and $g ^ { \prime }$ are semisimple, and the order of $g ^ { \prime }$ is $r \mathrm { ~ - ~ } m$

Let $( e _ { 1 } , \ldots , e _ { r } )$ be a vector defined in a vector space, and consider the matrix of the coeffcients of the quadratic form

$$
\psi _ { 2 } ( e ) = - { \textstyle { \frac { 1 } { 2 } } } \sum _ { \sigma , \tau , \lambda , \mu } e _ { \sigma } e _ { \tau } c _ { \sigma \lambda \mu } c _ { \tau \mu \lambda } = \sum _ { \sigma , \tau } g _ { \sigma \tau } e _ { \sigma } e _ { \tau } ,
$$

$$
g _ { \sigma \tau } = - { \scriptstyle { \frac { 1 } { 2 } } } \sum _ { \lambda , \mu } c _ { \sigma \lambda \mu } c _ { \tau \mu \lambda } ,
$$

as the matrix of the linear transformation

$$
e _ { \rho } ^ { \prime } = \sum _ { \sigma } g _ { \rho \sigma } e _ { \sigma }
$$

from a vector $( e _ { 1 } , \ldots , e _ { r } )$ to a vector $( e _ { 1 } ^ { \prime } , \ldots , e _ { r } ^ { \prime } )$ . The vector $( e _ { 1 } , \ldots , e _ { r } )$ corresponding to a root of the characteristic equation defines the root space (Wur zelraum) $E _ { r }$ .A group is integrable if and only if the quadratic form $\psi _ { 2 } ( e )$ is zero for each vector $( e _ { 1 } , \ldots , e _ { r } )$ defining the derived group. If an arbitrary vector of a group becomes zero for a root of the character-istic equation, then the group is integrable. If two vectors $( e _ { 1 } , \ldots , e _ { r } )$ and $( \epsilon _ { 1 } , \ldots , \epsilon _ { r } )$ are such that $\begin{array} { r } { \sum _ { \sigma , \tau } g _ { \sigma \tau } e _ { \sigma } \epsilon _ { \tau } = 0 } \end{array}$ for each pair of $\pmb { \sigma }$ and $\boldsymbol { \tau } ,$ then they are said to be orthogonal. For a simple group,any vector of the root space $E _ { 0 }$ corresponding to a zero root of the characteristic equation is orthogonal to any vector of the other root space $E _ { \alpha }$ . In fact,

$$
\sum _ { \sigma , \tau } g _ { \sigma \tau } e _ { \sigma } ^ { ( \alpha ) } \epsilon _ { \tau } = 0
$$

for an $e ^ { ( \alpha ) }$ corresponding to the root space $E _ { \alpha }$ .Denote the vector in $E _ { 0 }$ by $e _ { 1 } ^ { ( 0 ) } , \ldots , e _ { r } ^ { ( 0 ) }$ ； then $\textstyle \sum _ { \sigma } e _ { \sigma } ^ { ( 0 ) } X _ { \sigma } f$ constitute a subgroup of $G _ { r }$ Denote it by $U .$ .It can be shown that such a subgroup $U$ is Abelian for a semisimple group（Cartan，1914,1914a,1926,1927,1927a,1927b,1929, 1930,1933,1936,1938；Weyl，1924,1925,1926,1926a,1931，1939; Chevalley, 1946,1951).

# 4.29REPRESENTATION OF A LIE GROUP

A finite and continuous abstract group is called a Lie group if in a sufficiently small neighborhood of the unit element we can find the coordinates or the parameters $a _ { 1 } , \ldots , a _ { r }$ such that-with the given $A$ with parameters $a _ { i }$ as coordinates,and $B$ with parameters $b _ { i }$ as coordinates - the product $C = A B$ with parameters $c _ { 1 } , \ldots , c _ { r }$ is of class $C ^ { 2 }$ and is represented by $c _ { i } = \phi _ { i } ( a , b )$ . The operations on the parameter group are generated by the linearly independent infinitesimal transformations $X _ { 1 } f , \ldots , X _ { r } f .$ They are such that $\begin{array} { r } { ( X _ { i } , X _ { j } ) = \sum _ { s } c _ { i j s } X _ { s } . } \end{array}$ and $c _ { i j s }$ satisfy the requirements of the third fundamental theorem ofLie.In the place of $X$ we take the notation $T$ and regard it as a vector in an $r -$ dimensional space. An arbitrary vector of this space is written $\begin{array} { r } { \bar { A } = \sum _ { \mu } \alpha ^ { \mu } T _ { \mu } . } \end{array}$ ，where the coefficients $\alpha ^ { \mu }$ are the components of vector $\bar { A }$ From $\bar { A }$ we can construct a ring,which by definition does not obey the permutation law. In contrast to a group, we call it the infinitesimal ring of the group.A ring is an aggregate of elements obeying two operations 一 addition and multiplication - ·where the commutative law is not obeyed for multiplication.The distributive law is obeyed by both a ring and a group, that is,if $\begin{array} { r } { \bar { A } = \sum _ { \mu } \alpha ^ { \mu } T _ { \mu } } \end{array}$ ， $\begin{array} { r } { \bar { B } = \sum _ { \mu } \beta ^ { \mu } T _ { \mu } } \end{array}$ ，then $( { \bar { A } } { \bar { B } } ) = { \bar { A } } \times { \bar { B } }$ and

$$
A \ \times \ B = \sum _ { \kappa , \lambda } \alpha ^ { \kappa } \beta ^ { \lambda } ( T _ { \kappa } , T _ { \lambda } ) = \sum _ { \kappa , \lambda , \mu } \alpha ^ { \kappa } \beta ^ { \lambda } c _ { \kappa \lambda \mu } T _ { \mu } ,
$$

$$
A + B = \sum _ { \mu } \alpha ^ { \mu } T _ { \mu } + \sum _ { \mu } \beta ^ { \mu } T _ { \mu } = \sum _ { \mu } \left( \alpha ^ { \mu } + \beta ^ { \mu } \right) T _ { \mu } = \bar { B } + \bar { A } ,
$$

and

$$
\begin{array} { c l l } { { ( \bar { A } + \bar { B } ) \times \bar { C } = \bar { A } \times \bar { C } + \bar { B } \times \bar { C } , } } & { { } } \\ { { \bar { A } \times ( \bar { B } + \bar { C } ) = \bar { A } \times \bar { B } + \bar { A } \times \bar { C } , } } & { { } } \\ { { ( k \bar { A } ) \times \bar { B } = k ( \bar { A } \times \bar { B } ) = \bar { A } \times ( k \bar { B } ) } } & { { } } \end{array}
$$

with a scalar $k$ .Such operations are called linear (Van der Waerden, 1955, 1959;Weyl, 1926,1931, 1939).We have $\bar { A } \times \bar { B } = - \bar { B } \times \bar { A }$ instead of the commutative law, and

$$
( { \bar { A } } \times { \bar { B } } ) \times { \bar { C } } + ( { \bar { B } } \times { \bar { C } } ) \times { \bar { A } } + ( { \bar { C } } \times { \bar { A } } ) \times { \bar { B } } \equiv 0
$$

(let us call this the combination law) instead of the associative law.This is determined by the third fundamental theorem of Lie. If these rules are satisfied, then Lie's $\boldsymbol { r }$ -parameter group, that is, an $_ r$ -parameter group germ,belongs to it. By Schreier's (1926,1927) definition: if the (new) combination law in the neighborhood of the unit element $\boldsymbol { \mathit { I } }$ of a local continuous group can be applied only for the elements sufficiently near the center $I _ { s }$ then this part of space is called the group germ (Gruppenkeim). This combination law is the same as the law for the continuous group. The algebra in which this combination law obeys Lie's fundamental theorems, that is, the Lie algebra-an algebra in which we restrict ourselves only in such infinitesimal elements of a group—is called the kernel $( K e r n )$ .Such an algebra determines the group germ on the kernel of such infinitesimal elements.

With a group or a ring, or an algebra $\mathfrak { A }$ in general, it is possible to associate a linear transformation $( a ) \colon x \to x ^ { \prime } = a x$ that operates on the varying element $_ x$ of the algebra $\mathfrak { A }$ (Schur,1905,1906,1924;Frobenius and Schur,1906; Brauer and Schur,1930; von Neumann,1929; Van der Waerden,1933,1935； Chevalley,1951). Then an algebra $\mathfrak { A }$ of order $_ n$ can be regarded either as an aggregate of element $^ a$ corresponding to the transformation $( a )$ , or as an $\pmb { n }$ -dimensional vector space $\Omega$ in which this transformation operates. This homomorphic correspondence $( { \mathfrak { A } } ) : a \to ( a )$ defines the representation (Darstellung); that is, to any element $s$ of a group there corresponds its image $U ( s )$ in the $\pmb { n } \cdot$ -dimensional vector space $\Re = \Re _ { n }$ ，such that $U ( s ) U ( t ) = U ( s t )$ . In this case, $s$ is said to induce (induzieren) the mapping image or simply the image $U ( s )$ in the representation space $\Re$ .If it is isomorphic instead of homomorphic,then it is the true representation (Murnaghan, l938). Introducing the coordinates in $\Re$ we represent the mapping $U ( s )$ bya matrix with a nonzero determinant consisting of $_ { n }$ rows and $\pmb { n }$ columns. If $\pmb { A }$ is a transformation of this coordinate to another, then the image of the mapping $U ( s )$ is represented by the matrix $A U ( s ) A ^ { - 1 }$

Let the correspondence $s \to U ( s )$ be the representation $\mathfrak { H }$ ； then the correspondence $s  A U ( s ) A ^ { - 1 }$ is also the representation $\mathfrak { S }$ .These two representations are equiualent, only the coordinate system being differently chosen. The trace, that is, the sum of the elements on the principal diagonal of the matrix $\pmb { A }$ , is not affected by the choice of such coordinates.

In the representation $\mathfrak { s }$ of a group $G$ ,the trace $\chi ( s )$ of the image $U ( s )$ corresponding to the element $\pmb { \mathscr { s } }$ of the group depends on ${ \mathfrak { s } } .$ . This is called the character of the representation (Littlewood， 1940). Equivalent representations have the same character. Since

$$
U ( t s t ^ { - 1 } ) = U ( t ) U ( s ) U ( t ^ { - 1 } ) = U ( t ) U ( s ) U ^ { - 1 } ( t ) ,
$$

the images $U ( s )$ and $U ( t s t ^ { - 1 } )$ differ by position but have the same trace, i.e., $\chi ( t s t ^ { - 1 } ) = \chi ( s )$

A representation,if transitive, is called irreducible； otherwise,reducible. A reducible representation may sometimes be regarded as the sum of ireducible representations. The matrix of an irreducible representation does not possess any invariant submatrix.

Let $U _ { \mu }$ correspond uniquely to Lie's infinitesimal transformation $T _ { \mu }$ ， and $\textstyle \sum _ { \mu } \alpha ^ { \mu } U _ { \mu }$ to $\textstyle \sum _ { \mu } \alpha ^ { \mu } T _ { \mu }$ .Put $\begin{array} { r } { \sum _ { \mu } \alpha ^ { \mu } U _ { \mu } = A _ { ; } } \end{array}$ ， $\begin{array} { r } { \sum _ { \mu } \beta ^ { \mu } U _ { \mu } = B } \end{array}$ ; then

$$
( A B ) = \sum _ { \kappa , \lambda , \mu } \alpha ^ { \kappa } \beta ^ { \lambda } c _ { \kappa \lambda \mu } U _ { \mu } , \sum _ { \mu } \delta ^ { \mu } U _ { \mu } = A \times B ,
$$

$$
\delta ^ { \mu } = \sum _ { \kappa , \lambda } \alpha ^ { \kappa } \beta ^ { \lambda } c _ { \kappa \lambda \mu } .
$$

Thus， to $\textstyle \sum _ { \mu } \delta ^ { \mu } U _ { \mu }$ corresponds the parameter group $\sum _ { \mu } \delta ^ { \mu } T _ { \mu }$ Thecoefficients $\delta ^ { \mu }$ are the same for both.To

$$
\bar { A } = \sum _ { \mu } \alpha ^ { \mu } T _ { \mu } , \qquad \bar { B } = \sum _ { \mu } \beta ^ { \mu } T _ { \mu }
$$

corresponds the transformation $( { \bar { A } } { \bar { B } } ) \to ( A B )$ .Thus to each vector $\bar { A }$ of an infinitesimal ring corresponds uniquely the infinitesimal transformation $\pmb { A }$ ，such that $( { \bar { A } } { \bar { B } } )$ corresponds to $( A B )$ ，and $\textstyle \sum _ { \mu } \delta ^ { \mu } U _ { \mu }$ to $\textstyle \sum _ { \mu } \delta ^ { \mu } T _ { \mu }$ . If the representation of an infinitesimal ring by means of linear infinitesimal transformations

$$
A ^ { i } = \sum _ { j } a _ { j } ^ { i } x ^ { j } , \qquad B ^ { i } = \sum _ { j } b _ { j } ^ { i } x ^ { j } ,
$$

is such that the matrix $\pmb { A }$ corresponds to $\bar { A }$ and the operator

$$
( A B ) = A B - B A
$$

corresponds to $\begin{array} { r } { \bar { A } \times \bar { B } _ { \mathrm { i } } } \end{array}$ , then the representation of Lie groups by means of linear infinitesimal transformations becomes a problem in algebra.

An adjoint group can be the representation of the original group by means of $C _ { \kappa } = ( c _ { \kappa \lambda } ^ { \mu } )$ ， where the structure constants ${ c _ { \kappa \lambda \mu } }$ of the previous

sections are now denoted by $c _ { \kappa \lambda } ^ { \mu }$ . In order to see this possibility, let $\begin{array} { r } { \displaystyle { \vec { B } } = \sum _ { \mu } \beta ^ { \mu } T _ { \mu } } \end{array}$ be an element of the infinitesimal ring. Then

$$
C _ { \kappa } \bar { B } = \sum _ { \kappa , \mu } c _ { \kappa \lambda } ^ { \mu } \beta ^ { \lambda } T _ { \mu } = T _ { \kappa } \times \bar { B } ;
$$

and this means that the linear transformation $C _ { \kappa }$ generates $T _ { \kappa }$ . Similarly, for an arbitrary infinitesimal transformation $\begin{array} { r } { A _ { a } = \sum _ { \kappa } \alpha ^ { \kappa } C _ { \kappa } } \end{array}$ of an adjoint group,

$$
\mathop { \overline { { A } } _ { a } \bar { B } } = \sum _ { \kappa } \alpha ^ { \kappa } C _ { \kappa } \bar { B } = \sum _ { \kappa } \alpha ^ { \kappa } ( T _ { \kappa } \times \bar { B } ) = \bar { A } \times \bar { B } .
$$

Thus a linear transformation $A _ { \mathfrak { a } }$ means multiplication by $\vec { A } .$ It is known from the correspondence $\bar { A } \times \bar { B }  A _ { a } B _ { a } - B _ { a } A _ { a }$ that $A _ { a }$ of the adjoint group gives the representation of $\bar { A }$ ，By applying this to an arbitrary vector $\bar { C }$ we obtain

$$
\begin{array} { c } { { ( \bar { A } \times \bar { B } ) \times \bar { C } = ( A _ { a } B _ { a } - B _ { a } A _ { a } ) \bar { C } = A _ { a } ( B _ { a } \bar { C } ) - B _ { a } ( A _ { a } \bar { C } ) } } \\ { { = \bar { A } \times ( \bar { B } \times \bar { C } ) - \bar { B } \times ( \bar { A } \times \bar { C } ) . } } \end{array}
$$

Thus the third fundamental theorem of Lie is obtained.

For a linear infinitesimal transformation, consider a linear operato1

$$
e ^ { \tau A } = I + \tau A + \cdot \cdot \cdot
$$

as in Section 4.26,and write $A = \left( \alpha _ { \mu \nu } \right)$ .Let $\begin{array} { r } { S ( A ) = \sum _ { \tau } \alpha _ { \tau \tau } } \end{array}$ be the trace of $A$ ; then $\left| e ^ { A } \right| = e ^ { S ( A ) }$ For if $e ^ { \tau A }$ is the group of the matrix, then $\left| e ^ { \tau A } \right|$ is the representation of this group. Since $| e ^ { \tau A } | = 1 + \tau S ( A ) + \cdot \cdot \cdot ,$ then, from Lie's fundamental theorem,

$$
\left( \frac { \partial } { \partial \tau } \left| e ^ { \tau A } \right| \right) _ { 0 } = \left( \frac { \partial } { \partial \tau } e ^ { \tau S ( A ) } \right) _ { 0 } = S ( A ) e ^ { \tau S ( A ) } = S ( A ) .
$$

Hence $S ( A )$ is the infinitesimal transformation of the representation of the group $\left\{ e ^ { \tau A } \right\}$ by means of a determinant. By this property we can judge whether or not the group is Abelian by looking at the infinitesimal ring of that group; if it is Abelian, then we obtain $B e ^ { A \tau } B ^ { - 1 } = e ^ { A \tau }$ from an arbitrary element $\boldsymbol { B }$ of the group and an arbitrary infinitesimal transformation $A$ ； in other words, $e ^ { \ b { A } \tau }$ is invariant for an arbitrary transformation of the adjoint group. It is necessary and sufficient that $\pmb { A }$ be invariant for an arbitrary transformation of the adjoint group; that is, $A$ must be such that $C B - B C = C \times B = 0$ 、This should be satisfied for any two elements ofthe infinitesimal ring,in order that the corresponding group may be Abelian.

Another interesting application is to the composition series. By taking $G _ { i + 1 }$ as the largest normal divisor of $G _ { i }$ ， we construct Lie's subgroups $G , G _ { 1 } , G _ { 2 } , . . . , G _ { r } = I$ with a finite number of steps.If we transfer this subgroup to the infinitesimal ring belonging to $G$ ，the composition series is $g , g _ { 1 } , g _ { 2 } , . . . , g _ { \tau } = 0$ . The element $g _ { i + 1 }$ is the largest ideal among $g _ { i }$ Generally it is known that to a normal divisor in group theory there corresponds an ideal in ring theory; that is,a subset $_ m$ of a ring is called an ideal, if $a - b \in m$ follows from $a \in m$ ， $b \in m$ ； and if $a r \in m$ follows from $a \in m$ with an element $r$ of the ring (Krull, 1935). Such an ideal is called a right-hand ideal. If $r a \in m$ ， then it is a left-hand ideal.Here $a \in m$ means that $^ a$ is an element contained in $m$ . Thus, if the factor group $G _ { i } / G _ { i + 1 } = \overline { { G } } _ { i }$ is Abelian, then $G$ is also Abelian. Conversely, if a group contains a composition series such that all its factor groups $\overline { { G } } _ { i }$ are Abelian, then the group $G$ is said to be integrable (Section 4.28). The symbol $a b a ^ { - 1 } b ^ { - 1 }$ formed from $a \in G$ $b \in G$ represents the commutator of $^ a$ and $b$ . The expression $\begin{array} { r } { c = \prod _ { r = 1 } ^ { k } \left( a _ { r } b _ { r } a _ { r } ^ { - 1 } b _ { r } ^ { - 1 } \right) } \end{array}$ represents an element of the commutator group and is written $G ^ { ( 1 ) }$ . The commutator group $G ^ { ( 1 ) }$ is a normal divisor of $G$ and is indeed the smallest normal divisor with Abelian factor group $G / G ^ { ( 1 ) }$ . It can be shown that the commutator ring $g ^ { ( 1 ) } = g \times g$ of the infinitesimal ring $g$ of $G$ is the smallest ideal with an Abelian remainder class ring in $g$ The group $G$ is integrable if and only if $g ^ { ( s ) } = 0$ for a finite $s$ ,such that $g \supset g ^ { ( 1 ) } \supset g ^ { ( 2 ) } \supset \cdot \cdot \cdot \supset g ^ { ( s ) } = 0$

The largest integrable ideal r in an infinitesimal ring $g$ is called semisimple if $\mathbf { r } = 0$ .The corresponding group is called semisimple (Section 4.28).The largest integrable ideal is called the radical,as in the theory of hypercomplex numbers.Suppose that multiplication obeys the associative law in the hypercomplex number system $\Omega$ ，such that $( u v ) w = u ( v w )$ If a positive integer $\rho$ can be found such as ${ \mathfrak { a } } ^ { \rho } = 0$ for an ideal $\alpha$ ,then $\mathfrak { a }$ is called a null-power ideal in $\Omega$ A null-power ideal in $\Omega$ corresponds to an integrable ideal in $\mathfrak { g }$ .Thus,the largest null-power ideal is a radical r. For $\mathfrak { r } = 0$ this system $\Omega$ is semisimple.

A group $G$ having no normal divisor except itself and the unit element has been called simple (Section 4.28). Similarly,a ring $g$ ,or an ideal $\mathfrak { g }$ or the system Ω, is called simple if it contains no ideal other than itself and zero.A semisimple infinitesimal ring can be expressed as the sum of simple rings that vanish by pairs in multiplication.

Suppose that an integrable group $G$ is represented by a linear transformation in a vector space.It can be proved that there exists a vector $\mathbf { e }$ such that $A \mathbf { e } = \lambda \mathbf { e }$ for all $A$ of the infinitesimal ring of the representation $\mathfrak { G }$ of $G$ (Lie's theorem).

Consider the representation of a semisimple group. Suppose that the representation of an infinitesimal ring $g$ by means of a matrix $A$ is given. Addition of a quantity written as $\beta ( g )$ is called modification.How should $\beta ( g )$ be chosen in order that $\{ A \}$ and $\{ A + I \beta ( g ) \}$ shall both be representations of $g !$ The necessary and sufficient condition is that the quantity $\beta ( g )$ be a representation of the ring by means of numbers. It can be shown that the representation $\mathfrak { G }$ by means of a matrix $A$ can be replaced by its representation by means of another matrix whose traces are all null.Then the theorem can be proved: that the irreducible representation $\mathfrak { G }$ of $g$ is the representation of the factor group $g / h$ and that a null matrix can be made to correspond to each element of $h$

Let $t = \sum _ { i } \mu _ { i } g _ { i }$ be a general element of $g$ ,and let

$$
\begin{array} { r } { \mathsf { f } \left( t , \zeta \right) = | I \zeta - T | . } \end{array}
$$

Let $\boldsymbol { \mathfrak { f } } ^ { * } ( t , \zeta )$ be the result of dropping multiple factors from $\mathfrak { f }$ for a general value of $t .$ Assume that $\tilde { \uparrow } ^ { * }$ contains no double root for $t = t _ { 0 }$ .Let $\mathfrak { r } _ { \alpha }$ be a partial space of the root space for the root $\pmb { \alpha }$ . We say that the element $r = r ^ { ( \alpha ) }$ of $\mathfrak { r } _ { \alpha }$ belongs to the root $\pmb { \alpha }$ ,that is, $( T _ { 0 } - \alpha ) ^ { l } r = 0$ for a sufficiently large l. It can be shown that,if $\mathfrak { r } _ { \alpha }$ belongs to the root $\pmb { \alpha }$ ,and $\mathfrak { r } _ { \beta }$ to the root $\beta$ ,then $\mathfrak { r } _ { \alpha } \times \mathfrak { r } _ { \beta }$ belongs to the root $\alpha + \beta$

Suppose that $\pmb { \alpha }$ and $- \alpha$ are the roots. If $\Lambda = \Lambda ( h )$ is a linear form in $\lambda$ ， then $\Lambda _ { \alpha } ( h _ { \alpha } )$ is written $\Lambda _ { \alpha }$ . Suppose further that the representation of $g$ is given in the correspondence $h  H _ { : }$ ， $e _ { \alpha } \to I _ { \alpha }$ .Then there exists an $\epsilon \neq 0$ such that $H e = \Lambda e$ ; we say that $\pmb { \ell }$ is of weight $\Lambda$ .The only possible weights are the roots of the characteristic equation. If $r$ is of weight $\Lambda$ ,then so is $H r$ and $I _ { \alpha }$ is of weight $\Lambda + \alpha$ If a vector of weight $\Lambda$ is the sum of vectors of other weights, that vector equals zero. If $e _ { \alpha } ^ { * } \subset H _ { \alpha }$ ， $h _ { \alpha } ^ { * } = e _ { \alpha } \times e _ { - \alpha }$ and $\Lambda$ is a representation root, that is, $\Lambda ( h _ { \alpha } ^ { * } ) = \Lambda _ { \alpha } ^ { * }$ ，then $\Lambda _ { \alpha } ^ { * }$ isa rational multiple of $e _ { \alpha } ^ { \ast }$ ·

The ring $g$ is integrable if the equation $\phi ( t ) = 0$ holds for all elements of $g \ \times \ g .$ where $\phi ( t )$ appears in Cartan's criterion for integrability and semisimplicity of $g$ If $g$ is semisimple, then the quadratic form $\phi ( t )$ is nondegenerate. Thus we obtain the important decomposition theorem that any semisimple group is the direct sum of simple groups.

Weyl (1925,1926,1926a) and Peter and Weyl (1927) discussed the representation. Instead of leading by linear transformations to quadratic forms,they led the representation by unitary transformations to Hermitian forms (cf.,Van der Waerden, 1933; Littlewood, 1940). Cartan (1914, 1914a,1926,1927,1927a,1927b,1929,1938) considered the unitary geometry in the Riemannian space instead of referring to the notion of representation.

# REFERENCES

Arnold, V. I. 1963Usp. Mat. Nauk,18,91; trans.by U.S.Department of Commerce as Small Denominator and Problems of Movement in Classical and Celestial Mechanics,1964.

1902 Proc.London Math.Soc.,35,347,379;34,91,332,335. 1904Ibid.,ser.2,2, 293.   
Bianchi, L. 1918 Teoria dei Gruppi Continui Finiti di Trasformazioni, Zanichell, Bologna.   
Birkhoff, G. D. 1927 Dynamical Systems, Am. Math. Soc., New York.   
Born, M. 1926 Z.Physik, 40,167.   
Born,M.,and Fock,V. 1928 Z.Physik,51,165.   
Brauer, R., and Schur, I. 1930 Sitzber. preuss.Akad.Wiss.Berlin,1930,209.   
Buhl, A. 1928 Apersu Moderne sur la Theorie des Groupes Finis et Continus, Gauthier-Villars, Paris.   
Burgers, J. M. 1917 Phil. Mag.,33, 514.   
Campbell, J. E. 1903’Theory ofContinuous Groups, Clarendon Press Oxford.   
Caratheodory, C. 1933 Variationsrechnung, Teubner,Leipzig.   
Cartan, E. 1914 J. Math. pures appl., ser. 6,10,149. 1914a Ann.Ecole Norm. Sup.,ser. 3, 31,263. 1922 Legons sur les Invariants Intégraux,Hermann, Paris. 1926 Bull.Soc.Math.France,54,214. 1927 Ibid., 55, 114. 1927a Ann. Ecole Norm. Sup.,ser.3,44,345. 1927b Annali Mat. pura appl.,ser.4,4, 209. 1929 J. Math. pures appl., ser. 9, 8, 1. 1930 La Theorie des Groupes Finis et Continus et l'Analyse Situs,Gauthier-Villars, Paris. 1931 Bull.Soc.Math.France,59,88. 1933 Sur la Structure des Groupes de Transformations Finies et Continues, Gauthier-Villars,Paris. 1936 La Topologie des Groupes de Lie,Hermann,Paris. 1937 Theorie des Groupes Finis et Continus et la Geometrie Diferentielle, Gauthier-Villars,Paris. 1938 J. Math. pures appl., ser. 9, 17,1. 1945 Les Systemes Diferentielles Exterieurs et leurs Applications Geometriques, Hermann,Paris.   
Cerf, G. 1929 Transformations de Contact et Probleme de Pfaff, Gauthier-Villars, Paris.   
Chevalley, C. 1946 Theory of Lie Groups,I,Princeton University Press,Princeton,N.J. 1951 Théorie des Groupes de Lie,II, Hermann, Paris.   
de Donder, Th. 1901 Rendiconti Cir.Mat. Palermo,15,65. 1902 Ibid.,16,155. 1927Theorie des Invariants Integraux, Gauthier-Villars, Paris.   
Dragoni, G. S. 1934Rendiconti Cir. Mat.Palermo, 58, 311. 1934a Annali Mat. pura appl.,ser.4,13, 335.   
Ehrenfest, P. 1917Phil. Mag.,33,500.   
Eisenhardt, L.P. 1933 Continuous Groups of Transformations, Am. Math. Soc., New York.   
Engel, F. 1916 Nachr.Gottingen Ges. Wiss, 1916, 270. 1917 Ibid.,1917, 189.   
Engel, F. and Faber,K. 1932 Lie'scheTheorie der partiellen Differentialgleichung erster Ordnung, Teubner, Leipzig.   
Fermi, E.and Persico, F. 1926 Rendiconti Accad. Lincei, ser.6,4, 452.   
Frobenius, G.and Schur,I. 1906_Sitzber. preuss. Akad. Wiss. Berlin, 1906,209.   
Geppert, H. 1928 Rendiconti Accad.Lincei,13,30,191,294. 1929 Math.Annalen,102,193.   
Goursat, H. 1908 J. Math. pures appl., ser. 6, 4, 331. 1912 Lecons sur le Probleme de Pfaf,Gauthier-Villars, Paris. 1915 J. Math. pures appl., ser.7,1, 241. 1921 Legons sur l'Intégration des Equations Differentielles aux Dérivees Partielles du Premier Ordre,Gauthier-Villars,Paris. 1923 Compt.Rend.,147,1089.   
Graf, D. 1936 Annali Mat. pura appl., ser. 4, 15,87.   
Grobner, W. 1960 Die Lie-Reihen und ihre Anwendungen, Deutsch. Verlag, Berlin.   
Hagihara, Y. 1938 Compt.Rend.,207,390. 1940 Festschrift Elis Stromgren,58, Munksgaard, Copenhagen. 1944 Proc.Imp.Acad.Japan,20,501.   
Hille,E. 1948 Functional Analysis and Semi-Groups， Am. Math. Soc., New York.   
Hoheisel, G. 1928Partielle Diferentialglechung, Gruyter, Leipzig.   
Jacobi, C. G. J. 1842 Astron. Nachr., 20, 81.Reprinted in Jacobi'sche Gesammete Werke, IV,295,Reimer,Berlin,1886. 1844 J.reine angew.Math.,26,115.Reprinted in Jacobi'sche Gesammette Werke,IV,295,Reimer,Berlin,1886.   
Λasuga, 1. 1961 Proc. Japan Acad.,37,366,372,377.   
Knapp, H. 1964Monatshefte Math., 68,33.   
Kneser, H. 1924 Math. Annalen, 91,155.   
Koopman, B. O. 1931 Proc. Natl. Acad. Sci. U.S., 17,315.   
Kowalewski, G. 1931 Einfuhrung in die Theorie der Continuierlichen Gruppen, Akad. Verlagsgesell.,Leipzig.   
Krull, W. 1935Idealtheorie,Springer,Berlin.   
Kulsrud, R. M. 1957Phys.Rev.,106,205.   
Lenard, A. 1959 Ann. Phys. (N.Y.),6,261.   
Levi-Civita, T. 1928 Abhandl. Math. Sem.Hamburg,5,323. 1928a Ati Congresso Internaz. Mat. Bologna, new series, 3. 1934J.Math.Phys.M.I.T.,13,18.   
Lie,S. 1874 Math.Annalen,8,215. 1888 Theorie der Transformationsgruppen,I, Teubner,Leipig. 1890 Ibid., I1. 1891 Vorlesungen iber Differentialgleichungen mit bekannten infinitesimalen Transformationen, Teubner, Leipzig. 1893 Theorie der Transformationsgruppen, III. 1893a Kontinuierliche Gruppen,Teubner,Leipzig. 1896 Geometrieder eruhrungstransformation,Teuber,Lig. 1927 Gesammelte Abhandlung,VI,248,Teubner,Leig. 1929Ibid., VI,1.   
Littlewood, D. E. 1940 The Theory of Group Characters and Matrix Representations of Group, Clarendon Press, Oxford.   
Mattioli, G. 1932Annali Mat. pura appl.,ser. 4, 10, 283.   
Mineur, H. 1937J.Ecole Polytech., Paris, ser.3,cahier 143.   
Murnaghan, F. D. 1938The Theory of Group Representations, Johns Hopkins Press, Baltimore,Md.   
Peter,F.,and Weyl, H. 1927 Math. Annalen, 97, 737.   
Pfeiffer, G. 1933 Acta Math.,61,203, 239.   
Poincaré, H. 1889Acta Math.,13,1.Reprinted in Oeures de Henri Poincaré,VI1,286, Gauthier-Villars, Paris,1952. in Oeuures,VII, 500. 1899 Methodes Nouveles de la Mecanique Celeste, III, Gauthier-Villars, Paris. 1905 Lecons de la Mecanique Celeste, I， Chap. 2, Gauthier-Villars, Paris.   
Pontrjagin, L. 1939 Topological Groups,Princeton University Press,Princeton, N.J.   
Potron, M. 1936 Les Groupes de Lie, Gauthier-Villars, Paris.   
Saltykov, N. 1934 Etudes sur l'évolution des méthodes modernes d'integration des équations differentielles partielles du premier ordre， Mem. Acad. Roy. Belgique, ser.2, 13, fasc. 6. 1935 Methodes modernes d'intégration des équations aux dérivees partielles du premier ordre d une fonction inconnue, Gauthier-Villars, Paris. 1950 Bull.Astron., ser.2,15,293.   
Schmeidler, F. 1963 Astron. Nachr., 287,1.   
Schouten, J. A., and van der Kulk, W. 1949Pfaff's Problem and its Generalizations, Clarendon Press, Oxford.   
Schreier, O. 1926 Abhandl. Math. Sem. Hamburg,4, 15. 1927 Ibid.,5, 161, 233.   
Schur, I. 1891 Math.Annalen,38, 263. 1905 Sit zber. preuss.Akad.Wiss.,1905, 406. 1906 Ibid.,1906,164. 1924 Ibid.,1924,189,297,346.   
Sommerfeld, A. 1922Atombau und Spektralinien, 3rd ed., Vieweg, Brunswick.   
Speiser, A. 1923Theorie der Gruppen endlicher Ordnung, Springer, Berlin.   
Stone, M. H. 1932 Linear Transformations in Hilbert Space and their Applications to Analysis, Am.Math.Soc., New York.   
Van der Waerden, B.L. 1933 Math. Z., 36, 780; 37, 446. 1935 Gruppen von linearen Transformation, Springer, Berlin. 1955 Der Modernen Algebra, I, 4th ed., Springer, Berlin. 1959 Ibid.,II,4th ed., Springer,Berlin.   
van Kampen, E.R., and Wintner, A. 1938 Trans. Am. Math. Soc., 44, 168.   
Vivanti, L. 1918 Teoria dei Gruppi Continui Finiti di Trasformazioni, Zanichelli, Bologna.   
von Neumann, J. 1929 Math. Z.,30,3. 1932 Annals of Math., 33, 587.   
Weber, H. v. 1900Vorlesungen iber das Pfaffsche Problem, Teubner,Leipzig.   
Weyl, H. 1923 Raum,Zeit,Materie, Springer,Berlin. 1923a Mathematische Analyse des Raumproblems, Springer, Berlin. 1924 Sitzber.preuss. Akad.Wiss. Berlin,1924,338. 1925 Math. Z., 23,271. 1926 Ibid.,24,328, 377. 1926a Acta Math.,48,255. 1931 Gruppentheorie und Quantenmechanik, 2nd rev. ed.， S. Hirzel, Leipzig. 1939 The Classical Groups, Princeton University Press, Princeton, N.J.   
Williamson, J. 1940 Am.J. Math., 62, 881.

# Reduction of the n-Body Problem

# TRANSFORMATION OF LAGRANGE

# 5.ITRANSFORMATION OF LAGRANGE

Consider at first the three-body problem and let the positions of the three bodies be $P _ { 1 } , P _ { 2 } , P _ { 3 }$ and their masses be $m _ { 1 } , m _ { 2 } , m _ { 3 }$ Denote the coordinates of $P _ { 3 }$ relative to $P _ { 2 }$ ，the coordinates of $P _ { 1 }$ relative to $P _ { 3 }$ ,and the coordinates of $P _ { 2 }$ relative to $P _ { 1 }$ ,by $x _ { 1 } , x _ { 2 } , x _ { 3 } ; x _ { 4 } , x _ { 5 } , x _ { 6 }$ ; and ${ \pmb x } , _ { 7 } { \pmb x } _ { 8 } , { \pmb x } _ { 9 } ,$ respectively.The differential equations of motion of the three bodies in these relative coordinates are

$$
\frac { d ^ { 2 } x _ { 3 \mu - 2 } } { d t ^ { 2 } } + \left( \sum _ { \mu = 1 } ^ { 3 } m _ { \mu } \right) \frac { x _ { 3 \mu - 2 } } { r _ { \mu } ^ { 3 } } - m _ { \mu } \Bigl ( \sum _ { \mu = 1 } ^ { 3 } \frac { x _ { 3 \mu - 2 } } { r _ { \mu } ^ { 3 } } \Bigr ) = 0 ,
$$

$$
\frac { d ^ { 2 } x _ { 3 \mu - 1 } } { d t ^ { 2 } } + \left( \sum _ { \mu = 1 } ^ { 3 } m _ { \mu } \right) \frac { x _ { 3 \mu - 1 } } { r _ { \mu } ^ { 3 } } - m _ { \mu } \Bigl ( \sum _ { \mu = 1 } ^ { 3 } \frac { x _ { 3 \mu - 1 } } { r _ { \mu } ^ { 3 } } \Bigr ) = 0 ,
$$

$$
\frac { d ^ { 2 } x _ { 3 \mu } } { d t ^ { 2 } } + \biggl ( \sum _ { \mu = 1 } ^ { 3 } m _ { \mu } \biggr ) \frac { x _ { 3 \mu } } { r _ { \mu } ^ { 3 } } - m _ { \mu } \biggl ( \sum _ { \mu = 1 } ^ { 3 } \frac { x _ { 3 \mu } } { r _ { \mu } ^ { 3 } } \biggr ) = 0 \qquad ( \mu = 1 , 2 , 3 ) .
$$

$$
r _ { \mu } ^ { 2 } = x _ { 3 \mu - 2 } ^ { 2 } + x _ { 3 \mu - 1 } ^ { 2 } + x _ { 3 \mu } ^ { 2 } ,
$$

$$
\sum _ { \mu = 1 } ^ { 3 } x _ { 3 \mu - 2 } = \sum _ { \mu = 1 } ^ { 3 } x _ { 3 \mu - 1 } = \sum _ { \mu = 1 } ^ { 3 } x _ { 3 \mu } = 0 .
$$

If the motions of $P _ { 2 }$ and $P _ { 3 }$ relative to $\boldsymbol { P } _ { 1 }$ are known, then the motion of $P _ { 1 }$ relative to $P _ { 2 }$ can be obtained, since the equations (l） have been written in an explicit form.

Let the constants of the angular momentum be $c _ { 1 } , c _ { 2 } , c _ { 3 }$ ; then

$$
\begin{array} { l } { { \displaystyle \sum _ { \mu = 1 } ^ { 3 } \frac { 1 } { m _ { \mu } } \left( x _ { 3 \mu - 1 } \frac { d x _ { 3 \mu } } { d t } - x _ { 3 \mu } \frac { d x _ { 3 \mu - 1 } } { d t } \right) = c _ { 1 } } , } \\ { { \displaystyle \sum _ { \mu = 1 } ^ { 3 } \frac { 1 } { m _ { \mu } } \left( x _ { 3 \mu } \frac { d x _ { 3 \mu - 2 } } { d t } - x _ { 3 \mu - 2 } \frac { d x _ { 3 \mu } } { d t } \right) = c _ { 2 } } , } \end{array}
$$

$$
\sum _ { \mu = 1 } ^ { 3 } \frac { 1 } { m _ { \mu } } \left( x _ { 3 \mu - 2 } \frac { d x _ { 3 \mu - 1 } } { d t } - x _ { 3 \mu - 1 } \frac { d x _ { 3 \mu - 2 } } { d t } \right) = c _ { 3 } .
$$

Write

$$
u _ { \mu } ^ { 2 } = \left( \frac { d x _ { 3 \mu - 2 } } { d t } \right) ^ { 2 } + \left( \frac { d x _ { 3 \mu - 1 } } { d t } \right) ^ { 2 } + \left( \frac { d x _ { 3 \mu } } { d t } \right) ^ { 2 } \qquad ( \mu = 1 , 2 , 3 ) ;
$$

then, if $\scriptstyle h$ is an arbitrary constant, the energy integral is

$$
\sum _ { \mu = 1 } ^ { 3 } \frac { u _ { \mu } ^ { 2 } } { m _ { \mu } } - 2 \Bigl ( \sum _ { \mu = 1 } ^ { 3 } m _ { \mu } \Bigr ) \Bigl ( \sum _ { \mu = 1 } ^ { 3 } \frac { 1 } { m _ { \mu } r _ { \mu } } \Bigr ) = h .
$$

Referring to (3) we see that the solution requires six unknown functions and their derivatives.These functions are determined by six differential equations of order two.However, since four integrals from (5） and (6) are known,eight unknown quantities are required; that is,the order of the differential equations of the three-body problem is reduced to eight from $3 \times 6 = 1 8$

Lagrange tried to determine the sides of the triangle formed by the three bodies and then the position in space of the plane of the triangle (Lagrange, 1873； Tisserand, 1889；Radau, 1868, 1886; Bohlin,1907). Let $\lambda , \mu , \nu$ be a cyclic permutation of 1, 2, 3, and put

$$
{ \frac { 1 } { r _ { \mu } ^ { 3 } } } - { \frac { 1 } { r _ { \nu } ^ { 3 } } } = q _ { \lambda } \qquad ( \lambda , \mu , \nu = 1 , 2 , 3 ) ;
$$

then

$$
q _ { 1 } + q _ { 2 } + q _ { 3 } = 0 , \frac { q _ { 1 } } { r _ { 1 } ^ { 3 } } + \frac { q _ { 2 } } { r _ { 2 } ^ { 3 } } + \frac { q _ { 3 } } { r _ { 3 } ^ { 3 } } = 0 .
$$

Furthermore, by writing

we obtain

$$
- \phi _ { \lambda } = x _ { 3 \mu - 2 } x _ { 3 \nu - 2 } + x _ { 3 \mu - 1 } x _ { 3 \nu - 1 } + x _ { 3 \mu } x _ { 3 \nu }
$$

$$
\begin{array} { r } { \ p _ { \mu } + \ p _ { \nu } = r _ { \lambda } ^ { 2 } } \end{array}
$$

$$
\begin{array} { r } { \dot { p } _ { \lambda } = \frac { 1 } { 2 } ( r _ { \mu } ^ { 2 } + r _ { v } ^ { 2 } - r _ { \lambda } ^ { 2 } ) , } \end{array}
$$

from (2) and (3). Differentiate (2) twice with respect to $t$ ,then,because of (5)，

$$
\frac { 1 } { 2 } \frac { d ^ { 2 } r _ { 1 } ^ { 2 } } { d t ^ { 2 } } = x _ { 1 } \frac { d ^ { 2 } x _ { 1 } } { d t ^ { 2 } } + x _ { 2 } \frac { d ^ { 2 } x _ { 2 } } { d t ^ { 2 } } + x _ { 3 } \frac { d ^ { 2 } x _ { 3 } } { d t ^ { 2 } } + u _ { 1 } ^ { 2 } .
$$

Substituting (1) in the right-hand side and referring to (9) and (10), we obtain

$$
{ \frac { 1 } { 2 } } { \frac { d ^ { 2 } r _ { 1 } ^ { 2 } } { d t ^ { 2 } } } + { \frac { m _ { 1 } + m _ { 2 } + m _ { 3 } } { r _ { 1 } } } - u _ { 1 } ^ { 2 } = m _ { 1 } \Big ( { \frac { \dot { p } _ { 2 } + \dot { p } _ { 3 } } { r _ { 1 } ^ { 3 } } } - { \frac { \dot { p } _ { 2 } } { r _ { 3 } ^ { 3 } } } - { \frac { \dot { p } _ { 3 } } { r _ { 2 } ^ { 3 } } } \Big ) ,
$$

which, by (7), reduces to

$$
\frac { 1 } { 2 } \frac { d ^ { 2 } r _ { \lambda } ^ { 2 } } { d t ^ { 2 } } + \frac { m _ { \lambda } + m _ { \mu } + m _ { \nu } } { r _ { \lambda } } + m _ { \lambda } ( \not p _ { \mu } q _ { \mu } - \not p _ { \nu } q _ { \nu } ) - u _ { \lambda } ^ { 2 } = 0
$$

$$
( \lambda , \mu , \nu = 1 , 2 , 3 ) .
$$

From (12) we can determine $u _ { 1 } ^ { 2 } , u _ { 2 } ^ { 2 } , u _ { 3 } ^ { 2 }$ as functions of $r _ { 1 } , r _ { 2 } , r _ { 3 }$ ; this gives

$$
\sum _ { \lambda = 1 } ^ { 3 } \frac { u _ { \lambda } ^ { 2 } } { m _ { \lambda } } = \sum _ { \lambda = 1 } ^ { 3 } \frac { 1 } { 2 m _ { \lambda } } \frac { d ^ { 2 } r _ { \lambda } ^ { 2 } } { d t ^ { 2 } } + \biggl ( \sum _ { \lambda = 1 } ^ { 3 } m _ { \lambda } \biggr ) \biggl ( \sum _ { \lambda = 1 } ^ { 3 } \frac { 1 } { m _ { \lambda } r _ { \lambda } } \biggr ) ,
$$

or, by (6),

$$
\frac { d ^ { 2 } } { d t ^ { 2 } } \left( \frac { r _ { 1 } ^ { 2 } } { m _ { 1 } } + \frac { r _ { 2 } ^ { 2 } } { m _ { 2 } } + \frac { r _ { 3 } ^ { 2 } } { m _ { 3 } } \right) - ( m _ { 1 } + m _ { 2 } + m _ { 3 } ) \left( \frac { 1 } { m _ { 1 } r _ { 1 } } + \frac { 1 } { m _ { 2 } r _ { 2 } } + \frac { 1 } { m _ { 3 } r _ { 3 } } \right) =
$$

This is one of the fundamental equations of Lagrange.It plays an important role in Sundman's theory of the regularization of the three-body problem, as will be shown in a later chapter. We can write, as can be easily verified,

$$
\begin{array} { c } { { \cdot _ { 3 \mu - 2 } \displaystyle \frac { d x _ { 3 \nu - 2 } } { d t } ~ + ~ x _ { 3 \mu - 1 } \displaystyle \frac { d x _ { 3 \nu - 1 } } { d t } ~ + ~ x _ { 3 \mu } \displaystyle \frac { d x _ { 3 \nu } } { d t } \nonumber } } \\ { { ~ - ~ \left( x _ { 3 \nu - 2 } \displaystyle \frac { d x _ { 3 \mu - 2 } } { d t } ~ + ~ x _ { 3 \nu - 1 } \displaystyle \frac { d x _ { 3 \mu - 1 } } { d t } ~ + ~ x _ { 3 \nu } \displaystyle \frac { d x _ { 3 \mu } } { d t } \right) ~ = ~ } } \\ { { ~ ( \mu , \nu = 1 , 2 , 3 ) , ~ ( \nu , \nu = 1 , 3 , 3 ) } } \end{array}
$$

and, by differentiating (9),

$$
x _ { 3 \mu - 2 } \frac { d x _ { 3 \nu - 2 } } { d t } + x _ { 3 \mu - 1 } \frac { d x _ { 3 \nu - 1 } } { d t } + x _ { 3 \mu } \frac { d x _ { 3 \nu } } { d t } = \frac { 1 } { 2 } \left( - \frac { d { p _ { \lambda } } } { d t } + \rho \right)
$$

$$
( \lambda , \mu , \nu = 1 , 2 , 3 ) .
$$

Denote the cosines of the angles between the vectors

$$
\begin{array} { r l r l } & { \mathfrak { U } \Big ( - \frac { x _ { 4 } } { r _ { 2 } } , - \frac { x _ { 5 } } { r _ { 2 } } , - \frac { x _ { 6 } } { r _ { 2 } } \Big ) , } & & { \mathfrak { V } \Big ( - \frac { 1 } { u _ { 2 } } \frac { d x _ { 4 } } { d t } , - \frac { 1 } { u _ { 2 } } \frac { d x _ { 5 } } { d t } , - \frac { 1 } { u _ { 2 } } \frac { d x _ { 6 } } { d t } \Big ) , } \\ & { \mathfrak { U } ^ { \prime } \Big ( \frac { x _ { 7 } } { r _ { 3 } } , \frac { x _ { 8 } } { r _ { 3 } } , \frac { x _ { 9 } } { r _ { 3 } } \Big ) , } & & { \mathfrak { V } ^ { \prime } \Big ( \frac { 1 } { u _ { 3 } } \frac { d x _ { 7 } } { d t } , \frac { 1 } { u _ { 3 } } \frac { d x _ { 8 } } { d t } , \frac { 1 } { u _ { 3 } } \frac { d x _ { 9 } } { d t } \Big ) , } \end{array}
$$

by

$$
\begin{array} { r l r l r } & { \alpha ^ { \prime } = \frac { \mathfrak { U } \cdot \mathfrak { U ^ { \prime } } } { | \mathfrak { U } | | \mathbf { \mathfrak { U ^ { \prime } } } | } , } & & { \beta ^ { \prime } = \frac { \mathfrak { U } \cdot \mathfrak { V ^ { \prime } } } { | \mathfrak { U } | | \mathbf { \mathfrak { V ^ { \prime } } } | } , } & & { \gamma ^ { \prime } = \frac { \mathfrak { U } \cdot \mathfrak { V } } { | \mathfrak { U } | \mathbf { \mathfrak { V } } | } , } \\ & { \alpha ^ { \prime \prime } = \frac { \mathfrak { V } \cdot \mathfrak { V ^ { \prime } } } { | \mathfrak { V } | \cdot | \mathfrak { V ^ { \prime } } | } , } & & { \beta ^ { \prime \prime } = \frac { \mathfrak { U ^ { \prime } } \cdot \mathfrak { V } } { | \mathfrak { U ^ { \prime } } | \cdot | \mathfrak { V } | } , } & & { \gamma ^ { \prime \prime } = \frac { \mathfrak { U ^ { \prime } } \cdot \mathfrak { V ^ { \prime } } } { | \mathfrak { U ^ { \prime } } | \cdot | \mathfrak { V ^ { \prime } } | } . } \end{array}
$$

Making the substitution

$$
2 v _ { \lambda } = u _ { \mu } ^ { 2 } + u _ { \nu } ^ { 2 } - u _ { \lambda } ^ { 2 } ,
$$

we obtain, after some computation,

$$
\alpha ^ { \prime } = \frac { \phi _ { 1 } } { \sqrt { ( \phi _ { 1 } + \phi _ { 2 } ) ( \phi _ { 1 } + \phi _ { 3 } ) } } , \qquad \beta ^ { \prime } = \frac { d \phi _ { 1 } / d t - \rho } { 2 \sqrt { ( \phi _ { 1 } + \phi _ { 3 } ) ( v _ { 1 } + v _ { 2 } ) } } ,
$$

$$
\gamma ^ { \prime } = \frac { d / p _ { 1 } / d t + d / p _ { 3 } / d t } { 2 \sqrt { ( / p _ { 1 } + / p _ { 3 } ) ( v _ { 1 } + v _ { 3 } ) } } ,
$$

$$
\alpha ^ { \prime \prime } = \frac { v _ { 1 } } { \sqrt { ( v _ { 1 } + v _ { 2 } ) ( v _ { 1 } + v _ { 3 } ) } } , \qquad \beta ^ { \prime \prime } = \frac { d p _ { 1 } / d t + \rho } { 2 \sqrt { ( p _ { 1 } + p _ { 2 } ) ( v _ { 1 } + v _ { 3 } ) } } ,
$$

$$
\gamma ^ { \prime \prime } = \frac { d / p _ { 1 } / d t + d / p _ { 2 } / d t } { 2 \sqrt { ( / p _ { 1 } + / p _ { 2 } ) ( v _ { 1 } + v _ { 2 } ) } } \cdot
$$

These six cosines are not independent. Because $- \gamma ^ { \prime } , \beta ^ { \prime \prime } , \gamma ^ { \prime \prime } , \beta ^ { \prime } , \alpha ^ { \prime } , \alpha ^ { \prime \prime }$ being the cosines of the four sides and the diagonals of the spherical quadrangle formed by the ends of the four vectors $\mathfrak { U } , \mathfrak { U } ^ { \prime } , \mathfrak { V } , \mathfrak { V } ^ { \prime }$ on the spherical surface of unit radius-we have the relation (Tisserand,1889)

$$
\begin{array} { r l } & { - \ \big ( \alpha ^ { \prime 2 } + \beta ^ { \prime 2 } + \gamma ^ { \prime 2 } + \alpha ^ { \prime 2 } + \beta ^ { \prime 2 } + \gamma ^ { \prime 2 } \big ) } \\ & { + \ 2 ( \alpha ^ { \prime } \beta ^ { \prime \prime } \gamma ^ { \prime } + \alpha ^ { \prime } \beta ^ { \prime } \gamma ^ { \prime \prime } + \alpha ^ { \prime \prime } \beta ^ { \prime } \gamma ^ { \prime } + \alpha ^ { \prime } \beta ^ { \prime } \gamma ^ { \prime } + \alpha ^ { \prime \prime } \beta ^ { \prime \prime } \gamma ^ { \prime \prime } ) + \alpha ^ { \prime \prime } \alpha ^ { \prime \prime 2 } + \beta ^ { \prime \prime } \beta ^ { \prime \prime } } \\ & { \qquad + \ \gamma ^ { \prime 2 } \gamma ^ { \prime \prime 2 } - 2 \alpha ^ { \prime } \alpha ^ { \prime \prime } \beta ^ { \prime } \beta ^ { \prime \prime } - 2 \beta ^ { \prime } \beta ^ { \prime \prime } \gamma ^ { \prime } \gamma ^ { \prime \prime } - 2 \gamma ^ { \prime } \gamma ^ { \prime \prime } \alpha ^ { \prime } \alpha ^ { \prime \prime } = 0 . \quad ( 1 } \end{array}
$$

Put

$$
4 \Sigma _ { \lambda } = \rho ^ { 2 } ( { \ p } _ { \mu } + { p } _ { v } ) + 2 \rho \Big ( { p } _ { \mu } \frac { d { p } _ { v } } { d t } - { p } _ { v } \frac { d { p } _ { \mu } } { d t } \Big )
$$

$$
+ \ p _ { \mu } \Big ( \frac { d \phi _ { \nu } } { d t } \Big ) ^ { 2 } + \ p _ { v } \Big ( \frac { d \phi _ { \mu } } { d t } \Big ) ^ { 2 } + \ p _ { \lambda } \Big ( \frac { d \phi _ { \mu } } { d t } + \frac { d \phi _ { v } } { d t } \Big ) ^ { 2 } ;
$$

then

$$
\begin{array} { l } { { ( v _ { 2 } v _ { 3 } + v _ { 3 } v _ { 1 } + v _ { 1 } v _ { 2 } ) ( { p _ { 2 } } \rho _ { 3 } + { p _ { 3 } } \beta _ { 1 } + { p _ { 1 } } \beta _ { 2 } ) - ( v _ { 1 } \Sigma _ { 1 } + v _ { 2 } \Sigma _ { 2 } + v _ { 3 } \Sigma _ { 3 } ) } } \\ { { \mathrm { } } } \\ { { \displaystyle \qquad + \frac { 1 } { 1 6 } \left( \rho ^ { 2 } + \frac { d \beta _ { 2 } } { d t } \frac { d \beta _ { 3 } } { d t } + \frac { d \beta _ { 3 } } { d t } \frac { d \beta _ { 1 } } { d t } + \frac { d \beta _ { 1 } } { d t } \frac { d \beta _ { 2 } } { d t } \right) ^ { 2 } = 0 . } } \end{array}
$$

This is the second fundamental equation of Lagrange. $\Sigma$ of (20) is a quadratic form in $\rho$ containing terms of $\pmb { \rho }$ and $\rho ^ { 2 }$ but no term of $\rho ^ { 3 }$ $\boldsymbol { u }$ can be expressed in terms of $r , \dot { r } ,$ and $\ddot { r }$ from (7),(11), (12); and $\boldsymbol { v }$ can be expressed in terms of $r , \dot { r } ,$ and $\ddot { r }$ from (17). Hence, (21) expresses the auxiliary function $\pmb { \rho }$ in terms of $r _ { 1 } , r _ { 2 } , r _ { 3 } , { \dot { r } } _ { 1 } , { \dot { r } } _ { 2 } , { \dot { r } } _ { 3 } , { \ddot { r } } _ { 1 } , { \ddot { r } } _ { 2 } , { \ddot { r } } _ { 3 } .$

Differentiate (l4) with respect to $t _ { : }$ ,eliminate the derivatives of $_ x$ with respect to $t$ by means of (1),and refer to the definition of $\pmb { \mathscr { p } }$ and $q$ in (7) and (9). Then we obtain

$$
\frac { d \rho } { d t } + m _ { 1 } \hbar _ { 1 } q _ { 1 } + m _ { 2 } \hbar _ { 2 } q _ { 2 } + m _ { 3 } \hbar _ { 3 } q _ { 3 } = 0 .
$$

This expresses $d \rho / d t$ in terms of $r _ { 1 } , r _ { 2 } , r _ { 3 }$

Next put

$$
\begin{array} { r l } { \displaystyle _ { a } = \bigg ( x _ { 3 u - 1 } \frac { d x _ { 3 u } } { d t } - x _ { 3 u } \frac { d x _ { 3 u - 1 } } { d t } \bigg ) ^ { 2 } + \bigg ( x _ { 3 u } \frac { d x _ { 3 u - 2 } } { d t } - x _ { 3 u - 2 } \frac { d x _ { 3 u } } { d t } \bigg ) ^ { 2 } } \\ { + \bigg ( x _ { 3 u - 2 } \frac { d x _ { 3 u - 1 } } { d t } - x _ { 3 u - 1 } \frac { d x _ { 3 u - 2 } } { d t } \bigg ) ^ { 2 } , } \\ { \displaystyle _ { a } = \bigg ( x _ { 3 u + 2 } \frac { d x _ { 3 u + 3 } } { d t } - x _ { 3 u + 3 } \frac { d x _ { 3 u + 2 } } { d t } \bigg ) \bigg ( x _ { 3 u + 5 } \frac { d x _ { 3 u + 6 } } { d t } - x _ { 3 u + 6 } \frac { d x _ { 3 u + 5 } } { d t } \bigg ) } \\ { + \bigg ( x _ { 3 u + 3 } \frac { d x _ { 3 u + 1 } } { d t } - x _ { 3 u + 1 } \frac { d x _ { 3 u + 3 } } { d t } \bigg ) \bigg ( x _ { 3 u + 6 } \frac { d x _ { 3 u + 4 } } { d t } - x _ { 3 u + 4 } \frac { d x _ { 3 u + 4 } } { d t } } \end{array}
$$$$
\begin{array} { r } { \Psi _ { \mu } = \bigg ( x _ { 3 \mu + 2 } \frac { d x _ { 3 \mu + 3 } } { d t } - x _ { 3 \mu + 3 } \frac { d x _ { 3 \mu + 2 } } { d t } \bigg ) \bigg ( x _ { 3 \mu + 5 } \frac { d x _ { 3 \mu + 6 } } { d t } - x _ { 3 \mu + 6 } \frac { d x _ { 3 \mu + 5 } } { d t } \bigg ) } \\ { + \bigg ( x _ { 3 \mu + 3 } \frac { d x _ { 3 \mu + 1 } } { d t } - x _ { 3 \mu + 1 } \frac { d x _ { 3 \mu + 3 } } { d t } \bigg ) \bigg ( x _ { 3 \mu + 6 } \frac { d x _ { 3 \mu + 4 } } { d t } - x _ { 3 \mu + 4 } \frac { d x _ { 3 \mu } } { d t } } \\ { + \bigg ( x _ { 3 \mu + 1 } \frac { d x _ { 3 \mu + 2 } } { d t } - x _ { 3 \mu + 2 } \frac { d x _ { 3 \mu + 1 } } { d t } \bigg ) \bigg ( x _ { 3 \mu + 4 } \frac { d x _ { 3 \mu + 5 } } { d t } - x _ { 3 \mu + 5 } \frac { d x _ { 3 \mu } } { d t } } \end{array}
$$

where $3 \mu + \nu ( \nu = 1 , 2 , 3 , 4 , 5 , 6 ; \mu = 1 , 2 , 3 )$ ， denote integers congruent by mod 9; then we have

$$
\frac { \Pi _ { 1 } } { m _ { 1 } } + \frac { \Pi _ { 2 } } { m _ { 2 } } + \frac { \Pi _ { 3 } } { m _ { 3 } } + \frac { 2 \Psi _ { 1 } } { m _ { 2 } m _ { 3 } } + \frac { 2 \Psi _ { 2 } } { m _ { 3 } m _ { 1 } } + \frac { 2 \Psi _ { 3 } } { m _ { 1 } m _ { 2 } } = k ^ { 2 } = c _ { 1 } ^ { 2 } + c _ { 2 } ^ { 2 } + c _ { 3 } ^ { 2 } .
$$

Equations (23) can be transformed by the identities

$$
\begin{array} { r l } & { - { c B } ) ^ { 2 } + ( c A - a C ) ^ { 2 } + ( a B - b A ) ^ { 2 } } \\ & { \phantom { - } \equiv ( a ^ { 2 } + b ^ { 2 } + c ^ { 2 } ) ( A ^ { 2 } + B ^ { 2 } + C ^ { 2 } ) - ( a A + b B + c C ) ^ { 2 } , } \\ & { - c B ) ( b ^ { \prime } C ^ { \prime } - c ^ { \prime } B ^ { \prime } ) + ( c A - a C ) ( c ^ { \prime } A ^ { \prime } - a ^ { \prime } C ^ { \prime } ) } \\ & { \phantom { - } + ( a B - b A ) ( a ^ { \prime } B ^ { \prime } - b ^ { \prime } A ^ { \prime } ) } \\ & { \equiv ( a a ^ { \prime } + b b ^ { \prime } + c c ^ { \prime } ) ( A A ^ { \prime } + B B ^ { \prime } + C C ^ { \prime } ) } \\ & { \phantom { - } - ( a A ^ { \prime } + b B ^ { \prime } + c C ^ { \prime } ) ( a ^ { \prime } A + b ^ { \prime } B + c ^ { \prime } C ) , } \end{array}
$$

and by (2), (5), (9),and (15) to the form

$$
\Pi _ { \mu } = u _ { \mu } ^ { 2 } r _ { \mu } ^ { 2 } - r _ { \mu } ^ { 2 } \biggl ( { \frac { d r _ { \mu } } { d t } } \biggr ) ^ { 2 } , \qquad \Psi _ { \mu } = \phi _ { \mu } v _ { \mu } + { \frac { 1 } { 4 } } \rho ^ { 2 } - { \frac { 1 } { 4 } } \biggl ( { \frac { d \phi _ { \mu } } { d t } } \biggr ) ^ { 2 } .
$$

From (25)and (24) we obtain

$$
\begin{array} { l } { { \displaystyle \sum _ { \nu = 1 } ^ { 3 } \frac { 1 } { m _ { \nu } ^ { 2 } } \left[ u _ { \nu } ^ { 2 } r _ { \nu } ^ { 2 } - r _ { \nu } ^ { 2 } \Big ( \frac { d r _ { \nu } } { d t } \Big ) ^ { 2 } \right] + \sum _ { \nu = 1 } ^ { 3 } \frac { 2 } { m _ { \nu + 1 } m _ { \nu + 2 } } \left[ \phi _ { \nu } v _ { \nu } - { \frac { 1 } { 4 } } \left( \frac { d \phi _ { \nu } } { d t } \right) ^ { 2 } \right] } } \\ { { + \frac { m _ { 1 } + m _ { 2 } + m _ { 3 } } { 2 m _ { 1 } m _ { 2 } m _ { 3 } } \rho ^ { 2 } = k ^ { 2 } . } } \end{array}
$$

The left-hand members of (25) and (26) are expressed in terms of $r _ { 1 } , r _ { 2 } , r _ { 3 }$ and their first-and second-order derivatives.

Thus we have obtained four fundamental equations (13), (21), (22), and (26). $\not p _ { 1 } , \not p _ { 2 } , \not p _ { 3 } , \ u _ { 1 } ^ { 2 } , \ u _ { 2 } ^ { 2 } , \ u _ { 3 } ^ { 2 } , \ v _ { 1 } , \ v _ { 2 } , \ v _ { 3 } , \Sigma _ { 1 } , \Sigma _ { 2 } , \Sigma _ { 3 }$ are expressed in terms of $r _ { 1 } , r _ { 2 } , r _ { 3 }$ and their derivatives because of (ll), (l2), (l7),and (20). Then(21） expresses $\rho$ in terms of these functions.Hence, the differential equations of the three-body problem reduce to the three equations (l3), (22),and (26) for the three unknown functions $r _ { 1 } , r _ { 2 } , r _ { 3 }$ Equations (13) and (26) are second-order differential equations and contain two constants $h$ and $k$ ； (22）is a third-order differential equation.Hence,the whole set of differential equations is of order 7,and we require 7 integrals. Adding $h$ and $k$ brings the number of integration constants up to nine. Although we can solve this set of differential equations with these constants,we still require three more constants: two representing the position of the plane of the triangle formed by the three bodies,and another representing the orientation of the triangle on that plane.Thus we require twelve integration constants in all, in order to determine the relative motion of the three bodies. There are six more integration constants corresponding to the rectilinear motion in space of the center of mass of the whole system. Thus, by this transformation of Lagrange we can reduce the order of the differential equations from l8 to 7.We can reduce the order still further,as will be explained in this chapter.

Differentiate (5) with respect to $t$ and refer to (1). Then, usig (2), (10),(15),we obtain

$$
\frac { d u _ { \mu } ^ { 2 } } { d t } = 2 ( m _ { 1 } + m _ { 2 } + m _ { 3 } ) \frac { d } { d t } \frac { 1 } { r _ { \mu } } + m _ { \mu } \Big ( q _ { \mu + 2 } \frac { d \phi _ { \mu + 2 } } { d t } - q _ { \mu + 1 } \frac { d \phi _ { \mu + 1 } } { d t } - q _ { \mu }
$$

Integrate (27) with respect to $t$ and substitute $u _ { 1 } ^ { 2 } , u _ { 2 } ^ { 2 } , u _ { 3 } ^ { 2 }$ in (12) and (27); then

$$
\begin{array} { l } { { \displaystyle \frac { { \mathit { l } } ^ { 2 } r _ { \mu } ^ { 2 } } { d t ^ { 2 } } - \frac { m _ { 1 } + m _ { 2 } + m _ { 3 } } { r _ { \mu } } } } \\ { ~ - ~ m _ { \mu } \Big [ \phi _ { \mu + 2 } q _ { \mu + 2 } - \phi _ { \mu + 1 } q _ { \mu + 1 } } \\ { ~ + \int \left( q _ { \mu + 2 } \frac { d \phi _ { \mu + 2 } } { d t } - q _ { \mu + 1 } \frac { d \phi _ { \mu + 1 } } { d t } - q _ { \mu } \rho \right) d t \Big ] ~ { = } ~ 0 . }  \end{array}
$$

The third-order differential equation obtained by differentiating this equation is important to the theory underlying Lindstedt's solution of the three-body problem (Sundman, 1913; Lindstedt, l883, 1884). Lagrange treated the particular solution of the three-body problem that we discussed in Chapter 3 on the basis of this set of seventh-order differential equations.Instead of the mutual distances of the three bodies in the problem of Lagrange, the distances of the three bodies from the center of mass were employed as the unknown functions by Charlier (1899).

# 5.2LEVI-CIVITA's TRANSFORMATION

Now we propose to reduce the order of the differential equations from 18 to 8 by a transformation arising from the geometrical considerations of Levi-Civita (1915；see also Charlier, 1899；Ronchi, 1917).

We take the rectangular coordinates $\xi , \eta$ in the invariable plane of the three bodies $P _ { 1 } , P _ { 2 } , P _ { 3 }$ and the $\zeta$ axis perpendicular to that plane.The center of mass $o$ is taken as the coordinate origin.The instantaneous moving plane containing the three bodies at any instant passes always through the origin $o$ .Denote the positions of the three bodies referred to the moving rectangular coordinates on this instantaneous plane at $t$ by $x _ { 2 v - 1 }$ ， $\scriptstyle x _ { 2 v }$ $( \nu = 1 , 2 , 3 )$ .The $X$ axis is taken to be in the direction of the ascending node of this instantaneous plane on the invariable plane, in such a way that the positive direction is counted from the lower to the upper side of the invariable plane in positive rotation around the $O \xi$ axis. The $z$ axis is normal to this instantaneous moving plane. Let the angular distance of this ascending node from the $O \xi$ axis be $\psi$ ,and the angle between these two planes be $\theta .$ ，Twice the area of the triangle formed by the three bodies is

$$
V = { \left| \begin{array} { l l l } { x _ { 1 } } & { x _ { 2 } } & { 1 } \\ { x _ { 3 } } & { x _ { 4 } } & { 1 } \\ { x _ { 5 } } & { x _ { 6 } } & { 1 } \end{array} \right| } .
$$

Let the masses of the three bodies be $m _ { 2 \nu - 1 } = m _ { 2 v } ( \nu = 1 , 2 , 3 )$ ， $\begin{array} { r } { \sum _ { \nu = 1 } ^ { 3 } m _ { 2 \nu - 1 } = \sum _ { \nu = 1 } ^ { 3 } m _ { 2 \nu } = m _ { 1 } } \end{array}$ ，and the moments and the products of inertia around $O X$ and $O Y ,$ ，respectively,be

$$
I _ { X } = \sum _ { \nu = 1 } ^ { 3 } m _ { 2 \nu } x _ { 2 \nu } ^ { 2 } , \qquad I _ { Y } = \sum _ { \nu = 1 } ^ { 3 } m _ { 2 \nu - 1 } x _ { 2 \nu - 1 } ^ { 2 } ,
$$

$$
{ \cal J } = \sum _ { \nu = 1 } ^ { 3 } m _ { 2 \nu } x _ { 2 \nu - 1 } x _ { 2 \nu } , \qquad { \cal D } = \left| { \cal I } _ { x } \quad { \cal J } \right| ;
$$

then it can be proved that

$$
m _ { 2 } m _ { 4 } m _ { 6 } V ^ { 2 } = m D > 0 .
$$

The direction cosines of the normal to the invariable plane referred to the coordinate system OXYZ defined by the instantaneous moving plane of the three bodies are O,sin $\theta _ { \cdot }$ cos $\theta _ { \cdot }$ 、Hence, the total kinetic energy of the system is

$$
T = \frac { 1 } { 2 } \sum _ { \nu = 1 } ^ { 3 } m _ { 2 \nu - 1 } [ ( \dot { x } _ { 2 \nu - 1 } - \dot { \psi } \cos \theta \cdot x _ { 2 \nu } ) ^ { 2 } + ( \dot { x } _ { 2 \nu } + \dot { \psi } \cos \theta \cdot x _ { 2 \nu - 1 } ) ^ { 2 }
$$

and the force function, assuming the gravitational constant to be unity, is

$$
\begin{array} { r } { U = \frac { m _ { 2 } m _ { 4 } } { r _ { 1 2 } } + \frac { m _ { 4 } m _ { 6 } } { r _ { 2 3 } } + \frac { m _ { 6 } m _ { 2 } } { r _ { 3 1 } } . } \end{array}
$$

The canonically conjugate variables to these variables $x _ { 2 \nu - 1 } , ~ x _ { 2 \nu } , ~ \theta , ~ \psi$ are

$$
y _ { 2 \nu - 1 } = \frac { \partial T } { \partial \dot { x } _ { 2 \nu - 1 } } = m _ { 2 \nu - 1 } ( \dot { x } _ { 2 \nu - 1 } - \dot { \psi } \cos \theta \cdot x _ { 2 \nu } ) ,
$$

$$
y _ { 2 \nu } = \frac { \partial T } { \partial \dot { x } _ { 2 \nu } } = m _ { 2 \nu } ( \dot { x } _ { 2 \nu } + \dot { \psi } \cos \theta \cdot x _ { 2 \nu - 1 } ) ,
$$

$$
\Theta = \frac { \partial T } { \partial \dot { \theta } } = I _ { x } \dot { \theta } - J \dot { \psi } \sin \theta ,
$$

$$
\begin{array} { l } { \displaystyle \Psi = \frac { \partial T } { \partial \dot { \psi } } = \sin \theta ( - J \dot { \theta } + I _ { \tt Y } \dot { \psi } \sin \theta ) + \cos \theta \cdot M , } \\ { \displaystyle } \\ { \displaystyle M = \sum _ { y = 1 } ^ { 3 } \big ( x _ { 2 \nu - 1 } y _ { 2 \nu } - x _ { 2 \nu } y _ { 2 \nu - 1 } \big ) . } \end{array}
$$

Thus,

$$
\begin{array} { l } { { \displaystyle T = \mathbf T + f , } } \\ { { \displaystyle \mathbf T = \frac 1 2 \sum _ { \nu = 1 } ^ { 3 } \frac 1 { m _ { 2 \nu } } ( y _ { 2 \nu - 1 } ^ { 2 } + y _ { 2 \nu } ^ { 2 } ) , } } \end{array}
$$

$$
f = \frac { 1 } { 2 } \sum _ { \nu = 1 } ^ { 3 } m _ { 2 \nu } ( { \dot { \theta } } x _ { 2 \nu } - { \dot { \psi } } \sin { \theta } { \cdot } x _ { 2 \nu - 1 } ) ^ { 2 } .
$$

Substituting $\Theta , \Psi$ ，we obtain the function $f$ in the form

$$
f ( { \dot { \theta } } , { \dot { \psi } } ) = F ( \Theta , \Psi )
$$

and

$$
H = \mathbf { H } + F , \mathbf { H } = \mathbf { T } - \mathbf { \Omega } U .
$$

Thus, the equations of motion are

$$
\begin{array} { r l r l r l } & { \frac { d x _ { 2 \nu - 1 } } { d t } = \frac { \partial H } { \partial y _ { 2 \nu - 1 } } , } & & { \frac { d x _ { 2 \nu } } { d t } = \frac { \partial H } { \partial y _ { 2 \nu } } , } & & { \frac { d \theta } { d t } = \frac { \partial F } { \partial \Theta } , } & & { \frac { d \psi } { d t } = } \\ & { \frac { d y _ { 2 \nu - 1 } } { d t } = - \frac { \partial H } { \partial x _ { 2 \nu - 1 } } , } & & { \frac { d y _ { 2 \nu } } { d t } = - \frac { \partial H } { \partial x _ { 2 \nu } } , } & & { \frac { d \Theta } { d t } = - \frac { \partial F } { \partial \theta } , } & & { \frac { d \Psi } { d t } = } \end{array}
$$

$$
( \nu = 1 , 2 , 3 ) .
$$

The total angular momentum $\mathbf { \delta x }$ has, with respect to OXYZ, components

$$
\begin{array} { r } { I _ { x } \dot { \theta } - J \dot { \psi } \sin \theta , \qquad - J \dot { \theta } + I _ { x } \dot { \psi } \sin \theta , \qquad M , } \end{array}
$$

but, on the other hand, the components are 0, $\kappa$ sin $\theta$ ， $\kappa$ cos $\theta$ ，where $| \mathbf { K } | = K .$ Hence

$$
\begin{array} { r } { I _ { x } \dot { \theta } - J \dot { \psi } \sin \theta = 0 , ~ - J \dot { \theta } + I _ { x } \sin \theta = K \sin \theta , ~ M = K \cos \theta . } \end{array}
$$

Substituting the definition of $\Theta , \Psi$ ， we obtain the integrals of (32):

$$
\Theta = 0 , ~ \Psi = M \cos \theta + K \sin ^ { 2 } \theta = K , ~ M = K \cos \theta .
$$

Consequently,

$$
\frac { \partial F } { \partial \theta } = \frac { \partial F } { \partial \psi } = 0 .
$$

Eliminating $\theta$ and $\psi$ from (33), we obtain

$$
\dot { \psi } = \frac { K } { D } I _ { x } , \qquad \dot { \theta } = \frac { K } { D } J \sin \theta .
$$

Since $F _ { : }$ , and accordingly $f ,$ is a homogeneous quadratic form in ${ \dot { \theta } } , { \dot { \psi } }$ by (31), therefore

$$
F = \frac { 1 } { 2 } \left( \frac { \partial f } { \partial \dot { \theta } } \dot { \theta } + \frac { \partial f } { \partial \dot { \psi } } \dot { \psi } \right) = \frac { 1 } { 2 } [ \Theta \dot { \theta } + ( \Psi - M \cos \theta ) \dot { \psi } ] .
$$

From (34) and (35) we obtain

$$
{ \cal F } = \frac { 1 } { 2 } \frac { K - M \cos \theta } { D } K I _ { x } ,
$$

or, substituting from the third equation of (34),

$$
\frac { 1 } { 2 } \frac { I _ { X } } { D } \left( K ^ { 2 } - M ^ { 2 } \right) = { \bf F } ,
$$

which depends only on $x _ { 2 \nu - 1 } , \ x _ { 2 \nu } , \ y _ { 2 \nu - 1 } , \ y _ { 2 \nu } .$ Hence,the first twelve equations of (32） depend only on these twelve variables, for now $H = \mathbf { H } + \mathbf { F }$ . Thus the problem is reduced to the plane problem in which the inclination $\theta$ is kept constant.

Furthermore, we transform by

$$
\begin{array} { r l r l r l r } { x _ { 2 \nu - 1 } ^ { \prime } = x _ { 2 \nu - 1 } - x _ { 5 } , } & { { } } & { x _ { 2 \nu } ^ { \prime } = x _ { 2 \nu } - x _ { 6 } , } & { { } } & { x _ { 5 } ^ { \prime } = x _ { 5 } , } & { { } } & { x _ { 6 } ^ { \prime } = x _ { 6 } , } \end{array}
$$

$$
y _ { 2 \nu - 1 } ^ { \prime } = y _ { 2 \nu - 1 } , \quad y _ { 2 \nu } ^ { \prime } = y _ { 2 \nu } , \quad y _ { 5 } ^ { \prime } = y _ { 1 } + y _ { 3 } + y _ { 5 } , \quad y _ { 6 } ^ { \prime } = y _ { 2 } + y _ { 4 } + y _ { 5 } ,
$$

The transformation is canonical. $y _ { 5 } ^ { \prime } , \ y _ { 6 } ^ { \prime }$ are the components of the momentum of the center of mass, which we can equate to zero. Then

$$
\frac { \partial H } { \partial x _ { 5 } ^ { \prime } } = \frac { \partial H } { \partial x _ { 6 } ^ { \prime } } = 0 .
$$

Thus $H$ depends neither on ${ \pmb x } _ { 5 } ^ { \prime }$ nor on ${ \pmb x } _ { 6 } ^ { \prime }$ . Hence, the equations of motion are of the eighth order and of the form

$$
\begin{array} { l l } { { \displaystyle { \frac { d x _ { 2 \nu - 1 } ^ { \prime } } { d t } } = \frac { \partial H } { \partial y _ { 2 \nu - 1 } ^ { \prime } } } , } & { { ~ \displaystyle { \frac { d x _ { 2 \nu } ^ { \prime } } { d t } } = \frac { \partial H } { \partial y _ { 2 \nu } ^ { \prime } } , } } \\ { { \displaystyle { \frac { d y _ { 2 \nu - 1 } ^ { \prime } } { d t } } = - \frac { \partial H } { \partial x _ { 2 \nu - 1 } ^ { \prime } } } , } & { { ~ \displaystyle { \frac { d y _ { 2 \nu } ^ { \prime } } { d t } } = - \frac { \partial H } { \partial x _ { 2 \nu } ^ { \prime } } } \quad { } ~ ( \nu = 1 , 2 ) . }  \end{array}
$$

After we solve these equations for $x _ { 2 \nu - 1 } ^ { \prime } , \ x _ { 2 \nu } ^ { \prime } , \ y _ { 2 \nu - 1 } ^ { \prime } , \ y _ { 2 \nu } ^ { \prime } ,$ we compute $x _ { 2 \nu - 1 } , \ x _ { 2 \nu } , \ y _ { 2 \nu - 1 } , \ y _ { 2 \nu }$ by the inverse transformation of (37） and then compute $I _ { x } , I _ { y } , J , D$ Then we obtain $\psi$ and $\theta$ from

$$
\frac { d \psi } { d t } = \frac { K } { D } I _ { x } , \frac { d \theta } { d t } = \frac { K } { D } J \sin \theta .
$$

Instead of the transformation (37) we may use the mutual distances of the three bodies according to Lagrange. Let $\phi _ { v }$ be the inclination to the $X$ axis of the side opposite the vertex $\left( x _ { 2 v - 1 } , x _ { 2 v } \right)$ of the triangle formed by the three bodies； then

$$
\begin{array} { r } { \cos \phi _ { v } = \frac { x _ { 2 \lambda - 1 } - x _ { 2 \mu - 1 } } { \sqrt { ( x _ { 2 \lambda - 1 } - x _ { 2 \mu - 1 } ) ^ { 2 } + ( x _ { 2 \lambda } - x _ { 2 \mu } ) ^ { 2 } } } , } \\ { \sin \phi _ { v } = \frac { x _ { 2 \lambda } - x _ { 2 \mu } } { \sqrt { ( x _ { 2 \lambda - 1 } - x _ { 2 \mu - 1 } ) ^ { 2 } + ( x _ { 2 \lambda } - x _ { 2 \mu } ) ^ { 2 } } } , } \end{array}
$$

where $\lambda , \mu , \nu$ are a cyclic permutation of 1, 2, 3. Put $\theta _ { \nu } = \phi _ { \lambda } - \phi _ { \mu }$ ; then $\begin{array} { r } { \sum _ { v = 1 } ^ { 3 } \theta _ { v } = 0 . } \end{array}$ Introduce

$$
\omega = \frac { 1 } { 3 } \sum _ { \nu = 1 } ^ { 3 } \phi _ { \nu } , \quad r _ { \lambda } \equiv r _ { \mu , \nu } = [ ( x _ { 2 \mu - 1 } - x _ { 2 \nu - 1 } ) ^ { 2 } + ( x _ { 2 \mu } - x _ { 2 \nu } ) ^ { 2 } ] ^ { 1 / 2 } ,
$$

and denote the corresponding momenta by $\Omega , P _ { \lambda } ,$ respectively. Then we can prove that

$$
\begin{array} { c } { { y _ { 2 \lambda - 1 } = P _ { \mu } \cos \phi _ { \mu } - P _ { v } \cos \phi _ { \nu } - \frac { \Omega } { 3 } \Big ( \frac { \sin \phi _ { \mu } } { r _ { \mu } } - \frac { \sin \phi _ { v } } { r _ { v } } \Big ) , } } \\ { { { } } } \\ { { y _ { 2 \lambda } = P _ { \mu } \sin \phi _ { \mu } - P _ { v } \sin \phi _ { \nu } + \frac { \Omega } { 3 } \Big ( \frac { \cos \phi _ { \mu } } { r _ { \mu } } - \frac { \cos \phi _ { v } } { r _ { v } } \Big ) , } } \end{array}
$$

where

$$
\begin{array} { r l } & { \mathfrak { s } \theta _ { \lambda } = \frac { \cdot _ { \lambda } } { 2 r _ { \mu } r _ { \nu } } , \qquad \sin \theta _ { \lambda } = \frac { \mathfrak { r } } { r _ { \mu } r _ { \nu } } , } \\ & { } \\ & { 4 V ^ { 2 } = ( r _ { 1 } + r _ { 2 } + r _ { 3 } ) ( - r _ { 1 } + r _ { 2 } + r _ { 3 } ) ( r _ { 1 } - r _ { 2 } + r _ { 3 } ) ( r _ { 1 } + r _ { 2 } - r _ { 3 } ) , } \\ & { } \\ & { \phi _ { \lambda } = \omega + \frac { 1 } { 3 } ( \theta _ { \mu } - \theta _ { \nu } ) . } \end{array}
$$

Hence

$$
\begin{array} { r } { m x _ { 2 \lambda - 1 } = m _ { v } r _ { \mu } \cos \phi _ { \mu } - m _ { \mu } r _ { v } \cos \phi _ { v } , } \\ { m x _ { 2 \lambda } = m _ { v } r _ { \mu } \sin \phi _ { \mu } - m _ { \mu } r _ { v } \sin \phi _ { v } , } \end{array}
$$

and

$$
{ \frac { 1 } { 2 } } \sum { \frac { 1 } { m _ { \lambda } } } \left( y _ { 2 \nu - 1 } ^ { 2 } + y _ { 2 \nu } ^ { 2 } \right) = { \frac { 1 } { 2 } } \sum { \frac { 1 } { m _ { \lambda } } } \left( P _ { \mu } ^ { 2 } + P _ { \nu } ^ { 2 } - 2 P _ { \mu } P _ { \nu } \cos \theta _ { \lambda } \right)
$$

$$
\begin{array} { l } { { \displaystyle + \frac { \Omega } { 3 } \sum \frac { 1 } { m _ { \lambda } } \left( \frac { P _ { \mu } } { r _ { \nu } } - \frac { P _ { \nu } } { r _ { \mu } } \right) \sin \theta _ { \lambda } } } \\ { { \displaystyle + \frac { \Omega ^ { 2 } } { 1 8 } \sum \frac { 1 } { m _ { \lambda } } \left( \frac { 1 } { r _ { \mu } ^ { 2 } } + \frac { 1 } { r _ { \nu } ^ { 2 } } - \frac { 2 \cos \theta _ { \lambda } } { r _ { \mu } r _ { \nu } } \right) } , } \end{array}
$$

$$
I _ { Y } = \frac { m _ { 2 } m _ { 4 } m _ { 6 } } { m } \sum \frac { 1 } { m _ { \lambda } } r _ { \lambda } ^ { 2 } \sin ^ { 2 } { [ \omega + \textstyle { \frac { 1 } { 3 } } ( \theta _ { v } - \theta _ { \mu } ) ] } .
$$

Then the Hamiltonian function of (32) is

$$
\begin{array} { c } { { H = \displaystyle \frac { 1 } { 2 } \sum \displaystyle \frac { 1 } { m _ { \lambda } } \left( y _ { 2 \lambda - 1 } ^ { 2 } + y _ { 2 \lambda } ^ { 2 } \right) \mathrm { ~ - ~ } \sum \displaystyle \frac { m _ { \mu } m _ { \nu } } { r _ { \lambda } } } } \\ { { \mathrm { ~ } } } \\ { { \mathrm { ~ } + \displaystyle \frac { 1 } { D } \left[ \Theta ^ { 2 } I _ { x . } - 2 \Theta \displaystyle \frac { M \cos \theta - \Psi } { \sin \theta } J + \frac { ( M \cos \theta - \Psi ) ^ { 2 } } { \sin ^ { 2 } \theta } I _ { \mathrm { { r } } } \right] . } } \end{array}
$$

Since the origin is the center of mass,

$$
\begin{array} { l l l } { { \displaystyle { \frac { d r _ { \lambda } } { d t } } = \frac { \partial H } { \partial P _ { \lambda } } , } } & { { ~ \displaystyle { \frac { d \omega } { d t } } = \frac { \partial H } { \partial \Omega } , } } & { { } } \\ { { } } & { { } } & { { } } \\ { { \displaystyle { \frac { d P _ { \lambda } } { d t } } = - \frac { \partial H } { \partial r _ { \lambda } } , \quad { \displaystyle { \frac { d \Omega } { d t } } = - \frac { \partial H } { \partial \omega } } } } & { { ~ ( \lambda = 1 , 2 , 3 ) . } } \end{array}
$$

If we express $H$ in terms of $\omega$ $\phantom { } _ { 0 } , r _ { \lambda } , \Omega , P _ { \lambda }$ by substituting in $\boldsymbol { H }$ from equations (41),(42), (43),and (44),we obtain

$$
\begin{array} { l } { { = \displaystyle \sum _ { \bar { 2 } \overline { { { m _ { \lambda } } } } } ( P _ { \mu } ^ { z } + P _ { \nu } ^ { z } - 2 P _ { \mu } P _ { \nu } \cos \theta _ { \lambda } ) } } \\ { { \mathrm { } } } \\ { { \displaystyle \quad + \frac { \Omega } { 3 } \sum \left( \frac { P _ { \mu } } { r _ { \nu } } - \frac { P _ { \nu } } { r _ { \mu } } \right) \frac { \sin \theta _ { \lambda } } { m _ { \lambda } } + \frac { \Omega ^ { 2 } } { 1 8 } \sum _ { m _ { \lambda } } \left( \frac { 1 } { r _ { \mu } ^ { 2 } } + \frac { 1 } { r _ { \nu } ^ { 2 } } - \frac { 2 \cos \theta _ { \lambda } } { r _ { \mu } r _ { \nu } } \right) } } \\ { { \mathrm { } } } \\ { { \displaystyle \quad + \frac { K ^ { 2 } - \Omega ^ { 2 } } { D ^ { 2 } } \sum \frac { r _ { \lambda } ^ { 2 } } { m _ { \lambda } } \sin ^ { 2 } \left[ \omega + \frac { 1 } { 3 } ( \theta _ { \mu } - \theta _ { \nu } ) \right] - \sum \frac { m _ { \mu } m _ { \nu } } { r _ { \lambda } } , } } \end{array}
$$

where

# $\lambda , \mu , \nu$ are a cyclic permutation of 1, 2, 3

and

by (42) and (43). Thus the order of the differential equations (46) is reduced to eight.

Van Kampen and Wintner (1937) and Murnaghan (1936) applied this transformation of Levi-Civita to the plane motion of three bodies.

Lemaitre (1952；see also Deprit and Delie, l961,l963;Deprit and Roels, l962) discovered an elegant symmetric coordinate system for the reduction of the three-body problem.Write

$$
m _ { 1 } + m _ { 2 } + m _ { 3 } , \qquad \mu m _ { 0 } ^ { 2 } = m _ { 1 } m _ { 2 } m _ { 3 } , \qquad \tan \alpha _ { i } = \frac { m _ { i } } { m _ { 0 } } \quad ( i = 1 , 2 , 3 ) ,
$$

$$
m _ { 1 } m _ { 2 } m _ { 3 } A ^ { 2 } = ( m _ { 1 } + m _ { 2 } ) ( m _ { 2 } + m _ { 3 } ) ( m _ { 3 } + m _ { 1 } ) ,
$$

$$
\begin{array} { c c } { { \chi = \frac { 1 } { 3 } ( \chi _ { 1 } + \chi _ { 2 } + \chi _ { 3 } ) , } } & { { \chi _ { 1 } = \chi + \frac { 1 } { 3 } ( \alpha _ { 3 } - \alpha _ { 2 } ) , } } \\ { { { } } } & { { { } } } \\ { { \chi _ { 2 } = \chi + \frac { 1 } { 3 } ( \alpha _ { 1 } - \alpha _ { 3 } ) , } } & { { \chi _ { 3 } = \chi + \frac { 1 } { 3 } ( \alpha _ { 2 } - \alpha _ { 1 } ) . } } \end{array}
$$

It can be shown that

$$
\sum _ { i } \sin \ b \beta \alpha _ { i } \cos \ b \beta \chi _ { i } \equiv 0 , \qquad \sum _ { i } \sin \ b \beta \alpha _ { i } \sin \ b \beta \chi _ { i } \equiv 0
$$

$$
\sum _ { i } m _ { i } \cos ^ { 2 } \alpha _ { i } \cos ^ { 2 } \chi _ { i } \equiv \frac { \mu } { A ^ { 2 } } , \quad \quad \sum _ { i } m _ { i } \cos ^ { 2 } \alpha _ { i } \sin ^ { 2 } \chi _ { i } \equiv \frac { \mu } { A ^ { 2 } } ,
$$

$$
\sum _ { i } m _ { i } \cos ^ { 2 } \alpha _ { i } \cos \chi _ { i } \sin \chi _ { i } \equiv 0 .
$$

In the plane of the three bodies,Lemaitre introduced

xi $= A r \cos \alpha _ { i } \cos \sigma \cos \chi _ { i } , ~ y _ { i } = A r \cos \alpha _ { i } \sin \sigma \sin \chi _ { i } ~ ( i = 1 , 2 , \frac { \pi } { 4 } )$ 3），so that

$$
\sum _ { i } m _ { i } x _ { i } = 0 , \sum _ { i } m _ { i } y _ { i } = 0 , \sum _ { i } m _ { i } x _ { i } y _ { i } = 0 ;
$$

that is, the xyz coordinate axes coincide with the principal axes of inertia. Let the moments of inertia be

$$
I _ { x x } = \sum _ { i } m _ { i } x _ { i } ^ { 2 } = \mu r ^ { 2 } \cos ^ { 2 } \sigma , \qquad I _ { y y } = \sum _ { i } m _ { i } y _ { i } ^ { 2 } = \mu r ^ { 2 } \sin ^ { 2 } \sigma ,
$$

$$
J = I _ { x x } + I _ { y y } = \mu r ^ { 2 } ,
$$

where $\boldsymbol { r }$ denotes the radius of gyration with respect to the center of mass of the three masses in the plane of the triangle. The area of the triangle $m _ { 1 } m _ { 2 } m _ { 3 }$ is

$$
\Delta = \frac { 1 } { 4 } \frac { J } { m _ { 0 } ^ { 2 } } \sin 2 \sigma ,
$$

which is a symmetric function of the three masses. Denote the angle of the vector $m _ { j } m _ { k }$ with the $_ x$ axis by $\phi _ { i }$ $_ i \ ( i , j , k = 1 , 2 , 3 )$ ; then

$$
\tan \phi _ { i } = - \tan \sigma \cot \chi _ { i } \qquad ( i = 1 , 2 , 3 ) .
$$

The angle $x$ gives the position of the triangle with respect to the $_ { x }$ axis in the plane of the three bodies.

Let $\rho _ { i }$ be the distance of $m _ { j }$ and $m _ { k }$ and $\theta _ { i }$ be the angle between the vectors $m _ { i } m _ { k }$ and $m _ { i } m _ { j } .$ Then

$$
\begin{array} { c } { \displaystyle { \rho _ { i } = \frac { 1 } { \sqrt { 2 } } A r \sin \alpha _ { i } \sqrt { 1 - \cos { 2 \sigma } \cos { 2 \chi _ { i } } } , } } \\ { \displaystyle { \sin \theta _ { i } = \frac { \sin \alpha _ { i } \sin { 2 \sigma } } { \sqrt { 1 - \cos { 2 \sigma } \cos { 2 \chi _ { i } } } \sqrt { 1 - \cos { 2 \sigma } \cos { 2 \chi _ { k } } } } , } } \\ { \displaystyle { \cos { \theta _ { i } } = - \frac { \cos { \alpha _ { i } } + \cos { 2 \sigma } \cos { ( \chi _ { i } + \chi _ { k } ) } } { \sqrt { 1 - \cos { 2 \sigma } \cos { 2 \chi _ { i } } } \sqrt { 1 - \cos { 2 \sigma } \cos { 2 \chi _ { k } } } } . } } \end{array}
$$

Refer the moving $x , y$ ， $_ z$ axes to the $X , ~ Y , ~ Z$ axes fixed in space by means of Euler's angles $\theta , \phi , \psi$ ; there the angular velocity components are

$$
\begin{array} { r l r l } & { \omega _ { x } = \dot { \theta } \cos \phi + \dot { \psi } \sin \theta \sin \phi , } & & { \omega _ { y } = - \dot { \theta } \sin \phi + \dot { \psi } \sin \theta \cos \phi , } \\ & { \omega _ { z } = \dot { \phi } + \dot { \psi } \cos \theta . } \end{array}
$$

Then,

$$
\begin{array} { c } { T = T _ { r } + \omega _ { z } C _ { r } + \frac { 1 } { 2 } ( I _ { y y } \omega _ { x } ^ { 2 } + I _ { x x } \omega _ { y } ^ { 2 } + J \omega _ { z } ^ { 2 } ) , } \\ { U = k ^ { 2 } \Big ( \frac { m _ { 2 } m _ { 3 } } { \rho _ { 1 } } + \frac { m _ { 3 } m _ { 1 } } { \rho _ { 2 } } + \frac { m _ { 1 } m _ { 2 } } { \rho _ { 3 } } \Big ) } \\ { \displaystyle = \frac { k ^ { 2 } \sqrt { 2 } } { { \cal A } r } \sum _ { i } \frac { m _ { i } m _ { k } } { \sin { \alpha _ { i } } \sqrt { 1 - \cos { 2 \sigma } \cos { 2 \chi _ { i } } } } , } \end{array}
$$

where

$$
\begin{array} { l } { { \displaystyle { \cal T } _ { r } = \frac { 1 } { 2 } \sum _ { i } m _ { i } ( \dot { x } _ { i } ^ { 2 } + \dot { y } _ { i } ^ { 2 } ) = \frac { 1 } { 2 } \mu ( \dot { r } ^ { 2 } + r ^ { 2 } \dot { \sigma } ^ { 2 } + r ^ { 2 } \dot { \chi } ^ { 2 } ) , } } \\ { { \displaystyle } } \\ { { \displaystyle C _ { r } = \sum _ { i } m _ { i } ( x _ { i } \dot { y } _ { i } - \dot { x } _ { i } y _ { i } ) = \mu r ^ { 2 } \dot { \chi } \sin 2 \sigma . } } \end{array}
$$

Since ${ \cal L } = T + U _ { : }$

$$
\begin{array} { l } { \displaystyle { \dot { \mathcal { P } } _ { \theta } = \frac { \partial L } { \partial \dot { \theta } } = \frac { \partial T } { \partial \omega _ { x } } \cos \phi - \frac { \partial T } { \partial \omega _ { y } } \sin \phi \mathrm { , } } } \\ { \displaystyle { \dot { \mathcal { P } } _ { \phi } = \frac { \partial L } { \partial \dot { \phi } } = \frac { \partial T } { \partial \omega _ { z } } \mathrm { , } } } \\ { \displaystyle { \dot { \mathcal { P } } _ { \psi } = \frac { \partial L } { \partial \dot { \psi } } = \frac { \partial T } { \partial \omega _ { x } } \sin \theta \sin \phi + \frac { \partial T } { \partial \omega _ { y } } \sin \theta \cos \phi + \frac { \partial T } { \partial \omega _ { z } } \cos \theta \mathrm { . } } } \end{array}
$$

We compute the Routh function

$$
\begin{array} { r l } & { R = \dot { \theta } \dot { p } _ { \theta } + \dot { \phi } p _ { \phi } + \dot { \psi } p _ { \psi } - L } \\ & { \quad = \frac { 1 } { 2 } ( I _ { y y } \omega _ { x } ^ { 2 } + I _ { x x } \omega _ { y } ^ { 2 } + J \omega _ { z } ^ { 2 } ) - T _ { \tau } - U } \\ & { \quad = \frac { 1 } { 2 I _ { y y } } [ b _ { \theta } \cos \phi + ( \dot { p } _ { \psi } - p _ { \phi } \cos \theta ) \cos \epsilon \theta \sin \phi ] ^ { 2 } } \\ & { \quad \quad + \frac { 1 } { 2 I _ { x x } } [ - \dot { p } _ { \theta } \sin \phi + ( \dot { p } _ { \psi } - \dot { p } _ { \phi } \cos \theta ) \cos \epsilon \cos \theta \cos \phi ] ^ { 2 } } \\ & { \quad \quad \quad + \frac { 1 } { 2 J } ( \dot { p } _ { \phi } - C _ { \tau } ) ^ { 2 } - T _ { \tau } - U . } \end{array}
$$

Denote the magnitude of the angular momentum vector $c$ by $\kappa$ ; then we have integrals

$$
{ \dot { p } } _ { \theta } = 0 , \qquad { \dot { p } } _ { \phi } = K \cos \theta , \qquad { \dot { p } } _ { \psi } = K .
$$

The third equation shows that $\psi$ is an ignorable coordinate. The first integral shows that $\theta$ can be replaced by its value ${ \cos ^ { - 1 } \left( \pmb { \rho } _ { \phi } / \pmb { K } \right) }$ in $R$ Thus,

$$
\begin{array} { l } { { \displaystyle R = \frac { 1 - \cos 2 \sigma \cos 2 \phi } { J \sin ^ { 2 } 2 \sigma } \left( K ^ { 2 } - \phi _ { \phi } ^ { 2 } \right) + \frac { 1 } { 2 } \frac { \dot { p } _ { \phi } ^ { 2 } } { J } } } \\ { { \displaystyle \qquad - \dot { \chi } \phi _ { \phi } \sin 2 \sigma - \frac { 1 } { 2 } \mu \big ( \dot { r } ^ { 2 } + r ^ { 2 } \dot { \sigma } ^ { 2 } + r ^ { 2 } \dot { \chi } ^ { 2 } \cos ^ { 2 } 2 \sigma \big ) - U . } } \end{array}
$$

The equations of motion are then reduced to those of the eighth order

$$
\dot { \phi } = { \frac { \partial R } { \partial \dot { p } _ { \phi } } } , \qquad \dot { p } _ { \phi } = - { \frac { \partial R } { \partial \phi } } ,
$$

$$
\frac { d } { d t } \frac { \partial R } { \partial \dot { r } } - \frac { \partial R } { \partial r } = 0 , \frac { d } { d t } \frac { \partial R } { \partial \dot { \sigma } } - \frac { \partial R } { \partial \sigma } = 0 ,
$$

$$
\frac { d } { d t } \frac { \partial R } { \partial \dot { x } } - \frac { \partial R } { \partial \chi } = 0 .
$$

We have the energy integral with a constant $\pmb { h }$ .The third equation for $\pmb { r }$ gives

$$
\ddot { r } + \frac { \dot { r } ^ { 2 } } { r } - \frac { 1 } { \mu r } \left( U + 2 h \right) = 0 ,
$$

from which we obtain the relation due to Lagrange

$$
\ddot { J } = 2 U + 4 h .
$$

The Hamiltonian function is

$$
\begin{array} { l } { { H = \dot { r } \dot { p } _ { r } + \dot { \sigma } \dot { p } _ { \sigma } + \dot { \chi } \dot { p } _ { x } + R } } \\ { { \mathrm { ~ } = \displaystyle \frac { 1 } { 2 \mu } \dot { p } _ { r } ^ { 2 } + \frac { 1 } { 2 J } \left( \dot { p } _ { \sigma } ^ { 2 } + \frac { 1 } { \cos ^ { 2 } 2 \sigma } \dot { p } _ { x } ^ { 2 } - 2 \frac { \sin 2 \sigma } { \cos ^ { 2 } 2 \sigma } \dot { p } _ { x } \dot { p } _ { \phi } \right) } } \\ { { \mathrm { ~ } + \frac { 1 } { 2 J \cos ^ { 2 } 2 \sigma } \dot { p } _ { \phi } ^ { 2 } + \frac { 1 - \cos 2 \sigma \cos 2 \phi } { \sin ^ { 2 } 2 \sigma } \frac { K ^ { 2 } - \dot { p } _ { \phi } ^ { 2 } } { J } - U . } } \end{array}
$$

Put

$$
q _ { 1 } = q \cos \lambda \cos \beta , \qquad q _ { 2 } = q \sin \lambda \cos \beta , \qquad q _ { 3 } = q \sin \beta .
$$

Then

P1 $: q ^ { 2 } ( 1 - \cos ^ { 2 } \lambda \cos ^ { 2 } \beta ) , ~ \rho _ { 2 } = q ^ { 2 } ( 1 - \sin ^ { 2 } \lambda \cos ^ { 2 } \beta ) , ~ \rho _ { 3 } = q ^ { 2 } \cos ^ { 2 } \beta ,$ and the canonical variables are

$$
\begin{array} { c c c c c c } { { J } } & { { \lambda } } & { { } } & { { \beta } } & { { } } & { { \phi } } \\ { { \phi _ { J } } } & { { \varpi _ { \lambda } } } & { { } } & { { \varpi _ { \beta } } } & { { } } & { { \rho _ { \phi } , } } \end{array}
$$

where

$$
\begin{array} { r l } & { w _ { \lambda } = \dot { p } _ { \lambda } - A _ { \lambda } \dot { p } _ { \phi } , \quad \quad w _ { \beta } = \dot { p } _ { \beta } - A _ { \beta } \dot { p } _ { \phi } , } \\ & { A _ { \lambda } = \frac { 2 m _ { 0 } ^ { 3 } q _ { 1 } q _ { 2 } \sin { 2 \sigma } } { J ^ { 2 } \cos ^ { 2 } { 2 \sigma } } \Phi , \quad \quad A _ { \beta } = \frac { 2 m _ { 0 } ^ { 3 } q q _ { 3 } \sin { 2 \sigma } } { J ^ { 2 } \cos ^ { 2 } { 2 \sigma } \cos { \beta } } \Psi , } \\ & { \Phi = \frac { J } { m _ { 0 } ^ { 2 } } ( q ^ { 2 } + q _ { 3 } ^ { 2 } ) - 4 \bigg ( \frac { \rho _ { 1 } } { m _ { 1 } } + \frac { \rho _ { 2 } } { m _ { 2 } } \bigg ) q ^ { 2 } q _ { 3 } ^ { 2 } , } \\ & { \Psi = \frac { J } { m _ { 0 } ^ { 2 } } ( q _ { 3 } ^ { 2 } - q _ { 1 } ^ { 2 } ) + 4 \bigg ( \frac { \rho _ { 1 } } { m _ { 1 } } - \frac { \rho _ { 2 } } { m _ { 2 } } \bigg ) q _ { 1 } ^ { 2 } q _ { 2 } ^ { 2 } , } \end{array}
$$

with the Hamiltonian

$$
\begin{array} { l } { { H ^ { \prime } = \displaystyle \frac { 1 } { 2 } \left( \frac { \varpi _ { \lambda } ^ { 2 } } { \cos ^ { 2 } \beta } + \varpi _ { \beta } ^ { 2 } \right) + 2 m _ { 0 } ^ { 2 } q ^ { 2 } \rho _ { 1 } \rho _ { 2 } \rho _ { 3 } \bigg ( 4 \beta _ { J } ^ { 2 } + \frac { \dot { p } _ { \phi } ^ { 2 } } { J ^ { 2 } } - 2 \frac { U } { J } - 2 \frac { h } { J } \bigg ) } } \\ { { { } } } \\ { { + \frac { 4 m _ { 0 } ^ { 2 } q ^ { 2 } \rho _ { 1 } \rho _ { 2 } \rho _ { 3 } } { J ^ { 2 } } \frac { 1 - \cos { 2 \sigma } \cos { 2 \phi } } { \sin ^ { 2 } 2 \sigma } ( K ^ { 2 } - \dot { p } _ { \phi } ^ { 2 } ) . } } \end{array}
$$

The equations of motion are now

$$
\dot { r } = \frac { 1 } { \mu } \dot { p } _ { r } , \qquad \dot { x } = \frac { 1 } { J \cos ^ { 2 } 2 \sigma } \varpi _ { x } , \qquad \dot { \sigma } = \frac { 1 } { J } \dot { p } _ { \sigma } ,
$$

$$
\begin{array} { l } { { \displaystyle { \dot { p } } _ { r } = \frac { 1 } { \mu \nu ^ { 3 } } \left( { \dot { p } } _ { \sigma } ^ { 2 } + \frac { 1 } { \cos ^ { 2 } { 2 \sigma } } \varpi ^ { 2 } + { \dot { p } } _ { \sigma } ^ { 2 } \right) } } \\ { { \displaystyle ~ + \frac { 2 ( 1 - \cos { 2 \sigma } \cos { 2 \phi } ) } { \mu \nu ^ { 3 } \sin ^ { 2 } { 2 \sigma } } ( K ^ { 2 } - p _ { \sigma } ^ { 2 } ) - \frac { U } { r } , } } \\ { { \displaystyle { \dot { p } } _ { \sigma } = - \frac { 2 \sin { 2 \sigma } } { J \cos ^ { 3 } { 2 \sigma } } \varpi _ { x } ^ { 2 } + \frac { 2 ( K ^ { 2 } - p _ { \sigma } ^ { 2 } ) } { J \sin ^ { 3 } { 2 \sigma } } } } \\ { { \displaystyle ~ \times \left[ 2 \cos { 2 \sigma } ( 1 - \cos { 2 \sigma } \cos { 2 \phi } ) - \sin ^ { 2 } { 2 \sigma } \cos { 2 \phi } \right] } } \\ { { \displaystyle ~ + \frac { 2 \dot { \chi } } { \mu \nu } \rho _ { \sigma } \cos { 2 \sigma } - \frac { 3 } { 2 } k ^ { 2 } A ^ { 2 } r ^ { 2 } \sin { 2 \sigma } \sum _ { i , j , k } \frac { m _ { j } m _ { k } } { \rho _ { \sigma } ^ { 3 } } \sin ^ { 2 } { \alpha } _ { i } \cos { 2 \phi } } . }  \end{array}
$$

$$
\dot { \varpi } _ { \chi } = - 2 \dot { \sigma } \phi _ { \phi } \cos 2 \sigma - \textstyle { \frac { 1 } { 2 } } k ^ { 2 } A ^ { 2 } r ^ { 2 } \cos 2 \sigma \sum _ { i , j , k } \frac { m _ { j } m _ { k } } { \rho _ { i } ^ { 3 } } \sin ^ { 2 } \alpha _ { i } \sin 2 \chi _ { i } ,
$$

$$
\begin{array} { r } { \varpi _ { x } = \phi _ { x } - \phi _ { \phi } \sin 2 \sigma , } \end{array}
$$

where the summation is to be extended to three terms obtained by the cyclic permutation of $i , j , k$ among 1, 2, 3.

Lemaitre further introduced $\rho _ { i } = q _ { j } ^ { 2 } + q _ { k } ^ { 2 }$ with cyclic permutation of $i , j , k$ .The new coordinates $q _ { 1 } , q _ { 2 } , q _ { 3 }$ are such that: (i) $q _ { 1 } = q _ { 2 } = q _ { 3 } = 0$ represents the triple simultaneous collision of the three bodies；(ii) $q _ { i } = 0$ represents the straight line of the collinear configuration with $m _ { i }$ situated between $m _ { j }$ and $m _ { k }$ ；(ii） the plane $q _ { j } = q _ { k }$ represents the isosceles configuration with $m _ { j }$ and $m _ { k }$ as base; and (iv) $q _ { 1 } = q _ { 2 } = q _ { 3 }$ represents the equilateral configuration (see Chapter 3).

Put $q ^ { 2 } = q _ { 1 } ^ { 2 } + q _ { 2 } ^ { 2 } + q _ { 3 } ^ { 2 }$ ; then

$$
2 q ^ { 2 } = \rho _ { 1 } + \rho _ { 2 } + \rho _ { 3 } , \qquad 2 q _ { i } ^ { 2 } = \rho _ { j } + \rho _ { k } - \rho _ { i } , \qquad \Delta = q q _ { 1 } q _ { 2 } q _ { 3 } ,
$$

$$
1 + \cos \theta _ { i } = \frac { 2 q _ { j } ^ { 2 } q _ { k } ^ { 2 } } { \rho _ { j } \rho _ { k } } , \qquad 1 - \cos \theta _ { i } = \frac { 2 q ^ { 2 } q _ { i } ^ { 2 } } { \rho _ { j } \rho _ { k } } ,
$$

$$
\cos { \theta _ { j } } + \cos { \theta _ { k } } = - 2 \frac { q _ { j } ^ { 2 } q _ { k } ^ { 2 } } { \rho _ { j } \rho _ { k } } \frac { \rho _ { j } + \rho _ { k } } { \rho _ { i } } ,
$$

$$
\cos \theta _ { j } - \cos \theta _ { k } = 2 \frac { q ^ { 2 } q _ { i } ^ { 2 } } { \rho _ { j } \rho _ { k } } \frac { \rho _ { j } - \rho _ { k } } { \rho _ { i } } ,
$$

$$
\begin{array} { r } { 1 + \cos \theta _ { 1 } + \cos \theta _ { 2 } + \cos \theta _ { 3 } = - 4 \frac { q _ { 1 } ^ { 2 } q _ { 2 } ^ { 2 } q _ { 3 } ^ { 2 } } { \rho _ { 1 } \rho _ { 2 } \rho _ { 3 } } , } \end{array}
$$

$$
\tan { \frac { 1 } { 2 } } \theta _ { i } = { \frac { q q _ { i } } { \rho _ { j } \rho _ { k } } } ,
$$

with cyclic permutation of $i , j , k .$ 、This set of variables was used in the regularization of the three-body problem by Lemaitre (see Section 17.15 dealing with regularizing variables).

Following Murnaghan (1936), put

$$
J = \mu r ^ { 2 } , \qquad x = \frac { q _ { 1 } } { q + q _ { 3 } } , \qquad y = \frac { q _ { 2 } } { q + q _ { 3 } } ,
$$

or

$$
{ \mathrm {  ~ \sigma ~ } } _ { 1 } = q \frac { 2 x } { 1 + x ^ { 2 } + y ^ { 2 } } , \qquad q _ { 2 } = q \frac { 2 y } { 1 + x ^ { 2 } + y ^ { 2 } } , \qquad q _ { 3 } = q \frac { 1 - x ^ { 2 } - y } { 1 + x ^ { 2 } + y }
$$

$$
\lambda = \tan ^ { - 1 } { \frac { y } { x } } , \qquad \beta = \sin ^ { - 1 } { \frac { 1 - x ^ { 2 } - y ^ { 2 } } { 1 + x ^ { 2 } + y ^ { 2 } } } ,
$$

and

$$
w = \int ^ { t } \frac { \mu r ^ { 2 } ( \tau ) [ 1 + x ^ { 2 } ( \tau ) + y ^ { 2 } ( \tau ) ] ^ { 2 } d \tau } { 1 6 m _ { 0 } ^ { 2 } q ^ { 2 } ( \tau ) \rho _ { 1 } ( \tau ) \rho _ { 2 } ( \tau ) \rho _ { 3 } ( \tau ) } .
$$

Then the canonical equations with the independent variable $\boldsymbol { w }$ are

$$
\begin{array} { r l r l r l r l } { r } & { { } } & { \pmb { x } } & { { } } & { \pmb { y } } & { { } } & { \pmb { \phi } } \\ { \pmb { \phi _ { r } } } & { { } } & { \pmb { \varpi _ { x } } } & { { } } & { \pmb { \varpi _ { y } } } & { { } } & { \pmb { \phi _ { \phi } } , } \end{array}
$$

where

$$
\begin{array} { r l } & { w _ { x } = \displaystyle { \hat { p } _ { x } - \hat { p } _ { \phi } A _ { x } } , \quad \quad \varpi _ { y } = \displaystyle { \hat { p } _ { y } - \hat { p } _ { \phi } A _ { y } } , } \\ & { \quad } \\ & { \quad \quad \quad A _ { x } = \frac { 2 m _ { 0 } ^ { 3 } q ^ { 2 } x \sin { 2 \sigma } } { { \mathcal { J } ^ { 2 } } ( x ^ { 2 } + y ^ { 2 } ) ( 1 + x ^ { 2 } + y ^ { 2 } ) ^ { 2 } \cos ^ { 2 } { 2 \sigma } } } \\ & { \quad \quad \quad \quad \quad \quad \quad \quad \times [ 4 y ^ { 2 } \Phi + ( 1 - x ^ { 2 } - y ^ { 2 } ) ( 1 + x ^ { 2 } + y ^ { 2 } ) \Psi ] , } \\ & { \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad 2 m _ { 0 } ^ { 3 } q ^ { 2 } y \sin { 2 \sigma } } \\ & { \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad } \\ & { \quad \quad \quad \quad \quad \quad \quad \quad \quad \times [ 4 x ^ { 2 } \Phi - ( 1 + x ^ { 2 } + y ^ { 2 } ) ^ { 2 } \cos ^ { 2 } { 2 \sigma } } \\ & { \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \times [ 4 x ^ { 2 } \Phi - ( 1 - x ^ { 2 } - y ^ { 2 } ) ( 1 + x ^ { 2 } + y ^ { 2 } ) \Psi ] , } \end{array}
$$

with

$$
H ^ { \prime } = \frac { 1 } { 2 } ( \varpi _ { x } ^ { 2 } + \varpi _ { y } ^ { 2 } ) + \frac { 8 m _ { 0 } ^ { 2 } q ^ { 2 } \rho _ { 1 } \rho _ { 2 } \rho _ { 3 } } { J ( 1 + x ^ { 2 } + y ^ { 2 } ) ^ { 2 } } \left( \frac { 1 } { \mu } \rlap / p _ { r } ^ { 2 } + \frac { \rho _ { \phi } ^ { 2 } } { J } - 2 ( U + h ) \right)
$$

$$
+ \frac { 1 6 m _ { 0 } ^ { 2 } q ^ { 2 } \rho _ { 1 } \rho _ { 2 } \rho _ { 3 } } { ( 1 + x ^ { 2 } + y ^ { 2 } ) ^ { 2 } } \frac { 1 - \cos 2 \sigma \cos 2 \phi } { J ^ { 2 } \sin ^ { 2 } 2 \sigma } ( K ^ { 2 } - \rlap / p _ { \phi } ^ { 2 } ) .
$$

# TRANSFORMATION OF POINCARE

# 5·3TRANSFORMATION OF POINCARE

Take the differential equations of motion of the $\pmb { n }$ -body problem in the form

$$
\frac { d x _ { i } } { d t } = \frac { \partial H } { \partial y _ { i } } , \qquad \frac { d y _ { i } } { d t } = - \frac { \partial H } { \partial x _ { i } } \qquad ( i = 1 , 2 , . . . , 3 n ) .
$$

Following Poincaré (1897)， we denote the mass of the $\mu \mathrm { t h }$ body by $\displaystyle { m _ { 3 \mu - 2 } = m _ { 3 \mu - 1 } = m _ { 3 \mu } } _ { ; }$ ,its position coordinates by $x _ { 3 \mu - 2 } , x _ { 3 \mu - 1 } , x _ { 3 \mu } .$ and its momentum components by $y _ { 3 \mu - 2 } , y _ { 3 \mu - 1 } , y _ { 3 \mu }$ If $\begin{array} { r } { \sum _ { i } x _ { i } ^ { \prime } d y _ { i } ^ { \prime } - \sum _ { i } x _ { i } d y _ { i } } \end{array}$ in the transformation from ${ x _ { i } } , { y _ { i } }$ to $x _ { i } ^ { \prime } , y _ { i } ^ { \prime }$ is a perfect differential, then the equations of motion conserve their canonical form. $\operatorname { I f } x _ { i } ^ { \prime } ( \ = 1 , 2 , . . . , 3 n )$ are functions of $\boldsymbol { x } _ { j }$ $( j = 1 , 2 , . . . , 3 n )$ only,and $y _ { i } ^ { \prime }$ $( i = 1 , 2 , . . . , 3 n )$ are functions of $y _ { j }$ $( j = 1 , 2 , . . . , 3 n )$ only, then the condition is writen

$$
\sum _ { i } y _ { i } ^ { \prime } x _ { i } ^ { \prime } = \sum _ { j } y _ { j } x _ { j } .
$$

We make further assumptions:

(1) $x _ { 3 v - 2 } ^ { \prime } ( \nu = 1 , 2 , . . . , n )$ are functions of $x _ { 3 \mu - 2 } \ ( \mu = 1 , 2 , . . . , n )$ only, $y _ { 3 v - 2 } ^ { \prime } ~ ( \nu = 1 , 2 , . . . , n )$ are functions of $y _ { 3 \mu - 2 } ~ ( \mu = 1 , 2 , . . . , n )$ only, $x _ { 3 v - 1 } ^ { \prime } ~ ( \nu = 1 , 2 , . . . , n )$ are functions of $x _ { 3 \mu - 1 } \ ( \mu = 1 , 2 , . . . , n )$ only, $y _ { 3 \nu - 1 } ^ { \prime } ~ ( \nu = 1 , 2 , . . . , n )$ are functions of $y _ { 3 \mu - 1 } ~ ( \mu = 1 , 2 , . . . , n )$ only, $x _ { 3 v } ^ { \prime } \quad ( \nu = 1 , 2 , . . . , n )$ are functions of ${ \pmb x } _ { 3 { \pmb \mu } }$ $( \mu = 1 , 2 , . . . , n )$ only, y'sv $( \nu = 1 , 2 , . . . , n )$ are functions of $y _ { 3 \mu }$ $( \mu = 1 , 2 , . . . , n )$ only.

(ii) The linear homogeneous relations are the same: between   
$x _ { 3 \nu - 2 } ^ { \prime } \ ( \nu = 1 , 2 , . . . , n )$ and $x _ { 3 \mu - 2 } \ ( \mu = 1 , 2 , . . . , n ) ;$ between   
$x _ { 3 \nu - 1 } ^ { \prime } \left( \nu = 1 , 2 , . . . , n \right) \quad \mathrm { a n d } \quad x _ { 3 \mu - 1 } \left( \mu = 1 , 2 , . . . , n \right) ;$ between   
$\pmb { x } _ { 3 \nu } ^ { \prime }$ $\ L _ { \nu } \left( \nu = 1 , 2 , . . . , n \right) \mathrm { a n d } x _ { 3 \mu } \left( \mu = 1 , 2 , . . . , n \right) .$ (iii) The relations are the same: between   
$y _ { 3 \nu - 2 } ^ { \prime } ~ ( \nu = 1 , 2 , . . . , n ) \quad \mathrm { a n d } \quad y _ { 3 \mu - 2 } ~ ( \mu = 1 , 2 , . . . . , n ) ;$ between   
$y _ { 3 \nu - 1 } ^ { \prime } ~ ( \nu = 1 , 2 , . . . , n ) \quad \mathrm { a n d } \quad y _ { 3 \mu - 1 } ~ ( \mu = 1 , 2 , . . . , n ) ;$ between   
$y _ { 3 v } ^ { \prime } \quad ( \nu = 1 , 2 , . . . , n )$ and y3μ (μ, = 1,2..,n).

Assumption (ii) is a consequence of assumptions (i) and (ii） and equation (48). By assumption (i), equation (48) becomes

$$
\begin{array} { l } { { \displaystyle \sum _ { \nu } y _ { 3 \nu - 2 } ^ { \prime } x _ { 3 \nu - 2 } ^ { \prime } = \sum _ { \mu } y _ { 3 \mu - 2 } x _ { 3 \mu - 2 } , } } \\ { { \displaystyle \sum _ { \nu } y _ { 3 \nu - 1 } ^ { \prime } x _ { 3 \nu - 1 } ^ { \prime } = \sum _ { \mu } y _ { 3 \mu - 1 } x _ { 3 \mu - 1 } , } } \\ { { \displaystyle \sum _ { \nu } y _ { 3 \nu } ^ { \prime } x _ { 3 \nu } ^ { \prime } = \sum _ { \mu } y _ { 3 \mu } x _ { 3 \mu } , } } \end{array}
$$

or, by assumption (ii),

$$
\begin{array} { l } { { \displaystyle \sum _ { \nu } y _ { 3 \nu - 2 } ^ { \prime } x _ { 3 \nu - 1 } ^ { \prime } = \sum _ { \mu } y _ { 3 \mu - 2 } x _ { 3 \mu - 1 } } , } \\ { { \displaystyle \sum _ { \nu } y _ { 3 \nu - 1 } ^ { \prime } x _ { 3 \nu - 2 } ^ { \prime } = \sum _ { \mu } y _ { 3 \mu - 1 } x _ { 3 \mu - 2 } } , } \end{array}
$$

and, in general,

$$
\sum _ { v } y _ { 3 v - \kappa } ^ { \prime } x _ { 3 v - \lambda } ^ { \prime } = \sum _ { \mu } y _ { 3 \mu - \kappa } x _ { 3 \mu - \lambda } \qquad ( \kappa , \lambda = 0 , 1 , 2 ) .
$$

From these we obtain

$$
\sum _ { \nu } \left( x _ { 3 \nu } ^ { \prime } y _ { 3 \nu - 1 } ^ { \prime } - x _ { 3 \nu - 1 } ^ { \prime } y _ { 3 \nu } ^ { \prime } \right) = \sum _ { \mu } \left( x _ { 3 \mu } y _ { 3 \mu - 1 } - x _ { 3 \mu - 1 } y _ { 3 \mu } \right) ,
$$

$$
\sum _ { \nu } \left( x _ { 3 \nu - 2 } ^ { \prime } y _ { 3 \nu } ^ { \prime } - x _ { 3 \nu } ^ { \prime } y _ { 3 \nu - 2 } ^ { \prime } \right) = \sum _ { \mu } \left( x _ { 3 \mu - 2 } y _ { 3 \mu } - x _ { 3 \mu } y _ { 3 \mu - 2 } \right) ,
$$

$$
\sum _ { \nu } \left( x _ { 3 \nu - 1 } ^ { \prime } y _ { 3 \nu - 2 } ^ { \prime } - x _ { 3 \nu - 2 } ^ { \prime } y _ { 3 \nu - 1 } ^ { \prime } \right) = \sum _ { \mu } \left( x _ { 3 \mu - 1 } y _ { 3 \mu - 2 } - x _ { 3 \mu - 2 } y _ { 3 \mu - 1 } \right) .
$$

Hence, the form of each angular-momentum integral remains unaltered by our transformation.

As our transformation satisfies the conditions of equation (48), we write equations (47) in the form

$$
\frac { d x _ { i } ^ { \prime } } { d t } = \frac { \partial H } { \partial y _ { i } ^ { \prime } } , \qquad \frac { d y _ { i } ^ { \prime } } { d t } = - \frac { \partial H } { \partial x _ { i } ^ { \prime } } \qquad ( i = 1 , 2 , . . . , 3 n ) .
$$

We further assume that

$$
k y _ { 3 n - 2 } ^ { \prime } = \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu - 2 } ,
$$

where $k$ is constant, and that $x _ { 3 \mu - 2 } ~ ( \mu = 1 , 2 , . . . , n )$ , depend only on $x _ { 3 \mu - 2 } - x _ { 3 n - 2 }$ $( \mu = 1 , 2 , . . . , n - 1 )$ , that is,on the coordinates relative to the nth body. The momentum $y _ { 3 n - 2 } ^ { \prime }$ is then a constant multiple of the momentum of the center of mass.We can assume that the center of mass is at rest. Hence, $\boldsymbol { H }$ does not depend on $\pmb { x } _ { 3 n - 2 } ^ { \prime }$ as

$$
\frac { d y _ { 3 n - 2 } ^ { \prime } } { d t } = - \frac { \partial H } { \partial x _ { 3 n - 2 } ^ { \prime } } .
$$

Similarly, it does not depend on ${ \pmb x } _ { 3 n - 1 } ^ { \prime }$ .This happens when $x _ { 3 \nu - 2 } ^ { \prime }$ depends only on the difference $x _ { 3 \mu - 2 } - x _ { 3 n - 2 }$ in the relation between the old and the new variables. Actually, $T$ depends on $y ^ { \prime }$ and not on $x ^ { \prime }$ ， since it depends on $y$ and not on x. $U$ depends on $x _ { 3 \nu - 2 } ^ { \prime } , x _ { 3 \nu - 1 } ^ { \prime } , x _ { 3 \nu } ^ { \prime }$ and not on $x _ { 3 n - 2 } ^ { \prime } , x _ { 3 n - 1 } ^ { \prime } , x _ { 3 n } ^ { \prime }$ ,since it depends only on $x _ { 3 \mu - 2 } - x _ { 3 n - 2 } , x _ { 3 \mu - 1 } - x _ { 3 n - 1 }$ ${ x _ { 3 \mu } } \mathrm { ~ - ~ } { x _ { 3 n } }$ Hence, $H$ does not contain ${ x } _ { 3 n - 2 } ^ { \prime }$ ， $x _ { 3 n - 1 } ^ { \prime } , x _ { 3 n } ^ { \prime }$

We can prove that the assumption (51） is not contradictory. If $\begin{array} { r } { k y _ { 3 n - 2 } ^ { \prime } = \sum _ { \nu } y _ { 3 \nu - 2 ; } } \end{array}$ ，then $x _ { 3 y - 2 } ^ { \prime }$ can be shown to depend on $x _ { 3 v - 2 } - x _ { 3 n - 2 } ,$ because,if $x _ { 3 \mu - 2 }$ are all equal to $x _ { 3 n - 2 }$ in $\begin{array} { r } { \sum _ { \nu } x _ { 3 \nu - 2 } ^ { \prime } y _ { 3 \nu - 2 } ^ { \prime } = \sum _ { \mu } x _ { 3 \mu - 2 } y _ { 3 \mu - 2 } , } \end{array}$ then the right-hand side is proportional to $\sum { } _ { \nu } y _ { 3 \nu - 2 }$ that is,to $y _ { 3 n - 2 } ^ { \prime }$ Hence, $y _ { 3 \nu - 2 } ^ { \prime }$ on the left-hand side must be zero, and $x _ { 3 \mu - 2 } ^ { \prime }$ must vanish with $x _ { 3 \mu - 2 } - x _ { 3 n - 2 } .$ Thus $x _ { 3 y - 2 } ^ { \prime }$ is a linear expression of these differences.

It is seen that $H$ does not depend on $x _ { 3 n - 2 } ^ { \prime } , x _ { 3 n - 1 } ^ { \prime } , x _ { 3 n } ^ { \prime }$ . Hence we can suppose that $y _ { 3 n - 2 } ^ { \prime } = y _ { 3 n - 1 } ^ { \prime } = y _ { 3 n } ^ { \prime } = 0$ ,and the order of the differential equations (47) is reduced by six.

# 5·4ELIMINATION OF THE NODES

We consider fictitious bodies according to Radau in the following manner (Bertrand,l852).The coordinates and the momenta of the first fictitious body are $x _ { 1 } ^ { \prime } , x _ { 2 } ^ { \prime } , x _ { 3 } ^ { \prime } , y _ { 1 } ^ { \prime } , y _ { 2 } ^ { \prime } , y _ { 3 } ^ { \prime }$ ；its instantaneous orbital plane, that is, the plane containing the origin, the first fictitious body,and the momentum vector, is perpendicular to the vector

$$
{ \mathfrak { v } } _ { 1 } = \big ( x _ { 2 } ^ { \prime } y _ { 3 } ^ { \prime } - x _ { 3 } ^ { \prime } y _ { 2 } ^ { \prime } , x _ { 3 } ^ { \prime } y _ { 1 } ^ { \prime } - x _ { 1 } ^ { \prime } y _ { 3 } ^ { \prime } , x _ { 1 } ^ { \prime } y _ { 2 } ^ { \prime } - x _ { 2 } ^ { \prime } y _ { 1 } ^ { \prime } \big ) .
$$

The position coordinates and the momenta of the second fictitious body are $x _ { 4 } ^ { \prime } , x _ { 5 } ^ { \prime } , x _ { 6 } ^ { \prime } , y _ { 4 } ^ { \prime } , y _ { 5 } ^ { \prime } , y _ { 6 } ^ { \prime }$ ,and its instantaneous orbital plane is perpendicular to the vector $\mathfrak { v } _ { 2 } = ( x _ { 5 } ^ { \prime } y _ { 6 } ^ { \prime } - x _ { 6 } ^ { \prime } y _ { 5 } ^ { \prime } , x _ { 6 } ^ { \prime } y _ { 4 } ^ { \prime } - x _ { 4 } ^ { \prime } y _ { 6 } ^ { \prime } , x _ { 4 } ^ { \prime } y _ { 5 } ^ { \prime } - x _ { 5 } ^ { \prime } y _ { 4 } ^ { \prime } )$ . In general, we define the vector

$$
\mathfrak { d } _ { \mu } = \bigl ( x _ { 3 \mu - 1 } ^ { \prime } y _ { 3 \mu } ^ { \prime } - x _ { 3 \mu } ^ { \prime } y _ { 3 \mu - 1 } ^ { \prime } , x _ { 3 \mu } ^ { \prime } y _ { 3 \mu - 2 } ^ { \prime } - x _ { 3 \mu - 2 } ^ { \prime } y _ { 3 \mu } ^ { \prime } , x _ { 3 \mu - 2 } ^ { \prime } y _ { 3 \mu - 1 } ^ { \prime } - x _ { 3 \mu - 1 } ^ { \prime } y _ { 3 \mu } ^ { \prime } \bigr )
$$

The sum of vector $\begin{array} { r } { \sum _ { \mu = 2 } ^ { n - 1 } \mathfrak { v } _ { \mu } = \mathfrak { V } _ { 1 } } \end{array}$ and vector ${ \mathfrak { p } } _ { 1 }$ , has the components

$$
\sum _ { \mu = 1 } ^ { n } ( x _ { 3 \mu - 1 } y _ { 3 \mu } - x _ { 3 \mu } y _ { 3 \mu - 1 } ) , \qquad \sum _ { \mu = 1 } ^ { n } ( x _ { 3 \mu } y _ { 3 \mu - 2 } - x _ { 3 \mu - 2 } y _ { 3 \mu } ) ,
$$

$$
\sum _ { \mu = 1 } ^ { n } ( x _ { 3 \mu - 2 } y _ { 3 \mu - 1 } - x _ { 3 \mu - 1 } y _ { 3 \mu - 2 } ) ,
$$

since $y _ { 3 n - 2 } ^ { \prime } = y _ { 3 n - 1 } ^ { \prime } = y _ { 3 n } ^ { \prime } = 0$ ，and this vector sum equals the total angular momentum vector $\mathfrak { V }$ of the $\pmb { n }$ -body system.As the total angular momentum vector is perpendicular to the invariable plane of the $\pmb { n }$ -body system，the vector $\mathfrak { v } _ { 1 } + \mathfrak { V } _ { 1 }$ is perpendicular to the invariable plane. Consequently $\mathfrak { d } _ { 1 } , \ \mathfrak { V } _ { 1 }$ and $\mathfrak { B }$ are all in the same plane. Hence,the intersection of the two planes perpendicular respectively to the vectors ${ \mathfrak { v } } _ { 1 }$ and ${ \mathfrak { V } } _ { 1 }$ is parallel to the invariable plane.In other words, the intersection of the instantaneous orbital plane of an arbitrary one of the $n - 1$ fictitious bodies and the plane perpendicular to the vector repre-senting the resultant angular momentum of the remaining $n - 2$ fictitious bodies is parallel to the invariable plane of the original $\pmb { n }$ -body system. This is an extension to the $_ { n }$ -body problem of the Jacobi theorem on the elimination of the nodes in the three-body problem.

In particular,in the three-body problem ( $( n = 3 )$ ）the theorem reduces to the following: the direction of the ascending node of the instantaneous orbital plane of the first fictitious body differs by l80 degrees from the direction of the ascending node of the instantaneous orbital plane of the second fictitious body.If the triangle formed by ${ \mathfrak { p } } _ { 1 }$ ， ${ \mathfrak { V } } _ { 1 }$ ,and $\mathfrak { L }$ is acute, then the components of the vectors ${ \mathfrak { p } } _ { 1 }$ and ${ \mathfrak { V } } _ { 1 }$ in the direction of $\mathfrak { L }$ are of the same direction； that is,the two fictitious bodies revolve either both in the direct sense or both in the retrograde sense.But as the components in the direction perpendicular to $\mathfrak { B }$ are of opposite direction, the inclinations of the instantaneous orbital planes of the two fictitious bodies relative to the invariable plane (which is perpendicular to $\mathfrak { B }$ ） should have opposite signs.If the triangle formed of ${ \mathfrak { p } } _ { 1 } , \ { \mathfrak { V } } _ { 1 } .$ and $\mathfrak { L }$ is obtuse, then the components perpendicular to $\mathfrak { B }$ of the vectors ${ \mathfrak { p } } _ { 1 }$ and ${ \mathfrak { V } } _ { 1 }$ are of opposite directions,and one of the two fictitious bodies revolves in the direct sense and the other in the retrograde sense. In either case the ascending nodes in question should difer by l80 degrees in direction, because the intersections of the two orbital planes with the invariable plane should be parallel. This is the Jacobi theorem (Jacobi,1842,1844) on the elimination of the nodes,and it is an integral of the problem.

# 5.5TRANSFORMATION $\propto$

Poincaré has specified his transformation by further assumptions and classified the cases $\alpha , ~ \beta$ and $\gamma$ .The transformation，which was first discovered by Cauchy, is

$$
x _ { 3 \mu - 2 } ^ { \prime } = x _ { 3 \mu - 2 } - x _ { 3 n - 2 } , \qquad x _ { 3 \mu - 1 } ^ { \prime } = x _ { 3 \mu - 1 } - x _ { 3 n - 1 } , \qquad x _ { 3 \mu } ^ { \prime } = x _ { 3 \mu } - x _ { 3 \mu } .
$$

$$
x _ { 3 n - 2 } ^ { \prime } = x _ { 3 n - 2 } , \qquad x _ { 3 n - 1 } ^ { \prime } = x _ { 3 n - 1 } , \qquad x _ { 3 n } ^ { \prime } = x _ { 3 n } ,
$$

$$
y _ { i } ^ { \prime } = y _ { i } \qquad ( i = 1 , 2 , . . . , 3 n - 3 )
$$

$$
y _ { 3 n - 2 } ^ { \prime } = \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu - 2 } , \qquad y _ { 3 n - 1 } ^ { \prime } = \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu - 1 } , \qquad y _ { 3 n } ^ { \prime } = \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu } .
$$

The motion of the $\mu \mathrm { t h }$ fictitious body that we are now considering is a relative motion around the nth body, but its momentum is that due to its absolute motion, and $y _ { 3 n - 2 } ^ { \prime } , y _ { 3 n - 1 } ^ { \prime } , y _ { 3 n } ^ { \prime }$ are the components of the total momentum of the whole $\pmb { n } .$ -body system.

There is another transformation by Poincaré which is not essentially different from this:

$$
\begin{array} { c } { { x _ { 3 \mu - 2 } ^ { \prime } = x _ { 3 \mu - 2 } - x _ { 3 n - 2 } , \qquad x _ { 3 \mu - 1 } ^ { \prime } = x _ { 3 \mu - 1 } - x _ { 3 n - 1 } , } } \\ { { x _ { 3 \mu } ^ { \prime } = x _ { 3 \mu } - x _ { 3 n } \qquad ( \mu = 1 , 2 , . . . , n - 1 ) , } } \end{array}
$$

$$
x _ { 3 n - 2 } ^ { \prime } = { \frac { \displaystyle \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu - 2 } x _ { 3 \mu - 2 } } { \displaystyle \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu - 2 } } } , \qquad x _ { 3 n - 1 } ^ { \prime } = { \frac { \displaystyle \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu - 1 } x _ { 3 \mu - 1 } } { \displaystyle \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu - 1 } } } ,
$$

$$
x _ { 3 n } ^ { \prime } = \frac { \displaystyle \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu } x _ { 3 \mu } } { \displaystyle \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu } } ,
$$

$$
y _ { 3 \mu - 2 } ^ { \prime } = y _ { 3 \mu - 2 } - { \frac { m _ { 3 \mu - 2 } y _ { 3 n - 2 } ^ { \prime } } { \underset { \mu = 1 } { \overset { n } { \sum } } } } , \qquad y _ { 3 \mu - 1 } ^ { \prime } = y _ { 3 \mu - 1 } - { \frac { m _ { 3 \mu - 1 } y _ { 3 n - 1 } ^ { \prime } } { \underset { \mu = 1 } { \overset { n } { \sum } } } } ,
$$

$$
y _ { 3 \mu } ^ { \prime } = y _ { 3 \mu } - \frac { m _ { 3 \mu } y _ { 3 n } ^ { \prime } } { \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu } } ,
$$

$$
y _ { 3 n - 2 } ^ { \prime } = \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu - 2 } , \qquad y _ { 3 n - 1 } ^ { \prime } = \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu - 1 } , \qquad y _ { 3 n } ^ { \prime } = \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu }
$$

As we may suppose that the center of mass is at rest, we put

$$
\sum _ { \mu = 1 } ^ { n } y _ { 3 \mu - 2 } = 0 .
$$

Then $x _ { 3 \mu - 2 } ^ { \prime } , y _ { 3 \mu - 2 } ^ { \prime } , y _ { 3 n - 2 } ^ { \prime }$ have the same values as in (52). Only ${ \pmb x } _ { 3 n - 2 } ^ { \prime }$ is different. The Hamiltonian $\pmb { H }$ depends on $x _ { 3 \mu - 2 } ^ { \prime } = x _ { 3 \mu - 2 } - x _ { 3 n - 2 }$ but does not depend on ${ \pmb x } _ { 3 n - 2 } ^ { \prime }$

In order to reduce the order of the differential equations (47)

$$
\frac { d x _ { i } } { d t } = \frac { \partial H } { \partial y _ { i } } , \frac { d y _ { i } } { d t } = - \frac { \partial H } { \partial x _ { i } } ~ ( i = 1 , 2 , . . . , 3 n ) ,
$$

in conjunction with

$$
H = \sum _ { i = 1 } ^ { 3 n } { \frac { y _ { i } ^ { 2 } } { 2 m _ { i } } } - \sum _ { \mu , v } ^ { n } { \frac { m _ { 3 \mu } m _ { 3 v } } { r _ { 3 \mu , 3 v } } } ,
$$

from $6 n$ to $6 n - 6$ ，we make the contact transformation

$$
x _ { i } = \frac { \partial W } { \partial y _ { i } } , \qquad y _ { i } ^ { \prime } = \frac { \partial W } { \partial x _ { i } ^ { \prime } } , \qquad ( i = 1 , 2 , . . . , 3 n ) ,
$$

$$
W = \Big ( \sum _ { i = 1 } ^ { 3 ^ { n - 3 } } x _ { i } ^ { \prime } y _ { i } \Big ) \ + \ x _ { 3 n - 2 } ^ { \prime } \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu - 2 } \ + \ x _ { 3 n - 1 } ^ { \prime } \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu - 1 } \ + \ x _ { 3 n } ^ { \prime } \sum _ { \mu = 1 } ^ { n } y _ { 3 \mu }
$$

Then the transformed equations are

$$
\frac { d x _ { i } ^ { \prime } } { d t } = \frac { \partial H } { \partial y _ { i } ^ { \prime } } , \frac { d y _ { i } ^ { \prime } } { d t } = - \frac { \partial H } { \partial x _ { i } ^ { \prime } } ( i = 1 , 2 , . . . , 3 n - 3 ) ,
$$

$$
\begin{array} { r } { I = \displaystyle \sum _ { i = 1 } ^ { 3 n - 3 } \frac { y _ { i } ^ { \prime 2 } } { 2 m _ { i } } + \frac { 1 } { 2 m _ { 3 n } } [ ( y _ { 3 n - 2 } ^ { \prime } - \sum _ { \mu = 1 } ^ { n - 1 } y _ { 3 \mu - 2 } ^ { \prime } ) ^ { 2 } + ( y _ { 3 n - 1 } ^ { \prime } - \sum _ { \mu = 1 } ^ { n - 1 } y _ { 3 \mu - 1 } ^ { \prime }  } \\ {  +  ( y _ { 3 n } ^ { \prime } - \sum _ { \mu = 1 } ^ { n - 1 } y _ { 3 \mu } ^ { \prime } ) ^ { 2 } ] - \sum _ { \mu , \nu = 1 } ^ { n } \frac { m _ { 3 \mu } m _ { 3 \mu } } { r _ { 3 \mu , 3 } } } \end{array}
$$

where $x _ { i }$ in $\boldsymbol { r } _ { 3 \mu , 3 \nu }$ are rewritten in terms of the new variables. As $\boldsymbol { H }$ is independent of ${ x } _ { 3 n - 2 } ^ { \prime }$ ， $x _ { 3 n - 1 } ^ { \prime } , \ x _ { 3 n } ^ { \prime }$ ， we obtain the integrals of the total angular momentum $y _ { 3 n - 2 } ^ { \prime } =$ constant, $y _ { 3 n - 1 } ^ { \prime } =$ constant, $y _ { 3 n } ^ { \prime } =$ constant.

We set these constants equal to zero by considering the motion relative to the center of mass; that is, we remove the primes:

$$
\begin{array} { r l } {  { : \frac { \partial H } { \partial y _ { i } } , \quad \frac { \partial y _ { i } } { \partial t } = - \frac { \partial H } { \partial x _ { i } } } \quad } & { ( i = 1 , 2 , \ldots , 3 n - 3 ) , } \\ & { \stackrel { \mathrm { \scriptsize ~ 3 n - 3 ~ } } { = } \frac { y _ { i } ^ { 2 } } { i - 1 } \frac { \partial ^ { 2 } } { 2 m _ { i } } + \frac { 1 } { 2 m _ { 3 n } } \biggl [ \biggl ( \displaystyle \sum _ { \mu = 1 } ^ { n - 1 } y _ { 3 \mu - 2 } \biggr ) ^ { 2 } + \biggl ( \displaystyle \sum _ { \mu = 1 } ^ { n - 1 } y _ { 3 \mu - 1 } \biggr ) ^ { 2 } + \biggl ( \displaystyle \sum _ { \mu = 1 } ^ { n - 1 } y _ { 3 \mu } \biggr ) ^ { 2 } \biggr ] } \\ & { - \underbrace { \sum } _ { \mu , \nu = 1 } ^ { n } \frac { m _ { 3 \mu } m _ { 3 \nu } } { r _ { 3 \mu , 3 \nu } } , } \\ & { r _ { 3 \mu , 3 \nu } ^ { 2 } = ( x _ { 3 \mu - 2 } - x _ { 3 \nu - 2 } ) ^ { 2 } + ( x _ { 3 \mu - 1 } - x _ { 3 \nu - 1 } ) ^ { 2 } + ( x _ { 3 \mu } - x _ { 3 \nu } ) ^ { 2 } } \\ & { \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad \quad } \\ & { r _ { 3 \mu , 3 \nu } ^ { 2 } = x _ { 3 \mu - 2 } ^ { 2 } + x _ { 3 \mu - 1 } ^ { 2 } + x _ { 3 \nu + 1 } ^ { 2 } . } \end{array}
$$

# 5.6TRANSFORMATION $\beta$

Put

$$
y _ { i } ^ { \prime } = m _ { i } ^ { \prime } { \frac { d x _ { i } ^ { \prime } } { d t } } , \qquad m _ { 3 \mu - 2 } ^ { \prime } = m _ { 3 \mu - 1 } ^ { \prime } = m _ { 3 \mu } ^ { \prime } \qquad ( \mu = 1 , 2 , . . . , n ) ;
$$

then the relation between $y _ { i } ^ { \prime }$ and $y _ { i }$ is the same as the relation between $m _ { i } ^ { \prime } x _ { i } ^ { \prime }$ and $m _ { i } x _ { i }$ ,and (48) takes the form

$$
\sum _ { i } m _ { i } ^ { \prime } x _ { i } ^ { \prime 2 } = \sum _ { i } m _ { i } x _ { i } ^ { 2 } .
$$

This is nothing but the kinetic energy in terms of the new variables

$$
T = \sum _ { i } { \frac { m _ { i } ^ { \prime } } { 2 } } \left( { \frac { d x _ { i } ^ { \prime } } { d t } } \right) ^ { 2 } = \sum _ { i } { \frac { 1 } { 2 m _ { i } ^ { \prime } } } y _ { i } ^ { \prime 2 } .
$$

Hence, the expressons not only for the total momentum but also for the kinetic energy are invariant by this transformation $\beta$ ，Moreover, the canonical form of the differential equations is conserved.

We propose to obtain such a transformation. Consider the transformation

$$
x _ { 3 \mu - 2 } = \sum _ { k = 1 } ^ { n } c _ { \mu \kappa } x _ { 3 \kappa - 2 } ^ { \prime } , x _ { 3 \mu - 1 } = \sum _ { \kappa = 1 } ^ { n } c _ { \mu \kappa } x _ { 3 \kappa - 1 } ^ { \prime } , x _ { 3 \mu } = \sum _ { \kappa = 1 } ^ { n } c _ { \mu \kappa } x _ { 3 \kappa } ^ { \prime } .
$$

There are $n ^ { 2 }$ coefficients ${ \pmb { c } } _ { { \pmb { \mu } } { \pmb { \kappa } } }$ .Condition (48) becomes

$$
\sum _ { \mu } m _ { 3 \mu - 2 } ^ { \prime } x _ { 3 \mu - 2 } ^ { \prime 2 } = \sum _ { \mu } m _ { 3 \mu - 2 } x _ { 3 \mu - 2 } ^ { 2 } .
$$

Substituting (59) in this expression and equating the coefficients of $x _ { 3 \rho - 2 } ^ { \prime } x _ { 3 \sigma - 2 } ^ { \prime }$ to zero, we obtain $n ( n - 1 )$ relations

$$
\sum _ { \mu } m _ { 3 \mu - 2 } c _ { \mu \rho } c _ { \mu \sigma } = 0 \qquad ( \rho , \sigma = 1 , 2 , . . . , n ) .
$$

From the coefficients of ${ \pmb x } _ { 3 \kappa - 2 } ^ { \prime }$ we obtain $\pmb { n }$ relations

$$
\sum _ { \mu } m _ { 3 \mu - 2 } c _ { \mu \kappa } ^ { 2 } = m _ { 3 \kappa - 2 } ^ { \prime } \qquad ( \kappa = 1 , 2 , . . . , n ) .
$$

Altogether we have $\scriptstyle { \pmb { n } } ^ { 2 }$ relations among the coefcients ${ c } _ { \mu \kappa }$

Following Jacobi, take the coordinates

$$
x _ { 3 \kappa - 2 } ^ { \prime } , x _ { 3 \kappa - 1 } ^ { \prime } , x _ { 3 \kappa } ^ { \prime } \qquad ( \kappa = 1 , 2 , . . . , n - 1 ) ,
$$

so that the $( \kappa + 1 ) \operatorname { t h }$ body is referred to the center of masss of the first $\kappa$ bodies, that is,

$$
x _ { 3 \kappa - 2 } ^ { \prime } = x _ { 3 ( \kappa + 1 ) - 2 } - { \biggl ( \sum _ { \mu = 1 } ^ { \kappa } m _ { 3 \mu - 2 } x _ { 3 \mu - 2 } \biggr ) } \bigg / { \biggl ( \sum _ { \mu = 1 } ^ { \kappa } m _ { 3 \mu - 2 } \biggr ) }
$$

$$
x _ { 3 n - 2 } ^ { \prime } = \mathrm { ~ - \biggl ( \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu - 2 } \it { x _ { 3 \mu - 2 } } \biggr ) \bigg / \biggl ( \sum _ { \mu = 1 } ^ { n } m _ { 3 \mu - 2 } \biggr ) \cdot \nonumber }
$$

To satisfy (61) and (62), put

$$
M _ { \kappa } = \sum _ { \mu = 1 } ^ { \kappa } m _ { 3 \mu - 2 } \qquad ( \kappa = 1 , 2 , . . . , n - 1 ) ;
$$

then solve (63) for ${ x _ { 3 \mu - 2 } }$

$$
x _ { 3 ( \kappa + 1 ) - 2 } = x _ { 1 } + \sum _ { \mu = 1 } ^ { \kappa - 1 } \frac { m _ { 3 \mu - 2 } x _ { 3 \mu - 2 } ^ { \prime } } { M _ { \mu } } + x _ { 3 \kappa - 2 } ^ { \prime } .
$$

Next substitute this expression for $x _ { 3 \mu - 2 }$ in the last equation of (63):

$$
\begin{array} { l } { { \displaystyle M _ { n } x _ { 3 n - 2 } ^ { \prime } = M _ { n } x _ { 1 } + \sum _ { \mu = 1 } ^ { n - 1 } m _ { 3 \mu - 2 } ( M _ { \mu } + m _ { 3 ( \mu + 1 ) - 2 } + \cdot \cdot \cdot + m _ { 3 n - 2 } ) } } \\ { { \displaystyle \qquad \times \frac { x _ { 3 \mu - 2 } ^ { \prime } } { M _ { \mu } } + m _ { 3 n - 2 } ^ { \prime } x _ { 3 n - 2 } ^ { \prime } } , } \end{array}
$$

and substitute this $\pmb { x } _ { 1 }$ in (65). Then,

$$
x _ { 3 \kappa - 2 } = x _ { 3 n - 2 } ^ { \prime } - \sum _ { \mu = \kappa + 1 } ^ { n } m _ { 3 \mu - 2 } { \frac { x _ { 3 \mu - 2 } ^ { \prime } } { M _ { \mu } } } + { \frac { M _ { \kappa - 1 } } { M _ { \kappa } } } x _ { 3 \kappa - 2 } ^ { \prime } .
$$

Comparing this expression with (59), we obtain

$$
\begin{array} { l l l } { { c _ { \kappa \mu } = - { \displaystyle \frac { m _ { 3 \mu - 2 } } { M _ { \kappa } } } } } & { { } } & { { ( \mu > \kappa ) , } } \\ { { } } & { { } } & { { } } \\ { { c _ { \kappa \mu } = 0 } } & { { } } & { { ( \mu < \kappa ) , } } \\ { { } } & { { } } & { { c _ { \kappa \mu } = { \displaystyle \frac { M _ { \kappa - 1 } } { M _ { \kappa } } } } } & { { } } & { { ( \mu = \kappa ) . } } \end{array}
$$

Equation (61) is satisfied by these coefficients.Put (67) in (62)； then the right-hand member becomes

$$
\frac { M _ { \kappa - 1 } } { M _ { \kappa } } m _ { 3 \kappa - 2 }
$$

and we have

$$
m _ { 3 \kappa - 2 } ^ { \prime } = m _ { 3 \kappa - 2 } \frac { M _ { \kappa - 1 } } { M _ { \kappa } } \qquad ( \kappa = 1 , 2 , . . . , n ) .
$$

Condition (48) is satisfied if we determine $y _ { 3 \kappa - 2 } ^ { \prime }$ by (61) and (63). This is an extension of Poincaré's transformation $\beta$ ; the $y _ { 3 \kappa - 2 } ^ { \prime }$ are called the relative canonical coordinates of Jacobi.

The expression for the force function $U$ is rather complicated when (66) is used. Taking the center of masses as the origin, we have

$$
x _ { 3 n - 2 } ^ { \prime } = x _ { 3 n - 1 } ^ { \prime } = x _ { 3 n } ^ { \prime } = 0 .
$$

The center of mass has zero momentum; that is, it is at rest:

$$
y _ { 3 n - 2 } ^ { \prime } = y _ { 3 n - 1 } ^ { \prime } = y _ { 3 n } ^ { \prime } = 0 .
$$

By this transformation, the order of differential equations (47) is reduced from $6 n$ to $6 n - 6$

$$
y _ { i } = { \frac { \partial W } { \partial x _ { i } } } , \qquad x _ { i } ^ { \prime } = { \frac { \partial W } { \partial y _ { i } ^ { \prime } } } \qquad ( i = 1 , 2 , . . . , 3 n ) ,
$$

$$
\begin{array} { r l } { W = \displaystyle \sum _ { x = 1 } ^ { n - 1 } \{ y _ { \mathrm { a s } = 2 } ^ { x } [ x _ { \mathrm { a } ( x + 1 ) - 2 } - ( \displaystyle \sum _ { u = 1 } ^ { X } m _ { \mathrm { a s } - 2 } x _ { \mathrm { a } ( u - 2 ) } ) \middle / ( \displaystyle \sum _ { u = 1 } ^ { X } m _ { \mathrm { a s } - 2 } ) ]  } \\ {  + y _ { \mathrm { a s } = 1 } ^ { y } [ x _ { \mathrm { 3 } ( x + 1 ) - 1 } - ( \displaystyle \sum _ { u = 1 } ^ { X } m _ { \mathrm { a s } - 1 } x _ { \mathrm { a } ( u - 1 ) - 1 } ) \middle / ( \displaystyle \sum _ { u = 1 } ^ { X } m _ { \mathrm { a s } - 1 } ) ]  } \\ {  + y _ { \mathrm { a s } } ^ { r } [ x _ { \mathrm { a } ( x + 1 ) - } ( \displaystyle \sum _ { u = 1 } ^ { X } m _ { \mathrm { a s } } x _ { \mathrm { a } u } ) \middle / ( \displaystyle \sum _ { u = 1 } ^ { X } m _ { \mathrm { a s } } ) ] \} } \\ {  + y _ { \mathrm { a s } - 2 } ^ { r } ( \displaystyle \sum _ { u = 1 } ^ { B } m _ { \mathrm { a s } - 2 } x _ { \mathrm { a } u - 2 } ) + y _ { \mathrm { a s } - 1 } ^ { r } ( \displaystyle \sum _ { u = 1 } ^ { B } m _ { \mathrm { a s } - 1 } x _ { \mathrm { a } u - 2 } )  } \\ {  + y _ { \mathrm { a s } } ^ { r } ( \displaystyle \sum _ { u = 1 } ^ { B } m _ { \mathrm { a s } } x _ { \mathrm { a } u } ) . } \end{array}
$$